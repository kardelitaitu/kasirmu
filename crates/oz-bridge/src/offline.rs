//! Offline-queue command bodies (enqueue, list and sync parked transactions).
//!
//! Wave F pre-wire: this module holds the extracted command bodies for
//! `apps/desktop-client/src/commands/offline.rs`. It is inert until that lane
//! fills it.
