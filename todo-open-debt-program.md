# Open Debt Program — four workstreams, four fences

<!-- Audit stamp: 2026-09-14 · DSH · status: NEW, UNEXECUTED (one measurement run) · authoring HEAD `257ff6122` on branch `0.0.39`, working tree carrying a peer session's 6 ` D` coder-N-journal.md plus ` M platform/core/src/database/migrations.rs` (neither mine, neither touched). Every anchor below was re-read in this checkout; every count was produced by the command printed beside it. Claims CARRIED from another document rather than re-measured here are labelled `[carried]` with the document and line named — nothing in this file is asserted on the strength of another doc's say-so without that label. · One command was RUN rather than cited: `cargo test -p oz-bridge --release` → 1231 passed / 76 failed, which reproduces `todo-refactor-oz-pos-app-agents-3.md:142`'s record on both axes (see Phase 1). No other test command was executed, so no green in this file is a green this pass observed. · Scope note: this is the FIRST plan doc in this repo written as ONE file covering FOUR workstreams, which deviates from the established one-workstream-per-file pattern (`todo-refactor-<area>-agents-N.md`). The deviation is deliberate and its consequence is stated under "Why this file is one document"; per AGENTS.md §4 nothing about the naming is load-bearing beyond the `todo-` token. -->

**Document:** `todo-open-debt-program.md`
**Role:** Program order — four independently dispatchable workstreams
**Goal:** Close the four debts the repository's own records already name as unfinished, in an order that respects their real dependencies, without inventing scope and without quoting a rotted number.
**Acceptance:** per phase, its own named command. AGENTS.md §4 governs the rename: a `done-` prefix is earned ONLY when that phase's acceptance command has been RUN and PASSED, and renames happen **in place at the repo root** — never into `.agents/archived/`.

**Name map — read this before running anything below (added 2026-09-18, HEAD `19437867c`).** This file was written before the rebrand and before restructure phases P11/P12, so **every `oz-*` name in it is a RENAME, not a missing file** — `cargo -p oz-bridge` answers *"package ID specification did not match any packages"*, which reads like *gone* and is not.

| In this file | In this tree today |
|---|---|
| `oz-bridge` | `kasirmu-bridge` |
| `oz-core` | `kasirmu-core` |
| `oz-payment` | `kasirmu-payment` |
| `oz-hal` | `kasirmu-hal` |
| `oz-api` | `kasirmu-api` |
| `oz-cloud-server` | `kasirmu-cloud` (`apps/cloud-server`) |
| `oz-pos-tablet` / `apps/tablet-client` | `kasirmu-mobile` / `apps/mobile-tauri` |
| `oz-pos-app` / `apps/desktop-client` | `kasirmu-app` / `apps/desktop-tauri` |

The **Fence / Commit prefix / Acceptance** lines of each phase have been renamed to the current names, because those are the lines a worker executes or edits against. **Dated records, `[carried]` measurements and ticked boxes are deliberately left as written** — this file's own rule is that a dated record is corrected forward, never back, and rewriting the paths inside one would destroy the record it is. Resolve any stale path you meet through the table above.

---

## Why this file is one document

The owner asked for a single `todo-*.md` covering all four workstreams. Four consequences follow, and they are recorded rather than hidden:

1. **Each phase carries its own owned-path fence and its own commit-subject prefix.** Two phases can therefore run concurrently, and a worker must never edit outside its phase's fence.
2. **The phases are not equally ready.** Phase 1 and Phase 2 are executable now. Phase 3 is executable but contains one item that needs a product ruling before code. Phase 4 is *mostly blocked on inputs the repository does not have* — that is the honest finding, and it is the reason Phase 4 is last.
3. **Splitting is cheap if the owner prefers parallel files.** Cut on the `## Phase N` headings into `todo-open-debt-agents-N.md`; the fences already partition the tree, so no cross-file coordination is lost. Do not split by *task* — only by phase.
4. **The program-level acceptance is not a command.** There is no single gate that can see all four. Each phase's acceptance is its own; a green Phase 1 says nothing about Phase 2.

---

## Program rules — binding on every phase

### Hard repo rules (restated because a worker reads THIS file, not `AGENTS.md`)

- **NEVER create a branch. NEVER switch a branch. NEVER push.** This program runs on whichever branch is already checked out (authoring branch: `0.0.39`). A push to `main` runs CI *and* deploys.
- **NEVER `git add`, `git stage`, `git commit -a`, `git commit --amend`, `git stash`.** The only permitted commit form is ONE line with an explicit pathspec: `git commit -m "<type>(<area>): <subject>" -- path/one path/two`. A *new* file needs the one-call chain (`git add -- new/one && git commit -m "..." -- new/one`) and nothing else.
- **Version is locked at `0.0.39`.** Do not touch a version string anywhere.
- **`cargo fmt --all` is FORBIDDEN** in this checkout — it reformats other sessions' in-flight `.rs` files in the working tree. Format a single file with `rustfmt --edition 2024 <path>` if you must.
- **Forward slashes in every path argument.**
- **Money is `i64` minor units via `Money`. Never `f32`/`f64`.**
- **SQLite writes go through an explicit `rusqlite` transaction.**

### The shared checkout

One worktree, several live agent sessions, one shared `HEAD` and one shared index. Consequences that have already bitten this repo:

- `git status` mutates `.git/index`; a killed status call can leave a stale `.git/index.lock` that fails *every* commit repo-wide. Check the lock's mtime and whether any `git`/`cargo` process is actually alive before removing it, and back the index up first.
- A `git commit` returning *"nothing to commit, working tree clean"* when you expected otherwise does **not** mean your work is lost. Run `git show --stat HEAD` before concluding anything.
- Files may be **mid-write by another session** at the moment you read them. A test failure observed during a concurrent-agent window must be re-run before it is reported as a verdict. This exact trap produced a false vitest failure on 2026-09-14 (55 s after the run, a peer edited the failing file).

### Measurement discipline — a claim is its command

When you write a number into a doc, the command printed beside it must be run VERBATIM and must print that number. An equivalent command you chose yourself is not a substitute: `ls ui/src/__tests__/* | wc -l` prints 575 while the correct count is 572, because `ls dir/*` emits a per-subdirectory header and omits dotfiles that `find -type f` counts. *(Recorded as rule D7 in `.agents/manager-wave4-rules.md`, added 2026-09-14 after a review finding.)*

Corollaries this program depends on:

- `git ls-files` answers *"is it tracked today"*, never *"did it ever exist"*. Use `git show <sha>:<path>` for history.
- A grep of one file is evidence about one file. Before writing "only in X", grep the tree.
- **Do not trust an audit stamp's numbers.** Stamps are point-in-time records by this repo's own convention, and several quoted in this file have rotted since they were written. See "Claims that have rotted".

### Counting method for checkbox census

Any box census you write must state its method, because two defensible methods disagree:

```bash
grep -c  '^- \[ \]' <file>            # unindented boxes only
grep -cE '^[[:space:]]*- \[ \]' <file>  # any depth, including nested boxes
```

This file uses the **any-depth** form and says so where it quotes a number.

---

## Phase 1 — The release profile is red and no gate can see it

**Fence:** `crates/kasirmu-bridge/src/**`, `crates/kasirmu-core/src/**`, the release-path runners (`scripts/check.sh`, `scripts/check.ps1`, `scripts/coverage.sh`, `scripts/coverage.ps1`, `scripts/release.sh`, `.github/workflows/release.yml`).
**Commit prefix:** `test(kasirmu-bridge):` · `fix(kasirmu-bridge):` · `test(kasirmu-core):` · `ci(release):`
**Acceptance:** `cargo test -p kasirmu-bridge --release` → **0 failed**, plus a runner that can see this failure set.

### The debt

`cargo test -p oz-bridge --release` does not pass, and **no runner in this repo builds these crates in release**, so nothing reports it.

| Figure | Value | Source |
|---|---|---|
| release result, **measured at authoring HEAD `257ff6122`** | **1231 passed / 76 failed / 0 ignored**, `finished in 181.69s`, command wall time 5m49s including the release compile | **run this pass** — `cargo test -p oz-bridge --release`, result line verbatim |
| release result, earlier record | 1228 passed / 76 FAILED / exit 101 | `[carried]` `todo-refactor-oz-pos-app-agents-3.md:142`, a dated record of one real run |
| debug result | 1305 passed / 0 failed | `[carried]` same |
| release test in `scripts/check.sh` | `grep -c -- --release scripts/check.sh` → **0** | `[carried]` same, re-run 2026-09-14 |
| release test in CI | `dev-ci.yml:244` is `cargo nextest run --workspace --all-features` — no `--release` | measured this pass |

**The re-run confirms the earlier record on both axes, and the confirmation is the interesting part.** The predicted values were 1228 + 3 = **1231 passed** and **76 failed, unchanged**; the measured values are exactly that. So:

- The **failure count did not move** — nothing has landed since the record that adds or removes a release-only failure.
- The **passed count moved by exactly +3**, which is `68e6dd356`'s three `#[tokio::test]` cases, exactly as the record predicted.
- **The per-module breakdown reproduces exactly** — subscription 21 · auth 18 · audit 11 · pos 7 · staff 6 · workspaces 5 · inventory 4 · terminals 2 · locations 2 = 76. (Counted from the failing-test name list, which the runner truncates to the tail; the 20 names not shown are the audit 11 plus the remaining auth 9, which is what makes the visible 56 sum to 76.)

**One observation the classification task must handle, and it crosses into Phase 3.** Among the failures are the *scope* tests: `subscription::subscription_tests::verdict_scope_clears_inside_the_assigned_location`, `…verdict_scope_denies_when_the_assignment_excludes_the_session_location`, `…verdict_scope_denies_when_the_workspace_dimension_excludes_the_session_type`, plus `workspaces::workspaces_tests::scoped_assignment_branch_dimension_denies_out_of_scope_store_for_session`, `…scoped_assignment_filters_session_workspace_listing`, `…scoped_assignment_workspace_dimension_filters_for_store_listing`. **Phase 3's branch/workspace scope surface is currently red in release.** The likeliest cause is the same sentinel fixture dishonesty (these tests need a verifying signature), which would make it Phase 1's problem rather than a scope-logic defect — but that is a hypothesis, not a finding. Classify it before Phase 3b writes anything against that surface.

**Environmental note, not a repo defect:** the build's stderr carried a sandbox security-policy block on `wmic.exe` and `reg.exe`. The release build and the whole test run completed regardless, and the result is consistent with the prior record, so the block is recorded as an environment characteristic of this sandbox rather than a cause of anything.

Failure families, `[carried]` from the same record and **not** re-classified by the re-run above: 39× `InvalidSubscriptionSignature … Invalid symbol 95, offset 9`; ~16× `assertion left == right`; ~10× `PermissionDenied("audit log requires the Premium plan or above (current tier: Free)")`. The re-run confirms the *counts*; it does not confirm the *causes*, which is the classification box below.

### What is already established — do not re-derive

- **The 39 base64 reds are a test-fixture defect, not a broken signature path.** `crates/oz-core/src/license_verification.rs:391-394` returns `Ok(())` for the sentinel `BOOTSTRAP_FREE` under `#[cfg(debug_assertions)]` only; in release the same string reaches a standard base64 decode at `:398` and fails on the `_` in the sentinel's own text. Producer and consumer alphabets agree (`apps/license-server/main.go:1113` `base64.StdEncoding.EncodeToString`), and a forged non-sentinel signature still fails closed in BOTH profiles. `[carried]`
- **The sentinel is also the production seed.** `crates/oz-core/migrations/20260813_init.sql:1512-1514` writes `BOOTSTRAP_FREE` into every fresh install; the PG twin repeats it at `20260813_init.pg.sql:2100-2101` and is **generated** by `scripts/generate-pg-migration.py`, never hand-edited. Activation rewrites the row (`crates/oz-bridge/src/license.rs:175` → `crates/oz-core/src/license_verification.rs:625` `INSERT OR REPLACE`), so an activated install never sees the sentinel. `[carried]`
- **The 1305 → 1304 gap is one `#[test]` compiled out of release**, not a miscount: `crates/oz-bridge/src/sync_tests.rs:35-37` is `#[cfg(debug_assertions)]`. `[carried]`
- **The both-profile assertion idiom already exists here and is proven to fail.** `crates/oz-core/src/db/audit_security_tests.rs:418` (`assert_eq!(recorded, cfg!(debug_assertions))`) and `crates/oz-bridge/src/subscription_tests.rs:26/39-48`, with `crates/oz-bridge/src/license_tests.rs:475-479` as its first use inside `oz-bridge`. So the absence of such an assertion at a site is a coverage gap, not a language limitation. `[carried]`

### Tasks

- [x] **Reproduce and record.** `cargo test -p oz-bridge --release` at HEAD `257ff6122` → **1231 passed / 76 failed**, `finished in 181.69s`. Reproduces the earlier record exactly (see the table above). Done in this pass; the numbers are in "The debt".
- [ ] **Classify each of the 76.** Per failing test, decide (a) *fixture is profile-dishonest* — it seeds the sentinel and asserts a debug-only outcome, or (b) *genuine profile difference*. The count 76 must not be "corrected" by a passing test: a test that passes in both profiles lands in `passed` and cannot move the failure count.  RE-SCOPED, not done. The per-test arm now sits downstream of ONE root cause the owner page carries as **item 14, "Is a seeded sentinel debt at all if no CI leg ever compiles the profile that rejects it?"** (`docs/plans/notes.md:1380`), and the population is stated there as 368 test-shaped sites rather than 76 independent failures. Box stays open: the classification verdict table was never written.
  - Start with the six scope tests named above — they are the ones Phase 3b will build on.
  - `crates/oz-bridge/src/locations_tests.rs:147-148` is a worked example of the failure mode in the other direction: *"Plus is used instead of Free because debug builds upgrade only the bootstrap Free tier"* — i.e. the author routed AROUND the bypass rather than asserting it in either profile.
- [ ] **Close the one bypass still unreachable under test: `crates/kasirmu-bridge/src/locations.rs:231-236`.** All three calling tests currently die one line earlier at `:227` `sub.verify_signature()?` on the sentinel. Determine whether a release-side assertion is reachable at all; **if it is not, record it as a parked arm beside `license.rs:670-683` rather than weakening production or faking a test.** Do not apply a shared bootstrap/Free helper to the ~21 token-required sites — that would convert a loud forged-row error (pinned in both profiles at `crates/oz-bridge/src/auth_tests.rs:400-402`) into a silent Free run.  ANCHORS HOLD, live: `crates/oz-bridge/src/locations.rs:231-236` is still the `#[cfg(debug_assertions)]` tier swap ahead of `store.enforce_location_quota(&tier)?` at `:237`, and `crates/oz-bridge/src/sync_tests.rs:35-37` is still `#[cfg(test)]` + `#[cfg(debug_assertions)]`.
- [ ] **Fix the profile-dishonest fixtures** using the existing both-profile idiom, so each assertion is true in debug AND release rather than weakened in one. Prove each fix is *failable*: mutate the line, confirm the opposite profile goes red, restore.
- [ ] **Decide `crates/kasirmu-bridge/src/sync_tests.rs:35-37`.** Either it should run in release (drop the `cfg`) or the doc should stop presenting the debug/release total gap as unexplained.
- [ ] **Add a `--release` runner.** The recorded options are `scripts/release.sh:62,65` and `.github/workflows/release.yml:149` — note both currently carry `--exclude kasirmu-app --exclude kasirmu-mobile`. Do **not** drop those excludes on the strength of a green debug run. A cheap alternative that satisfies "some gate can see it" without touching the release path is a dedicated release-test step in `dev-ci.yml` or `scripts/check.sh`.  STILL A LIVE DEFECT, re-measured tonight: `git grep -n -e '--release' -- .github/workflows/dev-ci.yml` → NOMATCH, and the three recorded runners still exclude by crate — `scripts/release.sh:62,65`, `.github/workflows/release.yml:149` and the `test workspace (nextest)` step in `scripts/check.sh` (`grep -n 'test workspace (nextest)' scripts/check.sh`) each carry `--exclude oz-pos-app --exclude oz-pos-tablet` with no `--release`. No leg compiles the profile that rejects the seed.
- [ ] **Owner ruling required — the parked arm.** `crates/kasirmu-bridge/src/license.rs:670-683` (expired past grace → `is_active: false` + `Expired`) cannot be executed from this crate: reaching it needs a payload whose signature verifies, and `license_verification.rs:36` embeds only the public half. Both ways to cover it change production (an injected verifier on the call path, or a `#[cfg(test)]` key compiled in). **Recommendation on record: leave it parked rather than weakening production to make a test reachable.** Get the ruling; do not decide it in a worker lane.  STILL PARKED, anchor verified at `crates/oz-bridge/src/license.rs:670-673` (`#[cfg(not(debug_assertions))]` → `is_active: false`). Blocked on the ruling named in this row, not on code.
- [ ] **Commit milestones** — one per logical step, e.g. `test(kasirmu-bridge): assert the location-quota bypass in both build profiles`.
  - **2026-09-15, docs audit at HEAD `460e33285`:** NOT WORK. The box restates a rule that already binds every worker (`AGENTS.md` §3, one pathspec commit per logical step; this file's own Program rules at `:23`) — it can be neither done nor undone, so it is counted here as not-work rather than as a debt.

### Why this phase is first

It is the only one of the four where the repository is **already failing a command**, and its blast radius includes Phase 2: the failure is *live* on the tablet shell, which registers zero license commands (`grep -c license apps/tablet-client/src/lib.rs` → **0**, measured) and has no licence gate in `ui/src/frontend/shell/tablet/TabletAppShell.tsx:64-84`. `[carried]` Reachable today only via a hand-built release APK, because `android.yml`/`ios.yml` are `.bak` and no live workflow builds mobile.

---

## Phase 2 — Tablet ↔ desktop wire parity

**Fence:** `apps/mobile-tauri/src/**`, shared DTOs under `crates/kasirmu-bridge/src/**` (additions only — never change a desktop-side wire shape without a back-compat alias).
**Commit prefix:** `refactor(tablet):` · `test(tablet):` · `refactor(bridge):`
**Acceptance:** `cargo check -p kasirmu-mobile` clean · `cargo test -p kasirmu-mobile` green · `python scripts/verify-ipc-parity.py` → `IPC parity: OK`.

### The debt

Desktop and tablet each declare their own argument structs for the same commands, and the divergence class is **not hypothetical**: the two most recent slices of this work each found a live wire bug.

- T2 (`7a7c1c3dc2`) — void/browser/scale contracts shared; closed a tablet-only wire bug where the UI sends camelCase `args: { saleId, reason }` while the local `VoidSaleScopedArgs` was snake_case-only.
- T3 (`e821c2814b`) — regional + `local_payment` DTOs shared; closed a **three-shell** wire bug dating to slice 6 where `LocalPaymentRailArgs` declared `rename_all = "camelCase"` on desktop, bridge and tablet while the only UI caller has always sent snake_case, so the settings card's save never worked against a real backend. Fixed with back-compat aliases plus the first wire-deserialization tests.

The pattern is **2-for-2 on finding hidden casing bugs**, which is the justification for doing the audit before the next domain rather than after.

### Measured scope

| Measure | Command | Value |
|---|---|---|
| tablet command modules | `ls apps/tablet-client/src/commands/*.rs \| wc -l` | 98 |
| tablet-local `*Args` structs | `grep -rn "^pub struct [A-Za-z]*Args" apps/tablet-client/src/commands/*.rs \| wc -l` | **54** |
| tablet re-exports from the bridge | `grep -rn "pub use oz_bridge" apps/tablet-client/src/commands/*.rs \| wc -l` | **11** |
| bridge `*Args` structs | `grep -rn "^pub struct [A-Za-z]*Args" crates/oz-bridge/src/*.rs \| wc -l` | 103 |
| tablet registered commands | `[carried]` `todo-refactor-oz-pos-app-agents-3.md:121` | 322 in `apps/tablet-client/src/lib.rs:445-784` |

The 54 locally-declared structs against 11 already shared is the audit's working set. It is an **upper bound on candidates, not a defect count** — some tablet structs are legitimately tablet-only.

**Reference implementations to copy:** `crates/oz-bridge/src/local_payment.rs:30` (`LocalPaymentRailArgs`) and `crates/oz-bridge/src/void.rs:33` (`VoidSaleScopedArgs`), with the tablet side re-exporting at `apps/tablet-client/src/commands/local_payment.rs:25` (`pub use oz_bridge::local_payment::LocalPaymentRailArgs;`). The wire-deserialization test shape is in `crates/oz-bridge/src/void_tests.rs:66-92`.

### Tasks

- [x] **Wire audit first, per domain, before moving any DTO.** For each of `pos`, `staff`, `auth`, `products`, `settings`: diff the tablet-local struct against the bridge's for the same command and record, per field, (a) same name, (b) different name, (c) **different serde rename strategy**. Class (c) is the bug class; class (b) is the next most likely. Publish the audit as a table in this file or in a sibling journal — the audit is the deliverable, the refactor is the follow-up.
  - Cross-shell caution: `settings` registers **28** commands on tablet vs **19** on desktop `[carried]`, so never quote one settings count for both shells.
  - **DONE 2026-09-15, audit run read-only at tip `5802101ca` — and this box is the AUDIT, which is what the row asked for; the refactor stays downstream in the row beneath it, **Then share the DTOs, one domain per commit**.** The populations reproduce the Measured-scope table above exactly, by its own commands: **54** tablet-local `*Args`, **103** bridge `*Args`, **11** `pub use oz_bridge` lines. The audit's own working figures on top of those, attributed to it rather than re-derived here: **34** types carried by the 11 lines (a reader re-counting those 11 statements gets **78** identifiers, so the 34 counts a narrower unit than every name inside the braces — it excludes the function and constant items like `build_image_query` and `BILL_TYPE_OPEN_BILL`), and **50** exact-name pairs compared field-by-field, **4** flagged, **3** of them real. The `:136` "2-for-2 on hidden casing bugs" prediction held and was exceeded in yield — this pass found 3, **2 of them live data loss on a shipped path**:
      - **`UpdateSyncSettingsArgs` — class (c), live.** Tablet declared the copy with no `rename_all` while the UI sends camelCase and the bridge twin declares `rename_all = "camelCase"`. Both divergent fields are `Option<String>`, so serde filled `None` instead of erroring and the `unwrap_or("")` in `update_sync_settings_data` wrote an EMPTY row — the settings save silently WIPED the sync server URL on tablet while the same save worked on desktop.
      - **`CompleteSetupArgs` — class (b), live.** The tablet copy has no `default_currency` field, so the wizard's currency choice is dropped on tablet only. Same silence, same mechanism: a field the copy does not declare is not an error, it is a lost write.
      - **`ProcessRefundArgs` — class (c), latent.** Casing diverges from the bridge twin, but the command is **unregistered**, so nothing can reach it today. That is why the count of live losses is 2 and not 3. **Closed the same day by `5cb702c08`, which deleted the tablet copy rather than patching its field list — and `process_refund` is still registered in neither shell, so read it as a latent trap removed, not a data loss repaired.**
      - The fourth flag was a false positive on re-read and is dropped here rather than carried as a defect.
  - **THE STRUCTURAL CAUSE, which is the finding and belongs here rather than in three bug rows: local copy versus re-export.** The drift is not spread over the 54 — it sits entirely on the copied side. The **34** types arriving through the **11** `pub use oz_bridge` lines cannot drift, because there is one declaration of them; every one of the three findings sits on the copied side of the ledger — each names a tablet `pub struct` duplicating a bridge twin of the same qualified name, in `apps/tablet-client/src/commands/{sync,setup,refunds}.rs` against `crates/oz-bridge/src/{sync,setup,refunds}.rs`. A copy and a re-export also fail DIFFERENTLY under the same stale key: the copied struct ignores it (serde leaves an unmatched field alone, an `Option` fills `None`, and the caller writes a blank over real data), while a bridge type carrying `deny_unknown_fields` rejects it loudly. So the silence is a property of the copy, and the policy-shaped conclusion for the DTO-sharing row beneath it is narrower than "fix three structs": **stop declaring the type twice** — re-export, and where a copy must exist give it the strict attribute the bridge has. Alias-chasing the three findings closes three rows and leaves the shape that produced them standing over 50 pairs, 47 of which were clean by agreement rather than by construction.

- [x] **Then share the DTOs, one domain per commit**, following the T2/T3 pattern: bridge owns the type, the tablet re-exports it, a back-compat alias carries the old spelling, and a wire-deserialization test pins the frontend's actual payload.
  - **PAID 2026-09-15, one commit per domain where the drift was proven live — `72b28d025` (sync) and `a84e0d523` (setup).** This row was held open at `3e306f879` on the ground that no SHA existed for the work then in flight; both now do, and each carries the wire-deserialisation test this row asked for, which is the thing that separates this tick from the audit tick above it.
      - **`72b28d025 fix(tablet-client): camelCase sync-settings IPC keys so the settings page stops wiping the server URL`** (+19/−0 `apps/tablet-client/src/commands/sync.rs`, +77/−5 `sync_tests.rs`) — **the wipe was two bugs, and the lane found the second one.** The inbound `UpdateSyncSettingsArgs` was snake_case against the camelCase the UI sends, but the OUTBOUND `SyncSettingsDto` was snake_case too while `ui/src/api/offline.ts:123-127` types `serverUrl`/`hasApiKey` — so fixing only the request half left `ui/src/features/settings/hooks/saveDiff.ts:325` computing `normUrl(serverSync.serverUrl)` over an `undefined` read, which yields `null`, and the payload assembled on the next line re-sent the wipe through the same `unwrap_or("")`. Both structs now carry `rename_all = "camelCase"`. Pinned by `update_sync_settings_wire_keys_match_bridge_twin` (`sync_tests.rs:59`), which feeds **three** payloads — UI camelCase, legacy snake_case, omitted-key — into the tablet struct AND the bridge twin and asserts them equal field by field, plus a tail assertion that camelCase resolves to VALUES rather than to two matching `None`s, which is the shape that hid the bug: two `None`s are equal. Plant red `EXIT=101`, `server_url casing drift`, at `sync_tests.rs:84`, restored to byte-identical hashes. `cargo test -p oz-pos-tablet sync` → 33 passed, attributed to that lane's run — this pass executed nothing.
      - **`a84e0d523 fix(tablet-client): stop dropping the wizard default_currency on tablet`** (+68/−43 `apps/tablet-client/src/commands/setup.rs`, +74/−18 `setup_tests.rs`) — mechanism: **the copy was deleted, not patched.** The tablet's local `CompleteSetupArgs` is gone in favour of `pub use oz_bridge::setup::CompleteSetupArgs;` (`setup.rs:30`), chosen over adding the missing field because a copy silently ignores the NEXT key the bridge gains — the argument in the structural-cause note above. The body still had to gain the `Settings::set_default_currency` leg (`setup.rs:113`), and `fn write_setup(conn, args)` was extracted (`setup.rs:81`) so the `#[command]` and the tests execute ONE statement list. **That extraction is the load-bearing half:** the old `run_complete_setup` helper re-implemented the operation list inside the test file, so a production leg could vanish and no test was able to read red for it; `git show a84e0d523` replaces it with `run_complete_setup_args`, which is `write_setup(conn, args)?` plus `Ok(())`. New case `complete_setup_currency_survives_the_wizard_payload` asserts `USD` both survives deserialisation and lands in `currency.default` — USD chosen because no serde fallback can produce it (the fallback is `IDR`, which its sibling case pins). One existing test, `complete_setup_args_debug`, had to gain the field to compile: that is a genuine dependency on its absence and is not the same thing as `complete_setup_args_deserialize`, which merely could not SEE the loss. 24 passed in that file, same attribution.
  - **SCOPE — read this before calling Phase 2 shared.** Both commits cover the two domains whose drift was proven live; **three further types declared themselves twice and neither commit touched them — `ProcessRefundArgs`, `SetupStatus` and `EnabledFeaturesResult` — and all three have SINCE been closed by their own SHAs, so the clause this bullet used to carry (`every one of them still doubled`) was false by the time a reader got here and is corrected in place rather than left to rot: `5cb702c08 refactor(tablet): re-export ProcessRefundArgs from oz-bridge and pin the camelCase wire` deleted the tablet copy at `apps/tablet-client/src/commands/refunds.rs:34` in favour of `crates/oz-bridge/src/refunds.rs:48`, and `528dba14c refactor(tablet-client): re-export SetupStatus and EnabledFeaturesResult from oz-bridge and pin their wire keys` (+17/-21 `apps/tablet-client/src/commands/setup.rs`, +72/-0 `setup_tests.rs`) deleted the two at `setup.rs:36` and `:47` in favour of `crates/oz-bridge/src/setup.rs:37` and `:48`, so tablet `setup.rs:46` now reads `pub use oz_bridge::setup::{EnabledFeaturesResult, SetupStatus};`.** Two of those three were the OUTBOUND direction — a `Serialize` twin whose failure is the one `72b28d025` proved expensive: the front-end reads a key the struct never emits, gets `undefined`, and computes a `null` forward into a clearing write — and `528dba14c` closed both by re-export, with its own wire-key pin. **What actually survives the audit is a different pair, and it is not these three names: `ProcessRefundScopedArgs` and `ProcessRefundResult` are still tablet-local copies of `crates/oz-bridge/src/refunds.rs:65` and `:79`** (the `:64`/`:76` pair this line was handed was off by one and by two — the live numbers were re-derived at HEAD `b133728fa` this pass, where each name resolves in both trees). Of the two, `ProcessRefundResult` is the outbound `Serialize` twin carrying that same failure shape, and `ProcessRefundScopedArgs` is the one **on the live path with a real caller** — `ui/src/api/sales.ts:657` sends it and `apps/tablet-client/src/lib.rs:609` registers `process_refund_scoped` — which is exactly why folding it is a wire decision that needs its own pin rather than a cleanup riding along on a dedup. **PAID SINCE, AND THE WAIT WAS THE POINT:** the someone was the lane reported as `d15c78a4` — **a session handle, not a commit: `git cat-file -t d15c78a4` prints `fatal: Not a valid object name`, so nothing here should cite it as a SHA** — and the work landed as `437d7c102 refactor(tablet-client): re-export the last two doubled refund wire types` (+5/−27 `apps/tablet-client/src/commands/refunds.rs`, +66/−0 `refunds_tests.rs`), so tablet `refunds.rs:48` is now one `pub use oz_bridge::refunds::{…}` over four names and **no refund or setup wire type declares itself twice in either tree**. **Casing verdict for this pair: NO drift — a dedup with no wire movement**, and saying so is what separates it from `5cb702c08`: both copies already carried the identical `#[serde(rename_all = "camelCase")]` over identical field sets (re-read at `437d7c102^`, where tablet `refunds.rs:51` and `:83` hold the attribute), and the UI read sites had been agreeing with that spelling all along — `ui/src/api/sales.ts:616-619` declares `ProcessRefundResult { refundId, totalMinor }`, `:650-655` declares `ProcessRefundScopedArgs { saleId, reason, note?, lines }`. **What the lane declined to sell, and the gap that therefore survives it:** after the dedup the casing attribute has exactly one home — `crates/oz-bridge/src/refunds.rs:28`/`:46`/`:64`/`:77` — and that file sat outside the lane's fence, so the plant simulated the read-side symptom inside its own file rather than proving the bridge attribute cannot regress. **`12 passed` is not evidence that the bridge cannot drift**; the in-fence guard for that direction is the snake_case-refusal assertion in `refund_wire_types_are_bridge_reexports_graded_on_values` (`refunds_tests.rs:386`-`:389`, with its twin for the unscoped type at `:434`-`:436`), which goes red if camelCase ever stops being the only accepted key set — a bridge-side regression is still caught only by the shell that reads it. **And the arc's cost, since this closes the last of the duplication: the tablet-wire audit found 3 real drifts and 2 of them were live data loss** — `UpdateSyncSettingsArgs` (casing, live, silently wiped the stored sync server URL) and `CompleteSetupArgs` (missing field, live, dropped the wizard's currency on tablet) closed at `72b28d025` and `a84e0d523`; `ProcessRefundArgs` (casing, latent behind an unregistered command) at `5cb702c08`; `SetupStatus` + `EnabledFeaturesResult` (drift-prone outbound shape, never proven live) at `528dba14c`; the last pair at `437d7c102`. The final three commits removed a shape, not a symptom — nothing was wrong on the wire for them. **What the arc still does not have is its acceptance:** `cargo test -p oz-pos-tablet` and `python scripts/verify-ipc-parity.py` RUN SINCE, and the sentence that said they had not is corrected here rather than left to rot a reader's trust in the rest of the line: at tip `1dafb894c`, with zero dirty paths under `apps/tablet-client`, so it is a commit fact and not a working-tree reading — `python scripts/verify-ipc-parity.py` → exit **0** printing `IPC parity: OK`, populations unmoved at **458 UI command strings / 453 registered desktop / 322 registered tablet**, and `cargo test -p oz-pos-tablet` with no filter → exit **0**, `test result: ok. 660 passed; 0 failed; 0 ignored` (an ops pass measured it warm at 84.12 s; this lane re-ran it at the same tip and read 79.05 s for the same 660) — where tonight's scoped prints had shown 33 sync, 27 setup and 12 refunds. `RUSTFLAGS='-D warnings' cargo check -p oz-pos-tablet --all-targets` → exit 0, and the figure that makes that mean anything is the **78.4 s re-run under `CARGO_INCREMENTAL=0`**, because a `-D warnings` pass that answers in well under a second is a cache hit on a stale fingerprint, so the cold number is the one graded; this lane repeated the cold run too and got exit 0 again. **So: does the run entitle a tick? No.** Phase 2's acceptance is a shared-DTO *end-state*, and 660 green plus a parity figure that did not move is evidence the three `pub use` re-exports broke nothing — not proof the phase's remaining boxes are done. `:169` (the `AppState` → `BridgeCtx` seam, which still waits on a Phase-1-fence ruling) and `:170` (keep the parity gate green at every step) **stay open, and no box moved on this page on account of these commits or of this run** — the same rule that held `:163` un-ticked an hour before it could be ticked. Re-derive the census rather than trusting any sentence on this page, because it has already rotted once today, which is the whole reason the sentence above carries a SHA instead of a present tense: `for t in ProcessRefundArgs SetupStatus EnabledFeaturesResult ProcessRefundScopedArgs ProcessRefundResult; do printf '%s ' $t; grep -rlE "pub struct $t" apps/tablet-client/src/commands crates/oz-bridge/src; done` — measured at HEAD `437d7c102`, **all five names answer once (bridge only) and none answers twice**, which is the first reading of this census since the audit opened it. Prefer that per-name form over the anchored `grep -rn "^pub struct …"` the Measured-scope table above is written in: the anchor silently misses any declaration a file has indented, which is how a still-live tablet copy drops out of a census that reports the population clean.
  - **OPEN QUESTION, recorded not resolved, 2026-09-15.** `a84e0d523` deliberately did NOT mirror the bridge's leading `store.seed_default_roles()` (`crates/oz-bridge/src/setup.rs:99`) into the tablet body, and says so at `apps/tablet-client/src/commands/setup.rs:79` — "that seeding is not this command's behaviour to take on today". Fair for a fix lane, but it leaves a **live tablet-versus-bridge divergence in setup behaviour**: `complete_setup` on the bridge path seeds default roles, `complete_setup` on tablet does not, and no ruling says which is right for a fresh tablet install. So the question for an owner is: *is default-role seeding part of `complete_setup`, or does the tablet path already seed it somewhere else?* Ask it before the next tablet setup change, because the mirror-and-test idiom this commit establishes makes the divergence visible in exactly one place — a doc comment — which is not where someone hunting a missing-role bug will look.
- [ ] **Decide the `AppState` → `BridgeCtx` seam.** Measured field-by-field and deferred: 7 of 14 fields match; `Arc`-ing `db`/`terminal_id` is mechanical; `plugins` would drag the `mlua` Lua VM into the Android APK for a forever-`None` field. `[carried]` This needs a Phase-1-fence decision on the bridge side before it can move.
- [ ] **Keep the parity gate green at every step.** `python scripts/verify-ipc-parity.py` and `bash scripts/verify-scoped-coverage.sh` both read the registration macros verbatim; a DTO move that changes a registered path breaks them.
- [ ] **Commit milestones** — e.g. `refactor(tablet): share the pos wire contracts with kasirmu-bridge`.
  - **2026-09-15, docs audit at HEAD `460e33285`:** NOT WORK — same reason as the Phase 1 milestone box: it names a commit form, not a defect.

### Interaction with Phase 1 — do not run these two blind

The tablet shell is where the release-profile rejection is live, and it registers **zero** license commands. A Phase 2 worker adding tablet command registrations touches the surface a Phase 1 worker is trying to make testable. Sequence Phase 1's classification task first, or keep the two fences strictly disjoint.

---

## Phase 3 — Gate on the permission vocabulary, not on a role rank

**Fence:** `ui/src/utils/role.ts`, `ui/src/features/workspaces/WorkspaceHome.tsx`, `ui/src/features/settings/SettingsPage.tsx`, and — for the scope axes — `crates/kasirmu-core/src/db/staff.rs`, `crates/kasirmu-bridge/src/subscription.rs`, plus a new migration if the design requires one.
**Commit prefix:** `refactor(ui):` · `feat(rbac):` · `docs(rbac):`
**Acceptance:** `cd ui && npx vitest run` green · `cargo test -p kasirmu-core` green · `python scripts/verify-ipc-parity.py` → `IPC parity: OK` · `bash scripts/lint-i18n.sh` clean if any `.ftl` key is added.

### 3a — The ruling already made, and the two code shapes it contradicts

**Ruling (2026-09-14): stop asking a role name for a rank.** Gate the Tools section on the permission vocabulary like every other gate, instead of `WorkspaceHome.tsx:363`'s `ROLE_HIERARCHY[roleName] ?? 0`.

The doctrine is already written into the bridge. `crates/oz-bridge/src/subscription.rs:238-241`:

> *The registry permission whose holding decides the verdict's `role` axis, per feature. Permissions are the vocabulary (ADR #47 stop_memo ruling A2): no rank map is invented, and a custom role holding the gate permission passes the same way a preset would.*

`gate_permission()` at `:241` is the established pattern, including its non-obvious consumer: `AvailabilityFeature::Locations | PosInstances` both map onto `permissions::TOPOLOGY_WRITE`.

Against that, the UI has a rank map in two places:

| Site | What it is |
|---|---|
| `ui/src/utils/role.ts:37` | canonical `ROLE_HIERARCHY`, with `roleAtLeast()` at `:70-74`; consumed by `SettingsPage.tsx:10`, `RoleBadge.tsx:9`, `RoleIcon.tsx:6` |
| `ui/src/features/workspaces/WorkspaceHome.tsx:97` | a **duplicate** of the same table, acknowledged in `role.ts:32-35` as *"a recorded follow-up — that file is out of scope for this change and stays untouched, so the duplication is known and temporary rather than accidental"* |

Rank-based gates in `WorkspaceHome.tsx`, all measured this pass: `:363` (`roleLevel`), `:372` (`canAddWorkspace`), `:378` (`canAccess`), `:414` (`canSeeTools`).

Supporting fact, read this pass: the `roles` table (`crates/oz-core/migrations/20260813_init.sql:571-578`) declares exactly `id, name, description, permissions, created_at, updated_at` — **no parent/clone column**, so "inherit the cloned preset" is not expressible without a migration.

### 3b — The two scope axes that do not exist

`require_permission_scoped` already evaluates **branch and workspace** scope on the write path — `crates/oz-core/src/db/staff.rs:229`, doc comment `:225-228` (*"the scope-aware gate (ADR #35 D5 / spec 0048): … plus the assignment's branch/workspace scope is evaluated for scoped assignments"*), refusal at `:243-245`. The same axis reaches the availability resolver as a first-class fact (`crates/oz-core/src/entitlements.rs:234-235`, `:247`, `:262`; `crates/oz-core/src/availability.rs:395` `let scope_denies = facts.scope_granted == Some(false);`). `[carried]`

**What remains is organisation-wide and terminal-level scope, and nothing evaluates either.** `[carried]` `todo-tools.md:443`, whose own closing note says this *"needs its own `todo-` order"* — which is this phase. Note that ADR #35 D5 (`docs/decisions/2026-08-11-adr35-rbac-role-assignments-user-profile.md:104`) and spec 0048 (`docs/specs/_done/0048-rbac-assignment-model-and-taxonomy/`, §8 non-goals) both **defer** the org-tenant layer to D7. So this is not unfinished work from 0048; it is work 0048 explicitly declined, now being picked up.

### Tasks

- [x] **3a.1 — Fold the duplicate rank table.** `WorkspaceHome.tsx:97` onto `ui/src/utils/role.ts:37`, closing the follow-up recorded at `role.ts:32-35`. Pure refactor; the settings-page gate (`SettingsPage.tsx:10`) is the regression surface.  ANCHOR MOVED, DEBT LIVE — a second instance of the defect this file corrects for Phase 4. The row spells the file `WorkspaceHome.tsx`; at this HEAD it is `ui/src/features/workspaces/WorkspaceHome.tsx` (plural directory) and `:96-98` is a comment about the Tools catalogue, not a rank table. `git grep -rn -E 'ROLE_HIERARCHY|roleAtLeast' -- ui/src` → 44 hits, so the second vocabulary still stands. ~~`git grep -rn -E 'ROLE_HIERARCHY|roleAtLeast' -- ui/src` → 44 hits, so the second vocabulary still stands~~ — **evidence struck at `718dbe8e4`**: the 44 stand (re-counted, same total) but they do not show a second vocabulary. The split: **24** of the 44 are `ui/src/__tests__/role.test.ts` grading the ONE table, **6** are `role.ts` itself (1 declaration + the floor type + the comparator + 3 doc mentions), **5** are `WorkspaceHome.tsx` (1 import + 4 reads), **3** are `SettingsPage.tsx`, and the remainder is prose — comments in four other test files and one line in each of `settings.ftl` / `settings.id.ftl`. A count of mentions of one table is not a count of tables. <!-- Amendment: 2026-09-15 · docs · **NOT WORK — the duplication this row waits on was already paid, and the row was read from a superseded revision of the comment it cites.** Falsified at HEAD `718dbe8e4`, three ways. (1) The anchor is empty: `ui/src/features/workspaces/WorkspaceHome.tsx:97` is a BLANK line and `:98-99` hold the comment "(The Tools catalogue — entries, icons, groups, access policy — lives in ./tools and is imported below.)", which is where the drift was noticed. (2) The follow-up it closes says the opposite of a follow-up — `ui/src/utils/role.ts:31-35` reads "There is exactly one such table. `features/workspaces/WorkspaceHome.tsx` imports this export (`:13`) rather than inlining a copy of its own… The duplication an earlier revision of this comment called known-and-temporary **is gone, not pending.**" The row's `role.ts:32-35` pointer is live; what it says has been inverted since, so the reconciliation was written against an older reading of it. (3) The only table near the cited line is `WorkspaceHome.tsx:72-78`, `WS_ORDER`, a **workspace sort order** keyed by `type_key` (`restaurant-pos` 1, `store-pos` 2, `kds` 3, `warehouse` 4, `admin` 5) whose `admin` is the admin *workspace*, not the admin *role* — folding it would delete the card ordering and merge two keyspaces that coincide on one string. `WorkspaceHome.tsx:13` imports `ROLE_HIERARCHY` and its four comparisons (`:351`, `:360`, `:366`, `:402`) all read that import; a grep for every numeric-ranking shape in `ui/src` (`owner: N|admin: N|manager: N|staff: N|auditor: N`) leaves `role.ts:37` as the only production declaration. -->
  - **PAID 2026-09-15 ~16:35 at tip `75e5e5432` · TICKED — CLASS: ALREADY PAID, and unlike the 13:15 reading of this row there is now a named diff to point at.** `c8efd4b2a refactor(ui): fold WorkspaceHome ROLE_HIERARCHY onto the canonical utils/role table` is the fold (`git log -S 'const ROLE_HIERARCHY' -- ui/src/features/workspaces/WorkspaceHome.tsx` returns it and the commit that introduced the copy). Verified at this tip: `WorkspaceHome.tsx:13` is `import { ROLE_HIERARCHY } from '@/utils/role';`, a local `const ROLE_HIERARCHY` returns **0**, and the single owner is `ui/src/utils/role.ts:37`. The row's own anchors stay stale (`WorkspaceHome.tsx:97` is no rank table), which is why the tick cites `c8efd4b2a` rather than a line number.
- [ ] **3a.2 — Replace the rank comparisons with permission checks.** One gate at a time (`:363`/`:372`/`:378`/`:414`), each with a test that pins a **custom role holding the gate permission passing the same way a preset would** — that is the property the doctrine exists to protect, and a test that only covers presets does not pin it. `WorkspaceHomeTools.test.tsx` (9) + `WorkspaceHomeTools.navParity.test.tsx` (2) + `pageRegistry.test.ts` (28) are the existing parity surface — **39 tests across 3 files**, a figure recorded as *run* in an earlier pass on 2026-09-14, not re-measured here. Re-run them before relying on the number. <!-- 2026-09-15, attached here rather than added as a new row, because it is a property of exactly the comparisons this row replaces. FALSIFIED ASYMMETRY, measured at `718dbe8e4`: `WorkspaceHome.tsx:366` evaluates `roleLevel >= (ROLE_HIERARCHY[access.minimumRole] ?? 0)` -- an unrecognised `minimumRole` therefore demands **0** and the gate **FAILS OPEN**, while `roleAtLeast` at `ui/src/utils/role.ts:72` demands `Number.MAX_SAFE_INTEGER` for an unknown floor and fails CLOSED. Two consumers of one table, opposite defaults. NOT LIVE TODAY, and the reason is a type: `ToolRole = 'owner' | 'admin' | 'manager'` (`ui/src/features/workspaces/tools.tsx:25`) and the catalogue uses only those three (`admin` x4, `manager` x11, `owner` x2 by `grep -oE "minimumRole: '[a-z-]+'" | uniq -c`), all present in the table, so the divergence is type-blocked, not exercised. It was NOT routed through `roleAtLeast` in this pass on purpose: swapping a fail-open default for a fail-closed one is a change in who can see an admin tool, which is an owner call and not a refactor -- and this row's own instruction is to move one gate at a time with a test pinning it. Whoever takes 3a.2 should decide the default BEFORE the first gate moves, and pin it with a custom-role test, because that is the case both vocabularies currently disagree about. -->
- [ ] **3a.3 — Decide the fate of `roleAtLeast` / `ROLE_HIERARCHY`.** If nothing needs a rank after 3a.2, delete them rather than leaving a second vocabulary in place; `ui/src/__tests__/role.test.ts:2` is the test that has to change with them.
- [ ] **3b.1 — Design doc first, no code.** Organisation-wide and terminal-level scope needs its own design before any migration: which entity owns the organisation axis, how a terminal-scoped assignment resolves at the enforcement boundary (ADR #4 keeps store data in per-store DBs while roles live in the global identity DB), and what "explicit `all`" means on each new axis. Treat the design doc as the deliverable for this box — a worker that writes a migration without it is inventing the axis the owner has not ruled on.
- [ ] **3b.2 — Implement only after 3b.1 is accepted.** Migration + evaluation + gate wiring + tests, following the `assignments` precedent from 0048 cycle 1.
- [x] **Small adjacent defect, fix while in the file.** `ui/src/features/workspaces/WorkspaceHome.tsx:561` [full path, was the bare basename -- the same capped-handler grep also lands on `ui/src/features/kds/hooks/useKdsKeyboardShortcuts.ts:91`, which has a capped handler and NO uncapped hint, so a basename-only row invites fixing the wrong keyboard surface] accepts only keys `1`–`9` (`if (e.key >= '1' && e.key <= '9' && …)`), but the hint renders `Press {idx + 1} to open` uncapped — `<Localized id="workspace-home-shortcut-hint">` opens at `:860`, the text is at `:861` — so a card past index 9 advertises a shortcut that does nothing. Either cap the hint or extend the handler; pick one and pin it with a test. <!-- DONE 2026-09-15 at `e51fa247c` (fix(ui): stop the keypad hint advertising a key the handler refuses). **Direction decided on evidence, not taste:** the handler maps a key with `parseInt(e.key, 10) - 1` -- ONE key character -- so the addressable indices are 0-8 and index 9 has no single-key name at all ("10" arrives as two keydowns, `1` then `0`, and the second is outside the accepted range). The cap is therefore the design and the LABEL was the lie, so the label is what changed: the overlay hint is gated on `idx < MAX_DIGIT_SHORTCUT` (= 9), declared with that reasoning beside `WS_ORDER`. No keystroke was altered, no key added, and cards were NOT renumbered, which would only move the lie to the last slot. **Re-taken cites** (the row's own were stale by the time it was paid): handler `:561` -> **`:549`** with the mapping at **`:552`**; hint `:860`/`:861` -> **`:848-849`**. Also established while paying it: the list the handler indexes and the list the hint renders are the SAME list -- `sortedWorkspaces` filters `admin` out at `:329` before both -- so there is no off-by-one between label and target beyond the cap, and a 10-workspace fixture that reuses the admin type renders 8 cards, which is the filter working. **Pinned by a test written first, whose red is quoted in that commit:** `WorkspaceHome.test.tsx > keyboard shortcuts > advertises a digit shortcut only for the cards that digit can reach`, red at `every one of the 10 cards should not carry a digit hint: expected 10 to be 9`, green at 49 passed in that file and 116 across the eight suites that exercise the surface. **Why this row gets a tick and 3a.1 does not:** work was performed HERE -- two files changed, one of them a test that fails without the change -- so the tick claims something was done and it was; 3a.1 was dispositioned NOT WORK because the duplication had already been paid by someone else in an earlier commit and this box changed no file, and a tick on a retired row reads as completed work that no reader can find a diff for. DONE is the only disposition that takes a tick; SUPERSEDED, NOT WORK and PARKED stay un-ticked with their reason. Counts move accordingly and both forms agree: 23 open / 2 ticked -> **22 open / 3 ticked**, the completion count rising by exactly one, with zero indented checkbox rows in this file so the anchored and any-depth greps cannot disagree about it. -->
- [ ] **Commit milestones** — e.g. `refactor(ui): gate the workspace tools section on the permission vocabulary`.
  - **2026-09-15, docs audit at HEAD `460e33285`:** NOT WORK — same reason; the thing this phase actually waits on is the 3b product ruling named in the row above.

### Why this phase needs a product ruling before 3b

Nothing about the organisation axis is inferable from the code: `roles` has no hierarchy column, the assignment tables carry branch and workspace dimensions only, and 0048's own non-goals section declined this layer. A worker that invents the axis will produce a migration the owner then has to reject.

---

## Phase 4 — Payment: the remainder is smaller than the master doc suggests, and mostly blocked

**Fence:** `crates/kasirmu-payment/src/**`, `crates/kasirmu-hal/src/drivers/edc/**`, `apps/cloud-server/src/payment_api.rs`, `apps/cloud-server/src/webhooks.rs`, `ui/src/features/sales/**`.
**Commit prefix:** `feat(payment):` · `refactor(payment):` · `docs(payment):` · `test(payment):`
**Acceptance:** `cargo test -p kasirmu-payment` green · `cargo test -p kasirmu-hal` green · `cd ui && npm run check:all` (see the gate list below) · `python scripts/verify-ipc-parity.py` → `IPC parity: OK`.

### Read this before reading `todo-payment.md`

`todo-payment.md` is a **research artifact written before the epic**, not a status surface. Its own sibling declares this outright (`todo-payment-agents-4.md:53-61`):

> *This document is the single status authority for the payment epic, and the per-item verdicts in this section — the R1–R7 headings with their own CLOSED/OPEN lines — are **CANONICAL**. … `todo-payment.md` is the research artifact, not a status surface.*

The master doc still shows **36 any-depth open boxes / 11 done** (`grep -cE '^[[:space:]]*- \[ \]' todo-payment.md` → 36; `…- \[x\]` → 11, measured this pass). **Those boxes are not a triage queue.** Roughly half have shipped under other commits than the order that planned them, and several were superseded by decisions the epic made on the wire. The epic closed as agents-1…3 plus `09eec83868`.

### The genuine remainder — R4 through R7

| Item | Status | What it actually needs |
|---|---|---|
| **R4** Multi-terminal EDC | OPEN | A **second real terminal** to exist. Single implicit terminal ships; `crates/oz-core/src/db/edc_terminals.rs:5` reads *"commands should take a terminal_id once more than one terminal is configured"* (read this pass). The interim default is an alias, not a design — `platform/startup/src/hardware.rs:203-212` says so in its own words: *"The alias is interim, not design: `edc_terminals` has no `is_default` column, so 'which terminal is this register's' is answered by creation order"* (read this pass). |
| **R5** Resilience cluster | OPEN | A **design doc first**. `crates/oz-payment/src/registry.rs:57-67` `build_from_config` is a documented PLANNED stub failing closed; no `method -> Vec<processor>` chain, no `ResilientProcessor`, no expiry/reconciliation job. Largest remaining slice, cloud + crate blast radius. `[carried]` |
| **R6** Evidence debt | OPEN | **Credentials and a merchant account, not code.** Sandbox probe for generic-QR interop, targeted-QR restriction, refund behaviour, per-merchant acquirer activation. `[carried]` |
| **R7** No real EDC hardware driver | OPEN | **Real hardware or vendor protocol documentation.** Every layer of the EDC chain shipped except the one that touches money-holding hardware. `[carried]` |

> **Correction forward, 2026-09-15 ~19:55 at tip `13f5d5262` — the R4 row above is superseded as to its BLOCKER, and stands untouched as the dated reading it was.** `13f5d5262 docs(plans): record R4/R6/R7 as blocked-on-input with each missing input named` measured that what R4 actually waits on is **a product decision — what binds a register to a terminal: an `edc_terminals.is_default` column, or commands taking an explicit `terminal_id` plus a UI that supplies it** — not the existence of a second terminal. Three measurements, re-taken at this tip and each re-derivable in one pass, so a reader checks rather than trusts: (1) `grep -c is_default crates/oz-core/migrations/20260824_media_edc.sql` -> **0**, while that table does carry `is_active`, so the schema cannot even express a default — the row's premise is not merely unbuilt but unrepresentable; (2) `platform/startup/src/hardware.rs:203-212` answers the question which terminal a register belongs to by **creation order** and names itself interim in its own words; (3) the alias is defined at `crates/oz-bridge/src/edc.rs:27` and consumed at `:79`, with **8 non-test uses across `crates/`, `platform/`, `apps/`** (`grep -rn DEFAULT_TERMINAL_ID crates platform apps --include='*.rs' | grep -v '_tests|/tests/' | wc -l`) — the price of either choice, and it is small. **Hardware is needed only to VERIFY the outcome of the ruling, not to make it**, so the honest conclusion below this line — 'R4 waits on hardware' — holds for R4's proof-of-life half and not for its schema-and-plumbing half, which waits on an answer the owner can give from this checkout today with no box, no terminal and no vendor. Recorded as a dated line under the row rather than by editing it: the row's own text says 'read this pass', and a dated record is corrected forward, never back.

### The honest conclusion about this phase

**Three of the four items cannot be started by a coding agent from this checkout.** R4 waits on hardware, R6 waits on credentials, R7 waits on hardware or vendor docs. Only R5 is code-workable, and it requires a design pass before its first box. Phase 4's real deliverable is therefore **documentation and a decision record**, not code — and it is sequenced last for that reason. Do not dispatch a lane to "work the payment backlog": the backlog is not the list of open boxes.

### Anchor correction — the EDC stub paths are recorded without their crate

`todo-payment-agents-4.md:137-139` cites `drivers/edc/wired.rs` (112 lines), `wireless.rs` (132) and `protocol/{pax,ingenico,verifone}.rs` (46 lines each). **Those paths are correct only relative to `crates/oz-hal/src/`.** Measured this pass:

```bash
wc -l crates/oz-hal/src/drivers/edc/wired.rs crates/oz-hal/src/drivers/edc/wireless.rs \
      crates/oz-hal/src/drivers/edc/protocol/{pax,ingenico,verifone}.rs
#  112 / 132 / 46 / 46 / 46   ← the line counts reproduce exactly
```

`crates/oz-payment/src/drivers/` contains `mock.rs`, `paddle.rs`, `qris.rs`, `square.rs`, `stripe.rs` — **no `edc/` and no `protocol/` directory**, and `grep -rn "HalError::Unsupported" crates/oz-payment` → **0**. The stubs moved into `oz-hal` during the HAL unification; `crates/oz-hal/src/drivers/edc/wired.rs:2` says so in its own header: *"moved in from oz-payment during the HAL unification"*. A worker who greps the recorded path finds nothing and may conclude the stub is gone. Both files fail closed with `HalError::Unsupported` (2 occurrences in `wired.rs`, 2 in `wireless.rs`, 1 in `pax.rs`, measured).

### Tasks

- [x] **Triage, not implementation.** Re-run the agents-4 method — every open box in `todo-payment.md` re-tested with `git grep` against the code — and produce a fresh verdict table. The prior pass's own recorded limit is instructive: *"A box-by-box audit of claims catches lies; it does not catch true-but-misleading summaries"* — R7 was invisible to the checkbox pass and was found only by reading a stub's doc comment. Read the code, not the boxes.
  - **CLOSED 2026-09-15 (`08b11846e docs(payment): close the nine boxes the code already implements and name the twenty-one parked`, `todo-payment.md` only, +22/-14), and re-counted here rather than by that subject line: at HEAD `460e33285` `todo-payment.md` reads 30 open / 20 ticked by BOTH counting forms.** The verdicts were written per row inside `todo-payment.md` instead of as a separate table, which is what this box asked for in substance. What it does NOT settle, and stays open beside it: **R5 design doc** (no such file in `docs/plans/`) and the **R4/R6/R7 decision record**.
- [ ] **R5 design doc** (`docs/plans/`), covering the `method -> Vec<processor>` chain, `ResilientProcessor`, the breaker's `(tenant_id, gateway)` keying, and the expiry/reconciliation job. No code until accepted.
- [x] **R4/R6/R7 decision record.** State each as blocked-on-input with the specific input named, so the next reader does not re-open them as "unknown". Include the correction above so the next anchor search starts in `oz-hal`.
  - **R4 Multi-terminal EDC — BLOCKED ON A RULING, NOT ON HARDWARE, and the table above at `:249` names only the hardware half (2026-09-15 ~19:35, read-only pass at tip `3981e8916`).** Absent input, stated precisely: **a product decision on what binds a register to a terminal** — either an `edc_terminals.is_default` column, or commands that take an explicit `terminal_id` and a UI that supplies it. Measured here, not carried: `edc_terminals` (migration `crates/oz-core/migrations/20260824_media_edc.sql`) has `is_active` and **0 occurrences of `is_default`**, so `platform/startup/src/hardware.rs:203-212` answers the question by creation order and calls that interim in its own words; the alias is defined at `crates/oz-bridge/src/edc.rs:27` and consumed at `:79` (`.terminal(DEFAULT_TERMINAL_ID)`), with **8 non-test uses across `crates/`, `platform/` and `apps/`** — that count is the price of the change, and it is small. **Who can supply it: the owner, from this checkout, today — no box, no terminal, no vendor.** A second physical terminal is needed only to VERIFY the outcome, not to choose it, so R4's schema-and-plumbing half is waiting on an answer while only its proof-of-life half waits on hardware. **What a lane CAN do meanwhile without guessing:** record the call sites and the price (done, above), and note that `platform/startup/src/hardware.rs:168` says it mirrors `oz_pos_app::commands::edc::DEFAULT_TERMINAL_ID` while the only definition outside that file is now in `crates/oz-bridge/src/edc.rs` — a stale mirror comment a lane may fix without touching behaviour. What a lane must NOT do is add the column: it encodes the unanswered choice.
  - **R6 Evidence debt — BLOCKED ON A NAMED CREDENTIAL, and the green that hides it is designed in (2026-09-15 ~19:35, same pass).** Absent input, named exactly: **a Midtrans/Xendit sandbox `MIDTRANS_SERVER_KEY` attached to a QRIS-enabled merchant account** — the variable the code actually reads at `crates/oz-payment/src/drivers/qris.rs:306` (siblings: `STRIPE_SECRET_KEY` at `stripe.rs:192`, `PADDLE_API_KEY` at `paddle.rs:91`). **Who can supply it: the owner, who already keeps this class of secret as user-scope `OZPOS_*` environment variables per `AGENTS.md`; this one has no `OZPOS_` twin, and that is the whole gap.** The finding worth more than the blocker: **a missing key does not read as a failure.** `crates/oz-payment/src/drivers/qris_tests.rs:39-49` asserts in BOTH directions — `Ok(_) => assert!(result.is_ok())`, `Err(_) => assert!(...contains("not set"))` — so the suite is green with and without the credential; and all 21 tests in `crates/oz-payment/tests/qris_integration.rs` run against a local `wiremock` server with **0 `#[ignore]`**, so they grade our side of the contract only. Four claims therefore stay unproven by anything in this repo: generic-QR interop, the targeted-QR restriction, real refund behaviour, and per-merchant acquirer activation. **So do not let a future lane cite a green QRIS suite as R6 evidence — it cannot be evidence.** What a lane CAN do meanwhile: nothing that produces evidence; it can keep the mock suite honest, and it can write down which four acquirer behaviours the mock cannot see, which is what this bullet is.
  - **R7 No real EDC hardware driver — BLOCKED ON A WIRE PROTOCOL, and the repo holds no copy of one (2026-09-15 ~19:35, same pass).** Absent input: **the vendor framing for one named target model** — a spec, or better a single captured byte trace from a real terminal — plus the terminal itself to check the answer against. Measured here: `crates/oz-hal/src/drivers/edc/wired.rs` **112 lines**, `wireless.rs` **132**, and `protocol/{pax,ingenico,verifone}.rs` **46 each**, all self-labelled PLANNED stubs returning `HalError::Unsupported` (2 in `wired.rs`, 1 in `pax.rs`), and `protocol/protocol_tests.rs` holds **0** fixtures — no spec text, no capture, no golden vectors anywhere in that directory. **Who can supply it: the owner, via a vendor relationship — or a lane, cheaply, by asking which single merchant terminal model is the target, because the input needed is 3 documents for 3 vendors or 1 capture for 1 vendor.** **Anchor correction the row asks for, restated so this bullet is the last place a reader needs to look: the EDC paths live under `crates/oz-hal/src/drivers/edc/`, and the live citation is `.agents/done-todo-payment-agents-4.md:116`, `:126`, `:127` — read here and crate-prefixed at all three, but note its state: that file is UNTRACKED (`??`) and the root copy is ` D`, so those numbers are a working-tree reading of another session's half-finished move, not a commit fact, and they will need re-deriving once the move lands — the root `todo-payment-agents-4.md` shows ` D` with untracked successors from another session's in-flight move and is NOT citable, so its line numbers are not quoted anywhere in this file.** What a lane CAN do meanwhile, and it is not nothing: a loopback terminal simulator over the existing transport so the state machine — timeout, retry, cancel, receipt, fail-closed on an incomplete read — is covered by tests that need no vendor at all. What a lane must NOT do is invent framing from a plausible reading of a third-party document: an invented codec passes its own tests and fails at a counter.
  - **TICKED 2026-09-15 ~19:55 at tip `13f5d5262`, on the ruling that this row's acceptance is a decision document, not a build.** Its text asks for each of R4/R6/R7 'stated as blocked-on-input with the specific input named, so the next reader does not re-open them as unknown', and the three bullets above do exactly that, each naming its input as a variable, a column or a document: **R4** = the register-to-terminal ruling, priced at 8 alias sites; **R6** = a Midtrans/Xendit sandbox `MIDTRANS_SERVER_KEY` on a QRIS-enabled merchant account, read at `crates/oz-payment/src/drivers/qris.rs:306`, with its trap named beside it (`qris_tests.rs:39-49` asserts in both directions, so a green QRIS suite cannot be R6 evidence); **R7** = a vendor wire protocol for one named model, the repo holding no spec, no capture and 0 fixtures across stubs of 112/132/46/46/46 lines that all fail closed with `HalError::Unsupported`. **The tick closes the RECORD, not the three requirements** — R4, R6 and R7 stay OPEN in the table above and nothing here unblocks them; and the contradiction this record exposed between `:249` and its own findings is met by the dated correction-forward line under that table, not by editing the dated row. One item identified and deliberately left undone, because a docs box that edits Rust is how tonight's collisions started: the stale mirror comment at `platform/startup/src/hardware.rs:168`, which cites `oz_pos_app::commands::edc::DEFAULT_TERMINAL_ID` as its source while the only definition outside that path is now in `crates/oz-bridge/src/edc.rs`, is a one-line code fix and is not made here.
  - **POINTER REFRESH ONLY, 2026-09-15 ~20:55 at tip `1264c943a` — the three entries above were re-derived against HEAD, not the working tree, and two of their line pointers had rotted by a line or two while their substance held.** Moved: the P-2 comment quoted in the R4 entry is at `platform/startup/src/event_handlers.rs:84-85`, not `:82-83` (the enqueue itself is still at `:87`, so the mechanism is unchanged); and the both-directions assertion quoted in the R6 entry opens at `crates/oz-payment/src/drivers/qris_tests.rs:40`, not `:39` (`fn qris_from_env_missing_key`, still asserting `Ok(_) => assert!(result.is_ok())` against `Err(_) => …contains('not set')`). Held exactly as written, re-checked at this tip: `crates/oz-bridge/src/edc.rs:27` definition and `:79` consumption; the 8 non-test `DEFAULT_TERMINAL_ID` uses across `crates/`, `platform/`, `apps/`; `grep -c is_default crates/oz-core/migrations/20260824_media_edc.sql` -> **0**; `crates/oz-core/src/db/offline.rs:155` falling through to `SyncPriority::Normal`; `crates/oz-payment/src/drivers/qris.rs:306` reading `MIDTRANS_SERVER_KEY`; the `ui/src/api/offline.ts:67`-`:113` export set with no enqueue among them; and the R7 stub sizes at 112 / 132 / 46 / 46 / 46 lines. **Nothing here changes a blocker, an input or a who-supplies-it: R4 still waits on the register-to-terminal ruling, R6 on a QRIS-enabled merchant account's sandbox key, R7 on a vendor wire protocol for one named model — none of the three has been paid by any commit since, which was checked with `git log 13f5d5262..HEAD -- crates/oz-hal/src/drivers/edc crates/oz-payment platform/startup/src/hardware.rs crates/oz-core/src/db/offline.rs crates/oz-core/migrations` returning nothing at all.** Recorded as a dated line under the row rather than by editing the three entries: those bullets are dated measurements and this page corrects forward, never back. No box ticked, no requirement re-opened; note also for whoever acts on this that the row above is already ticked by `2ff2182a4`, so the open work is not the record but the three answers.
- [x] **Fix the anchor in `todo-payment-agents-4.md`** — add the crate prefix to the R7 paths. This is a one-line docs fix and it is the reason a worker would otherwise lose an hour.
  - **2026-09-15, docs audit at HEAD `460e33285`:** STILL UNFIXED, and its target is outside this fence — `todo-payment-agents-4.md:126` and `:137` still print `drivers/edc/wired.rs` / `wireless.rs` with no crate prefix. Reported here, not edited there: the file belongs to another lane, and the discipline two lanes used tonight applies (supersession recorded by title and path, other file untouched).
  - **PAID 2026-09-15 ~16:35 at tip `75e5e5432` · TICKED, and the path it fixed is now DEAD — both halves named, neither merged.** The one-line repair landed as `f09c5e28e docs(plans): give the payment-agents-4 pointers their crates`: `git show HEAD:todo-payment-agents-4.md` line 126 reads `crates/oz-hal/src/drivers/edc/wired.rs`, `.../wireless.rs` and `.../protocol/{pax,ingenico,verifone}.rs`, crate-prefixed, which is what an R7 hunt needed. **Then the file moved:** `git status --porcelain -- todo-payment-agents-4.md` -> ` D`, another lane's in-flight move with no SHA behind it — so this row is paid against committed content while its target path is absent from the working tree. Reported as the state, not edited, not renamed, not claimed by this tick.
- [ ] **Commit milestones** — e.g. `docs(payment): correct the EDC stub crate in the remainder inventory`.
  - **2026-09-15, docs audit at HEAD `460e33285`:** NOT WORK — same reason, and the docs fix it follows is still unfixed (see the anchor row above).

### `check:all` — the gate list, stated from the file

`ui/package.json:11` is `"check:all": "node ../scripts/check-ui.mjs"`. `scripts/check-ui.mjs` runs **eight numbered gates** plus a manifest self-audit:

1. Lint (`:123`) · 2. TypeScript (`:126`) · 3. Unit tests (`:129`) · 4. i18n lint (`:132`) · 5. FTL dedupe (`:135`) · 6. Bundle budget (`:138`) · 7. E2E (`:141`, Docker-gated) · 8. Perf smoke (`:153`, skipped without Playwright browsers) — then the gate-manifest self-audit against `scripts/gates.json` (`:169`), which fails closed if a manifest `check:all` gate is not declared in the runner.

Two recorded traps for whoever runs it: the perf-smoke leg cannot pass inside a sandbox whose delete shim refuses Playwright's cleanup of `ui/test-results`, and that is an environment limit, not a repo defect; and the gate list is **eight + manifest**, not the five-step chain some documents still describe.

---

## Dispatch order and dependencies

```
Phase 1 (release red)  ──┬─→ must classify BEFORE Phase 2 registers more tablet commands
                         └─→ needs an OWNER RULING on the parked arm
Phase 2 (tablet wire)  ──→ independent of Phase 3; keep fences disjoint from Phase 1
Phase 3 (authz)        ──→ 3a executable now; 3b needs a design doc, then a ruling
Phase 4 (payment)      ──→ docs first; R5 design doc is the only code-adjacent deliverable
```

- **Phase 1 and Phase 3a can run concurrently** — disjoint fences, no shared file.
- **Phase 2 should follow Phase 1's classification**, not run alongside it: both touch the tablet surface that Phase 1's release failure is live on.
- **Phase 4 is last and mostly blocked.** Dispatch it only for documentation.
- **Nothing here authorises a push.** `dev-ci.yml:695` gates the deploy on `push` to `main` **or** to a `0.0.*` branch, and the `0.0.*` arm is currently inert only because `on.push.branches` lists `main` alone. Widening that list would make every release-branch push deploy to production.

---

## Claims that have rotted — do not quote these

Recorded because each one is still live in some document a worker might read, and each was re-measured this pass.

| Rotted claim | Where | Measured now |
|---|---|---|
| `PaymentModal.tsx = 2,436` lines | `todo-payment.md` audit stamp | **1,999** (`wc -l ui/src/features/sales/PaymentModal.tsx`; 89,855 bytes, trailing newline present). Was 2,436 at `ec2edf258` (`git show ec2edf258:… \| wc -l` → 2436) — the refactor campaign shrank it via `79d96f7c9` (cash tender panel extracted), `1583ff08b`, `e455e9d13` |
| box census `39 unchecked / 8 checked` | same stamp | **36 / 11** any-depth. The stamp's own claim that its counting method "equals `wc -l` here" was about line counts, not boxes |
| the master doc's open boxes are the work | same stamp + `## Phased TODO` | they are absorbed or superseded; `todo-payment-agents-4.md:53-61` declares R1–R7 canonical |
| `payment:qris-manual` / `:midtrans` / `:edc` feature keys | `todo-payment.md` Phase 0/2/3 | **never existed in code.** One occurrence repo-wide, and it is a comment saying so: `ui/src/features/sales/useLocalPaymentRails.ts:4` |
| `drivers/edc/*.rs` | `todo-payment-agents-4.md:137-139` | the files are real but live under `crates/oz-hal/src/`, not `crates/oz-payment/src/` |
| `.circleci/workflows/06-cargo-nextest.yml:37` | several plans | retired by `e3aff7b56` (2026-09-14); `git ls-files \| grep -c circleci` → 0. Use `git show e3aff7b56^:<path>` |
| "a push to `main` deploys" as a one-liner | older notes | still true, but incomplete — see `dev-ci.yml:695` |
| `cargo test -p oz-bridge --release` = **76 failed** | `:105` (and the audit stamp, `:106`, `:112`) | **1 failed** — 1315 passed / 1 failed in 195.65s, re-run this pass. The survivor is *deliberately* red; see the Phase 1 re-measurement block. |
| `todo-font-system.md (5 open)` | `:334`, out-of-scope list | **0 open / 13 ticked** — closed earlier today. Its 5 became 0 without this file noticing, the same census-rot `:504` names for `:404`. |

---

## Out of scope for this program

- `todo-font-system.md` (5 open) · `todo-refactor-kds-agents-merged.md` (18 open) · `todo-refactor-pos-screen-agents-3.md` (21 open) · `todo-refactor-settings-agents-3.md` (11 open) — any-depth box counts, measured this pass. Each is its own plan; none is covered here.
- `todo-kds.md` is **superseded** by `todo-refactor-kds-agents-merged.md` and carries 0 boxes — do not treat its absence of boxes as completion.
- `todo-tools.md` and `done-todo-tools-agents-3.md` each hold one open box that their own retirement notes assign elsewhere: the org/terminal scope is Phase 3b here; `check:all` is Phase 4's acceptance surface.
- Anything requiring a push, a branch, or a version bump.

---

## Open items this file deliberately leaves open

- **The release-profile failure set is now measured, but not yet classified.** `cargo test -p oz-bridge --release` → 1231 passed / 76 failed at HEAD `257ff6122`, reproducing the earlier record exactly. *Why* each of the 76 fails is still open, and Phase 1's second box exists to close it.
- **The parked arm** (`license.rs:670-683`) needs an owner ruling. Named as a task, not decided.
- **The organisation scope axis** needs a design doc before a migration. Named as a task, not decided.
- **Whether this file should be split** into `todo-open-debt-agents-N.md`. Owner's call; the fences already support it.
- **The commit-message scope token is unchecked, and this file's author left proof of it.** `.githooks/commit-msg:53` enumerates types (`feat|fix|docs|style|refactor|perf|test|ci|chore|audit`) but `:55` is `^($TYPES)(\([^()]+\))?!?: .+` — the scope is only "non-empty and parenthesis-free", with no allowlist, exactly as its own comment at `:54` says ("Optional scope"). A typo in the area is therefore accepted and, because this repo forbids `--amend`, is permanent: `f18b62f45` shipped as `docs(clans):` instead of `docs(cloud-sync):`, noticed only after the commit, and the only available repair is a second commit that does not fix the first. Deliberately left as a named item rather than filed as a box: an allowlist has a maintenance cost that grows with the repo (new crate, new area, new gate to teach), and whether that trade is worth making is a policy call, not a docs-lane call. Cheap version, if the owner wants one: derive the legal areas from `crates/*/`, `apps/*/`, `platform/*/` plus a short prose list (`plans`, `docs`, `ci`), and warn rather than fail.

---

## Box-count reconciliation (2026-09-15, docs audit, HEAD 9ab4e58da) — no historical number above is rewritten

Two places in this file state a box total beside a command that no longer produces it. The commands as written are correct in form; the numbers are stale. Re-measured with this file's own `:59` any-depth pattern, brackets backslash-escaped:

- `:227` states **36 any-depth open / 11 done** for `todo-payment.md`. Today: `grep -cE '^[[:space:]]*[-*][[:space:]]+\[ \]' todo-payment.md` -> **30**, and the same pattern with `\[[xX]\]` -> **20**. Six boxes were ticked after that line was written, which is the line's own success, and the `11 done` figure cannot be reproduced by any form now on the tree.
- `:307` states `todo-refactor-kds-agents-merged.md` **18 open**, `todo-refactor-pos-screen-agents-3.md` **21 open**, `todo-refactor-settings-agents-3.md` **11 open**, `todo-font-system.md` **5 open**. The canonical command returns **7**, **11**, **2** and **5** respectively. One of four reproduced; three did not, and the direction is not uniform — two overstate and one understates — so this is not one stale snapshot but a census nobody re-ran. The `5 open` for `todo-font-system.md` is the only one of the four that a reader may still trust, and only until the next tick.

Canonical pair, marker- and indent- and bracket-spacing-tolerant, for use in any brief or clause in this tree:

```bash
grep -cE '^[[:space:]]*[-*][[:space:]]+\[[[:space:]]\]' file.md   # OPEN boxes
grep -cE '^[[:space:]]*[-*][[:space:]]+\[[xX]\]'        file.md   # TICKED boxes (accepts [X])
```

Two traps these lines exist to close, both measured the same day: (1) **unescaped brackets are not literals in ERE** — `grep -cE '^- [ ] '` returns **0** on `todo-font-system.md`, which holds **5** open boxes, because `[ ]` is a bracket expression matching one space, so the pattern silently asks for hyphen-plus-three-spaces. A zero from that form is a regex artifact, never a harvest. (2) **anchoring the marker to column zero under-counts TICKED and only ticked** — `todo-refactor-oz-pos-app-agents-3.md` reads **22** with `^- \[[xX]\]` and **43** with the canonical line, because 21 of its boxes are indented sub-rows, while its open count is 4 either way (no indented or `*`-marker open box exists anywhere in the 19 root plans, so open counts are form-insensitive today and that is luck, not a property). A completion ratio computed from anchored numerators and any-depth denominators is wrong in both directions at once.

## Reconciled against what this tree closed — 2026-09-15 (docs audit at HEAD `460e33285`)

This section is the ledger read against tonight, not a new work list. Every number is a measurement taken this pass, grep and read only — no suite, no cargo, no build, no Docker.

- **This file's own census, by both forms the Counting method section at `:53-62` names — RE-DERIVED 2026-09-15 at HEAD `6008279b9`, because the numbers this line carried (24 open / 1 ticked at `460e33285`, then 23 / 2 after that pass) had rotted while the file gained rows, which is the ordinary failure of a page that counts its own open items in prose:** `grep -c '^- \[ \]' todo-open-debt-program.md` → **27 open**, `grep -cE '^[[:space:]]*- \[ \]' todo-open-debt-program.md` → **27 open**, and the two ticked counterparts `grep -c '^- \[[xX]\]'` and `grep -cE '^[[:space:]]*- \[[xX]\]'` → **3 ticked** each. The two methods AGREE here, and the reason is the page's own at `:62`: it has no indented checkbox rows — the only indented lines under a row are non-marker sub-bullets (`:107-108`, `:154`) — so the unindented and any-depth forms cannot diverge, and the 22-row under-read a nested plan suffers cannot happen here. A dated figure is allowed and an undated assertion is what rotted; the rule that a census must state its method already lives at `:55` and is not mine to rewrite.
  - **Census moved the same day, one row later, and is recorded rather than back-edited:** ticking the Phase 2 audit row at `:154` takes this file to **26 open / 4 ticked**, total **30** unchanged — measured at tip `5802101ca` by the canonical pair `grep -cE '^[[:space:]]*[-*][[:space:]]+\[ \]'` → 26 and its `\[[xX]\]` counterpart → 4, the anchored forms agreeing, `grep -cE '^[[:space:]]+- \[[ xX]\]'` → **0** indented checkbox rows so the two forms still cannot disagree. The asymmetry is the whole point of a completion: open falls by one, ticked rises by one, the denominator does not move. The audit's own dated note sits under `:154` as a non-marker sub-bullet, so it adds no box to any of these counts.
  - **Moved again the same day, by the row beneath it, and recorded forward rather than back-edited:** ticking **Then share the DTOs** takes this file to **25 open / 5 ticked** with the total still **30** — same canonical pair, open `grep -cE '^[[:space:]]*[-*][[:space:]]+\[ \]'` → 25, ticked `\[[xX]\]` → 5, and `grep -cE '^[[:space:]]+- \[[ xX]\]'` → **0** indented checkbox rows, both Phase 2 notes being non-marker sub-bullets. Two closes in one day and identical arithmetic each time: open −1, ticked +1, denominator fixed. The 26 / 4 directly above is left verbatim as the dated record it is.
- **2026-09-15, the same failure mode checked against its neighbours at HEAD `718dbe8e4`, read-only and un-edited except where the falsification is as clean.** Three rows in this section cite `WorkspaceHome.tsx` lines that no longer hold what they name, but they do not all mean the same thing, and the flat `ANCHOR MOVED, DEBT LIVE` label cannot tell them apart:
  - **3a.1 — ANCHOR MOVED BECAUSE THE DEBT WAS PAID.** Disposed above as NOT WORK.
  - **3a.2 — ANCHOR MOVED, DEBT LIVE, pointers stale.** It cites `:363`/`:372`/`:378`/`:414` for the rank comparisons; at this tip `:363` is blank, `:372` is a comment, `:378` and `:414` are arrow-function signatures, and the four comparisons actually sit at `:351`, `:360`, `:366`, `:402`. The premise still holds, so the row stands — but its four pointers are off, which is the same drift that made 3a.1 look live.
  - **"Small adjacent defect, fix while in the file" — ANCHOR MOVED, DEBT LIVE, and the defect re-confirmed.** It cites `:561` for the digit handler and `:860`/`:861` for the hint; at this tip the handler is `:549` (`if (e.key >= '1' && e.key <= '9' && !e.ctrlKey && !e.altKey && !e.metaKey)`), `:561` is `const active = document.activeElement;`, and the hint is `:848`-`:849` (`vars={{ key: \`${idx + 1}\` }}` over `Press {idx + 1} to open`) while `:860`-`:861` are two `</div>`s. Capped handler, uncapped hint: still true, so this row is correctly open and only its line numbers need re-taking. **Superseded in the row itself at `e51fa247c`, where it is now DONE** -- the re-confirmation was correct when written (the defect was live and un-taken), and it is what made the fix land on the right lines rather than the row's own; the bullet above stays verbatim as that dated reading.
  - **3a.3 — cite still accurate.** `ui/src/__tests__/role.test.ts:2` is the import of `normalizeRole, roleAtLeast`, so the row's premise about the test that has to change with the vocabulary holds.
  **Marker, and the rule it broke:** this row was briefly ticked by `784d3fcae` and the tick is undone by the commit that adds this line, because a tick claims completed work and this clause claims none -- NOT WORK rows stay un-ticked with their reason, exactly so a later reader cannot mistake a retired row for a paid one. `git show 784d3fcae -- todo-open-debt-program.md` prints the offending `-`/`+` pair (`- [ ]` -> `- [x]`), so the mark is attributed rather than assumed. **And a count that is not re-derivable by the command a reader would reach for is the same class of defect as a line number that aged:** the commit body of `784d3fcae` says the NOT WORK count went 7 -> 8; measured, the string count is `grep -c "NOT WORK" todo-open-debt-program.md` = **6 before, 8 after**, and the row-line form `grep -cE '^- \[.\].*NOT WORK'` gives **0 before, 1 after** because the four earlier labels sit on dated sub-bullets and in prose, not on a row line -- so that command cannot re-derive the older prose figure of "4 marked NOT WORK", (the count is 9 at the line you are reading, because this correction adds a *mention* of the phrase, not a disposition -- string counts of a label move when the label is discussed as well as when it is applied, which is the last reason to cite the command) and the census numbers that DO hold under both forms are 23 open / 2 ticked before and after this correction, with no completion count moved.
  The general form worth carrying: a row that cites a *comment* to establish pending work must re-read that comment, not confirm that the pointer resolves. `role.ts:32-35` resolved and still said the opposite of what the row believed, which is how a paid debt survived one reconciliation.

- **Still a live defect (re-measured):** the `#[cfg(debug_assertions)]` tier swap at `crates/oz-bridge/src/locations.rs:231-236` ahead of `enforce_location_quota` at `:237`; `crates/oz-bridge/src/sync_tests.rs:35-37` still `#[cfg(test)]` + `#[cfg(debug_assertions)]`; the parked arm at `crates/oz-bridge/src/license.rs:670-673`; **no `--release` leg anywhere** — `git grep -n -e '--release' -- .github/workflows/dev-ci.yml` → NOMATCH, and `scripts/release.sh:62,65`, `.github/workflows/release.yml:149` and the `test workspace (nextest)` step in `scripts/check.sh` (`grep -n 'test workspace (nextest)' scripts/check.sh`) all carry `--exclude oz-pos-app --exclude oz-pos-tablet` with no release profile; Phase 2 has shared **nothing** since its own table (`git grep -rn -F 'pub use oz_bridge' -- apps/tablet-client/src/commands` → **11**, the same number the scope table records); the rank vocabulary is still standing (`git grep -rn -E 'ROLE_HIERARCHY|roleAtLeast' -- ui/src` → **44**); no R5 design doc in `docs/plans/`.
- **No longer describes what it was written against (disposed in place, no tick):** **Classify each of the 76** — the per-test arm sits downstream of one seed now, and the owner page carries the profiling question as **item 14, "Is a seeded sentinel debt at all if no CI leg ever compiles the profile that rejects it?"** (`docs/plans/notes.md:1380`) against a 368-site population, not 76 independent reds; **3a.1 — Fold the duplicate rank table** — the row spells the file `WorkspaceHome.tsx` while the tree has `ui/src/features/workspaces/WorkspaceHome.tsx` and `:96-98` is a comment, the same anchor class this file corrects for Phase 4; **Fix the anchor in `todo-payment-agents-4.md`** — still crate-less at `todo-payment-agents-4.md:126` and `:137`, and reported rather than edited because that plan is another lane's fence.
- **Now a debt on the owner page, not in the tree:** the release-profile question (item 14, above) and the **132 off-scale leading tokens** left when the leading-token programme closed 31 of 31 sites — owner page **item 10, "Is the leading scale a target the UI normalises onto, or an exception the literals are allowed to keep?"** (`docs/plans/notes.md:1352`). Neither is funded by a code path, so neither is a box here.
- **Carried in, not rows of this file:** the sheet guard registered 2 screens and 2 sections tonight and its `BASELINE_UNCITED` array went **54 → 23** `.css` paths (`ui/src/__tests__/screenExtraction.test.ts:1690`, counted by hand in the array), with two rows proved unclosable by citation — one sheet has no mount, the other is named only in prose; root-only fallback tails cleared or priced across three sweeps, **126 + 93 + 77**, the third still in flight in two shared sheets; the seeded-row refusal helper went from eight copies to one home with three sites still to fold; and four plans were reconciled, **two of which turned out to be a decision record and an audit rather than work lists, so their open boxes were never debts** — the same mistake the four NOT WORK boxes above were.


<!-- 2026-09-15 · DSH · the dead-class campaign is closed and this is one append: rows plus ONE law · nothing above this line was edited · every figure re-read from a commit body or a command run in this box, none carried from chat · tip read: `git rev-parse --short HEAD` = d5d7b9d61 -->

## The dead-class campaign, what it found, and what its numbers never meant

**THE LAW — a count of dead names is a count of what the classifier could see, not of what exists.** Quote no population figure without the mechanism that produced it AND the number of looks that mechanism does not make. The corollary for any row written here: a name with a rule and no reachable composition is **debt**; a name with a mention the extractor cannot follow is **blindness**; a name with neither a rule nor a mention is an **array entry to retire**, not a rule to delete. Tonight taught this twice — `30c2470e9` narrowed a `dynamicClassPrefixes` family into a form that cannot fail, which is decoration wearing a guard, and a 68-name DEAD roster became 44 when one more look was added to the same names, which means the roster was always a fact about four looks and never a fact about CSS.

**THE CLASSIFICATION PRINCIPLE, and why the ledger stayed at four members.** The externalClasses ledger earns an entry only when the value domain is **OPEN** — a map key, a server string, a data attribute. A `${…}` tail over a **closed union** is enumerable, so it belongs in the guard as whole names, not in the ledger. That is why three lanes found composition everywhere while `EXTERNAL_CLASS_LEDGER` still holds 4 members and 0 open questions — printed as `137 sheets indexed, 37 values graded, 4 violation(s), 4 ledger member(s)` and `ledgered=4 open-question=0` by `cd ui && npx vitest run src/__tests__/screenExtraction.test.ts` at `34d2007f3`.

What landed, each with its own commit and its own measuring box: `30c2470e9` narrowed the first family and established that a narrowing which cannot fail is decoration; `350c7f97f` landed the 22-field enumeration AFTER the reported `1720672b2` turned out not to exist as an object — `git cat-file -t 1720672b2` prints `fatal: Not a valid object name 1720672b2` and `git cat-file -t 1720672b2^{commit}` prints the same fatal — so that work was rescued from a shared working tree rather than lost; `ddbddde54` added the resolver unwired (165 lines, no finding moved); `ef5401d8b` added its 13 proof cases (+229/−1, and they are what caught the dead guard described below); `34d2007f3` wired it, corrected the two sentences that wiring makes false, and deleted an unreachable branch instead of testing it.

- [ ] **84 class names still survive the dead-class case on a `dynamicClassPrefixes` entry alone.** `34d2007f3` reports the census in its own body: 111 names survived on a catch-all, 27 are now credited for a reason, 84 still need only the prefix — a drop of 27, with the per-file breakdown WorkspaceHome 6, PaymentModal 4, GiftCardsScreen 4, MemosScreen 4, OfflineQueueScreen 3, RestaurantMenu 3, ProductLookupScreen 2, StaffLoginScreen 1, and nothing newly dead. The population a deletion starts from is the 44 a hand pass left with no literal naming them anywhere; re-measure before acting on any of it, because the figure is a property of the extractor at the tip it was read at.
  - **NEEDS A RULING, NOT A FIX — 2026-09-15 ~20:35, one finding turned into a tracked box-child under this row, and left open on purpose.** A zero-strike pass reported (lane handle `ca75ee74`, which **is not a git object here** — `git rev-parse --verify ca75ee74` fails and no commit or tag begins `ca75ee`, so it is named as a session handle, not cited as a diff; same class of thing as `d15c78a4` earlier tonight) that **the prefix arm's two halves disagree about which sheet is authoritative**, and the disagreement is real, but it is not undocumented: `ui/src/__tests__/screenExtraction.test.ts:115-119` states it as a design choice — the used-vs-defined check resolves against `css` UNION `parentCss`, while the duplicate check and the dead-class check keep walking `css` ALONE. **The consequence this row had not yet recorded:** a value can therefore be *credited* to one half of the arm and *rescuing nothing* to the other, at the same time, in the same run — which is why the surviving names on this row cannot be struck guard-side without first deciding which half the ledger answers to. **The live case, measured in this checkout rather than carried:** the `StockCountDetail` entry (`screenExtraction.test.ts:1125`) lists six `dynamicClassPrefixes` and cites `inventory/StockCountDetail.css` as its own sheet with `parentCss: ['inventory/StockCountBadge.css']` — and the two sheets split the six exactly along the disagreement: `grep -c sc-badge-- ` on the entry's OWN sheet returns **0**, while `ui/src/features/inventory/StockCountBadge.css` returns **4**, so those four (`sc-badge--draft`, `-in_progress`, `-completed`, `-cancelled`) are credited ONLY through `parentCss`, and only because `960d00568 refactor(inventory): hoist the sc-badge family into one imported sheet` (+24, one sheet now imported by `StockCountDetail.tsx:25`, `StockCountHistory.tsx:19` and `StockCountsScreen.tsx:17`) moved them there; the other two are owned outright — `sc-add-line-item--` = **1** and `sc-diff-` = **2** definitions in the entry's own sheet. **So the finding as handed to me over-counted by two: four of the six are parentCss-only, not six**, and it is those four that are simultaneously credited and dead-blind. **Two more figures the pass quoted could NOT be re-derived from anything in the repo, and are recorded as attributed rather than repeated: a count of 34 and a set of twelve sole-claim-0 entries — `grep -n 'sole-claim\|soleClaim' ui/src/__tests__/screenExtraction.test.ts` returns 0 matches, so no print in the guard emits a sole-claim number.** What the guard DOES print at this tip (own run, redirected not piped, exit 0): `prefix credit: 100 of 100 graded values credit at least one class defined inside the sheets their own entry cites` and `dead-class census (86 graded entries / 84 distinct names): DEAD 0 pair(s) / 0 distinct name(s); PREFIX-RESCUED 18 pair(s) / 18 distinct name(s)` — **this row's 84 is still that 84**, and the arm's own numbers are those two prints, not a 34. **Re-derive with:** `cd ui && npx vitest run src/__tests__/screenExtraction.test.ts 2>&1 | grep -E 'prefix credit|dead-class census'` — and note the pipe reports GREP's exit, not vitest's, so grade the run from a redirect. **The ruling the row needs from the owner, which no lane should make:** is `parentCss` authoritative for a prefix credit, in which case the four badge values are legitimately credited and this row's count should be read as *per-entry reach*; or is the OWNED sheet authoritative, in which case those four are another screen's classes borrowed a second time and this row shrinks by four while the dead-class case grows. **A third option the file's own history suggests and this pass does not recommend silently:** keep both, and print the split — the guard already prints `PREFIX-RESCUED` separately from `DEAD`, so a `credited-via-parentCss` line would make the disagreement visible at every run instead of argued in prose. **Nothing is ticked here, no box was added, and no guard file or stylesheet was touched: the defect being tracked is an unresolved question about the ledger, and the row above it stays open as the box that owns it.**
- [ ] **THE WIDENING IS PERMISSIVE, SAY IT IN THE SAME BREATH AS THE DROP.** Every name the resolver credits is one fewer finding, and `34d2007f3` opens with `PERMISSIVE BY DESIGN` rather than burying it. It cannot create a finding, and it cannot be read as a fix: a permissive change shipped under a subject that reads like one is how a quiet loss of coverage looks clean for a year.
- [ ] **One of the four shipped mechanisms has zero real sites, and the fix to it moved no tree number.** Shape 4 — an UPPER_SNAKE map indexed in a template — is exercised only by a synthetic fixture: `WS_COLORS` at ui/src/features/workspaces/WorkspaceHome.tsx:21-27 is read into a local and the local is applied, so no interpolation reaches it. Measured, not assumed: the caret fix that turned the map tail check from a guard that cannot fail into one that can (it had no `^`, so `admin: 'ws-color-admin' + suffix` was passing as a plain literal) left the census at 111 / 27 / 84 before and after, so that fix is proven by one reversed unit assertion and by nothing in the features tree.
- [ ] **Two censuses of overlapping names disagree and both stay on the record.** The scratch classifier (dc-census/strict.cjs — unversioned, outside this repo, so nobody can re-run it) labels 22 of the 27 freed names DEAD; `34d2007f3` calls them freed for a reason. They differ because one counts four looks and the other five. Do not merge them into one number, and do not delete on the strength of the smaller one.
- [ ] **Figures this append refuses.** The deletion lane that owns the guard file and a stylesheet has landed nothing on top of `34d2007f3` as of tip d5d7b9d61 — `git log --oneline -6 -- ui/src/__tests__/screenExtraction.test.ts` still ends at that commit, and `git --no-optional-locks status --porcelain -- ui/src/__tests__/ ui/src/features` returned nothing at all — so no post-wiring census is quoted here as current, and 111 → 84 is attributed to `34d2007f3` and its tip, not to the tree a reader opens.

**This file's own census, by both forms the Counting method section names, measured before this append:** `grep -cE '^[[:space:]]*[-*][[:space:]]+\[ \]' todo-open-debt-program.md` = **22** open and its `\[[xX]\]` counterpart = **3** ticked; the anchored forms `grep -c '^- \[ \]'` and `grep -c '^- \[[xX]\]'` give the same **22 / 3**, because this file has no indented checkbox rows. Note that `:349` records 24 open / 1 ticked for an earlier pass and that figure no longer reproduces — which is the law above applying to this page, and the reason the four rows added here are un-ticked: none of them is completed work, and a retired row is not a completion.

**CORRECTION TO THE BODY OF `e94a98b4f`, where a reader hits it.** That message's last paragraph says the animation walker never read the declaration this commit removed. It did, and the two harvest lines are the retraction: before the deletion the walker printed `316 animation declarations = 142 graded (44.9%) + 44 excused-by-essential + 56 excused-by-value + 74 swallowed-by-file-wide-reduce`; after it, `315 = 141 graded (44.8%) + 44 + 56 + 74`. One declaration left the **graded** bucket and the swallowed bucket did not move, because a rule inside a `prefers-reduced-motion: no-preference` block is honoured by that walker's own no-preference pattern (`ui/src/__tests__/animationCompliance.test.ts:73-76`) even in a sheet that also opens a reduce block: the file-wide amnesty is a true statement about this repo's sheets and was a FALSE statement about this rule. So the pair is certified by two independent prints, not one -- the census moved 23 to 22 and the walker's graded denominator moved 142 to 141. Attribution, because only one of those lines can be re-run today: the after-line is `npx vitest run src/__tests__/animationCompliance.test.ts` at this tip (2 passed), the before-line is the same command run at `2ddaab108`, one commit earlier, and re-deriving it now would mean restoring the CSS of a sheet this box does not own.
**THE FALSIFICATION THAT DID NOT HOLD, and it is the more useful of the two findings.** Striking a surviving whole name from the narrowed family -- `restaurant-card--pinned`, then `restaurant-card--disabled` -- still prints `DEAD 0 pair(s)` and `Tests 269 passed (269)`, because both names are held alive by the extractor at `ui/src/features/restaurant/components/MenuItemTile.tsx:190-191` (`if (pinned) cardClass += ' restaurant-card--pinned'`, `if (!product.inStock) cardClass += ' restaurant-card--disabled'`) and not by the mute. That is the `settings-license-value--tier-plus` asymmetry in a new costume, and it says the property this programme asks a narrowing to prove -- strike the entry, watch the name go dead -- is the WRONG property for a family whose members are genuinely composed: there, a whole name and a catch-all are equally unfalsifiable from the entry side. The asymmetry that does hold is on the other side: the catch-all restored on top of the already-deleted CSS printed `DEAD 0, PREFIX-RESCUED 22, 269 passed`, green and blind, which is why `2ddaab108` and `e94a98b4f` are one sequence and neither is a deletion by itself.
**THE UNIT OF THE NEW CENSUS, and what a green tip now means.** `2ddaab108` gave the guard a print of its own whose stated unit is **name-and-entry pairs**, with the distinct-name count printed beside it so a pair count is never read as a population: `86 graded entries / 84 distinct names`, the gap being `OfflineQueueScreen` and `TaxConfigurationScreen`, which each appear twice in `SCREENS`, and a floor asserts the run count against `SCREENS.length` so an empty census cannot read as a clean tree. The consequence, once and without a victory lap: **a green tip used to have an empty dead list by construction, and it no longer does.** The same two commits carry a retracted claim and a falsification that failed, and this page is better for naming both than for quoting only the delta.

---

<!-- 2026-09-15 · DSH · one append: PHASE 5 · nothing above this line edited · found while retiring `todo-refactor-cloud-sync-agents-1.md` · tip read: `git rev-parse --short HEAD` = `8eec36261` — HEAD moved MID-PASS off `503591a10`, so every figure below was re-run at the later tip and the 47-arm split is identical on both · every number came from a command run in this box, none carried from another document · and the phase's PREMISE was rewritten once during measurement, which is recorded rather than hidden: see "How this phase was almost wrong". -->

## Phase 5 — cloud-server, kasirmu-api and platform-sync print a local green for PG cases that never ran, and the obvious fix would delete 64 real CI tests

**Fence:** the **twelve** Rust test files in the table below (**66** skip arms — **64** of them PG-gated — across **three** crates), `scripts/verify-pg-tests-ran.py` (the runner this phase built), and nothing else — **no production module, no `sync_store*/` file, no test file edited by the runner itself**. If the gate half is funded later, add exactly one CI surface: `.github/workflows/dev-ci.yml` (the `cargo-nextest` job, `:200`-`:244`), plus a `scripts/gates.json` row and its two mirror edits — see "Decide whether … becomes a gate". If the `slow-tests`-idiom box is funded instead, the fence **widens by three paths** — those same twelve test files plus one `[features]` block apiece in `apps/cloud-server/Cargo.toml`, `crates/kasirmu-api/Cargo.toml` and `platform/sync/Cargo.toml`, which already holds the idiom's own `slow-tests = []` at `:34`. Fifteen files total, and the delta over today's fence is exactly the three manifests.
**Commit prefix:** `test(cloud-server):` · `test(kasirmu-api):` · `test(platform-sync):` · `ci(dev-ci):` · `chore(scripts):` — three crates own arms now, so a single-crate prefix would misattribute two thirds of the work.
**Acceptance:** a run of `cargo test -p kasirmu-cloud -- --nocapture` that a lane is *told* is a verification run, whose captured log shows **0** `test skipped` lines, **and** the 64 PG arms still listed as runnable-by-CI rather than ignored. The first half is unsatisfiable without Docker; the second half is satisfiable now and is the half that protects CI.

### The debt

`todo-refactor-cloud-sync-agents-1.md` set a tick condition on *"the captured output must show ZERO `skipped:` lines"* — a condition that cannot fail, because these arms print their message and then `return`, and libtest discards the output of a **passing** test. Measured on the whole crate at `8eec36261`, container down:

| Figure | Value | How to re-derive |
|---|---|---|
| crate result, **run this pass** | **347 passed / 0 failed / 4 ignored** in the bin target, `finished in 357.98s`, **exit 0** (+5 and +2 in two other targets = **354 ok** lines total) | `cargo test -p oz-cloud-server -- --nocapture` |
| cases that printed a skip inside that green | **36** — 29 × `Connection error: … pool.get() …`, 7 × `cannot create throwaway DB` | same run, count `test skipped` |
| cases that actually ran | **318 of 354** — **10.2 %** of a clean-looking exit-0 run verified nothing | subtraction of the two rows above |
| skip arms repo-wide | **66**, across **12** files in **3** crates — *moved from **64** on 2026-09-16 when `0289509b8` gave the two `migrate_sqlite_to_pg` PG tests a "cannot create throwaway DB" arm apiece, beside their existing connect arm* | `git ls-files '*.rs' \| Select-String 'eprintln!\("[^"]*[Ss]kipped'` grouped by file — 66 total strings, minus `crates/oz-cli/src/commands/backup.rs:~"wal_checkpoint skipped"` (production runtime note, not a test) and `crates/oz-security/src/lib_tests.rs` (a `match` fallback that prints and **continues** asserting — a different shape, excluded) |
| split by file | **`apps/cloud-server` 49 / 9 files:** `email_pg_tests` 12 · `db_tests` 9 · `sync_store_tests` 7 · `prune_tests` 5 · `sync_api_tests` 5 · `webhooks_tests` 3 · `main_tests` 2 · `redis_backend_tests` 2 · `migrate_sqlite_to_pg_tests` **4** (was 2; the two new arms are this phase's own doing, and the guard's baseline assertion caught them the moment they were written). **`crates/oz-api` 13 / 2 files:** `pg_tests` 12 · `routes/settings_tests` 1. **`platform/sync` 4 / 1 file:** `pg_transport_tests` 4 | same command, grouped by file |
| split by infra | **64 PG** · **2 Redis** — by subtraction, and it closes: 68 total strings, minus the 1 production string and the 1 keyring-shaped arm, minus 2 Redis = 64 (before `0289509b8` it read 62 PG across 66 strings and 64 arms). 57 **distinct** message strings across the 66 counted arms, so the reasons are per-test prose, not one shared constant | same command; count the `Redis`/keyring/`wal_checkpoint` strings and subtract |
| arms that can fire today | **64.** The 2 Redis arms sit at `redis_backend_tests.rs:85`/`:89` inside the helper `test_backend()` (`:78`), whose only three callers (`:97`, `:128`, `:142`) all carry `#[ignore]` — so **0 Redis skips printed** in the 354-test run | `Select-String -Pattern '#\[(ignore\|tokio::test)'` on that file |

**This phase's own population was wrong when it was first written, and the correction is the load-bearing part of it.** The table above originally read *"47 across 9 files"* and the fence named `apps/cloud-server/src/*_tests.rs` — because the number arrived from the cloud-sync retirement, which had only ever swept its own crate. The next box on this list (the plan-doc sweep) generalised the query to `git ls-files '*.rs'` and found **17 more arms in two other crates**, which is the same mistake this program's `:391` law names, one tier up: a count taken from one crate was carried into a fence as though it were a count of the tree. Both other crates' arms are the **identical print-then-`return` shape** — verified per-site, not assumed: `crates/oz-api/src/pg_tests.rs` `:257`, `:597`, `:830`, `:870`, `:999`, `:1099`, `:1327`, `:1500`, `:1583`, `:1756` each `return` on the line after the message, and `platform/sync/src/pg_transport_tests.rs:392`, `:400`, `:460`, `:468` likewise.

### How this phase was almost wrong — read this before touching one arm

The finding as it arrived from the cloud-sync retirement was *"CI cannot see these skips either; convert the arms to `#[ignore]`, the idiom `platform/sync` already uses."* **Both halves are false, and the second is the kind of change that quietly deletes coverage for a year.**

- **CI is configured to run all 64 PG arms, in all three crates.** `dev-ci.yml:205`-`:218` declares a `postgres:17-alpine` **service** (`ports: 5432:5432`, `pg_isready` healthcheck), and `:219`-`:220` sets `OZ_TEST_PG_URL` at **job level**, reaching `cargo nextest run --workspace --all-features` at `:244` — a workspace run, so `apps/cloud-server`, `crates/oz-api` and `platform/sync` are all covered by the same env. And every one of the three crates' guards prefers that variable: `sync_store_tests.rs:15`-`:16`, `crates/oz-api/src/pg_tests.rs:251`-`:252` and `:590`, `crates/oz-api/src/routes/settings_tests.rs:602`-`:603`, `platform/sync/src/pg_transport_tests.rs:383`-`:384` and `:452`-`:453` are each `std::env::var("OZ_TEST_PG_URL").unwrap_or_else(|_| "postgres://…localhost:15432…")`. **In CI there is a database, so those arms connect and those tests run.** That is a claim about configuration, observed here; this pass ran no CI and asserts nothing about the CI *result*.
- **So `#[ignore]`ing them removes them from CI.** `git grep -n -e '--run-ignored' -e 'run-ignored' -- scripts .github` → **0**: nothing opts ignored tests back in, and nextest skips `#[ignore]` by default. Conversion is not neutral — it is **−64 PG cases in the only environment that executes them**, and that set includes `pg_integration_conflict_tables_enforce_tenant_isolation`, the email and webhook RLS cases, and `platform/sync`'s transport isolation tests. A change that reads as tidiness and takes away three crates' Postgres-side tenant-isolation surface is this program's own `:391` law in a new costume: *"a count of dead names is a count of what the classifier could see"* — here, a count of **live** names read without asking which runner can see them.
- **What is actually broken is local reporting, not CI gating.** With no container and no `OZ_TEST_PG_URL`, 36 local cases return-and-pass — and the plan that commissioned this phase told its next reader to grep for a message the runner suppresses. Narrow, real, and it already bit one verification box in the wild.

### What is already established — do not re-derive

- **`#[ignore]` is already the house idiom where it is honest — and the crate this argument points at uses both idioms in the same package.** `platform/sync/tests/pg_integration.rs:112`,`:146` carry `#[ignore = "requires a disposable PostgreSQL instance"]`, while `platform/sync/src/pg_transport_tests.rs:392`,`:400`,`:460`,`:468` in that **same crate** use the print-then-`return` shape. So *"adopt the neighbouring crate's idiom"* was not just dangerous, it was **incoherent as written** — there is no single neighbouring idiom to adopt. `redis_backend_tests.rs:96`,`:127`,`:141` carry `"Redis integration tests disabled in dev CI"`, a *correct* admission, because `dev-ci.yml` has **no Redis service**: `git grep -n -i redis -- .github/workflows/dev-ci.yml` returns one hit, and it is the word "rediscovering" in a comment at `:541`. So Redis is unreachable in both environments and honestly labelled; PG is reachable in one and is not labelled, in any of the three crates. **That asymmetry, not the arms, is the defect.**
- **The runner property a fix must exploit:** nextest reports ignored tests as a *count*; a `return`ed pass is indistinguishable from a real one in every runner's summary. So the mechanism has to produce a number, not a skip.
- **This repo was fooled by this exact arm shape before, in its own words:** `sync_store_tests.rs:42`-`:44` records that hyphenated DB names made `CREATE DATABASE` a syntax error and *"silently skipped every sync-store PG test."* That was fixed by changing how DB names are generated; the skip *shape* survived, in 47 copies.
- **The local container, if the first half is funded:** `scripts/reset-dev-pg.sh:19`-`:21` carries `docker run -d --name oz-pg-test-15432 … -p 127.0.0.1:15432:5432`, `:37` probes it, `:49` resets the schema. Docker is fenced off from doc/audit lanes in this sandbox, so this is an owner or dedicated-lane action, not a drive-by.

### Tasks

- [x] **Decide the mechanism, then write it once.** — **DECIDED AND BUILT 2026-09-15, and the decision reversed the recommendation this box originally carried.**
  - **What was written first:** option (a), an `OZ_REQUIRE_PG=1` env gate at each arm, labelled here "(a) is stronger because it survives a lane that forgets the wrapper", against option (b), a wrapper script, as "the smaller diff because it touches zero test files."
  - **Why (a) lost.** The premise of (a) was that the arms share a choke point. They do not. Reading the 64 sites by shape rather than by count gives **three** distinct ones: (i) `let Some(x) = helper() else { eprintln!; return }` — `sync_store_tests.rs:273`, `crates/oz-api/src/pg_tests.rs:256`; (ii) `match connect() { Err(e) => { eprintln!; return } }` inline, no helper at all — `db_tests.rs:246`-`:251`; (iii) a helper ending in a bare `None` two frames from the skip — `redis_backend_tests.rs:85`/`:89`. And one arm (`pg_tests.rs:887`-`:893`) cleans up its throwaway database across **four statements between the `eprintln!` and its `return;`**, which is the kind of thing no window-based rule finds on the first try. So (a) is 64 hand edits across three crates in a checkout where other lanes commit hourly, and its weaker sibling touches none. That is a cost discovered by measurement, not taste — and the taste recommendation was wrong about it.
  - **What was built instead: `scripts/verify-pg-tests-ran.py`.** A runner, not a gate: it edits no test and is wired into no workflow, so it cannot remove one PG case from CI — the failure mode this phase was written to prevent. Two independent checks, because one channel has already lied here: **LOG** (a `--nocapture` run must print zero skip events, each event named) and **SOURCE** (the tree must still *contain* the arms — else deleting the `eprintln!` while keeping the `return` makes LOG green and blind, this program's own `30c2470e9` *"decoration wearing a guard"*). It follows the house `--self-test` convention, including the rule `scripts/verify-test-shadow-copies.py`'s note records: an empty-corpus case that **refuses** to report clean.
  - **The census is now machine-derived and agrees with the prose from an independent direction:** `python scripts/verify-pg-tests-ran.py --census-only` → **64 arms across 12 test files**, matching the table above, which was written from a `git ls-files | Select-String` walk before the script existed. Two derivations of one number, different code paths.
  - **Two bugs the design phase caught, both recorded because they generalise.** (1) `\bpanic!\b` **can never match** — `!` is non-word and a following `(` is too, so there is no boundary to assert — which made an early census silently `return`-only while still printing a plausible total. Fixed to `(?:\bpanic!|\bfail!|\bunreachable!)\s*\(`, with planted fixtures both ways so it cannot regress quietly. (2) The first exclusion of `crates/oz-security/src/lib_tests.rs` was an **accident** of that bug, and a window-based rule read the `panic!` belonging to the *neighbouring* match arm as this arm abandoning; the census now stops at the arm's own closing brace, pinned by a fixture.
  - **And the script reproduced this thread's central bug in its own first draft**, which is why it gets the paragraph: grading a log captured *without* `--nocapture` printed a `WARN` about suppression and then **exited 0**. An unproven clean reading is not a clean reading — exactly what `todo-refactor-cloud-sync-agents-1.md:64` had wrong all evening. It now returns **UNPROVEN → exit 1** unless the caller passes `--assume-nocapture`, which moves the claim onto the human loudly instead of the tool granting it silently.
  - **The SOURCE channel was then armed with two guards of different kinds, not one.** A bare `arms > 0` catches only total blindness; if a lane rewords the messages the census quietly shrinks 64 → 40 and a green still prints. So there is a **magnitude floor** (`ARM_FLOOR = 55`, headroom below the measured 64, message naming the baseline it fell from) *and* a **per-crate membership rule** (`ARM_CRATES`: each of the three crates must keep at least one arm). That split is this repo's own recorded lesson — `AGENTS.md` documents `402b11660` replacing `popupBackgroundCompliance`'s five `toBeGreaterThan(0)` floors with magnitude floors plus an identity check, and the plant that earned it: *"all four magnitude floors passed and only the identity check fired."* The fixture here reproduces that shape exactly: deleting `platform/sync`'s rows leaves **60 counted, above the floor, so size reads healthy**, and only membership sees it.
  - **Two checked negatives, recorded because a null result that is only *asserted* is how this file got the 47 it retracted.** (i) *Does the census under-read by macro?* `git grep -E '(^|[^e])println!\s*\(\s*"[^"]*[Ss]kip'` → 4 hits, all in `apps/cloud-server/src/bin/migrate_sqlite_to_pg/copy.rs` (`:102` `"  {table:<24} skipped (empty on source)"`, `:106` `(missing on source)`) — production migration progress output in a non-test path, correctly excluded by the `_tests` rule. `eprint!`/`print!`/`writeln!` → **0** each. So 64 is not a `println!`-blind artifact. (ii) *Was the census changed by simplifying* `ARM_RE` *from* `[Ss][Kk][Ii][Pp]` *to* `skip` *under* `IGNORECASE`? Re-measured after the edit: still **64 across 12 files**, byte-identical result — the equivalence was proven, not assumed from reading the flag.
  - **The census is a text scan, so dead text can inflate it — a third checked negative, fixed even though it measured zero.** Probing the current tree for arms sitting on a comment line returns **0 of 64** (script-relative walk over the same `_tests`/`/tests/` population, flagging any `ARM_RE` hit whose first non-whitespace characters are `//`, `/*` or `*`). So today's figure is uncontaminated — but that is a fact about this revision, not a property of the tool, and the obvious way it gets contaminated is a lane commenting out a PG arm while debugging, which would *raise* a census meant to count live silent-pass sites. `counts_as_arm` now excludes comment-leading lines, with the residual limit stated instead of glossed: a nested `/* */` whose arm line begins with other text is still counted, and no Rust lexer is being reimplemented here. Three fixtures keep it honest, including the false-positive side — `eprintln!("PG test skipped: see http://…")` must still count as an arm, so the rule cannot quietly eat real ones. The predicate is shared with the self-test's helper deliberately: `arm_abandons`'s own docstring promises one implementation so "a fixture can never drift from the scan", and the comment rule nearly landed on only one of the two call sites, which would have had the suite proving a rule the census does not apply.
  - **The baseline is now asserted, not printed.** `--self-test` includes `tree census still equals the stated baseline (66)`, so when the tree legitimately changes the suite goes red and `ARM_BASELINE`/`ARM_FLOOR` must be moved deliberately, in the same commit as the edit. That is the coupling this phase's §5-style warning asked for, made mechanical rather than advisory: a stale floor is either silently firing or silently not firing, and a maintainer cannot tell which from a green.
  - **And one bug in my own test code, caught by the test code.** The fixture helper was first named `arms()`, shadowing the census variable of the same name in the same scope, so `arms < ARM_FLOOR` compared a function to an int and `--self-test` died with `TypeError: '<' not supported between instances of 'function' and 'int'`. Renamed to `arms_in()`. It is worth recording that the failure came from running the suite rather than from reading it: a green nobody executes cannot catch its own harness. Related, and less flattering: one assertion I wrote in that same batch was **tautological** — `expect(..., reduced >= ARM_FLOOR or True)`, which can never fail, planted in the very commit whose subject is that guards must be failable. Replaced with a real two-sided claim (floor silent **and** membership firing once).
- [x] **Prove the chosen mechanism failable in both directions.** — **DONE 2026-09-15, and read the scope of this tick before citing it: it grades the TOOL, not the suite.** Both directions are now observed, not reasoned: **FAIL** in 5 of 5 standard-harness runs of `oz-cloud-server` and in every three-crate run before that, and **PASS twice, exit 0, 2 of 2** — `harness: serialized (--test-threads=1)` · `RUN: 1085 passed; 0 failed; 7 ignored` · `ok LOG: zero skip events`, byte-identical on both runs. A guard that had only ever gone red was not proven: the green half is what shows the tool is not simply always-failing, which is the same failure mode as a guard that is always green, arrived at from the other side.
  - **What this tick does NOT mean.** The suite is not clean. Zero events is reachable only by serializing, and the PASS prints that caveat itself (`note: this is a SERIALIZED run… it cannot show whether they interfere under the default parallel harness`) because the tool must not be able to report a stronger result than the configuration that produced it. The contention is its own open box above, and `todo-refactor-cloud-sync-agents-1.md:59`/`:100` stay open for the reason stated there.
  - **Green was unreachable when this bullet was written and is now nearly reachable — corrected in place, per the rule that a current measurement is fixed rather than layered over.** What this first asserted (`127.0.0.1:15432` refuses, probed twice) was a live reading, not a dated record, and the owner brought the container up later the same day. The trajectory of the identical command, four runs, all three crates, in order: **47 events** (no container) → **19** (container up, base DB drifted) → **2** (base DB reset, residue from this lane's own probe runs present) → **1** (clean). `RUN` went `1083 passed; 1 failed` → `1085 passed; 0 failed; 7 ignored`. **The box was held open at 1 event while the count was the question, and is now ticked because the question turned out to be different from the count**: this box asks whether the *mechanism* can fail and can pass, and both halves are now observed. Reaching zero events was the wrong bar for it — zero was reachable only by serializing, which changes the harness rather than proving the guard, so a tick earned by `--test-threads=1` needed to say so out loud, which is why the caveat clause exists. The suite-level bar is untouched and lives in `Kill the migration-test race` plus `:59`/`:100`. **And the consequence for the plan being retired here is harder than "one left": the event is not noise.** Measured in the box above, it appears in **5 of 5** standard-harness runs and **0 of 2** serialized ones, so `todo-refactor-cloud-sync-agents-1.md:59`/`:100`'s stated condition — a run showing zero `test skipped` — cannot be satisfied on this machine by a normal `cargo test` at all, only by `--test-threads=1` or by the fix. Those two boxes therefore need a code change, not another run; recording that is more useful than leaving it to read as almost-green.
  - **Proven red, and now on all three crates, not one.** Against tonight's real full-crate `--nocapture` capture: `RUN: 354 passed; 0 failed; 4 ignored`, `FAIL LOG: 36 silent skip event(s)`, exit **1** — the tool found, independently of the prose, the same 36 this file's table records. Then the two crates previously covered only by the census: **`oz-api` → `326 passed; 0 failed; 0 ignored` with 9 skip events, exit 1**, and **`platform-sync` → `405 passed; 0 failed; 3 ignored` with 2 skip events, exit 1**. So the shape generalises because it was observed to, not because three crates looked alike. Also proven red **end-to-end**: run without `--log`, the script makes its own cargo call and prints FAIL. And proven red on the **suppression** case: a plain log with no skip lines yields UNPROVEN / exit **1**, never PASS. `--self-test` → **29 green, 0 red, PASS**.
  - **The three crates' event totals sum to 47, and that number must not be confused with the 47 this phase already retracted.** Units: **64 = arms in source** (a static count of sites that *can* pass silently); **47 = events observed tonight** (36 + 9 + 2), which is arms that *did* fire on this machine, this hour, with the container down. 64 − 47 = 17 arms that never got the chance — two Redis arms behind three `#[ignore]`s, and arms in modules the selected feature set did not compile. The earlier retraction was of "47" read as a *population*; this 47 is a different unit that happens to reuse the digits, and a future reader who merges them will re-create the original error.
  - **`--all-features` is now part of the runner's own command, and the reason is a measurement, not a style choice.** The first draft invoked plain `cargo test -p <crate>`, but CI's command is `cargo nextest run --workspace --all-features` (`dev-ci.yml:244`), so a per-crate default-features run is not the same population. Re-running `platform-sync` both ways: **default features → `386 passed; 0 failed; 22 ignored`; `--all-features` → `405 passed; 0 failed; 3 ignored`.** Same total, 408, composition moved by 19. The cause is `platform/sync/tests/integration_test.rs`, which carries **19** sites of `#[cfg_attr(not(feature = "slow-tests"), ignore)]` against `slow-tests = []` at `platform/sync/Cargo.toml:34` — 22 − 19 = 3 ignored, 386 + 19 = 405, closing exactly. `oz-api` did not move (326/0/0 and 9 events both ways), and neither did `oz-cloud-server`, which was also run on both sides of the change: `354 passed; 0 failed; 4 ignored` → 36 events under default features, and the identical print under `--all-features`. So the shift is one package's feature design, not a property of the flag — but it is only knowable by running both sides in all three places, and it is the reason the three figures quoted above are now all `--all-features` figures and therefore directly comparable to CI.
  - **The one thing this tool must never be handed is the CI log itself, and it now says so instead of failing blind.** CI's command is `cargo nextest run --workspace --all-features` (`dev-ci.yml:244`), and nextest prints `Summary (N tests run: X passed, Y skipped)` where libtest prints `test result: …` — so feeding a lane's nearest green capture to this tool previously produced "no `test result:` summary line found" and left the reader to work out why. It now names the format and refuses the conflation, because the two "skips" are **different quantities with opposite meanings**: nextest's `skipped` counts tests it deliberately did **not** run (a filter, or a real `#[ignore]`), which is honest and visible; this tool's skip EVENT is a test that **did** run, printed, and returned as a pass, which is the deception. Reparsing one as the other would manufacture the exact false clean this file exists to refuse, so a nextest parser would need its own semantics rather than a relabel — and two fixtures keep the diagnosis from drifting, one asserting a nextest log is named, the other that a merely empty log is **not** blamed on nextest.
- [x] **Kill the migration-test race on the shared base DB.** — **CLOSED 2026-09-16 by `0289509b8`.** — **FOUND 2026-09-15 by the first container-backed runs, not by reading code.** `pg_integration_migrate_and_verify` (`migrate_sqlite_to_pg_tests.rs:96`) and `pg_integration_migrate_large_db` (`:230`) both call `connect_postgres(base_url)` → `schema.rs:41`-`:44`, which runs `batch_execute(PG_INIT)` against **the shared `postgres` database**, unconditionally and without serialization — unlike the other arms, which each build a throwaway DB. **The pair does not race with each other, though it was first filed that way** — run in isolation (`cargo test -p oz-cloud-server --all-features pg_integration_migrate -- --nocapture`) the two produced **0 events in 8 parallel attempts**, which killed the pair-vs-pair story this box originally carried. The co-racer is the rest of the suite: **eleven** test files reference `localhost:15432/postgres` directly (`git grep -l 'localhost:15432/postgres' -- '*_tests.rs'`), with 47 mutation-shaped hits in `db_tests.rs`, 53 in `crates/oz-api/src/pg_tests.rs` and 44 in `email_pg_tests.rs`. The migration tests are the loudest writers because they apply a **123-table schema plus seed inserts on every connect**.
  - **Signature that identifies it as a race rather than a defect.** Three full runs after the base DB was reset: `RUN: 1083 passed; 1 failed; 7 ignored` with 2 events, then `1083 passed; 1 failed` with 1 event, then `1085 passed; 0 failed; 7 ignored` with **1 event** — and the event **changed victim** between runs (`PG migration volume test skipped` on one run, `PG migration integration test skipped` on the next), same trailing `apply schema: db error`. A stable defect does not move between tests; a loser-of-a-race does. Both tests then **passed alone**: `migrate_and_verify` 0.66s, `migrate_large_db` 3.82s, each `1 passed; 0 failed`.
  - **The controlled version, which is what actually settles it.** Full `oz-cloud-server` suite, `--all-features`, same machine, same base DB, one variable — the harness's thread count. **Parallel: 1 event in 5 of 5 runs.** Serialized (`--test-threads=1`): **0 events in 2 of 2.** The victims rotated across runs (`migration integration` once, `migration volume` four times, plus both seen in the earlier three-crate runs), and wall time went `23.1/24.2/29.2s` parallel against `88.4/117.7s` serialized — a **3-4× cost to buy back one honest test result**.
  - **A second detection channel was tried and does not work, so do not rebuild it.** The idea: if a skip leaks its throwaway database, `pg_database` would count aborted cases out-of-band, beyond any summary line. Measured on a run with a known event — leftovers before **0**, event printed, leftovers after **0**. It fails structurally, not by luck: the victim dies inside `connect_postgres` before creating anything, and these two tests use the base DB rather than a throwaway one. Residue signals only a site that allocates *before* it fails, which is a property of each test and not of the skip mechanism, so a leftover count of zero licenses no conclusion in either direction.
  - **And the number that should be quoted anywhere this is discussed: `347 passed; 0 failed; 4 ignored` printed identically in all seven runs.** In the parallel five, one of those 347 is a migration test that connected, failed to apply its schema, printed a line, and `return`ed; in the serialized two, all 347 ran. Same print, different truth, differing by exactly the one case nobody can see from the summary — which is the entire argument of this phase, demonstrated by two flags and a pass count that never moved.
  - **The fix is not in this phase's fence, and that must be said before anyone dispatches it.** The cheap version edits `migrate_sqlite_to_pg_tests.rs` (one of the twelve) to give each test its own throwaway DB like everyone else. The *right* version is in `schema.rs` — `connect_postgres` should not apply a 123-table schema as a side effect of connecting at all — and `apps/cloud-server/src/bin/migrate_sqlite_to_pg/` is **production**, named in `todo-refactor-cloud-sync-agents-1.md`'s forbidden paths as Agent 3's old territory. Owner call, not a docs-lane call.
  - **There is a third option, and this repo already wrote it.** `crates/oz-api/src/pg_tests.rs:55` `pg_ddl_guard()` serializes base-DB DDL with `SELECT pg_advisory_lock(SCHEMA_LOCK_KEY)` under `SET lock_timeout = '120s'` (`:68`-`:74`), releasing deterministically via `PgDdlGuard::release` (`:42`), and it **works**: across a full three-crate capture the `continuing unserialized` fallback (`:81`) printed exactly **once** (`raw_up.log`, counted by `Select-String 'continuing unserialized'`). Note what that does and does not establish — the denominator, how many times the guard was *acquired*, is counted nowhere in the code, so "seen once" is the measurement and "14 of 15 held" would be an inference. What is safe to say is that the fallback is rare rather than universal, which is the distinction that decides whether porting this mechanism is a fix or a fig leaf. That is **15 call sites** of precedent (`git grep -n 'pg_ddl_guard(' -- crates/oz-api/src/pg_tests.rs` minus its `:55` definition) sitting in a sibling crate for precisely the contention measured above — and `git grep -c 'pg_ddl_guard' -- apps/cloud-server` returns **0**, so the crate that has the race is the one crate that never adopted the mechanism already proven against it. Taking it is a smaller and safer change than restructuring how `connect_postgres` applies a schema — the failure mode to avoid when porting it is `:78`-`:82`'s own shape, falling back to *unserialized and continuing*, which turns a lock that failed to acquire into a test that passes without it.
  - **How it was actually closed, and why not by the lock.** Both routes named in the two bullets above were live; the one taken is neither. The tests now build a **throwaway database** and point `connect_postgres` at it, so `schema.rs`'s unconditional `PG_INIT` apply happens in a database nobody else is in — contention removed at the resource rather than serialized at the lock. This is not this phase's design and not an invention: it is the fix this repo had **already shipped twice** and never applied to the migration bin. `sync_store_tests.rs:9`-`:11` states the reason in its own doc comment — *"isolated database to avoid AccessExclusiveLock deadlocks from concurrent PG_INIT DDL on the shared base DB"* — and `db_tests.rs:377`-`:380` refuses to apply schema on an admin connection for the same stated reason. `pg_ddl_guard` was therefore not ported: an advisory lock would serialize DDL that isolation makes unnecessary, and would leave the two tests sharing a database they do not need to share. **No production path was touched** — `schema.rs` still applies unconditionally, which is right for a real cutover and wrong for a test, and separating those is a decision this box does not make.
  - **Evidence, in the same one-variable form the box was opened with.** After the fix: `cargo test -p oz-cloud-server --all-features -- --nocapture`, default parallel harness, six consecutive full-suite runs → **0 `apply schema` events in 6** (baseline: 1 in 5 of 5), all six printing `test result: ok. 347 passed; 0 failed; 4 ignored`, plus a third **0** in a three-crate guard run. The pair also passes alone (`2 passed; 0 failed`) and leaves **0** `oz_migrate_test_*` databases behind, verified with `docker exec oz-pg-test-15432 psql -U postgres -tAc "select count(*) from pg_database where datname like 'oz_migrate_test_%'"`.
  - **Confound disclosed rather than smoothed over, because this is a shared checkout.** Every run above compiled the **working tree**, and at that moment two paths in this same crate carried another lane's uncommitted edits: `apps/cloud-server/src/sync_store.rs` and `sync_store/conflicts.rs`, one line each. Both were read before concluding anything — a `mod pg;` whitespace change and an import-list reorder — so the effect is nil, but these are observations *of a dirty tree* and are claimed as nothing more. `throwaway_pg_db` uses `.simple()` UUIDs for exactly the reason `sync_store_tests.rs:42`-`:44` records (hyphens turn `CREATE DATABASE` into a syntax error that skips silently), and the census moved 64 → 66 because each test gained a second skip arm — caught by the tool's own baseline assertion the instant the edit was made, which is the re-baseline-in-the-same-commit rule firing on its first real subject: this phase's own.
- [ ] **The race did not vanish; it moved to a narrower resource, and the surviving message misstates its own cause.** — **FOUND 2026-09-16 by the six-run confirmation above, which is the only reason it is known.** Zero `apply schema` events, and in their place **3 events across 6 runs**: `PG ordered-push`, `PG push-batch commit` and `PG tenant-isolation`, each printing `skipped: cannot create throwaway DB` (`sync_store_tests.rs`). Mechanism hypothesis, **not established**: `CREATE DATABASE` copies `template1` and takes catalog locks, so the throwaway helpers now contend on `pg_database` where they previously contended on the base schema — the same class one level down, in a smaller window.
  - **The alternative explanation, kept alive deliberately.** These events may predate the fix and have simply been out-shouted by the migration failures: the comparable pre-fix sample is two three-crate runs at container-up with **0** such events, which is evidence of absence in two tries, not proof of absence at ten. Settling it needs the fix reverted and the same six-run protocol run against that tree — a clean A/B, cheap, and nobody has done it, so this box says "moved or revealed, undetermined" rather than claiming a regression.
  - **What *is* established is the message, and it is the worse half.** `crates/oz-api/src/pg_tests.rs:596`-`:598` reads `let Some((pool, db_name, admin_pool)) = throwaway_test_pool(&url, "oz_rest_rls").await else { eprintln!("PG REST RLS test skipped (Postgres unreachable at {url})"); return; }` — and in the run that fired it, **Postgres was reachable**: `1085 passed; 0 failed` printed around it and `psql` answered on that port the same minute. The arm emits a *network* diagnosis for a *catalog* failure it never tested, because the helper collapses every `None` into one string. A lane dispatched on that message checks the container, the port and the firewall and finds nothing wrong — the wasted-expensive-channel shape this phase exists to prevent, arrived at from a new direction: a skip that not only passes silently but **points somewhere else**. The repair is prose and belongs with the race decision rather than ahead of it — either have the helper distinguish connect-failure from create-failure, or drop the parenthetical and print only what was observed.
- [ ] **Decide whether nextest's retry policy is hiding real flakiness from CI — measured, not theorised.** `.config/nextest.toml:15` sets `[profile.default] retries = { backoff = "exponential", count = 2, delay = "1s" }`, and **CI runs that profile**: `dev-ci.yml:244` is `cargo nextest run --workspace --all-features` with no `--profile`, and `--profile` occurs **0 times** in the whole workflow file (`Select-String '\-\-profile' .github/workflows/dev-ci.yml`). The `test workspace (nextest)` step in `scripts/check.sh` likewise passes none (re-find: `grep -n 'test workspace (nextest)' scripts/check.sh`). So the profile actually used by both CI and the local full-matrix script retries every test twice before allowing it to fail — which means the exact intermittent `pg_isolates_locations_by_tenant` failure observed in one libtest run tonight would be **absorbed and reported green** by the runner CI uses.
  - **The naming makes this easy to misread, and one clause here was wrong when written.** `[profile.ci]` (`:26`-`:30`, `fail-fast = true`, `retries = count 1`, 90s timeout) is **not what CI runs** — its only caller is `scripts/release.sh:65`. A reader who assumes "the ci profile is used by ci" will audit the wrong numbers, and the comment at `:11` ("stop after first failure in CI, keep going locally") describes a CI/local split the wiring does not implement, since both callers use the same profile. `[profile.quick]` (`:33`-`:35`) is the only no-retry, bounded-thread profile, and nothing in the repo invokes it.
  - **This is the third masking layer on this suite, and it is independent of the other two.** (1) print-then-`return` turns a non-run into a PASS; (2) libtest suppresses a passing test's stdout, so the evidence for (1) is not even greppable without `--nocapture`; (3) retries turn a genuine intermittent failure into a PASS on the second or third attempt. The guard closes (1) and (2) for local runs and has **no view of (3)** — it shells out to `cargo test`, which has no retry concept, so a guard run is *more* honest about flakiness than a CI run, not less. Recorded here rather than fixed: making retries visible to the guard means reading nextest's JUnit XML (`:21`-`:23` already writes `target/nextest/ci/junit.xml`), which is a different parser with different semantics — the same reason this tool refuses a nextest log above.
  - **Same family, smaller instance, found the same hour: the dev container and CI are not the same Postgres.** `.github/workflows/dev-ci.yml:207` is `image: postgres:17-alpine`, while `scripts/reset-dev-pg.sh` tells the operator to start **16** twice — in its usage comment at `:22` and in the message it prints at `:43`. (`docker inspect -f '{{.Config.Image}}' oz-pg-test-15432` on the container brought up 2026-09-15 returned `postgres:16-alpine`, i.e. the script's own instruction was followed.) So every local PG figure in this file was produced on a major version CI does not test, and the script is the reason. No version-specific symptom surfaced tonight — the 47→1 trajectory and the race behaved identically on the side tested — so this is an alignment question, not a bug claim: either those two lines move to 17 to match CI, or 16 is deliberate for dev and the script should say so. Cheap edit, two string literals in a shell script, and outside this phase's fence.
- [ ] **Adopt the `slow-tests` idiom if this phase is ever funded — the repo already contains the right mechanism, and this pass found it by accident.** The recommendation this phase first carried (an `OZ_REQUIRE_PG=1` env gate), then rejected (64 hand edits, no choke point), has a **native precedent three directories from the debt**: a no-op feature plus `#[cfg_attr(not(feature = "X"), ignore)]`. Applied to PG arms as `pg-tests = []`, it satisfies every constraint this file has been arguing about at once — **CI keeps the tests**, because CI passes `--all-features`, the feature is on, so the `ignore` attribute is never applied and the case runs against the service `dev-ci.yml:205`-`:220` provides; **a local run without the container stops reading as a pass**, because libtest and nextest both print `N ignored` *in the summary line*, which is the one channel that needs no `--nocapture` and the exact thing `todo-topology-editor.md:200` already proves this repo knows how to read; and **the skip is reversible by one flag** instead of by editing 64 call sites. That is strictly better than the env gate it would replace, and it is the same cost I costed out for (a) — 64 edits across three crates — so it does **not** change this round's decision, only what a funded round should build. One coupling to carry into that work: the guard's SOURCE floor is baseline **64**, and this migration deletes the `eprintln!` arms it counts, so the census legitimately drops and `ARM_FLOOR` fires; that is the floor doing its job, not a bug to route around, and the re-baseline belongs in the same commit as the migration.
- [x] **Then sweep the acceptance boxes in every plan doc that greps a test log.** — **RUN this pass, 2026-09-15, at tip `1182a3676`; the sweep found one broken condition and one broken fence, and the fence was this phase's own.**
  - **Method.** Two stages, because the first over-matched. Broad: `Get-ChildItem -File -Filter 'todo-*.md' | Select-String 'cargo test|cargo nextest'` → 5 live docs, 57 command sites; `… | Select-String 'skipped|ignored|nocapture|0 failed'` → 116 hits over 14 docs, including three `done-*` files whose figures are dated records and are not correctable without rewriting history. Narrow, and the narrowing is the point: **the trap can only exist where a test can skip-and-pass, so the candidate set is the docs that command a PG-gated Rust suite** — `Get-ChildItem -File -Filter 'todo-*.md' | Select-String '15432|oz-pg-test|pg_integration|OZ_TEST_PG_URL|throwaway_pool'` → 8 live docs, and intersecting that with `cargo test -p oz-cloud-server|nextest.*cloud-server` → **2**: `todo-refactor-cloud-sync-agents-1.md` (9 sites) and this file (2 sites, both written by this phase).
  - **Result on docs: the broken condition was unique, and already fixed.** `todo-refactor-cloud-sync-agents-1.md:64`/`:102` carry the `-- --nocapture` correction from its fourth pass. The other six PG-mentioning live docs **do not condition a tick on observing a skip**: `todo-refactor-kds-agents-1.md:214` and `todo-refactor-kds-agents-2.md:225` both state the block plainly ("Docker unreachable, port 15432 refuses", "a decision to retire that leg is the owner's, not a drift to tick over"), `todo-refactor-oz-pos-app-agents-3.md:197` is dispositioned **PARKED on environment**, `todo-refactor-pos-screen-agents-3.md:132` names `npm run check:all` — a **vitest** command, and vitest prints its skipped count in the summary line, so the libtest capture behaviour does not apply to it at all — and `todo-operational-integrity.md:8` / `todo-refactor-settings-agents-3.md:3` are `§4`-governance and audit-stamp prose. `todo-topology-editor.md:200` is the counter-example worth naming: it recorded `0 ignored` **from the summary line** as its non-vacuity proof, which is the one form of this that works without `--nocapture`.
  - **Result on the fence: the sweep invalidated this phase's own scope, and that is the finding.** Widening the arm query from one crate to the tree (`git ls-files '*.rs' | Select-String 'eprintln!\("[^"]*[Ss]kipped'`) turned "47 arms in 9 files in `apps/cloud-server`" into **66 strings / 64 test arms / 12 files / 3 crates**, because the number had been carried in from the cloud-sync retirement, which had only ever swept its own crate. The table and fence above now carry the corrected population. Box ticked because the sweep ran and each named command was checked against what its box claims; the correction it produced is filed as the first box's scope, not as a separate debt.
- [ ] **Rule on the 2 Redis arms** (`redis_backend_tests.rs:85`/`:89`, inside `test_backend()`): unreachable behind three `#[ignore]`s today, so the honest options are to delete them or to drop the `#[ignore]`s and add a Redis service — not to keep dead skip code implying a live skip path. **Recommendation on record: leave them and record the choice.** Dropping the ignores costs a dev-CI service nobody asked for; deleting the arms invites the next lane to re-add the helper's guards. (Note the census counts them at 2 of the 64 — the script's bare-`None` rule includes helper-propagated skips, which is the deliberate widening that took an earlier, too-narrow read of 61 up to 64.)
- [ ] **Decide whether `verify-pg-tests-ran.py` becomes a gate or stays a runner — this phase chose "stays a runner", and the reason is a count, not a preference.** Adding a `scripts/gates.json` row would move the status census `AGENTS.md:274`-area prose quotes (**53 required / 16 retired / 1 advisory**, measured 2026-09-14 by the `collections.Counter` one-liner printed there), which that page and its `.agents/AGENTS.md` mirror both carry; `verify-agents-mirrors.py` compares gate counts and workflow jobs, so a row added by a docs lane is a mirror edit in two files plus a status decision that is policy, not housekeeping. So: no row, no workflow edit, and the script is currently **nothing's acceptance command** — it only fires when someone invokes it, which is the honest description of its present power. To make it bind anything, either (i) a `check.sh` step plus a `gates.json` row plus the two mirror edits, or (ii) name it in the acceptance line of each plan doc whose box needs PG — (ii) is free and is what the next box does for this file's own phases.
- [ ] **Name the guard in the acceptance lines that need it, then re-run `--self-test` in CI if it ever becomes a gate.** `todo-refactor-cloud-sync-agents-1.md:59`/`:100` are the two boxes in this tree that ask for a PG-backed green today; pointing them at `python scripts/verify-pg-tests-ran.py --crate kasirmu-cloud` replaces "grep the log and trust the summary" with a command that cannot read suppression as absence. Do that as part of the container work, not before: until the green half above is proven, the command is only known to fail.
- [ ] **Commit milestones** — one per logical step, e.g. `test(cloud-server): fail the run when a PG case cannot reach a database`. Per `AGENTS.md` §3: one line, explicit pathspec, never `git add`.

### Why this phase is last

Phases 1-4 each say *"a gate cannot see X."* This one says **the gate CAN see X and the local run is the liar** — a smaller, less exciting claim and a bigger trap, because the tempting fix (adopt the idiom the neighbouring crate already uses) is precisely the change that would have stripped three crates' Postgres tenant-isolation and RLS coverage out of CI while making the local green *look* more honest. **The phase was wrong twice in the hour it was written, and both errors point the same way:** first the premise (CI cannot see it → CI runs it), then the population (47 arms in one crate → 64 in three), and the second was found only by the box that generalised a per-crate query to `git ls-files`. Both are the same act withheld: not asking *which runner*, and not asking *what tree*. The sections above exist so no future lane re-derives the wrong version of this phase from the four words "skip arms."

### Census this append contributes

Ten boxes added, **three now ticked** — and the three did not close the same way, which is the part a ratio cannot show: two (the mechanism decision, the plan-doc sweep) were **born and closed inside a single append**, while one (both-directions proof) was filed open by the first append, sat waiting on an environment this lane could not create, and was closed only after the owner brought the container up and a harness question was answered by measurement. Before: **22 open / 8 ticked / total 30**, by the canonical pair at `:360`-`:361` (`grep -cE '^[[:space:]]*[-*][[:space:]]+\[ \]'` and its `\[[xX]\]` counterpart), the anchored forms agreeing and any indented-checkbox match → **0** rows, which is why the two forms cannot diverge on this page. After: **29 open / 11 ticked / total 40** — open +7, ticked +3, denominator +10.

That is a mixed signature, and it is worth separating from the two this file already names because a completion ratio cannot see the difference: the **tick** (`:371`) moves open −1 / ticked +1 / total fixed; the **strike** (`todo-refactor-cloud-sync-agents-1.md` §8) moves open −1 / ticked +0 / total −1; and **new-and-run work** moves open +7 / ticked +3 / total +10. Reading only `ticked/(open+ticked)` sees 8/30 → 11/40 and cannot tell that three of those eleven were measured rather than deferred, that one of the three needed a container and a thread-count to close, nor that of the seven open here two are now blocked on a *code* change rather than an environment (the race, and the guard-naming that depends on it), one is hidden flakiness in CI's own runner (nextest retries), one needs only a funded 64-site edit (`Adopt the slow-tests idiom`), two need an owner's decision (gate-vs-runner, Redis arms), and one is a milestone row that binds nothing new. Every figure here carries the command that re-derives it for exactly that reason.

**And the file's own census rotted while this phase was being measured, by exactly the mechanism `:355` names.** `:404` states **22 open / 3 ticked**; measured now it is **22 open / 8 ticked** — the open figure holds, the ticked figure is five completions stale, and the narrative at `:370`-`:372` tracks only the first two of those five. `:404` is left verbatim as the dated record it is, per this tree's convention; this row is its forward correction. One further method note for anyone re-running it here: that canonical pair is a **bash** command. Under PowerShell's `Select-String`, `[[:space:]]` is not a POSIX class but a bracket expression over the literal characters `[:space]`, so it returns **0 on a file holding 22 open boxes** — silent, same shape as the `:364` escaping trap but a different cause (regex dialect, not bracket escaping). The `.NET` form `'^\s*[-*]\s+\[\s\]'` reproduces bash exactly and is what both figures above were taken with.

---

## Phase 1 re-measured 2026-09-16 (HEAD `0289509b8`) — the 76 is now 1, and the 1 is deliberate

Both profiles run by this lane on the same tree, with the proxy unset (`env -u HTTP_PROXY -u HTTPS_PROXY -u http_proxy -u https_proxy`, per the localhost-HTTP trap that otherwise fails every binding test here):

```bash
cargo test -p oz-bridge              # 1317 passed; 0 failed   (234.85s)
cargo test -p oz-bridge --release    # 1315 passed; 1 failed   (195.65s)
```

- **The headline has rotted from 76 to 1.** `:105` records **1231 passed / 76 failed** at HEAD `257ff6122`; measured now it is **1315 passed / 1 failed**. Other lanes closed 75 of them in the two days since. The classification box at `:106` is *re-scoped* rather than done, and one reason is now visible: the population moved underneath it twice.
- **The sanity check holds: debug total − release total = 1** (1317 − 1316), which is the single `#[cfg(debug_assertions)]` test at `crates/oz-bridge/src/sync_tests.rs:35-37`. The anchor was re-verified live this pass and is still `#[cfg(debug_assertions)]` + `#[test]`, so the gap is fully explained and is not a miscount.
- **The survivor is deliberately red, and the code says so.** `staff::security_events_tests::a_rejected_create_records_no_security_event` panics at `crates/oz-bridge/src/staff_security_events_tests.rs:209` with `InvalidSubscriptionSignature … Invalid symbol 95, offset 9` — symbol 95 is `_`, the underscore in `BOOTSTRAP_FREE`, i.e. exactly the release/base64 mechanism `:98` describes. Its own comment reads: *"DELIBERATELY LEFT RED in the release profile … Forking here would turn the test green while its subject — the recorder staying silent on a rejected mutation — stops being exercised … A green that asserts nothing is worse than a red with a reason; the honest fix is a verifying seeded row, which is an owner decision and not a fixture edit."* **That pattern occurs exactly once in the tree** (`grep -rc "DELIBERATELY LEFT RED" crates/ --include=*.rs | grep -v ":0"` → 1 file, 1 occurrence), so it is a decision, not a class.
- **The dispatch consequence is the useful part: Phase 1's remaining work is one owner ruling, not 76 fixture fixes.** `:110` (fix the profile-dishonest fixtures) and `:106` (classify each of the 76) have almost nothing left to bite on — the fixtures were fixed elsewhere, and the one survivor refuses a fixture edit on principle. What remains is the *verifying seeded row* its comment names: the same shape as the parked arm at `:113`, and like it, blocked on the owner rather than on code. **Until that ruling lands, `--release` cannot reach 0 failed, so Phase 1's acceptance stays unmet and a `--release` gate (`:112`) would land red** — which is the house rule's reason for ordering cleanup before enforcement.
- **Nothing here was fixed by this pass.** No source file was touched; the two commands above are the whole of the work, and the census is unchanged at **29 open / 11 ticked** (any-depth, per `:53`).

---

## Phase 1 CLOSED 2026-09-16 — the deliberately-red survivor is green, and the "verifying seeded row" it asked for was the wrong fix

Two commits, both measured in **both** profiles:

```bash
cargo test -p oz-bridge              # 1318 passed; 0 failed   (219.06s)
cargo test -p oz-bridge --release    # 1317 passed; 0 failed   (187.73s)
```

- **`--release` now reaches 0 failed**, so the acceptance the block above said was unmet is met, and a `--release` gate (`:112`) no longer lands red. The sanity check still holds: **1318 − 1317 = 1**, the single `#[cfg(debug_assertions)]` test at `crates/oz-bridge/src/sync_tests.rs:35-37`.
- **The "verifying seeded row" was not the fix — it is not available, and the earlier block was wrong to adopt the test's own framing.** That phrase came from the test's comment, and it reads like a fixture edit; it is not one. `verify_license_signature` takes no key parameter and reads a build-time `include_str!` public key, while the private half is git-ignored (`*.key`) and absent — `ls crates/oz-core/*.key*` returns only `oz-license.key.pub`. **No fixture in this crate can mint a signature that verifies**, so no seeded row lets a release create through. A verification seam would be a production change, and making release accept the sentinel instead would move a licence bypass into the shipped binary. The no-seam proof is at `crates/oz-bridge/src/testing.rs:103-148`; the one-command check above is the whole verification.
- **The actual fix moves the claim upstream of the subscription read.** `create_staff_scoped` gates `staff:create` at `staff.rs:1073` and only reaches `sub.verify_signature()` at `:1080` — so a *permission* refusal is live in both profiles while a *duplicate-username* one is not. `a_rejected_create_records_no_security_event` (`2dc500382`) now refuses at the permission gate, which is upstream of `:1080`, and additionally pins that no user row was written so the refusal cannot pass as a passport stamp. Its subject — a refused create writes no audit row — is therefore genuinely exercised in release instead of going green on `0 == 0`.
- **The duplicate half survives as its own test**, `a_duplicate_username_create_records_no_security_event`, forked on `seeded_row_loads()`: debug asserts it against a **non-zero** baseline (`before == 1`, so it is never `0 == 0`), release asserts the seeded-row refusal with row existence pinned and stops. Coverage was not traded away to get the green.
- **3a.2's default is ruled FAIL CLOSED (owner 2026-09-16).** An unknown role floor on an admin-tool gate must deny. `roleAtLeast` (`ui/src/utils/role.ts:72`) demands `Number.MAX_SAFE_INTEGER` for an unrecognised floor; the inline `?? 0` in `WorkspaceHome.canAccessTool` demanded **0**, so every role cleared it — a latent fail-open on an admin surface. The gate now routes through `roleAtLeast` (`a8c1fb4c5`), collapsing two consumers of one table onto one default. Type-blocked today (`ToolRole` is `owner|admin|manager`, all in the table), so it is behaviour-neutral now and removes the latent case. Pinned by two new tests in `role.test.ts`: an unknown floor denies every role, and a **custom role clears no preset floor** — the second is the property the doctrine exists to protect, and it is what 3a.2's remaining work (replacing rank comparisons with permission checks) must be measured against. `role.test.ts` 27 → **29**; the Tools parity surface re-measured at **39/39** across `WorkspaceHomeTools.test.tsx` (9) + `WorkspaceHomeTools.navParity.test.tsx` (2) + `pageRegistry.test.ts` (28) — that figure was recorded as *run* at `:217` with an explicit "re-run before relying on it", and it is now actually re-run.
- Census unchanged at **29 open / 11 ticked**; no box was ticked by this pass, so the counters are preserved rather than massaged.

**Method note for whoever takes the next release-profile fixture.** The transferable move is *read the command body for a gate before the subscription read*. `2dc500382` did not make the signature verify; it changed what the test refuses at, so the claim landed on a gate both profiles can reach. Check that before concluding a case is parked, and before forking anything on `seeded_row_loads()` — a fork is the right answer only when no upstream gate exists.

---

## Rename audit 2026-09-18 (HEAD `19437867c`) — NOT renamed, and the reason is a count, not a mood

Asked: review the implementation and rename with a `done-` prefix if it is really done. **It is not, so the file keeps its `todo-` token.** The rule being applied is this file's own `:8` plus `AGENTS.md` §4 (*"`done-todo-*` is earned ONLY when that file's own acceptance command was RUN and PASSED"*), and the file states at `:19` that there is no program-level command — the acceptance is per phase. Four of the five phases therefore have to close before one filename can claim the set, and they have not.

**Census re-derived this pass, both counting forms agreeing per `:53`: 29 open / 12 ticked.** Per phase:

| Phase | Its acceptance, re-read this pass | Verdict |
|---|---|---|
| 1 | `cargo test -p oz-bridge --release` → 0 failed, **plus a runner that can see this failure set** | half met, half red |
| 2 | `cargo check -p oz-pos-tablet` clean · `cargo test -p oz-pos-tablet` green · `IPC parity: OK` | parity met; boxes stay open **by this file's own ruling** at `:167` |
| 3 | `npx vitest run` · `cargo test -p oz-core` · parity · `lint-i18n.sh` | not met — `:217`/`:218`/`:219`/`:220` all open, 3b needs a product ruling |
| 4 | `cargo test -p oz-payment` · `cargo test -p oz-hal` · `check:all` · parity | not met — R4/R6/R7 stay OPEN, `:276` open |
| 5 | a `--nocapture` run with **0** `test skipped`, **and** the 64 PG arms runnable-by-CI | not met — this file says the first half is "unsatisfiable without Docker" |

- **Phase 1, half one: RE-VERIFIED HERE, and it passes with room to spare.** `env -u HTTP_PROXY … cargo test -p kasirmu-bridge --release` → `test result: ok. 1328 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 216.55s`, exit 0. The `:539` record read 1317; the population has grown by 11 and the 0 holds. The proxy unset is not optional and is the reason this is quoted with its command.
- **Phase 1, half two: the runner exists and CANNOT SEE THE FAILURE SET — it is red on `main` for a build script, not for a test.** `dev-ci.yml:272` `release-bridge-test` (added by `9d5c33c68 ci(dev-ci): add a release-profile bridge test job on push to main`) runs `cargo nextest run -p kasirmu-bridge --release` at `:289` — the only `--release` runner in the tree, since `scripts/release.sh:62` and `release.yml:149` both still carry `--exclude kasirmu-app --exclude kasirmu-mobile` and no release profile. It fails on run `35293028418` at `error: failed to run custom build command for \`libudev-sys v0.1.4\`` (build-script panic, `libudev-sys-0.1.4/build.rs:38`), i.e. **before the first test executes**. The job's own comment asserted the opposite — *"WHY NO SYSTEM DEPS: `kasirmu-bridge` is headless by contract … so unlike `cargo-nextest` this job skips the apt-get layer"* — and it is false for a reason that is one command away: `cargo tree -p kasirmu-bridge -i libudev-sys` resolves **`libudev-sys <- libudev <- serialport <- kasirmu-hal <- kasirmu-bridge`**. Headless-by-contract is a claim about tauri/gtk/webkit and it is true; it is not a claim about `serialport`. Both sibling jobs already install the header (`:187`, `:235`); this one did not. **Fixed this pass** — the narrow subset `libudev-dev pkg-config` added ahead of the nextest install, comment corrected to carry the tree path, so the gate can reach its own tests. `verify-agents-mirrors.py` → *all 2 mirrors agree*, `verify-ci-docs-drift.py` → *0 drift item(s)*, both after the edit.
- **THE FINDING THAT OUTRANKS THE VERDICT: not one acceptance command in this file runs verbatim in this tree.** Every phase names a pre-rebrand crate — `oz-bridge`, `oz-core`, `oz-payment`, `oz-hal`, `oz-cloud-server`, `oz-api`, `oz-pos-tablet` — and Phase 2 additionally names a path that no longer exists. Measured: `ls crates/` holds 17 `kasirmu-*` crates and **no** `oz-*`; `apps/tablet-client/` is gone, the shell now `apps/mobile-tauri/` (package `kasirmu-mobile`, `37dfce036` P11 + `bb1e72eb3`), alongside `desktop-tauri` (`kasirmu-app`) and `cloud-server` (`kasirmu-cloud`). The substance survives the rename — Phase 1's half-one above is that command re-run under its new name — but a reader who pastes `:70`, `:127`, `:184`, `:235` or `:421` gets "package ID specification did not match any packages", and a `did not match` reads exactly like a `gone`. **A name-refresh pass was owed before any of these gates could be re-run, and it is not a rename of the file** — it is a rename *inside* it, which is the opposite operation and is not what was asked for here. **DONE the same pass, as a follow-up rather than as part of the verdict** — see the block at the end of this section.
- **Boxes that are paid but unticked, recorded rather than massaged.** `:112`'s runner exists (`dev-ci.yml:272`, and now runnable); `:109`'s "record it as a parked arm" half is paid in the code itself — `crates/kasirmu-bridge/src/locations.rs:239-241` reads *"Dev shim, and a deliberately PARKED release arm (`todo-open-debt-program.md:109`)"*; `:111`'s alternative ("or the doc should stop presenting the debug/release total gap as unexplained") is met by the `1318 − 1317 = 1` explanation; `:114`'s commits landed. `grep -rl "DELIBERATELY LEFT RED" crates/` → **0 files**, which is the `2dc500382` fix holding. What keeps Phase 1 open regardless is `:113`, which is an owner ruling by its own text, and `:547`'s deliberate refusal to tick — this pass does not overturn that.
- **Phase 4's `:276` is stale in one direction and unmet in the other, and both halves matter.** The box asks for an R5 **design doc** and states there is *"no `ResilientProcessor`"*. Measured now: `crates/kasirmu-payment/src/resilience.rs` **implements** `ResilientProcessor`, `CircuitBreaker` (Closed/Open/HalfOpen) and `ResilientProcessorConfig`, `lib.rs:70` re-exports them, and `registry.rs:57-67` is `register_method_fallback(method, chain: Vec<Arc<dyn PaymentProcessor>>)` — the `method -> Vec<processor>` chain the row said did not exist. So the code landed and the doc did not: `grep -rliE 'ResilientProcessor|circuit.?breaker' docs/` → only `docs/archived/manager-2-journal.md`, no design doc anywhere live. And the decorator is **not wired**: `registry.rs` contains no reference to `resilience`/`Resilient`, so the breaker exists beside the dispatch path rather than in it. Code before doc is the inversion this box was written to prevent, and it leaves `:276` open for the reason it was opened.
- **Phase 5's arms are still there and still need a database.** `scripts/verify-pg-tests-ran.py` exists; the arms are present in all three crates (cloud-server alone: `db_tests` 11, `email_pg_tests` 12, `sync_store_tests` 7, `prune_tests` 5, `sync_api_tests` 5, `redis_backend_tests` 2, `main_tests` 2). Nothing about that changed, and the Docker precondition is unchanged, so `:497`/`:498`/`:499` and the `slow-tests` box stay open.

**What would change this verdict, stated so the next reader does not have to infer it.** Two things, and neither is more code: (1) the `:113` owner ruling on the parked arm, which is the last thing holding Phase 1's *boxes* open after `:112` became runnable; and (2) the Phase 3b product ruling on the organisation axis, which `:219` says must precede any migration. Until at least those land, `done-` would be a claim about one phase wearing the filename of five.

**Not done here, on purpose.** No box was ticked and no phase was renamed — a review that ticks its own subject is the failure mode `:547` already declined once.

---

## Name-refresh pass 2026-09-18 — the acceptance lines now RUN; the dated records were left alone

Follow-up to the audit above, and the thing that makes its per-phase verdicts re-testable. **Scope, drawn on one line: live scaffolding renamed, dated records untouched.**

- **What changed:** the `**Fence:**` / `**Commit prefix:**` / `**Acceptance:**` block of all five phases, plus the live task-box instructions that name a path a worker must open (`:109`, `:111`, `:112`, `:113`, `:114`, `:171`, `:499`) and the Phase 5 heading. Plus a **name map in the header** so any stale path anywhere in this file resolves without a reader having to guess.
- **What deliberately did not change:** every dated record, every `[carried]` measurement, every ticked box, and the audit stamp at `:3`. This file's own rule is that a dated record is corrected forward, never back — and rewriting the paths *inside* a measurement taken at HEAD `257ff6122` would destroy the record it is. Those lines still say `oz-bridge`, and the header map is how a reader gets from there to here.
- **Verification, so this is a claim with a command rather than a claim with a tone.** Every `-p <name>` in the live acceptance lines was resolved against the workspace: `cargo metadata --no-deps` lists **39** packages and **all six** names — `kasirmu-bridge`, `kasirmu-core`, `kasirmu-payment`, `kasirmu-hal`, `kasirmu-cloud`, `kasirmu-mobile` — resolve, with **0 unresolved**. Every path named in a live fence or a renamed task box was tested for existence: **28 of 28 `ok`, 0 `MISS`** (crates, apps, the five `ui/` paths, `crates/kasirmu-api/Cargo.toml`, and the seven `scripts/` runners).
- **`kasirmu-cloud`, not `kasirmu-cloud-server`** — the cloud crate's package name is `kasirmu-cloud` while its directory stays `apps/cloud-server`, which is exactly the dir-vs-package split the header map exists to record.
- **The census is untouched at 29 open / 12 ticked**, re-derived after the pass, and the live scaffolding now carries **0** stale `oz-` names. The verdict above does not move: renaming the commands makes the phases *testable*, which is not the same as *passed*.
