# Ownership arbitration — todo-open-debt-program.md (27 open boxes)

STATUS: Success — read-only; no plan doc edited, no commit run, version locked 0.0.39. Branch 0.0.39 @ d00f4d16a.
Census (canonical pair, open-debt :339-:340): open-debt 27 open / 3 ticked · agents-3 4/43 · payment-agents-4 0/0 · payment 30/20 · tools 1/13 · operational-integrity 17/2 · review-type 36/0 · notes.md 23 items / 0 boxes [Fact: measured this pass]

## 1. THE RULE — "fence-and-grade, falsified-last"
A box belongs to the ONE doc satisfying, in order: (i) its declared **Fence names the artifact the box mutates**; (ii) its declared **Acceptance command grades that artifact**; (iii) **its premise is still true of the tree** — a box the code contradicts is owned by nobody and is deleted as paid, not re-worded. A self-declared "CANONICAL / single status authority" governs *verdicts in prose only, never a work queue*, and only inside its own epic; a newer dated header never decides; "the consolidation doc wins" decides nothing and is the failure under arbitration. Precedence: (iii) > (i) > (ii) > explicit cession-note > canonicity > recency.
Test 1 — the release --release leg. (iii) holds: `git grep -n -- "--release" -- .github/workflows/dev-ci.yml` → NOMATCH (re-measured). (i) splits it: open-debt :68 fences `scripts/check.sh`, `release.sh`, `.github/workflows/release.yml`; agents-3 OWNED PATH FENCE (:25-:29) names only `apps/desktop-client/*` + `apps/tablet-client/*` — scripts/ is NOT fenced there, so agents-3:168 is a fence-less box on another lane paths, and it is a DIFFERENT mutation (strip `--exclude kasirmu-app/tablet`; its own gate at :169 = five runners at count 0; measured today check.sh=1 vs check.ps1=2, so the matched pair disagrees). operational-integrity :93 cedes the release-profile question to open-debt Phase 1 and :212 repeats it. Verdict: open-debt keeps :112; agents-3 keeps :168, sequenced behind it. [Fact: agents-3:25-29,:168-:169; OI:93,:212]
Test 2 — tablet/bridge DTO seam. (i) open-debt :125 fences `apps/tablet-client/src/**` + shared DTOs under `crates/oz-bridge/src/**`; agents-3 also fences tablet but every remaining tablet row there is TICKED (:119-:123) and the live remainder is prose (:117), so (ii) awards open-debt the only grading command (`cargo check -p kasirmu-tablet` + `verify-ipc-parity.py`). review-type declares no fence and its own audit concedes the queue is "NOT OBSERVABLE as one command" (:255) — and it already points :131 INTO open-debt Phase 2 as a duplicate (:283-:285). Different axis anyway: open-debt = desktop↔tablet `*Args`; review-type:133 = Rust `*Dto` ↔ TS `ui/src/api/` (`scripts/verify-dto-parity.py`, measured ABSENT). Verdict: not a three-way tie — open-debt owns shell parity, review-type owns the Rust↔TS mirror gate. [Fact: measured]
Test 3 — the org axis. (iii) kills open-debt premise: `enum ScopeType { Organization, LegalEntity, Location }` exists [Fact: crates/oz-core/src/db/assignments.rs:127-131] behind a CHECK-constrained column [Fact: migrations/20260916_role_assignment_scopes.sql] plus an org-wide backfill migration (20260917_assignment_backfill_org_wide.sql, measured present) and an enforcing gate [Fact: todo-tools.md:452-:453 cites staff.rs:274-296]. todo-tools.md:445 states "the organisation axis shipped". What survives is ruling 1A — "Workspaces and terminals are deliberately NOT scope types" [Fact: assignments.rs:124-125] — a human ruling whose cost is written out [Fact: todo-tools.md:467-:478] and which tools.md itself assigns to the owner page (:479-:481), where it is already filed as notes item 18 (:1424). Verdict: the fence rule ALONE would be WRONG here; (iii) plus the docs own agreement sends the ruling to notes:18 and leaves tools.md:443 as the evidence row.

## 2. BOX TABLE (27 rows; "R" = rival owner)
| box | verdict | move-to / merge-with / park | one-line reason |
|---|---|---|---|
| :106 Classify the 76 | delete-as-paid + park | R agents-3:142; park notes item 14 (:1389) | population moved 76→65 [Fact: JOURNAL:10950] →2 [Fact: JOURNAL:11057]; the verdict table was never written because the cause was found first |
| :109 locations.rs:231-236 bypass | KEEP | merge R agents-3:152 (prose, same site) | fence :68 owns `crates/oz-bridge/src/**`; anchors re-measured live |
| :110 Fix profile-dishonest fixtures | delete-as-paid | residual → notes item 12 | "every mechanical red is now gone" [Fact: JOURNAL:11057] |
| :111 sync_tests.rs:35-37 | KEEP | merge R agents-3:166 | re-measured: still `#[cfg(test)]` + `#[cfg(debug_assertions)]` |
| :112 Add a --release runner | KEEP | sequence R agents-3:168 after it | only fence naming the runners; OI:93 + OI:212 cede it |
| :113 Owner ruling license.rs:670-683 | park | notes item 12 (:1379); R agents-3:155 already says PARKED | one decision recorded three times; keep a pointer, not a box |
| :114 Commit milestones | delete-as-not-work | — | self-labelled NOT WORK at :115; AGENTS.md §3 binds every lane already |
| :154 Wire audit per domain | KEEP | merge R agents-3:117 + R review-type:131 | review-type:283-285 assigns itself as a duplicate of this row |
| :156 Share the DTOs | KEEP | merge R agents-3 Phase 3.3 (ticked precedent :119-:121) | (i) and (ii) both here only |
| :157 AppState → BridgeCtx seam | KEEP | re-point R agents-3:117 | agents-3 defers it to "an Agent-2-fence decision"; ctx.rs sits in THIS fence |
| :158 Keep parity gate green | delete-as-not-work | fold into acceptance :127; R review-type:121-:125 | a standing constraint, not a task |
| :159 Commit milestones | delete-as-not-work | — | self-labelled at :160 |
| :203 3a.1 Fold duplicate rank table | delete-as-paid | R closed at todo-tools.md:873-:878 | `c8efd4b2a` deleted it; WorkspaceHome.tsx:13 imports the one table (measured) |
| :204 3a.2 rank → permission checks | KEEP | merge R todo-tools.md:870-:872 | the retirement note assigns it HERE; re-point :363/:372/:378/:414 → measured :364/:373/:379/:415 |
| :205 3a.3 Fate of roleAtLeast | KEEP | — | no rival; gated by :204 |
| :206 3b.1 Org/terminal design doc | park | notes item 18 (:1424); evidence stays todo-tools.md:443 | premise false — the org axis shipped [Fact: assignments.rs:127-131] |
| :207 3b.2 Implement after 3b.1 | collapse into :206 | — | no lane may write it until notes:18 is ruled; only arm (b) is code |
| :209 Commit milestones | delete-as-not-work | — | self-labelled at :210 |
| :261 R5 design doc | KEEP | status verdict stays with R agents-4:141 | agents-4 declares CANONICAL (:53-:61) yet holds 0 boxes (measured) → cannot own a queue |
| :262 R4/R6/R7 decision record | KEEP | merge R agents-4 R4/R6/R7 headings; cross-ref notes item 15 | only tickable home in the tree |
| :263 Fix agents-4 anchor | delete-as-paid | R OI:184/:187 ping-ponged it to this file | a4:126/:137 now print `crates/oz-hal/src/...` (measured); landed f09c5e28e + 048411986 while two docs argued |
| :265 Commit milestones | delete-as-not-work | — | self-labelled at :266 |
| :374 84 class names survive | move | notes items 11/17 + the screenExtraction lane (todo-refactor-pos-screen-agents-3.md) | outside all four fences [Inference: fences :68/:125/:170/:220] |
| :375 Widening is permissive — say it | move | notes item 17 | a disclosure discipline about a commit, not a box |
| :376 Shape 4 has zero real sites | move | notes item 11 | guard-file owner; no fence here reaches `ui/src/__tests__` |
| :377 Two censuses disagree | park | notes item 11 | its own text forbids merging or deleting → a record, not work |
| :378 Figures this append refuses | delete-as-not-work | — | a citation-discipline clause with no artifact |

Tally: 10 KEEP · 7 park/move (6 → notes.md, which carries 0 boxes by design) · 3 delete-as-already-paid · 7 delete-as-not-work-or-blocked. Rows carrying a named rival: 15 in the four phases + 5 in the :374-:378 append (whose rival is notes.md) = 20, matching the review. Four rows (:114/:159/:209/:265) and the whole append share one category error — prose restating a rule, or another lane work, wearing a checkbox.

## 3. WHAT THE FILE SHOULD BE
**Option A — keep "four workstreams, four fences", pruned to 10 live boxes (P1 4 · P2 3 · P3 2 · P4 2).** The four fences at :68/:125/:170/:220 are the artifact: nowhere else does one doc name the release runners AND the tablet/bridge DTO surface as an owned pair, and each phase Acceptance line is the command AGENTS.md §4 needs to earn a `done-` rename.
**Option B — thin index pointing at the other plans.** Maximises single-homing, deletes the 10 dead rows for free, shrinks the page to ~60 lines — but an index carries no fence and no per-phase command, so the program becomes un-renameable under §4 and `scripts/` loses its only owner. It also hides the one thing only this doc supplies: the cross-phase sequencing at :281-:290, which no rival states.
**RECOMMENDATION: Option A**, plus three mechanical edits — add a Rivals-and-cessions table (OI:93, OI:212, review-type:283-:285, todo-tools.md:870-:872, agents-4:53-:61); replace each paid/parked row with a dated one-line pointer to notes items 12/14/18; lift :374-:378 out under its own heading. Rejected: Option C (two-box release-leg plan) — it discards Phase 2, the only phase fully fenced, fully commandable and rival-free.

## 4. THE TWO BOXES WHERE BEING WRONG IS EXPENSIVE
**A. :206 / :207 — the org axis.** Irreversible: the axis is already a CHECK-constrained column with a backfill behind it, so a lane that "designs the missing axis" adds a second competing identity for the same grant and changes the meaning of every scoped write on every existing install. A migration is not amendable in a shared checkout and `20260813_init.pg.sql` is generated, never hand-edited [Fact: AGENTS.md §4]. The price is already written [Fact: todo-tools.md:471-:473: parse + as_str + the CHECK + its PG twin + a new `resource_covered_by` arm]. Settles without guessing on three cheap reads: `assignments.rs:118-:137` (ruling 1A), registration in `migrations.rs`, and `git grep -n "ScopeType::" -- crates apps` — if only `Organization` is ever WRITTEN (measured: every non-test hit is Organization; LegalEntity/Location appear in test rows), then the live question is exactly notes item 18 arms (a)/(c): a one-line ruling, not a design doc.
**B. :106 / :110 — classify and fix the release reds.** Epistemically irreversible, and it already happened once: that campaign closed the reds by forking fixtures on a seed-row predicate (146 `seeded_row_loads` uses across 13 files) and the trade spent five named security properties — ticket rotation, ADR-47 grant containment, ADR-48 impersonation/restore-revocation, licence TTL, staff write-side audit [Fact: JOURNAL:11057] — so a re-run can leave a release leg permanently unable to tell you anything. Settles with ONE measurement, not a guess: `cargo test -p oz-bridge --release --no-fail-fast`, then diff the failing names against the two that entry calls deliberate. That output alone decides whether :106 closes as paid, becomes a notes-12 seam request (a minted real signature buys all five properties back), or stays a live red list.
Runner-up, already de-risked — do not re-open it in a lane: :113. Both ways to cover `license.rs:670-683` change production [Fact: open-debt :113; agents-3 :155], so it stays an owner ruling with a pointer.
METHOD LIMIT: no commit, suite, cargo or build was run; every "measured" figure is a grep/read of the working tree at d00f4d16a, and the 1307/2 release total is a lane print quoted from JOURNAL:11057, not re-observed. Three `crates/oz-bridge` files are reported ` M` by that entry, so each count is worth its timestamp (this file own rule at :41). [Inference]
