# Review backlog — codebase review, consolidated with corrections (rev 4, 2026-09-12, final pass)

Read the last two sections first, before the findings. **Baseline: read against HEAD a80e3b709 on branch 0.0.37; the tree moved twice more while this was written and the index said 282f4310d at save time, so every file:line must be re-verified before it is edited against — two auditors read a tree the index called 3be794945 and lines move faster than citations. Verification re-run at save time rather than asserted from an earlier draft: `git status --porcelain` reports 28 entries (23 at the start of this pass, the additions are other agents' work), five of them staged-plus-worktree 'MM', and this document is the only untracked file among them; `.gitignore` shows as modified but predates this session (mtime 2026-09-11T15:17:31.587Z) and was not touched
here; nothing was staged, committed, stashed or reset by this review, and the two stray files created earlier by a `>` redirect inside a quoted shell string were deleted and verified gone. Dimension-split, strictly read-only review: 28 workers, 23 adversarial refutation passes, ~35 reports (security, correctness, concurrency, tablet shell, topology, IPC reachability, gates/suppressions, migration durability, PG mirror, money, licensing, offline sync, ops, a11y/L10N) at **Baseline head 707ab6889 on branch 0.0.37**. **The tree moved ten times under this review: Dc148cb73 -> 0d0c1eda5 -> 8c626dcc1 -> 53fffa8c1 -> 99aa0817d -> 14a31a31f -> d0a9b2f47 -> 245e969d5 -> 245e969d5 -> 707ab6889 — Seven commits in ~20 minutes across five subsystems while workers
were still reading the files they changed, so every file:line cite here predates a moving baseline and should be
re-resolved before being edited against.** Two of those commits fix findings from this review (0d0c1eda5 mints the checkout idempotency key; 53fffa8c1 adds the cumulative refund quantity bound, +326/-4). Still dirty: 23 entries with five 'mm' (desktop commands/locations.rs + workspaces.rs, oz-bridge settings.rs + workspaces_tests.rs, ui WorkspaceHome.tsx) — **Read mid-edit, treat as unstable, including crates/oz-core/src/db/offline.rs + offline_tests.rs + quota_gate.rs, which carry the uncommitted enqueue gate at the centre of P0-2.  Run-vs-read, and the distinction is load-bearing: This review executed nothing — No build, no test, no cargo, no npm, no database opened, and the tree is concurrent-agent dirty (707ab6889 baseline; other
live agents own the changes) — So every conclusion below is static evidence. But that caveat no longer applies to the fixes, and the strongest verification evidence in the session is not ours: 99aa0817d raised a seeded qty 2 -> 3 in crates/oz-core/tests/refund_tax_integration.rs because 53fffa8c1's bound broke an existing test — A predicted failure does not tell you what the fixture used to hold, so somebody ran something.** Record it, or the user will under-trust a committed, tested change and over-trust our readings. The only executed work on our side is the refund/stock simulation (P0-6/P0-7), run on throwaway:memory: SQLite with guard arithmetic re-coded from source — A simulation of code we did not compile. **The process headline,
and it is the second finding: 11-12 relayed premises were killed, 7 of the review's own escalations were killed by the workers sent to confirm them, 4 findings were confirmed as published, and 6 numbers moved** (drift 31->131->53->21->3; tablet-only '25 features'->0 screens; shims 15-of-top-20->11/41 lines; inline tests 646/629/1,340; IPC 448/450/453/473; id-locale 40%->5.6%). *** no confidence list from this review survived contact with its own workers — And where a claim outlived its own supporting fact ('no readiness gate exists' is true while the count that supported it was fake), both are printed, because the claim is not protected by the evidence under it. Every count below therefore carries its definition and its survivor
status.*  The spine, earned by seven independent observations: This codebase already contains the correct implementation and exactly one call site misses it** — The clamped token TTL exists in oz-local-api (lib.rs:240-242) and not oz-api (auth.rs:200-202); the retail register has an aria-live announcer (RetailCartPanel.tsx:392) and the desktop cart does not (CartPanel.tsx:511-512 puts it on the undo pill); the pre-apply snapshot probe fails loudly (persistence.rs:520-536) and recovery discards every result (:563,:569); per-store opens soft-fail (desktop lib.rs:378,:632) while the global migration call is fatal (state.rs:212-213); the duplicate-id predicate guards two of three push handlers and not pg_daemon; store_db_exists() exists
(manager.rs:170-173) and recovery never calls it; **And now the cleanest instance: The desktop checkout does verify the subscription signature (oz-bridge/src/pos.rs:1557-1571: Load -> verify_signature()? -> enforce_pos_writable()?) and the queue path at offline.rs:238 does not — Same subscription row, one path signed and early, one not, separated by a discarded publish (P0-2).  Not missing knowledge — Missing application; the fix class is convention-enforcement, not seven patches.****

## P0 — Money and data that is wrong today

**P0-1. A tablet sale never leaves the device, silently, and every visible surface agrees that it has.** The escape hatch that was expected to soften this turned out not to exist; the qualification first carried here has been resolved to absence (see the killed-claims section). The sale is written to the global oz-pos.db (tablet pos.rs:1186-1187 state.db.lock ->:1187 Store::new), the reaper voids it there, and the queue row is enqueued there and is visible to the daemon — same file, two WAL connections (enqueue = startup/src/lib.rs:112 open_handler_connection(db_path) ->:49-55 Connection::open(the global path) -> SaleSyncEnqueuer:120-122 -> enqueue_offline_priority('complete_sale', Critical) at event_handlers.rs:63-72, fed live from
pos.rs:1264-1277; daemon read = tablet lib.rs:312-313 ->:325 -> db/offline.rs:256). The cause is a config split across two files: **The daemon reads the enable flag and server url from the global DB** (tablet lib.rs:314 SyncConfig::from_settings(&store) -> crates/oz-core/src/sync_client.rs:271,:275,:280-281 -> platform/core/src/settings/typed.rs:373-374 is_sync_enabled = get(conn, keys::SYNC_ENABLED)? == Some('1'),:332-334 for the key) **-> Ok(None) -> tablet lib.rs:335-337 'let Some(config) = config_opt else { continue; }' => every 30 seconds, silently, forever: No push, no error, no badge — While the only sync-settings command a till can call writes them into store-<id>.sqlite** (tablet lib.rs:761 update_sync_settings_scoped ->
sync.rs:357,:362 resolve_scope ->:368 update_sync_settings_data, tx:84-95). The UI's reader matches its own writer (sync.rs:338 per-store, behind ui/src/api/offline.ts:156) => **Settings shows the operator exactly what they wrote and looks consistent while the daemon never hears about it.** [Fact] **The full writer table, every production writer of sync_enabled / sync_server_url:** #1 tablet update_sync_settings_scoped = per-store, registered, the only one a till can call; #2 tablet sync.rs:59 update_sync_settings ->:63 state.db = global, **Not registered** (the tablet sync block is scoped-only except test_sync_connection at lib.rs:608); #3 crates/oz-bridge/src/sync.rs:253-258 -> ctx.lock_global() = global, **Not registered on either
shell** (desktop's sync registrations are all *_scoped, desktop lib.rs:1054,:1247-1249,:1253-1255,:1258-1259, and the fn sits unregistered at apps/desktop-client/src/commands/sync.rs:58 = dead IPC) => *** the global writer is unreachable on both shells, and that is the facade re-export finding (see P1) doing the damage. *** #4 apps/desktop-client/src/sync_bootstrap.rs:77-88 persist_provisioned_sync is the only caller of set_sync_enabled(true) (plus:225/:248 terminal id/secret) on the global DB — **But its call site is #[cfg(debug_assertions)] (desktop lib.rs:233) and the tablet has no sync_bootstrap at all (0 hits). #5 daemon_tick.rs:77-86 persist_migration_url writes the url only, never the flag, and the tablet daemon (lib.rs:304-400)
never calls daemon_tick. #6 tablet commands/settings.rs:547-564 set_setting (:561 state.db global, arbitrary key, SETTINGS_EDIT gate) is registered (lib.rs:589) so a capability exists, but no product surface calls it with a sync key (the only production setSetting caller is UpdateBanner.tsx:211, an updater key) [Inference from a capped grep]. #7 restore is closed, not unproven: Settings are carried in .ozpkg (crates/oz-core/src/ozpkg.rs:118) but **The tablet has no data surface at all** (0 hits import_data|create_backup|restore in tablet lib.rs; ipc-parity-allowlist.json:2 says so) so 'works by restore inheritance' needs a desktop restore on the till's own machine, which a till cannot perform. #8 nothing seeds the flag: 0 hits 'insert
into settings' and 0 for 'sync.' under crates/oz-core/migrations/*.sql. #9 no cloud->device settings writer (oz-api/src/pg.rs:63 set_setting_pg writes postgres cloud-side, not the till's sqlite); no remote-mgmt code (0 hits).  * The verdict lines: 'a paired tablet pushes: No — No pairing path writes those keys' (and crates/oz-bridge/src/pairing.rs does not exist at head: Glob **/*pairing* = 0 files, write_pairing_settings = 0 hits repo-wide — An earlier relay in this review's own chain, and the hedge it produced is deleted here). 'a fresh unpaired tablet pushes: No — No seed, no registered global writer, and the daemon reads the flag nothing sets.' [Inference, extending the audit's own writer table by one step] *** the only path that
calls set_sync_enabled(true) is cfg(debug_assertions), so no production build of either shell has a provisioner at all: The sync feature is debug-only, not merely tablet-missing. *** Also found: **LOCAL_SYNC_URL (sync_bootstrap.rs:38) holds 'https://license.ozpos.my.id' — A production cloud domain behind a local-sounding name, in a debug-only provisioner** (same host as P0-2's LICENSE_SERVER_URL: Two passes, one domain, found independently). *** Q3, the diagnostic closure: The offline queue screen binds list_pending_offline_scoped (offline.rs:417) and pending_offline_count_scoped (:451), both per-store, so a support agent standing at a till has no path to the truth — Only the unregistered global writers #2/#3, an adb-time set_setting,
or opening the two files. *** (test_sync_connection is the truth-teller in code but not in practice: With no url argument it reads state.db global (sync.rs:246-247,:252-256) and returns 'No server url configured' — The opposite of what settings displays; but both variants take url: Option<String> (:240,:538) so a pasted url gets ping_server = 'connected', and the scoped variant the section is wired to (api/offline.ts:269 -> sync.rs:546-554) reads the same per-store file settings wrote, so it always agrees with itself => a green 'test connection' proves a host answered, never that the daemon's own config exists. [Fact for the connections; inference for the wiring — Their testSyncConnection and setSetting greps hit the 16-result cap and
are marked so.]) three workers, three files, one cause (this, the unbounded queue at offline.rs:396,:409 — Something is filling it — And ops' 'sync never auto-starts and the UI lies'), and *** a second, independent reason the same sale might never sync: See P0-2(e)/P0-3. *** H4: **The app creates the wrong file on startup every time** (SettingsContext.tsx:268 getSyncSettingsScoped -> sync.rs:338 -> manager.rs:81 open_or_create_connection +:103-116 migrations::run => an empty-but-fully-migrated store-<id>.sqlite). [Inference on which screen calls which; the handle split is Fact.]

**P0-2 The unsigned /status body can lock a till, and the still-uncommitted enqueue gate then records sales that are structurally guaranteed never to sync.** (a) **The only locking value is lifecycle_state == SubscriptionLifecycleState::Expired (subscription.rs:899-902); the _ wildcard arm maps to Unavailable, which is writable (:833-869), so 'trial' / 'past_due' / 'suspended' / '' / null / malformed do not lock. An earlier malformed-body framing of this was retracted on re-read of the arms and is not stated in this document. Expired is reachable by exactly four writes: Status='expired' (:836); *** 'active' + an unparseable expires_at -> Expired at:848-851, whose own comment says 'fails closed here' — A fail-closed arm that locks a
till on a date-format disagreement ***; 'active' + now > expiry + offline_grace_days (:866-869); 'active' + trial end passed (:856-861). Plus two clock arms: Lifecycle_state_with_timestamp 'Err(_) => Expired' (:822-824) and compute_max_ledger_timestamp 'Err(_) => true' (:894). (b) **The write is unsigned: License_verification.rs:642-659 'update tenant_subscription set status = ?1, expires_at = ?2' from raw resp.status, no verify call, no allow-list; caller oz-bridge/src/license.rs:493-505 feeds &resp.status / resp.expires_at straight off /status and only tracing::warn!s on failure.** ('active' + missing expires_at is the harmless direction,:845-846 -> perpetual active.) (c) *** pure local read — 'the air gap is on the victim side, not
the poison side': TenantSubscription::load + MAX(created_at) over sales/audit_log (:434-452). No network, no token, no re-verify, no oz-local-api; a till that goes offline forever keeps refusing on a row it cannot RE-check. *** the server is the vendor's (LICENSE_SERVER_URL = https://license.ozpos.my.id, license_verification.rs:34; OZ_LICENSE_SERVER_URL override:47-48), not the merchant's cloud-server and not a settings row => **'one bad deploy stops every store' is true in shape: One /status response returning 'expired' to every online terminal, each writing it into its own local row, then never needing the network again. Two passes, one domain name — The same host P0-1 found in LOCAL_SYNC_URL.** (d) *** the data loss, and it is not
paralysis, it is silent revenue disappearance: Tablet commands/pos.rs:810 create_sale ->:814 Active ->:815 Completed, then:850 'if let Err(e) = bus.publish(&event) { // Logged by the bus; do not fail the command.' the failing handler is SaleSyncEnqueuer (platform/startup/src/event_handlers.rs:63-72) -> enqueue_offline_priority -> enqueue_offline_inner, *** where the uncommitted:238 gate lives. => sale row survives, sync row refused, nothing ever sent, caller returns success. *** create_sale carries no enforce_pos_writable of its own: Before:238 nothing on that path blocked an expired subscription; after:238 it records the sale and drops the sync behind a warn!. **The desktop canonical path is the good one — Sales_checkout.rs:206
enforce precedes unchecked_transaction at:210, and oz-bridge/src/pos.rs:1557-1571 does load -> verify_signature()? -> enforce (shortfalls variant identical at:1352-1369): A pre-commit refusal, no row, till paralysed, the customer's money not taken.** (e) **A 'backlog freeze' framing was retracted on re-check: the gate is ingress-only,:256 'where status = pending' is ungated (the hunk is @@ -237,0 +238,4), pending rows still push, and because the refused insert never executes, *** failed_count (offline.rs:440-447) is never fed — No monotonic failure number. 'ingestion starvation' is the live phrase. *** (f) K5: **Git show head:crates/oz-core/src/db/offline.rs | grep enforce_pos_writable = nothing (exit 1), so:238 is the first gate on
the queue surface and it is placed after the commit — Redundant up front, novel and wrong behind** (for the canonical path:206 already refuses, so:238 is a second copy of an existing guarantee; everyone checks the front door). (g) *** keep it cheap and honest: Offline.rs, offline_tests.rs and quota_gate.rs are verified still ' M' (uncommitted) at 02:40, so the placement can still be corrected before it lands — The only fix in this report that is free tonight, and why it ranks first in the fix list. *** (h) **Ordinary data can lock a till, with no server and no attacker.** An empty-table worry about this clock is dead: compute_max_ledger_timestamp cannot itself Err, because both MAX() calls end in .unwrap_or(None)
(subscription.rs:438,:445) and the empty-empty case returns Ok(Utc::now()) (:457-459). What does fire is a future-dated row: delta > CLOCK_SKEW_TOLERANCE_SECONDS (subscription.rs:486, tolerance 30 at:27) is an error, and ? propagates it out of validate_clock_rollback on login (tablet commands/auth.rs:499) — **one row written 31 or more seconds ahead locks the till at login.** Both columns are text on the same strftime default (sales.created_at, init.sql:606; audit_log.created_at,:46; zero hits for CURRENT_TIMESTAMP and zero for the space form across migrations/*.sql), every writer uses to_rfc3339_opts(Millis, true) (models.rs:174; audit.rs:79), and no client-supplied created_at exists on the tablet path, so the trigger is a watch set
forward or an NTP correction on a till whose clock ran ahead. **Published as a capability, not an incident: nobody measured one.** It also gives the future-dated 20261001 migration an ordinary, human route to matter — an operator setting a clock forward to test a dated feature writes a login-locking row on every till that receives it. (i) **The half that makes this P0 rather than a hardening note: there is no offline cure.** Two production writers move status off 'expired' and both need the network: store_subscription's INSERT OR REPLACE (license_verification.rs:601-618, signed, reached from activate_license at oz-bridge/src/license.rs:170 and renew_license at:282) and refresh_subscription_status_from_server's UPDATE (:649-656, **the
unsigned one**, sole caller check_license_status at:497). The migration seed is INSERT OR IGNORE (init.sql:1513) so it cannot overwrite, and pause/resume (license.rs:717-749,:755-784) never write the row. **The only way back from a lock is the same unsigned /status write that can cause it — the recovery authority and the attack surface are one mechanism.** On the till, check_license_status is registered on desktop only (desktop lib.rs:1129-1130; zero commands::license registrations in tablet lib.rs), so a locked tablet may have no operator-reachable re-verify at all; that clause is an inference from a registration grep, not a traced interface, and is recorded as unmeasured. Three holes stay open and are stated as open: the rusqlite
NULL-to-Option mapping is unverified, no enumeration of timezone-bearing writers for those columns was done, and no UI button was traced for the re-verify surface. and the spine instance folded in: **Offline.rs:238 also bypasses the secret deny-list — But not because the list is dangerous: SYNC_ENABLED and SYNC_SERVER_URL are in neither list (keys.rs SECRET_KEY_DENY_LIST:221-236 carries SYNC_API_KEY:222 and SYNC_TERMINAL_SECRET:223; NON_EXPORTABLE_DEVICE_KEYS:248 = [sync_terminal_id, machine_id]), the daemon uses typed accessors that never consult it (zero hits for is_secret|DENY_LIST in typed.rs or settings/mod.rs) and set is unblocked (run_set_setting gates only local_api./lan_server. Prefixes) => an 'and now it is unreadable by
design' escalation was killed on re-check and is not stated here; the flag is simply never written, which is simpler, duller and harder to argue with. Two [Minor] residues: Run_get_setting returns Ok(None) for a secret (:415) so IPC cannot distinguish unset from secret-set — The same absent-row-means-false ambiguity as P0-1, in another subsystem; and a restored .ozpkg yields a till with url + enabled flag and no credential (export drops the three sync_* secret keys), which the daemon re-mints (sync_auth.rs:471-472 via daemon.rs:170, daemon_tick.rs:36,:143) — Self-healing, but only once P0-1's flag ever reaches the global row, so the two findings are coupled and the second cannot be observed until the first is fixed.

**P0-3. IPC authorization is enforced per command by copy-paste, and the two till shells have drifted.** **27 commands check a permission on the desktop and nothing on the tablet — including the cash drawer, which the desktop gates on payments:cash and the tablet opens on any valid session.** Measured at a80e3b709: **tablet 442 command fns, 318 registered, 62 that authenticate a session and authorize nothing — all 62 on live IPC, none sitting on an unregistered fn — and 58 actionable.** The reason to trust 58 rather than 62 is that four auth-flow items (create_session, session_keepalive, destroy_session, switch_organization) are legitimately pre-permission, and that exclusion should be a written rule rather than a reader's taste. The
tablet figure is worth a method note because it read 2 for most of the review: a scan matched the literal `#[tauri::command]` while the tablet spells it `#[command]` (422 occurrences of one spelling, 25 of the other), so it saw 4.8% of the shell. **The same dead-IPC disease is three times worse on the till: 124 tablet command fns are not registered at all, against 43 on desktop.** Desktop for comparison: 491 fns, 448 registered, 404 take a session, 349 guarded, 60 session-but-no-authz of which 46 are live-and-called. Two earlier claims here were refuted and are not restated as findings: a 57-command count that came from a script missing two guard helpers, and an authorization catch-all arm that turned out to be an error converter
(authz.rs:227-258, with:238 preserving PermissionDenied). - Named items, each with the desktop twin that does check: **open_cash_drawer_scoped** (tablet commands/hardware.rs:388-402 — resolve_session, then drawer.open()) against oz-bridge/hardware.rs:416-424, which passes permissions::PAYMENTS_CASH and carries its own comment recording the intent ('F-017: enforce per-domain permission on this scoped command'); **adjust_stock_scoped**, where desktop passes permissions::INVENTORY_ADJUST (bridge products.rs:483), so **inventory shrink is permission-free on the floor**; **export_eod_report_scoped, export_daily_summary_scoped, export_sales_by_hour_scoped**, whole-store financial exports; and **get_setting_scoped plus four sibling setting
readers, in a file that carries its own secret-key deny-list header** — the same subsystem as P0-2, and the two chapters are one story: **the till can read keys the code calls sensitive, and the key it cannot read is the one that decides whether the till works.** complete_sale_with_resolved_shortfalls_scoped (a sale-completion path whose plain twin is checked and this variant is not) is classifier-only [inference], marked and excluded from the 58. - **Four of the 62 sit in the allowlist's scoped_orphans array, whose own text says each entry is 'a permission check that currently guards nothing' — for those four there is not even a check to guard nothing, and unlike desktop's 14 orphans these are reachable from a till.** A tidy
154-versus-154 coincidence between the tablet orphan count and the allowlist size was reported earlier and is false: 0 of the 62 appear in that array, which measures a different axis (interface calls the shell does not register). - **31 of the 60 desktop-side names are cited in a CI-read allowlist** (ipc-parity-allowlist.json: desktop 0, tablet 14, scoped_orphans 14, dev_mock 3); the gate catalogues these exact files and still reports OK, and verify-scoped-coverage.sh cannot disagree because it pins scoped-name existence and allowlist membership, never a permission call. **A command can be triple-certified — has a scoped variant, is allowlisted, passes ipc-parity — while checking no permission whatsoever.** Two entries,
list_topology_templates and load_topology_template, are carried by a category whose written admission test is 'still authenticates and checks its own permission inline' (verify-scoped-coverage.sh:38-40,:46-51); they satisfy the first half only, so **the file contradicts its own criterion.** Six of the 60 read the global identity db with a session and no permission (list_workspaces_scoped, list_workspaces_for_store_scoped, list_scanners_scoped, list_topology_templates, load_topology_template, enqueue_offline_scoped), which is what earns the gate's own line that anyone who can reach IPC can read any store — attributed honestly, the command that quote originally described, get_daily_revenue, takes no session at all and is a different
class. **25 of the desktop 60 are desktop-only (448 - 318 = 155, of which the earlier 154 is the same list off by one — stated rather than smoothed) and should be read as absence rather than exposure: they are unreachable on a till.** - **No denial test exists for any of these names.** All 23 repo-wide hits for the three hardware names are wiring or mocks (api-hardware-contract.test.ts:99; api-browser-contract.test.ts:34,:59 assert invoke name and argument shape, one asserts a 'command not found' mock), so **there is no template to copy: the gate functions are tested (authz_tests.rs, eight denial assertions) and the wiring is not** — the same shape as the drift classifier, the parity gate, the dev-mock and
publish_with_no_handlers_does_not_error. **Every test in this repository checks the mechanism; none checks the attachment.** - **The fix is one line per item:** add `ctx.require_user_permission_scoped(..., permissions::X)` where the registry already has the key (products:read, payments:card, settings:edit, terminals:read, workspaces:switch), **plus two new registry keys for the hardware and license/device families, which have no vocabulary to check against at all** — the 93-key registry contains neither, so for two whole domains the absence of a guard is not negligence, it is that nobody ever made a key.** Highest value per minute in the report.**

**P0-3b. The silent-drop mechanism behind P0-2(d), and it is its own chapter: EventBus::publish cannot fail, so the sale->sync enqueue is a single point of silent failure.** [Fact] platform/kernel/src/event_bus.rs:228-231 publish<e>(&self,&E) -> ModuleResult has **Three Ok(()) exits (:248 topic with no handlers, logged at debug!;:286) and no branch returns Err; there is no channel — Dispatch is synchronous over a snapshot cloned under a read lock (:239-251), lock poisoning is recovered not errored (:243 unwrap_or_else(PoisonError::into_inner)), and no try_publish / publish_priority / publish_all exist. *** two discard sites inside publish: (1):259-267 handler returns Err -> error!(topic, handler_index, module, 'event handler failed
(continuing)') -> loop continues -> publish returns Ok; (2):256,:268-281 handler panics -> catch_unwind -> error!(panic) -> dropped. *** the Err can never fire not because nothing fails but because every failure is pre-converted to a log line; the only failure channel in the bus is tracing.** consequence: **The 11 production 'if let Err(e) = bus.publish(...)' arms are unreachable, 11 of 11** (tablet commands/pos.rs:851,:1275,:1531; tablet commands/products.rs:76,:460,:730,:900; oz-bridge/src/settings.rs:1029,:1095,:1169; oz-bridge/src/ctx.rs:398 — Not platform/startup/src/lib.rs, where there are zero publish sites; an earlier citation there was wrong, and the desktop has none because it goes through ctx.publish_event) **And class (ii)
is structurally impossible: EventBus is a concrete struct, grep 'trait EventBus|dyn EventBus' = zero hits, publish is an inherent method — No test double, no no-op bus, no EventSink impl can exist. (An earlier fallible-trait-object hypothesis was the only thing that could have made the arms live, and it is false; '12' was 11 arms + one non-arm line, and 4 more sites are the ctx.rs:393-399 publish_event wrapper which eats the result itself, 14 are the file's own tests, 1 a doctest, 3 .unwrap() inside mod tests.)  * the arms carry comments: Pos.rs:852, products.rs:77 'Logged by the bus; do not fail the command.' the author knew; the arm exists only to satisfy #[must_use] on Result. That is a documented, intentional silent-failure
contract — Worse than an oversight, and the reason nobody will find it by grepping for missing error handling. *** the harm: **The sale->sync enqueue is a subscriber side-effect (startup/lib.rs:118-122 subscribes SaleSyncEnqueuer on 'sale.completed'; event_handlers.rs:63-72 map_err + error! Then ? Returns Err, which publish discards at:259-267), and an unfiltered git grep -n enqueue_offline -- crates apps platform modules ':!*tests*' returns only the queue commands and the store fn (db/offline.rs:105) — No call from db/sales*.rs or finalize_sale. => a poisoned handler conn (:46-49), a disk-full, or a constraint failure on the queue insert and the sale completes, the arm cannot fire, Ok(CompleteSaleResult) is returned at:857, and the
row the daemon would read does not exist.** strictly worse than 'the daemon never reads the queue' — **There is nothing to read, no user-visible error, one error! Line in a log file no customer has. So i now have two independent reasons a tablet sale might never sync, and even a correctly configured daemon can be starved.** the same shape covers the other subscribers on that topic — AuditLogHandler:133, SaleCompletedReporter:163, LoyaltyEarnHandler:171 => **Loyalty points and the audit row can each be silently missing while the sale reports success** [Inference: Those 5 are the only subscribers, from reading init_module_system's subscription block:104-172, not a runtime dump]. Zero-subscriber semantics are pinned by a test:
Event_bus.rs:481-484 publish_with_no_handlers_does_not_error asserts is_ok() with the silent early return at:246-249 => **No test can assert a publish failure was handled (C1b) and none asserts an event reached a subscriber except subscribe_and_publish (:465) which registers its own handler inline => a missing, mis-registered, renamed or orphaned subscriber is indistinguishable from success, by construction and by assertion.** and the repo admits it: Startup/lib.rs:124-133 carries a crm-06 comment recording that CrmHistoryHandler's sale.completed subscription was hand-removed ('its projection moved into the completion transaction ... The handler had no idempotency guard ... Would double-count every sale') — **The fix for a silent-bus
bug was to stop using the bus.** still-publishable instance: Modules_reporting::handlers::SaleCompletedReporter (:163-165) is wired only inside init_module_system, called at desktop lib.rs:112 and tablet lib.rs:80 — A shell or preset reaching on_start without that call, or topic-string drift between subscribe(...,'sale.completed') at:119,:134,:164,:172 (a raw literal five times) and event_name(), leaves EOD/daily reporting permanently under-counted with every publish returning Ok. **This is also the joint two independent passes hit from opposite directions: F05fe3dc found (via the bus) that a failed enqueue is invisible because publish discards the Err; 4fcf9d14 found (via the gate) that the enqueue can now be made to fail remotely and
is still invisible.** The strongest corroboration of the session, all on the money path.

**P0-4 the structural cause of P0-1 and of four things nobody connected — Two-database split-brain: The tablet writes sales to the global file and nearly everything about those sales to per-store files.** Handle table (state.db.lock vs resolve_scope|open_store|resolve_store): Pos.rs 36/0, void.rs 2/0, refunds.rs 6/0, memo.rs 4/0, reports.rs 1/0, **Kds.rs 0/5, fiscal.rs 0/5, local_payment.rs 0/2, loyalty.rs 1/8**, offline.rs 10/8, sync.rs 12/6, customers.rs 6/7, tables.rs 9/9, **History.rs 5/5 — Unscoped twins global, every *_scoped per-store, the split-screen file.** Nothing retargets downstream (sales_checkout.rs:210 uses the given conn). **One defect, five symptoms, all verified by path, none of which looks like the others from the
outside — Which is why it was never fixed: No single screen shows the cause.** - (K1) **NotFound on every tablet restaurant checkout — The sale cannot resolve its own kds child.** kds.rs:112 resolve_scope -> per-store Store ->:119 complete_sale_to_kds -> kds_lines.rs:61 get_sale(sale_id)?.ok_or_else(NotFound{entity:'sale'}); PaymentModal:778-786 toasts payment-toast-kds-failed while the sale stays committed. **Corrected by Q2: Sync_pull_scoped reads config per-store (sync.rs:585) but:604 'let db = state.db.lock().await' ->:607 apply_snapshot writes to global, and apply_snapshot upserts exactly products, tax_rates, users (crates/oz-core/src/sync_pull.rs:260-262; upsert_products:288-295 also stamps store_id at:290) — No stock rows at
all. So the per-store files are empty of catalog as well as sales: Not 'a table that exists but is empty of this sale' but a file with nothing in it. The earlier 'insidious version' (a pull half-provisioning the store file) is refuted, and phantom stock is not replicated across store files by the pull, so the fleet-wide-phantom worry is closed. Conversely a successful pull lands catalog in the same global DB the till sells from, so that half is coherent.** Same mechanism fiscal.rs:38,:81,:113,:135,:159; local_payment.rs:34,:45. [Inference on which screen calls which; the handle split is Fact.] - (K2) **Tablet void never clears the board — A kitchen keeps cooking voided orders while the customer has left.** The reaper's
cancel_kds_orders_for_sale_in_tx (sales_lifecycle.rs:615) runs on the global conn; kds_orders rows are per-store. **And the tablet cannot cancel them itself either: No void path writes the per-store kds_orders rows the board reads.** (Rev 2's 'inert' was corrected by its own author: Inert and part of this pattern.) - (receipt + wire) **The printed receipt uses client-side totals with no backend tax or lines.** get_sale_scoped reads per-store (history.rs:316,:321) and is paymentmodal's own receipt re-fetch (:1004-1006) => completedSale is None for every tablet sale, optional-chained per line at:1014-1017 so nothing surfaces; the unscoped twin in the same file (:112,:116) reads global and would have found it. Same split makes
list_sales_scoped / export_daily_summary_scoped / export_sales_by_hour_scoped / export_eod_report_scoped (history.rs:280,:351,:369,:387) empty for tablet-origin sales while the unscoped twins (:56,:143,:155,:203) are correct. See also P1's camelCase/snake wire mismatch, which kills the re-fetch through a different door. - (analytics) analytics.rs splits one function across both files — Names global (:65), aggregates per-store (:74-82) => **Sale_count and sale_total_minor = 0 for tablet cashiers with display names present: 'looks like missing data, not a wrong file'.** - (mirror) **Desktop is the inverse: Bridge checkout writes per-store (oz-bridge/src/pos.rs:1531-1534 open_store(&session.store_id)) while the desktop reaper reads global
(desktop lib.rs:112) => desktop's reaper is a no-op and desktop safety comes only from finalize_sale being wired, not from expiry. Neither shell has a working reaper against the DB its sales live in.** Session store_id is client-supplied (tablet auth.rs:432-434,:601,:888-1003) => **The filename comes from outside the tablet's own data** — The 'identity by naming' shape that also runs through the sync push protocol (P1b) and the unguarded authz surface (P0-3b). Eager store-DB creation is desktop-only (create_store_db's sole production caller: Oz-bridge/src/locations.rs:241).

**P0-5 D1, now one symptom of P0-4, outcome settled — Tablet finalize_sale / void_pending_sale are desktop-only and the reaper voids at +30 min.** Defined only at apps/desktop-client/src/commands/inventory.rs:457/:472, registered only at desktop lib.rs:950-951; **Zero hits** under apps/tablet-client (its sole generate_handler! Is lib.rs:445). The UI names the literal command (ui/src/api/sales.ts:375-382), loggedInvoke has no fallback and rethrows (utils/logged-invoke.ts:17-31) => on tablet the invoke rejects and PaymentModal.tsx:987-997 toasts 'Finalize failed, attempting void', calls the also-unregistered void, toasts 'Void also failed', throws — The recovery it attempts cannot work on this shell. The parked question is answered by
path: Tablet lib.rs:80 init_module_system -> startup/src/lib.rs:238 init_pending_sale_reaper ->:305/:283 open_reaper_connection ->:317 reap_stale_pending_sales (sole production caller) opens the same global file the checkout INSERTed 'pending' into (predicate db/sales_lifecycle.rs:640-643; threshold db/sales_checkout.rs:480,560) => **Voided: Payments rows from the same checkout tx remain (:512-533), stock restored, no EOD revenue, loyalty/customer stats never run (sales_lifecycle.rs:85-103).** 'Silently voided' was wrong wording — The cashier is told at sale time; nothing signals 30 min later and the reaper logs only tracing::info!('pending sale reaper: Voided {count} stale sale(s)'). No test surface can see it (dev-mock answers both
names () => null, tauri-api.ts:4071-4072; PaymentModal.test.tsx:58 mocks it; api-ipc-contract.test.ts:129-141 asserts only that the string is invoked; the tablet has zero Rust-side coverage because no reference exists under apps/tablet-client/). **Remedy constraint, required in any fix brief: The tablet envelope must lock state.db, not open_store — Copying the bridge verbatim issues update ... Where status = pending against a row not in that file => changed=0 => Ok(()): Silent no-op, and PaymentModal reports success on a sale the reaper then voids.** The no-rows->Ok(()) shape is the shipped design here (sales_lifecycle.rs:582-588).

**P0-6 confirmed live, three independent reads — ShiftReportDto drops cogsMinor/grossProfitMinor/grossMarginPercent that core computes, ts declares them required, and ShiftManagementScreen.tsx:1162 calls .toFixed(1) on it => every desktop shift with >=1 sale throws and the error boundary auto-reloads.** Core computes all three (db/shifts.rs:380-407,:458-462); the dto serializes 7 fields, none costing-related (oz-bridge/src/shifts.rs:249-264) and impl From<ShiftReport> for ShiftReportDto drops all three (:266-278); get_shift_report_scoped returns that dto (:462-482); ui/src/api/shifts.ts:94-98 declares them required. **Class total, fully enumerated: 1 throw + 2 soft** —:1162 grossMarginPercent.toFixed(1) is a hard TypeError (no ?., no
?? 0; the section opens on saleCount > 0 at:1132 and saleCount is sent at:262, so no costing flag or merchant config is needed);:1150 fmt(cogsMinor) is soft ('NaN' via:36-37, a wrong number, no crash); **:1156 the grossProfitMinor < 0 className test is soft and worse — Undefined < 0 is false, so the negative-profit styling can never fire: A silently wrong cell, not just a missing number.** Caught by <LocalizedErrorBoundary autoRefreshMs> (AppProviders.tsx:51-56 -> ErrorBoundary.tsx:45-52) which auto-reloads — **Which is why this has read as a flaky shell, not a data bug.** Three layers blind for three reasons: Fixtures supply all three fields (ShiftManagementScreen.test.tsx:226,:260), api-shifts-contract.test.ts:67-70 asserts name+args
only, and get_shift_report_scoped has zero dev-mock handlers (:4034) => null -> the:1034 guard fails -> no render -> no crash. Desktop-only: On tablet the invoke rejects -> .catch(() => setShiftReport(null)) (:99) and the report silently never renders.

**P0-7 measured, and now fixed in head — Repeat refunds re-credited stock: 70,000 accepted refunds on a 70,000-minor sale with 1-minor declared totals, crediting 140,000 units against 2 units sold = 70,000x phantom stock.** That was the measurement on dc148cb73; **53fffa8c1 'bound refunds by cumulative quantity per sale line' (+326/-4, refunds.rs + refunds_tests.rs) closes it**: A new '0b. Cumulative quantity bound' aggregates requested qty per sale_line_id against sale_lines.qty via refunded_qty_for_sale_line_in_tx (join refund_lines -> refunds on sale_id, excluding the current row r.id <> ?, fail-closed .map_err(CoreError::Db) + checked_add), and the credit path's per-call check was converted — '-if refund_qty > total_deducted'
becomes '+if already_credited + refund_qty > total_deducted' — Removing precisely the per-call shape i measured. Re-run of the fix's two new bounds on the same throwaway:memory: Fixtures: **Declared-1-minor case 1 refund accepted / 2 units, rejected at round 2 with 'refund qty 2 exceeds refundable quantity 0' (was 70,000 accepted / 140,000 units); truthful-totals case 1 accepted / 2 units (was 2/4). Both repro'D shapes close** — With their caveat honoured: 'a faithful transcription closes it', not 'verified in their build'; nothing here has ever been compiled against the real code, so this remains a simulation of a simulation. **The fix also does not decrement or sum deduction_locations — Total_deducted still comes from the immutable
blob — But the cumulative side is now derived from refund_lines in the DB, which is the correct choice (read the ledger, not the cache).** what survives the fix, all of it still open: **(i) the money hole: The refund lines are still never validated against their own declared totals — No total_minor-vs-SUM(line_minor) cross-check exists anywhere (the only line_minor reference is the unchanged insert at:129), so a refund header can still under-declare; (ii) the vacuity trap is dormant, not disarmed: Their qty block is inserted at +118, after the fail-closed sum read at:85-91, so it cannot pre-empt it, and refunds_tests.rs:224 passes an empty vec so 0b iterates an empty map —:226-230's assert!(result.is_err()) still tests the integer
overflow it was written for, and **Any total-vs-SUM cross-check placed where it looks natural (before the sum read, beside the currency check at:77) will reject a zero-line refund first with CoreError::Validation, so is_err() stays true and the only fail-closed test goes vacuous without turning red. Place the check after:91 or harden it to matches!(err, CoreError::Db(..)).** Their constraint was satisfied accidentally and remains live for the next person who adds the money-vs-lines check; (iii) the schema gap, which Q-B narrowed but did not close: **A 'refund_lines is an orphan / the new bound has no table to count from' claim was refuted and is out — The table is written by the only production refund writer at refunds.rs:211 in the
same tx before the credit path (non-test INSERTs repo-wide = exactly that one hit) and read at:586 by list_refunds_for_sale (:555), whose consumers are the refund screens (oz-bridge/refunds.rs:266, tablet refunds.rs:232,:252): The rows the bound counts are the rows the UI lists. What survives is that sale_line_id is 'a logical ref, no cascade' with **No FK** (init.sql:534), **No index of any kind on refund_lines, no unique(sale_id, sale_line_id) or refund-scoped unique anywhere, no trigger, and the only uniqueness is the two row PKs — So a replay under a fresh uuid is unconstrained and the guard is check-then-act inside unchecked_transaction() (no update...returning, no row lock). Bounds in code, nothing in the schema: Any second
writer or any race reopens the hole the new commit just closed.** Also unchanged: No cumulative qty before the fix (7 spellings, 0 repo-wide), RefundLine::new (modules/sales/src/models.rs:294-312) storing unit_price/line_total verbatim, total_refunded_for_sale's zero non-test callers and .unwrap_or(0) at:502, RefundModal.tsx:44 re-seeding every checkbox at full original qty. **The detection control is still defeated: Shifts.rs:186-187 subtract cash_refunds = SUM(r.total_minor) join sales ... Payment_method = 'cash' (:169-176) from expected cash, so an over-refund makes the drawer balance look right — Invisible in close-shift, the physical count divides by the same figure the attack writes. And the fix is evidence that somebody ran the
tests: 99aa0817d raised a seeded qty 2 -> 3 in crates/oz-core/tests/refund_tax_integration.rs because the bound broke an existing test — 'create_refund now bounds each refund by the units already refunded on that same sale line, which the money columns alone cannot express'; a predicted failure does not tell you what the fixture used to hold, and the under-declared-total exploit shape is encoded there as a regression test (tea_refund(6,600) then tea_refund(6,100); rejects_repeated_60_percent_quantity_refunds, accepts_partials_to_full_qty_and_rejects_the_third, full_quantity_refund_still_succeeds, quantity_bound_is_per_line_not_per_sale).**

**P0-8 measured — The phantom service sku is in the authoritative ledger, not the cache, and
rebuild_stock_summary() run verbatim did not remove it => fix the code and the data.** One refund of a product_type='service' line (30,000 / qty 3 @ 10,000, with the fallback deduction_locations row present per sales_checkout.rs:444-448) created stock_summary(P1,LOC-1,qty=3) + inventory(P1,qty=3) **And a real stock_movements row (delta=+3, reason='refund')**; the rebuild (delete from stock_summary + insert ... Select item_id, location_id, SUM(delta) from stock_movements group by) was run verbatim and **The phantom survived (summary 3, inventory 3, movement present, SUM(delta)=3) — The rebuild derives the summary from the movement ledger, and the movement is the lie.** The rebuild button therefore cannot remove it and re-materialises it
every time; +3 never-physical units become sellable, hidden from the two warehouse lists that filter where p.product_type != 'service' (products_crud.rs:92,:121) yet visible through every per-SKU SUM(ss.qty) surface (:29/:63/:147/:188). **[rev 4 corrections: It does not pollute inventory.rs:240 — That query is 'where location_id = ?1 and qty <> 0', a scoped per-location health count, so that claim is retracted here — and it does not spread across store files via the pull (apply_snapshot never writes an inventory table), so the fleet-wide worry is dead.]** Upstream: Sales_checkout.rs:249-252 skips non-tracking/recipe-less lines,:255-299 never adds recipe composites,:431-454 stamps a fallback row keyed on the parent sku; refunds.rs:308
credits that parent; products_stock_adjust.rs:67-243 has no tracks_inventory or product-type check (guards exist at products.rs:141, sales.rs:310/345/366, cart.rs:307/316/340, purchasing.rs:606 — None on this path). **Second producer, same shape: Sales_lifecycle.rs:560-594 (void_pending_sale replays the same json, reason 'void_pending') — The P0-5 reaper path manufactures phantoms too.** Composites wrong both ways: Parent over-credited, never-deducted ingredients never restored. The fix is a check the repo already owns (products_stock_adjust.rs:112; purchasing.rs:636).

**P0-9 confirmed by an independent verifier — 'claim stands', every cited line checked out: The topology false heal masks total store-data loss reported as successful recovery.** X1: **Workspace_instances is PER-store** (create table 20260813_init.sql:983; in oz_core::migrations::ALL per crates/oz-core/src/migrations.rs:35-36, run on every store file via StoreDatabaseManager::new(db_dir, all), desktop state.rs:303; insert commands.rs:855 behind open_store:675-677; reads:735,:763,:807,:843) so compensation correctly targets store A's own file (persistence.rs:549-557) — **While the journal row is global** (model.rs:261, via ctx.db at commands.rs:657-660): **The evidence is global and the repair is local, so a missing local file lets a
global ledger be cleared by a local NO-OP.** X2: Is_new toggles only a WAL pragma and migrations always re-run (manager.rs:103-121); there is no refuse, no known-stores lookup and no caller-side existence test anywhere in the chain (persistence.rs:419-435,:446-496,:549-557; the desktop wrapper is a bare pass-through, commands/topology/persistence.rs:41-49) — **Store_db_exists() exists (manager.rs:170-173) and recovery never calls it.** X3: **Clear is unconditional — Both tx.execute(...)? Results are dropped (:563,:569; ? Propagates errors only and zero rows is not an error), commit:584, nothing between replay and clear but restore + clear (:489-494); the in-apply path is identical (commands.rs:957-986), so silent zero-row acceptance is
not a recovery quirk — It is how compensation always works.** X4: The loud pre-apply probe (persistence.rs:520-536) runs only in the original apply (journal persisted before store mutations, commands.rs:646-653); recovery never re-probes (:482-488). X5: **In-product trigger exists: Delete_location_profile_scoped removes store-<id>.sqlite and its -wal/-shm with no pending-journal check (locations.rs:379-381; manager.rs:190-214): Apply fails, in-process compensation fails (journal retained, commands.rs:961-971), the operator deletes the broken location, restarts => recovery recreates an empty file, commits 0 rows, clears the journal, leaves a phantom empty store.** Adopted consequence wording: **The store is wholly empty — The
half-applied rows died with the file — So the false heal masks total store-data loss reported as successful recovery**; the half-applied reading survives only via the WAL-loss variant (file kept, -wal/-shm deleted by a backup tool), where the 0-row delete coincidentally matches the correct end state and still destroys the journal [Inference from WAL at manager.rs:109-111]. **The lockout contradiction is printed, not arbitrated: An adversarial V1-V4 check of this claim says the permanent cross-store lockout stands with no hinge found (desktop lib.rs:137-139 log line); the verifier says TOPOLOGY_APPLY_RECOVERY_KEY is read nowhere outside persistence.rs (6 hits, all in that file), the next Apply overwrites the journal (commands.rs:659)
and recovery retries at every startup (persistence.rs:416-418) => the lockout reading is probably wrong and the silent false-heal is the severe outcome.** Both cites verified; no verdict.

**P0-10 migration brick — The app never appears on screen.** AppState::new (desktop state.rs:198-229) -> Connection::open -> foreign_keys -> WAL -> **Migrations::run(&mut conn).map_err(...)? At:212-213**, and that function is the first statement of Tauri's setup closure, whose Err returns at lib.rs:109 **Before main_window.show() at:220-221**; the window is **Visible:false** (tauri.conf.json:25), **Release builds have no console** (main.rs:2), tauri_plugin_dialog is registered (lib.rs:103) and never used on this path, and the updater runs on an event loop that never starts. **The sole artifact is one line in an hourly rolling log (crates/oz-logging/src/lib.rs:206).** Tablet identical (tablet state.rs:117 inside lib.rs:76). Blast radius
global: The identity DB is migrated at:212 before any store opens, so one edited file = every install that already applied it fails to boot = **A lost till** — While the soft-fail pattern exists one level down (lib.rs:378,:632). **A backup can never be applied to save a bricked install — The brick happens before any code that could accept it exists** (the only in-app restore is .ozpkg row import via DataManagementScreen, oz-bridge/src/data.rs:469/:498, strictly after migrations; no safe mode, no pre-migration hook, no skip flag or env var — Grep SKIP_MIGRATION|ALLOW_MIGRATION returns nothing). **Oz migrate shares the bricking runner (commands/db.rs:19 -> the same oz_core::migrations::run) so it is not an escape hatch, and oz is not
shipped** (no externalBin/resources in tauri.conf.json; release.yml:176,:209,:216 build only installers; packaging/linux/oz-pos.desktop runs only Exec=kasirmu-app). **Previous installers are retained (release.yml:426-433 + SHA256SUMS:392-402) so the downgrade repair exists — Undocumented as a recovery step, and tauri_plugin_updater + latest.json (:393) auto-update a reinstalled old build straight back into the brick. The durable fix is the one docs/records/journal.md already performed by hand: Update schema_migrations set checksum = '<sha256>' where id = '<file.sql>' — Id is the file name, there is no version column.** both sides of the softener: Warn-and-continue is not free — 20260826_sale_line_snapshots.sql is three bare alter table
add column plus an update backfill, **Non-idempotent by construction**, so swallowing the Err means booting on a schema the app believes was repaired => *** corrupt-while-running instead of refuse-to-start, for a pos taking payments, arguably worse. *** The ? At platform/core/src/database/migrations.rs:90 is the entire brick (reapply_for_drift:292-311), and a softener must not also run:91 update_checksum. Reason reapply exists: 342f0e18c 'auto-patch migration checksum drift instead of panicking', whose comment says at:76-82 that if re-apply fails 'the user must act' — For a cashier. **The two routes by which 'should re-run migrations on an existing file' became 'just overwrite the checksum': 435d47f07 added checksum verification
(bricks on benign drift); the same day 9a55b0d6b removed the checksum update (makes every later benign edit fatal).** Each defensible alone; together no supported path remains, and no tool says so.

**P0-11 licensing residuals, not already in P0-2: No revocation list (auth.rs:15-16); cached claims skip exp (:253-260); OZ_LICENSE_SERVER_URL is an env override with no scheme/host validation (:47-48); and **OZ_ENFORCE_PLANS is never set to 1 in any committed file (config.rs:146 defaults false) => plan gating is off in every deployment this repo describes.** a server-only patch stops the bleeding from P0-2 only, because the sole writer of those columns is a setInterval alive while that screen is mounted [unverified — The mechanism line].** What did not survive: 'expiry_hours is a licensing field' (it is the oz-api JWT TTL, client-chosen at tokens.rs:218,270), the origin-empty 'csrf' framing (browser-only by design; origin=='' returning
true at web_otp.go:428-430 is an auth gap, not csrf), and 'a lapsed tenant becomes perpetual active' as a separate finding — It is the same unsigned-column write as P0-2 and is folded there.

## P1 — The structural cause (one chapter; the review's thesis)

Docs/decisions/2026-09-11-adr49-headless-command-bridge.md:5 — Dated the day of the review — Says the bridge is 'implemented for the desktop shell; **Tablet client not started**' (:9), and apps/tablet-client/src has zero references to oz_bridge while oz-bridge's crate description claims both shells use it: **The description overstates reach, and the omission is unfinished work, not a decision** (no ADR, comment or doc cites Android leanness or bundle size). So **18,852 lines across 42 of 46 tablet command files** re-implement the bridge's bodies against the same oz-core Store (13 of 18 store.* calls in tablet pos.rs are common — The duplication is the orchestration layer: Gates, lock order, tax scope, event publish) and **Divergence is
silent**. **P0-1 through P0-5 are what that silence buys.** Note the mechanism, since it is this review's own finding turned against it: Apps/desktop-client/src/commands/pos.rs:33-42 is a **Pub use RE-export** of oz_bridge::pos::{CompleteSaleResult, StartSaleResult, ...} — 41 lines of re-exports are how the bridge struct became the desktop's wire type without anyone registering a new response shape (see could-NOT-settle's wire-mismatch entry). **Option 3 re-ranked — A correction i owe, and it moves up. *** 0 merchant-visible tablet screens break or vanish if option 3 ships without porting anything. *** A '25 tablet-only features get deleted' figure circulated for six waves; it was a name-set relayed as a feature-set. Set arithmetic
from bracket-matched generate_handler![...] blocks, UI names from loggedInvoke literals under ui/src excluding __tests__/ and dev-mock/ (Desktop 448 · tablet 318 · both 293 · D-T-set = 155 desktop-only · T-D-set = 25 tablet-only · distinct UI-invoked strings 453 · U-T-set = 154 = the allowlist exactly · U-D-set = 27): Of the 25, **7 already have a bridge body** (oz-bridge/src/settings.rs get_hardware_settings/get_credit_settings/get_receipt_settings/get_store_settings; branding.rs set_brand_logo_path/set_brand_primary_colour/set_brand_store_name), **22 exist on desktop as <name>_scoped**, and **17 are called by no screen at all** (15 with no UI call string + 2 whose wrapper no screen imports) => **8 real called tablet-only names —
Export_eod_report, get_cart_deduction_location, get_hardware_settings, set_hardware_settings, get_sale, list_categories, list_sales, read_scale_weight — All 8 with their *_scoped twin registered on both shells and selected by the ADR #7 sessionToken ternary** (EodReportScreen.tsx:3,:402; useProducts.ts:3,:133; ScaleIndicator.tsx:4,:30; WeightScaleWidget.tsx:2,:52; SalesHistoryScreen.tsx:234,:401), **With every affected route requiredRole-manager or requiredPermission-gated (features/sales/register.tsx eod-report; settings/register.tsx data-management; shifts/register.tsx shifts) => the no-token fallback is unreachable in practice.** cost: **22 registration swaps (7 already bridged) + 3 names needing a decision**
(get_active_cart_scoped, list_active_carts_scoped, list_warehouse_products_scoped — Two of them the gate's own permission-checks-guarding-no-caller pair, both SALES_PROCESS). **It deletes nothing, and it is the only structural fix that removes the 18,852 duplicated lines the whole review blames** — That duplication is what made finalize_sale's absence invisible. Re-run hazards: Apps/desktop-client/src/lib.rs:20 carries a doc comment containing 'the 448-entry tauri::generate_handler list', so searching generate_handler without the '[![' match returns garbage (a worker got a wrong 318 mid-run); and the three-grep rule AGENTS.md documents (wrapper name, route string, register.tsx gating; ':!' pathspecs as their own literals; head -n
never) applied proactively is what collapsed 25 to 0. **Denominators: 154 (UI-called but unregistered) and 155 (registered only on desktop) are adjacent integers from different questions — Label which is which every time**; the '15 modules / 108 names' figure is an incomplete subset of the 155, correct per module but omitting 10 modules / 47 names (workspaces 12, terminals 6, sync 6, memo 5, health 4, kds 4, auth 3, branding 2, currencies 1, settings 1). **Neither figure gates option 3.** The gate layer, in one place. **Ipc-parity-allowlist.json holds 222 entries** (tablet 154, desktop 27, scoped_orphans 25, dev_mock 16) that **Grow without any check** — 142->154 over 20 commits (ec4a8a610 -> 0a82aec99), never reviewed — And
verify-ipc-parity.py:381-386 subtracts allowlisted misses before any failure is appended, so **No output line distinguishes '0 gaps' from '154 gaps'** (live run: 'IPC parity: OK', exit 0). **The file is a flat array of strings, so per-entry reasoning, owner or expiry is not even possible; its only prose is one global _comment covering about a dozen of the 154 => unreasoned is the default, not an exception.** Of the 154: **0 name a string the UI never references, but 57 (37%) name a string whose only appearance is inside ui/src/api — A wrapper no component, hook, context or feature imports**; 32 of those 57 are not _scoped so the alias excuse cannot cover them [Inference — Textual classification; list_workspace_screens_scoped (:121) is
the confirmed zero-consumer example]; and **74 of 154 are not _scoped at all** (activate_license, create_backup, edc_sale, apply_topology_diff, adjust_stock...) — **Precisely the absences no alias rule can excuse, which is the proof that the list is a record of missing surface, not of a design choice.  Correction to a mechanism i told you: The gate does not synthesize _scoped names into the shell verdict — False-OK via the alias rule = 0 of 154.** The shell side is extract_handlers over the literal generate_handler! Block (:94-101,:325,:330); synthesis exists only inside extract_dev_mock_answerable (:133-175,:351-353), so it makes the mock verdict lax, never the shell verdict, and deleting an allowlist entry for a
genuinely-unregistered _scoped name still turns the gate red (no double-hide). **The real mechanism is that a human wrote finalize_sale into a list that can never fail as it grows.** the alias class is 4, not 154: **Desktop 0 alias-class misses (all 27 of its misses are unscoped names), tablet exactly 4, named — List_workspace_screens_scoped (:121), list_workspaces_scoped (:123), open_product_images_scoped (:136), version_scoped (:185)**, each with one UI call site whose base is literally registered on tablet (the shell answers the base and 404s the scoped spelling), all four bare uncommented strings; the other 80 tablet _scoped misses are 76 absent surfaces plus 4 unclassified. **Zero un-allowlisted misses in either shell. A bug list
was expected in the hundreds and the answer is 4, so 4 is what is published; the earlier frame understated the problem by looking for a tool that lies and finding a list that grows.** what is large is the test layer: **73 UI commands are answered in browser preview only by the alias rule, and 61 of the 86 files that exercise the real API layer touch at least one alias-only wrapper — An assertion that reads as data but runs against null** (use 61 files with its caveat; 'touches' != 'asserts non-null data through it'; the 217-calls/~78-tests figures at dev-mock/tauri-api.ts:5155-5166 justify a past deletion and are not re-measurable in-fence). Plus **ListWorkspaceScreensScoped is allowlisted with zero callers outside its own
declaration** (ui/src/api/workspaces.ts:136) and is invisible to both gate directions because scoped_orphans scans only rust-registered names (:242-243) — **A dead ts wrapper costs nothing to keep and nothing notices.** And the enforcement is half-incharge: Verify-scoped-reads.py:255-256 defaults --shell to desktop and dev-ci.yml:596 runs it bare; **Verify-ipc-parity.py has no --self-test at all** (three --write-* flags only) — A gate that cannot provably fail; only 4 of the 20 CI-invoked gate scripts carry one (dev-ci.yml:577,:578,:579,:580) and none covers the 28 named static-gates steps. .github/workflows/dev-ci.yml has **No push trigger** (pull_request + workflow_dispatch only) while a **Second undocumented CI**
(.circleci/config.yml + workflows/01-07, no filters: Block => every branch; the README badge points at it) runs per-branch, **Excludes both app crates from nextest** (workflows/06-cargo-nextest.yml:37), and omits ipc-parity, bundle-parity, pg-schema-drift, migration-column-types and ftl-orphans => **Hook steps 4, 6, 7, 10 have zero remote enforcement on any branch.** net: **The gates certify the envelope, not the payload.** ## P1b — Offline-first: The sync chapter (three interim sentences dead; one headline added)**

*** the headline: 'a failed push is reported by a single number on a screen the merchant must navigate to, while the status bar that already polls that exact response object 60x/hour displays conflicts only.' *** failedCount has three non-test consumers (the type in ui/src/api/offline.ts; OfflineQueueScreen.tsx:328; SyncSection.tsx:389) and both UI ones are navigate-to pages, the nav entry carrying no badge (features/offline/register.tsx:13). But StatusBar.tsx:42-53 polls getOfflineQueueStatusSummaryScoped every 30s, **Receives failedCount in the object s and discards it**, rendering only {conflictCount > 0 && ...} at:73-80 — No failedCount reference exists anywhere in ui/src/frontend/shell. **The severity order is inverted in the
render: Resolved conflicts — Auto-resolved server-wins, the least severe thing there — Are visible at a glance, while permanent, unrecoverable, delete-only failures are invisible until someone goes looking. The fix is one expression in an already-polling file: The instrumentation exists, the data is in hand, and one missing line decides which failures a human ever sees.  Died: 'failedCount is wrong and a manual retry can never succeed.'** The duplicate-id special case exists on both live push paths (is_duplicate_id_rejection + DUPLICATE_ID_REJECTION_PREFIX = 'duplicate id:' at sync_client.rs:68-74,:308-321, whose comment gives the reason — Push failures have no requeue path, so marking a successful replay failed would strand it and
pollute failed_count; identically daemon.rs:195-198,:200-203; two named tests at sync_client_tests.rs:300 and daemon_tests.rs:1112 against a duplicate-id mock server at:1071). The bare mark_failed survives only in SyncEngine::run_sync_cycle (lib.rs:536-537), which has no production caller — **Dead but public and still-documented** (the usage example at lib.rs:28-29). Two further un-guarded copies are invisible to the shipped UI: **The bridge's own retry_offline_sync (oz-bridge/src/offline.rs:408-425) lacks the arm and no desktop UI calls retryOfflineSync (0 hits); pg_daemon.rs:289-298 lacks it too but is almost certainly unreachable — The cloud rejects non-sqlite transports outright ('offline sync is only available for sqlite
transports', sync_api.rs:423-429) and no binary wires PgSyncDaemon (grep PgSyncDaemon::new = zero hits; the other 25 hits are its own file plus a doc comment at lib.rs:17) => the stranding class is two live sites, not three.  Replaced by something worse in its own way: Push has no retry at all.** Selection is where status='pending' (db/offline.rs:256); mark_offline_failed writes status='failed' + retry_count+1 (:345-350), **But nothing selects on retry_count for push — Retry-3, dead-letter and requeue are the pull side only (queue.rs:279) => an item fails once and is stranded forever.** Money is safe (sales/stock commit locally at checkout). **The exhaustive affordance list is delete** (OfflineQueueScreen.tsx:562 ->
deleteOfflineItemScoped,:243): Git grep status = 'pending' in an update context = one hit, a doc comment about another table (kds_ops.rs:21); and **The Requeue button at:624 is wired to requeueRemoteFailureScoped, which deletes from sync_remote_failures and rewinds the pull anchor and cannot touch offline_queue (offline.rs:408-425) — A pull-side control sitting next to a push-side failure.** [rev 4 tail from P0-2(e): Once:238 lands, the refused insert never executes, so failed_count (offline.rs:440-447) is not fed — No monotonic number, 'ingestion starvation' not 'freeze'.] **Also died: 'no test covers conflict resolution' / 'two-node coverage is a comment, not a test.'** platform/sync/tests/integration_test.rs:476-540 runs a named
relay server with real cross-terminal tests (product_created_on_terminal_a_appears_on_terminal_b:545, stock_adjustment_on_terminal_a_reflected_on_terminal_b:656, full_sync_cycle_completes_under_one_second:753, large_scale_sync_throughput:859) plus crates/oz-core/src/db/multi_terminal_tests.rs (peer registration, shift isolation, **Concurrent stock**, held-cart isolation, session independence) and crates/oz-core/tests/terminal_integration.rs; ack-loss is tested (sync_tests.rs:85). Narrower form: **Two-node is covered against an in-test relay, never against the real apps/cloud-server push/pull handler; no clock-skew test exists** (skew appears only in two anchor comments, lib.rs:691, pg_daemon.rs:680); conflict coverage is the synthetic
matrix in conflict_tests.rs and **Its void cases are hand-built items, not two terminals that each voided** — The same shade of gap the a11y pass found in the dev-mock and the gate pass found in parity: **Tests that pass against a stub of the thing rather than the thing.  Void propagation, restated (an earlier 'UI-initiated voids never propagate' framing was half right): nothing enqueues a void op from any path.  No SaleVoided event at all** (git grep SaleVoided|sale.voided = 0 hits repo-wide, both shells); the only sync-triggering handler is SaleCompleted -> enqueue_offline_priority (startup/event_handlers.rs:44-64); **Sales are not in the snapshot** — SnapshotPayload carries products, tax_rates, users only
(crates/oz-core/src/sync_pull.rs:36-42) => **An absence, not a delay**; no reverse pull exists (patch /api/v1/sales/{id}/status, openapi_tests.rs:151, is inbound API); void.rs in all three shells has zero enqueue occurrences (the reaper calls cancel_kds_orders_for_sale_in_tx directly at sales_lifecycle.rs:755, never the command layer where a publish could have lived), and the void body writes only update sales set status='voided' (:706-710) + an AuditEntry (:720-729). **Void_sale is already in EVENT_ACTIONS (apps/cloud-server/src/outbound_webhooks.rs:38) => the vocabulary and fan-out exist and are never fed, so the gap is local-side only**; publish **'no void path enqueues a sync op'**, not 'sync drops voids'. Cloud revenue can then
exceed the local non-voided ledger for every void [Inference: Assumes the cloud materializes pushed ops into aggregates]. **Three whole feature areas never sync at all: Customer balance/loyalty, restaurant tables and held carts have no sync actions => siloed per terminal, cross-terminal edits never converge, in a multi-terminal product.  * The 'duplicate id:' contract is narrower than first reported: a 'not pinned, a one-word reword silently strands everything' claim was over-stated and is withdrawn. *** No cross-language sharing exists (git grep DUPLICATE_ID_REJECTION_PREFIX|is_duplicate_id_rejection -- apps/cloud-server = 0 hits) and the text is six independent literals (sync_store.rs:218,:291,:639,:724; sync_api.rs:335;
sync_client.rs:68) with no cross-language gate at all — **But it is pinned by tests on both ends and cross-referenced by comment** (sync_client.rs:55-57 names sync_store.rs push_batch and the exact format! String; sync_store_tests.rs:200,:237,:483; sync_api_tests.rs:1369,:1394) => **A plain reword fails loudly, in the server's own crate, case-sensitively.** the real hole: *** server tests pin a substring, the client depends on a prefix — 'rejected: Duplicate id: X' passes every server assertion and silently flips is_duplicate_id_rejection (starts_with,:72-73) from mark_synced to terminal-stranded; consumer tests hardcode the string in fixtures (sync_client_tests.rs:311; daemon_tests.rs:1080) so they pass whatever the server does. The
contract is asserted against fixtures on the consumer side and a substring on the producer side — No test proves the prefix matches. *** credit where due: **Sync_client_tests.rs:328-336 does guard 'merely contains must not be treated as synced' — One test author got it right and it is the only place the distinction is expressed.** The same match on PushOutcome is hand-copied five times with 3-way drift, and pg_daemon **Is safe today by accident, not by the guard**: Its only Rejected reasons are tenant-mismatch (pg_transport.rs:279-284) and 'pg insert failed' (:306-308), because a duplicate id goes on conflict (id) do nothing -> Ok(_) -> accepted (:295-305) and **Its rowcount is never checked, so the classification is 'no exception was
thrown', not 'my row landed'.** (that quoted phrase is the reviewer's shorthand for the rowcount gap) Policy and trust, compressed: The unit is one OfflineQueueItem op, not rows (offline_queue, init.sql:353); **Pull is properly idempotent — Cursor durable (sync_api.rs:549-557) and apply receipt-guarded (sync_applied_items / sync_receipts.item_id PK, init.sql:218) co-committed with the domain mutation (queue.rs:5, 'so a crash cannot ack a change it did not apply')**, 500/page, keyset cursor created_at|id (:388-398); the server INSERTs action/payload/created_at untouched with tenant_id overwritten from the JWT (sync_store.rs:201-214,:274). Conflict policy: Products/categories/tax_rates/users/staff = version LWW **Falling back to
created_at — The device clock — When version is missing (conflict.rs:10-17,:94-100), and because the enqueues that feed it use an empty object, the fallback is the wild-type path** [Inference]; sales = status-DAG rank void>completed>pending; stock = crdt delta merge; settings = key-value supersede (offline.rs:194); **Snapshot import overwrites local products/tax_rates/users wholesale.** [Inference] a crdt-merged winner re-enqueues under a fresh uuid (conflict.rs:153-176); if re-pushed and re-applied as a movement, **Stock can double-count, and no test re-applies a merged item.  Client queue unbounded (offline.rs:396,:409); server prunes at 90 days (prune.rs:114) => no backpressure warning, and P0-1 explains what fills it.** Sync never
blocks a sale (a health-check failure returns Ok and skips the cycle, lib.rs:485-495); sync does not use event_bus (grep publish in platform/sync = comment only, daemon.rs:86) — Which is why P0-3's swallowed-publish rule does not apply to this path; DaemonStatus.last_error (daemon.rs:63) is in-memory with no UI binding. **Store identity is claimed, not proven — Store_id rides inside client json payloads (queue.rs:42-51) and pull filters by tenant only (sync_store.rs:761) => any terminal of tenant t can push an op naming store b and every terminal of t applies it**; totals/timestamps are trusted (payload and created_at stored verbatim, never recomputed). Quotable: **Pull is receipt-confirmed against a local ledger; push has no local
ledger of what the server holds — Its only already-there signal is a starts_with('duplicate id:') match on a server-written sentence, and that is exactly the part not tested across the language boundary.** verdict, weakened to what can be supported: **Offline-first is safe against duplicate money today but not yet safe for money** — No sale-level idempotency key (now being minted: 0d0c1eda5 + the 20261001_sale_idempotency table); **No void path enqueues a sync op**; claimed-not-proven store identity; terminal stranding with no requeue affordance; conflict resolution never tested against the real server. **And the one-design-decision pattern, now backed by four passes: Server-side trust of client-asserted scope or amount (refund
total_minor, topology store_id, sync payload store_id/created_at/totals, and the 6 global-DB readers any cashier can call in P0-3b) is not four findings, it is one decision.**

- Void propagation in one line: nothing enqueues a void op from any path — no SaleVoided event exists repo-wide, SaleCompleted is the only sync-triggering handler (startup/event_handlers.rs:44-64), sales are not in the snapshot at all (SnapshotPayload carries products, tax_rates, users only, sync_pull.rs:36-42), void.rs has zero enqueue occurrences in all three shells, and void_sale is already in EVENT_ACTIONS (outbound_webhooks.rs:38): publish 'no void path enqueues a sync op', not 'sync drops voids'. Conflict resolution is tested against an in-test relay and never against the real push/pull handler, there is no clock-skew test, and the void cases
in conflict_tests.rs are hand-built items rather than two terminals that each voided. Client queue is unbounded (offline.rs:396,:409) against a 90-day server prune (prune.rs:114), so there is no backpressure warning; pull, by contrast, is receipt-confirmed (sync_applied_items, init.sql:218) and cursor-durable (sync_api.rs:549-557).

## The three-flag fix list (highest value per minute, ordered)

**Retraction first: an earlier recommendation that 'a visible error screen costs less than either runner change' is infeasible as a mechanism — Blocking_show() deadlocks in setup** (tauri-plugin-dialog 2.7.1: Blocking_show() = sync_channel(0)+rx.recv(), src/lib.rs:71-79; show_message_dialog dispatches handle.run_on_main_thread, desktop.rs:222; setup is the main thread and the event loop is not turning => the producer can never run; the plugin says so at src/lib.rs:355,:365, 'should not be used when running on the main thread context', and its example wraps the call in std::thread::spawn). 1. *** move the uncommitted:238 enqueue gate above the commit, or scope it to the enqueue decision only — The only fix in this report that is free
tonight. *** offline.rs + quota_gate.rs are still ' M' at 02:40, so the placement can still be corrected before it lands (P0-2(d)-(f)): As written it refuses a local-offline sale after create_sale + Completed on the tablet path, where bus.publish's Err is discarded (P0-3), so an expired-subscription till keeps recording sales structurally guaranteed never to sync. The intent is proven by their own new test_enqueue_offline_fails_when_subscription_read_only (offline_tests.rs) — **The test is right and the placement is wrong; the fix is the same guarantee, earlier, with verify_signature() on the row it reads, exactly as the desktop checkout already does at oz-bridge/src/pos.rs:1557-1571.** 2. **One line per command for the 46 live
unguarded desktop commands (P0-3):** add ctx.require_user_permission_scoped(..., permissions::X) where the registry already has a key (products:read, payments:card, settings:edit, terminals:read, workspaces:switch), **Plus two new registry keys for the hardware:* and license:/device:* families that have no vocabulary to check against** (the 93-key registry contains neither). Highest value-per-minute item in the report; then fix the two allowlist entries that fail their own written admission test (verify-scoped-coverage.sh:38-40,:46-51) and add one denial test as a template, since none exists (authz_tests.rs has 8 for the gate functions, zero for the wiring). 3. *** 2 lines — Highest value-per-line fix in the entire review: A .map_err
at platform/core/src/database/migrations.rs:90 naming mig.id + the data dir. *** Invisible exit(1) becomes one support call, and **It is a restoration, not an invention** — Pre-342f0e18c printed exactly that ('migration 128_assignments.sql definition drift: Applied checksum 79826c1b... != current 55abc2a6...', docs/records/journal.md 2026-08-12). Then ~6-10 lines: OZ-POS-startup-failure.txt next to the DB at apps/desktop-client/src/state.rs:212-213 via std::fs::write before the ? — No dependency, no event loop, **And the directory is cashier-reachable: %appdata%\com.ozpos.app** (identifier tauri.conf.json:5; resolve_db_path state.rs:738-744). Then ~9 lines: Rfd directly (rfd 0.16.0 already in Cargo.lock:5858-5861 as the plugin's own
backend; MessageDialog::new().show() runs on the calling thread) — **But linux features gtk3 / xdg-portal must be re-picked (the plugin enables rfd/gtk3, rfd/xdg-portal, rfd/wayland, rfd/tokio at Cargo.toml:58-62) or the box silently no-ops on linux.** Show-anyway = 60-120+ lines, ranks last: .manage() never ran so every invoke() rejects, **And a react error boundary catches render throws, not rejected promises.** 4. Dev-ci.yml:596 -> python scripts/verify-scoped-reads.py --shell desktop,tablet. One flag. The gate was purpose-built after desktop round-66, pointed at the wrong shell, and **Would have failed on finalize_sale at merge**; the same flag covers the 4 alias-class names. Pair it with verify-ipc-parity.py printing '{len(allowed
& missing[shell])} allowlisted' per shell and failing on a rise without a gates.json entry carrying owner+expiry — **This kills silent growth for all four lists at once, and the repo already has the pattern: Architecture-boundaries-baseline.json carries dated 'expires' fields, in one file, unused by the four lists that grew.** 5. Sync (P0-1): (a) **Make update_sync_settings_scoped write both scopes, or register the 8 unscoped offline / unscoped sync commands** so the operator's keystroke reaches the file the daemon reads; (b) **Point pending_offline_count_scoped (offline.rs:451) and pending_sync_count_scoped (sync.rs:451) at the file the queue is in** so the badge stops reporting 0 [unverified which file it should read once (a) lands];
(c) ~30 lines mirroring SyncConfig::from_settings per store, or a release-build sync_bootstrap for both shells, since the only set_sync_enabled(true) caller is cfg(debug_assertions); (d) add failedCount to StatusBar.tsx:73-80 (one expression); (e) give the bridge's retry_offline_sync (offline.rs:408-425) the is_duplicate_id_rejection arm the other paths have, plus a requeue affordance inside offline_queue, since delete is the only one today; (f) name the dotted-key bug while you are there: SettingsPage.tsx:479 setSettingScoped(sessionToken, 'sync.auth_token', ...) writes a dotted key nobody reads — Auth_token has 0 readers in platform/core/src and keys::SYNC_API_KEY is the underscored one (typed.rs:333,:341) => **The UI's API-key save
may be writing an orphan key** [Inference, readers not enumerated repo-wide]. 6. False-heal trio, all settings-KV / rowcount work so none of it can enter the migration-brick path: (a) **Call the guard that already exists — Store_db_exists() (manager.rs:170-173) from the recovery path**; (b) **Treat changed=0 as failure** — Stop dropping the execute results at persistence.rs:563/:569 and gate the clear at:489-494 on them; (c) a per-store suffix on TOPOLOGY_APPLY_RECOVERY_KEY (model.rs:261) mirroring persistence.rs:33-42, plus a pending-journal check in delete_location_profile_scoped (locations.rs:379-381; manager.rs:190-214). Do (a) and (b) regardless of how the printed lockout contradiction resolves, since the false-heal is confirmed
either way. 7. ShiftReportDto: Add the three fields (oz-bridge/src/shifts.rs:249-264) + map them in From (:266-278 — The existing rename_all='camelCase' already emits the names ts expects) + (x ?? 0) at ShiftManagementScreen.tsx:1162 + a get_shift_report_scoped dev-mock handler that omits nothing. 'three lines + one From impl'. Same file, same class: **Add rename_all='camelCase' to the checkout results (CompleteSaleResult, StartSaleResult, SaleListItem) or fix the ts mirrors — The repo has done exactly this fix before and documented it at oz-bridge/src/history.rs:128-129, 'rename_all above is the drift fix - UI + dev-mock both read camelCase; the backend was the wrong side.' The rule is per-struct opt-in (36 structs with rename_all vs
81 without across the 56 non-test files in crates/oz-bridge/src, including Money itself at foundation/src/money.rs:18-24), so 'these two forgot it' is wrong as a class claim and 'the camelcase ts twins of non-opted-in structs are the liars' is right — Which is also why the whole camelCase-vs-snake drift argument was partly an artifact: Many apparent 'gaps' were snake ts matching snake rust correctly. The 3-real-pairs figure stands, but for a different reason than rev 3 gave.** 8. Tablet split-brain / D1 (P0-4, P0-5), constraint up front: Register finalize_sale + void_pending_sale as tablet-local envelopes that lock state.db (mirror desktop lib.rs:950-951 into tablet lib.rs:445, copying tablet void.rs's own state.db pattern, not the
bridge's open_store) — ~25 lines, 2-3 files, divergence surface = one permission constant (resolve_session(...).map_err(Into::into) before the db handle, the authz.rs:161-167 shape; the scoped twin at inventory.rs:625-632 is copyable), source-commented 'envelope twin — Bridge has the body, delete this when tablet migrates to oz-bridge'; plus a test in ui/src/__tests__/api-ipc-contract.test.ts asserting both names bind a non-null handler, and stop allowlisting the names. Rejected alternatives, so nobody re-litigates: Rewriting tablet to call the bridge desktop-style (500+ lines in a file already 2,531, and it creates a second drift site); moving UI checkout onto complete_sale (6-10 files / ~1,500-2,500 lines, giving up tablet's
finalize-only side-effects). Then finish ADR-49, **Which now costs zero merchant-visible screens and is the only fix that removes the 18,852 duplicated lines.** 9. Status = 'active' predicate in the runtime stock/KDS plan readers (oz-bridge/src/topology/pos.rs:70-101 filters the plan json on ports/relationship only, no status/deleted_at term and no SQL at all; kds.rs:63-74 returns the stored plan verbatim) => **Archived-but-present instances keep receiving stock deduction and kds chits**, while the row-presence guard does fail loudly (location_resolver.rs:323-335 -> CoreError::NotFound -> app-error-not-found, api/src/error.rs:171). Also the license pair: License_verification.rs:642-659 must refuse an unsigned/null status
(verify_signature before the update, as pos.rs:1557-1571 already does), /status must emit subscription status and never null expires_at, and OZ_LICENSE_SERVER_URL needs https+allowlist (:47-48). Plus the clamp oz-local-api already has (auth.rs:200-202 vs lib.rs:240-242). 10. ~25-30 line shiplock manifest as an extension of scripts/verify-migration-column-types.py — **No gates.json edit needed, the entry already exists at:523**, four gated sites confirmed (.githooks/pre-commit:235, dev-ci.yml:565, scripts/check.sh:364, scripts/run-pre-push.py:114); it already globs *.sql (:141), has --staged-only (:140) and a committed whitelist compare (:164), and CI deliberately runs whole-tree. **It is not sufficient alone: The 2026-08-12 incident
was a draft applied at 08:16 and final bytes committed at 08:26, so a manifest blessed at first commit sees nothing — That class is runtime-only**, which is why fix 3 is the necessary half. The existing parity test is migration_registry_matches_fileset (crates/oz-core/src/migrations.rs:14) = **Membership, not content**, and the 'never edit a shipped migration' rule exists only as an unenforced code comment (migrations.rs:62-65) plus post-mortem prose — No procedure in AGENTS.md, contributing.md (:186 is about resetting the dev PG container) or docs/guides (grep 'never edit|do not edit|edit a shipped|historical migration' across all docs hits only append-only audit-log prose). **Add the rule to AGENTS.md in the same commit as the
manifest.** 11. Four a11y/L10N lines, two booleans, three css declarations (unchanged by the calibration): (a) wrap total due and change due in a persistent role=status / aria-live=polite / aria-atomic=true region **Mounted on dialog open, not on completion**, and give useFocusTrap an explicit initialFocusRef instead of focusable[0]; (b) one boolean — Gate the outer trap's active flag with and not showQr and not showCustomerSearch at PaymentModal.tsx:1120; (c) min-height var(--touch-target-min) on .retail-fn-btn, .retail-cart-qty-btn, .payment-quick-btn — **The token already exists and is honoured in 8 other css files**; (d) replace the two hardcoded filenames in ui/src/__tests__/i18nBundle.test.tsx:396-412 with a loop over all 26
bundle pairs plus per-message identity counting. And one hygiene fix the tree hands you: A commit touching crates/oz-core/src/migrations.rs must also git add crates/oz-core/migrations/20261001_sale_idempotency.sql — **The .sql is untracked while the include_str! Is in the working copy, so the next pathspec-less commit makes head fail to compile (include_str! Of a missing file): The explicit-pathspec hazard AGENTS.md documents, waiting to happen.** The future-dated name is not a backdate bug (its own comment says 'date 20261001 sorts after the last', and the newest committed migration is 20260930_sales_tax_estimate_note.sql) — Contrast journal.md's genuinely backdated 2026-09-10 entry; say both so the reader does not call the migration
a bug and does not call the journal entry a feature.

## A11Y / L10N — calibrated (three sentences from an earlier draft of this document are degraded here, two by the pass that filed them)

## What the review could not settle

**The wire mismatch: Mechanism confirmed on both shells, magnitude still open — So it stays here, not in P0.** [Fact] crates/oz-bridge/src/pos.rs:897-906 is '#[derive(Debug, Serialize)] pub struct CompleteSaleResult { pub sale_id: String, pub total: Option<Money>, pub line_count: Usize }' with **No rename_all**, and **The desktop does not define its own dto** — Apps/desktop-client/src/commands/pos.rs:33-42 is a pub use re-export, then :294-303 complete_sale_scoped -> oz_bridge::pos::complete_sale_scoped, registered at desktop lib.rs:920 (start_sale_scoped :916, complete_sale_with_resolved_shortfalls_scoped :921); **The tablet has its own copy (commands/pos.rs:732-741), also without rename_all, registered at tablet lib.rs:543-544
=> both shells emit sale_id.** the ts reader declares saleId/lineCount (ui/src/api/sales.ts:90-94), so **The type is the liar, the response is snake** — And the repo has verified this before, at oz-bridge/src/history.rs:128-129: 'rename_all above is the drift fix - UI + dev-mock both read camelCase; the backend was the wrong side.' the re-fetch is **String, not Option** (desktop commands/history.rs:51-55) so a missing 'id' is a de serialization Err, and PaymentModal.tsx:1006-1012 puts it in a try whose catch at :1061-1063 is 'Receipt/KDS may not be configured - non-blocking' => **The rejected invoke is discarded: No toast, no receipt preview, no print — Silent-by-catch, not silent-by-option, and the comment exonerates the wrong
suspect (it is labelled as handling a missing receipt config and actually eats an IPC argument error). The qris path is worse: :727-733 + :827-830 => a false 'qr payment failed' notice for a sale that already committed.** visible symptoms: 'sale-' + saleResult.saleId (:1018,:739) would print sale-undefined but the re-fetch aborts the same try first, so the symptom is no receipt at all; :1066-1070 createKdsOrderFromSaleScoped(sessionToken!, saleResult.saleId) -> console.error + payment-toast-kds-failed => **'kitchen ticket failed' on every restaurant sale — The same toast the handle audit blamed on the cross-file split, two independent causes for one symptom, which is why neither was ever diagnosed**; :1077-1085 redeemLoyaltyPoints
inside 'catch {} // non-blocking' => **Loyalty silently not redeemed**. Same class already acknowledged in-repo: History.rs:21-23 SaleListItem has no rename_all (line_count, payment_method, created_at) vs sales.ts:434-442, read at SalesHistoryScreen.tsx:96,:98 as 'new Date(sale.createdAt).toLocaleString()' => **Invalid date on the sales history screen**; and **Docs/plans/todo.md:299-300 already says 'SaleListItem still serializes snake_case on the wire while the ts type is camelCase (pre-existing mismatch, out of scope)' — So this is not an undiscovered bug, it is a known, documented, deferred one, which also explains why 'no receipt' and 'invalid date' are just atmosphere in this product.  * why it is not P0, the paradox they
refused to argue away: The identical mechanism hits StartSaleResult.cart_id vs ui/src/features/sales/hooks/usePosCartActions.ts:88 'const { cartId: NewCartId } = await startSaleScoped(...)'. If that truly arrives undefined, no desktop sale could ever add a line — So either the desktop pos is broken far more loudly than any money finding here, or these paths are never exercised against the rust backend. *** Supporting the second branch, all quoted: Ui/e2e/README.md:5-7 (E2E runs 'with mocked tauri IPC ... No rust backend'); dev-mock/tauri-api.ts:3581 returns both spellings (deduction_location_id and deductionLocationId) — 'a fossil of exactly this mismatch being papered over in the mock rather than the backend'; :3630-3632 the
complete_sale mock returns id + lineCount (neither sale_id nor saleId) => **No test could ever catch it**; pos_tests.rs:217-224 camelCase regression tests cover args deserialize only and :205-215 asserts format!({:?}), never key names. **[unverified — The two queries that would settle it: Git log -s rename_all to date the drop (regression from the bridge port, or born that way), and one real cargo tauri dev invoke to see whether a shipping desktop path completes a sale. Neither was permitted inside a read-only review. Rev 3's hedge was that a shell might not use this struct; that part is resolved — The struct is the shipping wire type on both shells.]**
- **Does any tablet ever sync at all — Now two independent reasons, neither observed.** P0-1's config split and P0-3's discarded handler Err are both path math from quoted lines. Three facts decide bug vs latent bug and none is checkable statically: (1) **[resolved — Absence] pairing does not write those keys and the file i was told about does not exist** (crates/oz-bridge/src/pairing.rs absent; write_pairing_settings 0 hits), so the hedge is gone and the strong form ships; (2) whether a real paired tablet's session.store_id differs from 'default', i.e. How many store-*.sqlite files one device accumulates; (3) [resolved by refutation] whether a pull ever provisions the store file — It does not, apply_snapshot writes global and
touches no inventory table (P0-4/K1). What remains genuinely open is magnitude: How many tills are affected, which is a runtime question this review could not ask.
- **Three clock and recovery details remain unmeasured** (P0-2(h),(i)): how rusqlite maps a NULL created_at into the Option the parser reads; whether any writer can produce a timezone-bearing string for sales.created_at or audit_log.created_at; and whether the re-verify surface has any button on the tablet, which a registration grep says not but no interface trace confirms.
- **Which trap actually wins the Tab race** (A11Y item 3) — Registration order says the qr trap; one instrumented keydown listener or one NVDA Tab press settles it, and the answer changes the sentence from 'Tab no-ops' to 'focus escapes into the dimmed panel'.
- **How many real _scoped call sites pass sessionToken = null.** The remaining tablet throws (TopologyScreen.tsx:291 and topologyApply.ts:173, both .filter on an undefined listWorkspacesScoped result, both route-gated to manager + multi-store) sit behind the ADR #7 ternary, so the count may be zero; that number decides whether this is a data-integrity finding or a dead-code finding. Related, cheap and unresolved: **Is_ready() has zero non-test callers repo-wide (the only hit is crates/oz-hal/src/traits/printer.rs:40, a printer-state helper) so 'no readiness concept exists on the money path' is true — CompleteSaleResult carries no ready/provisioned/configured flag and the wider sweep
(is_ready|not_ready|ready_|provisioned|sync_configured) yields only kds food-ticket ready_at columns, webhook dedup, and sync_bootstrap.rs:77 persist_provisioned_sync which writes settings at bootstrap and is never consulted by a sale. The 415-vs-14 count that started that sub-claim is unreproducible as a readiness claim because there is no readiness symbol to count — Publish the absence-by-grep, not the number.**
- **The :238 gate's own fate, which is a process question: Offline.rs / offline_tests.rs / quota_gate.rs are ' M' at 02:40, i.e. Uncommitted, so P0-2's data-loss path does not exist in head yet — And the head migration series has outgrown the calendar (20261001_sale_idempotency.sql is untracked while its include_str! Is in the working copy; 707ab6889 later committed both together, closing the compile hazard, but 'git grep -c sale_idempotency head -- crates/oz-bridge/src crates/oz-core/src' returns only migrations.rs, so *** nothing in head queries the new table by name — A loose end, not a finding. *** whoever commits migrations.rs without the .sql reopens it: Head will not compile (include_str! Of a missing file).
- **Document and count numbers still in conflict, both cites left standing:** 448 / 450 / 453 / 473 IPC names (different definitions of 'registered'/'invoked', one polluted needle); **154 vs 155 (the same list off by one — Print the off-by-one, do not smooth it)**; nextest --exclude true for dev-ci, false for CircleCI 06-cargo-nextest.yml:37; bundle-parity 4,771 / 9,146 / ~118; 44 vs 53/54 migration files [unverified — Relayed]; inline tests 646/629/1,340; scoped call sites 477 vs 760 and 0 vs 224 for the same tree; 11 vs 12 dead publish arms. Each above is printed with both cites and no verdict.
- **Method defects that are now warnings for any RE-RUN:** (i) **Windows backslash path filters return zero silently** (':!ui/src\__tests__\' = 0 where ':!ui/src/__tests__/' = 1); (ii) **Struct-vs-interface diffing is not a measurement instrument** — 14 field-order rearrangements matched as drift, and Q4 shows the deeper version: **81 structs have no rename_all and many 'gaps' were snake ts correctly matching snake rust**, so the whole case-sensitivity chapter was partly an artifact of the instrument (drift 31->131->53->21->3); (iii) **Relayed names rot in both directions** — A 'finalize_sale_scoped' was reported then refuted as absent, a command was reported as a typo of another, **And the worst case this hour was a file:
pairing.rs, cited by path by a worker, present in an earlier relay, absent at head — 'whoever told you pairing calls it with the global conn was citing a file that is not at head'**, which nearly made me soften the review's strongest finding on the strength of a phantom file; (iv) head -n / --max-count truncation presented as totality, and **Two of this document's conclusions rest on greps that hit the 16-result cap (testSyncConnection, setSetting) and are marked so on purpose**; (v) 'in the working tree' stood in as a shrug for both 'uncommitted' and 'not yet checked against git log' — d0a9b2f47 shows the deny-list work was already committed — So **Every such sentence now needs a 'git diff head' proof or is dropped**; (vi)
searching generate_handler without '[![' matches prose; (vii) '>' inside a quoted shell string is a redirect on this platform (see the verification note in metrics); (viii) 'the UI never calls it' needs the wrapper name, the route string and the register.tsx gating; (ix) **The faster question when asking 'does the new gate break this feature' is not 'where is the check' but 'does the code that reads this even see the file that contains the check' — Grep the consumer for the guard's own name and take zero hits as the answer**; (x) **The first number is always a measurement of the query, not of the code** — '57 unguarded' was directionally right and numerically wrong in both directions, and the only thing that fixed it was asking
the same worker to re-run its own script with the guard tokens it had missed; (xi) **When a worker hands you a command instead of a guess, run it yourself before briefing anyone else** — twice in ten minutes a question dispatched to a worker was answered by ten seconds of the manager's own git check at the gate, including the build hazard the thinker had promoted over the whole review, which was already fixed.**

## Killed or restated under verification (the review's own claims that did not survive contact with its workers)

A claim's real status, if it is not in this table, is not 'settled'. Rows are ordered by how much they changed the report. Everything below was published earlier in this session and then refuted, narrowed, or confirmed by an independent pass; the first-person framing of the earlier drafts has been removed, but the substance of each retraction is kept because it is the most useful part of the document.

| Claim as first reported | Status | Correction, with cites |
|---|---|---|
| Inventory aggregation collapses across stores (M6, filed major) | False positive, now minor | inventory_locations has no store_id and no tenant_id column (20260813_init.sql:178-187) and stock_summary's PK is (item_id, location_id) (:739-746), so another store's rows do not exist in the file the query runs on. The sums at products_crud.rs:29,:63,:88,:147,:188 are the documented whole-store roll-up (products.rs:60-61 says so in the comment), the stocktake matches the display on purpose (stock_counts.rs:505-510), checkout never reads the aggregate, and the unscoped low_stock_alerts is #[deprecated] (reports.rs:731) and unreached (the live path is low_stock_alerts_at_location, :765-789, carrying the location predicate twice). Leftovers, both minor: it overstates sellable availability by counting transit, damaged and virtual bins (init.sql:181-182), and desktop's warehouse screen reads per-location on a per-store handle while tablet's list_warehouse_products reads all bins on the global handle (oz-bridge/src/products.rs:339-355 vs tablet commands/products.rs:159-163) — same screen name, two answers. A related claim, that the phantom service SKU pollutes the health check at inventory.rs:240, is retracted: that query is 'WHERE location_id = ?1 AND qty <> 0', scoped. |
| The authz converter has a catch-all arm that allows unrecognised permission strings | Refuted completely | authz.rs:227-258 is impl From<BridgeError> for AppError, and :238 preserves PermissionDenied with its message intact; every call site matches PermissionDenied and the deny path is proven by authz_tests.rs:28-49 asserting is_err() on a read permission with a write grant. The 57-command count that rode on it came from a script missing two guard helpers (ctx.require_user_permission_scoped and require_session_permission). Both are replaced by the named 60/46 result in P0-3b; topology, the example suspect, is guarded (apply_topology_diff calls the helper with TOPOLOGY_WRITE and returns PermissionDenied from 5 sites). |
| refund_lines is an orphan table, so the new cumulative bound has nothing to count from | Refuted | The table is written by the only production refund writer at refunds.rs:211 in the same tx before the credit path (non-test inserts repo-wide are that one hit) and read at :586 by list_refunds_for_sale, whose consumers are the refund screens (oz-bridge/refunds.rs:266; tablet refunds.rs:232,:252). What survives is the schema gap: sale_line_id is a logical reference with no FK (init.sql:534), refund_lines has no index and no unique constraint of any kind, there is no trigger, and the guard is check-then-act inside unchecked_transaction(). Bounds in code, nothing in the schema. |
| No readiness gate exists on the money path, evidenced by 415 reads against 14 is_ready sites | Claim true, evidence fake, restated | The number is unreproducible as a readiness claim. 'git grep is_ready -- "!*tests*" "*.rs"' returns one line repo-wide (oz-hal/src/traits/printer.rs:40, a printer-state helper with zero non-test callers). The claim stands on absence: CompleteSaleResult (pos.rs:897-906) carries no ready/provisioned/configured flag, and the sweep for is_ready|not_ready|ready_|provisioned|sync_configured yields only KDS food-ticket ready_at columns, webhook dedup, and sync_bootstrap.rs:77 persist_provisioned_sync, which writes settings at bootstrap and is never consulted by a sale. Publish the absence-by-grep, not the ratio. |
| The setting deny-list now makes the sync flag unreadable by design | Retracted before publication | The daemon reads through typed accessors (sync_client.rs:271,:275,:280-281 -> platform/core/src/settings/typed.rs:373-374,:332-334) that never consult the list (zero hits for is_secret/deny_list in typed.rs or settings/mod.rs); sync_enabled and sync_server_url are in neither list (SECRET_KEY_DENY_LIST, keys.rs:221-236; NON_EXPORTABLE_DEVICE_KEYS, :248); and set is unblocked (run_set_setting gates only local_api./lan_server. prefixes, :69-72,:422-434). The flag is simply never written, which is simpler, duller and harder to argue with. |
| The unsigned /status body can lock every register by sending a malformed value | Retracted; a narrower version is true | The lifecycle parser maps the wildcard arm to Unavailable (subscription.rs:833-869) and Unavailable is writable, so 'trial', 'past_due', 'suspended', empty string and null do not lock a till. The reachable locking values are the four listed in P0-2(a). |
| A rejected push pollutes failed_count and a manual retry can never succeed | Refuted as stated | is_duplicate_id_rejection screens the rejection on both live push paths (sync_client.rs:68-74,:308-321; daemon.rs:195-198) with two named tests; the unguarded mark_failed survives only in dead-but-public run_sync_cycle (lib.rs:536-537). The real rule is harsher: push has no retry at all, selection is status='pending' only, nothing reads retry_count, and the Requeue button targets the pull quarantine (offline.rs:408-425). |
| A one-word reword of the 'duplicate id:' string would silently strand everything | Over-stated, withdrawn | Server tests pin the producer text (sync_store_tests.rs:200,:237,:483; sync_api_tests.rs:1369,:1394) and the client comment cross-references it (sync_client.rs:55-57), so a plain reword fails loudly in the server's own crate. The surviving hole is the substring-versus-prefix gap with fixture-only consumer tests (P1b). |
| Option 3 deletes about 25 tablet features | Retracted; cost is zero screens | A name-set relayed as a feature-set: of 25 tablet-only names, 7 already have a bridge body, 22 exist on desktop as the scoped twin, and 17 are called by no screen; all 8 genuinely called names have their scoped twin registered on both shells behind role-gated routes (P1). |
| The tablet queue is invisible because the sale queues to one database and the daemon reads another | Mechanism wrong, outcome right | Same file, two WAL connections; the actual split is between where the daemon reads its config and where the only registered writer puts it (P0-1). |
| The parity gate's scoped-name synthesis is what let finalize_sale sit in the allowlist printing OK | Refuted as a mechanism | The shell comparison is literal; synthesis is mock-only, so 0 of the 154 entries were alias-explained and the alias class is 4 names on tablet, 0 on desktop. The real mechanism is a list that cannot fail as it grows (P1). |
| The qris double focus trap blocks a sale and lets focus escape into the background | Degraded twice, by the pass that filed it | Both traps call preventDefault() on Tab with no stopPropagation (count 0 in the hook), the winner is decided by registration order, and QrisQrDisplay has exactly one focusable element, so Tab is a no-op with focus ping-pong: major, not sale-blocking. The nested customer-search dialog is unaffected (it renders inside panelRef) and StockShortfallDialog is unaffected (the panel is unmounted, so the null guard at :74 returns first). |
| Nothing can announce inside the payment modal | Degraded | A permanent app-wide polite region exists (useToast.tsx:59, role=status aria-live=polite, mounted at main.tablet.tsx:46) and is simply never fed the amount, because the success paths call setDone without addToast; and retail's own announcer is unmounted exactly while paying (RetailPosScreen.tsx:1423-1425), so the good example turns itself off when it matters. |
| Every tablet boot shows a permanent partial-settings banner that trains operators to ignore warnings | Deleted | There is no banner. hasPartialError has one consumer (SettingsPage.tsx:371-373) firing a 4-second auto-dismissed toast once per settings visit, and the boot fan-out uses Promise.allSettled (SettingsContext.tsx:264-273,:324-335) so nothing throws. The outcome is smaller and opposite: a boot that never opens settings shows only a blank About version string, and no safety behaviour reads it (UpdateBanner.tsx:37-42 calls updater.check() directly). |
| A visible error screen is the cheapest migration-brick fix | Retracted as infeasible | blocking_show() deadlocks inside setup: it is sync_channel(0) plus rx.recv() (plugin src/lib.rs:71-79) while the plugin dispatches run_on_main_thread (desktop.rs:222), and setup runs on the main thread with the event loop not yet started; the plugin's own docs warn against that context (src/lib.rs:355,:365). The two-line map_err and the failure file in %APPDATA%\com.ozpos.app replace it in the fix list. |
| A shiplock manifest would have prevented the 2026-08-12 drift class | Insufficient | The incident was a draft migration applied at 08:16 and final bytes committed at 08:26, so no already-shipped file ever changed and a manifest blessed at first commit sees nothing. That class is runtime-only, which is why the loud error message is the necessary half. |
| Confirmed as published, and still standing | 4 rows | Tablet finalize_sale/void_pending_sale are desktop-only (inventory.rs:457,:472; desktop lib.rs:950-951; zero tablet hits); ShiftReportDto drops three costing fields and the shift screen throws (P0-6); the topology false heal was independently re-verified with two mutations (P0-9); and the reaper voids a tablet pending sale at +30 minutes against the same global file (P0-5). The cross-store lockout, initially counted here, moved to contested: the recovery key is read nowhere outside persistence.rs, the next apply overwrites the journal (commands.rs:659) and recovery retries at every startup (persistence.rs:416-418), against a log line at desktop lib.rs:137-139 that asserts the block. |

## Metrics and verification notes

Scale: 28 workers (26 dimension-by-severity plus 2 targeted), 23 adversarial refutation passes, ~35 digested reports, a 3,875-line review journal. Outcome: 11-12 relayed premises killed, 7 first- or second-hand escalations killed by the workers sent to confirm them, 4 findings confirmed as published, 1 contested, 6 numbers moved (drift 31->131->53->21->3; tablet-only features 25->8->0 screens; dead shims 15-of-top-20->11 shims/41 lines; inline tests 646/629/1,340; IPC 448/450/453/473; Indonesian locale coverage 40%->5.6%). No confidence list from this review survived contact with its own workers, and where a claim outlived the fact supporting it — the readiness gate above is the cleanest case — both are recorded, because a claim is not
protected by the evidence under it. Evidence mix. Executed: none, by this review, at any point — no build, no test, no cargo or npm run, no database opened, no PostgreSQL contact. Path-proven by handle and call-site counts: P0-1, P0-3, P0-3b, P0-4, P0-5, P0-6, P0-9. Simulated: the refund and phantom-stock arithmetic in P0-7 and P0-8, reproduced on throwaway:memory: SQLite with guard logic re-coded from refunds.rs:85-109/:290-366 and products_stock_adjust.rs:92-208 — a transcription of code that was never compiled, and the fix in 53fffa8c1 was re-simulated the same way, so both numbers are simulations of transcriptions. Relayed and not re-refereed, still marked [unverified]: the a11y contrast and touch-target measurements, the licensing
setInterval mechanism, the 44-versus-53 migration count, and the reference-clock tail in P0-2(h). Cite stability. Every file:line in this document was read at a HEAD earlier than a80e3b709 and several moved under it; two commits in the window (53fffa8c1 in refunds.rs, 707ab6889 adding crates/oz-core/migrations/20261001_sale_idempotency.sql plus its include_str! at migrations.rs:282-283) shifted line numbers in files cited here. Treat each cite as a name-plus-region to re-locate, not as a guaranteed line number. Where a cite could not be re-resolved, it is marked rather than silently re-derived. Open markers, restated at save time so nothing here waits on a claim that has since been settled. **Nothing from the earlier draft is still held open by its own
qualifier:** the [pending] hedge on P0-1 is resolved to absence (the pairing write-path it waited on does not exist in this tree), and the empty-table branch of the reference clock is closed — compute_max_ledger_timestamp cannot Err (P0-2(h)). Still open, each stated where it is used: [pending] which focus trap wins the Tab race (A11Y item 3, one instrumented keydown listener settles it); unmeasured and marked as such — the rusqlite NULL-to-Option mapping, whether any writer emits a timezone-bearing created_at string, and whether a locked till has any operator-reachable re-verify at all (P0-2(i), inference from a registration grep with no interface traced); [unverified] which database the pending counters should read after the sync fix lands (fix 5b);
[unverified] the relayed a11y contrast/touch-target measurements and the licensing setInterval mechanism; [unverified] the 44-versus-53 migration count; and unresolved magnitude on the camelCase wire mismatch (last chapter). The count conflicts listed there are printed with both cites and no verdict, and the topology cross-store lockout is printed as contested rather than arbitrated.
lexicographic reference-clock tail in P0-2(h); [unverified] which database the pending counters should read once the sync fix lands (fix 5b); [unverified] the relayed a11y and licensing measurements; and the magnitude question on the camelCase wire mismatch in the last chapter. Everything the previous draft carried as [pending] on P0-1 is resolved — the escape hatch it waited on does not exist in this tree.

**Doc-drift note, condensed from an earlier draft of this chapter (each item is corrected in place above):** AGENTS.md's 'all ten steps now have a CI backstop' is wrong in both directions; the same file says the ui-test job runs typecheck where dev-ci.yml:275-276 runs lint; README's 'CI nextest carries no --exclude' is true for dev-ci and false for CircleCI 06-cargo-nextest.yml:37; '9,146 Fluent IDs across 52 files' counts both locales; '44 migration files' against 53-54 on disk [unverified, relayed]; and the ADR-49 chapter's '15 modules / 108 names' is an incomplete subset. A third of this review's corrections were counts measured with the wrong needle, not arithmetic errors.
