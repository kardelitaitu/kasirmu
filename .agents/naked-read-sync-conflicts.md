# sync::list_sync_conflicts_scoped ships unguarded — the ratchet is red on your commit, and it is right

Handoff note, not an edit. Written 2026-09-13 ~07:40 +07 by the registration-ratchet lane (Manager-3).
Evidence gathered by a read-only audit at 00:29:56Z-00:35:00Z UTC, tip `46cbe764e`.

## The finding
`sync::list_sync_conflicts_scoped` is registered and reaches the network with **no permission check at any depth**.

- defined `apps/desktop-client/src/commands/sync.rs:429`, registered `apps/desktop-client/src/lib.rs:1262`
- its entire authorization surface is [sync.rs:434] `state.resolve_scope(&session_token)?;`
- depth 2 `AppState::resolve_scope` [state.rs:598-626] = `resolve_session(token)` + `open_store(...)`
- depth 3 `resolve_session` [state.rs:450-475] = session-store lookup + expiry check + `InvalidSession`
- **permission markers found in all three: none.** Not a macro, not a router, not a bridge shim (`oz_bridge::` never appears in the body), so the `gated_bridge_stems` escape does not apply.
- then it builds an outbound reqwest GET to `{base}/api/sync/conflicts` [:440] and returns `body.conflicts` [:465].

The contrast is in the same file, nine lines away:

- [sync.rs:482] `require_permission_for_session(&state, &session, permissions::SYNC_MANAGE).await?` — the **write** twin, `resolve_sync_conflict_scoped`, asks for `SYNC_MANAGE`.

Authenticate-then-assume on a read whose write neighbour is gated. That is the shape this pin exists to catch.

## Provenance
`git blame` puts `lib.rs:1262` and `sync.rs:429-434` in **`028056eaaeb`** `feat(sync-ui): add conflict review screen, IPC commands and mock handlers` (06:43:58+07:00). That commit's 6-file stat contains `sync.rs`, `lib.rs`, `ui/src/api/syncConflicts.ts`, `tauri-api.ts`, `SyncConflictReviewScreen.tsx`, `ConflictDiffViewer.tsx` — **`registration_gate_debt.generated.rs` is not among them.** The ledger was last regenerated by `a32b13aaa2` at 05:08:17+07, **95 minutes before** the registration.

## What is red, and why it is your repair, not mine
`cargo test -p oz-pos-app registration` → 6 passed / 2 failed:

- `drift_pin_three_way_partition_is_complete_and_sums` — `new_holes = ["sync::list_sync_conflicts_scoped"]`, `paid_stale = []`
- `drift_pin_debt_ceilings_only_shrink` — 70 ungated against a ceiling of 69, and 27 authenticate-then-assume against 26

The two numbers move **only** with a regenerated ledger plus a recorded decision. The pin's own message says it: record the reason in `docs/records/JOURNAL.md` before the number moves.

## Two honest ways out (yours to choose)
1. **Guard it.** Add `require_permission_for_session(..., permissions::SYNC_MANAGE)` the way the write twin at `:482` does, and the count goes back to 69 by itself. Preferred: a conflict list is the read half of an operation the write half already authorises.
2. **Pay for it.** Regenerate the debt ledger, move the ceilings, and write the JOURNAL reason for why an unguarded read of cross-tenant sync conflicts is acceptable. That is a legitimate outcome of this design — the ratchet demands a decision, not a specific one.

## What would be the wrong fix, from either of us
Widening `GUARD_MARKERS` to recognise `resolve_scope`/`resolve_session` as a guard. It would reclassify the entire 27-name authenticate-then-assume bucket as gated and make the ceiling fall to zero **by construction**, and `drift_pin_guard_marker_vocabulary_is_closed` would not catch it, because that pin polices spellings containing `permission` and `resolve_scope` is not one. A vocabulary widened to silence a specific red is a goalpost move.

## One thing I am fixing on my side
`REGISTERED_TOTAL = 447` and `REGISTERED_FLOOR = 447` [registration_gate_tests.rs:60] compare a floor against the **stale ledger**, not against the tree — so that leg stayed green while `lib.rs` grew to ~449 registered pairs. A pin that cannot fail is worse than a red, so the leg is being changed to measure the tree. It will then go red at 447 vs your count, which is the same conversation as above, one floor earlier.

## Caveats on the numbers here
70 / 43 / 27 and the failure strings are an audit's **execution of the harness's own predicates over current source**, not captured `cargo` stdout (no build budget in a 12-minute box). One run confirms or corrects them:
`cargo test -p oz-pos-app drift_pin -- --nocapture`
And `449` is a regex over the `generate_handler! […]` bracket, not `registered_names()`'s parse; they can differ by a name or two if the macro nests a bracket.

Also untracked-dirty in the shared worktree at 00:34:44Z, i.e. your lane is still mid-flight: `ui/src/features/sync/SyncConflictReviewScreen.tsx`, `ui/src/dev-mock/tauri-api.ts`, `platform/sync/src/lib.rs` (staged **and** modified), `crates/oz-core/migrations/20260813_init.pg.sql`.

Nothing in this note edits your files. The ratchet lane holds its pin as it stands.
