# Agents Configuration & Rules

<!-- Audit stamp: 2026-09-04 · DSH · status: ACCURATE · version lock: 0.0.39 · 7 pre-commit gates (count corrected 2026-09-13, when the cargo fmt step was removed) · conventional commits enforced -->

## 🚨 Critical Agent Directives (MUST FOLLOW)

| Rule | Direct Instruction | Why / Context |
|---|---|---|
| **Branching** | **NEVER create new branches. NEVER switch branches.** | Always work directly on the currently active branch unless specifically requested by the user. |
| **Commits** | **ALWAYS commit with format `<type>(<area>): <description>`.** | Must follow conventional commits. Make local commits after each logical task — the permitted *form* of the commit is the **Commit Writing** row below. |
| **Commit Writing** | **NEVER `git add` — with the sole exception of the §3 one-line new-file chain —, NEVER `git stage`, NEVER `git commit -a`, NEVER `git commit --amend`, NEVER `git stash`. The only permitted commit form is ONE line with an explicit pathspec: `git commit -m "<type>(<area>): <subject>" -- path/one path/two`.** | A pathspec commit takes those files from the working tree and, for already-tracked paths, needs no prior `add` — a *new* file cannot enter a commit in that form, and the §3 new-file chain is the only sanctioned exception. Nor can a pathspec commit collect anything another session staged. In a checkout where several agents commit concurrently the index is a single shared object, so anything left staged is a hazard for someone else even when it is harmless for you. Git & Commit Policy §3 carries the incidents and the detail. |
| **Pushing** | **NEVER run `git push` without an explicit direct order.** | Even after completing all checks, wait for the user to explicitly say "push". |
| **Version Lock** | **Version is locked at `0.0.39`. NEVER modify version numbers.** | Do not bump version in `Cargo.toml`, `package.json`, `tauri.conf.json`, etc. |
| **File Paths** | **ALWAYS use forward slashes (`/`) in path arguments on Windows.** | Avoid path escaping bugs (e.g., use `C:/My Script/oz-pos/`). |
| **File Reading** | **ALWAYS read files in small chunks (≤ 500 lines).** | Preserves context window and prevents output truncation. |
| **Discovery** | **ALWAYS use `codebase-memory-mcp` first for code exploration.** | Graph discovery saves context tokens and surfaces call chains faster. |
| **Currency** | **ALWAYS use `Money` struct (`i64` minor units). NEVER use float.** | Monetary values must never use `f32`/`f64`. |
| **DB Writes** | **ALWAYS use `rusqlite` transactions for database writes.** | Never write to SQLite outside an explicit transaction. |

---

## 🛠️ Quick Setup & Pre-Commit Gates

```bash
git config core.hooksPath .githooks   # enable pre-commit hook (EOL + bundle-parity + FTL dedupe + column types + PG drift + Go + ftl orphans)
```

The `.githooks/pre-commit` hook runs **seven steps** automatically before every commit, in this order (< 1s total on a typical commit; each step is *triggered* only when the paths it cares about are in the commit):
1. **Line-ending normalization** — strips CR from staged text files in the working tree and index, re-staging them so the committed blob is LF (backs `.gitattributes` `* text=auto eol=lf`). Skips files whose effective `eol` is `crlf` (`*.bat`/`*.cmd` — the working tree must stay CRLF for cmd.exe) and real binaries (`grep -qI`; `text=auto` reports "auto" for PNGs too, and stripping their CRs destroys the signature). Both exclusions were missing until 0.0.36; `scripts/test-eol-guard.sh` guards them.
2. **`Bundle parity: staged files only`** — `scripts/verify-bundle-parity.py --staged-only` with `--include-getstring --include-nav-keys --include-key-fields --include-dynamic-literals --include-id-maps --check-domain-pairs`, over staged files in `features`, `components`, `frontend`, `contexts`, `hooks` and `platform`. Fails if any of the **eight checked surfaces** references a key missing from `.ftl`/`.id.ftl`.
3. **`FTL dedupe dry-run`** — `scripts/dedupe-ftl.py --dry-run` when `.ftl` files are staged.
4. **`Migration column-type lint`** — `scripts/verify-migration-column-types.py --staged-only`, when `crates/oz-core/migrations/*.sql` is staged.
5. **`PG schema drift guard`** — `scripts/generate-pg-migration.py --check`; `20260813_init.pg.sql` is generated, **never hand-edited**.
6. **`Go gate`** — when `apps/license-server/*.go` is staged: `gofmt -w` + `go vet ./...`. Aborts if `go`/`gofmt` are missing.
7. **`FTL orphan lint`** — `scripts/verify-ftl-orphans.py --staged-only` when the commit stages a `.ftl` file. Mirror of step 2: that asks whether code names a key no bundle defines, this asks whether a key has any code reading it. Nothing checked the second direction, and the debt is real (`topology-shortcuts-*`, 18 keys whose feature was removed; ~23 `warehouse-*` keys with zero references anywhere). Staged-scoped because dynamic composition leaves 93 honest whole-tree candidates; `--census` reports them, `--self-test` proves the check can fail.

> 🚫 **`cargo fmt` is no longer a pre-commit step (removed 2026-09-13).** The old step ran `cargo fmt --all` — the whole workspace — whenever any `.rs` file was staged, which under concurrent agents reformatted every other session's in-flight `.rs` files in the working tree. Formatting is still enforced, but check-only and outside the commit path: `cargo fmt --all -- --check` runs in pre-push (`scripts/run-pre-push.py`), CI (`dev-ci.yml#cargo-check`), `scripts/check.sh`, and `scripts/release.sh`.

> ✅ **All seven steps now have a CI backstop — six of them as enforcement, steps 1 and 7 as local-only with a CI stand-in.** Heavy UI typechecking (`npm run typecheck`) and Vitest i18n (`scripts/lint-i18n.sh`) are offloaded from per-commit to `pre-push` (which runs them in parallel via `scripts/run-pre-push.py`) and CI (`dev-ci.yml#ui-test` and `dev-ci.yml#i18n`), keeping atomic pre-commit fast (<1s) for multi-agent workflows. Steps 4 and 5 (`verify-migration-column-types.py`, `generate-pg-migration.py --check`) lived in `ci.yml`, were retired to `.bak` (`23c96330`), and were restored into `dev-ci.yml#static-gates` in **0.0.37** — until then the opt-in hook was their only guard, and they had no `gates.json` record either, so the drift checker could not even report them as unenforced. CI runs the column-type check without `--staged-only`, so it scans every migration file in the checkout, not just the ones a commit touched — 59 today as measured 2026-09-13 by `ls crates/oz-core/migrations/*.sql | wc -l`; the checker prints its own count because the number moves with every migration. Both are pure text comparison (~1.1s together): no Docker, no psycopg, no sqlite handle. Step 6 (Go) has been CI-backed since 0.0.36 (`13f2a1dc`): `dev-ci.yml#static-gates` runs `gofmt -l`, `go vet ./...` and `go test -short` on `apps/license-server`. Note the CI gate is `gofmt -l` (report-only, fails on any unformatted file) while the hook runs `gofmt -w` and re-stages, so a commit made without `core.hooksPath` can be unformatted and CI will reject it rather than fix it. On a clone without `core.hooksPath` set, none of the seven run at commit time — but CI now catches all of them, with the qualification below. The hook's comments used to cite the retired `ci.yml`; **three of the four now name `dev-ci.yml`** (`grep -c dev-ci.yml .githooks/pre-commit` = 3, at `:164`, `:182`, `:203`) and `verify-ci-docs-drift.py` polices any `.github/workflows/<name>.yml` a hook comment mentions — while the fourth, the bundle-parity block at `:103`, names **no workflow file at all** (it cites "CI's `lint-i18n.sh`"), so no file-name rule can reach it. **And step 1's "backstop" is a self-test, not a gate:** CI runs `bash scripts/test-eol-guard.sh` (`dev-ci.yml:576-577`) to prove the guard still leaves `eol=crlf` files and binaries alone; nothing in a live workflow rejects a committed blob carrying CRLF, so that normalization is enforced only where `core.hooksPath` is set — the same class of local-only step 7 admits to being.
>
> **What CI actually runs.** Two workflows are live: `dev-ci.yml` and `release.yml` (`v*` tags, restored desktop-only in 0.0.36). `dev-ci.yml` is triggered by `pull_request` targeting `main`, by `workflow_dispatch`, **and by `push` to `main`** — `dev-ci.yml:3-8` declares `push:` / `branches: [main]` (`sed -n '3,8p' .github/workflows/dev-ci.yml`, 2026-09-14, and the shipped checker's GROUND TRUTH block prints the same three parsed events for this file -- item (7) of the amendment below records that the older parser read none of them), so **pushing `main` does run CI** (the workflow's own comment at `dev-ci.yml:662-663` adds that such a push also deploys, via `northflank-deploy`'s `if:` at `:695`); a PR is what gates the **merge**, and the case that runs nothing is a push to a non-`main` branch. `dev-ci.yml` jobs — ten of them, printed by `awk '/^jobs:/{f=1;next} f&&/^  [a-zA-Z0-9_-]+:$/{print $1}' .github/workflows/dev-ci.yml | tr -d ':'` (re-run 2026-09-14: exactly those ten; the command previously cited here carried a stray `,$/` in the pattern and died with `awk: cmd. line:1: unterminated regexp`, printing nothing — a documented command that does not run is worse than none) — are `changes`, `website`, `cargo-check`, `cargo-nextest`, `ui-test` (typecheck → lint → vitest → tz-invariance), `i18n`, `ci-docs-drift`, `static-gates`, `release-readiness`, `northflank-deploy`. **`cargo-check` runs two steps and no more: `cargo fmt --all -- --check` (`dev-ci.yml:194-195`) then `cargo check --workspace --all-targets --all-features` (`dev-ci.yml:196-197`). It does not run clippy, and no live workflow does** — `grep -c clippy .github/workflows/dev-ci.yml .github/workflows/release.yml` = 0 for both (2026-09-14); the only clippy left under `.github/workflows/` is inside the retired `ci.yml.bak:251`. **Clippy is therefore NOT a merge gate: a PR — or a push to `main` — can land with clippy warnings.** It is local policy: `scripts/check.sh:44` and `scripts/release.sh:61-62`. Formatting is the opposite — `cargo fmt --all -- --check` IS CI-enforced at `dev-ci.yml:195` (and in pre-push, `check.sh`, `release.sh`) — so read "fmt → check → clippy" wherever you met it as "fmt → check". CI's `cargo nextest run --workspace --all-features` carries **no `--exclude`**, so it tests app crates that `check.sh` skips. `northflank-deploy` omits `ci-docs-drift` from `needs`, so a docs-drift red does not by itself stop a deploy -- **but the drift check is blocking in CI, not advisory**: the step `Verify CI docs agree with gates.json and the workflows` (`dev-ci.yml:381`, `id: drift`) ends on `[ "$status" = "PASS" ]` at `:407`, that step's last command, so a false test fails the job (measured 2026-09-14) — `:407` is not the job's last step, which is `AGENTS.md mirror truthfulness` (`:426-429`). `continue-on-error` appears at `:329` (a comment), `:336` (`i18n`'s `FTL orphan census (informational)` setting — job at `:301`) and `:346` (a comment), and as a setting nowhere in this job; what `:346` read when this sentence was written ("Deliberately continue-on-error ... fails with 69 items") has since been replaced in the workflow itself by `040435d11` — `:346-347` now read "BLOCKING, not continue-on-error". `scripts/gates.json` has it as `status: "required"`, its own note recording "the step is now blocking -- the stated condition"; the workflow comment this clause cited at `dev-ci.yml:662-664` no longer exists — `040435d11` deleted the "deliberately NOT a dependency" wording used to locate it (`grep -n "deliberately NOT a dependency" .github/workflows/dev-ci.yml` exits 1, measured 2026-09-14), and the replacement block states at `dev-ci.yml:684-687` that ci-docs-drift is NOT advisory and stays off `needs` as a deploy-topology choice (recorded at `:349`) — the report was taken up in the workflow, so only the missing `needs` edge stands. E2E, a11y, security and nightly are **not** enforced — a green Dev CI run is not proof those passed.


> **What `verify-agents-mirrors.py` does, and what it does not.** It compares, against the repo itself: the **gate count** and the **step-name enumeration** (the `# ── … ──` headers in `.githooks/pre-commit`, read from the worktree *and* from `HEAD:`), the **accepted commit types** (`.githooks/commit-msg`), the **version** (`Cargo.toml`), the **workflow job names** a mirror cites as `dev-ci.yml#<job>`, and any claim that a step CI really covers is local-only. What it does **not** police: **which tools a job inside a live workflow actually runs** — the rule never opens a job's `run:` lines, and `grep -n PUSH_CLAIM_RE` still finds no such constant (so cite line numbers, never a name). Everything else about triggers is now policed, in both directions, as of `c8abceaf6` shipped the same day this paragraph was written and `813992350` narrowed it an hour later — and that first change repaired the parser as much as the logic: the old `triggers_of()` never matched a block-style `on:` at all, so run against this tree's real `dev-ci.yml` it returns `[]` (`git show c8abceaf6^:scripts/verify-agents-mirrors.py`, measured 2026-09-14), which made the old rule not merely narrow but **inoperable in both directions** — permanently unable to catch a false denial, and ready to flag a true sentence as false. The shipped version parses the block at its own indent, and the WRONG-guard at `:1883-1900` refuses to let a dead parser read as clean. Every pointer below is read off the committed script at `813992350` (2026-09-14), because a line number into `scripts/` is only true of the revision it was taken from. The rule itself: `push_claim_findings()` (def `scripts/verify-agents-mirrors.py:496`, called at `:1064`) matches **denials first** (`PUSH_DENIAL_RES` `:405`, applied `:541-542`) and only then **assertions** (`PUSH_ASSERTION_RES` `:389`, applied `:545`), because "there is no push trigger" also contains the assertion phrase and the comment at `:536` exists to keep that order from being reversed. It is **per-workflow, not OR-ed**: a claim must name a workflow and that workflow's own parsed `on:` set decides, and what the checker saw is printed for every workflow (the `triggers of <name>:` lines in its GROUND TRUTH block, `:1127`), so the parse is auditable instead of asserted. Three guards decide whether a sentence is even eligible. It needs a CI-ish subject, which `CI_SUBJECT_RE` requires (`:381`, tested against the whole slice at `:533`), and it must not be checker prose — which is now a LOCAL test, not a global one: `checker_prose_span()` (def `:447`) applies `CHECKER_PROSE_RE` (`:429`) to the matched claim span plus `CHECKER_PROSE_WINDOW` = 48 characters (`:444`), called at `:549`, and historical markers are read per slice (`:528`) and whole-line only inside a `<!--` dated record (`:512`), so a live sentence can no longer borrow a comment's immunity. Backticked and quoted phrases are stripped by `unquoted()` before matching, and a branch qualifier keeps the true sentence "a push to a non-`main` branch runs nothing" from reading as a denial (`:542`). A dead-parser guard backs the fixture itself: if `triggers_of()` (`:335`) cannot see the push trigger that is on disk, the self-test reports WRONG rather than grading nothing (`:1883-1900` — and it has **four** branches, one of which fires when planting the assertion in the fixture changes nothing, so a parser that quietly stopped working cannot read as a clean run). Measured against `c8abceaf6` and re-measured against `813992350` on 2026-09-14: `python scripts/verify-agents-mirrors.py --self-test` → 33 green = 26 CAUGHT + 7 CLEAN, 0 red, exit 0, identically on both revisions — the surface did not move — and the live run exits 0 on both. The residue is narrow and measured: a false CI claim can now escape only by sitting within 48 characters of a word like `mirror`, `checker`, `rule` or `gate` — that is, inside the checker's own documentation, not merely in a paragraph that mentions it (before `813992350`, one such word anywhere on a 3,500-character markdown line exempted every CI claim in that line, which is exactly how `.agents/AGENTS.md`'s own 'What CI actually runs' paragraph read clean) — or by writing `push` inside quotes or backticks, which `unquoted()` strips before matching — or by carrying no CI-ish subject at all. **The gate's silence is still not agreement**, but the radius of that silence is now 48 characters rather than a whole line. That gap is exactly how two false claims sat in both mirrors while the gate stayed green: "there is no `push` trigger" (a `push:` block is declared at `dev-ci.yml:6-7`) and "`cargo-check` (fmt → check → clippy)" (clippy occurs 0 times in either live workflow). Both were corrected on 2026-09-14. A rule that would catch them should compare prose against `scripts/gates.json` and against a raw read of the `on:` lines, and **not** through PyYAML: a default loader parses the YAML key `on:` as boolean `True` and silently drops the very block such a check exists to read (and `triggers_of()` is now a regex that takes the block at its own indent and accepts the `True:` key — repaired in `c8abceaf6`, because the earlier regex matched nothing on a block-style `on:`, exactly as its own docstring now records). Read the manifest by its fields, not by its nouns, because both have already misled a reader here: `status` is a POLICY word, so `rust-clippy` is `status: "required"` with `runners: {"check.sh": ["clippy"]}` and the note "Local-only / manual. Skipped in CI, verified locally in check.sh / pre-push" — i.e. **required-by-policy but CI-unrun, with `check.sh` as its only runner and no `ci` block at all**. It must not be called retired, because that word is a machine-state here, not a synonym: `scripts/verify-ci-docs-drift.py:104-105` defines the status set and says a `retired` gate "asserts that the check enforces NOTHING today" and must carry no `ci` block, whereas clippy does run — locally, on every `check.sh` (measured 2026-09-14: `python -c "import json,collections; g=json.load(open('scripts/gates.json'))['gates']; print(collections.Counter(x['status'] for x in g))"` → 53 required / 16 retired / 1 advisory, and the single advisory row is `a11y-advisory`). `runners` is not authoritative either: the `ci-docs-drift` row lists only `check.sh` although that job runs, and blocks, in `dev-ci.yml:352-407`. A manifest is a third source to cross-check, never a substitute for opening the workflow.

> **Tree counts, re-measured 2026-09-14** (each number carries its command; where an older prose figure differs, this one wins): **16 crates** — `ls -d crates/*/ | wc -l` (the "13 crates" figure omitted `oz-bridge`, `oz-lan`, `oz-local-api`) · **277 feature `.tsx`** — `find ui/src/features -name '*.tsx' | wc -l` (was 183) · **572 files under `ui/src/__tests__`** — `find ui/src/__tests__ -type f | wc -l` = 303 `.tsx` + 268 `.ts` + 1 `.json` (was 516 files / 8,056 cases) — and the **Vitest case total is deliberately not quoted**, because it exists only inside a test run (`cd ui && npm run test`) and no static command re-derives it · **8,252 `#[test]`** — `grep -rn --include='*.rs' -o '#\[test\]' . | wc -l` (was 8,709) · **54 `.ftl` files — 27 en + 27 id — holding 9,825 message definitions (4,875 en + 4,950 id) and 4,950 distinct IDs**, of which **75 IDs have no English definition**. Count them **per file**, which is the only safe way: `ls ui/src/locales/*.ftl | xargs grep -hcE '^[A-Za-z][A-Za-z0-9_.-]*[[:space:]]*=' | awk '{s+=$1} END {print s}'` → 9,825 (and 4,875 restricted to the non-`.id.ftl` files, 4,950 to the `.id.ftl` files — the two add up exactly); `grep -hoE '^[A-Za-z][A-Za-z0-9_.-]*[[:space:]]*=' ui/src/locales/*.ftl | sed -E 's/[[:space:]]*=$//' | sort -u | wc -l` → 4,950 distinct; 75 for the no-English gap (per-file `comm -23` of each `*.id.ftl` key set against its English twin). **The `cat` boundary trap, measured here the same day:** `ui/src/locales/sales.ftl` has no trailing newline — its last byte is `}`, which `for f in ui/src/locales/*.ftl; do tail -c1 "$f" | od -An -c; done` shows for exactly one of the 54 files — so `cat ui/src/locales/*.ftl | grep -c` welds that definition onto the next file's first line and silently loses a match, reading 9,824 and 4,949. **Those two numbers are an artifact of the missing newline, not a second method**, and the mechanism first written here ("README's regex also counts dotted `key.attr` lines") is false: a dotted-key pattern matches 0 lines in the tree, so no prose in either mirror may attribute a ±1 to it · **IPC: 453 desktop / 322 tablet registered, 297 in both shells, 478 distinct** — `python scripts/verify-ipc-parity.py` prints the per-shell registered totals (458 UI command strings / 453 registered desktop / 322 registered tablet) and exits 0; the both/union figures come from intersecting the two `generate_handler![…]` lists (`apps/desktop-client/src/lib.rs:845`, `apps/tablet-client/src/lib.rs:445`) on a top-level-comma split, which that tool does not print. Earlier prose said 450 registered (425 desktop / 297 tablet / 272 both), measured 08-09-26.

> **Keeping this list honest:** `scripts/bump-version.ps1` updates the *version* lines in these mirrors but never did anything about the *gate* list — which is how all three drifted to different counts, and twice into claims the repo contradicts. `scripts/verify-agents-mirrors.py` now closes that: it reads the gate count from `.githooks/pre-commit`, the CI coverage from `.github/workflows/*.yml`, the accepted commit types from `.githooks/commit-msg` and the version from `Cargo.toml`, then fails if any mirror disagrees. It runs in `check.sh` and in `dev-ci.yml#ci-docs-drift`, so a stale mirror is a red build rather than a silent wrong belief. The hook is still the source of truth: `grep -n '^# ──' .githooks/pre-commit`.

> For full repository verification mirroring the entire CI matrix, see [`scripts/check.sh`](../scripts/check.sh).

### ⚠️ A stylesheet has no linter, no hook step, and no verification claim of its own (measured 2026-09-15)

**A commit message that claims `eslint exit 0` as verification of a `.css` change is claiming a gate that does not exist.** Four measurements behind that sentence, each with the command that re-derives it:

**How the claim gets written.** `git log --since="2026-09-13 00:00" --format="%h" -- "*.css"` → 23 commits have touched a stylesheet since that hour, and **4** of them name `eslint` in the body — `289be3959`, `26ffd89c1`, `09eec8386`, `903b30a71`; the same filter over all history returns **52 of 963** (both measured 2026-09-15). None of them lied: each commit also carried `.tsx` files, and there `eslint` does read the source and does grade it. The false part is the REACH of the sentence — one surface was verified, a second was silently covered by the same clause, and nothing in this repo's own docs told the author the difference. A briefing tonight counted eight such commits; the command above names four since 2026-09-13, so trust the command, not the count.

- **No CSS linter exists anywhere in the repo.** `ui/package.json:15` is `"lint": "eslint ."`, and `grep -nE "stylelint|postcss|prettier|lint-staged|husky" ui/package.json` exits 1 — neither the scripts block nor `devDependencies` names one. Repo-wide those five names return **7 paths and not one of them is a config**: `stylelint` survives only as a stale ignore line (`.gitignore:179`, `/ui/.stylelintcache`), `postcss` only inside `ui/package-lock.json` and `website/package-lock.json` as Vite's own transitive dependency, `prettier` only in `.vscode/`, one archived journal and one script comment. There is not even an unused config to revive — and `grep -ci css ui/eslint.config.js` returns 0, so the linter's own config never says the word.
- **Why the green is VACUOUS, not narrow.** The only `files:` patterns in `ui/eslint.config.js` are at `:62`, `:73`, `:84`, `:92` and `:107`: `vite.config.js`, `vite.config.d.ts`, `src/features/**/register.tsx`, `e2e/probe-ws.mjs`, `src/__tests__/**/*.test.{ts,tsx}`. No `.css` glob, no CSS processor — so ESLint 9 prints `File ignored because no matching configuration was supplied` for a stylesheet and **exits 0**. Measured, not inferred: `cd ui && npx eslint src/features/sales/VoidOrdersScreen.css` → that warning, exit code 0. A warning about an ignored file is not a pass over the file. CI's own `UI lint` step (`dev-ci.yml:266-267`) runs the same `eslint .` and is blind the same way.
- **What DOES read CSS is Vitest, and it is a minority of the suite.** `find ui/src/__tests__ -type f | wc -l` → **589** files; `grep -rl "\.css" ui/src/__tests__ | wc -l` → **27** mention a stylesheet at all, while `grep -rlE "features/[A-Za-z0-9_./-]+\.css" ui/src/__tests__ | wc -l` → **12** name a feature-sheet path; the union of those namers with the six tree-walkers below is **17** files that open a sheet at all. Six walk a directory — `themeTokenCompliance`, `composedRuleIdenticalPair`, `popupBackgroundCompliance`, `animationCompliance` and `noiseDitherCompliance` read **every** sheet under `ui/src/features`, while `screenExtraction` reads only the sheets a **registered** screen names, so an unregistered sheet is invisible to it — and the twelve namers hold curated path lists, `focusVisibleCompliance` and `touchTargetSizing` each at **57** (`grep -oE "features/[A-Za-z0-9_./-]+\.css" <file> | sort -u | wc -l`), `colorContrastCompliance` reading `tokens.css` and nothing else. Both greps UNDER-read, and that is the caution to carry: `StaffLoginScreen.test.tsx:290` builds its path from separate arguments (`resolve(UI_SRC, 'features', 'auth', 'StaffLoginScreen.css')`) so neither pattern sees it, three more files open only a shared sheet (`tokens.css`, `frontend/themes/components.css`, `frontend/shell/Tooltip.css`), and the remainder of the 27 name a stylesheet in prose or a `vi.mock` without reading it. A briefing tonight said "19 of 580"; as measured here it is 27 mentioning and 17 opening, out of 589 — dated 2026-09-15, test files land hourly, so re-run the commands rather than trust either set.
- **The hook cannot see CSS either.** `grep -c '^# ──' .githooks/pre-commit` → 7 sections. Only step 1 (line-ending normalization) has no pathspec filter — `git diff --cached --name-only --diff-filter=ACM` at `:38` — and it asserts nothing about content. Steps 2–7 are extension-locked: `.tsx|.ts` at `:127`, `*.ftl` at `:145` and `:255`, `migrations/*.sql` at `:166` and `:185`, `apps/license-server/*.go` at `:204`; the string `.css` appears NOWHERE in the hook (`grep -c "\.css" .githooks/pre-commit` exits 1). And the hook **now runs** in this checkout: `git config --get core.hooksPath` prints `.githooks` and exits 0 (re-measured 2026-09-15 from three directions: this lane's own `319a18e41` printed the hook's eligibility skip for a `__tests__` path, and two concurrent lanes reported the same hook firing on their commits — one of them printing `verify-bundle-parity: 0 missing key(s)`. Those two reports name sessions, not commits, so no SHA is quoted for them and none of the three establishes WHEN it was set). What this sentence asserted when the section was written — that the hook never ran here and the LF bytes came from `.gitattributes:26` alone — was a current measurement with a reproducing command, not a dated `<!--` record, so it is corrected in place rather than superseded in a new clause; and because `core.hooksPath` is local config that is deliberately not versioned, no commit names the moment it was set and none is claimed. None of the section's four measurements moves: `.css` appears NOWHERE in `.githooks/pre-commit`, so a live hook is exactly as blind to a stylesheet as an unset one was. What the correction does remove is the escape hatch — a lane can no longer account for its own commit by saying the gate never fired, which makes silence from a committing lane weakly positive evidence that steps 2 through 7 ran and passed on its staged paths, and evidence about nothing before.

**The replacement, because a prohibition with no alternative is how people go back to the vacuous command.** Verify a stylesheet with the five sheet-walking suites — `cd ui && npx vitest run src/__tests__/themeTokenCompliance.test.ts src/__tests__/composedRuleIdenticalPair.test.ts src/__tests__/popupBackgroundCompliance.test.ts src/__tests__/animationCompliance.test.ts src/__tests__/noiseDitherCompliance.test.ts` — report the pass counts THEY print, and then state plainly that no linter saw the file. If the edit introduces a property none of those suites asserts, say THAT rather than implying coverage; each of the five grades a fixed set of shapes, and a new longhand outside them is graded by nothing. The honest summary: **a stylesheet is the only surface in this repo with nothing mechanical between an author and a commit.**

**How narrow that replacement is, measured 2026-09-15 at HEAD `70f057db8`.** `composedRuleIdenticalPair.test.ts` composed its whole population from `FEATURES_DIR = path.resolve(process.cwd(), 'src', 'features')` (`:78`), walked at `:289` through `cssFiles(FEATURES_DIR)` — so every stylesheet outside `ui/src/features` was outside that walker's directory by construction: nothing under `ui/src/components/**` or `ui/src/frontend/themes/**` was composed when this sentence was written, which is why the root-only fallback tails left outside the last sweep fence (`ef6b4d5bf` cleared 126, `42e6c8201` 93 more) sat in sheets this suite never opened. **SCOPE SUPERSEDED THE SAME DAY by `d9fcc7820`, the commit that widened the walk — the live scope sentence closing this paragraph states what it covers now; the rest of this paragraph stays valid as the shape of the caveat, because the widening moved the directory and not the parser.** Inside its own scope, the size of the graded slice is in the file's header: "105 feature stylesheets, 948 blocks that carry both a color: and a background-side declaration, 394 of them gradable through at least one theme, and 0 identical pairs as of `268af4a6e`" (`:47`-`:49`) — **394 of 948, or 41.6 %**, the rest lost to shapes no resolver reaches there: a named colour, an `rgba()`, or a custom-property chain that never lands on a hex value. Those are prose and a dated measurement, not assertions — the runtime case at `:396`-`:406` floors the population it walked (more than 0 sheets, more than 0 composed pairs, more than 10 gradable) so a green cannot be vacuous, and puts **no bound on the ceiling**, meaning a run cannot tell you whether the graded slice widened or narrowed since the header was written. A lane re-measuring after the literal-fallback fix at `1a1a70a90` reports 958 composed and 417 graded (43.5 %), 29 pairs blocked by the other side of the pair being one of those unresolvable shapes and 501 lost to them outright, and reports further that widening the same walk to all of `ui/src` composes 1,058 and grades 461 with no violation surfaced — all attributed, not asserted, because `cd ui && npx vitest run src/__tests__/composedRuleIdenticalPair.test.ts` at this HEAD, before `d9fcc7820`, printed exactly one coverage line and it was not a composed/graded pair count: "detailed reads outside tokens.css (component-scoped, not gradable here): 24". **The consequence, read narrowly: a 0-violation print from this suite is a claim about a minority of the pairs the walker itself found — and, as the sentence stood before `d9fcc7820`, about none of the sheets outside one directory, which is no longer the case.** It is not a statement that a stylesheet's colour pair was checked, and the suite says so of itself at `:33`-`:34`; nothing here proposes widening it, and this section still proposes no linter and no gate. · **LIVE SCOPE, measured by the lane that widened the walk, 2026-09-15 at `d9fcc7820`:** `composedRuleIdenticalPair.test.ts` now composes from `WALK_ROOT = path.resolve(process.cwd(), 'src')` (`:84`), walked at `:298` through `cssFiles(WALK_ROOT)`, and its own scope floor prints what it walked — `walk scope: 137 sheets = 105 under src/features + 32 shared sheets the features-only walk never opened` — while the violation case carries its denominator in its title: `no rule sets color and background to the same resolved colour (walked src: 461 pairs graded of 1058 composed across 0 violation)`. **Widening the DIRECTORY did not widen the SHAPES:** the resolver still refuses a named colour, `rgb()`/`rgba()`, a gradient, `currentColor`, `transparent` and any `var()` side carrying a fallback, so **597 of the 1,058 composed pairs remain ungraded**; the root-only fallback tails the literal sweeps left behind are inside the walk now, but only their hex-resolvable pairs are graded. Re-derive with `cd ui && npx vitest run src/__tests__/composedRuleIdenticalPair.test.ts --reporter=verbose`, which prints both the scope line and the graded-of-composed triple.

**The other four of the five carry the same shape of caveat, measured 2026-09-15 by a read-only review at HEAD `70f057db8` with the suites run as one scoped command.** The percentages are that lane's run properties — attributed, not asserted — while each mechanism is a line in the file, cited so it can be re-read without a run. `animationCompliance` grades 98 of the 321 animation declarations in `ui/src` (30.5 %): 30 excused by the curated `ESSENTIAL_KEYFRAMES` set (`:26`), 55 by a `none`/`auto` value, and 138 by a **file-wide** reduced-motion amnesty — `hasReduceBlock` at `:110` asks only whether a `prefers-reduced-motion: reduce` block appears anywhere in the sheet, so one block excuses every animation in that file, and 30 files carry one; the 505 `transition` declarations that reduced motion also governs are never read. `popupBackgroundCompliance` is the narrowest print in the set and the clearest case of a file count being read as a rule count: it reports 137 CSS files scanned while accepting **1 rule of the 6,071 top-level rules it parsed** — the toast rule in the shared `frontend/themes/components.css` — because its primary-class boundary at `:108` cannot match this repo's hyphenated names (443 popup-shaped selectors rejected, 266 of them declaring no background at all), and its extractor skips every at-block wholesale at `:69`-`:71`, so 818 further rules vanish before any check runs. Stated as a claim about one selector that is fair; softened, it would not be. `noiseDitherCompliance` grades 122 of 455 `box-shadow` declarations (26.8 %) and skips 333 for being hardcoded rather than tokenised — an inverted incentive, since un-tokenising a shadow escapes the gate, a consequence the file records against itself at `:224` — plus two files it refuses to open by basename (`:350`-`:351`: `tokens.css`, `components.css`) and a per-file `catch { /* skip */ }` at `:398` that lets an unparseable sheet pass with no counter, the same blackout class `composedRuleIdenticalPair` had before it was given a per-file floor. `themeTokenCompliance` is the only one of the five whose denominator is stated in the file rather than inferred, and its hole was named — it harvested 15,261 `var()` references and did not see the 163 sitting inside another token's fallback, where 25 inner names were recorded nowhere; exposure then was zero because all 25 happened to be declared. **CLOSED at `b65ced27a`, with two of those three figures corrected in the same commit:** 163 was never a nested-var count — it counted tails carrying ANY paren, `calc()` and `rgba()` included — and the shipped population is **13,906 outer `var()` refs in `ui/src/features` carrying 79 nested reads over 24 distinct inner names (85 sites and 25 names across all of `ui/src`)**, printed in that floor case's own title and floored at nested ≥ 60 (`:1159`), distinct inner names ≥ 15 (`:1160`) and nested(`ui/src`) ≥ nested(`features`), so a harvest that stops descending reads red rather than counting the outer reference and looking healthy. **What the fix proved, both directions:** planting `.p { color: var(--color-fg, var(--ghost-inner-token)) }` into the feature sources makes the post-fix file FAIL, naming `--ghost-inner-token (1 site) -> PLANT.css:1`, while the same plant at the same position in the pre-fix blob prints `Tests 20 passed (20)` — the hole was silent, not narrow — and both mutations were reverted byte-identically, leaving no plant residue in the tree. The invariant that kept everything else calibrated: `VAR_REFS` and `ALL_VAR_REFS` stay OUTER-only, so `TAILED_TOKEN_REFS` (218), the disagreeing worklist (83 pairs / 133 sites) and the foreign-scheme freeze (61/88) are still measured on exactly the lists they were built from, all three green without an edit. So the honest description of that suite is now a stated denominator with the hole in it closed, and the earlier description of it as a named-but-open hole is superseded. **And the sentence that ties the five together, RE-RUN 2026-09-15 at HEAD `7d8213f24` through the scoped five-file command named above, because the text below it described last night's code: every one of them prints a pass count, and the five now split into three tiers rather than two: three print what they graded against what they found, one prints what it read without saying what it skipped, and one still prints a file count where a denominator belongs: `composedRuleIdenticalPair` carries its graded-of-composed denominator in the violation case title and its scope in a floor case's print since `d9fcc7820`, and `themeTokenCompliance` prints its outer-plus-nested population in its floor title since `b65ced27a` — animationCompliance and noiseDitherCompliance joined them tonight, and the animation print is the widest in the set: "prints its own denominator: 142 of 316 animation declarations graded (44.9%) across 136 sheets" **(that is the print at the tip this paragraph names, `7d8213f24`, and it is superseded here rather than deleted: `e94a98b4f` took it to 141 of 315 by removing one animation rule from `ui/src/features/restaurant/RestaurantMenu.css`, so a GRADED declaration left the population through a guard-certified deletion and not through a parser change, and the percentage recomputes off the two figures being superseded rather than standing as a third fact to chase. The reason this clause is shaped as a citation and not as a rewrite: the paragraph went stale twice inside one hour — once at `ae1b1749b`, where the walker stopped counting declarations that need no keyframes, and once here, where a stylesheet rule was legitimately deleted — which is not the sentence being badly written but what happens whenever prose quotes a run instead of quoting a command. Re-derive with `cd ui && npx vitest run src/__tests__/animationCompliance.test.ts`, which prints the harvest line)**, its stdout harvest naming every bucket — 44 excused-by-essential, 56 excused-by-value, 74 swallowed by a file-wide reduce block of which "57 of those 74 would fail patterns A and C alone", 61 naming no @keyframes in their own sheet **(that is the figure at the tip the paragraph names, `7d8213f24`, and it is superseded here rather than deleted: since `ae1b1749b` the same walker prints 5, because the misses counter ran BEFORE the `none`/`auto` value excuse and so was counting declarations that name no keyframes by design — 56 of the 61 read literally `animation: none` or `animation: auto` (`grep -rnoE 'animation:[[:space:]]*(none|auto)' ui/src --include='*.css' | wc -l` → 56, 4 of them in `KdsScreen.css`), which makes the old print an upper bound with a known inflation and not a census of misses; re-derive with `cd ui && npx vitest run src/__tests__/animationCompliance.test.ts`, which prints the harvest line)**, and "504 transition declarations are never read" — and the dither walk answering with four numbers: "the shadow walk reports its own denominator: 122 rules graded of 433 box-shadow rules, 5914 rules examined, 0 unparseable sheets". Both quoted off that run. Every figure for these two suites in the paragraph above is DATED and kept verbatim per this page's convention rather than repeated here: animation's 98 of 321, its 30.5 %, its 30/55/138 excuse split and its 505 unread transitions all predate the walker edits that shipped as `074655fb0` and neighbours, and the dither line "122 of 455" is a different UNIT, not a shrinking population — 455 counted declarations, 433 counts rules. `themeTokenCompliance` is the half-print of the five: its floor case title says "reads a real population (never a vacuous green): 13901 outer refs + 79 nested reads carrying 24 inner names", which states what it READ and never what it skipped, so it belongs in neither the full-print nor the no-print group. And the set this section calls uniform is not uniform in SCOPE: animation walks 136 sheets while composed, popup and dither each report 137, because `animationCompliance.test.ts:15` drops every `.module.css` in its own `findCssFiles` — one real file, `ui/src/features/settings/WorkspaceSettingsModal.module.css`, that the other four open and this one never parses; re-derive with `find ui/src -name '*.css' | wc -l` = 137 and `find ui/src -name '*.css' ! -name '*.module.css' | wc -l` = 136, both measured at `7d8213f24`. Only `popupBackgroundCompliance` still prints a file count where a rule denominator belongs: "every popup container has an opaque background (137 CSS files scanned)", and its wholesale at-block skip at `:84`-`:86` (read at `7d8213f24`) stays uncounted even after `5effd09ed` gave it per-rule file-line reporting. **So the five print in three tiers, not two: graded-of-total (`composedRuleIdenticalPair` 461/1058, `animationCompliance` 142/316 **(141/315 since `e94a98b4f`, which removed one graded animation declaration from `ui/src/features/restaurant/RestaurantMenu.css` — a certified deletion, not a walker change, and `142/316` stands here only as the figure at `7d8213f24`; re-derive with `cd ui && npx vitest run src/__tests__/animationCompliance.test.ts`)**, `noiseDitherCompliance` 122/433), population-only (`themeTokenCompliance`), and file-count-only (`popupBackgroundCompliance`) — and a printed denominator is not a widened scope: animation still never reads `transition` and never reads a `.module.css`, popup still never reaches inside an at-block, and the dither walk's 311 ungraded rules (433 minus 122) are ungraded because a sheet escaped the gate by hardcoding its shadow, which that file records against itself at `:224`. "137 sheets" is not one denominator across the set, and the sentence above that calls the five uniform should be read with that exception.** A lane reading "passed the five walkers" as "the sheet was checked" is reading a wrong claim about ONE suite now rather than three — so read this section's earlier line, that each of the five grades a fixed set of shapes, together with the denominators above rather than in place of them. No test file was touched to write this and nothing here proposes changing one.

**Do not overread the CI half — CI is NOT blind to CSS.** `dev-ci.yml#ui-test` runs `cd ui && npm test` (`:277`), it is gated at `:251` on the `changes` router, and that router's UI rule at `:98` matches `^ui/` by DIRECTORY and not by extension — so a CSS-only commit does reach the walkers above, and `scripts/check.sh:211` runs the same suite locally. What is missing is a LINTER and a COMMIT-TIME gate, not all verification. Naming the gap and stopping there: nothing in this repo checks a stylesheet for syntax or token-vs-literal drift the way ESLint checks TSX, and closing that would be a workflow edit plus a `scripts/gates.json` row — an owner decision, not a docs edit, and this section proposes neither.

---

## 💻 Running CLI Tools on Windows

### 1. UI & Front-End CLI (TypeScript / ESLint / Vite)

> ⚠️ **MANDATORY RULE:** `tsc` and `eslint` are **project-local** in `ui/node_modules/.bin/` and are NOT on the system PATH.
> **Agents must ALWAYS run npm scripts from inside the `ui/` directory (`npm run <script>`).** Never invoke bare `tsc` or `eslint`.

| Task | Command (Run from `ui/`) | Description |
|---|---|---|
| **All UI Gates** | `npm run check:all` | Chained gate: lint → typecheck → test → i18n → E2E |
| **Type Check** | `npm run typecheck` | Runs project TypeScript type-checking |
| **Lint** | `npm run lint` | Runs ESLint (a11y + React rules) |
| **Lint Auto-Fix** | `npm run lint:fix` | Runs ESLint with auto-fix |
| **Unit Tests** | `npm run test` | Runs Vitest unit tests |
| **Build Bundle** | `npm run build` | Validates bundle compilation |
| **E2E Suite** | `npm run e2e` | Docker backend + Vite + Playwright + cleanup |
| **E2E UI Only** | `npm run e2e:ui` | Runs Playwright UI spec subset |

*If `node_modules` is missing, install cleanly:*
```powershell
cd ui
npm ci --no-audit --no-fund
```

### 2. Rust Backend CLI

- **Active Development Iteration:**
  - Quick compilation check: `cargo check -p <crate>`
  - Run specific test: `cargo test -p <crate> <test_name>`
  - 🛑 **Agents must NOT run `cargo clippy` or full workspace tests (`cargo test --workspace`) during routine iteration.**
- **Pre-Push / Final Verification Only:**
  - `cargo fmt --all`
  - `cargo clippy --all-targets --all-features -- -D warnings` (must resolve all warnings — **and you are the only thing that will**: no live workflow runs clippy, so CI neither blocks nor reports it; see *What CI actually runs*)
  - Full workspace tests prior to pushing

---

## 📐 Architecture & Coding Standards

### 1. Rust Standards
- Every public function, struct, enum, and trait must have a doc comment (`///`).
- **Module Documentation:** Every production `.rs` file must start with a short module-level doc comment (`//!`, 5–15 lines max: purpose, key types, main functions, invariants).
- Use `thiserror` for error types and `anyhow` for application-level error propagation.
- **Monetary Values:** Store as integer minor units (`i64`) using `Money`. Never use `f32`/`f64`.
- **Database Writes:** Must run inside a `rusqlite` transaction.

### 2. Test File Organization
- Keep production `.rs` files under 1,000 lines (preferably < 600 lines).
- **Never put unit tests inside production `.rs` files.**
  - Place unit tests in a sibling file named `*_tests.rs` (e.g. `sales.rs` → `sales_tests.rs`).
  - At the bottom of the production file, wire:
    ```rust
    #[cfg(test)]
    #[path = "sales_tests.rs"]
    mod tests;
    ```
  - Inside `sales_tests.rs`, start with `use super::*;`.
  - Integration tests belong in the top-level `tests/` directory (outside `src/`).

### 3. Tauri & UI Standards
- Tauri IPC commands live in `apps/desktop-client/src/commands/` or `apps/tablet-client/src/commands/` and are registered in their respective `lib.rs`.
- Front-end API calls must route through `ui/src/api/` (per-domain files). **Never call `invoke(...)` directly inside React components.**
- **Accessibility:** All React components must have ARIA labels and pass `eslint-plugin-jsx-a11y` checks.
- **Localization:** All user-visible strings must use `@fluent/react`. No hardcoded English strings in JSX.

### 4. Database & Hardware
- **HAL Drivers:** Hardware drivers must have a mock implementation in `crates/oz-hal/src/drivers/mock.rs`.
- **SQLite is the schema source of truth:** `crates/oz-core/migrations/*.sql` + the registry in `migrations.rs` (registry order is canonical). See [`docs/records/sqlite-pg-roles.md`](../docs/records/sqlite-pg-roles.md).
- **`init.pg.sql` is generated, never hand-edited:** after any migration change run `python3 scripts/generate-pg-migration.py` and re-stage `crates/oz-core/migrations/20260813_init.pg.sql`. Pre-commit step 5 fails on drift, and since **0.0.37** so does CI (`dev-ci.yml#static-gates` → `generate-pg-migration.py --check`). Between `23c96330` retiring `ci.yml` and 0.0.37 the local hook really was the only guard — and because the gate had no `gates.json` record, the drift checker could not report a gate it never saw.
- **PostgreSQL Drift:** When modifying Postgres schemas, run `bash scripts/reset-dev-pg.sh` to re-synchronize the shared dev container schema (`oz-pg-test-15432`).

---

## 🌿 Git & Commit Policy

### 1. Branch Policy
- **Never create new branches unless asked specifically by the user.** Always work directly on the currently active branch.
- **Never switch local branches unless explicitly asked by the user.**
- Branch naming convention (when explicitly requested): `feat/<name>`, `fix/<name>`, `docs/<name>`, `chore/<name>`, `test/<name>`, `refactor/<name>`.

### 2. Commit Format Requirement
Every commit message **MUST** strictly follow the conventional format:
```
<type>(<area>): <description>

[optional body explaining changes, rationale, or issue references]
```

- **`<type>`** must be one of:
  - `feat`: New feature or capability
  - `fix`: Bug fix
  - `docs`: Documentation updates
  - `chore`: Maintenance, dependencies, configs
  - `test`: Adding or modifying tests
  - `refactor`: Code refactoring without functional changes
  - `style`: Cosmetic changes that alter no runtime behaviour — `cargo fmt` wraps, CSS adjustments, copy edits. In established use (`130c7556`, `c3b7c72b`, `ad9c60e9`, `e0f2ca9b`, `04465711`, `2d517b55`, `7dde51c2`, `cfd0f183`); listed here so this mirror matches the set `.githooks/commit-msg` actually accepts.
  - `perf`: Performance improvements
  - `ci`: CI workflows, GitHub Actions, build scripts
  - `audit`: Code audit stamps and remediations
- **`<area>`**: Domain, crate, or component (e.g. `sales`, `admin`, `website`, `ci`, `core`, `desktop-client`, `ui`, `licensing`, `agents`).
- **`<description>`**: Imperative, concise summary of the change (e.g. `add gift card tender`, `resolve modal overflow`).

### 3. Commit Cadence & Push Rule
- **Always make a local commit after each major modification.** Whenever a logical task or feature step is completed and verified locally, commit it before moving on to the next task.
- **Never run `git push` without an explicit, direct order from the user.** Even after committing code or completing verification, always wait for the user to explicitly instruct you to push before executing any `git push` command.
- Never commit secrets, `.env` files, or SQLite database files (`*.db`, `*.sqlite`).
- **⚠️ Multiple agents commit to this branch concurrently. The ONLY permitted commit form is ONE line with an explicit pathspec — `git commit -m "<type>(<area>): <subject>" -- path/one path/two`.** Never bare `git commit`, never `git commit -a`, and never `git add` or `git stage` as a separate step first. A pathspec commit takes those paths from the *working tree* and — for already-tracked paths — needs no prior `add`; a new file cannot be introduced in that form, and the chain below is the only sanctioned exception. Nor can it collect anything another session staged. A whole-tree commit does the opposite: it consumes whatever *another* agent has staged and files it under your message; their 90-line rationale is then discarded while their 10 files silently join your commit. This happened at `3b10ea3a`, whose subject describes a pricing-card tweak and whose diff includes the restoration of the entire release pipeline. The victim's `git commit` returns `nothing to commit, working tree clean` — a success-looking message that actually means "someone else took your index".
  ```bash
  git commit -m "<type>(<area>): <subject>" -- path/one path/two   # one line, no prior add, only your files
  git show --stat                                                   # prove the file list is exactly yours
  git status --porcelain                                            # after committing: confirm leftovers are theirs
  ```
  If `git commit` reports nothing to commit when you expected it to, **do not conclude your work was lost** — check `git show --stat HEAD` first; your files may already be in someone else's commit, in which case record your rationale in the relevant doc (see R36-13). Review a commit by its **file list**, not its subject line: the offender's commit looks entirely normal at a glance.
- **⚠️ New files are the one case the no-staging rule needs its own form.** `git commit -m "..." -- new/file` fails with *"pathspec 'new/file' did not match any file(s) known to git"*, and `git commit --include -- new/file` fails the same way — both proved live here on 2026-09-13; do not re-derive them. The sanctioned exception is ONE shell line in which the `add` names exactly the new paths and is consumed at once by a pathspec commit:
  ```bash
  git add -- new/one new/two && git commit -m "<type>(<area>): <subject>" -- new/one new/two
  ```
  Chain rules: the `add` lists **only untracked new paths** (a `git add` of a *tracked* file with pending edits is precisely the forbidden staging); the commit pathspec is a superset of the add list; and it is one `&&` line, so the staged window is the milliseconds inside a single invocation rather than the minutes a separately-typed `add` leaves open. If the commit half fails — most often `index.lock` held by a concurrent agent, which happened twice on 2026-09-13 — **re-run the whole line, never the commit alone**: committing without its add fails loudly, and a retry that stages without committing leaves new content as live ammunition for the next agent. Verify with `git show --stat` that the `create mode` entries are exactly your files.
- **⚠️ In a shared checkout the index is a single shared object: never leave anything staged, even your own.** `git add` is not a harmless preparatory step here — the one sanctioned exception is the same-line new-file chain above. Whatever is staged when someone else commits is what *they* commit — a staged set that is harmless for you is live ammunition for the next agent — which is why the form above has no staging step of its own. Inspect the paths you are about to name, immediately before naming them: `git --no-optional-locks status --porcelain -- path/one path/two`. A pathspec commit records the *working-tree* copy of those paths, so if another session has its own uncommitted edits in the same file, your commit sweeps them in under your subject; if a path you want is dirty with content that is not yours, stop and say so rather than committing it. A docs-only commit is not exempt from this — it is the same mechanism on smaller files. None of this is mechanically enforceable: a pre-commit hook never sees the commit's command line, so it cannot tell a pathspec commit from a bare one — §3 is prose law, and the only backstop is the discipline itself.
- **⚠️ Never `git commit --amend` on a shared branch — not even with a pathspec, and not even after inspecting the index.** An amend re-commits the whole index, so it inherits whatever another agent staged in the seconds since you last looked. This bullet previously taught the opposite ("an amend needs an explicit pathspec too") and that is the same hazard wearing a pathspec: the incident it cited happened while *following* that advice — `git diff --cached --name-only` printed only my file, and four seconds later `git commit --amend -m ...` produced a 5-file commit containing someone else's `.circleci/config.yml`, `scripts/compose-circleci.py`, and two generated workflow files. The check was true when it ran and false by the time it mattered, because with concurrent agents the index is a racing value, so inspecting it before an amend proves nothing. <!-- dead-ref: ok -- `scripts/compose-circleci.py` is cited deliberately, as an artefact of the `3b10ea3a` incident this bullet describes: the script was added by `9deab2c16` and deleted by `e3aff7b56` when CircleCI was replaced by GitHub Actions, so there is no successor path and its absence is the expected state. Repointing it at a live file would keep the sentence resolving and destroy its meaning. -->
  **The recovery for a bad commit is a NEW commit that undoes it — not an amend, not a reset.** `git revert` with a pathspec where possible; when the undo needs to be staged first, the commit that records it is still one pathspec line (`git revert --no-commit <sha>`, then `git commit -m "fix(<area>): undo <sha>" -- path/one path/two`), and if the bad commit swept in another agent's files, undo only yours and tell them what landed where. A revert is additive: the original commit and its subject stay visible for the person who wrote them, and no branch tip moves under anyone. The `git reset --mixed HEAD~1` this bullet used to offer as the recovery is withdrawn for the same reason — it relocates a tip that other agents are committing to at the same moment.
- **⚠️ Never `git stash` to work around a dirty file another agent may own.** Stashing removes their uncommitted work from the working tree for the duration; if they write to that file while it is stashed, the `pop` conflicts or silently discards their edit. That is the same loss as a swept commit but with *no trace in any message*, so nothing later reveals it. To find out whether a failure is committed or only in flight, compare the versions directly instead — `git show HEAD:<path>` versus the file on disk — or check a `git worktree add` of HEAD, neither of which touches their tree. If you have already stashed, verify the round-trip before moving on: byte size, a content count, and an empty `git stash list`.
- **⚠️ Not every ` M` is content.** Under `text=auto` + `core.autocrlf=true` a file can show as modified while its working-tree bytes are identical to the HEAD blob — an EOL/stat artifact. Before "committing" it, compare `git hash-object <path>` with `git rev-parse :<path>`; if the hashes match there is nothing to commit, and the entry clears on its own or via `git update-index --refresh`. Four phantom entries cost one full review pass on 2026-09-13.

### 4. Plan-doc naming is a checker token, not just a convention
- **Eighteen `todo-*.md` live at the repo root** (`ls todo-*.md | wc -l` = 18, re-measured 2026-09-14; `git ls-tree --name-only HEAD` matches 16 root names containing `todo-` and `git ls-tree -r --name-only HEAD | grep -c 'todo-'` matches 53 — those 16 tracked at the root plus 37 `done-todo-*` now committed under `.agents/archived/`, while the on-disk 18 are the 16 plus the two plans authored 2026-09-14 that are still untracked), and those 18 occupy **five** states, not two: open · gate-met-but-acceptance-unrun · parked-on-a-human-ruling · superseded-by-another-plan · audit-with-no-boxes. A `todo-`/`done-` pair names ONE axis — *has acceptance run* — so it cannot encode the other four, and none of the four is `ls`-readable.
- **The rule in force:** `done-todo-*` is earned ONLY when that file's own acceptance command was RUN and PASSED. A plan whose `npm run check:all` (or its documented equivalent) was never executed STAYS `todo-`, however complete its code is. `todo-tools-agents-3.md` is the live case: sizing gate met, guard box ticked, `check:all` recorded as NOT run, so it is not renameable. The other reasons a plan is not accepted — parked, superseded, or an audit whose verdicts keep items open — belong in a **dated header line**, never in the filename: superseded is not done, and a 0-box audit is not done either (`todo-kds.md`: 3 lines, 0 boxes, superseded).
- **THE MACHINE FACT: a tool reads the name.** `is_historical_doc()` at `.agents/skills/docs-auditor/scripts/check-dead-refs.py:193` grants a plan doc its dead-reference exemption at `:213`, and since `cc382ef20` (2026-09-14, +14/-1 to that script) the test is a SUBSTRING — `any(k in name for k in ("todo-", "plan-", "prd-"))` — not `startswith`. The widening exists because status words are prefixed IN PLACE (`done-todo-x`, `parked-todo-x`) and `startswith` revoked the exemption for exactly that rename: a finished document started generating findings about itself. The script's own comment at `:201-212` records the accepted cost — over-exemption, since any name carrying the token anywhere is exempt — and warns not to tighten it back without re-deciding that trade-off. So the token is load-bearing even though its position no longer is.
- **Where renames happen: IN PLACE at the repo root.** Do not move plans into `.agents/archived/` as a substitute for naming them, for two measured reasons: that directory is **not** in `HIST_DIR_PREFIXES` (`:74-76`, which lists eight `docs/` trees — `decisions/`, `records/`, `archived/`, `specs/_done/`, `specs/_archive/`, `specs/_active/`, `releases/`, `plans/`), so a moved plan is exempt only while its name still carries the token; and moving it takes the file out of the root glob a triage pass reads. Recorded before the move landed, then superseded: `git ls-files .agents/archived` = 0 while their root copies read ` D` — one session's in-flight move, uncommitted. Measured 2026-09-14, that move is COMMITTED: `git ls-files .agents/archived | wc -l` = 37, untracked there = 0, and `git status --porcelain | grep -c '^ D done-todo'` = 0, so the root ` D` entries no longer exist. The rule survives its example: never rename or move another session's uncommitted file — the name is shared state, like the index — and a committed move is still not an approved one, because `.agents/archived/` is not in `HIST_DIR_PREFIXES` (`check-dead-refs.py:74-76`), so those 37 stay dead-ref-exempt only because `cc382ef20` made `is_historical_doc()` a substring test — the over-exemption is now load-bearing for 37 files.
- **Limitation, so this section is not over-read:** the five states are not `ls`-distinguishable and that is deliberate — triage reads the dated header line — and no tool enforces any of this prose in either direction. `scripts/verify-agents-mirrors.py` compares gate counts, step names, commit types, the version and workflow-job anchors; it never looks at a plan-doc name.

<!-- Amendment: 2026-09-14 · DSH · two FALSE CI claims, one stale line pointer and the checker's own scope corrected in LIVE prose; this mirror and root `AGENTS.md` were brought back into agreement with each other and with the tree. · WHY THEY WENT FALSE THE SAME MORNING: `e3aff7b56` (2026-09-14, "ci(actions): migrate from circleci to github actions and skip clippy on ci") removed `cargo-check`'s `Cargo clippy` step AND added `push: branches: [main]` to the `on:` block in ONE commit -- `git --no-optional-locks show e3aff7b56 -- .github/workflows/dev-ci.yml | grep -E '^[-+].*(clippy|push:)'` prints `-      - name: Cargo clippy`, `-        run: cargo clippy --all-targets --all-features -- -D warnings`, `+  push:`, `+    branches: [main]`. Both sentences had been true on 09-08 (the audit stamp above, and the 09-04 stamp in the `.agents` mirror, are point-in-time records and were NOT edited, per this file's convention: the stamp's "dev-ci triggers pull_request + workflow_dispatch and no push trigger" and "28 named steps" stand as written and are superseded here, not rewritten). · (1) TRIGGER, now stated with its source: `dev-ci.yml:3-8` declares `pull_request` + `push` + `workflow_dispatch` (`sed -n '3,8p' .github/workflows/dev-ci.yml`); a push to `main` runs CI and reaches `northflank-deploy` (`if:` at `:668`), a PR gates the merge, and a push to a non-`main` branch still runs nothing. · (2) CLIPPY: `grep -c clippy .github/workflows/dev-ci.yml .github/workflows/release.yml` = 0 and 0, so clippy is NOT a merge gate -- a PR or a push to `main` can land with warnings; it runs only in `scripts/check.sh:44` and `scripts/release.sh:61-62`. The pre-existing correct sentence that `cargo fmt --all -- --check` runs in pre-push / CI / `check.sh` / `release.sh` was left alone, because it is true (`dev-ci.yml:195`) and the hole is clippy-shaped, not fmt-shaped. · (3) COUNTS now carried in live prose with their commands and the date: 16 crates (`ls -d crates/*/ | wc -l`), 277 feature `.tsx`, 572 files under `ui/src/__tests__` (303 .tsx + 268 .ts + 1 .json) with NO Vitest case total quoted because none is measurable without running it, 8,252 `#[test]`, 54 `.ftl` / 9,825 definitions (4,875 en + 4,950 id) / 4,950 distinct IDs / 75 IDs with no English definition, IPC 453 desktop + 322 tablet registered / 297 both / 478 distinct. The ±1 against the first numbers written into this note was RESOLVED in the same pass, not left open: 9,825 / 4,950 is correct and the 9,824 / 4,949 first measured in this pass is an ARTIFACT -- `ui/src/locales/sales.ftl` has no final newline (last byte `}`; one of 54 files fails `tail -c1 | od -An -c`), so `cat *.ftl | grep -c` welds its last definition onto the next file's first line and loses one match, while per-file `grep -hc` is immune and sums to 9,825 = 4,875 en + 4,950 id. The mechanism first written here (that README's regex also counted dotted `key.attr =` lines) was itself false -- a dotted-key pattern matches 0 lines tree-wide -- and was deleted in the same pass, because an invented explanation beside a real number is the part a later reader trusts. · (4) WHAT THE GATE CANNOT SEE is now written down: `verify-agents-mirrors.py` compares version, hook step SECTIONS and their enumerated NAMES, accepted commit types, live workflow FILE names, workflow job names cited by anchor (the hash form, which resolves to a job that must exist), and per-step coverage claims -- it never looks at a job's steps, and although it DOES parse the `on:` block with `triggers_of()` (`scripts/verify-agents-mirrors.py:331-342`, a regex that also accepts the PyYAML `True:` key; called at `:833`), the only thing ever done with that value is the one-directional `says_push and not any_push` test at `:831-836`. An earlier draft of this note asserted the checker 'never reads an `on:` block'; that was an inference about `triggers_of()` which measurement refuted, and it is corrected here -- the rule is narrow and phrasing-bound, not absent. It exits 0 both before and after this edit, so its green is not evidence about items 1 and 2; that is the whole reason those two claims were able to live here. · (5) TWO MORE LIVE CLAIMS of the same class, found by measurement and corrected the same day in the paragraphs above. (a) `scripts/gates.json` does NOT file `rust-clippy` as retired: the row is `status: "required"`, `runners: {"check.sh": ["clippy"]}`, _note "Local-only / manual. Skipped in CI, verified locally in check.sh / pre-push"; across 70 rows the counts are 53 required / 16 retired / 1 advisory and that one is `a11y-advisory` -- so "required, CI never runs it" was the truth and "retired" was invented. Guard both words: `status` is policy, and `runners` is itself incomplete (`ci-docs-drift` lists only `check.sh` despite running in `dev-ci.yml`), so neither field may be quoted as if it were the workflow. (b) "`ci-docs-drift` is advisory" is FALSE now: the job ends in `[ "$status" = "PASS" ]` (`dev-ci.yml:407`, no `continue-on-error` on that job -- `:336` and the `:346` comment are other jobs) and gates.json's note states "the step is now blocking -- the stated condition"; only the missing `needs` edge survives, and `dev-ci.yml:662-664` still prints the stale word "advisory" in a code comment that is out of this fence and was reported, not edited. · (c) The trigger rule is also PHRASING-BOUND, so "the gate is green" was never evidence about item 1: the detector is an inline regex at `scripts/verify-agents-mirrors.py:831` (`grep -n PUSH_CLAIM_RE scripts/verify-agents-mirrors.py` returns nothing in this checkout, so cite the line, not a constant name) and it needs the literal verb pair `runs` + `on` near `CI`/`dev-ci` and `push` -- the corrected sentence in the mirrors scores 0 matches, measured by running that exact pattern over both files. `scripts/` was NOT touched: another worker (b9f79136) is fixing the checker in parallel. · (6) SUPERSEDED, same day, by that fix and its successors — the sentences above stay verbatim as history, per this file's convention. The trigger rule was rewritten in `c8abceaf6`: `push_claim_findings()` (def `scripts/verify-agents-mirrors.py:465`, called `:1014`) replaced the inline `says_push` block the old pointers `:830-836`/`:831`/`:833` named; it is bidirectional (denials `PUSH_DENIAL_RES` `:401` matched first at `:493-494`, then assertions `PUSH_ASSERTION_RES` `:385` at `:497`), per-workflow rather than OR-ed with a `triggers of <name>:` printout at `:1077`, guarded against false positives by `CI_SUBJECT_RE` `:377` + `CHECKER_PROSE_RE` `:423` (tested `:485`), `unquoted()`, and a non-`main` branch exemption at `:494`. Measured on the shipped script: `--self-test` -> 33 green = 26 CAUGHT + 7 CLEAN, 0 red, exit 0; live run exit 0. STILL TRUE: the checker never opens a job's `run:` lines, so "which tools a job actually runs" remains the one hole, and `grep -n PUSH_CLAIM_RE` still finds nothing. ALSO corrected the same day in live prose only: the jobs-list `awk` that died on `unterminated regexp` (now `:$`, prints ten); "its last step is `[ "$status" = "PASS" ]`" (that is the `Verify CI docs agree with gates.json and the workflows` step at `:381`->`:407`; the job's last step is `AGENTS.md mirror truthfulness` `:426-429`); "both belong to other jobs" (`:336` is `i18n`'s census step, job at `:301`; `:346` is this job's own stale header comment); "all four" hook comments (three cite `dev-ci.yml` -- `:164`, `:182`, `:203` -- and bundle-parity at `:103` names no file); "all seven steps have a CI backstop" (step 1's is `test-eol-guard.sh` at `dev-ci.yml:576-577`, a self-test; no workflow rejects a committed CRLF); and "differ by 42 lines" (42 is a line-count gap; `diff` reports 140 changed lines, 91 in the `.bak` only, 49 in the live file only). · (7) ITEM (4)'S "which measurement refuted" WAS ITSELF WRONG, and this item supersedes it (the (4) sentence stays verbatim above, per this file's convention). The researcher's inference -- that the old `triggers_of()` could not parse a block `on:` -- was RIGHT; the refutation was the error, because what got judged was a parser nobody had run. Reproduced on this tree: load `triggers_of()` from `git show c8abceaf6^:scripts/verify-agents-mirrors.py` and call it on the real files -> `dev-ci.yml -> []` and `release.yml -> []`; the same call on the shipped script -> `['pull_request','push','workflow_dispatch']` and `['push']`. So `any_push` was permanently False: the old one-directional rule could not fire on the false denial, and would have reported a FALSE POSITIVE against the then-TRUE sentence `CI runs on push`. A green gate was reading an empty ground truth, which is a worse state than a missing one. `c8abceaf6` repaired `triggers_of()` (its docstring names the defect: under `re.M` the trailing `\s*` can only land at the end of the `on:` line, so the group that must begin with `^` never matched) and added the dead-parser guard at `:1836-1843` so an empty parse reports WRONG instead of passing. The lesson is the one the count lines already apply: never cite a constant name you have not grepped, and never call a parser alive without running it on the real file. · (8) `813992350` (committed, +55/-5, read 2026-09-14) narrowed the checker-prose exemption to the claim span plus 48 characters and, in doing so, shifted EVERY pointer quoted in (6) and (7): `push_claim_findings()` `:465` -> `:496`, its call site `:1014` -> `:1064`, the dead-parser WRONG guard `:1836-1843` -> `:1883-1900` (now four branches, one firing when planting the assertion changes nothing). (6) and (7) stand verbatim as written and are superseded for line numbers only; the live paragraph carries the post-`813992350` numbers. Why this is recorded rather than quietly fixed: a paragraph describing a gate went stale inside the same hour because the gate itself was improved -- so any pointer into `scripts/` has to be re-checked after every edit to that file, which is why these numbers are dated and SHA-anchored instead of bare. The whole-slice pass that made (7)'s residue look narrower than it was is gone: `checker_prose_span()` (`:447`, `CHECKER_PROSE_WINDOW` `:444`, called `:549`) replaced it, so one word like `mirror` anywhere on a long markdown line no longer exempts every CI claim on it. -->

> last audited 04-09-26 by DSH
> live CI claims re-verified 14-09-26 (see the 2026-09-14 amendment above)
