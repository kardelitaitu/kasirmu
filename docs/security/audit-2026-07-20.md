# Dependency Audit — 2026-07-20

<!-- Audit stamp: 2026-09-09 . DSH . status: HISTORICAL-RECORD, annotated not rewritten .
Verified against branch 0.0.37: three of the four rows still describe the tree —
Cargo.lock:7827 unic-ucd-ident 0.9.0, Cargo.lock:7838 unic-ucd-version 0.9.0,
Cargo.lock:2309 glib 0.18.5 — and `cargo audit` still lists all three (18 allowed
warnings). Two claims are false today and are annotated, not deleted: there is no
`[patch.crates-io]` table in any Cargo.toml anywhere (spin resolves to 0.9.9 at
Cargo.lock:6575 by ordinary resolution), and no live workflow runs cargo-audit,
cargo-deny or npm audit — they exist only in the inert ci.yml.bak / security.yml.bak,
and scripts/gates.json records both owning gates as "retired". Body kept verbatim
because this is a dated snapshot; the Currency block below carries the corrections.
-->

## Rust — `cargo audit`

| Crate | Version | Issue | Severity | Status |
|-------|---------|-------|----------|--------|
| `unic-ucd-ident` | 0.9.0 | [RUSTSEC-2025-0100](https://rustsec.org/advisories/RUSTSEC-2025-0100) — Unmaintained | Low | Transitive dep; no fix available |
| `unic-ucd-version` | 0.9.0 | [RUSTSEC-2025-0098](https://rustsec.org/advisories/RUSTSEC-2025-0098) — Unmaintained | Low | Transitive dep; no fix available |
| `glib` | 0.18.5 | [RUSTSEC-2024-0429](https://rustsec.org/advisories/RUSTSEC-2024-0429) — Unsound `Iterator` impl | Medium | GTK/Tauri dep; upgrade when available |
| `spin` | 0.9.8 | Yanked | Low | Transitive dep; pin to 0.9.9 via `[patch]` |

### Mitigations

- **unic-*** crates: Unicode character property databases. Used indirectly through `idna` / `url`. No known exploits — unmaintained status means no security patches for future CVEs. Acceptable risk for now.
- **glib**: The unsound `Iterator` impl for `VariantStrIter` can cause UB. Only triggered when iterating `Variant` children — not used in OZ-POS code paths. Tauri 2.x will ship updated glib.
- **spin**: Yanked versions are not pulled by `cargo update`. Pinning via `[patch.crates-io]` in workspace `Cargo.toml` ensures 0.9.9+ is used.

### CI Gate

Added `cargo-audit` CI job (non-blocking) that runs on every PR. Known issues are documented here with rationale.

## UI — `npm audit`

| Level | Count |
|-------|-------|
| Critical | 0 |
| High | 0 |
| Moderate | 0 |
| Low | 0 |
| **Total** | **0** |

542 dependencies audited — all clean.

---

## Currency (re-checked 2026-09-09 against branch 0.0.37 — annotation only; the record above is unchanged)

- **The row list is a subset.** unic-ucd-ident 0.9.0 (`Cargo.lock:7827`), unic-ucd-version 0.9.0 (`Cargo.lock:7838`) and glib 0.18.5 (`Cargo.lock:2309`) are all still locked and still reported, but `cargo audit` today prints **18 allowed warnings**, not 4: the same family also flags unic-char-property (RUSTSEC-2025-0081), unic-char-range (RUSTSEC-2025-0075) and unic-common (RUSTSEC-2025-0080), and none of the gtk-rs GTK3 binding warnings (RUSTSEC-2024-0411…0420) appear here. Re-measure: `cargo audit`.
- **"Tauri 2.x will ship updated glib" did not happen.** Tauri is 2.11.3 (`Cargo.lock:6786`) and still pulls glib 0.18.5, so RUSTSEC-2024-0429 is open on the current stack rather than awaiting an upstream release.
- **The dependency edge is different.** In the current lock the only consumer of unic-ucd-ident is `urlpattern` 0.3.0 (`Cargo.lock:7916`); idna 1.1.0 pulls idna_adapter/smallvec/utf8_iter only (`Cargo.lock:2825-2829`). Re-measure: `grep -n unic-ucd-ident Cargo.lock`.
- **The stated `spin` mitigation does not exist.** No `[patch.crates-io]` — indeed no `[patch]` table at all — is present in the root or any member `Cargo.toml` (`grep -rn '\[patch' --include=Cargo.toml .` and `git log -S '[patch.crates-io]' -- Cargo.toml`, both empty). The wanted outcome did land: `Cargo.lock:6575` is spin 0.9.9 and the yanked-version warning is gone — via dependency resolution in `11ad1c30b` ("add deny.toml … update spin/event-listener"), not via a pin, so nothing recorded here is what holds it there. Re-measure: `grep -A1 'name = "spin"' Cargo.lock`.
- **The "CI Gate" sentence no longer describes reality — this is the load-bearing correction.** Nothing in live CI runs `cargo audit`: the only live workflows are `dev-ci.yml` and `release.yml`, and cargo-audit appears solely in the inert `.github/workflows/ci.yml.bak:825-830` and `:882-889` plus `.github/workflows/security.yml.bak:33`. `scripts/gates.json` states it plainly: gate `audit` → `"status": "retired"` ("Dependency audit ran in ci.yml#audit only. `npm ci --no-audit` in check.sh explicitly SUPPRESSES it."), gate `security-pr` → retired ("cargo audit and cargo deny appear in neither dev-ci.yml nor check.sh"). So no Rust advisory and no npm advisory is gated anywhere: `deny.toml` exists but nothing runs `cargo deny`, and `npm audit` also lives only in `ci.yml.bak`. Two config files still assert the opposite and are **flagged, not fixed** (out of scope for this document): `.cargo/audit.toml` claims the CI `audit` job "is REQUIRED on main pushes" and `security-pr` "is fail-closed", and `deny.toml` refers to "the weekly security.yml run" — that workflow is `.bak`. Re-measure: `grep -rn 'cargo audit\|cargo deny\|npm audit' .github/workflows scripts/check.sh`.
- **The UI table is superseded.** `cd ui && npm audit` on 2026-09-09 reports **1 high severity vulnerability** (browserslist `<=4.28.6`), where the table said Total 0, and the lock now carries 594 package entries where this note said 542 audited. Because the gate above is retired, that high finding is invisible to CI.

> last audited 09-09-26 by docs-auditor
