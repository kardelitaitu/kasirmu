# SAST Audit — 2026-07-20
<!-- Audit stamp: 2026-09-09 · DSH · status: HISTORICAL-RECORD, annotated not rewritten (1 finding from .agents/skills/docs-auditor/scripts/check-ci-claims.py) · Dated record by filename and by the dates inside it, so every original sentence stands verbatim; the flagged line carries a `ci-claim: ok` pragma pointing at the Currency note added under Recommendations, which holds the correction · Verified against the files: .github/workflows/ holds exactly two live *.yml (dev-ci.yml, release.yml) and eleven *.yml.bak; security.yml was renamed to .bak by 23c963303 on 2026-09-02 (R100 rename, live path has 12 earlier commits, so the file did exist live); cargo-audit is at security.yml.bak:19-34 and cargo-deny at :36-50, not the :20-26 this record cites for deny · Nothing re-declared fixed: gates.json still records audit / security-pr / docker-build-scan as retired with no ci block (scripts/gates.json:323-332), no live workflow runs trivy or an audit tool, and no live workflow declares a schedule, so recommendation 3 remains open · Re-checked as still true: RUSTFLAGS -D warnings in CI (.github/workflows/dev-ci.yml:10) · Left alone deliberately: the clippy/npm audit/trivy results tables are 2026-07-20 measurements of a tree four weeks older; re-running them is a code-audit task, not a doc repair, so no number in the tables above was restated. -->

## Clippy (Rust Static Analysis)

Run: `cargo clippy --workspace --all-targets --all-features -- -D warnings`

### Results

| Finding | Location | Severity | Status |
|---------|----------|----------|--------|
| 1 warning | `crates/oz-core/benches/cart_bench.rs` | Low | Known — benchmark-only, not production code |

**Verdict:** ✅ Clean. The sole warning is in a benchmark file and does not affect production code. All production code passes `-D warnings` with zero issues.

### Current Clippy Configuration

The following lints are enforced:
- `-D warnings` in CI (`env.RUSTFLAGS`)
- `cargo clippy -- -D warnings` in the clippy job

No lints are explicitly allowed at the workspace level beyond what individual modules require for domain-specific patterns.

### Security-Relevant Lints

The following clippy lints are relevant to security and are all clean:

| Lint | Description | Status |
|------|-------------|--------|
| `unwrap_used` | Bans `.unwrap()` in production code | 🟡 Not enforced (too restrictive for POS domain) |
| `expect_used` | Bans `.expect()` | 🟡 Not enforced |
| `panic` | Bans `panic!()` | 🟡 Enforced via code review |
| `integer_arithmetic` | Checks for overflow | ✅ No warnings |
| `cast_possible_truncation` | Dangerous integer casts | ✅ No warnings in production code |
| `as_conversions` | Bans `as` casts | 🟡 Not enforced (common in HAL drivers) |

### Unwrap/Expect Audit

A comprehensive audit of all `unwrap()`/`expect()` calls was performed in P32-1. Results:
- **191 `unwrap()` calls** in `apps/desktop-tauri/src/commands/` — all in `#[cfg(test)]` blocks
- **Production code** uses `?` propagation or explicit error handling
- **Startup `expect()`** calls are intentional (fail-fast for missing configuration)

### Dependency Scanning

| Tool | Findings | Status |
|------|----------|--------|
| `cargo audit` | 4 findings (transitive deps, documented) | ✅ Triaged |
| `npm audit` | 0 vulnerabilities (542 deps) | ✅ Clean |
| `trivy` (container) | ✅ CI job on `aquasecurity/trivy-action@v0.36.0` (bumped twice since this audit) | ✅ Complete |
| `cargo deny` | ✅ Implemented (`security.yml:20-26`) — was recommendation #2 | ✅ Complete |

### Recommendations

1. **Enforce `unwrap_used` for production paths**: Exclude test files via `#[allow(clippy::unwrap_used)]` annotations. This catches accidental unwraps in new production code.
2. **Add `cargo-deny`**: Run in CI to catch duplicate dependencies and license issues before they reach audit. ✅ DONE — implemented in `.github/workflows/security.yml:20-26`. <!-- ci-claim: ok: dated SAST record (2026-07-20), kept verbatim per docs-auditor rule E; see the Currency note below this list -->
3. **Weekly SAST scan**: Schedule a weekly Trivy + cargo-audit + cargo-deny scan that files GitHub issues automatically.

> **Currency (2026-09-09, docs-auditor — the text above is the 2026-07-20 record, unedited).**
> Recommendation 2 was true when written and its artifact is now unreachable: the
> security workflow it cites was live until `23c963303` renamed it to
> `.github/workflows/security.yml.bak` on 2026-09-02 (a pure rename —
> `git log --name-status -1 23c963303` shows `R100 security.yml -> security.yml.bak`
> — and 12 commits touch the live path before that), and GitHub never executes a
> `.bak` file. Its cargo-audit job reads `security.yml.bak:19-34` today and
> cargo-deny `:36-50`, so even the `:20-26` line pointer above only matches the
> retired file by luck.
>
> **What enforces any of this now: nothing.** `scripts/gates.json` records
> `security-pr` as `retired` with the note "cargo audit and cargo deny appear in
> neither dev-ci.yml nor check.sh" (`scripts/gates.json:329-332`), `audit` likewise
> retired, and `docker-build-scan` retired with "Trivy scan runs nowhere"
> (`scripts/gates.json:323-326`). Neither live workflow (`dev-ci.yml`, `release.yml`)
> contains a `trivy`, `cargo audit`, `cargo deny` or `npm audit` step —
> `git grep -in -E 'trivy|cargo audit|npm audit' -- .github/workflows/dev-ci.yml .github/workflows/release.yml`
> returns one hit, a comment at `.github/workflows/release.yml:18` saying the scans
> were dropped. And recommendation 3 cannot be met by editing a schedule: **no live
> workflow declares a `schedule:` trigger at all**, so "weekly" would need a new
> workflow file, which is why that recommendation is still open.
>
> What did survive: `-D warnings` for Rust is still set in CI
> (`.github/workflows/dev-ci.yml:10`, `env.RUSTFLAGS`) and clippy runs as a step of
> the `cargo-check` job — but only on a PR to `main`; `dev-ci.yml` has no push trigger.
> **How to re-measure this whole note:** list the live files with
> `Get-ChildItem .github/workflows`, then grep them for the tool names above.

---

> last audited 09-09-26 by docs-auditor
