# Orchestrator Agent 3: Test Relocation, Thin Shell & IPC Parity

<!-- Audit stamp: 2026-09-14 · DSH · status: ACCURATE-after-repair (every [x] re-confirmed landed; 21 corrections applied — 15 in rev 1, 6 in rev 2) · corrections applied: 21 · The worst ORIGINAL error was a stale pointer, not a count: Phase 3.4 sent the next worker to delete test excludes in `.circleci/workflows/06-cargo-nextest.yml:37`, a file **retired from this repository by `e3aff7b56`** (*ci(actions): migrate from circleci to github actions and skip clippy on ci*, 2026-09-14, an ancestor of HEAD) — the path is gone from both worktree and index (`git ls-files | grep -c circleci` = 0), so the box is unactionable as written and the live equivalents are `dev-ci.yml#cargo-nextest` plus `scripts/check.sh:106`. REV 2 (same day): rev 1 over-corrected that finding twice and both are retracted here — (a) it called the CircleCI file one that "never existed", when `git show e3aff7b56^:.circleci/workflows/06-cargo-nextest.yml` line 37 proves the command and its two `--exclude` flags were real until this morning, and (b) it wrote that the exclude existed "in one and only place" off a single-file grep of `check.sh`, when the repo-wide grep returns NINE live executable paths including `check.ps1`, `coverage.*`, `release.sh` and `release.yml`. Lesson for the next reader: `git ls-files` answers "is it tracked today", never "did it ever exist"; and a grep of one file is evidence about one file. -->

**Document:** `todo-refactor-oz-pos-app-agents-3.md`  
**Role:** Orchestrator Agent 3 (The Closer & Auditor)  
**Goal:** Relocate unit tests from `apps/desktop-client/src/commands/` into `crates/oz-bridge` to eliminate memory bloat during test compilation, consolidate `apps/desktop-client/src/lib.rs` into a thin Tauri router, share handlers with `apps/tablet-client`, and perform full matrix parity verification.

**Sibling Documents:**
- `done-todo-refactor-oz-pos-app-agents-1.md` (Agent 1 — Infrastructure, Daemons & CI Stabilizer) — **retired/closed work order**; cite by bare name, the file may sit at the root or under the archive directory depending on checkout state
- `done-todo-refactor-oz-pos-app-agents-2.md` (Agent 2 — Core Command Middleware & `oz-bridge`) — **retired/closed work order**; same convention

---

## 🔒 Coordination & Dependency Checking Rules

1. **No Direct Inter-Agent Communication:**
   - Communication happens strictly through the Git commit history.
2. **Commit Subject Convention:**
   - All commits made by Agent 3 MUST use:
     - `test(bridge): ...`
     - `refactor(shell): ...`
     - `refactor(tablet): ...`
     - `ci(desktop): ...`
3. **Owned Path Fence (Exclusive to Agent 3):**
   - `apps/desktop-client/src/commands/*_tests.rs` (Moving to `crates/oz-bridge/`)
   - `apps/desktop-client/src/lib.rs` (The `invoke_handler!` macro & shell builder)
   - `apps/desktop-client/Cargo.toml` (Dependency cleanup)
   - `apps/tablet-client/` (Adopting shared `oz-bridge` handlers)
4. **Git Commit Waiting Protocol:**
   - Before moving tests for a specific wave, check that Agent 2 has committed that wave using:
     ```powershell
     git log -n 50 --oneline --grep="feat(bridge): extract <wave-name>"
     ```
   - If the commit is not found, pause and wait for the commit to appear in the Git history.

---

## 📋 Task Checklist

### Phase 3.0: Baseline Audit & Invariant Protection

Establish baseline metrics before performing test relocations and shell consolidations.

- [x] Run `python scripts/verify-ipc-parity.py` and record exit code (must be 0). → **EXIT=0, "IPC parity: OK"**; re-run 2026-09-14 at HEAD `ec2edf258` (same exit 0): desktop 458 UI strings / 453 registered / 27 unregistered (27 allowlisted) · tablet 458/322/156 (157 allowlisted) · dev-mock 541 handlers, 16 allowlisted unanswerable · 24 allowlisted scoped orphans of which **2** are GATED DEAD SURFACE (`get_active_cart_scoped`, `list_active_carts_scoped`). The 2026-09-10 figures recorded here (452/448/27 · 534 handlers · 10 gated dead) are superseded.
- [x] Run `bash scripts/verify-scoped-coverage.sh` (must be PASS). → **EXIT=0, PASS: all registered commands covered** (Git-bash path, not WSL).
- [x] Record current lines of code in `apps/desktop-client/src/lib.rs` (Baseline: ~1,282 lines). → was **1,281 lines** at baseline; **1,349 today** (`wc -l < apps/desktop-client/src/lib.rs` at HEAD `ec2edf258`; a newline-split count reads 1,350 — convention, not error). `generate_handler!` begins at **lib.rs:843** (block ends :1338), and the list holds **453 command-path entries across 59 distinct module prefixes**, not 448/57. ⚠ The code carries the same rot: `lib.rs:21` still asserts "the 448-entry `tauri::generate_handler` list". That comment is outside this doc's fence — recorded here as a known follow-up for the shell owner. **Re-measured 2026-09-15 evening (HEAD `503591a10`, branch `0.0.39`): 1,351 lines, `generate_handler![` at `lib.rs:845`, closes at `:1340`, 453 entries across 59 module prefixes — every figure on this row except the total still holds.** Three repairs to that ⚠. **(a) The code rot is gone.** `lib.rs:21-23` now reads "the 453-entry `tauri::generate_handler` list (measured 2026-09-14) — re-measure it with `python scripts/verify-ipc-parity.py` instead of trusting the number here" (`98e8e29a8` *docs(desktop-client,core): correct two stale doc comments - 453 handler entries*, 2026-09-14, an ancestor of HEAD), and 453 verifies today (`info[desktop]: 458 UI command strings, 453 registered`, EXIT 0). The follow-up this row named is closed, and no code edit was needed to close it. **(b) The fence claim is ambiguous, not wrong** — rule 3 fences `apps/desktop-client/src/lib.rs` but qualifies it to "The `invoke_handler!` macro & shell builder", and a module-doc header is neither; moot, since the file needed nothing. **(c) This row's headline is retired as a metric** by "Headline metric restated (2026-09-15 evening)" at the foot of this file: Phase 3.2's Option A pinned this number by decision, so leaving it as the objective invites a lane to undo that ruling for a digit.
- [x] Record current total file count in `apps/desktop-client/src/commands/` (Baseline: ~123 files). → baseline read **137 files** = 123 top-level + 14 under `topology/`, **70 `*_tests.rs` / 31,803 lines** (largest then: topology_command_tests 2,071 · topology_stress 1,936 · kds 1,877). That census is obsolete — Phase 3.1 has since landed. Re-measured 2026-09-14 (`find <dir> -name '*.rs' | wc -l`; sizes `wc -l`): **70 top-level + 9 under `topology/` = 79 `.rs` files**, of which **10 `*_tests.rs` totalling 5,873 lines** (a newline-split count reads 5,883). Largest now `registration_gate_tests.rs` **2,057** and `topology/topology_command_tests.rs` **1,732**; `kds_tests.rs` and most of `topology/` no longer live desktop-side.
- [x] **Commit Baseline Record (Optional / Local note):** Keep record ready. → Kept as local note in `docs/archived/manager-2-journal.md` (Agent-3 section, D-A3; moved from root at archival), per "Optional" — no commit; measured at HEAD `38d08f115` (branch `0.0.37`), zero refactor-campaign commits existed at audit time; and `crates/oz-bridge` was absent. That last clause is **no longer true and that is the point** — the crate now exists: `find crates/oz-bridge/src -name '*.rs' | wc -l` = **135 files** (121 top-level + 14 under `topology/`), including `pos_tests.rs` 1,479 · `kds_tests.rs` 1,651 · `settings_tests.rs` 1,925 · `staff_tests.rs` 1,514 · `topology/topology_stress_tests.rs` 1,910 (`wc -l`). Baseline kept as history; the relocation it predicted is done.

> **Baseline record (Agent-3, 2026-09-10):** measurement commands and full output tails live in `docs/archived/manager-2-journal.md` § Objective: todo-refactor-oz-pos-app-agents-3.md / D-A3. Phase 3.1–3.4 are gated on Agent-2's `feat(bridge):` per-wave commits (wait protocol L28-33); battle plan accepted in journal (thin-shell Option A with recorded deviation, adjacent-test placement, per-wave slices A0–E5d).
>
> **Re-measurement method (2026-09-14, HEAD `ec2edf258`, branch `main`):** every count above was taken through Git-bash (`C:/Program Files/Git/bin/bash.exe`) with `wc -l`, `find | wc -l`, `grep -c` and `sort -u`; handler-list counts by stripping `//` comments then splitting on commas; and `python scripts/verify-ipc-parity.py` plus `bash scripts/verify-scoped-coverage.sh` were re-run to EXIT=0 rather than quoted from memory. Where another session's figure differs by exactly 1 line per file, the cause is `wc -l` (counts newlines) vs a split-on-newline count, not a disagreement about the code. Counts in this file are volatile by nature — parallel sessions register commands and add tests continuously; re-run the named command before repeating any number.

---

### Phase 3.1: Unit Test Relocation (Follows Agent 2's Waves)

Move command unit tests out of the Tauri application target (`apps/desktop-client`) into `crates/oz-bridge`. This removes the need for `rustc` to compile Tauri, WebKitGTK, and GUI dependencies when running unit tests.

> **COMPLETION RECORD (Agent-3, 2026-09-13): all five waves CLOSED.** The wait-gate greps below were written against a commit-subject convention Agent 2 never used (the real convention is `feat(bridge): extract X into oz-bridge` / `into oz_bridge::x`, 65 commits bearing that subject prefix (`git log --oneline --grep "^feat(bridge)" | wc -l` = 65; 66 if the match is not anchored) — none of the five literal subjects exists except a coincidental devmock-ops match). The relocations were tracked by the *test-side* convention instead: 43 × `test(bridge): relocate …` commits, the last being `8630d50b9` (topology command subset). **Permanent desktop residue by ruling** (deviation register, journal D-A3.84/85): `authz_tests.rs` (9/9 assert desktop `AppError::PermissionDenied` values from desktop-side gate fns — class-3 cross-crate), `edc_tests.rs` (FCR-3 option-c deferral), `local_api_command_tests.rs` (Agent-1 stay), plus `registration_gate_tests.rs` / `kds_lan_live_tests.rs` (shell-surface and live-hardware suites, out of 3.1 scope by construction). Residue conservation verified at close: 16+39=55 tests accounted for — **this arithmetic could not be reproduced at HEAD `ec2edf258` and is kept as an at-close observation, not a current measurement** (the six surviving desktop-side files now hold **45** test fns between them: `authz_tests` 5 `#[test]` + 4 `#[tokio::test]` · `edc_tests` 5+3 · `local_api_command_tests` 0+9 · `registration_gate_tests` 12+0 · `kds_lan_live_tests` 0+5 · `settings_tests` 2+0; method `grep -cE '^\s*#\[(test\]|tokio::test'` — deliberately **no closing bracket**: `kds_lan_live_tests.rs` writes `#[tokio::test(flavor = "multi_thread", worker_threads = 2)]`, so a bracketed pattern returns 0 for its 5 tests and undercounts the set by 5). Also: single git identity for all window commits; attribution by subject-plan, never `%an`.
>
> **Re-measured 2026-09-14 — three register corrections.** (1) The register names 5 files but **6** survive: `settings_tests.rs` (163 ln, 2 `#[test]`, added by `ab7cd72d5` *test(settings): pin the read door refusing stripe.api_key in both shells*, 2026-09-13) was never listed. It is a **6th permanent residue file, not stragglers from a wave** — it pins the desktop shim's read door (and asserts `get_setting` reaches `oz_bridge::settings::run_get_setting`), which is shell-surface work exactly like `registration_gate_tests.rs`. (2) `authz_tests.rs`'s "9/9" is **5 `#[test]` + 4 `#[tokio::test]`** fns — the count 9 holds, but there are no `rstest`/`#[case]` markers on disk; `grep -c '^#\[test\]'` alone returns 5. (3) The commit-count folklore: "66+ commits" is 65 subjects beginning `feat(bridge):` / 66 matching anywhere in the subject (`git log --oneline --grep "^feat(bridge)" | wc -l` = 65); **"43 × `test(bridge): relocate …`" reproduces exactly** (`git log --oneline --grep "test(bridge): relocate" | wc -l` = 43, newest = `8630d50b9`). Every short SHA cited in this file resolves as a commit at HEAD `ec2edf258` (`git cat-file -e <sha>^{commit}`), so they are left untouched. Token census, stated because it moves as the doc cites evidence: the pre-audit text carried 58 distinct 9-10-hex tokens (all 58 resolve), this revision carries 60 — the delta is citations added by the audit itself, not new history.

- [x] **Wave A Tests (Catalog & Tax):**
  - [x] *Wait Gate:* the doc's literal grep is dead — the real triggers are `feat(bridge): extract product read commands into oz_bridge::products` (`7741fddddc`), product write (`91d0c988af`), product-variant (`71babb1401`), product-image (`adeea02c30`), tax-rate (`54b158dd21`), and the topology batches (Wave E see below).
  - [x] Move `categories_tests.rs`, `products_tests.rs`, `tax_tests.rs`, etc. → landed as `3f31d64a04` (category and product), `73dfbb16ed` (tax), `3013c49f77` (currency, fiscal, product-variant).
  - [x] Imports updated to `oz_bridge::*` (each relocate commit carries its own import repair).
  - [x] `cargo test -p oz-bridge -- catalog tax` green at close (full `cargo test -p oz-bridge` green before 3.2).
  - [x] **Commit Milestone:** subject-shape honored across the wave: `test(bridge): relocate category and product unit tests to oz-bridge` (3f31d64a04) et al.

- [x] **Wave B Tests (CRM & Staff):**
  - [x] *Wait Gate:* real triggers `6c9a796877` (customer), `120d9c9cc5` (loyalty), `94e55f73a2` (staff), `3a1e0d038c`/`84e6ec1909` (auth).
  - [x] Move `customers_tests.rs`, `loyalty_tests.rs`, `auth_tests.rs`, `staff_tests.rs` → `7df15b94d3`, `c2b10abb6a`, `731732aeed`, `ea448eda24`.
  - [x] `cargo test -p oz-bridge -- crm staff auth` green at close.
  - [x] **Commit Milestone:** `7df15b94d3` / `ea448eda24` et al.

- [x] **Wave C Tests (Inventory & Purchasing):**
  - [x] *Wait Gate:* real triggers `5b6bd0d462` (inventory), `147fd3b226` (purchasing), `7d422b2fd4` (inventory-count), `90a44bd155` (stock-transfer).
  - [x] Move `inventory_tests.rs`, `stock_transfers_tests.rs`, `purchasing_tests.rs` → `348fa2c948`, `f026a35606`, `8563f76208` (plus payables in the same slice).
  - [x] `cargo test -p oz-bridge -- inventory purchasing` green at close.
  - [x] **Commit Milestone:** `348fa2c948` / `8563f76208`.

- [x] **Wave D Tests (POS, Shifts & Peripherals):**
  - [x] *Wait Gate:* real triggers `276b4027aa` (point-of-sale checkout), `ec72ad22f3` (kds), `cab58764cc` (hardware), `1a0277548f` (edc and scale), `02c3f0c98b` (shift, refund, void).
  - [x] Move `pos_tests.rs`, `kds_tests.rs` (64 KB test file!), `hardware_tests.rs` → `0fbd7f01a9` (pos), `119ec84f4c` (kds), `e4498ae745` (hardware and scale), `eefa576d1c` (shift, refund, void).
  - [x] `cargo test -p oz-bridge -- pos kds hardware` green at close.
  - [x] **Commit Milestone:** `0fbd7f01a9` / `119ec84f4c` / `e4498ae745`.

- [x] **Wave E Tests (Enterprise & Settings):**
  - [x] *Wait Gate:* real triggers `a7485e17a2` (workspace), `b51ba70f95` (location), `6a9bc8cddc`+`636b0c942a` (settings), `cd80b9a115` (license), `f837f64531` (report), `f2557c6d2f` (audit), `cb53a79ebf` (setup and analytics), `0f2ee7ad7b` (subscription).
  - [x] Move `workspaces_tests.rs`, `locations_tests.rs`, `settings_tests.rs`, `license_tests.rs` → `08b079771f`, `817eb8120d`, `1f7552878f`, `7751cdb468` (plus `bc5e3f1ff8` report, `1e969ffe80` audit, `851e48ad5e` subscription, `46a8c32263` setup, and the topology nine `982afd319f…8630d50b9` closing set).
  - [x] `cargo test -p oz-bridge` green at close.
  - [x] **Commit Milestone:** `08b079771f` et al.; wave fully closed at `8630d50b9`.

---

### Phase 3.2: Consolidate `apps/desktop-client/src/lib.rs` into a Thin Shell

Once all commands delegate through `oz-bridge`, replace the monolithic 120-command list in `lib.rs` with clean domain router registrations or unified bridge handlers.

> **COMPLETION RECORD (Agent-3, 2026-09-13): CLOSED at `6cdd7d3b89` (2026-09-11). RECORDED DEVIATION — Option A:** the `oz_bridge::tauri_handler()` replacement from the pre-record plan text landed as a deliberate NO-OP. (The old cross-reference "named at L110-114" is now stale — those lines are Phase 3.3 prose; no `tauri_handler()` step survives in this file outside this sentence.) The **453**-entry `tauri::generate_handler!` list STAYS (measured 2026-09-14; `lib.rs:843-1338`): the three verification gates (IPC parity `verify-ipc-parity.py` first-`]` parse, `verify-scoped-coverage.sh`, and the registration-gate ratchet) all read that macro verbatim, so collapsing it breaks three green gates for zero measured benefit (the shell is already thin — every body lives in `oz_bridge::`). The wave's real substance was the DEP PRUNE: `axum`, `lettre`, `snow` removed from `apps/desktop-client/Cargo.toml` (0 refs each in desktop src); `rusqlite` stays (53 refs across 24 files — `grep -rn rusqlite apps/desktop-client/src --include=*.rs`; the baseline said 48/24, so the file count held and the ref count moved). Option D (rewriting the gate scripts to parse a new shape) stays PARKED for the gate owner. Gates at close: parity 452/448/27, scoped-coverage PASS, `cargo check -p oz-pos-app` green. Re-run 2026-09-14: parity **458/453/27** EXIT=0, scoped-coverage still `PASS: all registered commands covered` (EXIT=0).

- [x] *Wait Gate:* Agent 2's Wave E complete (all real-wave `feat(bridge): extract` subjects present, see 3.1 record) and Agent 1's daemon extractions landed (local_api/pg_sync daemons stay in-shell by design — see lib.rs:20 module note).
- [x] In `apps/desktop-client/src/lib.rs`: the handler-block replacement is the recorded Option-A no-op above; the module-level doc (`lib.rs:20`) carries the shell/daemon-residue note instead.
- [x] In `apps/desktop-client/Cargo.toml`: `axum`, `lettre`, `snow` removed (`6cdd7d3b89`, −3 dep lines + lock refresh).
- [x] Frontend contracts verified: `python scripts/verify-ipc-parity.py` → EXIT=0 (452/448/27 at close; **458/453/27** re-measured 2026-09-14, still EXIT=0); `bash scripts/verify-scoped-coverage.sh` → PASS (re-run 2026-09-14: PASS, EXIT=0).
- [x] `cargo check -p oz-pos-app` green at close.
- [x] **Commit Milestone:** `refactor(shell): consolidate desktop invoke_handler into thin oz-bridge router` = `6cdd7d3b89` (subject verbatim; pathspec was lib.rs + Cargo.toml + Cargo.lock).

---

### Phase 3.3: Tablet Client Command Sharing (`apps/tablet-client`)

Eliminate code duplication between desktop and tablet shells.

> **PROGRESS RECORD (Agent-3, 2026-09-13): T1 contract slice LANDED at `eb4fdd07c1` (health repoint + `From<BridgeError>` seam + oz-bridge dep). T2 LANDED at `7a7c1c3dc2` (void/browser/scale wire contracts shared — closed a tablet-only wire bug: the UI sends camelCase `args: { saleId, reason }`; the old local `VoidSaleScopedArgs` was snake_case-only). T3 LANDED at `e821c2814b` (regional+local_payment DTOs shared — closed a **three-shell wire bug** dating to slice 6: `LocalPaymentRailArgs` declared `rename_all = "camelCase"` on desktop, bridge, and tablet while the only UI caller has always sent snake_case, so the settings card's save never worked on a real backend; fixed in the shared bridge DTO with back-compat aliases + the first wire-deserialization tests). The phase remains OPEN — later slices queued (the bigs: pos/staff/auth/products/settings, **wire-audit first**: the shared-DTO pattern is 2-for-2 on hidden casing bugs). T2's other findings: the tablet `AppState` → `BridgeCtx` seam was measured field-by-field and deferred with evidence (7/14 fields match; Arc-ing `db`/`terminal_id` is mechanical; `plugins` would drag the mlua Lua VM into the Android APK for a forever-`None` field — needs an Agent-2-fence decision); the T1 `Cargo.lock` deviation resolved (the tablet `oz-bridge` lock row landed with T2). Phase-closing note: the sync-conflict parity repair (`ded4686776`) was performed under this phase's fence before T1 — it ports desktop's list/resolve sync-conflict commands into the tablet shell (in-shell, gated `SYNC_MANAGE` on both) and repaired both registration-gate ratchets.

- [x] Add `oz-bridge` as a dependency in `apps/tablet-client/Cargo.toml`. → `eb4fdd07c1` (workspace alias existed; root Cargo.toml untouched).
- [x] Replace duplicated commands in `apps/tablet-client/src/commands/` with `oz-bridge` handlers. → T1: `health.rs` repointed (ping/version/get_device_id/get_local_ip forward to `oz_bridge::health`; identity constants still resolve in the tablet shim so About answers "oz-pos-tablet"); `authz.rs` gained the `From<BridgeError> for AppError` seam. Remaining domains queued (see record above).
- [x] Run `cargo check -p oz-pos-tablet`. → CLEAN, 0 warnings at `eb4fdd07c1`.
- [x] Verify `python scripts/verify-ipc-parity.py` against tablet registrations. → tablet-side info line green. **Resolved tablet figure: 322 registered command paths**, block `apps/tablet-client/src/lib.rs:445-784` in a 796-line file (`wc -l`), 156 unregistered names / 157 allowlisted / 0 unregistered tauri fns. This replaces the two conflicting numbers previously in this file (L43's "tablet … 318" and this box's "320 registered"), and a third figure circulating in another session — **338 entries — could not be reproduced at HEAD `ec2edf258`**: four independent counts (comma-token split, trailing-comma lines, comment-stripped tokens, whole-file `commands::` token grep) all return 322, as does the gate itself. Method note for the next reader: ask the question you are answering — *distinct registered `commands::<mod>::<fn>` paths* = 322 here, and there is no duplicate-entry variant to compare it against (`uniq -d` on the block is empty); a *lines-in-block* count reads 324. Cross-shell caution: `settings` registers **28** commands on tablet vs **19** on desktop, so never quote one settings count for both shells. The 2 desktop-side violations observed at T1 verify-time (`get/save_kds_routing_rules_scoped`) are now registered at `apps/desktop-client/src/lib.rs:1032-1033` — closed history, not this phase's debt.
- [x] **Commit Milestone:**
  ```bash
  git commit -m "refactor(tablet): reuse oz-bridge command handlers in tablet-client" apps/tablet-client/
  ```
  → `eb4fdd07c1` (subject verbatim; pathspec narrowed to the 3 files changed — `apps/tablet-client/Cargo.toml`, `commands/health.rs`, `commands/authz.rs`).

> ### T4 LANDED 2026-09-15 (evening) — and the wire audit is now 3-for-3 on real defects
>
> **Two commits.** `f32fac445` *fix(bridge): give CreditSaleDto the camelCase wire the retail credit list reads* (bridge struct + its first-ever serialization test) and `747791993` *refactor(tablet): share the settings DTOs and their run_\* twins with oz-bridge* (−173/+302 across `apps/tablet-client/src/commands/settings.rs` and its tests). Seven DTOs — `ReceiptSettingsDto`, `StoreSettingsDto`, `CreditSettingsDto`, `CreditSaleDto`, `UserPrefEntry`, `GatewayStatusEntry`, `DeploymentInfo` — were **byte-identical second definitions** in the tablet shell, each with its own `rename_all` copy; they are now `pub use oz_bridge::settings::{…}`, and the four duplicated `run_get/set_receipt/store_settings` bodies forward to the bridge's, which the sweep confirms are statement-for-statement identical (bridge `settings.rs:991-1012` vs the deleted tablet `:110-131`). `DeploymentInfo` keeps a tablet-local builder on purpose: `CARGO_PKG_VERSION` expands against the crate that writes it, so a tablet reports the tablet build (T1's identity-constant decision).
>
> **Verification, re-run after the last edit.** `cargo test -p oz-pos-tablet --lib` → **654 passed / 0 failed**; `cargo test -p oz-bridge --lib` → **1,315 passed / 0 failed**; `cargo check -p oz-pos-app --all-targets` → exit 0; `cargo clippy -p oz-pos-tablet --all-targets -- -D warnings` → exit 0; `python scripts/verify-ipc-parity.py` → `IPC parity: OK`, exit 0; `bash scripts/verify-scoped-coverage.sh` → PASS, exit 0. Seven new tablet pins plus one bridge pin assert each DTO's key set against `ui/src/api/settings.ts` rather than against a fixture transcribed from the DTO — that is the whole method, and it is what made "this refactor is wire-neutral" checkable instead of asserted.
>
> **The ratchet has an unsound heuristic, found by this slice's own red run.** Delegating `list_credit_sales_scoped` to the byte-identical `oz_bridge::settings::run_list_credit_sales` turned `drift_pin_three_way_partition_is_complete_and_sums` (`apps/tablet-client/src/commands/registration_gate_tests.rs:561`) red: the command moved from "on the ledger, ungated" to "gated", because the sweep reads a call into `oz_bridge::` as evidence that a shared funnel owns the RBAC. It does not here — `run_list_credit_sales` is a pure query helper with no permission check, and nothing about the command's authorization changed. **The delegation was reverted** (both credit-sale bodies keep their inline SQL, with the reason written on the function) rather than regenerating the ledger, because erasing real debt from a ratchet to satisfy a green is the failure mode the ratchet exists to prevent. Anyone widening that sweep should read this first.
>
> **Findings, each marked with how far it was verified.**
>
> 1. **VERIFIED as a static divergence; its consequence is NOT yet settled — six tablet scoped setters require an argument the UI never sends.** `set_receipt_settings_scoped`, `set_store_settings_scoped`, `set_credit_settings_scoped`, `settle_credit_scoped`, `set_hardware_settings_scoped`, `set_setting_scoped` all declare `user_id: String` (measured at HEAD `747791993`, lines 555/585/619/654/697/735 of the tablet file), while the single shared wrappers send `{ sessionToken, args }` and nothing else (`ui/src/api/settings.ts:30`, `:50`, `:90`, `:143`, `:247`) — a shape `ui/src/__tests__/api-settings-contract.test.ts:34` actively pins. Desktop's equivalents take no `user_id`; the bridge derives it from the session (`apps/desktop-client/src/commands/settings.rs:57`, `:95`, `:121`, `:196`, `:291`). A missing non-Option command arg is a hard Tauri rejection, so either these six fail on tablet today — with the rejection counted at `ui/src/features/settings/hooks/useSettingsSave.ts:264-272`, which is a toast, not silence — or the tablet never reaches the screens that call them. **That fork needs execution, not more reading**: a settings surface that cannot save would have been noticed, so the second leg deserves a real look before anyone rewrites six signatures.
> 2. **VERIFIED — a settings card silently resets a stored value it never displays.** `ui/src/features/settings/workspace-cards/WorkspaceRestaurantPosSettings.tsx:100-111` builds a `set_receipt_settings_scoped` payload carrying **ten** of the eleven receipt keys — `taxRoundingMode` omitted — serde's `#[serde(default = "default_tax_rounding_mode")]` turns that absence into `"half_up"`, and `crates/oz-bridge/src/settings.rs:1029` writes it **unconditionally** inside the transaction. A merchant who set rounding to `truncate` and then saved anything from the restaurant POS card gets `half_up` back, with no error and no visible field. Repair shape: `Option<String>` plus a conditional write, in the bridge. Note `saveDiff.ts`'s read-back merge hides the same class on the general Settings page — which is why this card, which bypasses that merge, is the one that shows it.
> 3. **VERIFIED — the hardware surface is two different features.** `oz_bridge::settings::HardwareSettingsDto` carries **15** keys (`:236-277`); the tablet's local copy carries **5**, and both of its bodies read and write only those five — through a different store, too: this shell goes to the `settings` KV table via `Settings::get_printer_*`, the bridge goes to `hardware_profiles` via `TerminalProfile` and needs `base_dir`. The renderer declares all 15 (`ui/src/api/settings.ts:103-119`) and builds a 15-key payload (`ui/src/features/settings/hooks/useTerminalHardware.ts:116-125`). So on tablet ten keys are missing from every read, and on the write path ten are accepted-and-dropped by serde (no `deny_unknown_fields`). This is why `HardwareSettingsDto` is the one DTO the slice deliberately did **not** re-export: the swap would move the failure from a visible absence to a silent drop without adding a single write. The repair needs the `AppState` → `BridgeCtx` seam T2 deferred (7/14 fields match; `plugins` would drag mlua into the APK) **and** a decision about which table is canonical.
> 4. **VERIFIED — `set_settings_scoped` is registered on desktop and absent from tablet**, with three live UI callers (`WorkspaceRestaurantPosSettings.tsx:114`, `WorkspaceKdsSettings.tsx:136`, `WorkspaceInventorySettings.tsx:97`); counted at `747791993` by grepping both `generate_handler!` blocks — desktop 1, tablet 0 — and it sits in `scripts/ipc-parity-allowlist.json`. Those three cards cannot write on tablet.
> 5. **VERIFIED — `get_user_preferences` / `set_user_preferences` are defined in the tablet shell and registered in neither shell** (0 hits in each `generate_handler!` block); the scoped twins are registered in both. Dead command surface, and part of the 496-definition-sites vs 453-registered gap the metric section records.
> 6. **VERIFIED — the bridge lint surface is already red at HEAD, independently of this slice.** `cargo clippy -p oz-bridge --all-targets -- -D warnings` fails on `crates/oz-bridge/src/auth_tests.rs:6` (`empty_line_after_doc_comments`) and `crates/oz-bridge/src/testing.rs:649` (`assertions_on_constants`), both present at `HEAD` in files this pass never touched (confirmed with `git show HEAD:<path>`; `cargo clippy -p oz-bridge --lib`, which excludes `cfg(test)` modules, is exit 0). `scripts/release.sh:62` runs clippy over the workspace excluding only the two app crates, so **the release clippy gate cannot pass today**, and neither can `scripts/check.sh:44`. That bears directly on the exclude-strip row's blocker 2: stripping `--exclude` would not *newly* expose a lint surface — the bridge is already in scope and already red.
> 7. **ATTRIBUTED, not re-verified here** (a read-only subagent's sweep, plausible, partly corroborated): the eight unscoped settings commands plus `list_credit_sales` / `settle_credit` have no UI wrapper at all — only the dev-mock (`ui/src/dev-mock/handlers/settings.ts:55-83`) and test mocks answer those names; `get/set_credit_settings_scoped` have wrappers but no component caller; `useSettingsSave.ts:289` swallows the sync-mirror write in an empty catch.

- [x] **T4-1 · Settle the six-signature fork in finding 1 by execution, then fix it one way or the other.** Either a tablet build reaches those cards — in which case five settings saves fail today and the fix is to drop `user_id` from the six tablet signatures and take `session.user_id`, which *strengthens* the gate (a caller can no longer name the user) — or the tablet routes never mount them, which makes them six dead registrations and a different cleanup. Reproduce before editing. <!-- IMPLEMENTED 2026-09-15, commit 84aed0b86 "fix(tablet): derive the session user in the six scoped settings setters". THE FORK IS SETTLED, AND ON THE FIRST BRANCH: the tablet DOES reach those cards. The decisive measurement is TabletAppShell.tsx:71-91, which registers an F10 handler and comments "parity with the desktop shell" -- it did not exist when the earlier note recorded the tablet as having no F10 route -- and :125, which renders the same WorkspaceSettingsModal AppShell.tsx:403 renders, with workspaceType derived from activeWorkspace at :122. WorkspaceSettingsModal.tsx:57/:63 mount WorkspaceRestaurantPosSettings / WorkspaceStorePosSettings for restaurant-pos / store-pos, and both call the scoped setters with `{ sessionToken, args }` only (ui/src/api/settings.ts:30, :50). So the six required a `user_id` no caller could supply, and the saves failed. FIX AS PREDICTED: `user_id` dropped from all six; `require_permission_for_session(&state, &session, …)` replaces `require_permission_for_user(&store, &user_id, …)`. That is strictly stronger, not merely equivalent -- it is scope-aware (ADR #35 D5, authz.rs:99-110 evaluates the session's branch and type_key) AND the caller can no longer name the user. Evidence: `cargo check -p oz-pos-tablet --all-targets` exit 0; `cargo test -p oz-pos-tablet --lib` -> 654 passed / 0 failed, the same total the T4 record left; `python scripts/verify-ipc-parity.py` -> "IPC parity: OK" exit 0; `bash scripts/verify-scoped-coverage.sh` -> PASS exit 0. No test called the six before or after (`grep -n` over apps/tablet-client/src/commands/settings_tests.rs -> 0 hits), so this change is covered by the gates above rather than by a new case; the denial path is now pinned for the batch sibling added under T4-4. -->
- [x] **T4-2 · Stop the restaurant POS card resetting `taxRoundingMode` (finding 2).** Needs an `Option<String>` plus a conditional write in `crates/oz-bridge/src/settings.rs` — an `oz-bridge` edit, so it also answers the fence question the T3 record already begs (`fix(bridge):` landed in `crates/oz-bridge/src/local_payment.rs` under this phase). <!-- IMPLEMENTED 2026-09-15, commit 67cdf9b6d "fix(bridge): stop set_receipt_settings resetting an omitted taxRoundingMode". WRITTEN RED FIRST, and the red is the finding verbatim: a new case `set_receipt_settings_without_tax_rounding_mode_leaves_the_stored_mode_alone` deserializes the restaurant card's exact ten-key payload over a stored `truncate` and, before the fix, panicked with `left: "half_up" / right: "truncate"` -- the silent reset, reproduced. Fix shape is the one this row prescribed: `tax_rounding_mode: Option<String>`, the `#[serde(default = "default_tax_rounding_mode")]` attribute and its now-unused pub fn removed, the read path answering `Some(..)`, and the write guarded by `if let Some(mode)`. Absent now means "this caller does not speak to this key"; a value that IS present still goes through the validating setter, so an unknown mode is still refused rather than written. Verified empirically rather than assumed: a missing `Option` field deserializes to `None` with no `#[serde(default)]` needed -- the red test proves it, since it passes after the fix with the key absent. Gates: `cargo test -p oz-bridge --lib settings::` -> 57 passed / 0 failed; tablet settings suite -> 72 passed; `cargo check -p oz-pos-tablet --all-targets` and `-p oz-pos-app --all-targets` both exit 0. Two follow-on edits kept the tree honest: the tablet re-exports this DTO, so its five test sites moved to `Some(..)` and its doc comment stopped naming the deleted fn. CROSS-PLAN NOTE: `todo-refactor-settings-agents-2.md` filed the same defect independently as its item (b) ("Consolidation") and names the same locus; the two plans agree, and this commit is the repair both asked for. Its item (a) -- that read-merge-write is not one transaction, so a peer terminal can still lose an update -- is NOT addressed here and remains open in that file. -->
- [ ] **T4-3 · Decide the hardware storage source, then land the 15-key surface on tablet (finding 3).** Blocked on the `AppState` → `BridgeCtx` seam T2 deferred with field-by-field evidence, and on which table is canonical (`settings` KV vs `hardware_profiles`). Do NOT "fix" it by re-exporting the DTO: that converts ten visible absences into ten silent drops. <!-- RE-MEASURED 2026-09-15, STILL OPEN, AND NOW NARROWED TO ONE QUESTION FOR THE OWNER. The row's own figures hold, re-derived from the files rather than repeated: bridge `HardwareSettingsDto` carries 15 fields and tablet's local copy carries 5 (`printer_connection`, `printer_device_path`, `printer_paper_size`, `scanner_device_id`, `scanner_input_mode`), while the renderer declares 15 (`ui/src/api/settings.ts`). The two shells also read different stores, confirmed at the call sites rather than from the summary: the tablet reads and writes the `settings` KV table via `Settings::get_printer_*` / `set_printer_*` (settings.rs:274-278 read, :293-297 write, and the same pair again on the scoped path at :732-736 / :753-757), whereas the bridge reads and writes `hardware_profiles` in the GLOBAL db, storing a serialized `TerminalProfile` as `profile_json` (settings.rs:820-880) with a file fallback through `TerminalProfile::profile_path(base_dir, …)` (:836-839). `base_dir` is the missing dependency, and the bridge's own module note at :21 records that `BridgeCtx` carries the app CACHE dir for it. So the row is blocked on exactly two things and neither is a coding task: (1) WHICH STORE IS CANONICAL -- `settings` KV or `hardware_profiles`; and (2) whether the tablet should reach the bridge's path at all, given T2 measured the `AppState` -> `BridgeCtx` seam at 7/14 fields matching and recorded that `plugins` would drag the mlua Lua VM into the Android APK for a field that is permanently `None` on this shell. NOT IMPLEMENTED, deliberately: the row's own prohibition stands and was re-read before writing this -- re-exporting the bridge DTO would move ten keys from a visible absence to a serde accepted-and-dropped write, which is the worse failure of the two. A decision, not a drift. -->
>
> ### Independent re-verification of the three T4 ticks — 2026-09-15, later pass, by the session that filed them
>
> A tick written by the lane that did the work and a tick read back by the lane that filed the finding are not the same evidence, so the four T4 rows were re-measured against the tree rather than trusted. **All three landed ticks hold, and T4-3 is correctly still open.** Re-derived at this pass: none of the eight scoped settings setters takes a `user_id` any more; `tax_rounding_mode` is `Option<String>` (`crates/oz-bridge/src/settings.rs:161`) with the write guarded at `:1037-1038`; `set_settings_scoped` is registered tablet-side (1 hit in `apps/tablet-client/src/lib.rs`) and the two dead unscoped preference commands return 0 definitions; tablet's local `HardwareSettingsDto` still carries exactly 5 fields. Gates on the combined tree — their T4 work and this file's T4 commits in one build: `cargo test -p oz-pos-tablet --lib` → **656 passed / 0 failed** (T4-1's own record says 654 at its close, so two cases arrived after it), `cargo test -p oz-bridge --lib settings` → **88 passed / 0 failed**, `python scripts/verify-ipc-parity.py` → `IPC parity: OK` exit 0 with **desktop 453 registered / tablet 323 registered, 156 allowlisted** (322/157 before the batch write was registered — the allowlist really did move, as the row predicted), `bash scripts/verify-scoped-coverage.sh` → PASS exit 0. The tablet's `0 unregistered tauri command fns` also holds, so deleting the two dead preference commands closed part of the F-006 side of that gap rather than moving it.
>
> **Two things worth adding for the next reader, neither of which the implementing record has.**
>
> 1. **A window-too-shallow false alarm, recorded because this file's own RBAC audits use windows.** Re-checking "did dropping the parameter also drop the check?" with a 14-line slice below each signature reported `set_setting_scoped` as **un-gated** — a serious finding, and wrong: `require_permission_for_session(&state, &session, permissions::SETTINGS_EDIT)` sits 17 lines into that body, behind the `terminal_id` lock that exists to keep an `.await` out of the DB lock. Reading to the closing brace instead of a fixed window turns it green, and the same pass then reports the honest result: **seven of eight gated**. Anyone re-auditing this class must read the whole function, because the bodies that are *correctly* guarded are also the longest, which is exactly the arrangement that makes a shallow window fail loudest on the good cases.
> 2. **`set_user_preferences_scoped` is the eighth, and it is not a gap — do not "find" it again.** It resolves the session and writes `UserPreferences::set_batch(&db, &session.user_id, …)` with no permission call, which looked like the residue of the sweep above. It is not: the shared path is identical (`crates/oz-bridge/src/settings.rs`, `set_user_preferences_scoped` — resolve session, open store, write for `session.user_id`, no permission check), so both shells agree, and the commands are already accounted for on this shell's own ledger at `apps/tablet-client/src/commands/registration_gate_debt.generated.rs:183` and `:187`. Personal UI preferences scoped to the caller's own user id need no `settings:edit`; that is a design position, and it is recorded here so the next window-sweep does not file it as a hole.
>
> Nothing was edited by that pass except its own record: no code file was touched, no box was ticked or unticked (**46 ticked / 5 open** measured there — the implementing pass had moved it from the 43/8 this record inherited by ticking T4-1, T4-2 and T4-4, with T4-3 the settings row still open), and the 656 tablet / 88 bridge-settings figures it reports are runs of a tree that pass did not modify. The pass below this heading does modify it.
>
> ### T5 · tax, and T6 · staff bootstrap — 2026-09-15 night pass (HEAD `7011487c5`, branch `0.0.39`)
>
> **The next domain was chosen by a census, not by this file's written order.** `staff` came first on the strength of the ordering proposed when T4 opened, and the ordering was wrong: a per-file count over `apps/tablet-client/src/commands/*.rs` of local `Deserialize`/`Serialize` derives, local `rename_all` attributes and `oz_bridge::` references showed staff was **already consolidated** — 21 types re-exported verbatim at `staff.rs:44-50`, and every one of the 11 registered commands' outer arguments matching `ui/src/api/staff.ts` one-for-one after Tauri's camelCase conversion (`get_staff_profile_scoped` is the tell: the wrapper sends `userId` at `staff.ts:463`, the Rust takes `user_id`, which is exactly the class that broke settings and which does not break here). `sync` was the other early candidate and was **deliberately skipped**: another lane had already written cross-shell drift guards there (`sync_tests.rs:64-112` deserializes one payload into both the tablet and the bridge `UpdateSyncSettingsArgs`, and `:114-135` compares serialized `SyncSettingsDto`s, with a comment at `:136-139` that anticipates the vacuous-equality trap). Sharing those three DTOs would have made both tests compare a type against itself — the durable fix arriving by way of deleting an interim guard someone built on purpose, for roughly thirty lines. `tax` was the live target on the census: 8 local DTOs, 5 local `rename_all`, 0 `oz_bridge::` references, and a desktop crate that is already a pure 260-line shim.
>
> **The eight tax field sets were diffed item by item before anything was deleted, and all eight were identical** — `TaxRateDto` (10), `TaxRateScopeDto` (3), `TaxRateWindowDto` (2), `CreateTaxRateArgs` (8), `UpdateTaxRateArgs` (9), `SetCategoryTaxRatesArgs` (2), `CategoryTaxRateRow` (2), `TaxRateDependencyCountsDto` (3) — so the re-export is wire-neutral by construction and the tablet's 640-line test module keeps constructing the same names. `use serde::{Deserialize, Serialize}` went with them; `cargo clippy -p oz-pos-tablet --all-targets -- -D warnings` is exit 0, so no import was left dangling.
>
> **T5-1, FOUND, FIXED at `0daafb098` + `42f65c297` — the tax delete guard read a key the backend never sent.** `TaxRateDependencyCountsDto` carried `#[serde(rename_all = "camelCase")]` in **both** copies, so it emitted `saleLines`; the renderer declares `sale_lines` (`ui/src/api/tax.ts:114`) and the guard that consumes it reads `pendingDeleteCounts.sale_lines` at `ui/src/features/tax/TaxConfigurationScreen.tsx:903/:908/:911/:933`, the last being `disabled={(pendingDeleteCounts?.sale_lines ?? 0) > 0}`. Against a real backend that is `undefined`, `undefined > 0` is `false`, so the "this rate is referenced by N historical sale lines" warning never rendered and Confirm was never disabled. Red, before any fix, from the new bridge pin: `left: "categories,products,saleLines"` / `right: "categories,products,sale_lines"`.
>
> **Severity, stated only after checking the server side — this cost an operator a warning, not their data.** `Store::delete_tax_rate` (`crates/oz-core/src/db/tax/rates.rs:356-367`) recomputes the counts inside the transaction and returns `CoreError::Validation` ("cannot archive tax rate {id}: referenced by N historical sale line(s)") when `sale_lines > 0`, so the delete was always refused; what the broken key destroyed is the *advance* notice the DTO exists to give, which its own doc states — `crates/oz-bridge/src/tax.rs` `dependency_counts_scoped` says the confirm UI fetches these counts to show "what blocks it (historical sale lines)". The dialog therefore asked the operator to confirm a delete that was going to fail, and explained nothing when it did.
>
> **And the desktop shell had the identical hole, which is the argument for the programme in one paragraph.** `apps/desktop-client/src/commands/tax.rs:36` re-exports the same `TaxRateDependencyCountsDto` and `:189` returns it from `dependency_counts_scoped`, so both shells emitted `saleLines` and both were fixed by the one bridge edit — while a tablet-only fix in the tablet's own copy would have left the desktop broken and the two files formally divergent. Two duplicated definitions of a wire type are also two chances for the copies to agree with each other and disagree with the renderer, which is the exact failure self-referential tests cannot see; `saleLines` appears **zero times** anywhere in this repository, so nothing on the emitting side could ever have complained, and the dev-mock answers `sale_lines` (`ui/src/dev-mock/handlers/catalog.ts:391`) so the screen looked correct in mock mode. This is the fourth consecutive domain audit that found a real wire defect (T2 void-sale casing, T3 `LocalPaymentRailArgs`, T4 `CreditSaleDto`, T5 the delete guard), and the first where the dead field was a safety check rather than a display string.
>
> **T6 · staff, `7011487c5` — the one staff body that is shareable today.** `run_bootstrap_owner` was duplicated line-for-line between `apps/tablet-client/src/commands/staff.rs:616-670` and `crates/oz-bridge/src/staff.rs:1301-1355` (same trim/lowercase, same three validations in the same order, same "staff accounts already exist" guard against `list_users`, same seed-then-create sequence, same `picker_ticket: String::new()` that the command wrapper then replaces), and unlike the other ten bridge command fns it takes only `&Connection`, so no `BridgeCtx` seam is needed. The tablet copy is now a three-line forwarder behind its original name, because six tests (`staff_tests.rs:200-303`) drive it directly — and those six are the equivalence proof: 6 passed before the change against the local copy, 6 passed after against the bridge copy, with `BridgeError::Invalid → AppError::Invalid` keeping every message byte-equal. The remaining ten staff commands cannot be shared from this shell until the deferred seam lands, and the two command wrappers keep their tablet-native picker-ticket minting, which needs the per-process secret.
>
> **Two things checked and deliberately NOT changed, recorded so the next pass does not re-find them.**
>
> 1. **`roundingMode` on the tax create/update args is a documented pending wire-add, not a drop bug.** `ui/src/api/tax.ts:71` and `:95` declare `roundingMode?: RoundingModeKey | ''` and neither Rust `CreateTaxRateArgs` nor `UpdateTaxRateArgs` carries the field, so serde accepts and discards it. That is the `MISMATCH-DROPS` shape — but the declaration says so itself at `:66-70`: *"Wire-additive: the backend Args do not carry the field yet and serde drops unknown keys, so sending it is a no-op until that slice lands."* Adding the field is a wire change with a storage question behind it, not a casing repair; it belongs to E1-6, not to this slice.
> 2. **The tablet's seven `require_permission_for_user(&store, &session.user_id, …)` calls in staff are not weaker-than-desktop drift.** They read like the settings bug and are not: the bridge makes the same call at `staff.rs:812/:846/:870/:1030/:1073`, its module note at `:545-556` explains that users and roles are global identity records so those gates run on `Store::require_permission` rather than the scope-aware form, and the desktop carries 19 `for_user` call sites against 30 `for_session`. Consistent across all three layers by design. (This is the same trap the re-verification block above records for a 14-line window: the deliberately-guarded bodies are the long ones.)
>
> **Open items this pass files, with what is verified and what is not.**

- [x] **T5-2 · Settle whether `has_users` and `refresh_picker_ticket` are reachable from the tablet before treating them as gaps.** Both are desktop-only and both are allowlisted (`scripts/ipc-parity-allowlist.json:104` and `:154`), and both are called from shared code: `has_users` from `ui/src/frontend/shell/AppShell.tsx:204/:218`, `refresh_picker_ticket` from `ui/src/contexts/WorkspaceContext.tsx:363/:573`. The sweep that found them concluded the first blocks first-run setup on a fresh tablet (`hasAnyUsers` stays `null`, and `AppShell.tsx:222-227` treats unknown as not-empty, while `bootstrap_owner` *is* registered tablet-side at `lib.rs:498`) — but that reasoning assumed `AppShell.tsx` was the tablet's boot path. **SETTLED BOTH HALVES, and they land on opposite sides.** The two shells are not one bundle with a branch: the desktop entry is `ui/src/App.tsx:2/:34` (`import AppShell` → `<AppShell />`) building `ui/dist` (`apps/desktop-client/tauri.conf.json:9-10`), while the tablet builds `ui/dist-tablet` from `build:tablet` = `tsc -b && vite build --config vite.tablet.config.ts` (`ui/package.json:13`), whose input is `./index.tablet.html` (`ui/vite.tablet.config.ts:23/:25`) → `ui/src/main.tablet.tsx`, which imports `TabletAppShell` (`:15`) and renders it (`:65`) and never imports `AppShell`. So **`has_users` has no tablet caller at all** — the allowlist exemption is correct, and the tablet's first-run gate is the setup-wizard API it does register (`TabletAppShell.tsx:6` imports `getSetupStatus`/`completeSetup`/`dismissSetupWizard`). Worth naming why the gate flags it: `verify-ipc-parity.py` counts **458 UI command strings across the source tree**, not per bundle, so a desktop-only caller in shared `ui/src` reads as an unregistered tablet command forever, and the allowlist is where that modeling gap is paid. **`refresh_picker_ticket` is the opposite case and is a real broken path**: its caller `WorkspaceProvider` is mounted directly in the tablet entry (`main.tablet.tsx:10`), and both call sites swallow the rejection — `:362-368` is `try { const refreshed = await refreshPickerTicket(prev); … } catch { // Refresh failed — use the existing ticket. }`, and `:573` has the same shape. The consequence is not a crash but a wrong-identity continue: `:356-358` states the refresh exists because "the hot-swap user needs a fresh ticket bound to THEIR identity (not the previous user's)", so on tablet the swap proceeds carrying the previous user's ticket into `createSession` at `:375`, which the tablet verifies — `apps/tablet-client/src/commands/auth.rs:352` calls `picker_ticket::verify_picker_ticket` and `:370` denies with "session creation denied — user_id mismatch with picker ticket". So the workspace hot-swap fails on tablet whenever a prior session token exists, and the one line that could have explained why is inside an empty `catch`. Filed as T5-5, which is the fix; nothing was edited by this verdict beyond this record.
- [x] **T5-3 · Decide the security-event flag drift between the shells.** Tablet `commands/auth.rs:65-66` calls `store.record_security_event(event, false)`; the bridge `crates/oz-bridge/src/auth.rs:159-160` calls it with `true`, and the flag is `debug_upgrade` (`cfg!(debug_assertions)`-gated). Net effect: on a **debug** build the desktop records the staff create/update security events and the tablet silently drops them; release builds agree. Reach: tablet `staff.rs:390/:518/:533`. Attributed to the audit subagent (`b5e8bc6f-688d-41a1-a8c9-bd62f202cec5`) and spot-verified at the two call sites by this pass, not traced through `record_security_event` itself — which is what the box is for. It is a one-argument change on either side, so the question is which behavior is wanted, not how to do it. <!-- DECIDED 2026-09-16, commit b44f9f2b8 (2 files, +82/-6): both values stay, because the asymmetry is not drift, and a test now makes that unsilenceable. THE ROW AS KILLED ME: this pass opened by agreeing with the row's framing -- two shells, one helper, identical bodies except a bool, the programme's whole thesis says collapse the fork -- and had the re-export written. Reading the tablet comment's line ABOVE the function stopped it: `debug_upgrade: false` is stated there as a deliberate per-client policy, not an oversight, and the bridge side states its own reason in turn ("matches the desktop's other tier reads (`require_audit_tier`)"). Collapsing to the bridge's `true` would have changed what a dev tablet writes into the security trail, on the authority of a row that had itself only "spot-verified the two call sites, not traced through `record_security_event`". THAT LAST CLAUSE IS WHY THIS ROW EXISTS, and it was right about its own limits, which is the rarest thing in a work order. WHAT TRACING IT FOUND, in `crates/oz-core/src/db/audit_security.rs`, which is the file neither shell's comment points at and the one that actually decides: the flag feeds `build_entitlements(...)` and the skip needs `ent.loaded && tier.audit_retention_days().is_none()` (`:389`), while a missing, tampered or unverifiable subscription row **fails open and records anyway** (`:357-367`, and the reasoning is quoted there: "an attacker who corrupts one row" must not be able to switch the security trail off, and "a stray audit row costs nothing, while a missing security event costs the evidence"). `:369-373` then names the per-client policy explicitly, including "tablet passes `false` so it never mirrors the desktop divergence" -- so the invariant is documented in the shared code, which is where a divergence between two callers of one function should be written down. TWO DEFECTS FOUND IN THE PROSE, both now repaired in the tablet's comment rather than papered over: (1) it cited commit **`dfbc41b2`** for the invariant, and that object does not exist -- `git cat-file -t dfbc41b2` → `fatal: Not a valid object name`. The substance is real and is carried by `06dcc61f4`'s message ("it is desktop-only -- commands/subscription.rs:143 passes debug_upgrade: true, the tablet never calls it, and the consolidation doc-comment records that divergence as preserved on purpose"), so the dead hash was replaced by the live citations instead of the claim being dropped. (2) Its last sentence -- "so a dev tablet on a Free row records nothing, **exactly like a production one**" -- is wrong in the direction that matters: with the flag `false`, a verified Free tenant records nothing in debug (the skip fires) but in a release build the unverified bootstrap row leaves `loaded == false`, and the recorder **writes on purpose**. The bridge's own test `staff_login_on_free_records_only_because_a_debug_build_promotes_it` had already worked this out and says so at its `:877-892`, including that the row count is 1 in both profiles so the count cannot separate the reasons -- only the flag can. THE PIN, and what it honestly does not cover: `the_debug_upgrade_policy_is_per_client_by_design` in `auth_tests.rs` asserts the tablet passes `false`, asserts the tablet does NOT also pass `true` (which would break the "one definition" claim in the same comment), asserts the bridge passes `true`, and asserts that the store's doc still contains "tablet passes `false`" -- four legs, so any one of the three documents can be changed only together with the others, and the failure text says to change the shared doc and this row, not one call site. Mutation-tested: flipping the tablet's bool to `true` fails it with that instruction; the file was restored byte-identical by hash. A semantic test would be better and is NOT possible from this crate today: the effect needs a validly-signed Active Free subscription row, and the only fixture that mints one is behind `crates/oz-bridge`'s private `#[cfg(test)] mod testing` (`lib.rs:153`). The test's own doc comment names the assertion to write if that fixture is ever exported, including the release caveat, so the gap is a to-do rather than a surprise. VERIFICATION: `cargo test -p oz-pos-tablet --lib` -> **667 passed / 0 failed** (666 + the new pin); clippy `--all-targets -- -D warnings` -> exit 0; `rustfmt` on both files; the pin passes unmutated and fails mutated. WHY THIS IS WORTH MORE THAN THE COLLAPSE: the thin-shell mandate is about bodies that differ by accident. Two callers of one store function that state opposite policies in writing, in the shared code that implements them, are the opposite case -- and this row is the second time this programme has nearly "consolidated" something whose difference was the point (the first was T12's hardware-settings fallback, which parity caught for me; this one I caught myself, one comment line earlier). -->
- [x] **T5-4 · Take the four dead legacy staff commands out, or say why they stay.** <!-- DONE 2026-09-16, commit 4f9f248c0 (2 files, +3/-116). WHAT THEY ACTUALLY ARE: not dead code by accident -- bodies that unconditionally return `AppError::PermissionDenied("legacy unscoped staff commands are disabled; use list_staff_scoped")`, each with a `**Deprecated for multi-store (ADR #7)**` doc comment pointing at its scoped twin. So they were written as polite refusals and then never registered: `git grep` over both `generate_handler!` lists finds no path for any of the four, which means the refusal text was unreachable and an old caller got Tauri's generic command-not-found instead. REACHABILITY, all four ways a name can be live: 0 callers in any crate (the four `store.list_roles()` call sites that remain in that file are a `Store` method sharing the spelling, not this fn -- the same call-shape trap as T13's helper); 0 UI invoke sites; 0 dev-mock handlers; 0 allowlist entries; 0 ledger rows, since the sweep only walks registered pairs. WHY THE TESTS WENT TOO: `staff_tests.rs` held `create_staff_args_deserialize`, `create_staff_args_debug`, `update_staff_args_deserialize`, `update_staff_args_debug`, and those four exist in `crates/oz-bridge/src/staff_tests.rs:216-252` **under the same names** testing the same bridge-owned structs (`CreateStaffArgs`/`UpdateStaffArgs` are declared at `crates/oz-bridge/src/staff.rs:316/331` and only re-exported by the tablet's `pub use`), so the tablet's copies were duplicates rather than coverage and the two names came out of the re-export list too. That is why the suite moved 670 -> **666** by exactly four cases rather than by some unrelated loss. VERIFICATION: `cargo test -p oz-pos-tablet --lib` -> 666 passed / 0 failed; `cargo clippy -p oz-pos-tablet --all-targets -- -D warnings` -> exit 0, which is the check that mattered, because CI runs `cargo check` under `RUSTFLAGS: -D warnings` and a stale re-export or a now-orphaned import would have failed it; `registration_gate` -> 9 passed; residue grep for the four names and the two DTOs across the crate -> empty. WHAT MADE THIS ROW POSSIBLE, and it is the bigger half: the gate could not see these functions at all until this round -- see T19, where the F-006 leg's zero turned out to be vacuous for this shell, and the same commit series that lists my four also lists 118 more. PROOF THE INSTRUMENT NOW TRACKS THIS: the leg's count moved **122 -> 118** on this deletion, exactly the four names, where before the fix the same deletion would have moved a printed 0 to a printed 0. --> `list_staff` (tablet `staff.rs:79`, defined nowhere on desktop), `list_roles` (`:91`/`d:75`), `create_staff` (`:105`/`d:89`), `update_staff` (`:122`/`d:106`) are registered in neither shell, and their DTOs still declare `caller_user_id: String` (`crates/oz-bridge/src/staff.rs:326/:343`) — which is the forgeable-caller shape that `ui/src/api/staff.ts:3` advertises the module does not have, and it advertises that about types that are no longer callable. Verified: registration counts (0 in each `generate_handler!` block) and the two field declarations.
- [x] **T5-5 · Give the tablet a `refresh_picker_ticket` command so the workspace hot-swap stops failing on a swallowed error (from T5-2).** <!-- DONE 2026-09-16, commit 683c9f2b9 (4 files, +33/-2). THE BODY IS A PURE SHIM, NOT THE ~12-LINE FORK THIS ROW PREDICTED: the row assumed the bridge helper could not be shared because it takes &BridgeCtx, and that was wrong -- AppState::bridge_ctx() exists and this shell already builds it (settings.rs), so the tablet now registers exactly what the desktop registers: `let ctx = state.bridge_ctx(); oz_bridge::auth::refresh_picker_ticket(&ctx, &session_token).map_err(Into::into)`, matching `apps/desktop-client/src/commands/auth.rs:294` line for line. The sweep therefore classes it GATED by delegation (bridge `auth.rs` does name `permissions::`), so no ledger row was added and `gateAccepted` moved 228 -> 229 with `registered` 318 -> 319 while `REGISTERED_TOTAL` stayed a valid lower bound. ONE THING THE PREVIOUS RECORDS HAD WRONG, found only by looking: the name was in `scripts/ipc-parity-allowlist.json` TWICE, at :154 and :233, both inside the tablet's own array (verified by walking the file's keys from the top, not by assuming the line numbers meant two shells). Two exemptions for one gap. Both were deleted with the command, which is also the correction to "drop :154" as written above. THE GATE THEN TAUGHT SOMETHING NEW: with the registration in place and the exemptions gone, `verify-ipc-parity.py` still failed, on an axis this file has not discussed -- its dev-mock side: "UI invokes 'refresh_picker_ticket' but no handler anywhere under ui/src/dev-mock registers it (the router and every extracted module under it were read, and no unscoped twin exists for the alias rule to reach) -- invoke() returns null and the caller silently renders its failure path". That is T12's aliasing mechanism confirmed from the other direction: `applyScopedAliases` can only borrow an unscoped handler, so a command with no unscoped twin needs its own mock entry, and the duplicate exemptions had been hiding exactly that hole in the E2E surface. Fixed by adding it to `ui/src/dev-mock/handlers/staff.ts` next to `destroy_session`, returning `{ picker_ticket: 'mock-picker-ticket' }` -- SNAKE, because `RefreshPickerTicketResult` (`crates/oz-bridge/src/auth.rs:134-137`) carries no `rename_all` and the renderer's own interface at `ui/src/api/staff.ts:556` declares `picker_ticket`, so a camelCase mock would have agreed with itself and disagreed with both shells. That is the fifth instance of the casing class in this programme and the first one I nearly wrote myself. VERIFICATION: `python scripts/verify-ipc-parity.py` -> `IPC parity: OK` exit 0 (458 UI strings / 319 tablet registered / 154 unregistered names / 155 allowlisted); `bash scripts/verify-scoped-coverage.sh` -> PASS; `cargo test -p oz-pos-tablet --lib` -> **670 passed / 0 failed**; `registration_gate` -> 9 passed; clippy `--all-targets -D warnings` -> exit 0; `npx tsc --noEmit` reports nothing for the mock file; and `npx vitest run src/__tests__/dev-mock-scoped-aliases.test.ts` -> **40 passed / 0 failed**, run because that file is the alias mechanism's own contract test and I had just added a key to the registry it walks. STILL OPEN, and deliberately not done in this commit: what the renderer does with the answer. `ui/src/contexts/WorkspaceContext.tsx:361-368` calls `refreshPickerTicket(prev)` inside an inner `try`, and its `catch { }` at `:366` comments "Refresh failed — use the existing ticket" and falls through to `ticket = pickerTicketRef.current ?? ""` — the possibly five-minute-old original — which is then handed to `createSession` at `:375`. So on a tablet, before this command existed, EVERY workspace hot-swap silently took that fallback and then depended on whether the login-time ticket happened to still be unexpired; the row's original "stops failing on a swallowed error" is only half-closed by making the call answerable. The swallow itself is a UI change with its own behavior question, but T12's lesson is directly on point: an empty catch is how a broken feature survives six rounds, and this one deserves at least a log line and a comment saying which ticket the user is about to get. --> The implementation exists once, in `crates/oz-bridge/src/auth.rs:741`, and is tested there (`auth_tests.rs:465` fresh ticket, `:511` invalid session, `:523` expired session — the three branches that matter); the desktop reaches it through a four-line command at `apps/desktop-client/src/commands/auth.rs:293-300`. The tablet's `auth.rs` registers eight commands (`:89 :149 :330 :556 :591 :780 :946 :991`) and this is not one of them, while its caller is mounted in the tablet bundle (`main.tablet.tsx:10` → `WorkspaceContext.tsx:363/:573`). **It is not a share, though — that question is answered, not open:** the bridge fn takes `ctx: &BridgeCtx<'_>` (`:742`) and the desktop's shim gets one from `state.bridge_ctx()` (`auth.rs:298`), which this shell's `AppState` does not expose — the same deferred seam T4-3 is blocked on. What the body actually needs is two things the tablet already has: a session resolve for the `user_id` binding (`:747`) and the signing secret (`:755`, and this shell holds it — `staff.rs:608` uses `state.picker_ticket_secret` for exactly this call). So the change is a ~12-line tablet-native command mirroring `auth.rs:741-766`, one `generate_handler!` entry, and dropping the name from `scripts/ipc-parity-allowlist.json:154` — the same three-file shape `set_settings_scoped` went through in `3a77ed981`, where parity moved 322 → 323 registered and 157 → 156 allowlisted. Two decisions belong in that commit rather than being discovered after it: **(1)** registration alone may not fix the swap, because `WorkspaceContext.tsx:174` reads `const updatePickerTicketFn = updatePickerTicket ?? PICKER_TICKET_NOOP;` and the stand-in at `:142-149` is documented as being for "shells that do not expose it" — so on a shell whose auth context omits the setter, a successfully refreshed ticket is still thrown away, and the empty `catch {}` at `:366-368` is what hides both halves; the catch should carry a line regardless, but that is shared `ui/` code with other owners, so it is proposed here, not owed by Agent 3. **(2)** whether this is worth doing before the seam lands at all, given the swap path also has to survive `auth.rs:352/:370`'s identity check. **Checked while in there and clean, so it is not re-checked:** neither `RefreshPickerTicketResult` (`crates/oz-bridge/src/auth.rs:134-137`) nor `BootstrapOwnerResult` (`crates/oz-bridge/src/staff.rs:530`) carries a `rename_all`, so both emit `picker_ticket`, which is what the TS declares (`ui/src/api/staff.ts:558`, `:92`) and what the caller reads (`WorkspaceContext.tsx:364`). No casing defect in the session/picker family — which makes five surfaces audited with four defects found and one claim in this file's own ordering (staff first) falsified.
>
> **Census after this slice, same command as before: 26 of 48 tablet command files still define local DTOs — 155 definitions, 46 local `rename_all`.** The top of that list is the queue, and the ordering this file assumed is not it: `hardware` (7 `Deserialize` + 11 `Serialize`, 0 bridge refs — and owned by T4-3's storage decision, plus another lane's in-flight `settings.rs` edit), `purchasing` (6 + 9, 0 `rename_all`), `customers` (5 + 10), `sync` (6 + 7, already drift-pinned, see above), `inventory_counts` (5 + 8, 5 `rename_all`, and only 23 lines of tablet tests), `categories` (3 + 7), `terminals` (3 + 6, 6 `rename_all`), `offline` (2 + 5, 5 `rename_all`). `tax` and `settings` have left the list. Note that `rename_all` density, not struct count, is the risk ranking — casing is where all four defects so far lived.
>
> **Verification for these three commits, all run against the tree they describe:** `cargo test -p oz-pos-tablet --lib` → **656 passed / 0 failed**; `cargo test -p oz-bridge --lib` → **1,317 passed / 0 failed**; `cargo check -p oz-pos-app --all-targets` → exit 0; `cargo clippy -p oz-pos-app -p oz-pos-tablet --all-targets --all-features -- -D warnings` → exit 0; `python scripts/verify-ipc-parity.py` → `IPC parity: OK`, exit 0, desktop 453 / tablet 323 registered (unchanged — this slice moved types, not commands); `bash scripts/verify-scoped-coverage.sh` → PASS. Nothing was pushed. Two new things the parity run surfaced in passing and this pass did **not** act on: `rotate_encryption_key` is an allowlist entry that matches no command in the tree and no UI invoke site on either side (the gate prints it as informational, "accepted by the file and enforced by nothing"), and the same run now prints 24 `scoped_orphans` with 2 flagged as gated dead surface (`get_active_cart_scoped`, `list_active_carts_scoped`).
>
> ### T7 · terminals — round 2 pass, 2026-09-15 (the RBAC half of the programme, found by accident: HEAD `50b2fd14e`)
>
> **Chosen by `rename_all` density, which pointed at the wrong defect class and still produced the largest find so far.** `terminals` carries 6 local `rename_all` attributes and 0 `oz_bridge::` references, so it looked like another casing slice. Its wire turned out to be **clean**: all six DTOs are field-for-field identical between this shell and `crates/oz-bridge/src/terminals.rs` (`TerminalDto` 8, `RegisterTerminalArgs` 4, `RegisterTerminalResult` 1, `UpdateTerminalArgs` 6, `UpdateTerminalResult` 1, `SetDeviceBindingArgs` 3), and `ui/src/api/terminals.ts:6-32` declares exactly those camelCase keys. No pin was needed because nothing disagreed. What the *bodies* were missing turned out to be the permission layer, and the evidence for that was already in this repository's own ledger, unexamined.
>
> **T7-1, FOUND AND FIXED — four tablet terminal reads authenticate a session and then assume the caller may read.** `list_terminals_scoped`, `get_terminal_scoped`, `ping_terminal_scoped` and `list_terminal_overrides_scoped` each did `let (_session, conn_arc) = state.resolve_scope(…)` — the underscore on the binding is the whole story — and went straight to the query. Each of the four bridge twins carries the check under a comment naming the work item: `crates/oz-bridge/src/terminals.rs:183-186`, `:212-214`, `:234-236`, `:258-260` all `ctx.require_session_permission(&session, permissions::TERMINALS_READ)` with **"F-017: enforce per-domain permission on this scoped command"**, and the desktop reaches them through that path (`apps/desktop-client/src/commands/terminals.rs:32-38`). So F-017 was applied to the shared code and to the desktop, and never to the tablet copies — which is why the tablet's own ledger had all four, spelled `"resolves_session_names_no_permission"` at `registration_gate_debt.generated.rs:367/:371/:375/:379`, the enum this file documents as *"Resolves a session and never asks whether the caller may: authenticate-then-assume"*. Consequence before the fix: any authenticated session on a tablet — a cashier with no terminals role — could enumerate every terminal in the store with its device id and metadata, read a terminal's feature overrides, and write a timestamp through `ping_terminal`. Not anonymous, scoped to the store, but unauthorized within it.
>
> **T7-2, FOUND AND FIXED — the entire terminal write surface on the tablet was unreachable, and the reason it was unreachable was the same argument that made it forgeable.** Five scoped commands declared `user_id: String` — `register_terminal_scoped`, `update_terminal_scoped`, `delete_terminal_scoped`, `set_terminal_override_scoped`, `delete_terminal_override_scoped` — while `ui/src/api/terminals.ts` sends `{ sessionToken, args }` (`:55`, `:63`), `{ sessionToken, id }` (`:79`) and `{ sessionToken, terminalId, feature, enabled }` / `{ sessionToken, terminalId, feature }` for the overrides. `loggedInvoke` injects nothing (`ui/src/utils/logged-invoke.ts:18`), so Tauri rejected all five before their bodies ran (`tauri-2.11.3/src/ipc/command.rs:100`, "command {} missing required key user_id"), and the check each one eventually performed was `require_permission_for_user(&store, &user_id, TERMINALS_REGISTER|EDIT|DELETE)` — an actor named by the caller. This is settings T4-1 reproduced at five times the scale, in the file the census happened to open first. All five now take the session (`require_permission_for_session(…, permissions::TERMINALS_*)`) with the gate placed before the store lock, so no `.await` sits inside it, matching this shell's own settings idiom. The tablet's `verify-scoped-coverage`/parity numbers are untouched: no command was added, removed or renamed, so the registered surface is byte-stable at 323.
>
> **Ledger bookkeeping, and one admission about the tooling.** Four commands stopped being debt, so their four rows were removed and `DEBT_CEILING` 125 → 121 with `RESOLVES_SESSION_NAMES_NO_PERMISSION` 78 → 74 (`NO_SESSION_RESOLUTION` 47 unchanged; 47 + 74 = 121, which is how the class counts must close). The file's own header says **"GENERATED … DO NOT HAND-EDIT"** and **"Regenerate rather than type: see the header of registration_gate_tests.rs"** — and that header, read in full at `:50-60`, names no command, no flag and no script. The only tool in the repo that touches the ledger, `.agents/measure_gate_gap.mjs`, **measures** (it parses the ledger and the `generate_handler!` list and prints counts) and writes nothing. So the file is hand-maintained while forbidding hand maintenance, and the only reason this pass could do it safely is that the ratchet enumerated its own correction: `drift_pin_three_way_partition_is_complete_and_sums` failed with *"Ungated now and NOT on the ledger (0 names …); on the ledger but no longer ungated (4 names, …)"* — the exact set, in both directions. It was applied literally, the ratchet re-passed, and the external tool cross-read the result independently: `##### tablet registered=323 … debtRows=121 (DEBT_CEILING=121) gateAccepted=202`, `stale ledger rows absent from live list: none`. A generated file whose generator does not exist is a debt on its own; recorded, not repaired here.
>
> **Two numbers that same tool printed which this pass did not act on, both larger than the four rows it shed.** `REGISTERED_TOTAL` is 318 on tablet against 323 live, and 447 on desktop against 453 live — the floor the ratchet asserts is **stale in the permissive direction on both shells**, so five and six registrations respectively landed without anyone touching it, which is what a floor is supposed to make loud. And the tool's own divergence line reads `##### DIVERGENCE shared=298 differentlyClassified=92`: of the commands both shells register, **92 are classified differently** (gated here, debt there). This slice closed 4 of them by hand-picking one file; the number says the remaining 88 are the actual shape of the rest of this programme, and they are answerable by the same diff — read the bridge twin's gate, compare to the tablet body — without inventing anything.

- [ ] **T7-3 · Add the denial tests this fix does not have.** The 34 existing `terminals_tests.rs` cases all still pass, which proves nothing about the nine new gates — they exercise `run_*`/store helpers and the unregistered unscoped forms, not the scoped commands, so no test would have caught the hole and no test currently holds it shut. Model to copy: `scoped_tax_command_denies_user_without_permission` in `apps/tablet-client/src/commands/tax_tests.rs`, which already does this for the tax surface in this same shell. Five commands want a denial case (the four reads plus one write to represent the five), and each case doubles as the proof that the UI-shaped call — `{ sessionToken, id }`, no `user_id` — now reaches the body instead of being rejected by Tauri, which is the half of T7-2 that no test can currently distinguish from a working command.
- [ ] **T7-4 · The six unscoped `terminals` commands that take a caller-named `user_id` are registered in neither shell.** Measured: `set_device_binding`, `register_terminal`, `update_terminal`, `delete_terminal`, `set_terminal_override`, `delete_terminal_override` plus `list_terminals`, `get_terminal`, `ping_terminal`, `list_terminal_overrides` return **not registered** for every name in the tablet's `generate_handler!` block, while `set_terminal_override`, `delete_terminal_override` and `list_terminal_overrides` still have UI names in `ui/src/api/terminals.ts` (`:120`, `:128`, and the unscoped list wrapper) — unscoped wrappers that no shell answers, sitting beside the scoped ones the screens actually call. Same disposition as T5-4 (delete or say why they stay), and same caution: `bootstrap_owner` and the like show that unregistered does not mean unreachable in dev-mock, where these names may still be answered.

- [x] **T7-3 · Add the denial tests this fix does not have.** <!-- IMPLEMENTED 2026-09-15, commit 740a195ad "test(tablet): hold the nine terminals gates this shell just grew, on both sides" (+231 lines, three cases). THE PAIR IS THE PROOF, NOT THE DENIAL CASE ALONE. `scoped_terminal_reads_deny_a_session_without_terminals_read` and `scoped_terminal_writes_deny_a_session_without_their_permission` assert `Err(AppError::PermissionDenied(_))` for a `role-staff` cashier across the four reads and four of the five writes; `scoped_terminal_writes_accept_an_owner_session` asserts, for the same commands with the same store id and the same token shape and only the role changed, that the result is NOT a permission refusal. So the denial cannot be vacuous in either direction: remove a gate and the first two fail, make a gate refuse everyone and the third fails. The owner case deliberately asserts absence-of-refusal rather than `Ok(..)` because the store under test holds no terminals, so NotFound and empty-list answers are the correct behavior of a body that was reached. `register_terminal_scoped` is the one write not asserted either way: it calls `sub.verify_signature()?` on the tenant subscription before its gate, and a test-only DB has no signed default tenant, so a refusal would be evidence about the subscription rather than about the permission. WHAT THESE CANNOT PROVE, and it is the larger fact: they call the command functions directly, and the missing-argument half of T7-2 lives only inside Tauri's argument resolution. Measured — `git grep -ln 'mock_ipc\|MockInvoke\|handle_invoke\|ipc::Command' -- apps/tablet-client apps/desktop-client ui/src` returns **no files**: no test in this repository crosses the IPC boundary in either direction. Verification: `cargo test -p oz-pos-tablet --lib terminals` → 37 passed / 0 failed (34 pre-existing + 3), `cargo test -p oz-pos-tablet --lib` → **659 passed / 0 failed**, `cargo clippy -p oz-pos-tablet --all-targets -- -D warnings` → exit 0. The three new cases are also the first in this module to touch a `#[command]` at all: the 46 cases that were here are store-level and drive `run_*` helpers, which is the reason the hole had no covering test on either side. -->

- [x] **T8 · Ten registered tablet commands in `tables` and `promotions` are the terminals defect a third and fourth time, enumerated and half-triaged.** <!-- FIXED 2026-09-15, commit e86643e3d "fix(tablet): derive the ten tables and promotion write identities from the session, and cover both halves" (4 files, +464/-35, one new file). All ten now resolve `(session, conn)` and call `require_permission_for_session(&state, &session, …).await?` before taking the store lock, exactly as `crates/oz-bridge`'s twins do, and no ledger row moved: they were classed GATED before and after, which is why this commit needed no ceiling change -- the ledger records the absence of a check, not the quality of the one present. THE TWO CALIBRATION FACTS, both produced by the tests rather than by reading, and both would have been wrong in the design: (1) Staff is NOT a no-tables role. `platform/core/src/rbac_presets.rs:152-155` grants `role-staff` `tables:assign`, `tables:merge`, `tables:split` and `tables:close` while withholding create/edit/delete -- so a first draft that asserted `PermissionDenied` for all six scoped writes failed on precisely the three a waiter must be allowed to do. The shipped file asserts denial for create/update/delete and *reaching the body* for assign/status/release, which pins the mapping in both directions and stops a future tightening from passing the suite by refusing everyone. (2) A scoped write that reaches its body can still be refused by it: `update_table_status("occupied")` answers Validation "occupied requires an active sale -- use assign_table_order" (`crates/oz-core/src/db/tables.rs:256-263`), so the functional test uses `cleaning`. That distinction is only visible through a positive functional case, which is why `scoped_table_write_path_actually_writes_for_an_owner` and `scoped_promotion_write_path_actually_writes_for_an_owner` exist alongside the denials -- create, rename, flip status, list back from disk, delete, list empty. Neither module had ANY command-level test before this commit: `tables` had no test file at all (created, 227 lines, wired with the `#[cfg(test)] #[path]` convention the other modules use) and `promotions_tests.rs` was 50 lines of DTO/deserialize cases. `promotions` is the sharper RBAC edge: Staff holds `discounts:apply` but NO `promotions:*` key at all, so a cashier may discount a line and may not create, edit, delete or APPLY a promotion; if that asymmetry is a product bug rather than a decision, the denial test is the thing to change, and it says so in its comment. VERIFICATION: `cargo test -p oz-pos-tablet --lib` -> **664 passed / 0 failed** (was 659, +5), `cargo clippy -p oz-pos-tablet --all-targets -- -D warnings` -> exit 0, `python scripts/verify-ipc-parity.py` -> exit 0 with tablet 323 registered unchanged, `verify-scoped-coverage.sh` -> PASS, `.agents/measure_gate_gap.mjs` -> `debtRows=121 (DEBT_CEILING=121) gateAccepted=202` unchanged and `differentlyClassified=92` unchanged, because these ten were already counted as gated: this commit fixed WHO the gate asks, not whether one exists. --> The sweep that found T7-2 generalized in one line: walk every `pub async fn *_scoped(` in `apps/tablet-client/src/commands/*.rs` and flag those whose first ten lines declare `user_id|operator_id|caller_user_id|target_user_id: String`. It returns **13 commands**, every one of them REGISTERED in `apps/tablet-client/src/lib.rs`, and it is the first measurement in this file that asks the question at population scale instead of per-domain. **Three are legitimate and must not be "fixed":** `auth::impersonate_user_scoped(target_user_id)` — the argument IS the target, not the actor; `staff::get_staff_profile_scoped(user_id)` and `analytics::get_staff_analytics_daily_scoped(user_id)` — both wrappers send the key (`ui/src/api/analytics.ts:50-54` sends `{ sessionToken, userId, from, to }`), which is exactly how `staff.ts:463` sends `userId` for the profile read. **Ten are the defect**, with the payload each UI wrapper sends re-read from the wrapper rather than inferred: `tables::create_table_scoped` ← `{sessionToken, table}`, `update_table_scoped` ← `{sessionToken, table}`, `delete_table_scoped` ← `{sessionToken, id}`, `update_table_status_scoped` ← `{sessionToken, id, status}`, `assign_table_order_scoped` ← `{sessionToken, tableId, saleId}`, `release_table_scoped` ← `{sessionToken, tableId}`; `promotions::create_promotion_scoped` ← `{sessionToken, args}`, `update_promotion_scoped` ← `{sessionToken, promotion}`, `delete_promotion_scoped` ← `{sessionToken, id}`, `apply_promotion_scoped` ← `{sessionToken, saleId, promotionId}`. None sends `userId`, and `loggedInvoke` injects nothing (`ui/src/utils/logged-invoke.ts:18`), so each is rejected by `tauri-2.11.3/src/ipc/command.rs:100` before its body — the whole table-management write surface and the whole promotion write/apply surface on this shell. The twins confirm it is drift rather than a different design, in both modules: tablet `create_table_scoped` (`apps/tablet-client/src/commands/tables.rs:176-191`) takes `user_id: String` and calls `require_permission_for_user(&store, &user_id, TABLES_CREATE)` while the bridge (`crates/oz-bridge/src/tables.rs:64`) takes `(ctx, session_token, table)` and calls `require_session_permission(&session, TABLES_CREATE)`; same shape for `promotions::create_promotion_scoped` (tablet `:207`, bridge `:111`, `PROMOTIONS_CREATE`). The fix is T4-1/T7-2 verbatim a third time, and it needs no ledger edit: these ten are classed GATED today (their modules contribute 3 ledger rows each, for other commands), and `require_permission_for_session` keeps them gated. Two cautions carried forward from the rows that did this before: place the await before the store lock, and re-read each body rather than pattern-matching a window — a 14-line window already produced one false RBAC alarm in this file.

- [ ] **T9 · Nothing tests the IPC boundary, so this class is uncatchable by tests — the argument-shape gate.** T4-1, T7-2 and T8 are the same defect found three times in five domains, and each time it was found by a human comparing a Rust signature against a TypeScript object literal. The mechanical form of that comparison is cheap here because the data is already parsed: `scripts/verify-ipc-parity.py` reads both the `generate_handler![…]` lists and the `loggedInvoke('name', { … })` payload keys, and `rustc` supplies the parameter list. A gate that, for every registered command, asks whether each required non-`Option` argument appears as a key in some caller's payload — camelCase-matched, since Tauri converts outer names and nothing else — would have failed on `set_receipt_settings_scoped`, `delete_terminal_scoped` and all ten of T8's commands on the day they shipped, and would keep failing until the shapes agreed. Scope it honestly: the checker has to treat `Option<T>` as satisfiable by absence, allow the `state`/`app`/`webview` injected parameters, and decide what to do about commands with no caller at all (T5-4, T7-4) — that last set is already enumerated by the `scoped_orphans` list this script prints, so the input exists. Before it exists, no green run of any suite in this repository distinguishes a command whose arguments the UI cannot supply from one that works: `git grep -ln 'mock_ipc\|MockInvoke\|handle_invoke\|ipc::Command' -- apps/tablet-client apps/desktop-client ui/src` → no files. It is a `scripts/` edit and an owner decision about failing the build on it, not an Agent-3 edit to make alone. <!-- AMENDED 2026-09-15, same day, because a claim in this row was true as measured and incomplete as stated. The grep behind "no test crosses the IPC boundary" looked for TAURI-side mock idioms (mock_ipc, MockInvoke, handle_invoke, ipc::Command) and found none, which remains correct: nothing on the Rust side resolves a command's arguments in a test. It said nothing about the JS side, and there IS a suite: ui/src/__tests__/api-tables-contract.test.ts:47 asserts expect(mockInvoke).toHaveBeenCalledWith('create_table_scoped', { sessionToken: TOKEN, table }) with a mocked invoke, 8 such assertions in the file, and `userId` occurs 0 times in it. That test was GREEN the entire time the Rust command required user_id: String and was therefore unreachable. So the class is not untested in the sense of nobody having written down the payload shape -- the shape is pinned, in the language of the caller, on the side of the boundary that cannot fail. Two consequences, and the second is the reason to keep the box. First, a green UI contract test is not evidence of reachability: it encodes caller intent, never callee requirement, which is the self-referential-agreement failure mode this file has already recorded three times for casing, now reproduced for required arguments. Second, the gate T9 asks for gets cheaper and better specified: both sides of the comparison already exist as text a script can read -- the wrapper's payload keys (either from the TS source, which verify-ipc-parity.py already parses, or from these assertions) against the Rust parameter list -- and the only new machinery is the camelCase mapping of outer argument names plus the Option/`state` exemptions named above. <!-- SECOND AMENDMENT 2026-09-16, narrowing what T9 actually has to add. T12's reverted deletion produced the accidental experiment: removing `settings::set_hardware_settings` from `generate_handler!` while `ui/src/api/settings.ts:139` still named it turned `verify-ipc-parity.py` RED with "tablet: UI invokes 'set_hardware_settings' but it is not in apps/tablet-client/src/lib.rs generate_handler (e.g. ui/src/api/settings.ts)". So the NAME dimension of this boundary is already gated, was already gating, and is why `verify-scoped-coverage` and parity were the two tools that never once lied to me across six rounds of wire work. T9's claim is therefore narrower than "nothing tests the boundary": nothing compares **argument keys** across it. The gate to write is the same comparison with one more column — for each registered command, the set of required non-`Option` outer parameters (camelCased by Tauri's own rule) against the key set the caller's payload object literal supplies — and the failure text above is the shape its message should take, since it names both sides and the file to look in. Recorded because "no test crosses the IPC boundary" and "no test compares argument shapes across it" are different claims with different fixes, and I wrote the first one. -->

- [ ] **T10 · The `*_scoped` sweep is closed; widening it to every registered command leaves seven, all legacy `settings`.** T8's method was narrow in two ways worth naming, because the narrow version is what made it tractable: it looked only at functions ending `_scoped` and only at four parameter names. Re-running it over **every** `pub async fn` in `apps/tablet-client/src/commands/*.rs` whose parameters include an identity-named required `String` (`*user_id|*operator_id|*actor_id|*staff_id|*employee_id: String`) and then keeping only the names present in the tablet's `generate_handler!` block returns **29 such functions, of which 10 are registered**: the three legitimate target-identity commands already excluded in T8 (`analytics::get_staff_analytics_daily_scoped`, `auth::impersonate_user_scoped`, `staff::get_staff_profile_scoped`) plus **seven in `settings`** — `set_receipt_settings`, `set_store_settings`, `set_credit_settings`, `list_credit_sales`, `settle_credit`, `set_hardware_settings`, `set_setting`. So the caller-supplied-identity class is closed for the scoped surface of this shell and open for these seven legacy twins. What is NOT yet known, and is the whole task: each of the seven is still *named* in the renderer — 1, 2, 1, 1, 1, 3 and 2 call sites respectively — while its `_scoped` twin is named more often (6, 5, 3, 2, 2, 2, 2). A name in `ui/src/api/*.ts` reached by a mounted screen is a live call into a command that trusts a caller-named actor; a name only in `ui/src/dev-mock/**` is a mock answering a question nobody asks. Deciding which is which is the bundle-reachability method already proven in T5-2 (the tablet entry is `ui/src/main.tablet.tsx`, not `AppShell`), and the disposition then follows T4-4/T7-4 for the dead ones and T8 for the live ones. Do not assume "legacy means dead": `set_hardware_settings` has three UI names against its twin's two. <!-- TRIAGED AND HALF-EXECUTED 2026-09-16, in two commits. THE SPLIT, measured from the call sites rather than inferred: of the seven, FIVE are named only inside `ui/src/dev-mock/handlers/**` (`set_receipt_settings` :68, `set_store_settings` :55, `set_credit_settings` :82 in settings.ts; `list_credit_sales` :231 and `settle_credit` :233 in payment.ts) plus one `__tests__` file, and have NO wrapper in `ui/src/api/**` at all -- no renderer path exists to reach them. The other TWO are genuinely live: `set_hardware_settings` ← `ui/src/api/settings.ts:139` (`export const setHardwareSettings = (args, userId)`) with exactly one caller, `ui/src/hooks/useTerminalHardware.ts:337`; and `set_setting` ← `:230` (`setSetting = (key, value, userId)`) with exactly one caller, `ui/src/frontend/shell/UpdateBanner.tsx:211`. Both send `userId` from the renderer, so they work -- which is why they were never suspicious -- and both let the caller name the actor the permission check asks about. (a) DELETED the five: `613d72f12`, 6 insertions / 108 deletions across `apps/tablet-client/src/commands/settings.rs`, its `lib.rs` registrations and the ledger. Chosen over "leave it" for the same reason T4-4 deleted the dead preference pair: the desktop crate and `crates/oz-bridge` define NONE of the five (measured: `pub async fn <name>(` → tablet-only), so deleting is the change that makes the shells agree, and each of the five has a registered `_scoped` twin that the UI actually calls. THE ONE THING THIS HALF ALMOST BROKE, caught before the edit: the five `ui/src/dev-mock/handlers` entries look like the same dead surface and are NOT. `applyScopedAliases` (`ui/src/dev-mock/core/mockDispatcher.ts:84`, patch site `:92`) copies each unscoped handler onto its `_scoped` name, so deleting `'set_store_settings'` from the mock would have silently removed the mock's answer to `set_store_settings_scoped`, the very call the workspace cards make. That is the mechanism this file has twice warned about in prose -- "unregistered does not mean unreachable in dev-mock" (T7-4) -- now named instead of assumed, and it is cited in `scripts/verify-ipc-parity.py:174-182`, which had to be widened to walk the whole mock tree after `ce8666604` moved the aliasing pass and the gate began reading 215 of 536 registrations. The mock entries stay. No allowlist edit was needed and none was made: `verify-ipc-parity.py` still counts **458 UI command strings and 155 unregistered UI names**, identical before and after, which is the mechanical proof the five names were never renderer-invoked. Side effect worth having: tablet registered 323 → **318**, which is exactly `REGISTERED_TOTAL`, so the floor lag recorded in T7 closed on this shell by subtraction. (b) GATED the six scoped settings reads: `4bdb4b4d7`. That half was not in T10's scope when written and is the better find -- it came from a note another lane had left in the code. Every one of `get_receipt_settings_scoped`, `get_store_settings_scoped`, `get_credit_settings_scoped`, `get_hardware_settings_scoped`, `get_setting_scoped` and `list_credit_sales_scoped` did `let (_session, …) = state.resolve_scope(…)` and ignored the session, while all six `oz_bridge::settings` twins gate (`crates/oz-bridge/src/settings.rs:779` and its neighbours: five on `SETTINGS_READ`, and `list_credit_sales_scoped` on `SALES_VIEW` under an F-017 comment that spells the exception out: "This one keeps sales:view, not settings:read — verbatim from the shell"). All six were already sitting on this shell's ledger as `resolves_session_names_no_permission` -- the ledger described the hole for weeks and nothing closed it, which is what a ledger without an owner is. THE SPLIT IS THE INTERESTING PART: a blanket "no settings reads for staff" would have been wrong, because `role-staff` holds `sales:view` and no `settings:*` key at all (`platform/core/src/rbac_presets.rs:136-159`), so a cashier listing the store's credit sales is authorized behavior while the same cashier reading receipt formatting is not. The three new cases therefore build their restricted session from a HAND-MADE role row carrying exactly `["sales:view"]` rather than from `role-staff`, so the grant decides each outcome and the test cannot drift with a preset edit. Ledger: 121 → **115** (rows for the six), then 115 → **110** (the five deletions, whose class was `no_session_resolution` 47 → 42; 42 + 68 = 110 closes ✓). Three sources agree: the ratchet 7/7, `cargo test -p oz-pos-tablet --lib` **667 passed / 0 failed**, clippy exit 0, and `.agents/measure_gate_gap.mjs` printing `debtRows=110 (DEBT_CEILING=110)`, `stale ledger rows … none`, and `gateAccepted` **202 → 208**, which is the six gates counted independently of both Rust and the ledger. One cross-lane closure to note: the deleted `list_credit_sales` carried a comment saying "**the repair is to gate it, not to delegate it**", referring to its scoped twin; `4bdb4b4d7` is that repair, so the note's TODO is now done and its text left with the code it described. WART, recorded because `--amend` is forbidden here: `4bdb4b4d7`'s subject ran on through a colon-bearing phrase ("… pin the sales:view exception these reads were not all the same command") and reads as a sentence missing its punctuation; the body of this record is the accurate version. (c) NOT DONE: the two live ones, now T11. -->

- [ ] **T11 · Migrate the two remaining renderer-supplied-identity settings writes, which are not Rust-only fixes.** `set_hardware_settings` and `set_setting` work today because `ui/src/api/settings.ts:139` and `:230` pass `userId` explicitly; that is the T4-1 argument rather than the T7-2 bug, and closing it means changing the caller, the command, or both, in a surface each shell registers differently — `set_setting` is the ONLY one of T10's seven registered in both shells (desktop registers it, tablet registers it plus its scoped twin), while `set_hardware_settings` is tablet-registered alone. Call sites are single and named: `ui/src/hooks/useTerminalHardware.ts:337` and `ui/src/frontend/shell/UpdateBanner.tsx:211`. Reachability is already established for the hardware one and not for the banner: `useTerminalHardware` is imported by `features/settings/workspace-cards/{TerminalPreferencesCard,WorkspaceRestaurantPosSettings,WorkspaceStorePosSettings}.tsx`, and T4-4 measured that the tablet mounts that same `WorkspaceSettingsModal` (`TabletAppShell.tsx:71-91`, `:125`) — so **a tablet operator saving hardware settings is naming their own identity in the payload today**. `UpdateBanner` is reached through `frontend/shell/AppLayout.tsx`, which is desktop-shell chrome; whether the tablet's `TabletAppShell` renders it is the open half of this row, and the answer decides whether `set_setting` needs a migration or just a deletion. Both shells already have `*_scoped` twins the renderer calls elsewhere (2 and 6 names respectively), so the migration is to repoint the wrapper and drop the legacy pair — a UI edit with a Rust deletion behind it, which is why it is its own row and not part of T10. <!-- MEASURED BOTH HALVES 2026-09-16, and the shape changed: neither half is what this row predicted. (a) THE HARDWARE HALF IS NOT THE LIVE BUG THIS ROW DESCRIBED. `ui/src/hooks/useTerminalHardware.ts:323-338` is already an ADR #7 conditional -- `if (sessionToken) await setHardwareSettingsScoped(sessionToken, dto) else await setHardwareSettings(dto, userId ?? '')` -- and the three workspace cards reach it through `hw.save(userId)` (`TerminalPreferencesCard.tsx:87`, `WorkspaceRestaurantPosSettings.tsx:118`, `WorkspaceStorePosSettings.tsx:103`), so a caller-named id is only ever sent on the NO-SESSION branch. The code's own comment at `:330-333` records why that branch is already dead on desktop -- `set_hardware_settings` is not registered there at all -- and re-measured here: tablet registers it and desktop does not, while `_scoped` is in both. A hardware save with no session token is not a thing a tablet can do today (the settings modal that hosts those cards opens from a logged-in shell), so this is the T4-4/T10 deletion case, not a migration: drop the `else` branch, delete the command, and the two shells agree. WHAT THIS ROW GOT WRONG, and the mechanism matters more than the sentence: it asserted "a tablet operator saving hardware settings is naming their own identity in the payload today", which reads the HOOK's existence as if it were the REACHED BRANCH. It is the same error this file has now caught four times in a different costume -- evidence one step short of the claim. (b) THE `set_setting` HALF IS A DIFFERENT AND WORSE DEFECT. Its only caller, `ui/src/frontend/shell/UpdateBanner.tsx:211`, passes the literal `'system_updater'` as the actor, and `git grep -rn system_updater -- crates apps ui/src` returns **exactly one line in the repository: that call site**. No user row, no seed, no migration, no constant. The command's check is real -- `crates/oz-bridge/src/settings.rs:1259` `ctx.require_permission_for_user(&store, user_id, permissions::SETTINGS_EDIT)?`, which lands in `Store::require_permission` -> `assignment_for_user(user_id)?` (`crates/oz-core/src/db/staff.rs:220`) -- so an id naming no user errors out BEFORE `run_set_setting` is reached. **The updater's preference write has therefore never worked, on either shell.** The banner's comment at `:204-207` states the unscoped form is used *deliberately* (no session token pre-login) and its `try { ... } catch { }` at `:212-214` throws the error away with the note "best-effort" -- which is the whole reason this is only now being written down: the renderer never reads the result, and the UI-side contract test asserts the payload, not the outcome. Pinned by `3debf7fec` "test(tablet): pin that the updater's hard-coded actor cannot write settings, which is the finding not the gate" (`set_setting_refuses_the_hard_coded_updater_actor_and_writes_nothing`, read back through `get_setting`, the same layer the write targets, so it encodes no table or column name); `cargo test -p oz-pos-tablet --lib` -> **668 passed / 0 failed**, clippy exit 0, parity exit 0. (c) BUNDLE REACHABILITY, which this row left open: `UpdateBanner` is mounted only by `frontend/shell/AppLayout.tsx`; `ui/src/frontend/shell/tablet/**` holds **zero** references to it and `TabletAppShell.tsx:5` imports `TabletAppLayout`. So the tablet registers a command its own bundle can never call (the T5-2 class, and the reason "UI command string" counts must never be read per-shell), while the shell that does call it cannot get the write to land. -->
- [ ] **T11b · Decide how a pre-login updater may persist a preference, then make the failure loud either way.** The write path above has three coherent repairs and they are not Agent-3's to choose between: **(i)** seed a real service identity (a `system_updater` user with `settings:edit`, which makes the current call correct and adds a row every other shell and the sync path must tolerate); **(ii)** an explicit unauthenticated allowlist of updater-owned keys inside `set_setting`, which narrows the door to the keys it needs and is a security-surface decision the owner should size; **(iii)** stop persisting pre-login -- keep dismiss/snooze state in memory or `localStorage` and write it once a session exists, which is the option that changes no permission model but does change what a user who updates before ever logging in sees. All three leave the fourth thing standing, and it is the one that can be done without the decision: **`UpdateBanner.tsx:212-214`'s empty `catch` is why a broken feature survived this long** -- replace it with a logged warning through whatever the shell's logger convention is (`loggedInvoke` already exists on the API side), so the next time a settings write is refused, something says so. Do not "fix" this by passing a real user's id from the renderer: that is the forgeable-actor door T4-1, T7-2, T8 and T10 have all been closing.

- [ ] **T12 · The hardware-settings unscoped write is not deletable surface, and the reason is better than the deletion would have been.** Attempted and **reverted** 2026-09-16. What I expected: `set_hardware_settings` is registered on the tablet only, its `_scoped` twin on both shells, and `useTerminalHardware.ts:334-338` reaches the unscoped form only when `sessionToken` is falsy — so delete the command, drop the `else`, and the shells agree (the T4-4/T10 disposition). What happened: `cargo test -p oz-pos-tablet --lib` stayed green at 668/0 and clippy at 0, and **`verify-ipc-parity.py` went red** — "tablet: UI invokes 'set_hardware_settings' but it is not in apps/tablet-client/src/lib.rs generate_handler (e.g. ui/src/api/settings.ts)". The renderer really does name the command, so the honest options were an allowlist entry (papering, refused) or a UI change behind it. Then the UI side refused too, and this is the part to keep: the fallback is not dead code but **specified behavior** — `ui/src/__tests__/useTerminalHardware.test.ts:223` is a case titled "save calls setHardwareSettings with DTO subset", `ui/src/features/settings/workspace-cards/types.ts:8` documents a shared prop as "User ID for unscoped API calls (e.g. setReceiptSettings, setHardwareSettings)", `test-utils/mocks/api.ts:144` mocks the wrapper, and `WorkspaceRestaurantPosSettings.test.tsx:99` stubs it. Four tests asserting `expect(mockSetHardwareSettings).not.toHaveBeenCalled()` elsewhere in that file are per-scenario, not global, and reading them as global is the mistake this row nearly made. **The finding the reversal produced:** the two branches do not write to the same database. The unscoped command takes `let conn = state.db.lock().await` — the GLOBAL identity DB — while `set_hardware_settings_scoped` takes `state.resolve_scope(&session_token)` and writes the session's STORE database (measured in the two bodies side by side, `apps/tablet-client/src/commands/settings.rs`). A printer or scanner setting saved while a session exists therefore lands somewhere a later read of the same setting may not look, and the hook's own ADR #7 comment (`:330-333`, which correctly notes the desktop never registered the unscoped form) does not mention it. So the migration is not a deletion but a decision about which store owns hardware settings — which is T4-3's storage question wearing the same clothes, and the reason the command is being left registered. The revert is `git checkout --` over exactly the four paths it touched, verified by `git status` empty, `verify-ipc-parity.py` exit 0, `measure_gate_gap.mjs` back at `registered=318 (REGISTERED_TOTAL=318) debtRows=110 (DEBT_CEILING=110)`, and no commit made because nothing was left changed. What to do with this row: settle the storage owner (T4-3), then either move the unscoped write onto the store queue or delete the `else` branch and its four tests together with the command, in one change that says which behavior is being dropped.

- [x] **T13 · The gate-detection vocabulary was the reason the divergence list read as 64 holes; corrected, and the ledger's class counts moved 110 → 90 (commit `f710680b3`).** Round 7 was meant to be "take the 92 divergences the tool enumerates, same proven method." It went to the largest cluster first — `inventory_counts`, ten commands classed `resolves_session_names_no_permission` on the tablet and `GATED` on the desktop — and found **nothing wrong with them**. The bug was in the instrument that produced the list. <!-- IMPLEMENTED 2026-09-16. HOW IT WAS MEASURED WRONG: `names_permission` in the tablet's `registration_gate_tests.rs` answered with `contains` over four markers — `require_permission`, `permissions::`, `has_permission`, `authorize_with`. But `commands/inventory_counts.rs` gates all ten of its commands through a domain helper, `require_inventory_count_permission(&state, &session.user_id)`, whose call sites contain none of those four strings (the actual `permissions::INVENTORY_COUNT` check sits inside the helper's body at `:178-186`, and the sweep scans per-command bodies). Same shape at `stock_transfers.rs:16-20` (`INVENTORY_TRANSFER`). So the sweep classed 20 gated commands as debt; the generated ledger, produced by that sweep, agreed; and the drift test comparing them passed, because both sides of the comparison come from the same predicate. Self-consistency is not correctness — this programme has documented that failure mode four times on the wire (two copies agreeing with each other and disagreeing with reality), and here the measurement layer was doing it to itself. THE CROSS-READ THAT WAS NOT A CROSS-READ: `.agents/measure_gate_gap.mjs:29` defines `gated = registered.has(n) && !debt.has(n)`, so it does not classify gates at all and inherits every miss above. The `DIVERGENCE … differentlyClassified` line quoted in the T7, T8 and T10 records as independent corroboration was therefore a copy of the instrument it was written to check. Both figures that depend on gate classification are re-read here: `gateAccepted` 202 → **228**, `differentlyClassified` 92 → 85 → **65**. Every such number in the earlier rows is superseded by these, and the cause is a detector change, not a code change: the code those rows described was already gated the whole time. THE DESKTOP HAD FIXED THIS AND THE TABLET NEVER GOT IT: `apps/desktop-client/src/commands/registration_gate_tests.rs:317` carries `GUARD_MARKERS` with ten bespoke names plus a test whose own comment states its purpose — "`drift_pin_guard_marker_vocabulary_is_closed` is what stops this list rotting again." The two files describe each other as mirrors, so this is a mirror-parity defect in a place nobody was counting. WHAT WAS DONE, AND WHY NOT THE OBVIOUS VERSION: the first fix was a loose `"_permission("` substring, which is the same mistake pointed the other way — any future `require_whatever_permission(` would then read as a gate silently. Replaced with the enumerated form: seven bespoke names this shell actually calls (12 / 12 / 11 / 11 / 8 / 4 / 1 occurrences), plus the ported closure test. THAT TEST CAUGHT TWO NAMES THE MOMENT IT EXISTED, and neither is a per-user permission check: `require_audit_tier` (`audit.rs:103`) returns `PermissionDenied` but inspects the SUBSCRIPTION plan, so treating it as a marker would make seven audit commands look RBAC-gated while a Premium-plan user holding no `audit:read` sails through; and `require_role_assignable` (`staff.rs:475` → `Store::require_role_assignable`) validates that a role assignment is legal, which is integrity around an authorization change, not the authorization. Both are exempted with those reasons in the code, because an exemption list is only honest if each entry says why. MUTATION PROOF, because a rescued green is not a fixed green: deleting the single `require_inventory_count_permission` entry from `GUARD_MARKERS` fails **four** tests at once, including the partition ratchet printing "Ungated now and NOT on the ledger (10 names, …)", so the 20-row deletion is now guarded by two independent mechanisms — the classification split and the vocabulary closure. The file was restored byte-identical (hash compared) and the suite returned to 9/9. VERIFICATION: `cargo test -p oz-pos-tablet --lib` → **670 passed / 0 failed** (was 668, +2 cases); `registration_gate` → **9 passed / 0 failed**; `cargo clippy -p oz-pos-tablet --all-targets -- -D warnings` → exit 0, after it correctly rejected the first draft of the loop; `verify-ipc-parity.py` → exit 0; `verify-scoped-coverage.sh` → PASS; `measure_gate_gap.mjs` → `debtRows=90 (DEBT_CEILING=90)`, `stale ledger rows absent from live list: none`, `registered=318 (REGISTERED_TOTAL=318)`. Arithmetic closes: 42 + 48 = 90. NO COMMAND BODY WAS EDITED — only the reader and the rows it had recorded, which is the correct shape for this kind of correction: the code was never wrong here, the ledger was. -->

- [x] **T14 · Mine the vocabulary asymmetry the other way: four guard names exist on the desktop and not on the tablet.** <!-- CLOSED 2026-09-16, no defect found; the amendment at the end of this row retracts the audit claim this row predicted, and notes that `AUDIT_READ` (named below) is not a real const — the bridge gates on `AUDIT_VIEW` and `AUDIT_EXPORT`. --> `GUARD_MARKERS` on the desktop (`:317`) lists `require_audit_permission`, `require_session_resource_permission`, `require_user_permission_scoped` and `authorize_topology_write`; none occurs anywhere under `apps/tablet-client/src/commands`. That asymmetry is a hint list for real holes — the opposite of what T13 just corrected — so it carries T13's caution: for each name, find the desktop bodies that call it, locate the tablet twin (if the tablet registers that command at all), and read **both bodies to the closing brace** before claiming anything, because a name-based sweep over this shell has now demonstrably been wrong in both directions. `authorize_topology_write` is the most likely live one (`crates/oz-core/src/topology.rs` is being edited by another lane as this row is written, and the tablet registers topology commands). The second question this row owes: whether the tablet's `audit` commands are gated at all, given `require_audit_tier` (a plan check) is their only guard-shaped call and there is no `require_audit_permission` helper in this shell — that is a genuine candidate for a missing `AUDIT_READ` gate, and unlike T13 it would be a hole in the code, not in the reader. <!-- MEASURED 2026-09-16, and it found NOTHING. All four names resolved to no defect, and the audit one resolved against me in a way worth more than a find. THE AUDIT CLAIM I MADE AND WITHDREW, IN ORDER: (1) I wrote above that the tablet's audit commands have only a plan-tier guard, because a whole-body scan of `apps/tablet-client/src/commands/audit.rs` found `require_audit_tier` in all seven and no permission const. (2) The scan was right about the text and useless as a conclusion, and it surfaced when I went to state the ledger consequence: I wrote "these sit in `resolves_session_names_no_permission`" and then looked, and **the tablet ledger has no `audit::` rows at all** — the sweep classes all six registered commands GATED. (3) Reading one body properly explains it: `list_audit_log_scoped` (`apps/tablet-client/src/commands/audit.rs:137-149`) is a pure shim — `require_audit_tier(&state)` first, then `let ctx = state.bridge_ctx(); oz_bridge::audit::list_audit_log_scoped(&ctx, &session_token, args)`, and the bridge's version at `crates/oz-bridge/src/audit.rs:123-129` does `resolve_scope` → `require_audit_tier(ctx)` → **`require_audit_permission(ctx, &session.user_id, permissions::AUDIT_VIEW)`**. So the per-user check runs on the tablet too, one call deeper than the line I was reading. The severity story I drafted (cashiers exporting audit logs on Premium plans, auditors exporting what desktop forbids, with role grants cited from `rbac_presets.rs:88-89`, `:202-203`, `:267`) is retracted in full — the grant facts in it are true and the inference from them was not, which is the most dangerous kind of finding: correct citations around a wrong conclusion. (4) The tier ordering that looked like the hole is deliberate and documented: the tablet's comment at `:141-142` says "this shell's strict tier gate, ahead of the bridge's own (wider) copy". THE HONEST ARITHMETIC OF THIS ROUND: my own earlier rows in this file — T7's shallow-window trap, T13's marker vocabulary — name the same failure I just repeated for the third time: **deciding from a text region instead of from the call graph**. A command body that mentions `require_…_permission` and one that delegates to a function that mentions it are the same thing at runtime and different things to a grep, and the sweep's `oz_bridge::` + gated-stem rule is therefore a **proxy, not a proof**: it reads GATED whenever a body names the bridge and that bridge MODULE names a permission ANYWHERE. That is sound enough to find what T13 found (where the calls really were in the bodies) and unsound enough to hide a command that delegates only a query helper and skips a gate. Nothing here proposes changing it — the alternative is no classification at all — but every "GATED" this file cites from it should be read as "not recorded as debt", and a security claim in either direction needs the body read, as this one finally was. THE OTHER THREE NAMES CAME UP EMPTY, and their emptiness has the same shape: they were read out of the desktop's `GUARD_MARKERS` **vocabulary list**, which records spellings, not usages. `authorize_topology_write` — the tablet registers **zero** `topology::` commands, so there is no surface to compare. `require_session_resource_permission` and `require_user_permission_scoped` live in the bridge (`regional.rs`, `ctx.rs`, `topology/commands.rs`) and in that marker list; the only place they appear in the desktop's own command layer is the list itself, and the tablet's two regional commands (`get_regional_config_scoped`, `set_regional_config_scoped`) both call `require_permission_for_session` in their own bodies. Checked and not defects, recorded so nobody re-derives them from a vocabulary list. WHAT T14 DOES LEAVE BEHIND, as its only surviving artifact: `require_audit_tier(state: &AppState)` takes no user and answers `PermissionDenied` on a subscription tier, so T13's exemption entry for it stays exactly right — it is a real gate for a real question, and it is not evidence that a permission was ever checked. VERIFICATION FOR THE CLOSED ROW: no code changed; `cargo test -p oz-pos-tablet --lib` -> **670 passed / 0 failed** and `verify-ipc-parity.py` -> exit 0 across the T5-5 commit that shares this round (`683c9f2b9`). -->

- [ ] **T15 · Make the picker-ticket refresh failure visible in the hot-swap path.** From T5-5, whose Rust half is done and whose UI half is not. `ui/src/contexts/WorkspaceContext.tsx:361-368` calls `refreshPickerTicket(prev)` inside an inner `try`; its `catch { }` says "Refresh failed — use the existing ticket" and lets `ticket` keep the value from `pickerTicketRef.current ?? ""` (`:359`) — the login-time ticket, whose TTL is five minutes — which is then handed to `createSession` at `:375`. So the caller cannot tell a fresh ticket from a stale one, and neither can anyone reading the log: on a shell where the command does not exist (every tablet before `683c9f2b9`) this block failed on **every** hot-swap and looked like nothing. Two changes, both small and both behavior-preserving except for visibility: log the failure with the reason — this same file already has the idiom, measured from a run rather than from a search: `WorkspaceContext: failed to create session token` and `WorkspaceContext: boot store resolution failed, using default` are printed by `createSession`/store-resolution failures in this context, so the refresh `catch` should follow that shape (`WorkspaceContext: picker-ticket refresh failed, reusing the login-time ticket`) rather than inventing one, and say in the comment which ticket the user is about to get and what happens when it has expired. Not in scope here: deciding whether the fallback should instead abort the swap — that is a product call about whether a stale ticket is a recoverable or a fatal condition, and it needs whoever owns the workspace-switch flow, not a refactor lane. Verify with `cd ui && npx vitest run src/__tests__/WorkspaceContext.test.tsx src/__tests__/WorkspaceContextSessionTokenFailure.test.tsx src/__tests__/workspaceContextValueStability.test.tsx` — all three exist and reference this path (measured by `git grep -ln 'performHotSwap\|hotSwap\|refreshPickerTicket' -- ui/src/__tests__`), and the middle one is by name about session-token failure, which is exactly the branch being touched — plus `python scripts/verify-ipc-parity.py`, which is what caught the missing handler in T5-5 and may have opinions about the mock again. <!-- DONE 2026-09-16, commit f483b0ab5 (2 files, +80/-2). THE CHANGE AS SCOPED: `ui/src/contexts/WorkspaceContext.tsx`'s empty catch now warns in the idiom this file already uses on its other failure paths — `console.warn("WorkspaceContext: picker-ticket refresh failed, reusing the login-time ticket", err)` — and the comment says what the fallback actually carries: the LOGIN-TIME ticket, TTL five minutes (`PICKER_TICKET_TTL_SECS` in `crates/oz-bridge/src/auth.rs`), i.e. possibly already aged out; and why nobody noticed for six rounds — the tablet had no such command until `683c9f2b9`, so every tablet hot-swap threw into this branch and looked identical to a healthy swap. Log-only, deliberately: the swap recovers, so raising `sessionError` or a toast would alarm the user about a condition that usually has no effect. THE FIND THAT OUTGREW THE ROW: **the success branch had no test at all.** Chain of evidence, since a claim like this needs one — (a) `ui/src/__tests__/WorkspaceContext.test.tsx:209-213` defaults `refreshPickerTicket` to REJECT, with a comment that independently documents the blind spot from the test side: "Before this mock existed, refreshPickerTicket was `undefined`, so the call threw synchronously into the same `catch` and every hot-swap test took the 'use the existing ticket' fallback. A rejected promise reaches the identical branch, so this preserves the existing suite's behaviour while making the other branch **reachable**." (b) `git grep -n 'mockResolvedValue.*picker\|refreshPickerTicket.mockResolved' -- ui/src/__tests__` returns **zero** hits, so reachable is as far as it ever got. (c) Two new cases, one per side of the `try`. (d) THE MUTATION, because a new green proves nothing on its own: with `ticket = refreshed.picker_ticket;` deleted from the source, `npx vitest run src/__tests__/WorkspaceContext.test.tsx -t 'picker-ticket refresh'` fails `AssertionError: expected 'ticket-abc' to be 'ticket-refreshed'`, `1 failed | 1 passed | 25 skipped`, and the file came back byte-identical by hash. So this row's real product is the lesson stated plainly: **reachable is not exercised** — a suite can configure a mock to make a branch *possible* and still never run it, and the branch that never runs is exactly the one the missing backend command had been starving. WHAT THE NEW TESTS DELIBERATELY DO NOT PIN: whether a stale ticket should ABORT the hot-swap. The fallback case asserts the swap still completes and announces itself; freezing abort-vs-continue would be a product decision taken by a test, and that ruling joins the two already outstanding (the release-profile fail-closed question, and whether this lane may keep editing `crates/oz-bridge`). VERIFICATION: `npx vitest run` over the three WorkspaceContext suites -> **33 passed / 0 failed** (was 31, +2); `npx eslint` on both touched files -> exit 0 with no output, which is a real gate here because ESLint does read `.tsx` (unlike the CSS case AGENTS.md documents); `npx tsc --noEmit` reports nothing naming either file (not a claim that the tree typechecks — other lanes have dirty files); `verify-ipc-parity.py` -> exit 0; and the pre-commit hook's bundle-parity step scanned `ui/src/contexts/` and printed `0 missing key(s)`. SIDE EFFECT WORTH NOTING, and it is the point of the change: two other tests in that file now print the new warning line to stderr, because they take the fallback path. Silent-before, noisy-now is the intended direction; the `vi.spyOn(console, 'warn')` in the fallback case is restored in a `finally` because the file's `afterEach` only calls `clearAllMocks()`, which does not restore spies. -->

- [x] **T16 · Key rotation's dev-mock handler was answering a command that was deleted for being an ungated bypass. It now answers the one that exists (commit `2422eddfe`).** <!-- DONE 2026-09-16, 2 files, +15/-2. WHAT WAS THERE: `rotate_encryption_key` -- the UNGATED variant -- was deleted from both shells, and `ui/src/api/security.ts:31` records why ("that command was ungated and is deleted"), and `ui/src/__tests__/api-security-contract.test.ts:38-44` deleted its three test cases on the grounds that "a test asserting that a wrapper exists for it would be a test that resurrects the bypass". The mock registry still had `'rotate_encryption_key': () => ({ success: true, … })` in `ui/src/dev-mock/handlers/system.ts`, and the tablet's allowlist still carried an exemption for it at `:159`. So the discipline the contract test applied was real, and the two files that were not under its spell had drifted anyway -- the bypass lives on in exactly one place in the repo, and it is the place no gate reads. WHAT SHIPPED: the handler moved to `'rotate_encryption_key_scoped'`, the name the desktop does register (`apps/desktop-client/src/lib.rs:1151`), and the dead exemption was deleted. WHY NOT JUST DELETE THE HANDLER, which was my first move and is the part of this row worth reading: I had measured the mock registry for "keys no shell answers and no UI names" and got **164**, which looked like a large class of dead dev-mock surface. That number was wrong by 20x, and the error was exactly the one this file keeps re-filing against other people. `'rotate_encryption_key'` is the SOURCE of an alias: `applyScopedAliases` copies an unscoped handler onto its `_scoped` name, so an unscoped mock key whose own name nobody types is still load-bearing the moment `x_scoped` is registered or invoked anywhere. With that term added the class collapses to **7**, and `rotate_encryption_key` is not among them -- deleting its handler would have silently taken `rotate_encryption_key_scoped` out of the dev-mock, which is precisely the T5-5 failure (a mock that returns null and a caller that renders its failure path) that this programme spent a round finding. The handler was therefore moved, not removed, and nothing regresses either way because no UI code names either spelling yet (`api/security.ts:34`: "key rotation has no front door, which is the point, not an oversight"). THE GATE THEN CAUGHT MY FORMATTING, AND LIED ABOUT IT: my string splice ate a newline and glued `  'set_brand_primary_colour': () => null,` onto the preceding `}),`, and parity went red with "UI invokes 'set_brand_primary_colour_scoped' but no handler anywhere under ui/src/dev-mock registers it … and **no unscoped twin exists for the alias rule to reach**". Both halves of that are worth noting: the parser finds handler keys anchored at line start, so a key that exists mid-line is invisible to it -- a real defect it happened to catch here, but a *cosmetic* cause producing a message that asserts a false condition about the tree. Unwrapped the line; parity OK. Third distinct appearance of T12's aliasing mechanism (first: why `set_hardware_settings` could not be deleted; second: why the picker-ticket mock had to be added by hand; now: why 157 of 164 apparent orphans are not orphans at all). VERIFICATION: `python scripts/verify-ipc-parity.py` -> `IPC parity: OK` exit 0, with the tablet's allowance count 155 -> 154; `npx eslint src/dev-mock/handlers/system.ts` -> exit 0; `npx vitest run` over `api-security-contract`, `dev-mock-scoped-aliases` and `SessionLockScreen` -> **80 passed / 0 failed** (the third file was included because it reads `handlers/system.ts` as TEXT at `:190-194` and asserts properties of its contents, so it is the only test that could have objected to editing that file's shape; it did not). -->

- [x] **T17 · Give the parity gate the reverse dev-mock check, and fix the line-blind handler parse it just exposed.** Three things, in decreasing certainty. (1) **A handler key that is not at the start of a line is reported as absent.** This is established by demonstration, not by reading the regex, and I did not confirm which construct causes it: with my splice leaving `  }),  'set_brand_primary_colour': () => null,` on one line the gate said *"no unscoped twin exists for the alias rule to reach"*, and unwrapping the same bytes onto their own line made it pass — so the extraction is insensitive to something purely positional (`read_dev_mock_sources` / `parse_dev_mock` / `answerable_sets` are the candidates, `scripts/verify-ipc-parity.py:226-339`). Consequence: Prettier, a merge, or a careless splice can turn a green into a red whose stated reason is false about the tree — the handler was there the whole time. Either make the parse position-tolerant or make the message say "not found at line start", so a formatting cause cannot masquerade as a missing handler. A fixture case for it belongs in that file's `--self-test` suite (`case(...)` entries around `:1672-1718` are the shape), because today nothing in the repo would notice if a refactor moved every key onto the same line as its predecessor. <!-- ITEM (1) DONE 2026-09-16, commit 0b1681c4a (+116/-0, purely additive: no existing line in the gate was changed). THE CHOICE WAS THE OPPOSITE OF WHAT THIS ROW SUGGESTED FIRST, and the reason is the anchoring itself: MOCK_LITERAL_KEY_RE's `^[ \t]*` is not laziness, it is what keeps the scanner from collecting quoted keys nested inside handler return payloads, so making the parse position-tolerant would have traded a rare false red for a permanent false widening. Instead the gate now NAMES its own blindness. WHAT SHIPPED: `MOCK_MIDLINE_KEY_RE`, byte-identical to the anchored pattern except that it requires a non-space character before the quote -- the difference is position and nothing else, which is what makes the result a measurement of the blind spot rather than a new claim; `find_midline_handler_keys(sources)`, which subtracts the names the anchored parse already sees and skips whole-line comments through the same `_mock_code` the parser uses (one source of truth, per T13's rule about not writing a second parser); the NOTE appended to the dev-mock gap message -- "'x' sits mid-line in <path> -- the key exists, and it is THIS PARSE that is line-anchored. Unwrap it onto its own line and re-run before accepting the gap as a missing handler." -- built from the command and, for a `_scoped` name, its unscoped seed, since that is the pair the alias rule actually consumes; and an `info[dev-mock]:` count line that prints only when the class is non-empty, because the class measures **0 across the whole current tree** (re-derived here, and it is the same number my throwaway probe read before I deleted it -- the probe was how I knew to expect silence, not the evidence). PROOF IN BOTH DIRECTIONS, four fixtures beside the file's existing real-tree guards: the anchored parse really misses a glued key; the scan reports exactly that key and only it; the same key unwrapped is visible to the parse AND unflagged by the scan (without this case the scan could be "everything not on a fresh line" and still pass the first two); and a key-shaped string inside a comment is not a key. `python scripts/verify-ipc-parity.py --self-test` -> `self-test: 23 cases + 12 extra guards, 114 assertions total; real tree 542 registered + 147 aliasable from 17 files`, `self-test: OK`. THEN THE INTEGRATION, on the real file rather than a fixture, because the fixture proves the function and not the wiring: I reproduced my own round-9 accident by gluing `set_brand_primary_colour` back onto the preceding `}),`, and the run came back red with the NOTE naming the key and `ui/src/dev-mock/handlers/system.ts`; the file was restored byte-identical (hash compared) and the clean tree returns `IPC parity: OK` exit 0 with no INVISIBLE line. CORRECTION TO THIS ROW AS WRITTEN: it pointed at `case(...)` entries "around `:1672-1718`" as the fixture shape to follow. Those are allowlist-document fixtures; the mock parser's own guards are the ones introduced by `real_per, real_loop = parse_dev_mock(read_dev_mock_sources())` (about `:2444` before this change), and that is where the four cases went. The line numbers in this row are pre-change and were not re-derived after the +116. ITEMS (2) AND (3) DONE 2026-09-16, commit db2035376 (+69/-0, again purely additive). `find_unreachable_mock_keys(per_file, shell_registered, ui_named)` implements the three-route rule verbatim -- a key is alive if a shell registers it, if UI code names it, or if it is an unscoped name whose `_scoped` twin takes either of the first two routes -- and main prints it as `info[dev-mock]: 7 handler key(s) across 4 file(s) reach nothing ...`. Two things came out better than expected. FIRST, THE INDEPENDENT CONFIRMATION: the gate's own parse sees 542 registered mock keys where my throwaway probe saw 521 -- two different extractors with a 21-name gap -- and both land on the SAME 7 dead names. Agreement between instruments with different populations is weaker evidence than agreement between identical ones, so read it as the definition being robust and the individual counts as not comparable. SECOND, THE FIXTURES PROVE THE ROUTES, not just the answer: `reach  registration, a UI name, or a consumed _scoped twin each keep a handler alive`, and its pair `reach  an unscoped seed dies with the twin nobody consumes` -- without the second, a scan that simply ignored the alias term would still pass the first. `--self-test` now reads "23 cases + 14 extra guards, 116 assertions total; real tree 542 registered + 147 aliasable from 17 files", `self-test: OK`, and the bare run is unchanged at `IPC parity: OK` exit 0, which is the point of shipping it informational. WHAT IS LEFT: the disposition of the seven and the promote-to-blocking choice, both moved to T18 below with the two misclassification risks named. --> (2) **Nothing asks whether a mock handler is reachable at all.** The gate checks the direction it needs for its own defect class (UI names X → something must answer X) and never the reverse (a mock answers Y → does any shell register Y, or does any UI name it, or is Y the alias seed of something that is?). The corrected definition and its measured population are in T16 above: 521 mock keys, 219 alias seeds with a live consumer, and **7 keys that are neither** — `delete_workspace_instance_scoped`, `generate_kds_pairing_token_scoped`, `get_product_stock`, `get_products`, `list_screens_scoped`, `search_products`, `set_default_instance_scoped`. Read those seven as a starting census, not a verdict: three unscoped product names whose `_scoped` twins are registered is a plausible live-seed case my own alias term may still be mis-scoring, so re-derive before deleting anything, and define the check against the alias loop rather than against a regex over text. (3) **Put it in the Python gate, not in a new TypeScript test.** A second parser of the same three sources is how T13's ledger and cross-read came to agree with each other and disagree with reality; the gate already extracts UI commands, both shells' registrations and the mock's answerable set, so an informational count there costs one print. Suggested graduation: print informationally until it reads 0 honestly, then make it blocking with accepted gaps going into the existing `dev_mock` allowlist section, which already supports `{"name", "reason"}` objects and is schema-shared through `scripts/allowlist-schema.py`.

- [x] **T18 · Dispose of the seven unreachable dev-mock handlers, and decide whether the count should ever block.** `scripts/verify-ipc-parity.py` now prints them on every run (`info[dev-mock]: 7 handler key(s) across 4 file(s) reach nothing -- no shell registers them, no UI code names them, and they seed no _scoped name that either does`): `delete_workspace_instance_scoped`, `generate_kds_pairing_token_scoped`, `get_product_stock`, `get_products`, `list_screens_scoped`, `search_products`, `set_default_instance_scoped`. Two readings belong before any deletion, because they are the two this programme has already been fooled by. (a) The four `_scoped` names cannot be alias seeds by construction -- `answerable_sets` only copies onto a `_scoped` name that is NOT already registered (scripts/verify-ipc-parity.py, the `f"{base}_scoped" not in registered` term) -- so they are handlers for commands no shell registers; but the set arithmetic compares BARE names, while `generate_handler!` spells `module::command`, so a shell that registers the same command under a different module prefix would be missed. Check that with `git grep -n '<name>,' -- apps/desktop-client/src/lib.rs apps/tablet-client/src/lib.rs` before calling any of them dead. (b) A handler that answers nothing today is also a handler some screen will silently need: the `rotate_encryption_key` case in T16 is precisely a mock that stayed alive past its command's deletion, and the fix there was to move the handler onto the surviving name, not to drop coverage. That is the lesson that makes bulk deletion the wrong first move. THEN THE PROMOTION DECISION, which T17 deliberately did not make: the count is informational because a mock key with no consumer is dead weight, not a defect, and this tree is full of surfaces built before their callers. Blocking needs a reason-carrying section (the `dev_mock` allowlist already accepts `{"name", "reason"}` objects, and `scripts/allowlist-schema.py` is the shared schema), and if it does become blocking it needs self-test cases for the ACCEPTANCE path, not only the detection path that ships today. <!-- DISPOSED 2026-09-16, commit 3f026e6a2 (4 files, -11 lines): all seven deleted, and the gate's census now reads zero. WHOSE ARE THEY: each of the seven occurs in exactly one file in the whole repo -- its own handler block. Verified with git grep over ui, apps, e2e, scripts, platform and crates for both quote styles: catalog.ts, workspaces.ts (x2), system.ts, staff.ts, and nothing else. So no test, no screen, and no E2E spec names any of them. WHAT THEY WERE, since "delete seven mystery handlers" is not a record: two are clean rename leftovers -- get_products and search_products for a surface the renderer actually calls list_products / list_products_scoped (registered on both shells, named in ui/src/api), and list_screens_scoped for list_workspace_screens(_scoped), which BOTH shells register and the UI names. One is a soft-delete substitution: delete_workspace_instance_scoped sits directly above archive_workspace_instance_scoped in workspaces.ts, and archive is the name the desktop registers and the UI calls -- the mock kept the hard-delete the product decided against. set_default_instance_scoped is likewise superseded by set_user_workspace_instances_scoped. get_product_stock has no successor at all (the live stock surface is adjust_stock / get_low_stock_alerts / list_warehouse_products_at_location, and no get_product_stock* is registered anywhere), and generate_kds_pairing_token_scoped is a handler for a command that was never written in either shell -- git grep for 'pairing' across both generate_handler lists and ui/src/api returns nothing. That last one is the interesting case for the census: a mock that answers a command nobody implemented is how an unbuilt feature reads as working in browser-preview mode, which is exactly the rotate_encryption_key shape from T16 minus the deleted-command part. THE PROOF THAT ZERO IS A LIVE ZERO: a green count of nothing is the least trustworthy number in this programme, so the check was re-armed by planting 'zz_probe_dead_key' into workspaces.ts -- the gate printed 'info[dev-mock]: 1 handler key(s) across 1 file(s) reach nothing ... zz_probe_dead_key' and IPC parity stayed OK (informational, as designed) -- then the file was restored byte-identical by hash and the line went silent. VERIFICATION of the deletion itself: eslint over all four touched files -> exit 0 (which is also what caught the first, broken attempt -- see below); npx vitest run src/__tests__/dev-mock-scoped-aliases.test.ts src/__tests__/SessionLockScreen.test.tsx -> 78 passed / 0 failed (the second reads handlers/system.ts as text and would have objected to a structural change); npx tsc --noEmit -> 0 errors for the whole project, so no import or unused-symbol broke when the last reference to a handler shape went; MOCK_PRODUCTS is still used 13 times in catalog.ts, so deleting its two consumers did not orphan the fixture. THE ROW'S FIRST ATTEMPT BROKE THE TREE AND THE LINTER SAID SO: a block-aware deletion script of mine scanned forward for the first line ending in a comma, which for a single-line handler whose BODY contains commas inside braces ('generate_kds_pairing_token_scoped': () => ({ ... token: ..., expires_at: ... , })) cut two lines out of four and left 'expires_at' stranded -- eslint: "539:3 error: Parsing error: ',' expected". Reverted all four files to HEAD (git status clean, eslint exit 0 confirming the restore rather than trusting the command), then read the seven blocks and deleted them by exact literal text, which also produced a diff I could eyeball line by line: 11 removed lines, nothing added, nothing glued. The round-9 newline-eating accident and this one are the same mistake in two different tools -- a heuristic splice where an exact match was available -- and both were caught by a linter rather than by a test, which is the lesson about which gate to trust for structure. THE PROMOTION DECISION, which this row asked for, is DECLINED, with the reason on the record: a handler with no consumer is dead weight, not a defect, and mocks legitimately land before their commands -- the browser preview is built ahead of the Rust surface in this repo (that is how 217 invokes across 4 commands were once left answering null, per the gate's own header). Blocking would grade authoring ORDER, which no other gate here does, and a red CI for "you wrote the mock first" teaches people to add a reason string rather than to think. Keep it printed, keep it at zero, and revisit if a dead handler is ever shown to have misled someone -- the T16 case is that evidence, and it argues for the NOTE and the count, not for the block. The acceptance-path self-test cases this row asked for are therefore NOT needed: there is nothing to accept. WHAT THE DESKTOP SLACK LEG FOUND ON THE WAY, so nobody re-files it: apps/desktop-client registration_gate_tests.rs has REGISTERED_FLOOR = 453 (already at live) and checks it only for the >= leg, while its pair leg is measured.abs_diff(debt::REGISTERED_TOTAL) <= REGISTERED_SLACK -- an accepted band, not the equality the tablet file asserts. So the desktop's printed '453 (ledger REGISTERED_TOTAL=447)' is a stale generated snapshot inside a sanctioned slack, not the false pair this row's sibling fixed on the tablet, and its own failure text says raising the floor belongs with a JOURNAL pass naming each addition. Checked and not a defect; the lag it shows is the missing-generator gap already recorded under T7, not a new one. -->


- [ ] **T19 · The 118 tablet and 43 desktop unregistered command fns that the gate can finally see, and what to do about them.** Filed by the fix that made them visible. `scripts/verify-ipc-parity.py`'s F-006 leg counted `#[tauri::command] pub fn` on one line with the attribute adjacent to the signature, so it saw **20** of this shell's command declarations and printed **0 unregistered tauri command fns** on every run of every round of this programme. Three stages, each with its own measurement: recognising the imported short form `#[command]` (which is how every tablet command module is written) took the visible set to 326 and the count to **59**; then `command_fns_in()` learning to skip doc comments and stacked attributes between the token and its signature took it to 441 visible and **122** unregistered; and this row's own first deletion — the four T5-4 stubs — moved it to **118**, which is the proof that the number now tracks the tree instead of the parser. The desktop went 16 → 16 → 43: its 16 were always visible (it spells the attribute out), so the desktop's delta is the second fix alone. THE SPLIT, computed with the gate's own `extract_ui_commands()` rather than a second scanner, so the units match what the tool prints: of the tablet's 118, **17 are named by production UI code** — the renderer invokes `print_receipt`, `open_cash_drawer`, `list_products`, `adjust_stock`, `lookup_by_barcode`, `list_scanners`, `set_terminal_override`, `delete_terminal_override`, `get_product_track_serial(_batch)` and others against fns no shell registers (those are functional gaps already carried by the UI leg's 154-name allowance, so parity is green on them *by exemption*, not by absence of problem) — and **101 are named nowhere at all**: unreachable Rust surface, of which the four in `staff.rs` are now gone. On the desktop, 3 UI-named and 40 unreachables. HOW TO WORK THE LIST, in the order that respects what this programme has learned: (1) start with the modules where a `_scoped` twin is registered AND the UI has been migrated to it, because there the unscoped fn is the residue T5-4 just deleted and the deletion is a `pub use`/test audit away; (2) for each candidate, read the body to its closing brace before calling it dead — a stub returning `PermissionDenied` and a fn with a live implementation are different dispositions even when both are unregistered (T5-4's four were stubs; several of the 101 will not be); (3) check the dev-mock and the ledger for each name, because an unregistered name can still be an alias seed in `ui/src/dev-mock` (T16/T17: 219 of 521 mock keys are seeds) and deleting the mock's side of a rename is how you take out the wrong half; (4) never delete in bulk — the 496-vs-453 note below is the record of what a confident number from a partial parser looks like. THE RECONCILIATION THIS ROW OWES, and it closes an open question in the file rather than raising a new one: an earlier pass wrote "496 `#[tauri::command]` definition sites across 59 files against 453 registered paths … parity grades a name-level form of the same gap and prints 16 … 43 != 16 is a unit difference — sites against names — and this pass did not reconcile them". It was not a unit difference. `command_fns_in()` now reports **43** unregistered command fns for the desktop, and 496 − 453 is 43; the two figures the note could not connect were the same population measured by a parser that could see only part of it. Its closing instruction — "a future one that quotes either must say which" — is honoured here: 43 is names, and it is no longer 16.
  **CENSUS CORRECTED AND THE LEG NOW CLASSIFIES, 2026-09-16 (commits d130a7df8 + 00eca795f, scripts only).** The paragraph above says "the rest are dead code" on the strength of *not being UI-named*, which is one signal short: an fn the registry refuses can still be called by the shell's own Rust. The leg now answers that itself and prints the split, because a probe written for this row got it wrong in three separate ways in one sitting, and the next lane will meet the same three: **desktop 43 = 3 UI-named + 1 called-by-own-code + 39 uncalled; tablet 118 = 17 UI-named + 0 helpers + 101 uncalled (5 of those reached only by their own tests: `create_exchange_rate`, `delete_offline_item`, `list_audit_log`, `pending_offline_count`, `sync_run`)**. THE THREE FALSE POSITIVES, in the order found. (1) `store.create_bundle(&bundle, &items)` is `Store::create_bundle`, a different function sharing a spelling, and counting it turned 101 unreachable fns into 78 "live helpers wearing a stale attribute" -- the T13 collision, reproduced inside this programme's own tooling, one layer over. (2) The block I wired into `main()` passed the loop's outer `lib_path`, a leftover from an earlier section still pointing at the *other* shell, so the desktop's 43 fns were graded against the tablet's sources and printed 3 helpers. It agreed with nothing and I only caught it because an independent probe said 32 and a number that disagrees with the tool that produced it is a finding. (3) The `.`-exclusion I had just written was **dead code**: the regex lookbehind `(?<![\w.])` already did that job, so my mutation test of the branch came back green and proved nothing about the mechanism it claimed to test. Fixed by making the branch the only mechanism, after which disabling it fails two fixtures as it must. THEN THE ONE THAT SHIPPED WRONG: with all three repaired, the desktop reported **32** helpers, and 31 of them were the shim pattern this entire programme exists to produce -- `pub async fn get_receipt_settings(…)` whose body is `oz_bridge::settings::get_receipt_settings(&ctx)`. A lowercase module segment is not evidence of *which* crate's function is meant. `FOREIGN_PATH_ROOTS` now drops workspace-crate paths and keeps `crate::`/`super::`, which takes the desktop from 32 to **1** genuine helper: `register_terminal`, called from `sync_bootstrap.rs:231` -- a real internal use, in the file that has been dirty in another lane for six rounds, so it is neither deletable nor touchable today. **The wrong 32 is in the history** (`d130a7df8` shipped it; `00eca795f` retired it), and disabling the rule reproduces 32 exactly, so the two commits together are the mutation test of the claim. WHAT THE NUMBERS NOW SUPPORT: 140 unreachable-and-uncalled legacy command fns across the two shells (101 + 39), each a real body superseded by a registered `_scoped` twin whose own implementation routes through the bridge rather than calling it -- which is why both shells show almost no helpers: the shared code is `crates/oz-bridge`, not the legacy fn. Deletion is therefore behavior-neutral *in the shell*, and the standing caution from T5-3 still applies per name: read the comment above the body before treating an unreachable fn as unwanted (T5-4's four were documented refusals), check the mock tree for the same spelling (T16/T17), and never bulk-delete: work it module by module, biggest first, with the leg's own count as the receipt -- it moved 122 -> 118 for the four in T5-4, and will move again per batch. A tool limitation worth recording for the next pass: AGENTS.md's graph-first rule could not be followed here -- `codebase-memory` is invoked as `tools.mcp__cbm__*` from `run_code`, and this session exposes no such tool -- so every caller figure above came from the grep path, which is the step the skill itself demands before believing any graph answer, but is not the substitute it recommends (measured cost of that: three of the four bugs above were call-shape errors a resolved call graph would not have made). **FIRST BATCH PAID, 2026-09-16 (commit aec68bc24, one file, +6/-99):** the six unreachable legacy bundle commands in the tablet (`list_bundles`, `get_bundle`, `create_bundle`, `update_bundle`, `delete_bundle`, `lookup_bundle_by_sku`), chosen as the largest clean self-contained cluster and graded per name on five signals before the cut -- absent from `generate_handler!` (the file registers only its six `_scoped` twins), named by no UI code, called by no production code (the leg's own classification, printed 0 helpers for this shell), absent from the shell's ledger rows, and unreferenced by `bundles_tests.rs`, which covers the scoped half. The leg moved **118 -> 112** and the uncalled population 101 -> 95, which is the receipt this row asked for. ONE THING A DELETION-CHECKLIST MUST CARRY, found only by reading what was left behind: the scoped twins each open with `/// Session-scoped variant of \`create_bundle\`.` -- six doc lines naming the functions being removed. Deleting the bodies without rewriting them leaves the surviving API documentation pointing at nothing, which is precisely the rot class this file already records about other artefacts, so they now read "Create a bundle resolved from a session token. ADR #7." in the idiom `apps/desktop-client/src/commands/settings.rs` uses. The DTOs stayed deliberately: `CreateBundleArgs` is still the scoped twin's own argument type, so this batch is a command removal and not a surface removal. VERIFIED: `cargo test -p oz-pos-tablet --lib` -> 667 passed / 0 failed (no test lost, because none of them named the deleted six); clippy `--all-targets -- -D warnings` -> exit 0, which is also the check that the surviving imports (`BundleItem`, `ProductBundle`, `command`) are still needed; `registration_gate` -> 9 passed; `verify-scoped-coverage.sh` -> PASS; parity exit 0. REMAINING, from the same print: 95 tablet candidates and 39 desktop ones, module by module -- and the two files still dirty elsewhere (`audit.rs` holds 1, `terminals.rs` holds 7 plus the one genuine desktop helper `register_terminal`, used from `sync_bootstrap.rs:231`), which are not this batch's to touch. **BATCHES 2-4, same day (commits 095e744d0 purchasing, f465c3d31 tables, 768a1f36b gift_cards):** ten legacy purchasing commands (-194 lines), nine table commands (-139), eight gift-card commands (-121), on top of the six bundles -- **33 fns and about 650 lines gone, and the leg's own count moved 118 -> 112 -> 102 -> 93 -> 85**, which closes on 118 minus the four batches exactly. Every batch graded on six signals now, two of them added by this round's findings: (5) **crate closedness** -- `git grep -l 'oz-pos-tablet' -- '**/Cargo.toml'` returns only `apps/tablet-client/Cargo.toml`, so nothing outside the shell can call these `pub` fns and "no caller inside" really does mean dead, which the first batch had assumed without proving; (6) **the prose left behind**, which turned out to be three different kinds and is the reason a deletion is not finished when the bodies are gone -- the scoped twins' `/// Session-scoped variant of \`x\`.` lines (26 of them across four files, each naming a function being removed), one **block comment with a live argument inside it** (tables.rs's T8 note ended "the six unscoped variants above still take `user_id` ... touching them would re-open the caller-named-actor door", which the deletion falsifies, so it now says the door is closed by absence instead), and a **user-facing error string in the shared store advising a command that does not exist** -- `Store::set_table_status` in `crates/oz-core/src/db/tables.rs` said "occupied requires an active sale - use assign_table_order", while `assign_table_order` is registered in neither shell (verified through the gate's own `extract_handlers`, both shells `False`) and the scoped form is registered in both. That one is fixed in d7f68d9b3, and it was found only because a residue grep after a deletion happened to hit a quoted copy of the message in a test comment. The core test asserts `message.contains("active sale")` and the UI's copy is a mock rejection, so the tail was free to change; the tablet test quoting the message verbatim was updated in the same commit, and its `tables.rs:256-263` citation became a fn name, because the line numbers moved when the comment was added. TWO WAYS THIS ROUND BROKE ITS OWN BUILD, both caught before a commit and both worth the mechanism: first, hand-typing the replacement strings per batch, where one batch's hashtable values omitted the `///` prefix and produced eight bare prose lines -- the parser then swallowed the attributes and `cargo clippy` came back with `expected one of ! or ::` plus a cascade of missing `__cmd__*_scoped` errors; the file was restored from HEAD (`git checkout --`, my own uncommitted edit, nothing of anyone else's) and redone. Second, the mechanical replacement loop that was supposed to remove the hand-typing derived **0 of 8** doc texts -- a nested-loop `$Matches` clobber, this programme's recurring PowerShell trap -- so the fallback wording went in lowercase and had to be polished to house style afterwards. The standing additions to the procedure, therefore: assert that no line of a Rust source starts with a capital and ends in a bare period (that is what a lost `///` looks like), and read the compiler output before the error cascade -- `expected one of` named the file and line, the `E0433` flood only named the victims. PROGRESS POSITION: 85 unregistered tablet fns = 17 UI-named gaps + 68 uncalled leftovers, and the desktop's 43 = 3 + 1 helper + 39; `audit.rs` (1) and `terminals.rs` (7) remain off-limits while other lanes hold them, and `sync.rs` (7), `offline.rs` (8), `pos.rs` (13), `promotions.rs` (7), `customers.rs` (5), `product_variants.rs` (5), `categories.rs` (3), `exchange_rates.rs` (3), `refunds.rs` (3) and the singles in `currencies.rs`, `void.rs`, `hardware.rs`, `scale.rs`, `products.rs` are next, with the five whose only references are their own tests (`create_exchange_rate`, `delete_offline_item`, `list_audit_log`, `pending_offline_count`, `sync_run`) needing a decision about deleting tests alongside -- the T5-4 pattern, where the bridge already carried the same cases under the same names, may or may not repeat. **BATCHES 5-6, 2026-09-16 (commits de0fe143f product_variants+promotions, 08df953d9 customers+categories, 828aaa79f + 586a58071 the tool):** twenty more retired -- five product-variant, seven promotion, five customer, three category -- and the leg moved **85 -> 73 -> 65**, closing on 85 minus twelve minus eight. This round the procedure was mechanised rather than repeated, because both of last round's build breaks were hand-typing: `scripts/retire-legacy-commands.py` (dry run by default, `--apply` to write) computes its candidates through the parity gate's own extractors -- the T13 single-source rule -- so a name is only retired if it is unregistered in this shell, unnamed by UI code, and uncalled by production code; it locates every span from **shape** (scan back over attributes and doc lines from the signature, forward to the column-zero brace) rather than from line numbers, and it refuses a file that is dirty in another lane, which `audit.rs` and `terminals.rs` proved it needed on first use. The wording for a surviving twin is harvested from the retired fn's own `///` line instead of being typed, and three invariants are asserted before anything is written: no line of the result may be a bare English sentence (that is exactly what a lost `///` looks like -- the failure mode that cost last round a build), the `#[command]` attribute count must fall by exactly the number of spans, and no retired name may still have a signature line. It also **reports the prose it cannot rewrite** rather than leaving it silently, which is where the round's two real finds came from. FIND 1: `categories.rs`'s module doc opened "Exposes `list_categories`, `create_category`, `update_category`, and `delete_category` to the front-end" -- three of those four commands were registered in neither shell, so the paragraph that tells a reader what this module offers was describing a surface no renderer could invoke. It now names the four that are registered and says plainly that the other three were retired. FIND 2, and the more interesting one: `customers_tests.rs` carries the CRM-02 finding as a comment -- "the tablet registered the LEGACY unguarded get_customer (no session, no permission, global db) while the desktop had moved to get_customer_scoped". That history is in the past tense and was true, and deleting the fn does not make it false, so it was **annotated rather than removed**: the note now records that the legacy fn was retired here and, crucially, that it had already been absent from `generate_handler!` in both shells by then -- which is the difference between a deletion that changes behavior and one that cannot, and it is the question any future reader of that comment will ask. WHAT THE TOOL DELIBERATELY DOES NOT DO, stated in its own docstring so its silence is not read as agreement: it does not touch imports, and clippy immediately earned that boundary (`unused import: require_permission_for_user`, `promotions.rs:8` -- the same import the tables batch had to drop by hand one round earlier); it does not delete tests that reference a doomed fn, because that is a decision with a precedent to check (T5-4 found the bridge already carried the same four cases under the same names; `offline.rs`'s two and `sync.rs`/`exchange_rates.rs`'s test-referenced candidates have not been compared yet); and it does not audit prose in a module it has nothing to retire from, which is a stale-document question, not a batch question. OPERATIONAL NOTE: one commit came back `exit=128 / Unable to create .git/index.lock` because another lane committed in the same second (`ed75932bd`). The correct response was to wait and retry -- the lock is another process's, and removing it by hand is exactly the corruption the repo's no-`git add` rule exists to avoid -- and the retry landed clean at 08df953d9 with all three files. POSITION AFTER SIX BATCHES: tablet 65 unregistered fns = 17 UI-named gaps + 0 helpers + **48 uncalled**, desktop 43 = 3 + 1 helper + **39**. Nothing about `registered=319` moved in any batch, which is the point: this is removal of surface that was never exposed, and the per-shell ratchets (`DEBT_CEILING=90`, `REGISTERED_FLOOR=319`) stayed untouched through 53 deletions. **BATCHES 7-9, 2026-09-16 (bd20fcfcd products+void+scale, da9240229 refunds+currencies, 6aa4cba00 hardware+exchange_rates, de6f55b8b the tool):** eleven more, and the leg moved **65 -> 61 -> 57 -> 54**, closing on the eleven exactly. Position: tablet 54 unregistered fns = 17 UI-named gaps + 0 helpers + **37 uncalled**; desktop still 43 = 3 + 1 + 39. TWO PROSE FINDS, both in paragraphs that had already done the analysis: `refunds.rs` carries a note from the 2026-09-15 wire audit which says, of the fn this round deleted, "This is NOT a data-loss fix. `process_refund` is registered in neither shell", with the two greps that prove it (the emphasis is mine, the sentence is the file’s) -- so the deletion was already argued for there, and what changed was the tense ("was registered") plus a new closing passage, because the paragraph's framing was "a latent trap exactly one `generate_handler!` line from live", and once the fn is gone it is not a trap, it is a closed door. The note stays, since it is the reason the DTO direction was chosen, and its cited greps still answer the same way: `ui/src/api/sales.ts:657` invokes the scoped command, and the unscoped name survives only in the dev-mock and in `dev-mock-envelope-shapes.test.ts`. `currencies.rs`'s module doc was simpler: it described the file as a `list_currencies` migration, and after the retirement that sentence named a fn that no longer exists, so it now lists what the front-end can actually invoke (`currency_info`, `list_currencies_scoped`, `get_default_currency`, `set_default_currency` and the scoped pair). `create_exchange_rate` was **left in place** -- not an oversight but the tool's new refusal, described next. WHAT THE TOOL LEARNED, three gates (de6f55b8b): `path_refs()` reports mentions of a candidate name with **no parenthesis**, because `fn_call_sites` looks for `name(` and a `generate_handler!` entry, a re-export list, or a function pointer has none -- a real gap in the evidence, though the retroactive answer is reassuring and worth stating: a path mention of a deleted fn is a compile error, so 667 passing tests across eleven batches is proof that no dangling paren-less reference survived any of them; the compiler is the backstop under the whole census. `--only a,b` refuses any name that is not a live candidate rather than guessing which of them are registered. And `--apply` now **refuses a candidate that a test file still references** unless `--allow-tests` is given, citing T5-4, because deleting such a fn means deleting or re-homing cases -- a decision with a precedent to check (in T5-4 the bridge already carried the same four cases under the same names) and not a side effect. THE DISCLOSURE THIS ROUND OWES: the first version of that test gate did not gate, it **crashed** with `NameError: name 'test_hits' is not defined`, and my own check masked it -- I filtered the tool's output for `abort|PATH|note:`, saw nothing, and printed "the apply was blocked; file unchanged" from the file's line count. A crash and a guard leave the same file state and mean different things, so the re-test was run with unfiltered output, where the block now reads as intended and names both the candidate and the test file. POS DELIBERATELY NOT TAKEN: `pos.rs` is the largest single prize left (13 fns, -366 lines by the tool's own count) and the only module whose stale-prose report points at *live* advice -- a `FRONTEND-03` helper doc ("Resolve the unit price for an `add_line` request") that survives the deletion and must be re-worded rather than templated, and an `ADR-19 §5.1` comment inside the cart path. A module carrying a numbered design decision gets read before it gets cut, not at the end of a round with a commit budget running down. `sync.rs` (7, one test-pinned) and `offline.rs` (8, two test-pinned) are the other pending decisions, and `audit.rs` (1), `terminals.rs` (7), `inventory_counts.rs` and `subscription.rs` remain held by other lanes. **BATCH 10 -- POS, 2026-09-16 (commit 32482f475, 2 files, +3/-369, the largest single diff of this programme):** the thirteen unscoped POS commands (`start_sale`, `add_line`, `set_cart_discount`, `override_line_price`, `override_cart_deduction_location`, `complete_sale`, `list_active_carts`, `get_active_cart`, `hold_cart`, `get_held_cart`, `list_held_carts`, `list_open_bills`, `delete_held_cart`), taken after the read that the previous round deferred rather than at the end of one. The leg moved **54 -> 41**, and 41 = 17 UI-named gaps + 0 helpers + 24 uncalled. Both pieces of live advice the batch had to survive were re-pointed at the scoped names rather than templated, and the file's own convention chose the wording for me: the error inside the surviving cart helper already reads "create via `start_sale_scoped` first", so the FRONTEND-03 helper doc now says "an `add_line_scoped` request" and the ADR-19 §5.1 comment says "reject `add_line_scoped`". One self-inflicted defect caught in the same pass: my first replacement of that comment **truncated it** -- I dropped the trailing " lock." while fitting the line, leaving "reject add_line_scoped when the cart has no deduction location", an incomplete sentence about a guard whose whole subject is the lock. It reads correctly now; the general rule is that a shortened line is a text change, not a formatting change, and rustfmt will not tell you.
 Then the queue-string question, because the residue grep put `"complete_sale", "void_sale"` in my face from a file I was not editing: could the offline queue replay be **dispatching on command names as strings**, which no call-site scan can see? Answered by measurement, not by comfort -- the only production mention is an example inside a doc comment (`offline.rs`: "The action to perform (e.g. ...)"), no `match` on action strings exists in the shell, and the UI's replay path invokes `pending_offline_count_scoped` (`ui/src/api/offline.ts:78`). So `action` is opaque data and the deletions cannot strand a queued operation; the example is updated to the scoped names anyway, since both commands it illustrated are now gone. The scan that found it is now **crate-wide** (`d8cc3b687`) rather than per-file, for exactly that reason: prose about a command survives in whichever comment found it interesting, and a module-local report cannot see across the seam -- on `sync` it now surfaces 13 mentions including two in `offline.rs`. **BATCHES 11-12 AND THE INSTRUMENT'S OWN AUDIT, 2026-09-16 (5a7e89f46 desktop purchasing+customers, 7087877ce tablet offline+sync, and the tool fixes inside them):** the retirement crossed to the other shell at last -- the desktop's ten unregistered purchasing and five unregistered customer commands (+11/-197), which is the first tablet-external batch this programme has made and the reason T19 was written as a two-shell census rather than a tablet tidy-up. Then the tablet's six offline and six sync commands, taken as explicit `--only` subsets so the three test-pinned names stayed behind on purpose. Receipts: **desktop 43 -> 28**, **tablet 41 -> 29**; the leg's own arithmetic closes on both (10+5, 6+6). Position now: desktop 28 = 3 UI-named + 1 helper + 24 uncalled; tablet 29 = 17 UI-named + 0 helpers + **12 uncalled, and every one of those twelve is deliberately blocked** -- as measured then: five by tests and eight in files another lane holds, which was not even a partition -- `list_audit_log` sat in both halves. **Two of those five pins proved to be phantoms the same day** (see the paragraph below), so the true figure at that moment was three, and `offline.rs` was never blocked at all. The sentence as first written named four and summed four with eight to reach twelve, which is arithmetic that only looks right because the shared member got counted on both sides of a partition that was never disjoint -- the same shape of error as a call-site census that counts a store method and a command as one signal. Stated as the leg prints it: 12 uncalled across five files. **BATCHES 13-16, THE DESKTOP CENSUS CLOSED, 2026-09-16 (462685933 reports, 3d0aac586 branding+settings+staff+authz, 942320d15 hardware+locations+sync+currencies+exchange_rates, cb1fbc798 and cb411d00d the tool):** the desktop went from 24 uncalled to **1**, and the F-006 leg reads **5 = 3 UI-named gaps + 1 helper + 1 uncalled**, that one being `create_exchange_rate` -- left deliberately, and the reason it had to be left is the finding of the round. Receipts: **43 -> 28 -> 11 -> 5**, closing on 8 + 9 + 6 = 23 retired (the desktop's 24 minus the one kept). Programme total, checked against the leg rather than against my own arithmetic: tablet 118 -> 29 and desktop 43 -> 5, so **127 unreachable command fns retired across both shells**, every per-shell ratchet untouched the whole way. THREE OF THE FOUR DESKTOP `staff.rs` CANDIDATES WERE DOCUMENTED REFUSALS, and the tool's path-mention gate is what made me read that: the bodies are `Err(AppError::PermissionDenied("legacy unscoped staff commands are disabled; use X_scoped"))` under a "**Deprecated for multi-store (ADR #7)**" doc, exactly T5-4's shape, and the file's `/* */` docs-auditor stamp from 31-08-26 calls all four of them "permission-denied tombstones". They went anyway, on evidence the stamp could not have had: **each is registered in neither shell**, so the refusal was never reachable, and the message described a door `generate_handler!` never opened. The stamp's own sentence was left intact and annotated in place (REV 2026-09-16, naming what changed and what did not), because a dated audit record is the reading it was; `authz.rs`'s list of writers was re-pointed at `create_staff_scoped`, which is where the writes actually go. THE MISTAKE OF THE ROUND, disclosed in full: `--apply` on `desktop/exchange_rates` took **`create_exchange_rate` with the other two**, several minutes after I had written down that it must stay because T20 is an owner decision. Nothing refused: the test gate knows whether a *test* names a fn, and no desktop test does. So the desktop half of a pending decision was pre-empted by a batch flag, and the two shells were left asymmetric -- the tablet's copy still standing, the desktop's gone -- which in a programme about removing divergence is the specific harm. Recovered by `git checkout HEAD --` on that one file and re-applying with `--only delete_exchange_rate,list_exchange_rates` (161 lines either way, the ADR #48 paragraph back at `:49`/`:53`), and then made impossible: **`plan_mentions()` reads the top-level `todo-*.md` work orders as a fourth signal**, and `--apply` refuses any candidate a plan file names unless `--only` states it, because typing the name is the human act the gate cannot fake. It earns its keep immediately -- it reports `sync_run` named in *another lane's* plan (`todo-refactor-devmock-router-consolidation.md:213`) as well as in this one, a cross-lane deferral no census in the world would have surfaced. TWO MORE INSTRUMENT DEFECTS FIXED THE SAME DAY: block-comment interiors were being read as CODE, which is why a stamp landed on the path-mention gate as a phantom function pointer and why a retired name inside a stamp would have been invisible to the stale-prose report (fixed by `prose_flags()`, which tracks `/*` ... `*/` state, so `staff.rs:4` moved from "PATH MENTION" to "stale prose", where it belongs); and the first version of the plan gate matched `(?<![\w`])`, excluding the backtick that is precisely how a markdown file writes a command name, so it found nothing anywhere -- and my own filtered test run reported that as "no PLAN MENTION" in the same breath it would have reported a crash, the masking failure I recorded on 2026-09-16 and repeated here verbatim, third instance: **a filtered run cannot distinguish "no findings" from "no function"**, so the plan gate was finally tested with unfiltered output, where it refuses with the two plan lines quoted and exits 2 while the file stays at 161 lines. **AND THE SIGNAL THAT HAD NEVER EXISTED (committed as 956ab3f6f, the last act of the desktop census):** while verifying why the tablet's `DEBT_CEILING` had moved from 90 to 89 without my touching it, `measure_gate_gap.mjs` led to `apps/tablet-client/src/commands/registration_gate_debt.generated.rs` -- and the path this tool had been reading was `.registration_gate_debt.generated.rs`, **with a leading dot, a file that does not exist**. So the `ledger=no` printed for every one of 127 retirements came from an empty string. Two independent reasons it could never have fired: the wrong path, and a pattern looking for `"<name>"` when every row is `("pos::add_line_scoped", "resolves_session_names_no_permission")`, module-qualified -- and third, its population is *registered but not gate-accepted* commands, disjoint by construction from a candidate set defined as unregistered. Right answer throughout, dead instrument throughout, which is the worst combination because `no` reads like a passed check and I quoted it 127 times as one of six signals. What replaced it is the parity allowlist read as **whole text through the gate's own `read_allowlist_text()`**, not a forked parser (the fork is what produced the shape mismatch): its `_comment` key is where commands kept on purpose are written ("the unscoped names stay, because the ADR #7 conditional still falls back to them", naming `create_backup`, `get_backup_status`, `export_data`, `import_data`, `import_preview`), so a candidate matching any allowlist line now prints a KEEP MENTION and blocks `--apply` unless `--only` states it, sharing the refusal path with the plan-file gate. THE CAPABILITY WAS PROVEN BEFORE THE SILENCE WAS ACCEPTED: the three remaining candidates print `allowlist=no` -- `sync_run`, `create_exchange_rate`, `delete_offline_item` -- which is the same word the broken check printed, so the check itself was run against a name known to be there: `create_backup` -> 2 lines (`:2` the comment, `:44` its entry) and `export_data` -> 2 lines, against 233 lines read. **A check that reports nothing because nothing matches is worth a different sentence from one that reports nothing because it cannot match, and the only way to tell them apart is a positive control.** Also verified, not assumed: none of ten sampled retired names appears in either generated ledger, so no batch of this programme left a stale debt row behind -- and the 90 -> 89 movement in the tablet ceiling is not mine; the ledger's population is registered commands, and my work never touched a registered name (`registered=319` tablet / `453` desktop held through all 127 deletions). The tablet's thin-shell census is finished as work that needs no decision. FOUR FIXES TO THE BATCH TOOL IN ONE ROUND, each surfaced by a different signal, which is the point worth keeping: (i) the attribute-count assertion compared `^\s*#\[command\]` while the **desktop spells it `#[tauri::command]`**, so it reported "fell by 0, expected 5" -- the same spelling blindness as the F-006 leg's original regex, three instances now of one defect class in my own tooling; (ii) the crate-wide scan listed the module being retired **twice**, once as its proposed text and once as its on-disk text, because the dedupe compared a basename to a relative path; (iii) widening the twin-doc matcher to `(?:Session-)?Scoped` to fit the desktop's idiom **silently stopped matching the tablet's lowercase `Session-scoped`** -- a matcher that got narrower while being made broader, caught only because a module that had always rewritten every twin printed "rewritten: 0 (of 6)"; the rule taken from it is to read the count a rewrite claims, never just that it ran; (iv) `for label, text in scan:` rebound the outer `text`, and since Python has no block scope the attribute assertion then compared *an unrelated file's* counts against the result, reporting "fell by -10" -- an arithmetic impossibility that is the only reason I looked, and the second recurrence of the stale-`lib_path` family from 2026-09-16. Plus a plain portability bug: the residue report prints real source lines, some carry an em dash or a section sign, and a Windows console defaults to cp1252, so the tool died with `UnicodeEncodeError` **out of the middle of its own diagnosis**; stdout is now reconfigured to UTF-8 with replacement rather than the text being sanitised. What the prose work was after that: every site was re-pointed from its consumer, not from the census -- `RequeueRemoteFailureArgs` is taken at `offline.rs:392` and `SyncPullArgs` at `sync.rs:459`, both inside registered scoped commands, so those two struct docs name the scoped pair; the OFF-09 ordering comment turned out to describe **`Store::list_pending_offline`, a store method that survives**, so it now says so instead of leaving a bare backticked name ambiguous between command and query; the two test-section headers were renamed to the `run_*` helpers they actually head (`run_list_remote_failures_empty_db` and friends never called the commands); and `sync.rs`'s module doc, which had presented `sync_run` as the file's headline, now states what is invocable -- the eleven registered names include `sync_run_scoped` and `test_sync_connection`, and exactly one call site in `sync_tests.rs` keeps the unscoped form alive, which is why the tool's new refusal is what stopped a batch flag from deleting it. THE MIRROR WART IS CLOSED (`8824b164c`): `apps/tablet-client/AGENTS.md` denied in two places that `dev-ci.yml` has a push trigger and cited root `AGENTS.md` as agreeing; `dev-ci.yml:3-8` declares `push: branches: [main]`, and the root file names that exact denial as one of two false CI claims it corrected. The meta-finding is why a correction pass missed it: **`verify-agents-mirrors.py` compares root `AGENTS.md` with `.agents/AGENTS.md` only** -- its "all 2 mirrors agree" is a statement about two files, and a per-directory mirror is invisible to it, so the checker's green proved nothing about the sentence I had just read. Two of my own PowerShell failures are in that commit's history too (a here-string that made `" + [char]0x2014 + "` literal, and an apostrophe inside a single-quoted string), and the parenthesis imbalance my first repair introduced was caught and fixed before the commit.
 **THE SIXTH EXCLUSION -- PINS THAT WERE NEVER PINS, 2026-09-16 (f16889f5d the instrument, 2273d8218 the retirement, 996bb10e5 the superseded split annotated at source):** the refusal gate that had been holding four tablet names back for rounds counted a *test function named after the command* as a call of the command. `apps/tablet-client/src/commands/offline_tests.rs` opens two cases as `fn pending_offline_count()` and `fn delete_offline_item()`, and their bodies call `store.pending_offline_count()` and `store.delete_offline_item(&item.id)` -- the `Store` methods, which the very first exclusion in `fn_call_sites` already rejects. So the only "caller" in the whole crate was the test title itself, matched because the definition-skip above it is anchored on `pub` and Rust test cases are private. Both commands are retired here (**tablet 29 -> 27**, `429 -> 405` lines, 2 fns, attributes 10 -> 8, tests still 667/0, because those two cases were testing `Store` all along and never touched the command). THE MECHANISM, stated where it belongs: `fn_call_sites` carries five exclusions, each bought with an incident, and all five were earned against **production** sources; the same function is then handed the test files and the exclusions are trusted to carry. They did not, because one keyed on a visibility qualifier test code never writes. A filter list is a property of the corpus it was fitted to, not of the classifier. THE PINS THAT WERE REAL, each checked by eye rather than by the aggregate that had just lied: `sync_run` at `sync_tests.rs:528` (`let result = sync_run(app.state()).await.unwrap();` -- a free call taking a `State`, which is the command); `create_exchange_rate` at four sites in `exchange_rates_tests.rs` (`:22`, `:37`, `:52`, `:68`, the CUR-05 validation cases T20 is about); `list_audit_log` at `audit_tests.rs:232`. The distinguishing token is one character wide: whether the line reads `fn name(` or `name(`. The leg moved with the fix and closed on the three: **`12 unreachable and uncalled (3 of those still tested)`**, where it had printed 5. Self-test assertions 136 -> **137**, the case asserting `"m.rs:10" not in call_sites and "m.rs:11" not in call_sites` over fixtures reading `fn create_bundle() {` and `async fn create_bundle() -> Result<(), E> {`, while the pre-existing exact-list case still demands precisely `m.rs:1`, `m.rs:4`, `m.rs:9` -- which is what proves the new exclusion removed definitions without swallowing the genuine calls. AFTER THIS: 10 tablet names remain uncalled -- seven in `terminals.rs` and one in `audit.rs`, both held dirty by other lanes -- plus the three real pins, one of which (`create_exchange_rate`) is the same name the other two shells still carry.

- [ ] **T20 — ADR #48's documented global-catalog path has no reachable entry point in either shell, in three copies.** Filed by the tool's refusal rather than by a plan: `--apply` declined `create_exchange_rate` because four cases in `apps/tablet-client/src/commands/exchange_rates_tests.rs` name it, and reading why before overriding turned up a contract nobody can reach. THE EVIDENCE, each line re-derivable: (1) the tablet's `create_exchange_rate` carries "Legacy compatibility command: it operates on the global catalog database… That is the deliberate, documented contract for the global path - see **ADR #48 (Decision 3)**… **Do not fix this to a timezone**: with no store in scope there is no location whose zone could apply", and the desktop's copy at `apps/desktop-client/src/commands/exchange_rates.rs:55-63` carries the **same paragraph word for word**, so this is one decision stated in three places rather than two shells disagreeing (unlike T5-3, where the difference was the point). (2) ADR #48 is real: `docs/decisions/2026-09-09-timezone-representation.md`, titled "ADR #48: Location Timezone Representation & as_of Se…" — checked because this file has already shipped a comment citing a commit object that does not exist. (3) Neither shell registers it: `git grep -n 'create_exchange_rate' -- apps/desktop-client/src/lib.rs apps/tablet-client/src/lib.rs` returns **only** the two `create_exchange_rate_scoped` lines (`desktop:937`, `tablet:519`), which is every registration form there is, not just the module-qualified one the F-006 leg reads. (4) The shared implementation is `crates/oz-bridge/src/currency.rs:206` `pub async fn create_exchange_rate(ctx, args)`, which takes `ctx.lock_global()` and repeats the same ADR #48 paragraph at its `:201-205` — and **nothing calls it**: the only bridge call site in either shell is `create_exchange_rate_scoped` (`desktop:136`). (5) The tablet fn is not even a shim of the bridge's: its body re-implements the path against `CurrencyRepository::create_exchange_rate` with its own `Utc::now().format("%Y-%m-%d")` default, so the two global-path copies could drift today and nothing would notice. (6) The comment at `exchange_rates.rs:89-90` states the purpose plainly — the legacy commands "are kept only as compatibility wrappers for **single-store deployments**" — and an unregistered command is not a compatibility wrapper for anything, because no client can invoke it. WHY THIS IS NOT A DELETION: retiring it would decide that single-store compatibility is dead, which is the owner's call and not this lane's, and ADR #48 Decision 3 would then describe a path that exists nowhere. The three options are (a) register the scoped-only reality and amend ADR #48 to say the global create is administrative/never client-reachable, (b) register the unscoped form where single-store deployments still need it, which re-opens the ADR #7 unscoped-write question T10 has been closing everywhere else, or (c) retire all three copies and the ADR paragraph with them. RECOMMENDATION: (a) — the registry has already voted; the scoped form is what both shells have shipped, and the honest fix is to stop documenting a door and start documenting its absence. IF A FUTURE PASS PICKS (c) OR (a)-with-deletion: the four CUR-05 cases are the only coverage of `validate_create_rate_args` at command level, but that validator is **shared** with the scoped path (the tablet's `:18-19` says so explicitly: "used by both the legacy and scoped command paths so the two cannot drift"), so re-pointing the four cases at `create_exchange_rate_scoped` preserves the coverage and the deletion is not a hole — which is the T5-4 shape (duplicates found elsewhere) and its opposite (here the tests are the only instance, so they must move, not go). Also in scope for the same sweep: the dev-mock key `'create_exchange_rate': () => null` at `ui/src/dev-mock/handlers/catalog.ts:365`, which per T16/T17 is load-bearing as an alias seed until someone proves otherwise, and the desktop's 39 uncalled copies, which are the same question in bulk.

- [ ] **T21 - ADR #7's no-session fallback arm calls commands registered in neither shell, and both instruments that could see it answer as if the door exists.** Found by the leg added at 6d0ad430e, not by hand. THE SHAPE: the UI pattern is `sessionToken ? () => scopedWrapper(sessionToken) : plainWrapper`, so with a token the scoped door is used and without one the unscoped door is used -- and the unscoped door is not registered. `info[tablet-fallback]` names **3** such commands with the file and line that reaches them: `list_products` (`ui/src/features/products/useProducts.ts:132`), `list_scanners` (`ui/src/features/sales/useBarcodeScanner.ts:125`), `stop_scanner` (same hook, `:71`); `info[desktop-fallback]` names **8**, including `export_eod_report` (`EodReportScreen.tsx:431`), `get_sale` (`SalesHistoryScreen.tsx:401`), `list_categories` and `list_sales`. All three tablet names have a real `#[command]` body in the shell (they are three of the 17 in T19's UI-named class), so this is not a missing feature -- it is a missing line in `generate_handler!`. WHY NOTHING CAUGHT IT, and this is the finding rather than the list: Vitest asserts the branch fires (`useBarcodeScanner.test.tsx:77`, `:322` expect `listScanners` to have been called, `:84`/`:273` expect it NOT to be called on the token path -- a well-written contract test of a call that cannot succeed in production), and the dev-mock answers the key (`ui/src/dev-mock/handlers/system.ts:501`), so in dev every click works, in CI every test passes, and only a real build with no session token finds a `command not found`. Two instruments agreeing with each other and disagreeing with reality: the same defect class as the sweep markers (T13), the alias seeds (T16), the shim bodies (T19) and the vacuous ledger (round 20). MY OWN RETRACTION FIRST: a probe written earlier in this round printed `tablet_has_fn=n` for all 154 allowlisted names and `desktop_has_fn=n` even beside `desktop_registered=Y`, and I read that as evidence that peer lanes had moved bodies into `crates/oz-bridge` and registered bridge paths directly. False: the probe's regex lacked `re.M`, so `^` matched only each file's first line. The correct instrument -- the same scan with `(?m)`, which produced T19's 17 -- refutes it. Recorded because a broken column that produces a *plausible story* is more dangerous than one that produces an error, and because I reasoned out loud from it before checking. WHAT TO DO, with the constraint that makes it a decision: `apps/tablet-client`'s debt ledger sits at its ceiling (`debtRows=89`, `DEBT_CEILING=89`), and none of the seventeen bodies resolves a session or checks a permission, so registering one as-is adds a debt row and the gate fails -- correctly, because an ungated door is what that ceiling exists to hold. Options: (a) gate then register: the hardware module's own doc states the rule for commands that touch no database row ("the tablet authenticates with `resolve_session` rather than `resolve_scope`"), so `list_scanners` / `stop_scanner` / `start_scanner` / `open_cash_drawer` / `print_receipt` have a written convention to follow and registering them after adding `resolve_session` closes the fallback without inventing a policy; (b) change the UI arm so the no-token case fails with a message instead of a Tauri lookup error, and allowlist the intent; (c) leave it and let the allowlist keep carrying the names as if they were product choices. RECOMMENDATION: (a) for the hardware five, which the file already licenses, and (b) for `list_products` / `list_categories` / `list_sales` / `get_sale` / `export_eod_report`, where an unscoped read is exactly what ADR #7 exists to stop -- registering them would resolve the store from nowhere. Do not raise a ceiling to make any of this green. The leg is informational by policy, like F-006: `IPC parity: OK` while it prints 3 and 8, because whether the fallback should exist is an owner ruling, not a regex. Five self-test assertions back it (131 -> 136), including a real-tree case that goes red if the shape is ever fixed -- and the case that failed first was my own, asserting an empty result while passing a genuine gap in the same list: a self-test that can only fail is as useless as one that can only pass. TWO MEASUREMENTS THAT CHANGE THE SHAPE OF THIS, both from the same leg. (1) **Option (a) is closed by the ceiling rule, and I had recommended it before reading that rule.** `drift_pin_debt_ceilings_only_shrink` (in `apps/tablet-client/src/commands/registration_gate_tests.rs`) pins `DEBT_CEILING` as a floor you may only lower, and a registered command counts as gate-accepted *only* if its body names a permission -- `GUARD_MARKERS` plus an inline `"domain:action"` literal, nothing else. The hardware commands resolve a session and name no permission, so **the six scoped twins already registered in the tablet are sitting in the debt ledger right now** (`hardware::list_scanners_scoped`, `stop_scanner_scoped`, `list_displays_scoped`, `display_show_scoped`, `display_clear_scoped`, `discover_hardware_scoped`), which is 6 of the 89 rows at the ceiling. Registering an unscoped fallback would therefore add a seventh and eighth row of the *worse* class (`no_session_resolution`), and the gate would refuse -- correctly. Following the hardware doc's "resolve_session is enough for a door that touches no row" would not help: the ledger's own vocabulary says that state is debt, not acceptance. (2) **Every fallback gap has a working door beside it, registered in the same shell.** All three tablet names and all eight desktop names have `<name>_scoped` present in `generate_handler!`: `list_products_scoped`, `list_scanners_scoped`, `stop_scanner_scoped` on the tablet; plus `export_eod_report_scoped`, `get_sale_scoped`, `list_categories_scoped`, `list_sales_scoped`, `read_scale_weight_scoped` on the desktop. So this is not a missing feature and not a registration oversight to be patched by opening another door: the UI's else-arm names a command that was never registered while the command that *is* registered sits one line away in the same file. The decision narrows to (b1) make the no-token arm fail with a message a user can act on, or (b2) delete the arm as dead code where the screen cannot mount before a session exists -- and which of those applies is per site, which is why the ten call sites are named individually above rather than as a count. `useProducts.ts:132`/`:133` are the interesting pair: if a product screen cannot be reached without a session, that fallback is dead UI code with a live dev-mock behind it, answering for a command neither shell registers. SEED CENSUS, THE FOLLOW-UP I OWNED: after 127 retirements, are any dev-mock alias seeds orphaned? Walked every key in `ui/src/dev-mock/**/*.ts` at the handler indent: **278 unscoped keys**, of which the criterion "its `_scoped` twin is registered in neither shell and the bare name is registered in neither" returns **5**, and all five are `admin`, `auditor`, `manager`, `owner`, `staff` -- role labels in a mock data map that my two-space indent regex mistook for handlers, not commands. So the answer is zero stranded seeds, with a named false-positive set rather than an unexamined zero, and the lesson kept: I looked at the five names before acting on the count, which is the step that turns a phantom list into a clean bill.

- [ ] **T22 - "The UI invokes it" counted a wrapper export as a caller, and 11 of the tablet's 17 registration gaps turned out to be dead surface on both sides of the boundary.** Filed 2026-09-16 (39dc0313d the leg, 460bd4712 the first tranche). WHAT THE NEW LEG SAYS: `info[<shell>-uinamed]` splits the F-006 leg's "named by UI code" set into commands reached by a screen/hook or the programmatic client, and commands whose only UI presence is an `export const` in `ui/src/api/*.ts` plus the contract test that pins the string inside it. Tablet: **17 = 6 reachable + 11 unreferenced** (`create_product`, `delete_product`, `delete_terminal_override`, `get_product_track_serial`, `get_product_track_serial_batch`, `list_terminal_overrides`, `lookup_product_by_sku`, `open_cash_drawer`, `print_receipt`, `set_terminal_override`, `update_product`); desktop: 3 = 1 + 2. WHY THE CONTRACT TESTS ARE NOT CALLERS: `api-*-contract.test.ts` exists to assert that a wrapper invokes a given string, so it imports `updateProduct` and calls it forever whether or not a screen ever does -- `ProductManagementScreen.tsx:267/317` imports `updateProductScoped`/`deleteProductScoped`, `RetailPosScreen.tsx:572` uses `updateProductScoped`, `EodReportScreen.tsx:373` uses `printReceiptScoped`, `TerminalManagementScreen.tsx:395` uses `setTerminalOverrideScoped`. The unscoped forms take a `userId` argument, which is the exact forgery ADR #7 was written against. WHAT I CHECKED BEFORE BELIEVING AN ABSENCE, because "nothing imports it" is a claim about a search and not about the tree: (1) `(?<![\w.])name\b` deliberately ignores property access, so I grepped every one of the 11 for a `.`-qualified call (namespace imports like `productsApi.listProducts(...)`) across `ui/src` excluding tests and dev-mock -- **zero**; (2) the same shape question for a barrel that would re-export and hide a caller -- `ui/src/api/index.ts` does not exist and no file in `ui/src/api` matches `export * from` (**0 files**); (3) the earlier version of this probe required the name to be followed by `(` and consequently reported all 17 unreachable, missing `useBarcodeScanner.ts:125`, where T21 had just proved the reference is a bare `: listScanners` arm -- a call-shape assumption pointed the wrong way in 60 seconds, fixed to a word match before any conclusion was drawn. THE TRANCHE: the three terminal-override wrappers deleted (21 lines), each with exactly one site in the tree -- its own definition -- and their scoped twins kept and still exercised; `vitest` on `api-terminals-contract.test.ts` + `TerminalManagementScreen.test.tsx` → **28 passed**, `tsc --noEmit` → exit 0. The leg moved the way it must: tablet UI-named **17 → 14**, uncalled deletion candidates **10 → 13**, `27 = 14 + 0 + 13` closing on the leg's own arithmetic. WHAT IS LEFT AND WHY IT IS NOT ONE COMMIT **(four of the eight are now gone -- see T24)**: the remaining eight wrappers each sit inside more scaffolding than the terminals trio did -- `api-products-contract.test.ts` and `api-hardware-contract.test.ts` hold cases that call `updateProduct`, `deleteProduct`, `createProduct`, `printReceipt`, `openCashDrawer` directly; `__tests__/test-utils/mocks/api.ts` and `mocks/retailPos.tsx` carry `vi.fn()` entries for them, which `mockFactorySurface.test.ts` enumerates, so a mock key cannot be dropped without reading that surface test first; and `getProductTrackSerial` / `getProductTrackSerialBatch` are also referenced from `api/sales.ts:389`/`:402`, a second api file, so removing the products.ts export needs that caller classified before it is treated as dead. NONE OF THE EIGHT SHOULD LOSE ITS DEV-MOCK KEY: every one has a registered `<name>_scoped` command, and per T16/T17 an unscoped mock handler is the alias seed that makes the scoped name answer in dev -- deleting a key would break the mock for a command that is very much alive. The Rust bodies follow the wrappers, not the other way round: retiring `print_receipt` while `api/hardware.ts` still names it would just move the name from the F-006 candidate bucket back into the UI-named gap bucket, which is the mistake the census has to avoid in both directions. The three terminal bodies this unblocked are still held by another lane's dirty `terminals.rs`.

- [x] **T23 - The unscoped cash-drawer and print-receipt doors, retired end to end in both shells, and the three defects in this lane's own verification habit that the work exposed.** Done 2026-09-16 (`b8ba7c573` UI, `d5d3fbb9c` allowlist, `3e066eb66` desktop, `f60ce83c1` tablet, `b6e38aa31` the tool). THE TRANCHE, in the order that keeps every instrument honest: first the UI wrappers (21 lines across `api/hardware.ts`, its contract suite, and the two `test-utils/mocks/api.ts` keys that `mockFactorySurface.test.ts` would otherwise have flagged as dead), then both shell allowlists, then the Rust bodies. `tsc --noEmit` exit 0 and 60 Vitest cases green across the four touched files before any Rust was touched. THE PIN THAT SURVIVED BY MOVING, not by being deleted: `api-hardware-contract.test.ts` carried the `deviceId -> device_id` wire pin -- and the comment above it explains why that pin exists, that Rust has no `#[serde(rename_all)]` so a camelCase key would not error but would **silently open the default drawer**. Two of its three cases sat on the wrapper being deleted; the mapping was already re-pinned on the scoped side, but the *omission* case (absent key, not `device_id: undefined`) existed only on the unscoped one, so it was rewritten onto `openCashDrawerScoped` rather than dropped. That is legitimate because both wrappers call the same module-private `cashDrawerWireArgs` -- verified at `hardware.ts:23`/`:204`, not assumed -- so the surviving case grades the code that actually ships. THE ARITHMETIC: tablet **27 -> 25**, desktop **5 -> 3**, programme total **131 retired**; tablet F-006 now reads `25 = 12 UI-named (6 reachable + 6 unreferenced wrappers) + 0 helpers + 13 uncalled`, desktop `3 = 1 + 1 helper + 1 uncalled`. WHAT THE FIRST TABLET APPLY DID NOT DO, and the lesson is bigger than the file: the tool printed `644 -> 607 lines, attributes 15 -> 13` and I reported a tablet retirement that had not happened. Those numbers describe the **plan**, they are printed before the write, and the KEEP gate stopped the write further down -- behind an output line my `Select-String` filter dropped. Two other lanes' activity made the confusion worse in the same minutes: `git diff --stat` showed 66 changed lines in `hardware.rs`, which was another session's uncommitted delegation work and not mine, and a `-D warnings` run blamed four unused imports on `commands/staff.rs`, a file this lane never opened, which had vanished by the next run. Three instrument defects, all in how this lane *reads* its own evidence rather than in what it changes: (1) plan numbers mistaken for landed changes -- fixed in `b6e38aa31` by `verify_on_disk`, which re-reads the file and fails with exit 3 on any disagreement, plus the word `planned:` on the pre-write line and `VERIFIED on disk:` after it. Positive control run before shipping: pointed at `apps/tablet-client/src/lib.rs` with a bogus 3-line expectation it returned **2 problems** (line count 793 vs 3, attributes 0 vs 13), pointed at the file it had just written it returned **0**; a check that has only ever seen success is not known to be able to fail. (2) `cargo clippy ... | Select-String ...; "exit=$LASTEXITCODE"` reports **Select-String's** exit code -- so several receipts this lane quoted as "clippy exit 0" measured the wrong process. **MECHANISM CORRECTED the following round, and the correction matters more than the anecdote.** The control nobody ran before writing the sentence: `cmd /c "exit 3" 2>&1 | Select-String -Pattern 'nothing-matches' | Out-Null; $LASTEXITCODE` prints **3**, so an ordinary pipeline does NOT replace a native command's exit code and my original explanation was false. What is true is narrower: a pipeline ending in `Select-Object -First N` can stop upstream and leave the *previous* native command's code in `$LASTEXITCODE`, which is how a `-D warnings` run that printed four errors was quoted as exit 0. The symptom was real, the diagnosis was invented on the spot -- the same self-confirmation this row is about, one layer up. `.agents/verify-lane.sh` avoids the whole question by running `"$@"` with no pipe and reading `$?` on the next statement. Fixed by `.agents/verify-lane.sh`, which captures `$?` on the statement after the run with no pipe between. (3) every gate here grades the **working tree**, not the commit, in a checkout where lanes land hourly, so a red can be borrowed from a stranger and a green can hide your own -- the same caveat `AGENTS.md` writes for the CSS walkers ("record which walked paths were dirty at the moment of the run"), which this lane never applied to its own Rust gates. The harness now prints HEAD and the dirty `.rs` list before and after every run, so a quoted green carries its provenance or is visibly incomplete. T21 CORRECTED, and it matters for whoever owns those doors: I wrote that registering the hardware fallbacks was **closed** because the scoped twins already occupy debt rows and the ceiling only shrinks. That mis-stated the mechanism. The ceiling is a price, not a wall, and the ledger records it being paid twice -- `Lowered from 125 by the terminals F-017 pass` (four `terminals::*` rows) and `Lowered from 90 by the hardware F-017 pass` (`hardware::open_cash_drawer_scoped`, at `5c67948d4`), which is the first shrink this lane has watched happen. The pattern in both: give the shell body the permission gate its `oz_bridge` twin **already enforces**, and the row leaves the ledger. So option (a) stands open for any door with a gated bridge twin. What survives of my claim is narrower and still true: the other six `hardware::*` rows are ungated in the bridge too -- the generated file says so at `:271-273` -- so those need an owner to name a permission before a gate can be copied, and `discover_hardware_scoped` and friends stay as debt until then.

- [x] **T24 - Four of the six unreferenced product wrappers and their commands, deleted from both sides at once.** Done 2026-09-16 (`5f27e40bd` UI, `cec17f0f1` allowlist, `45907cd9c` the commands). WHAT MOVED: `createProduct`, `updateProduct`, `deleteProduct`, `lookupProductBySku` out of `api/products.ts` together with `CreateProductArgs` and `UpdateProductArgs` -- both carried a required `userId`, and after the wrappers went their only remaining occurrences were a definition and one test title, so they were orphaned type surface, not shared vocabulary. 156 lines out of seven files. The three cases that pinned the unscoped wire shape (`create_product` with `args.userId`, `delete_product` with `args(userId, sku)`) were **deleted on purpose**: they document the forgable-identity argument ADR #7 replaced, and the scoped twins already have their own cases. The two cases that pinned a *mechanism* -- error propagation and return-type passthrough -- were **re-homed onto `createProductScoped`**, because a test that survives its subject's deletion by pointing at the survivor is worth more than a clean diff. That move paid for itself immediately: `tsc` rejected the inline literal with `Object literal may only specify known properties, and 'userId' does not exist`, which is the compiler confirming the scoped door cannot be handed a userId -- a stronger statement than the deleted case made. Eight mock keys went with the exports (`mocks/api.ts`, `mocks/retailPos.tsx`), because `mockFactorySurface.test.ts` grades mock keys against the module's real exports and would have reported them dead. Two test titles renamed to the wrapper the screen actually calls. `verify-ipc-parity.py` exit 0, self-test 141, `tsc --noEmit` exit 0, Vitest suite over the six touched files exit 0, tablet lib 667/0, `clippy -D unused-imports -D dead_code` exit 0. THEN 168 LINES AND FOUR ATTRIBUTES out of `products.rs` (`851 -> 683`, attributes `20 -> 16`), and for the first time the tool's own new check said so rather than me inferring it: **`VERIFIED on disk: apps/tablet-client/src/commands/products.rs is now 683 lines, 4 command fn(s) absent, 16 attributes remain`**. ALLOWLIST: both sections dropped the four names (`desktop 25 -> 21`, `tablet 152 -> 148`), and the repair that commit's message names is mine to own -- removing a final array element by line-match left `,\n  ]`, and `json.load` threw `Illegal trailing comma before end of array: line 24 column 19` before anything consumed it; a second attempt died on a PowerShell parse error from a stray line I should not have typed, which silently aborted the *whole script including the `git checkout` on its first statement* and left the broken file on disk. Both were caught by reading, not by assuming, and the file was restored to HEAD and rebuilt in Python with the comma rule explicit. ONE ANOMALY I CANNOT EXPLAIN, recorded rather than rationalised: the first allowlist write reported `-168 chars` and re-read as 25/152 counts in the same call, then `git status` showed the path **unmodified** minutes later -- so between my write and my commit something outside this session restored that file. No cause is claimed. What is claimed is the mitigation, which this round then used: measure, then commit, in the same breath, and re-check after any gap. NUMBERS: tablet F-006 **25 -> 21** (`8 UI-named [6 reachable + 2 unreferenced] + 0 helpers + 13 uncalled [3 test-pinned]`), desktop unchanged at 3, **programme total 135**. REMAINING of this tranche: the two `getProductTrackSerial*` wrappers, which are defined **twice** -- `api/products.ts` and `api/sales.ts` each export a `getProductTrackSerial` calling the same command, and only `sales.ts` has the batch form -- so the duplication is itself a finding: one command string had two UI homes, which is why "the UI invokes it" could be true while no screen did.

- [x] **T25 - The serial-tracking pair, a duplicated UI home, and the shipped door that had no test until its dead twin lost its only one.** Done 2026-09-16 (`11a574e41` UI, `b75d41d18` allowlist, `aa1a4b1be` the commands, `097145c48` the instrument, `7d471bf3a` the bookkeeping it found). WHAT THE DUPLICATION WAS: `get_product_track_serial` had **two** UI homes -- `api/products.ts:164` and `api/sales.ts:389` each exported a `getProductTrackSerial` invoking the same command string, and only `sales.ts` carried the batch form. That is how "the UI invokes it" stayed true for a command no screen reaches: the census counts command strings inside wrappers, and a wrapper can be exported twice and called zero times. Both unscoped forms deleted, with the `PERF-03` prose moved onto `getProductTrackSerialBatchScoped`, which is the door `RetailPosScreen.tsx:191` actually calls. THE PIN THAT MATTERED AND NEARLY WENT WITH IT: the batch case was the **only** wire-shape test either form had, and it pinned that the response rows are snake_case (`track_serial`), because `SerialTrackRow` declares the wire spelling -- a camelCase "cleanup" would read `undefined` for every SKU and the cart would treat every tracked product as untracked. So the case moved to the scoped door (import swapped, call and expectation rewritten) instead of being deleted, which is a strict improvement in coverage: the door the app ships now has a test it never had, and the dead one has none. The single-SKU scoped wrapper was left alone and is recorded here as a finding for the owner: `get_product_track_serial_scoped` is **registered in the tablet and named by no UI file at all** -- registered-but-unreached is the mirror direction this programme has never measured, and it is visible from T22 onward only because deleting wrappers keeps removing the noise in front of it. BOOKKEEPING THE SUITE POLICES ITSELF: 4 mock keys removed (`mocks/api.ts`, `mocks/retailPos.tsx`) and one name removed from `KNOWN_DEAD`, which is what that list's own instruction demands -- "a removed entry means real cleanup, delete it from KNOWN_DEAD too"; `mockFactorySurface.test.ts` grades mock keys against real exports, so it would have failed the commit otherwise. The two `dev-mock` handler keys stay: per T16/T17 they are alias seeds for the registered scoped twins. `tsc --noEmit` exit 0, Vitest **93 passed / 4 files** exit 0, tablet lib 667/0, `clippy -D unused-imports -D dead_code` exit 0. THE INSTRUMENT, THEN ITS FINDING: `097145c48` split the tool's single `allowlist=YES` into `ENTRY` (a parsed member of `desktop`/`tablet`/`scoped_orphans`/`dev_mock`) and `mention-only` (free text, usually `_comment`), because the round-23 apply had been stopped by a sentence this lane wrote hours after cleaning the entries -- a conflated signal cannot tell stale data from a live decision. Both still require `--only`: weakening the gate was considered and rejected, since this file's prose has repeatedly carried a real keep-decision. The tool also refuses to grade at all if the gate file will not parse. Run against `terminals.rs` it immediately found **six stale entries** -- the three terminal-override names whose wrappers this lane deleted in T22 and never un-allowlisted (`7d471bf3a`, `desktop 19 -> 16`, `tablet 146 -> 143`) -- which is a check earning its keep: improving it produced a new true finding rather than a cleaner report. MY OWN REPEATED DEFECT, stated because it is the third time in five rounds: `python scripts/retire-legacy-commands.py --force` with no `--module` is a usage error (the flag is required, argparse exits 2), and my `| Select-String` filter swallowed the message so the run looked like "no candidates". Round 20 wrote the sentence -- a filtered run cannot distinguish "nothing found" from "the tool never ran" -- and round 24/25 committed it again. The habit that fixes it is not attention but shape: run the unfiltered command at least once before quoting a count, or read the exit code as the primary signal and the output as commentary. STATE AFTER THIS ROUND: tablet F-006 **19** = `6 UI-named, all reachable, enumerated rather than summarised -- `hardware.rs`: `list_scanners`, `start_scanner`, `stop_scanner` (all three reached by `useBarcodeScanner`); `products.rs`: `list_products`, `lookup_by_barcode` (reached by `useProducts` and the scanner hook) and `adjust_stock`, which **no screen reaches and only `ui/src/api/client/products.ts` calls**, so it is a facade surface rather than a UI gap and belongs to a different decision + 0 helpers + 13 uncalled`; of the 13, **10** sit in `terminals.rs` -- held by another lane, and the tool's own dirt gate demonstrated it by refusing the apply -- plus the 3 real test pins (`sync_run`, `create_exchange_rate` under T20, `list_audit_log`). Desktop unchanged at 3. Dead-surface class: **empty** in both shells. **Programme total: 137 retired** (tablet 118 -> 19, desktop 43 -> 3).

- [x] **T26 - The mirror direction, instrumented at last: doors the shells opened that no shipped UI ever walks through.** Done 2026-09-16 (`010431cf9`). WHY IT WAS MISSING: F-006 has always asked "the renderer names a door nobody registered". A thin shell has the opposite surface -- "a door is registered and no client names it" -- and nothing in this repo had ever printed it, so "the shells are thin" was only half-measured while I was quoting a retirement total at it. THE LEG: `info[<shell>-unrequested]`, informational like its siblings, splitting the unrequested set into `rust_called` (no UI demand but the shell's own code calls the fn, so the body is load-bearing and deleting it breaks the build) and `dead_registration` (named by neither side -- the only population a retirement could start from). FIRST NUMBERS: **desktop 22 of 453 (4.9%), tablet 15 of 319 (4.7%), `rust_called` zero in both.** Cross-checked against each other so the number is not double-counted as a scary one: 17 desktop-only + 10 tablet-only + **5 unrequested in BOTH shells** = 22 and 15 closing on the same arithmetic, and the five are the actionable shortlist -- `enqueue_offline_scoped`, `get_over_quota_report`, `list_pending_offline_scoped`, `list_scale_devices_scoped`, `pending_sync_count_scoped`. WHAT I CHECKED BEFORE BELIEVING ABSENCE, because twice this programme a "nobody calls it" turned out to be "my search could not see it": (1) the census walks seven directories and `ui/src/platform` is not one of them, which would have made this leg's whole population fiction -- probed every shipped file outside the walk for an invoke literal and found **none**, so the walk is complete today, and the docstring records that a future file there breaks this leg and F-006 together since they share the census; (2) demand is matched on a string LITERAL, so a dynamic `loggedInvoke(cmdVariable, ...)` would hide a name -- probed for non-literal invoke targets and the only two hits are `ui/src/utils/logged-invoke.ts:14` and `:18`, the helper's own parameter, i.e. the definition of the mechanism, not a use of it. RECOMMENDATION, and the reason I did not act on it: the five are **registered**, which is a different category from the 137 this programme retired. Retiring them changes a runtime API an external consumer could be using -- `ui/src/api/client` is documented as a programmatic facade, and `verify-scoped-coverage.sh` covers them as live registrations today -- so the honest move is to hand the owner a named shortlist with the checks above attached, not to delete five doors in a lane that has spent 25 rounds being right about unreferenced code and wrong three times about what "nothing calls it" meant. A SEPARATE CEILING FACT, measured rather than inherited: the tablet ledger is `debtRows=89 (DEBT_CEILING=89)` and the desktop's is `69 (69)` -- **both exactly at their pins, so there is no room today** to register an unscoped fallback that would not name a permission. That sharpens T21 and qualifies T23: the ceiling is indeed a price that peers have paid twice (125 -> 90 -> 89 on the tablet), but paying it means *regenerating the ceiling down* in the same pass, which is an owner-visible act, not a free slot. A peer's `50b2fd14e ... drop four debt rows` is sitting in a dirty `terminals.rs` working state, not HEAD, so the headroom it creates is not available to a commit yet. ONE RETRACTION, the fourth instance of the habit this plan keeps catching: I opened this round by announcing that I had "found a real one" in `REGISTERED_TOTAL=447` against 453 live registrations, and only then grepped the test that produced the number, whose own comment at `apps/desktop-client/src/commands/registration_gate_tests.rs:78-87` says `REGISTERED_FLOOR = 453` is an **equality** measured from `lib.rs`, and that `REGISTERED_SLACK = 24` governs only how long the *generated ledger's* total may lag before a regeneration is overdue -- explicitly "not slack on this floor". So there was no drift and no defect: a documented tolerance I had read as an anomaly because I quoted a delta before opening the code that defines it. The symptom (two numbers disagreeing) was real and worth chasing; the claim was not, and it is retracted here in the same breath as the receipt that settles it. Self-test 141 -> **145** assertions, `parity` exit 0, `verify-scoped-coverage.sh` PASS.

- [x] **T27 - `sync_run` retired, and the reason it took a round to see is that the lane committed while its own checks were red.** Done 2026-09-16 (`aa4ba7d97` the test port, `689f85484` the retirement, `ce4d227a3` the instrument). WHAT WAS ACTUALLY WRONG: the tablet's plan-gating invariant -- a 403 `plan_required` must keep queued items `pending`, flag the UI, and never mark them failed -- was tested **only** through `sync_run`, a command registered in neither shell, so a real build could never execute the path the test exercised; the door the tablet ships, `sync_run_scoped` (SYNC_MANAGE-gated), had no plan-gate case at all. The two bodies are separate copies of the same three-phase logic, not a wrapper and a helper, so this was not duplicate coverage -- it was one copy tested and the shipped copy untested. THE PORT: `sync_run_scoped_plan_required_keeps_store_items_pending` built from the scoped scaffolding (seeded roles/user, `StoreDatabaseManager` on a temp dir, a `plan-gate-token` session, settings AND the queued item written through `resolve_store`, which is the queue the scoped push actually reads), asserting `plan_required`, `synced == 0`, `failed == 0`, the store row still `Pending`, and `pending_offline_count() == 1` so the queue is still offered next cycle. It PASSED, which is the useful outcome: the shipped path gates the plan identically, so no behavioural divergence existed -- and had it failed, the test would have caught a live bug rather than written a line of prose. `61 insertions / 13 deletions` in the test file, then `sync.rs 699 -> 633` with `attributes 12 -> 11`, VERIFIED ON DISK by the tool. PROSE, BOTH DIRECTIONS: the module note I wrote in round 21 -- "it survives only because `sync_tests.rs` still calls it directly, which makes its removal a decision about tests rather than about dead code" -- is the diagnosis this round executed, so it was rewritten as the resolution rather than deleted; the test attribution went to past tense and named its successor. ONE MENTION DELIBERATELY KEPT: `sync.rs:8` still names `sync_run` in the note that records its retirement, and the stale-prose heuristic will flag it on every future `--module sync` run. That is the correct state -- a dated record of a retired name has to contain the name -- and the alternative (rewriting to "this command's unscoped twin") was applied in the test file where the name carried no information, not here where it does. THE FAILURE OF THE ROUND, MINE: `cargo test` and `cargo clippy` printed **exit=101** and the next statement in the same call ran `git commit`. Two rounds of notes about reading the exit code as the primary signal, and I chained the commit anyway -- which is the actual lesson: a check that runs beside a commit will eventually be ignored by the commit; the habit has to be structural, not attentive. THE FACT THE 101 WAS: `crates/oz-bridge/src/pos.rs`, a peer's **uncommitted** edit (E0502 `cannot borrow cart as immutable`), not my content -- the pathspec commit took only my two files, so nothing of theirs entered my commit. Chasing attribution produced the bigger find: `drift_pin_guard_marker_vocabulary_is_closed` **FAILS at HEAD** (offender: `require_audit_permission`, a guard-shaped name committed in `audit.rs` with no `GUARD_MARKERS` entry) while the identical test **PASSES in this working tree**, because the fix is sitting uncommitted in that same dirty file one lane over. So a green working tree had been hiding a red commit -- the inverse of every other instrument failure in this plan, and the reason `.agents/verify-lane.sh --head` now exists (`ce4d227a3`): it checks HEAD out into a detached throwaway worktree, runs the lib tests and clippy there, and reports "HEAD is RED on its own" or grants the commit a receipt with no neighbour's dirt in the way. Verified: `bash -n` clean, and the mode dispatch was exercised over four argument shapes (empty, `--ui`, `--head`, `--head extra`) so the flag is not detected by luck. WHY I DID NOT FIX THE HEAD RED: it is a one-line `GUARD_MARKERS` add, but the marker vocabulary and the generated ledger are coupled -- `drift_pin_three_way_partition_is_complete_and_sums` requires the class counts to add up to the ledger -- so retargeting that name without regenerating `registration_gate_debt.generated.rs` in the same pass trades a red for a different red, in a file the lane currently editing `audit.rs` owns. Filed for them rather than grabbed. NUMBERS: tablet F-006 **19 -> 18**, uncalled 13 -> **12** with test pins down to **2** (`create_exchange_rate` under T20, `list_audit_log` in a dirty file); desktop unchanged at 3; **programme total 138** (tablet 118 -> 18, desktop 43 -> 3). Isolated checks: HEAD tablet tests 679/1 (the 1 is the gate-vocabulary failure above, not this commit), HEAD clippy `-D unused-imports -D dead_code` exit 0.

- [x] **T28 - Three instrument defects, one of them live and falsely accusing a neighbour; and a hypothesis of mine that turned out to be wrong.** Done 2026-09-16 (`116c7b328`, `c99abc832`). THE ROUND STARTED AS AN AUDIT OF MY OWN HISTORY: the retirement tool has always graded "uncalled" against the owning shell's sources only, so 138+ deletions rested on the unexamined assumption that nothing OUTSIDE `apps/<shell>` could call a `pub` command fn. It could: `oz_pos_tablet::commands::x::name(...)` is reachable from any crate. THE AUDIT: recovered the population by DIFF rather than by commit message -- **232 distinct command fns removed under the two shells' `src/commands/` since the oldest retirement-named commit**, walked against **973 rust files** workspace-wide. Two corrections on the way to an answer. First attempt recovered **0 names** (a regex that tried to skip attribute and doc lines in one expression matched nothing) and printed "0 suspects" over an empty population -- caught only because the population size is printed beside the result and a guard refuses to read a small one as clean. Second attempt produced 43 suspects then 219 "benign", but the benign test counted `impl` methods as definitions: it had read `crates/oz-core/src/db/terminals.rs:20 pub fn list_terminals(&self)` as a definition of the deleted tablet command of that name, which is the error direction that makes a clean audit meaningless. A **control on the control** caught that; and the control itself had to be corrected, because two of its three "expected" values were assertions from belief -- I expected `hash_pin` to live in `oz-core` and it does not appear there as a free fn at all. FINAL RESULT: **217 benign (the calling crate defines that name as a free fn and imports nothing from the shell), 0 true suspects.** The four survivors were `delete_customer` and `void_sale` at `crates/oz-core/src/db/customers.rs:6` and `db/sales.rs:4`, which are lines inside the repo's `/* */` audit stamps: `next: consider soft-delete or referential guard on delete_customer (COR-23)`. DEFECT 1, FIXED (`116c7b328`): `fn_call_sites` knew line PREFIXES (`//`, `*`) but nothing about a block-comment INTERIOR, so prose inside a stamp read as a caller. That is the same shape `retire-legacy-commands.py` has tracked since round 20 -- the gate fell behind its own tool. Seventh exclusion, self-test 145 -> 146, with the fixture asserting BOTH halves: a name inside a `/* */` is not a call, and a call after the block still is, because an exclusion that swallowed everything after the first `/*` would read cleaner than the truth. Nesting is not modelled, which can only over-exclude. DEFECT 2, LIVE AND FALSE (`c99abc832`): the parity gate has been red since 10:03 on the coursing lane's `a8a5eeb79`/`b07e8c3ac`, and one of the two violations accused a properly gated command of enforcing nothing -- "registers scoped command `set_line_course_scoped` that no client invokes -- it enforces no permission, so it is a redundant twin. Wire a caller, or add it to ipc-parity-allowlist.json scoped_orphans with a reason if it is host-only." It enforces `SALES_PROCESS`, at `crates/oz-bridge/src/pos.rs:505`, one line below the shell body that delegates to it. The harm was not cosmetic: following that advice would have written a `scoped_orphans` entry asserting a reason ("host-only") that is untrue, in a ledger whose whole value is that its entries carry real reasons. Fixed by following a `oz_bridge::<mod>::<fn>(` delegation one level before answering, and by matching `require_\w*permission` instead of the literal `require_permission` -- `ctx.require_session_permission` does NOT contain that substring, so the old check was narrow in the same direction twice. Now reports "it enforces SALES_PROCESS, so that check currently guards nothing", which is the true and far more useful sentence, and was always available: the leg had the wording, it never had the data to reach it. Self-test 146 -> **150**, four cases, one of them the real tree. ATTRIBUTION WAS PROVEN, NOT ASSUMED: I ran the PREVIOUS version of the gate -- `git show 116c7b328^:scripts/verify-ipc-parity.py` -- against this same tree and it exits **1 with the same two violations**, so the red predates and is independent of my change. Had I not run that, "the gate went red right after my commit" would have been the story. MY HYPOTHESIS WAS WRONG, AND SAYS SO: I expected the registration ledger to share the orphan leg's blind spot -- which would have explained the F-017 pattern (peers adding a gate to a shell body that the bridge already enforces) as instruments driving duplicated checks. It does not: `apps/tablet-client/src/commands/registration_gate_tests.rs:414-418` reads `resolves_session(text) && (names_permission(text) || (text.contains("oz_bridge::") && stems.contains(&module)))`, with `gated_bridge_stems()` computed at :403 -- the ledger already follows the delegation. That is why `set_line_course_scoped` is absent from the generated debt ledger, and it is the third time this programme a suspected second defect dissolved on contact with the code. Residual question for the owner, explicitly UNVERIFIED and offered as a question: the ledger's bridge rule is keyed by **module** (`stems.contains(&module)`) while the fix I just shipped is keyed by **fn**, so a module that gates some commands and not others could read as gated for all of them. No claim that it does; `names_permission` and `gated_bridge_stems` are the two lines to read if someone wants to know. DEFECT 3, AN OVERSTATEMENT OF MINE ONE ROUND OLD: T26 said of the mirror direction that "nothing in this repo had ever printed it". False -- the `scoped_orphans` leg has long printed the uncalled-and-scoped subclass, and enforces it. Measured the real overlap rather than arguing it: **desktop mine=23 vs orphan-leg=22, overlap 22, mine-only 1** (`get_over_quota_report`, unscoped, invisible to a `_scoped`-keyed leg); **tablet mine=16 vs orphan-leg=6, overlap 6, mine-only 10**. So the new leg's true contribution is the unscoped remainder, the local-Rust-caller split, and cross-shell reading -- not a blank spot filled. T26's headline sentence should be read as "not printed IN FULL", and this row is the correction. Cross-instrument agreement, worth having: the live violation name `set_line_course_scoped` appears in my leg's `dead_registration` for BOTH shells ✓ the two instruments see the same fact from different rules. THE DISCIPLINE SLIP OF THE ROUND: the gate was already red when I ran it at ~10:07 and I did not notice, because in that call I pattern-matched output lines instead of capturing `$LASTEXITCODE` -- the fifth instance of exactly that habit, and the reason `.agents/verify-lane.sh --head` was running for the first time in this round (its receipt for HEAD `806bdfac5` is in `.agents/head-receipt.out`, grading both shells' lib tests and clippy in a detached worktree with none of this tree's 24 dirty rust files in it).

- [x] **T29 - Absorbed a red the isolated receipt found, and found that the plan's "six reachable gaps" were the six names the ledger refuses to explain.** Done 2026-09-16 (`f24a14b44`). WHAT `--head` EARNED ON ITS FIRST FULL RUN: the receipt graded HEAD `806bdfac5`/`675d49a35` in a detached worktree and returned tablet lib **679/1** (`drift_pin_guard_marker_vocabulary_is_closed`, unchanged from T27) and desktop lib **153/2** -- `drift_pin_registration_floor_is_met` AND `drift_pin_debt_ceilings_only_shrink`. Two enforced pins red at HEAD across five commits, invisible to every working-tree run because the fix for one sat uncommitted in a neighbour's dirty file. THE FLOOR: `lib.rs registers 455 commands and this floor says 453`, raised by the coursing pair `b07e8c3ac` (`pos::set_line_course_scoped`, `pos::publish_course_fired_scoped`). The assertion does not merely allow a raise, it prescribes the companion act -- "name each addition in docs/records/JOURNAL.md in the same deliberate pass ... raising it records what landed, it does not approve it" -- and `c7210bb72` (13-09-26) is the template for exactly this absorb, so the const (453 -> 455), both prose measurements, and a JOURNAL entry in that shape went in ONE commit. Both new commands arrive **already gated**: the shell bodies are one-line delegations and the bridge resolves the session plus `ctx.require_session_permission(..., SALES_PROCESS)` at `crates/oz-bridge/src/pos.rs:505` and `:591`, and `gated_bridge_stems()` sees them -- so no ceiling and no ledger row moved here, which is why the floor leg is the only thing in the file that could have noticed. VERIFIED: `cargo test -p oz-pos-app --lib drift_pin` went **5 passed / 2 failed -> 6 passed / 1 failed**; the claim in the entry that the JOURNAL named neither command before was checked against `git show HEAD:docs/records/JOURNAL.md` (0 and 0) BEFORE committing, not after. THE CEILING I LEFT RED, ON PURPOSE: **27 names in `resolves_session_names_no_permission` against a ceiling of 26** (`no_session_resolution` 42 under its 43). That class is authenticate-then-assume, the ceiling may only shrink, and the leg's own words are "it moved without a decision" -- so the two honest options (gate the offending command, or raise the ceiling deliberately) both belong to the owner and neither to this pass. And this pass did **not identify the offender**: the leg prints two counts and no names, and re-deriving `resolves_session` in Python to find it would fork the sweep predicate this file has worked to keep single -- the same reason `classify_unregistered` and the mirror leg now share `shell_rust_sources`. The name is one regenerated ledger diff away: `registration_gate_debt.generated.rs` was last written 14-09-26 and enumerates the class members. SIX GAPS, MEASURED RATHER THAN RECALLED: the plan's "6 genuinely-reachable gaps" are **`adjust_stock, list_products, list_scanners, lookup_by_barcode, start_scanner, stop_scanner`** -- all UNSCOPED names, not the `_scoped` hardware set my own summary note had been carrying ("6 hardware debt rows"), which conflated a REGISTERED-BUT-UNGATED ledger row with a DEFINED-BUT-UNREGISTERED F-006 gap. Both populations exist; they are different work. And every one of the six is allowlisted in BOTH shells as a **bare string with no reason**, while the sections that accept a `{"name", "reason"}` object are `OBJECT_ALLOWED_SECTIONS = ("dev_mock", "scoped_orphans")` -- so the gate's own advice ("an allowlist entry may carry why the gap was accepted") cannot be followed for `desktop`/`tablet` entries without extending that parser first. That is the actual blocker on the six, and it is a `scripts/` change this lane owns. Half of them are also T21-class in a second sense the gate already prints: `info[tablet-fallback]` names 3 unregistered commands sitting behind a no-session branch production UI takes, `list_products` among them -- so the unscoped arm fails in a real build while mocks answer it, which is precisely why a bare allowlist string is not a reason. ONE PROBE DEFECT, SELF-CAUGHT: my first classification script printed `0 fns are defined, NOT registered, and named by shipped UI` -- because `SHELLS[s]` names `.../src/lib.rs`, so `.parent` is `src/` and the commands directory became `src/src/commands`, which globbed to nothing. The second probe, built differently, returned 6. Nothing was believed from the zero, and the guard that made it visible is the one from T28: print the population size beside the result. THE ROUND'S OWN HANDICAP: `terminals.rs` (10 candidates) and `audit.rs` (a 654 -> 247-line rewrite in progress, dropping all 7 `require_audit_permission` calls) are both still peer-held, so no retirement landed here; the vocabulary red at T27 appears closed by the marker now present in BOTH shells' gate files (grep: 1 occurrence each), stated as a grep, not as a green run.

- [x] **T30 - The six reasonless gaps are three different problems wearing one badge, and the fix I said this lane owns is not mine to own.** Done 2026-09-16. WHY: T29 closed with "the real blocker on the six is a `scripts/` change this lane owns". WRONG, one round after saying it -- `scripts/verify-scoped-reads.py` PINS the refusal in its own self-test (``SHAPE_CASES`` at `:1280-1285`: "an object in desktop is refused, not read", "an object in tablet is refused too -- the two shell sections stay strict"), and `scripts/allowlist-schema.py:72-76` splits `SHELL_SECTIONS` from `WRITER_OWNED_SECTIONS` as the shared definition. So giving `desktop`/`tablet` a reason field is three files in two other lanes, not a local edit. Second self-correction in as many rounds, and the pattern is consistent: every time I have located an owner and reached for the change, the owner turned out to be elsewhere. WHAT THE SIX ACTUALLY ARE (built with the gate's own helpers -- `ui_runtime_files`, `wrapper_reach`, `no_token_fallbacks`, `extract_handlers`, `extract_dev_mock_answerable` -- rather than a re-derived parser, because a second implementation of a rule is how two answers to one question get into a repo). All six share the same facts: **registered in neither shell, a `_scoped` twin registered in BOTH shells, and the dev-mock answers the unscoped name** -- which is precisely why the bare allowlist strings have stayed unexamined: every one of them works in a mock build. They divide three ways. **(A) Behind a no-session arm the UI really takes (3):** `list_products` at `ui/src/features/products/useProducts.ts:132`; `list_scanners` at `useBarcodeScanner.ts:125` AND `useWarehouseScanner.ts:111`; `stop_scanner` at `useBarcodeScanner.ts:71`. This is T21 asking to be answered: a logged-out build invokes a command no shell registers, and only the mocks hide it. **(B) Invoked with NO session branch at all (2):** `start_scanner` (`api/hardware.ts:129`, reached through `useBarcodeScanner#startScanner` / `useWarehouseScanner#startScanner`) and `lookup_by_barcode` (`api/products.ts:157`, same two hooks). These are worse than (A) in the plain sense -- no ternary means no build in which they could work -- and the same hook file mixes both shapes (`stop_scanner` guarded at `:71`, `start_scanner` unguarded), so the guard is a per-call choice that two calls in one file disagree about. STATED AS A MEASUREMENT, NOT A VERDICT: whether (B) fires in production depends on whether those hooks can be reached before a session exists, and that is a read of the hook guards, not a line this table printed; it is the first thing to check, and I did not check it. **(C) Facade-only (1):** `adjust_stock` -- `wrapper_reach` gives runtime=[] and client=`ui/src/api/client/products.ts#adjustStock`, i.e. no screen and no hook, only the programmatic facade. That independently confirms T25's derived split instead of resting on it. WHAT TO DO WITH IT: (A) is the owner's T21 ruling, now per-site with file:line rather than a count; (B) needs the two guards read before anyone calls it a bug or files it as one; (C) is the standing "does a facade method count as UI demand" question, unchanged. THREE PROBE DEFECTS, ALL CAUGHT BEFORE ANYTHING WAS BELIEVED, printed because they are the round's actual method: `ui_runtime_files()` yields (path, text) with the path as a STR, so `p.as_posix()` raised after the first name had already printed -- a table that dies midway is honest, a table that prints the first row and stops is not, and only the guard `if not shown` would have hidden it; `extract_dev_mock_answerable()` returns a 4-tuple (set, set, dict, str), and `set().union(*that)` counted the STRING's CHARACTERS as answerable names, reading 722 where the two sets give **680** -- a 42-name inflation found because the number was bigger than the surface it claimed to cover; and the first `in_allow` used `allowlist_section(AL, section)` on a hunch about its signature, reporting the six as "not listed" when the JSON lists each exactly twice (one entry per shell section, cross-checked with `grep -c "\"$n\""`), so the answer was rebuilt reading the parsed object directly. The gap between "not listed" and "BARE STRING in both" is the difference between a ledger that looks clean and one that reads accurately.

- [x] **T31 - `start_scanner` and `lookup_by_barcode` were NOT the class I just invented for them: the fallback detector could not see a parameterised arrow. Corrected in the same round it was filed.** Done 2026-09-16 (`ca5dbe7e7`). T30 above separates (A) arm-guarded from (B) "invoked with NO session branch at all", and (B) is FICTION: reading `ui/src/features/sales/useBarcodeScanner.ts` settles it -- `:63` `const start = sessionToken ? (id: string) => startScannerScoped(sessionToken, id) : startScanner;` and `:83` `const lookup = sessionToken ? (code: string) => lookupByBarcodeScoped(sessionToken, code) : lookupByBarcode;` are the same ADR #7 ternary as `:71` and `:125`, which the leg DID report. The difference is not in the code, it is in the instrument: `UI_FALLBACK_TERNARY_RE`'s optional arrow prefix was `(?:\(\s*\)\s*=>\s*)?` -- a literally EMPTY parameter list -- so a zero-argument arm matched and a parameterised one did not match at all. So six of the six names are the same population, all behind a no-session arm, and the two I had promoted to a worse class were missing measurements. THE FIX: the prefix is now `(?:\(\s*[^)]*\)\s*=>\s*)?` (`[^)]*` cannot cross the closing paren of the parameter list, so widening it this far is safe) -- and the leg's counts moved **desktop 8 -> 10, tablet 3 -> 5**, two names per shell appearing for the first time. THE POINT OF ORDER, for anyone reading this plan: the three synthetic fixtures for this leg were ALL zero-argument arrows, so the test and the regex had been written against the same single form and agreed with each other while disagreeing with the tree; that is instrument self-confirmation with a passing self-test behind it, and it is why three new cases are now attached -- the parameterised arm reported, the parameterised arm with a non-wrapper else-branch still silent, and both forms in one tree counted as two names and not four. Self-test 150 -> **153**, gate exit 0. WHAT SURVIVES OF T30: the (C) finding stands as measured (`adjust_stock` is facade-only -- runtime=[], client=`ui/src/api/client/products.ts#adjustStock`), the allowlist cross-check stands (each of the six appears exactly twice in the JSON, one BARE STRING per shell section), and the schema correction stands (the object form is pinned away by `verify-scoped-reads.py:1280-1285`, so per-site reasons are a three-file two-lane owner decision). What does not survive is the invented middle class, and the T21 brief it would have handed the owner: the ruling is now uniformly "3 tablet / 5 desktop... " no -- the ruling is that TEN distinct unregistered names sit behind production no-session arms in the desktop shell and FIVE in the tablet, and every one of them is answered by a registered `_scoped` twin and by the dev-mock, which is exactly the fail-loudly-vs-delete-the-arm decision and nothing else. T30's text is kept verbatim above, including the false class, per this file's convention that a superseded reading stays where it was written.

- [x] **T4-4 · Close the tablet settings registration gaps (findings 4 and 5).** Register `set_settings_scoped` on tablet or refuse the three cards there, and delete or register the two dead unscoped preference commands. `verify-ipc-parity.py` will need its allowlist touched for whichever way each goes. <!-- IMPLEMENTED 2026-09-15 in two commits, both halves. (a) REGISTERED, not refused: `3a77ed981` "fix(tablet): register the batch settings write the workspace cards call" adds `set_settings_scoped` to apps/tablet-client/src/commands/settings.rs, registers it in lib.rs, and drops the name from scripts/ipc-parity-allowlist.json (tablet 157 -> 156 entries). Registering beat refusing because the tablet mounts the settings modal (see T4-1), so the three cards are reachable and their Save is live -- hiding them would be a capability regression, not a fix. (b) DELETED, not registered: `f5f4173c2` "refactor(tablet): delete the two dead unscoped preference commands" removes `get_user_preferences` / `set_user_preferences`, which were defined here and registered in NEITHER shell, with the desktop crate defining neither -- so deletion is the change that makes the two shells agree. Registering them would have re-opened the caller-supplied `user_id` door T4-1 had just closed. THE ONE ENGINEERING FINDING THIS ROW PRODUCED, and it is a trap worth the ink: the tablet twin of the batch write must enqueue on the STORE queue, NOT mirror the bridge. The bridge enqueues on `ctx.db` and comments that its daemon watches only the global queue; this shell's daemon drains the store queue -- `sync_run_scoped` reads it through `resolve_scope` (sync.rs:400-411), and its Phase 3 comment at :437-447 records that writing those marks to the global connection instead stranded the store rows as `pending` forever. The new command therefore enqueues via the bridge's `enqueue_settings_updates` onto the store, with tenant `"default"` (this shell's staged sentinel, matching its own singular `enqueue_settings_update`) rather than the bridge's `session.store_id`. PROVEN FAILABLE, not merely green: mutating the enqueue to the global connection -- i.e. doing what the bridge does -- turned the new test red on exactly the store-queue assertion (`left: []`, `right: ["kds.auto_accept", "restaurant.course_firing"]`), and the mutation was reverted. Gates: `cargo test -p oz-pos-tablet --lib` -> 656 passed / 0 failed (654 + the 2 new); `cargo clippy -p oz-pos-tablet --all-targets -- -D warnings` exit 0; `python scripts/verify-ipc-parity.py` -> "IPC parity: OK" exit 0, tablet registered 322 -> 323 and unregistered UI names 156 -> 155, both consistent with the allowlist edit. A same-turn follow-up commit `0ebc8263e` "test(tablet): scope the store guard so the batch settings test passes clippy" fixed an `await_holding_lock` that the new test's second assertion introduced. -->


---

### Phase 3.4: Re-enable `oz-pos-app` in Test Runners & Final Audit

Verify that the memory killer is permanently solved and all test suites pass.

> **STATUS (Agent-3, 2026-09-13): NOT STARTED, deliberately unselected by the user in the 2026-09-13 resume round.** Scope: remove `--exclude oz-pos-app` AND `--exclude oz-pos-tablet` from every runner that still carries them. **The excludes are NOT confined to one file — rev 1 of this audit said they were, off a single-file grep, and that was wrong.** Re-measured repo-wide (`git ls-files | xargs grep -ln -- "--exclude oz-pos"`): **9 live executable paths** — `scripts/check.sh:106`, its PowerShell twin `scripts/check.ps1:116-117`, `scripts/coverage.sh:70-71` + `scripts/coverage.ps1:60-61`, `scripts/release.sh:62` (clippy) and `:65` (nextest `--profile ci`), `scripts/setup-dev.ps1:134` (a `cargo check`), `scripts/setup-cache.sh:49-50` + `scripts/setup-cache.ps1:70-71` (both echoed build-timing instructions, not gates), and `.github/workflows/release.yml:149` — plus, not actionable, `ci.yml.bak`, `nightly.yml.bak`, `release.yml.bak` and two prose citations (`docs/plans/0.0.36-backlog.md`, `docs/archived/20260822-tests-efficiency-improvement.md`). The actionable PAIR is `check.sh:106` **and** `check.ps1:116-117`: change one without the other and the local matrix re-splits per shell. The file this box originally named, `.circleci/workflows/06-cargo-nextest.yml:37`, **was real and is now retired** — `git show e3aff7b56^:.circleci/workflows/06-cargo-nextest.yml` line 37 read `cargo nextest run --workspace --all-features --exclude oz-pos-app --exclude oz-pos-tablet -j 2` until `e3aff7b56` deleted the tree on 2026-09-14; nothing to edit remains, so the work landed on the shell/PowerShell twins instead. **CI today is mixed, not clean:** `dev-ci.yml#cargo-nextest` runs `cargo nextest run --workspace --all-features` at **`dev-ci.yml:244`** with **no `--exclude`**, so DEV CI does test both app crates — but `release.yml:149` (`name: Run tests (desktop targets)`, `if: matrix.tauri`) carries both excludes plus `--profile ci`, so **on a `v*` tag neither app crate is tested by the release pipeline**. Rev 1's blanket "CI therefore already tests both app crates" was false for `release.yml`, and that half-difference is precisely this box's subject. Then prove the full matrix green — including `cargo nextest run --workspace`, which CI already runs without excludes. Prerequisite: the permanent desktop residue suites (3.1 register, now **6** files) must keep `oz_pos_app_lib` compiling within the low-memory envelope; the residue is test-mount light, so the risk is measured, not assumed.

- [x] Run `cargo test -p oz-bridge` (verify execution finishes rapidly with low RAM).
  → **MEASURED 2026-09-14, from the repo root: `cargo test -p oz-bridge` = 277 s end to end** = 1 m 14 s genuinely COLD compile (266 units, first line `Compiling proc-macro2 v1.0.106`) + 201.17 s execution. **1305 passed / 0 failed / 0 ignored**, doc-tests 0. RAM ~2.1 GB peak summed over cargo/rustc/build-script processes with effectively no swap activity (15,617,292 → 15,600,180 kB free on a 66.2 GB box). cargo 1.98.0; a second invocation compiled 0 units, which is what proves cache reuse. So the memory killer this box existed to test is solved: the crate runs in ~5 minutes and ~2 GB.
  > **Two honesty caveats, recorded because a future reader will re-run this and must not trust the shape of the number.** (1) The ~2.1 GB is a **floor, not a peak** — the test binary itself was NOT included in the summed process set. (2) The first run's literal exit code was **lost to the measurer's own shell-quoting bug** and was recovered only indirectly: one scoped subset re-run to `$?` = 0, alongside `0 failed` on all 1305. The evidence is indirect-by-accident, not direct.
  > These are the measurement run's numbers; a docs pass has no shell here, so nothing above was re-executed — only the file:line anchors below were re-read.

- [ ] **Release-profile failures (found 2026-09-14 while measuring :137). ROOT-CAUSED 2026-09-14; the box stays OPEN deliberately, because the finding is worse than 'unknown'.** `cargo test -p oz-bridge --release` returns **1228 passed / 76 FAILED / exit 101** where debug is 1305/1305 green. Families: 39x `InvalidSubscriptionSignature - failed to decode base64 signature: Invalid symbol 95, offset 9` (95 = `_`, a URL-safe character), ~16x `assertion left == right`, ~10x `PermissionDenied("audit log requires the Premium plan or above (current tier: Free)")`; by module subscription 21 / auth 18 / audit 11 / pos 7 / staff 6 / workspaces 5 / inventory 4 / terminals 2 / locations 2. Counts differ by one test, so at least one is `cfg(debug_assertions)`-gated. **What moved since those two figures were taken: the passed counts and the totals, NOT the 76.** `68e6dd356` added 3 `#[tokio::test]` cases to this crate, so a debug re-run should read **1305 + 3 = 1308** and release **1228 + 3 = 1231 passed / 76 FAILED**, i.e. a release total of 1304 → **1307, never 1308** — the profiles are still one test apart because `sync_tests.rs:35-37` is `#[cfg(debug_assertions)]`-gated, which is the gap bullet under this box. The debug total corroborates without a re-run: counting attrs over `git ls-files crates/oz-bridge` at HEAD `f7fa97e1a` gives **934 `#[test]` + 374 `#[tokio::test]` = 1308** exactly. **And do not “correct” 76:** it is the RELEASE *failure* count, not a debug figure — debug is 1305/1305 with 0 failed — and a test that passes in both profiles lands in `passed` and cannot move it; `git diff --numstat 68e6dd356..HEAD -- crates/oz-bridge crates/oz-core` is **empty**, so nothing else has landed that could shift the failure set either. The figures above stand as a dated record of one real run, unedited.
  - **DISPOSITION 2026-09-15 (plan audit, docs pass): PARKED — toolchain + owner ruling, not docs.** Two halves and neither is closable here: the fix needs `cargo test -p oz-bridge --release` and `cargo test -p oz-core --lib verify_bootstrap` runs, and a docs box is not allowed to spend those; and `:154` already parks the release arm of the expired-past-grace branch on a production ruling. The 2026-09-15 re-read that keeps this row's own anchors true: `grep -c -- --exclude` over the five runners returns `check.sh=1 check.ps1=2 coverage.sh=2 coverage.ps1=2 release.sh=2`, so every `--exclude` claim in this cluster still stands. No tick: the deliverable is a code change in `crates/oz-core`/`crates/oz-bridge`, and this audit touched no code.
  - **Why it is invisible, and what that means for the next box.** No runner in the repo tests in release: `grep -c -- --release scripts/check.sh` = **0** (re-run 2026-09-14; its test step at `:106` is `cargo nextest run --workspace --all-features --exclude oz-pos-app --exclude oz-pos-tablet`, debug), CI's `cargo-nextest` job is `dev-ci.yml:244` = `cargo nextest run --workspace --all-features` with no `--release`, and `scripts/run-pre-push.py` carries no release test either. So **no gate in this repo can see this failure set**. Consequence for the box below (:138): do NOT drop `--exclude oz-pos-app --exclude oz-pos-tablet` from any release-path runner (`scripts/release.sh:62,65`, `.github/workflows/release.yml:149` are precisely those) on the strength of a green debug run.
  - **Root cause of the 39: a TEST-HONESTY defect, not a broken signature path.** `crates/oz-core/src/license_verification.rs:391-394` returns `Ok(())` for the sentinel `BOOTSTRAP_FREE` under `#[cfg(debug_assertions)]` only; in release the same string reaches a STANDARD base64 decode at `:398`, and the error text names the input — `Invalid symbol 95, offset 9` is the first `_` of the sentinel itself. The fixture is not test-local: the sentinel is seeded by `crates/oz-core/migrations/20260813_init.sql:1514` (PG twin `20260813_init.pg.sql:2101`), so every test DB inherits it. Producer and consumer alphabets AGREE (`apps/license-server/main.go:1113` `base64.StdEncoding.EncodeToString` vs the STANDARD decoder), so nothing here indicates a real signature bug — and a forged non-sentinel signature still fails closed in release (`auth::tests::create_session_rejects_tampered_subscription_signature` is ok in BOTH profiles).
  - **The repo already knew the shape of this.** `crates/oz-core/src/license_verification_tests.rs:91-95`: *'intentionally no test that BOOTSTRAP_FREE is rejected in release builds, because `cargo test` always runs with `debug_assertions` enabled. The guard is validated by inspection.'* `cargo test -p oz-bridge --release` is the first EXECUTION of that claim in this checkout, and no runner in the repo builds these crates in release.
  - **The deciding measurement, run 2026-09-14 — the bypass is CONFIRMED COMPILED OUT.** `cargo test -p oz-core --lib verify_bootstrap` → EXIT 0 (2 passed, 3032 filtered, 40 s cold) while the same filter under `--release` → EXIT 101, with BOTH `license_verification_tests.rs:72` (`assert!(result.is_ok())`) and `subscription_tests.rs:136` (`sub.verify_signature().is_ok()`) failing exactly where `:391-394` skips verification in debug. So the shipped profile REJECTS the sentinel, the signature path is sound, and the 39x base64 red is 100% test fixture. That closes the gap the source admitted above: the inspection is now validated by execution, and it was right.
    - **CORRECTION (appended 2026-09-14, docs pass) — the sentence above is right about the 39 reds and WRONG about the sentinel being only a fixture.** `BOOTSTRAP_FREE` is also the **PRODUCTION SEED**: `crates/oz-core/migrations/20260813_init.sql:1512-1514` (`INSERT OR IGNORE INTO tenant_subscription … 'BOOTSTRAP_FREE'`) writes it into every fresh install, and the PG twin repeats it at `20260813_init.pg.sql:2100-2101` — that PG file is **GENERATED** by `scripts/generate-pg-migration.py`, never hand-edited (AGENTS.md §4). Second half of the correction: `bootstrap_free()` (`crates/oz-core/src/subscription.rs:628-641`) sets `signature: String::new()`, and release rejects an EMPTY signature too — so the three "defaulting to Free tier" warn-branches (`crates/oz-bridge/src/auth.rs:613-617`, `crates/oz-bridge/src/workspaces.rs:224-228`, `apps/tablet-client/src/commands/auth.rs:437-441`) do **not** degrade to a Free verdict; they error one line later at their own `sub.verify_signature()?`. So neither arm of the bootstrap lands on Free in release: seeded row → sentinel reject, missing row → empty-string reject. **What IS true and stays true:** activation REWRITES the row — `crates/oz-bridge/src/license.rs:175` `store_subscription(...)` → `crates/oz-core/src/license_verification.rs:625` `INSERT OR REPLACE INTO tenant_subscription` — so an activated release install never sees the sentinel and is unaffected by anything in this bullet.
  - **RELEASE-BRANCH EXECUTION, the five production debug-only bypasses in these two crates, and whether ANY test executes their release branch:**
    1. `oz-core/license_verification.rs:391` → **YES**: `verify_bootstrap_free_bypasses_rsa_in_debug` and `subscription::tests::verify_signature()` both FAIL under `--release` (exit 101), proving the guard is compiled out.
    2. `oz-core/entitlements.rs:103` (`apply_debug_upgrade`, Free → Premium only under `cfg!(debug_assertions)`, read `:102-108`) → **YES BY PROXY ONLY**: the proof lives in ANOTHER crate — `crates/oz-bridge/src/subscription_tests.rs:26/39-48` asserts `premium` under `#[cfg(debug_assertions)]` and `free` under `#[cfg(not(debug_assertions))]`, green in release. Its own two promote tests (`entitlements_tests.rs:150`, `:176`) **VANISH** in release rather than invert, and nothing in `oz-core` asserts its own release inertness. Count it as 4, not 5, if you refuse cross-crate proxy.
    3. `oz-bridge/locations.rs:231-236` (Free → Premium feeding `enforce_location_quota` at `:237`) → **NO, UNREACHABLE**: all three calling tests die one line earlier at `:227` `sub.verify_signature()?` on the sentinel (exit 101; 9 passed 3 failed under release vs 12/12 debug), and the tier value is never compared. The clearest admission is a test comment — `locations_tests.rs:147-148`: *'Plus is used instead of Free because debug builds upgrade only the bootstrap Free tier'* — i.e. the author routed AROUND the bypass in debug rather than asserting it in either profile.
    4-5. `oz-bridge/license.rs:659` (expired + past grace → `is_active: true`, `status: Valid`) and `:687` (no stored payload → `Valid (free tier)`) → **RESOLVED — EXECUTED IN BOTH PROFILES at `68e6dd356` (2026-09-14, *test(licensing): give get_license_status its first tests, asserting both build profiles*; `git show --numstat 68e6dd356` = +191/−0 on that one file). The "NO TEST EXISTS AT ALL" verdict that stood in this line is RETIRED; the text under it is kept as the record of what was measured, not as a claim about today.** `crates/oz-bridge/src/license_tests.rs` now calls `get_license_status` from three cases: `get_license_status_past_grace_reports_active_in_debug_only` (`:506`, pinning `:659`), `get_license_status_without_a_stored_payload_is_free_in_debug_and_missing_in_release` (`:459`, pinning `:687`, and doubling as the suite's both-profile control — the only seeded case whose release arm reaches a real verdict instead of the sentinel) and `get_license_status_active_payload_is_the_control_that_passes_in_debug` (`:549`). `cargo test -p oz-bridge --lib license` → **21 passed, exit 0**; the same filter under `--release` → **21 passed, exit 0** (before `68e6dd356` that filter read **18/18 with ZERO calls to `get_license_status`** — 18 = the 17 `#[test]` in the file plus `settings_tests.rs:1666`, which only names the string; the three new cases are `#[tokio::test]`, so a `#[test]` grep still reads 17 and the 18 → 21 delta is the check that they actually ran). Each asserts BOTH profiles in ONE body: `assert_eq!(dto.is_active, cfg!(debug_assertions))` at `license_tests.rs:475-479`, the idiom read off `crates/oz-core/src/db/audit_security_tests.rs:418`, with `if cfg!(debug_assertions) { … } else { … }` arms on the enum splits in the `subscription_tests.rs:26/39-48` form. **And they are proven failable, not merely green:** the author mutated one line to the debug-only form, RELEASE went red on the message naming the divergence ("debug reports an unlicensed install ACTIVE (the :687 bypass); release reports it INACTIVE") while DEBUG stayed green — which is the exact mechanism that kept this gap invisible: an assertion written for one profile gets "fixed" by weakening it in the other.
    - **SUPERSEDED, kept verbatim because its measurements still stand:** Tree-wide `get_license_status` appears **39** times (`grep -rn get_license_status . | wc -l`; 6 in `crates/`, 10 in `apps/`, the rest in `ui/` and docs) and at audit time **ZERO** times in any Rust `*_tests.rs` in either crate or in `apps/` — the only test hits were `ui/src/__tests__/api-license-contract.test.ts:31,34`, which assert the IPC name mapping, not the behaviour. `cargo test -p oz-bridge --release --lib license` returned 18/18 GREEN, and that green was the ABSENCE of any call to the function, not coverage of it. Two of the five enum arms those DTO tests touch are precisely `Expired` and `Missing` — the arms these branches produce.
    - **PARKED — a production decision, not a test change:** the release arm of the expired-past-grace branch itself, `license.rs:670-683` (`is_active: false` + `Expired`), **cannot be executed from this crate at all.** Reaching it needs a payload whose signature VERIFIES, and the only signature a test can seed is the `BOOTSTRAP_FREE` sentinel, which `oz-core/src/license_verification.rs:391-393` accepts under `#[cfg(debug_assertions)]` ONLY — so in release every seeded case returns `InvalidSignature` at `license.rs:594` and the date logic at `:638-684` never runs. A real signature means RSA-2048 PKCS1v15/SHA-256 made with the license server's **private** key, and `license_verification.rs:36` embeds only the **public** half. Both ways of covering it change production — an injected verifier on the call path, or a `#[cfg(test)]` key compiled into it. Recorded as **parked pending the owner**, with the recommendation that it **stays parked rather than weakening production to make a test reachable**. Not a sixth bypass: bypass 4's debug arm is pinned in both profiles now; only its release arm is unreachable, and `:506` asserts that unreachability instead of faking it (*"an Expired assertion here would be unreachable, not wrong"*).
  - **ONE FALSE PREMISE FOUND IN A TEST COMMENT, and it collapses three mysteries into one.** `entitlements_tests.rs:103` comments *'fresh_db seeds a signed active Free row'* and `:104` asserts `e.loaded`. It does not: `fresh_db` seeds the UNSIGNED SENTINEL (`BOOTSTRAP_FREE`), which only passes verification in debug — so in release the read model CORRECTLY returns the fail-closed projection (`loaded: false`) via `entitlements.rs:282` → `:321` → `:88`. That single mechanism, not three, explains the 3 non-`cfg` release failures (`analytics_addon_flows_only_while_active_or_grace`, its grace-state twin, and `active_row_projects_tier_answers_and_usage_identity`). The read model is profile-consistent — `fail_closed_projects_free_unavailable` at `entitlements_tests.rs:77` proves it — so do NOT describe those failures as an independent release behaviour difference or leave them 'under investigation'.
  - **WHY THIS BOX MUST NOT CLOSE ON A GREEN DEBUG RUN.** Production `sub.verify_signature()?` hard-propagation sites, re-counted this pass: **16** in `crates/oz-bridge/src/` — `pos.rs:1525,1737` · `workspaces.rs:232,311,478,530,657` · `staff.rs:1080` · `products.rs:691` · `inventory.rs:110` · `offline.rs:235` · `locations.rs:227` · `terminals.rs:432` · `history.rs:71` · `topology/commands.rs:635` · **`auth.rs:617`** (the drafted list of 15 omitted `auth.rs:617` and counted `subscription.rs:297`, which is a `match sub.verify_signature()`, not a `?` propagation). On a release build whose DB still holds the seeded row — any single-store install that never activated against a license server, which is exactly the deployment the sentinel documents — those sixteen return an ERROR rather than a Free-tier verdict. That may be intended fail-closed; it has never been EXECUTED. Unchecked, and not checkable by `cargo test` in debug.
    - **CORRECTION (appended 2026-09-14) — RETRACTS the REACHABILITY framing a manager wrote into the bullet above** ("any single-store install that never activated … those sixteen return an ERROR"). The framing over-reached; three parts, each checkable:
      - **It is not a login outage.** Login is `staff_login` (`crates/oz-bridge/src/auth.rs:319`), which calls NO `verify_signature` — `:617` is the only occurrence in that file. The check sits in `create_session` (`auth.rs:611-621`) on **workspace entry**, after login. Desktop gates first: `ui/src/frontend/shell/AppShell.tsx:401` (`if (!hasActiveLicense) return <ActivationFlow>`) precedes `:434` (`if (!hasCompletedSetup)`), so a desktop install cannot walk into the failing check — it is stopped at activation. **Falsified as an outage.**
      - **`completed` is not activation.** `AppShell.tsx:167-176` reads `status.completed` and fail-opens with `setHasActiveLicense(true)`; `completed` is the **setup-wizard dismissal** (`crates/oz-bridge/src/setup.rs:144`, key `SHOW_SETUP_WIZARD`), and the `catch` at `:188-197` fail-opens the same way on any startup error. Those two are why the path is *theoretically* open on a never-activated install; reaching it still needs the boot-catch to fire or setup to be marked done without activating.
      - **Most of the count is unreachable BY CONSTRUCTION while the sentinel stands:** **21 of the 27** sites (16 in `crates/oz-bridge` + 11 in `apps/tablet-client`; `oz-bridge/subscription.rs:297` and `apps/tablet-client/src/commands/subscription.rs:288` are `match` arms, not `?` propagation, and are NOT in the 27) take and resolve a session token — and no token can be minted, because minting is the `create_session` that rejects first. **THEREFORE 76 release-test failures ≠ 76 customer-visible bugs.** What survives of the bullet above untouched is its last claim: it has never been EXECUTED, and it still has not been.
      - **Where the failure IS live: the TABLET shell, and only there.** `ui/src/frontend/shell/tablet/TabletAppShell.tsx:64-84` reads `getSetupStatus()` and nothing else — **no licence gate at all** — and `apps/tablet-client/src/lib.rs` registers **zero** license commands (grep `license` → 0, `oz_bridge` → 0), while its four pre-session commands (`history.rs:63 list_sales`, `products.rs:295 create_product`, `pos.rs:784 complete_sale`, `offline.rs:162 enqueue_offline`) carry no token and hit `verify_signature()?` directly. A tablet `createSession` rejection is then swallowed at `ui/src/contexts/WorkspaceContext.tsx:520-531` into a `console.warn`, leaving `sessionToken` unset. Reachable today **only via a hand-built release APK**: `android.yml` / `ios.yml` are `.bak` and no live workflow builds mobile.
    - **LAST UNCLASSIFIED SITE, resolved this pass — `crates/oz-bridge/src/inventory.rs:100-115` (`create_inventory_location`, `?` at `:110`): TOKEN-REQUIRED, therefore unreachable while the sentinel stands.** The deciding line is `:95`, fifteen lines BEFORE the check: `let session = ctx.resolve_session(session_token)?;`, followed at `:96-101` by `require_session_permission`-style RBAC (`require_inventory_permission(ctx, &session.user_id, INVENTORY_LOCATIONS_MANAGE)`). It also has no bootstrap arm at all — `:108-109` uses `.ok_or_else(|| BridgeError::Internal("default tenant subscription not found".into()))?` rather than `unwrap_or_else(bootstrap_free)`, so a missing row is a loud error here, not a silent Free. `ui/src/api/inventory.ts:80` does expose it (`loggedInvoke('create_inventory_location', { sessionToken, … })`) but always with a token, so it joins the 21, not the 6. **Counting consequence: the 16 bridge `?` sites are 15 token-required + `auth.rs:617`; the 11 tablet sites are 6 token-required + 4 pre-session + tablet `auth.rs:441`.**
    - **IF a shared helper is ever built** for the bootstrap/Free arms, its home is `crates/oz-core/src/subscription.rs` next to `verify_signature` at `:475`. It must **NOT** be applied to the ~21 token-required sites: that would convert a loud forged-row error — the property `crates/oz-bridge/src/auth_tests.rs:400-402` pins in BOTH profiles — into a silent Free run, for zero customer benefit.
  - **Net for the repo, recounted after `68e6dd356`:** `audit_security_tests.rs:418` (`assert_eq!(recorded, cfg!(debug_assertions))`) and `oz-bridge/subscription_tests.rs:26/39` prove that BOTH-PROFILE assertion is a known, working idiom here, and `license_tests.rs:475-479` is now its first use inside `oz-bridge` — so its absence at a site is a coverage gap, not a language limit. **Of the five bypasses: 1 executed directly, 2 executed by cross-crate proxy, 4 and 5 executed in both profiles as of `68e6dd356`; that leaves exactly ONE site still open — 3, `oz-bridge/locations.rs:231-236`, whose release branch is still UNREACHABLE under test because all three calling tests die at `:227` — and ONE release ARM parked (`license.rs:670-683`), untestable without a production change.** **What the next session owes this box:** ~~a release-side test at `locations.rs:231-236`~~ **RETRACTED, MEASURED 2026-09-15: no such test can be written, and the "all three calling tests die at `:227`" clause earlier in this bullet is false.** Executed, not reasoned: `cargo test -p oz-bridge --release --lib create_location_profile_scoped` → **3 passed / 0 failed / exit 0, 1312 filtered out** — so the release total is **1315** against debug's **1316**, which is the documented one-test `#[cfg(debug_assertions)]` gap at `sync_tests.rs:35-37`, independently corroborated. **All three calling tests already pass in release.** Two carry an explicit release leg — `locations_tests.rs:138-141` and `:220-221`, both `if !seeded_row_loads() { assert_refused_by_the_seeded_row(&tb, result, "free").await; return; }` — so that clause was already false when it was written, and `testing.rs:248` names the profile-scoped location commands among the sites the helper covers. The third, `create_location_profile_scoped_denies_staff_without_settings_edit` (`:240-283`), needs **no** leg and must not be given one: its session holds only `sales:view`, and `require_session_permission` at `locations.rs:220-221` precedes `TenantSubscription::load` / `verify_signature` at `:230-232`, so the `Err(PermissionDenied)` asserted at `:282` is profile-independent by construction — the release run above *is* that assertion passing. **What remains genuinely open at this site is unchanged, and it is not a test:** the `#[cfg(debug_assertions)]` Free→Premium shim at `:236-241` has a release arm — Free actually reaching `enforce_location_quota` at `:242` — that no test can reach, because reaching `:242` in release needs a signature that verifies and the only seedable signature is the sentinel. That is the same production-change class as the parked arm at `license.rs:670-683`, and it stays parked for the same reason. **Still owed, and unchanged: an owner's ruling on the parked arm, and either way a `--release` runner somewhere that can see it.**
  - **The 1305 → 1228+76 = 1304 gap is one whole `#[test]` compiled out of release, not a mis-count:** `crates/oz-bridge/src/sync_tests.rs:35-37` is `#[cfg(debug_assertions)] #[test] fn sync_probe_falls_back_to_cloud_url_when_unconfigured()` — the same invisibility class as everything above, and the reason a release total will never equal the debug total.

- [ ] In **every** runner still carrying them — start with `scripts/check.sh:106` and `scripts/check.ps1:116-117` as a matched pair, then `coverage.sh:70-71` / `coverage.ps1:60-61`, `release.sh:62,65` and `.github/workflows/release.yml:149` (the `.circleci/workflows/06-cargo-nextest.yml:37` this box used to name was retired by `e3aff7b56` on 2026-09-14 — the file exists in history, so look for it with `git show`, not on disk).
  - **DISPOSITION 2026-09-15: OPEN WORK, half-delivered, sized.** The exclusions are still there and the matched pair is no longer matched — `grep -n -- --exclude scripts/check.sh` puts the last one inside the nextest step at `:106` (count 1) while `scripts/check.ps1:116-117` carries 2, so the two runners disagree about what is tested. Gate that would prove this row: all five runners at `--exclude` count 0 (`grep -c -- --exclude scripts/check.sh scripts/check.ps1 scripts/coverage.sh scripts/coverage.ps1 scripts/release.sh`), with `scripts/setup-cache.sh:49-50` / `.ps1:70-71` excluded from that target because `:229` already records them as echo-only.
  - Confirm `oz_pos_app_lib` compiles in seconds because all unit tests have moved to `oz-bridge`. **PREMISE CONTRADICTED 2026-09-15: all has not happened.** `ls apps/desktop-client/src/commands/*_tests.rs | wc -l` = **6** — `authz_tests.rs`, `edc_tests.rs`, `kds_lan_live_tests.rs`, `local_api_command_tests.rs`, `registration_gate_tests.rs`, `settings_tests.rs` — against `ls crates/oz-bridge/src/*_tests.rs | wc -l` = **59**. This clause stays unticked and unworded as completion: the confirmation it asks for cannot be given today.
  - **The 6 is only what that glob can reach; the tracked count is 10 — and this file already said so the day before it wrote 6.** `ls apps/desktop-client/src/commands/*_tests.rs` does not descend into `commands/topology/`, which is exactly the directory `:48` counted with `find` on 2026-09-14 to reach ten. Re-measured 2026-09-15 evening at `503591a10` with `git ls-files -- 'apps/desktop-client/src/commands/*_tests.rs'` → **10**: the six named above plus `topology/topology_command_tests.rs` (2,306 ln), `topology/topology_tests.rs` (256), `topology/topology_serde_tests.rs` (123), `topology/topology_stress_tests.rs` (47); all-ten line total **6,447** = 3,715 top-level + 2,732 topology (the split sums), against the 5,873 `:48` recorded — same ten files, ~574 lines added since, and `topology_command_tests.rs` has grown past the ratchet suite to become the largest test file desktop-side (1,732 → 2,306). Two consequences the next lane should not have to rediscover: **(i)** those four are **not stragglers of an unfinished wave** — they predate Phase 3.1's close (`d7e773832` 2026-08-12, `74383b567` 2026-08-20, against the closing `8630d50b9` 2026-09-11) and were never in the relocation's scope, so the relocation programme and the topology programme hold opposite readings of "all unit tests live in `oz-bridge`" in writing, with nothing reconciling them; **(ii)** **all four** share a basename with a `crates/oz-bridge/src/topology/` twin of a different size (command 2,306 vs 368 · stress 47 vs 1,910 · tests 256 vs 1,407 · serde 123 vs 288), so a search by filename returns two hits per name and reads as a copy — it is not one; `8630d50b9` relocated a *subset*. The row's verdict stands and hardens.

- [ ] Run full project verification: **DISPOSITION 2026-09-15: PARKED on environment.** Two of its three commands are unrunnable from a docs box and one is not: `python scripts/verify-ipc-parity.py` exists and is cheap, `cargo check --workspace` is a toolchain run, and `npm run check:all` in `ui/` reaches E2E, which needs Docker — `127.0.0.1:15432` refuses here. Never ticked on an inference.

  - `npm run check:all` in `ui/`
  - `python scripts/verify-ipc-parity.py`
  - `cargo check --workspace`
- [ ] **Commit Milestone:**
  - **DISPOSITION 2026-09-15: PARKED behind its own children, and it cannot be ticked.** House rule applied: a milestone whose children are open stays open — `:142` (release-profile, parked on toolchain + ruling), `:167` (exclusions still counted in all five runners), `:168` (6 `*_tests.rs` still under `apps/desktop-client/src/commands/`), and `:171`'s `verify-ipc-parity.py` is fine but `:170`'s `npm run check:all` needs Docker. The commit this milestone describes (`re-enable oz-pos-app in full nextest matrix post-refactor`) is exactly the change `:167` has not finished, so ticking it would claim the refactor closed while the matrix still excludes both app crates.
  ```bash
  git commit -m "ci(desktop): re-enable oz-pos-app in full nextest matrix post-refactor"
  ```

---

## Measurement (2026-09-14, HEAD 4f9d670e8)

> **Dated record for the open pair `:167` (strip the excludes from nine runners) and `:173` (the `ci(desktop)` commit that follows), beside `:142` and `:169`. Appended by a docs pass: no existing line was edited, nothing was renamed, NOTHING here is ticked — the verdict is that `:167` cannot be ticked yet.** Six crate-scope `cargo check` runs, no `--workspace`, no clippy, no `--release`. Every figure carries the command that produces it.

**HEADLINE — the box is aimed at the wrong target: Dev CI already runs what these runners skip.**

- `.github/workflows/dev-ci.yml:197` → `cargo check --workspace --all-targets --all-features` — **no `--exclude`** (step `Cargo check workspace`, job `cargo-check:` at `:165`).
- `.github/workflows/dev-ci.yml:244` → `cargo nextest run --workspace --all-features` — **no `--exclude`** (step `Run Nextest`, job `cargo-nextest:` at `:200`).

Both on Linux, both WITH system deps and WITH dist stubs — the `cargo-check` job installs system deps at `:179-185` and stubs the dists at `:187-190`; the `cargo-nextest` job carries its own copies at `:227-233` and `:235-238`. So both app crates are already compiled AND tested, un-excluded, on every PR and every `push` to `main`, in exactly the configuration the local runners skip. **The excludes are a local-machine and release-path artifact, not a code-health shield** — so `:167` is a release-pipeline and onboarding change, not a correctness change, and its green would say nothing about whether the code is sound.

**Corollary this measurement produced:** `scripts/check.sh:44` runs `cargo clippy --workspace --all-targets -- -D warnings` **un-excluded**, and `check.sh:110`'s no-nextest fallback (`cargo test --workspace --all-features -- --test-threads $cpu_count`) is un-excluded too. Its PowerShell twin matches: `check.ps1:97-98` clippy un-excluded, `check.ps1:124-125` fallback un-excluded, excludes only at `:116-117`. **`check.sh` / `check.ps1` already build both crates today; the excludes live only in the nextest and coverage steps.**

**THE SIX RUNS** (from the repo root, debug):

| # | command | exit | wall |
|---|---|---|---|
| 1 | `cargo check -p oz-pos-app` | 0 | 43.59 s |
| 2 | `cargo check -p oz-pos-app --all-targets` | 0 | 42.19 s |
| 3 | `cargo check -p oz-pos-tablet` | 0 | 54.34 s |
| 4 | `cargo check -p oz-pos-tablet --all-targets` | 0 | 1 m 08 s |
| 5 | `cargo check -p oz-pos-app --all-targets --all-features` | 0 | 41.45 s |
| 6 | `cargo check -p oz-pos-tablet --all-targets --all-features` | **101** | — |

**Run 6 anatomy: 67 copies of ONE cause.** Every line is `error: proc macro panicked` → `The frontendDist configuration is set to ../../ui/dist-tablet but this path doesn't exist`. Distribution, re-derivable: `grep -rn 'tauri::generate_context!' apps/tablet-client/src | wc -l` = **67** = 1 site in `apps/tablet-client/src/lib.rs` (`.run(tauri::generate_context!())` at `:785`) + **66** amplified copies across the 22 `apps/tablet-client/src/commands/*_tests.rs` files whose builders call `tauri::generate_context!()`. The trigger is `--all-features` — runs 3 and 4, same crate without it, exit 0.

**Zero `error[E...]` codes in all six logs**, six for six. Therefore the **“142 `E0599` from the tauri-free refactor”** figure this programme inherited **DOES NOT REPRODUCE today**, and **“170 errors” was never measured at all**. Provenance: that figure has no home in the tracked markdown — `git ls-files '*.md' | xargs grep -ln 'E0599'` names only `docs/records/JOURNAL.md` (`:6522`, `:6603`, `:10124`, none about this box) — so it arrived from briefing prose, not from a log. Do not carry either number forward as a count.

**CLASSIFICATION — the point of this entry: CODE 0 diagnostics / ENVIRONMENT 67, one directory.** `ui/dist-tablet` did not exist when run 6 was taken. Restorable two documented ways: `cd ui && npm run build:tablet` (`ui/package.json:13` = `tsc -b && vite build --config vite.tablet.config.ts`; `ui/vite.tablet.config.ts:23` = `outDir: 'dist-tablet'`), or a dev-ci-style stub (`mkdir -p ui/dist-tablet && touch ui/dist-tablet/index.html`, which is literally what `dev-ci.yml:187-190` does). **Desktop is clean under the SAME `--all-features --all-targets` (run 5, exit 0) because `ui/dist` exists on this machine and `ui/dist-tablet` did not** — `find ui/dist -type f | wc -l` = **197** at record time. Two crates, one flag, one missing directory: 67 red lines, none of them a code diagnostic.

> **WRITE-TIME RE-CHECK (this pass; HEAD `8229bd2b9`, four commits after `4f9d670e8`, +307/-30 across six `ui/src` files, zero Rust changes).** The premise “`ui/dist-tablet` does not exist” was true when run 6 was taken and **is false on this machine now**: `find ui/dist-tablet -type f | wc -l` = **209**, dir mtime `Sep 15 00:12`. Re-running the exact failing command → **EXIT 0 in 41.97 s**, `proc macro panicked` × **0**, `error[E...]` × **0**. Same crate, same flags, zero code edits, one restored directory → green. That flip is the proof of the classification above, **and it is also why the green must not be read as a licence to tick `:167`**: it says the failure was the path, and nothing about the three blockers below.

**THREE THINGS THAT MUST LAND BEFORE `:167` / `:173` ARE ACTIONABLE** — blockers, not opinions:

1. **`.github/workflows/release.yml` STEP ORDER.** The test step `- name: Run tests (desktop targets)` sits at `:147-149` (`if: matrix.tauri`; `cargo nextest run --workspace --all-features --exclude oz-pos-app --exclude oz-pos-tablet --profile ci`) and runs **BEFORE** `Install system deps (Linux)` `:151-158`, **BEFORE** `Install macOS deps` `:160-163`, and **BEFORE** `Install UI dependencies` `:169-171` (`cd ui && npm ci --no-audit --no-fund --ignore-scripts`). **`release.yml` has NO dist-stub step at all**, unlike `dev-ci.yml:187-190`: `grep -n 'dist\|mkdir' .github/workflows/release.yml` returns exactly one hit, `:391 mkdir -p release-assets`, which is not a `ui/dist`. So on a clean tag runner, at `:149`, there are no system libs, no `node_modules`, and no `ui/dist*`: **stripping the excludes there fails on all three OSes for environment reasons even with perfect code** — the run-6 mechanism, generalized. Landing `:167` requires reordering or adding workflow steps, a `.github/**` change that cannot be made from this file.
2. **A decision on `scripts/release.sh:62`** — the one site where the exclude is load-bearing for **LINT**. Stripping it puts, for the first time, roughly **139 desktop + 653 tablet test fns** under `clippy --workspace --all-targets --all-features … -- -D warnings` (`grep -rho --include='*.rs' -e '#\[test\]' -e '#\[tokio::test\]' apps/desktop-client/src | wc -l` = 139; same over `apps/tablet-client/src` = 653). And `scripts/release.sh:47` refuses a dirty tree (`git diff-index --quiet HEAD --` → `exit 1`), so the change **cannot be test-driven casually in a shared multi-agent checkout**. It needs an owner's call, not an edit.
   > **The LINT half of this blocker is retired, measured 2026-09-15 at `f6e6c1780`; the dirty-tree half stands.** The premise above is that the app crates' test surface has never been graded by `-D warnings`. It now has been, in the exact shape `release.sh:62` uses minus the two excludes: `cargo clippy -p oz-pos-app -p oz-pos-tablet --all-targets --all-features -- -D warnings` → **exit 0**, 2 m 25 s. Each crate is also clean alone (`-p oz-pos-app --all-targets` exit 0; `-p oz-pos-tablet --all-targets` exit 0). So the 139/653 counts are not a risk figure — they are the size of a surface that turns out to lint clean today, which is the opposite of what this item expected, and it is worth knowing before anyone funds a "fix the app lints" box that has nothing to fix. What the same measurement did turn up is one step sideways: `crates/oz-bridge` is **already** inside that clippy scope, excludes or not, and it is **red** — `crates/oz-bridge/src/auth_tests.rs:6` (`empty_line_after_doc_comments`) and `crates/oz-bridge/src/testing.rs:649` (`assertions_on_constants`), both present at `HEAD` (`git show HEAD:<path>`) in files the T4 pass never touched, while `cargo clippy -p oz-bridge --lib` — which skips `cfg(test)` modules — is exit 0. Two lines of editing stand between `release.sh:62` and a green, and they are not a decision. Blocker 1 (workflow step order) and item 3 (this plan's debug-green prohibition) are untouched by any of this. **BOTH `oz-bridge` SITES ARE CLEARED, 2026-09-15 (`f66eae2cc`) — and the red was LIVE, not latent.** `crates/oz-bridge` is a workspace member (`Cargo.toml:18` globs `crates/*`), so it sits inside `release.sh:62`'s `--workspace` scope with or without the two app excludes: the release lint gate was failing at `HEAD` on these two errors and, per the retired note above, on nothing else. Re-measured after the edit: `cargo clippy -p oz-bridge --all-targets --all-features -- -D warnings` → **exit 0, 48.06 s**; `cargo test -p oz-bridge --lib` → **1316 passed / 0 failed / exit 0, 210.13 s** (proxy unset, per the sandbox trap). The two edits, and why each is the fix rather than a silencer: `auth_tests.rs:6` demoted `///` → `//` — the comment documents no item, so it was an orphaned doc comment, not a lost doc; and `testing.rs:649`'s `assert!(!FAIL_CLOSED_GATES_LOCKED, "a locked gate reads false")` became `const _: () = assert!(…)`, the same pin promoted from a runtime `assert!(true)` — `FAIL_CLOSED_GATES_LOCKED` is `const bool = false` at `testing.rs:242`, so the old form asserted nothing at all — to a compile-time one that fails the BUILD if the const is ever flipped. Note the form: `const _: () = assert!(…)` does **not** re-trip `clippy::assertions_on_constants`, verified by the exit-0 run rather than assumed. **What this does NOT do:** it does not make `release.sh:62` green as a whole (the app crates were already clean; only the `oz-bridge` contribution was red) and it moves none of `:167`'s three blockers, least of all blocker 3 — the excludes on the release path still may not be stripped on the strength of a debug green.
3. **This plan's own standing instruction at `:143`**: do NOT drop `--exclude oz-pos-app --exclude oz-pos-tablet` from any release-path runner (`scripts/release.sh:62,65` and `.github/workflows/release.yml:149` are precisely those) on the strength of a green **debug** run. These six commands are exactly that — debug, crate-scope, no `--release`. The instruction stands until something in the release profile has been executed.

**`:142` is UNTOUCHED by this measurement.** No `--release` was run in any of the six, and no debug crate-scope `cargo check` can observe a release-profile failure set. `:142` stays open for its own reasons (76 release failures; the parked arm at `license.rs:670-683`), and `:169` stays open because its full-project verification has not been run.

**THE NINE RUNNERS THIS BOX COVERS** (enumerated at `:135`; `:167` itself names seven of them and omits `setup-dev.ps1:134` and the two `setup-cache` files), re-measured before writing (`git ls-files | xargs grep -ln -- '--exclude oz-pos' | grep -v '\.bak$' | grep -v '\.md$'` → exactly nine paths), with the risk each carries:

| runner site | what it is | risk if the exclude is stripped |
|---|---|---|
| `scripts/check.sh:106` | the nextest test step | **highest shared-gate risk** — every lane runs `check.sh`; red on this machine |
| `scripts/check.ps1:116-117` | its nextest twin — **TWO edit sites**: the `-RetryCommand` string (`:116`) AND the `-ScriptBlock` body (`:117`) | must move in lockstep with `:106` or the local matrix re-splits per shell |
| `scripts/coverage.sh:70-71` + `scripts/coverage.ps1:60-61` | `cargo llvm-cov` invocations | red here; and even when green they **RE-BASE every published coverage number and the ratchet inputs** |
| `scripts/release.sh:62` (clippy) + `:65` (nextest `--profile ci`) | release gate, hard `exit 1` on failure | **release-blocking** — see blocker 2 |
| `scripts/setup-dev.ps1:134` | `cargo check --workspace --all-features --exclude …` inside onboarding | **onboarding path**: a fresh clone with no `ui/dist-tablet` fails SETUP, before any gate ever runs |
| `scripts/setup-cache.sh:49-50` + `scripts/setup-cache.ps1:70-71` | `echo` / `Write-Host` text only | **EXECUTES NOTHING — zero risk.** Note these two name only `--exclude oz-pos-app` (`grep -c -- '--exclude oz-pos-tablet'` = 0 in both), so “strip both” overstates what is on those lines |
| `.github/workflows/release.yml:149` | nextest on the `v*` tag | see blocker 1 — fails on all three OSes for environment reasons |

**VERDICT: `:167` stays open; `:173` must not be committed.** This measurement retires one inherited false figure (the “142 `E0599`” / “170 errors” pair does not reproduce and was never sourced), and it confirms the excludes are not hiding code diagnostics — but it does not clear the release path, the new lint surface, or this plan's own `:143` prohibition, and it does not fix `release.yml`'s step order. When `:167` lands it lands as a nine-path change with a workflow reorder in front of it.

---

## Shape of this file, measured 2026-09-15 (docs audit, HEAD bc1a0445b)

A briefing that reached three lanes tonight told them a line-anchored grep for either checkbox form returns **0** on this file, and on 
`todo-font-system.md` and `todo-refactor-settings-agents-3.md`. It does not. This file is a task list and its rows are line-initial `- [ ]` / `- [x]`: 
`wc -l` 232 -> 257 after this pass (grow-only: four dated disposition clauses plus this section), `grep -cE "^[[:space:]]*[-*][[:space:]]+\[[[:space:]]\]"` = 4 open, 
`grep -cE "^[[:space:]]*[-*][[:space:]]+\[x\]"` = 43 ticked, 47 boxes; the any-indent substring counts are 4 and 44, and the one extra 
`[x]` is `:3`, the audit stamp prose "every [x] re-confirmed landed" quoting the form at itself — an over-read of exactly one, not a fifth self-quoting line. 
The two sibling files carry the same shape and are also NOT box-free: `todo-font-system.md` 146 lines, 5 open / 8 ticked anchored 
(6 any-open, the extra an indented sub-box); `todo-refactor-settings-agents-3.md` 588 lines, 2 open / 9 ticked anchored (5 / 10 any-form — 
three indented sub-boxes, which is the gap the rule about both forms predicts). No row was ticked by this pass, so the 43 stands.

One goal-level finding, because it is the programme question rather than a row: **the thin-shell objective is going backwards.** 
`wc -l apps/desktop-client/src/lib.rs` = **1,351** against the **1,281** this file recorded as its own baseline at `:47` — +70 lines, and the two most recent 
commits touching it are `98e8e29a8 docs(desktop-client,core): correct two stale doc comments - 453 handler entries` and `490b12c95 
style(desktop-client): clear clippy lints`, i.e. it grows with the handler table, not with tests. Sizing for whoever funds the next box: the file has 4 open rows, 
2 are genuinely open work (`:167`, `:168`), 2 are parked on environment or ruling (`:142`, `:169`, `:173` — three rows, one of them a milestone), and 0 are 
not-work: nothing in this plan names a path that is gone, so the programme is fundable, but its headline metric needs restating before another lane ticks toward it. → **Answered the same evening; see "Headline metric restated (2026-09-15 evening)" directly below. The "going backwards" verdict is withdrawn** — 5 of the +70 lines are new handler entries, which is where the Option A ruling at `:102` says a registered command must be recorded, and 31 of the remaining 65 are comment lines. Its figures stay above as the dated record they were.

---

## Headline metric restated (2026-09-15 evening, docs pass, HEAD `503591a10`, branch `0.0.39`)

> **What this pass did:** restated this file's headline metric, repaired one figure at `:47`'s row and one at the `:170` row, and recorded a self-citation defect below. **What it did not do:** nothing was ticked — 43 `[x]` / 4 `[ ]`, unchanged from "Shape of this file" above; no code file was edited; no `--release`, no `cargo check --workspace`, no UI suite was run. Every number carries the command that re-derives it, run from the repo root. Twenty-two paths in `git status` belong to other lanes (`oz-payment`, the cart UI, four deleted payment todos); this pass touched exactly one file.

### Why "lines of `lib.rs`" cannot be this plan's metric

Phase 3.2 closed on a **recorded ruling**, not an accident (Option A, `:102`): the `generate_handler!` list stays, because the verification gates parse that macro verbatim. That ruling re-reads true today, from the files rather than from the prose — `apps/desktop-client/src/commands/registration_gate_tests.rs:153` carries `const KEY: &str = "tauri::generate_handler![";` (a bracket-balanced, comment-stripped parse, per its own doc at `:150`), and `python scripts/verify-ipc-parity.py` exits 0 on its own first-`]` read of the same block.

**So the length of this file was pinned by decision on 2026-09-11.** A metric that measures a quantity a ruling fixed is not a progress signal — it is a trap, because the only way a lane can *improve* it is to undo the ruling and break three green gates. That is the mechanism by which "Shape of this file" could read `wc -l` = 1,351, call the objective "going backwards", and be green about it: nothing in the repo checks whether a metric matches its programme's own decisions.

### The decomposition (three revisions, one command each)

`git show <sha>:apps/desktop-client/src/lib.rs` → find the block at its `generate_handler![` line, end at the first line matching `^[[:space:]]*\][[:space:]]*)?[[:space:]]*;?[[:space:]]*$` → strip `//` **per line** → count non-empty interior lines as entries. *(The per-line strip matters: join-then-strip, which is how a first attempt here read, lets one comment line eat the rest of the file — it read 4 entries instead of 453, silently, and would have been quoted as a count.)*

| revision | total | block span | interior | **entries** | group-header comments | **outside the block** |
|---|---|---|---|---|---|---|
| `38d08f115` — Phase 3.0 baseline (branch `0.0.37`) | 1,281 | 780–1270 | 489 | 448 | 41 | 790 |
| `ec2edf258` — the 2026-09-14 audit | 1,349 | 843–1338 | 494 | 453 | 41 | 853 |
| `503591a10` — this pass | 1,351 | 845–1340 | 494 | **453** | 41 | **855** |

The +70 from baseline decomposes as **+5 handler entries** (448 → 453 *registered commands*, one line each, in the single place the ruling says they go) and **+65 lines outside the table** (`Compare-Object` over the outside region: 68 added, 3 removed). Of the 68 added, **31 are `//` comment lines** and 37 are code — and those 37 are one block: the kds-sync LAN discovery payload plus live queue provider, wired at `LanEventForwarder::new(...).with_discovery(...).with_kds_queue(...)` (its own comment cites `done-todo-kds-agents-2.md`, "App wiring — 13-09-26"), together with the typed `Settings::get_lan_server_psk` read replacing a raw one. That is daemon wiring — the residue class this shell keeps **by design**, named in the module note at `lib.rs:20` — arriving alongside 31 lines of explanation about an at-rest-encryption subtlety.

### The restated metric: three clauses, all three green at `503591a10`

| # | invariant | how to check it | value today |
|---|---|---|---|
| 1 | **The shell defines no command.** | `grep -cE '^[[:space:]]*#\[tauri::command\]' apps/desktop-client/src/lib.rs` | **0**. Unanchored the same pattern reads **3** (`lib.rs:10`, `:15`, `:25`) and all three are module-doc prose — the anchor is load-bearing. The only function in the file is `pub fn run()` at `lib.rs:95`; 0 `impl` blocks, no `mod tests`, 8 `mod` declarations. |
| 2 | **The table is one line per registered command, and carries nothing else.** | the interior split above, cross-read against `python scripts/verify-ipc-parity.py` | 494 interior = **453** entries + 41 header comments + **0** blank; 453 **distinct**, **0** duplicates, every one `commands::`-prefixed, across 59 module prefixes; parity prints `458 UI command strings, 453 registered … (27 allowlisted)` at **EXIT 0**. Two independent methods landing on 453 *is* the check. |
| 3 | **The Phase 3.2 dependency prune holds.** | grep `apps/desktop-client/Cargo.toml` for `axum` / `lettre` / `snow`, then grep `apps/desktop-client/src` for `axum::` / `lettre::` / `snow::` | 0 manifest lines and 0 `::` references for all three. `rusqlite` remains by ruling at **56 references across 24 files** today, against the 53/24 the 3.2 record left — file count held, ref count moved, which is the drift that record predicted rather than a change of policy. |

**Scale for clause 1, so nobody over-reads it.** `apps/desktop-client/src` holds **496** `#[tauri::command]` definition sites across 59 files against **453** registered paths. Those 43 sit in `commands/`, not in `lib.rs`, and parity grades a *name-level* form of the same gap and prints **16** (`F-006 tracker`). 43 ≠ 16 is a unit difference — sites against names — and this pass did not reconcile them; a future one that quotes either must say which.

### What a lane should take from this

- **Do not shrink `lib.rs`.** The number has no legitimate target. Registering a command adds exactly one line to that table and cannot add it anywhere else without breaking the parity / scoped-coverage / registration gates.
- **Do not invent a replacement target either.** The only region that *could* legitimately get thinner is the 855 lines outside the table, and tonight's measurement attributes its growth to daemon wiring plus comments — the shell doing the remaining job Phase 3.2 assigned it. No open row here asks for less, and this pass added none.
- **Read the old evidence backwards.** Wherever a lane sees 1,351 quoted as failure, the honest reading is *five commands registered, one feature block wired, thirty-one sentences of documentation added, and zero command bodies moved into the shell.*

### A same-class defect, found while restating: this file cites its own lines, and they have moved

A grow-only document invalidates its own line anchors on every append. Measured before this section was appended, the four open boxes sat at **142, 168, 172, 177**; appending this text — plus one bullet at the `:170` row — moved two of them, and they are at **142, 168, 173, 178** as this line is written, which is the defect demonstrating itself in the paragraph that names it. Against those positions the prose cites `:142` (exact), `:167` (−1), `:171` (−1), `:168` (−2) and `:173` (−4), and cites the setup-cache table row as `:229` when it is **235** (−6). Re-derive before quoting any `:N`: `grep -nE '^[[:space:]]*[-*][[:space:]]+\[[ x]\]' todo-refactor-oz-pos-app-agents-3.md`. New references should name row **text** or a **heading** — which is the rule this file already argues for code, at `popupBackgroundCompliance`'s "no line number is quoted for those three constants on purpose: lines move between revisions and names do not". The clauses above follow it: they cite `:47`, `:102`, `:170` only because those three are stable *phase-level* anchors quoted throughout, and every other pointer here is a filename, a heading, or a command.

---

## T4 implementation pass (2026-09-15 evening, branch `0.0.39`) — three of the four T4 rows landed, one is now an owner question

> **What this pass did:** implemented T4-1, T4-2 and T4-4 in four commits, re-measured T4-3 and left it open with its one remaining question named. **What it did not do:** it did not touch the four Phase 3.4 rows, and it did not tick T4-3. No box outside the T4 block was changed.

| row | outcome | commit(s) |
|---|---|---|
| T4-1 six-signature fork | **landed** — fork settled on the "tablet reaches the cards" branch | `84aed0b86` |
| T4-2 `taxRoundingMode` reset | **landed** — red first, then fixed | `67cdf9b6d` |
| T4-3 hardware storage source | **still open** — narrowed to one decision for the owner | — |
| T4-4 registration gaps | **landed** — registered one, deleted two | `3a77ed981` `f5f4173c2`, plus `0ebc8263e` |

**Checkbox state after this pass, anchored form:** open **5**, ticked **46** — `grep -cE "^[[:space:]]*[-*][[:space:]]+\[[[:space:]]\]"` -> 5, `'- \[x\]'` -> 46. The five remaining are T4-3, the two Phase 3.4 work rows, the Phase 3.4 verification row, and its milestone. This supersedes the "4 open / 43 ticked" figure in "Shape of this file" above, which was accurate when written and predates the T4 rows: the four T4 findings rows and their four action rows were added after it.

**A correction to a belief that was live in this workspace, because it decided T4-1.** The tablet shell was recorded elsewhere as having **no** F10 route into workspace settings, which would have made the six scoped setters unreachable and turned T4-1 into a dead-registration cleanup. It is not true today: `TabletAppShell.tsx:71-91` registers an F10 handler whose own comment reads "parity with the desktop shell", and `:125` renders the same `WorkspaceSettingsModal` the desktop renders at `AppShell.tsx:403`. `WorkspaceSettingsModal.tsx:57`/`:63` mount the restaurant-POS and store-POS cards, both of which call the scoped setters with `{ sessionToken, args }` and nothing else. That single fact is what makes T4-1 a real repair rather than a cleanup, and it is why the fix direction was the first branch of the fork rather than the second.

**The verification actually run this pass, with its prints.** `cargo test -p oz-bridge --lib settings::` -> **57 passed / 0 failed**. `cargo test -p oz-pos-tablet --lib` -> **656 passed / 0 failed** (654 at the T4 record, +2 added here). `cargo clippy -p oz-pos-tablet --all-targets -- -D warnings` -> **exit 0**. `cargo check -p oz-pos-tablet --all-targets`, `cargo check -p oz-pos-app --all-targets` and `cargo check --workspace` -> **exit 0** (the last in 43.83 s). `python scripts/verify-ipc-parity.py` -> **`IPC parity: OK`, exit 0**, tablet 323 registered / 155 unregistered / 156 allowlisted. `bash scripts/verify-scoped-coverage.sh` -> **`PASS: all registered commands covered`, exit 0**.

**Consequence for the Phase 3.4 verification row.** That row names three commands. Two of them now have a recorded run — `python scripts/verify-ipc-parity.py` (exit 0) and `cargo check --workspace` (exit 0) — and the third, `npm run check:all` in `ui/`, is still unreachable here because its E2E leg needs Docker and `127.0.0.1:15432` refuses. **The row stays open and unticked:** two of three is not the row's own acceptance, and its disposition already says never tick it on an inference.

**The one thing this pass could not decide, stated as a question rather than a plan.** T4-3 needs the owner to pick the canonical store for terminal hardware: the `settings` KV table (what the tablet reads and writes today, via `Settings::get_printer_*` / `set_printer_*`) or `hardware_profiles` in the global db (what the bridge reads and writes, storing a serialized `TerminalProfile` as `profile_json`). Until that is answered, landing the missing ten keys on the tablet has no correct implementation — and the row's own prohibition still holds, so re-exporting the bridge DTO is not a shortcut around the question but a way of converting ten visible absences into ten silent drops.

> Recorded 2026-09-15 by the T4 implementation lane. Files touched by this pass: the six in the four commits above, plus this file. The four Phase 3.4 rows were read and left alone; nothing outside the T4 block was ticked, opened or reworded.