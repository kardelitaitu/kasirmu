# Remediation Checklist - kasir.mu

**Progress at 2026-09-23.** **10 checklist items fully ticked** (C5, C5b, C6, C6b, C7 detection half, C9, C11 code half, C12, C13, C27) **plus 29 further slices landed inside items whose checkbox stays open until the whole item is done** (C1 S1 and S1.5, C2 step 1, C3 S1, C8 S1 and S2, C10a, C17 slices 1-2, C18 P1.1, P1.2, P1.3, P1.5). Every one names its commit SHA, and the verification log at the end carries the command and its result. Do not read a ticked line as 'this area is finished' - read the annotation beside it for the remainder.

Derived from manager-codebase-review.md (commit 954d4b094), 2026-09-23. Nothing here is new evidence: every item traces to a numbered section of that review, and the acceptance check is the one stated there.
---

## Wave 1 - before the next release (P0)

- [ ] **C1 [P0] Default to a real at-rest key instead of a public constant** (6.2, remediation P0-7) - **S1 DONE (012dd2663), S1.5 DONE (37a3c00b)**; S2a/S2b/S2c (install-key seam, keychain entry, `oz rekey`) and S3 (the .ozpkg PII pin) remain, plus one named follow-up: the two callers that swallow the new fail-closed error with `.unwrap_or(None)` (apps/desktop-tauri/src/lib.rs:703, platform/sync/src/daemon.rs:156) still degrade silently to "no credential" instead of surfacing it.
  Fence: crates/kasirmu-crypto/src/lib.rs:59-111; the boot path of apps/desktop-tauri/src/lib.rs, apps/mobile-tauri/src/lib.rs, apps/cloud-server/src/main.rs; the README clause that conflates the keychain with the at-rest key.
  **Slices from the D1 design** (each independently shippable, in this order): **S1** branch-tolerant read in crates/kasirmu-crypto/src/lib.rs - a candidate-key helper (legacy first, master second) behind one decrypt path that treats the AES-GCM tag as the sole oracle; no marker column, no envelope version byte, no schema change, no eager rewrap. Gate = the existing pin test in crates/kasirmu-core/tests/credential_storage_form.rs **inverted** (child under OZ_MASTER_KEY decrypts the parent's legacy row; a master-written row still fails in the parent). **S1.5** fail closed on shaped-but-undecryptable values at platform/core/src/settings/typed.rs:334, :361, :437, :553, :639 - today they `unwrap_or(v)` and hand the ciphertext back as the credential. **S2a** dormant install-key seam in crypto. **S2b** new keychain entry oz-pos/at-rest-key.v1 (generate-once-if-absent) wired at startup - do NOT reuse oz-pos/encryption-key, whose rotation archives the old key without re-wrapping any row. **S2c** opt-in `oz rekey` CLI that copies the DB first and prints counts, never values. **S3** a pin test that .ozpkg user JSON never carries national_id or monthly pay.
  Done when: a per-install key is held in the OS keychain, a test asserts the static fallback cannot be reached in a release build, and a row written under the legacy derivation still decrypts after the key exists. **Precondition, and it is most of the work: make the reader branch-tolerant first** (see D1) - simply setting the environment variable, or refusing to boot without it, silently bricks five credential families and both PII columns on any existing install, including two families that have no product setter to restore them.

- [ ] **C2 [P0] Put an integrity gate on plugin loading** (6.4, P0-8) - **first step DONE (28a2bfeb)**: content fingerprint at load, automatic hot-swap REMOVED (a change is refused with a visible error and the running set survives; restart picks up a change), and the three unread capability flags now fail closed. Signature verification and the operator grant store remain - they are the part your D7 answer decides.
  Fence: crates/kasirmu-plugin/src/lib.rs:8-12; manager.rs:109-129 and :218-308; apps/desktop-tauri/src/state.rs:325-347 and :702-755; manifest.rs:186-192.
  See D7 for the recommended shape (signed/checksummed manifest, operator grant, gated watcher - not removing the host). Done when: a modified .lua file is refused with a visible error and the previous plugin set keeps running; required_permissions become an operator grant rather than a self-declaration; allow_network / allow_filesystem / allow_http are either enforced or deleted.

- [ ] **C3 [P0] Make the pull idempotent per effect, not per item** (5.1, P0-2) **Slice status:** S1 DONE (schema befd3b71 + the index-surface pin bumped to 188 in 0db7ba6c - the pin is a census assertion over sqlite_master, so every migration that adds or drops a named index must move it, and the comment block above it now explains why each increment happened); **S2 DONE (6614f522)**: the origin and the effect key are threaded through the queue, both server stores and the transport (offline 91 tests, cloud sync_store 22). THE WARNING IN THE DESIGN WAS RIGHT: the miss was real and lived in a file the fence did not name - apps/cloud-server/src/sync_store.rs holds TWO MORE server INSERT column lists, the per-item fallbacks that run whenever the multirow fast path errors, and touching only pg.rs/sqlite.rs would have silently dropped the field for exactly those batches. The worker found it, included it, and added a discriminating test that poisons an id to force the fallback and was verified to fail when the fix is reverted. Declared open item: the applier still writes a NULL effect_key, so the partial index is inert until the receipt call is switched (S3a: BLOCKED on a fence conflict - the C4 lifetime-spend worker held queue.rs, so S3a stopped with zero edits rather than sweep foreign work. Its researched handoff is in the journal: the receipt writer is queue.rs:868 and the arms must change signature to yield Option<String>, with the effect keys per arm listed there, and two arms that cannot carry a key at all - stock.adjusted (no id in its payload) and stock.movement under the crdt_delta envelope (two effects in one item). Re-dispatch S3a once queue.rs is clean.). S3-S7 remain.
  Fence: platform/sync/src/queue.rs:438-443; apps/cloud-server/src/sync_store/pg.rs:163-166; plus a migration adding an origin/terminal column to offline_queue (20260813_init.sql:353-362 shows it has none).
  Done when: a test pushes a complete_sale, pulls it back, and asserts stock moved exactly once. Prefer the client-side origin check; use the server-side terminal filter only if a second puller class appears. **Prerequisite: C12.**

- [ ] **C4 [P0] Give refunds, voids and payments a sync arm** (5.2, P0-3)
  Fence: platform/sync/src/queue.rs:558-563 - they currently fall to Err(unsupported remote sync action) and dead-letter.
  Done when: a refund made on terminal A is visible on terminal B after a pull, with stock and shift figures consistent on both. Copy the idempotence pattern already used by finalize_sale (queue.rs:553-557) rather than inventing one.

- [x] **C5 [P0] Stop counting voided and pending sales as revenue** (8.2, P0-4) - **DONE 2026-09-23**, desktop 743f222f + tablet 1eee8e0f (C5b)
  Fence: crates/kasirmu-core/src/db/sales.rs:266-269 (export_daily_summary, no status predicate); crates/kasirmu-bridge/src/history.rs:319 versus :326, :347, :358 (two day definitions on one sheet).
  Done when: a voided sale does not change total_revenue, and the EOD header reconciles with its own payment breakdown on a refund-and-void fixture - a fixture that does not exist today and has to be written.

- [x] **C6 [P0] Make the report timezone contract match the write contract** (8.1, P0-5) - **DONE 2026-09-23, commit 7559a3f6b** (the provisioning-validation half is split out as C6b)
  Fence: crates/kasirmu-core/src/db/reports/datetime.rs:26-47 and :96-98 (25 call sites inherit the silent UTC fallback); crates/kasirmu-core/src/timezone.rs:18-45 (the correct mapping that already exists); crates/kasirmu-core/src/db/provisioning.rs:467-476 and :514-564 (no validation of the timezone it writes); ui/src/features/analytics/analytics-data.ts:86-97 and :118.
  Done when: a store provisioned as Asia/Jakarta reports a 00:30 local sale on that local day, asserted by a test, and provisioning rejects or normalizes anything the reporting path cannot interpret. This is a code fix - there is no data fix, because the writer refuses offsets.

- [x] **C7 [P0] Turn tenant isolation on for real - as a role change, not a schema edit** (7.1, P0-6) - **detection half DONE (e7aec886)**: a pure RlsPosture::from_facts verdict (Enforced / BypassedBySuperuser / BypassedByOwnerRole / PartiallyEnforced / NoProtectedTables) with 9 unit tests, an ERROR-level boot report, and rls_posture on /health plus its published schema. The protected-table set is read from pg_policies rather than pinning the generated 34 in Rust. The role cutover itself remains an owner/ops action. **THE SQL IS NO LONGER CODE-REVIEW-COVERED ONLY - IT IS MEASURED, commit 587aa0c88 (2026-09-23, `test(cloud): verify the RLS posture against a live PostgreSQL`, apps/cloud-server/src/db_tests.rs +345, db.rs UNTOUCHED - the shipped logic was tested, not rewritten).** The live verdict on the dev container is **`bypassed_by_superuser`**: `role=postgres superuser=true forced=0 protected=34`. Independent ground truth was taken BEFORE trusting the Rust code - `docker exec ... psql` on the same query returned `postgres|t|0|34` - and the test then corroborates all three facts against separate catalog reads (`current_user`, `pg_roles.rolsuper`, a `pg_policies` count) rather than against itself. **So the documented blocker is now a measured fact: the profile connects as a superuser and all 34 `tenant_isolation` policies are inert.** Negative controls in both directions: in-process, four distinct verdicts are asserted with `assert_ne!` between them (superuser+all-forced -> BypassedBySuperuser, non-superuser+0-forced -> BypassedByOwnerRole, non-superuser+all-forced -> Enforced, zero protected -> NoProtectedTables); and LIVE, `SET ROLE oz_app` on the test's own session CHANGES the verdict, with the result captured before asserting so a panic cannot return a pooled connection still wearing `oz_app`, and `RESET ROLE` restoring it. `/health` was proven end to end at the HTTP layer through the real `build_router` with a real PG pool, carrying `"db":"postgres"`, `"db_connected":true` and `"rls_posture":"bypassed_by_superuser"`. The reachability control is the strongest form available: against a DEAD port the suite printed **7 skip messages in 321.82s**, against the live port **0 skips in 61.20s**, both 36 passed - so a green run is proven to have RUN rather than skipped. **One finding worth keeping from getting there:** a first run reported `rls_posture: "unknown"`, `db_connected: false`, `db_latency_us: 2003217` because the health handler's own 2s pool-checkout bound fired on a loaded container - the health path was correct and the TEST was starving it. Pre-warming the pool dropped latency to 6511us. So **on a loaded container `/health` can report `unknown` for connection-establishment reasons unrelated to posture** - a property of the 2s fail-fast design, not a defect, and the pre-warm is the knob if that assertion ever flakes (the ORIGINAL claim that follows was false and is kept visible for the record): this environment has no PostgreSQL
  Fence: scripts/rls-cutover.sql:7-10, :33-42, :56 (its comment claiming 19 tables is stale against its own 34-table array); the connection configuration that carries DATABASE_URL.
  Sequence (see D2): create the oz_app role, grant DML on the 30 tables, point DATABASE_URL at it **and set OZ_APPLY_SCHEMA=0 in the same step**, verify, and only then consider FORCE as the follow-up. Keep **both** BYPASSRLS roles (webhook resolution and email discovery/prune) and the /status tolerance. Note the blocker this review first missed: the shipped PostgreSQL profile connects as a **superuser** (docker-compose.pg.yml sets POSTGRES_USER and DATABASE_URL from one variable, :21 and :39), and superusers bypass RLS even with FORCE on - so the cutover alone enforces nothing. Add a boot-time rolsuper / relforcerowsecurity assertion. Never add FORCE to the generated migration while the app connects as the table owner - every query would return zero rows on deploy.
  Done when: a tenant-table query without the tenant GUC returns zero rows and a write is rejected, a query with the GUC returns only that tenant's rows, and the reversal (NO FORCE plus DROP ROLE) is written down and rehearsed. Decide D2 first: if no deployment has run the cutover, this is live.

- [ ] **C8 [P0] Make recovery real: restore in the app, a backup that survives, verification on both sides** (14.1, P0-9) - **slice S1 DONE (6814222)**: backups are copy -> verify (integrity_check on the snapshot) -> rotate 3 generations -> atomic rename; a failed copy leaves the previous snapshot byte-identical; remove_destination_for_backup deleted with the directory-target typed error preserved. **S2 DONE (3add5b27)**: validate_candidate returns a typed verdict (Acceptable / OlderButAcceptable / NewerThanThisBuild / Corrupt plus a reason - never a bare bool), restore_from validates, snapshots to <db>.pre-restore.db, deletes the sidecars only AFTER validation, stages and re-verifies before an atomic rename, and rolls back on failure; the CLI now delegates to it so one validator and one swap serve both lanes (9 recovery tests pass, plus the 21 backup_restore_integration tests). A candidate with no schema_migrations table is OlderButAcceptable, which is what keeps a pre-migration snapshot restorable. **S3 DONE (9b551e76)**: list_restore_candidates reports each generation with its verdict (a corrupt one is LISTED as Corrupt, not omitted), restore_prepare gates on SETTINGS_EDIT then validates then requires the store name read from the CANDIDATE itself before writing <db>.restore-request.json, and restore_status reports a pending request (an unparsable one reports pending WITH an error, never 'nothing pending'). Nothing is restored by this slice - the boot consumer is S4. S4-S7 remain.
  Fence: crates/kasirmu-cli/src/commands/backup.rs:62-98 (the only restore); crates/kasirmu-core/src/db/mod.rs:264-272 and :281-305 (destination deleted before it is written) and :319 (check_integrity, no production caller); crates/kasirmu-bridge/src/data.rs:146-150 and :330-348; apps/desktop-tauri/src/commands/data.rs; ui/src/api/data.ts; ui/src/app/UpdateBanner.tsx:143-155 and :179-189.
  Approach (see D5): a **safe-mode boot flag** rather than an in-process restore - the live connection is an Arc shared with twelve detached daemons that cannot be joined, and there is no restart primitive. Done when: a restore_roundtrip test backs up through the same command the UI calls, corrupts the live database, restores from the app path, and asserts check_integrity() is Ok with a known sale reading back; a second case asserts a corrupt backup is refused and the live file is left byte-identical. Backups write to a temporary name and rename over the previous only on success; at least two generations are kept; the app either quiesces or refuses a restore while another process holds the database.

  **Slices from the D5 design** (each independently shippable; ship S1-S2 before S3+): **S1** backup durability in crates/kasirmu-core/src/db/mod.rs - tmp file in the same directory, check_integrity on the tmp, atomic rename, rotate three generations, delete remove_destination_for_backup. **S2** validate_candidate + restore_from (validate, pre-restore snapshot to `<db>.pre-restore.db`, delete -wal/-shm, tmp+rename, re-verify, roll back on failure), with the CLI's run_restore refactored onto it. **S3** bridge: list_restore_candidates / restore_prepare (SETTINGS_EDIT plus a typed store-name confirmation, writes `<db>.restore-request.json`) / restore_status. **S4** boot: a recovery module in both shells called BEFORE AppState::new, so no live connection or detached daemon is involved; a `<db>.restore.lock` made with create_new guards a double boot. **S5** IPC plus a Safe Mode screen with locale strings. **S6** the updater writes `<db>.restore-candidate.json` after its pre-update backup, because updater.last_backup_path is unreadable exactly when it is needed. **S7** runbook and CLI help. Schema gate: an OLDER candidate is acceptable (forward re-apply), a NEWER one is refused.
- [x] **C9 [P0] Decide what qris-core is, and make the manifest say it** - DONE 2026-09-23, commit 57e0f8c84 (14.4, P0-10)
  Fence: crates/qris-core/Cargo.toml:6, :7, :9 (publish = true, MIT OR Apache-2.0, repository YOUR_ORG); deny.toml:83-268 (no entry for it, while MIT and Apache-2.0 are already allowlisted so cargo deny passes); root Cargo.toml:40 and :42.
  Done when: either it is proprietary like everything else (publish = false, license.workspace = true, a deny.toml clarification entry) or it is genuinely dual-licensed (LICENSE-MIT and LICENSE-APACHE committed, a real repository and authors, the deny.toml entry). cargo deny must no longer be blind to the difference. Owner decision D4: recommendation is to make it proprietary - zero dependents and no offline QRIS role today, while the code is worth keeping for the planned notification listener.

- [ ] **C10 [P0] Fix the transaction mode, the missing guard and the test that cannot see either** (4.1-4.3, P0-1) - **transaction half DONE (515989936)**: all seven doors open TransactionBehavior::Immediate, fresh_db() applies production's pragmas, and the regression test now sets a busy timeout, asserts the loser fails with a busy/locked error, and detects SQLITE_BUSY/BUSY_SNAPSHOT. **The `CHECK (qty >= 0)` half is RE-SCOPED by D11 and is NOT what this item said** - see the C10b block below. The unconditional constraint contradicts a shipped feature.
  Fence: sales_checkout.rs:210, sales_lifecycle.rs:179 and :606, refunds.rs:66, gift_cards.rs:53, loyalty.rs:463, shifts.rs:109 (Immediate); a backfill migration before CHECK (qty >= 0) on stock_summary lands (20260813_init.sql:739-746 has none today); sales_tests.rs:3022-3092 (add busy_timeout, assert the lock error); migrations.rs:463-509 (fresh_db must apply production pragmas).
  Done when: (a) all seven sites use TransactionBehavior::Immediate; (b) the CHECK lands **after** a backfill that clamps or quarantines existing negatives, or it bricks startup; (c) fresh_db() carries busy_timeout 5000 and the loser of the concurrency test fails with a lock error rather than an unexamined one. **Prerequisite: C11.** Do not hunt for a reproduction of an oversell - the defect is the false invariant and the untested error path.

- [x] **C11 [P1, prerequisite] Snapshot before every migration** (14.2) - **code half DONE 2026-09-23, commit 9c6d76e1**; the runbook/pre-flight half is split out as C11b
  Fence: platform/core/src/database/migrations.rs:112-121 (drift is repaired by re-running SQL on live data) and :424-446 (per-statement fallback loses per-file atomicity); docs/operations/ (no pre-upgrade procedure for the desktop file); scripts/backup-db.sh.
  Done when: a test asserts a snapshot exists after migrations::run, a migration that fails midway leaves both the database and the snapshot intact, and the runbook names the four pinned un-re-runnable migrations (migrations_tests.rs:361-366) as the reason.

- [x] **C12 [P1, prerequisite] Variance report for stock already double-deducted** (5.1 remediation) - **DONE 2026-09-23, commit bdaffa47**; wiring it to an operator surface is C12b
  Fence: a new reconciliation over stock_summary against stock_movements per (item, location).
  Done when: the report exists, has a test on a seeded double-deducted fixture, and an operator can accept or adjust each difference. Never write a blanket qty = SUM(deltas) update - it destroys legitimate manual adjustments.

---

## Wave 2 - next, each small and testable (P1)

- [x] **C13 Bound the Lua tax hook and close the override asymmetry** (6.5) - range-check rate_bps exactly as discounts are checked (kasirmu-lua, sales.rs:473-506), and make the shortfall door pass the same overrides as the main door (bridge/pos.rs:1868-1873 versus :1567, :1575). - **DONE (1b7bd246 + 803efecf)**: plugin discounts now require SALES_DISCOUNT, a Lua rate_bps outside 0..=MAX_TAX_RATE_BPS is REJECTED (not clamped, so the rule owns the amount), and the shortfall door now passes the same overrides as the main door. 3 new bridge pos tests + 5 core sales_tax tests; 58 bridge pos tests and 158 core sales tests pass.
- [ ] **C14 Stop treating local_api.secret as plaintext and stop double-purposing it** (6.6) - crates/kasirmu-local-api/src/lib.rs:48-60, :202-227; clamp expiry_hours on POST /api/v1/tokens as the IPC mint already does (kasirmu-api/src/auth.rs:200-202).
- [ ] **C15 Give one sale one total** (9.2) - decide whether tip and service belong in sales.total_minor or only in payments, and make validate_payment_splits_cover_total compare like with like (db/sales.rs:247; sales_checkout.rs:522-534, :613).
- [ ] **C16 Fix the drawer: split tender and refund attribution** (8.4) - derive expected_cash from the payments table rather than the payment_method stamp; attribute refunds to refunds.processed_by (db/shifts.rs:147-186 versus :302-314; refunds.rs:340-342). Add the missing close_shift tests for split tender and house-account credit.
- [ ] **C17 Work the IPC gate debt down deliberately** (7.3) - **slice 1 DONE and verified (3aa5af0e)**: tablet ceilings 95 -> 92, class-1 51 -> 48, REGISTERED_TOTAL 345 -> 342, all four drift pins green; **slice 2 DONE (0eb2a045)**: the three dead settings reads deleted, ceilings 92 -> 89, class-1 48 -> 45, REGISTERED_TOTAL/FLOOR 342 -> 339, all four drift pins green, IPC parity gate OK. get_hardware_settings deferred to C17b. Items 1, 4-10 remain - delete the renderer-supplied user_id variant of settings::set_setting in favour of the scoped twin (commands/settings.rs:243-253); gate create_backup or document it as a deliberate unauthenticated filesystem write (bridge/data.rs:330-347); lower the two generated ceilings, never raise them.
- [ ] **C18 Wrap the money-path autocommit writes** (4.4) - ~~138 conn.execute sites in db/ outside tests~~ **CORRECTED BY MEASUREMENT 2026-09-23: 131 sites across 40 non-test files, of which 99 sit in functions with exactly ONE execute - already correct, because on a single shared `Store` connection a bare statement joins an open transaction implicitly, so a wrap there is a no-op and a test for it cannot fail. The real work is ~12 multi-statement functions. See the P3 note below and C61.** Original: make update_sale_status a conditional in-transaction update like void_sale already is (sales_crud.rs:558-600 versus sales_lifecycle.rs:787-798). **Slice status:** P1.2 DONE (b0e78e69) - create_cash_payout is now a compare-and-set inside the write, with a deterministic race test that was proved to fail against the unfixed SQL before shipping; P1.1 (update_sale_status) and P1.5 (fiscal numbering) in flight.
  **Ordered P1 list** (money and stock first, from the inventory in the journal): P1.1 sales_crud.rs:591 update_sale_status - dispatched (compare-and-set; its Conflict branch is unreachable today); P1.2 cash_payouts.rs:51 - dispatched (check-then-act race against the shift); P1.3 shifts.rs:80 open_shift (**DONE 4518a2b8**, proven RED against the unfixed SQL - the second open won and persisted a duplicate); P1.4 gift_cards.rs:635/:668 freeze/unfreeze; P1.5 fiscal.rs:213 statutory numbering (**DONE 2243cf096 - but reclassified: the CLAIM path at fiscal.rs:399 was already atomic inside the new IMMEDIATE transactions, so this was a house-rule fix on the bare autocommit upsert, not a race, and no test can fail against the pre-fix code; the worker proved that by running its race test against the unfixed code and watching it pass**); P1.6 payables.rs:63 (**DONE 3abd6e67**); P1.7 purchase_orders.rs:316 (**DONE 291b9065**); P1.8 products_crud.rs:555/:576 (**DONE 4fea3894**, honest partial - no discriminating test exists); P1.9 products_stock_query.rs:107 (wrap at its sync caller platform/sync/src/queue.rs:666, whose sibling arm already does); P1.10 stock_counts.rs:61-332; P1.11 stock_transfers.rs:327/:370 (its delete-then-compensate must be one transaction); P1.12 tables.rs:300/:320; P1.13 products.rs:248/:276.
| C10a | 2026-09-23 | grep TransactionBehavior::Immediate across the seven doors | sales_checkout 1, sales_lifecycle 4, refunds 1, gift_cards 1, loyalty 1, shifts 1 - all seven sites converted | 515989936 |
| C10a | 2026-09-23 | cargo test -p kasirmu-core concurrent_complete_sale | 1 passed - the test now sets busy_timeout (sales_tests.rs:3248), asserts the loser fails with a busy/locked error (:3278), and detects SQLITE_BUSY/BUSY_SNAPSHOT (:3290) | 515989936 |
| C10a | 2026-09-23 | cargo test -p kasirmu-core sales | 184 passed, 0 failed (was 177) | 515989936 |
| C18 P1.5 | 2026-09-23 | cargo test -p kasirmu-core fiscal | 19 passed, 0 failed - house-rule fix on the sequence upsert; the claim path was already atomic and the worker proved it by running its race test against the unfixed code and watching it pass (so no discriminating test exists) | 2243cf096 |
| C18 P1.1 | 2026-09-23 | cargo test -p kasirmu-core sales_crud | 2 passed - a_committed_competing_transition_is_not_overwritten and a_transition_that_lost_the_race_reports_the_conflict (the CONFLICT path, not a happy path) | c57c5e7ce |
| C8 S2 | 2026-09-23 | cargo test -p kasirmu-core recovery | 9 passed, 0 failed - four new cases (accept + restore, corrupt refused with live db AND sidecars untouched, newer-than-build refused with the newer reason, older accepted) plus the three S1 cases | 3add5b27 |
| C8 S3 | 2026-09-23 | cargo test -p kasirmu-bridge data | 70 passed, 0 failed - six new cases proving the FILE on disk, not just the DTO: a corrupt candidate refuses with no request written, a wrong store name refuses with no file, the right name writes a request naming that candidate, and a corrupt generation is LISTED as Corrupt rather than omitted | 9b551e76 |
| C18 P1.6 | 2026-09-23 | cargo test -p kasirmu-core payable | 21 passed - create_payable now opens the transaction before its first check and delegates to a create_payable_in_tx body; the worker PROVED the composition test bites by bypassing the wrapper and getting a nested-transaction panic, and then DOWNGRADED its own other test's claim because that one also passes with the wrapper bypassed (the FK-checked trigger aborts the write independently) | 3abd6e67 |
| C18 P1.7 | 2026-09-23 | cargo test -p kasirmu-core purchase_order | 51 passed + 1 integration - update_po_status is now a compare-and-set; the worker proved the test bites by deleting only the AND status predicate and watching it FAIL (it had validated only the status vocabulary, never a legal transition, and never read the current status) | 291b9065 |
| C4 S1 | 2026-09-23 | cargo test -p platform-sync | 398 passed - eight new named cases covering refund credit-once + replay no-op, refund with the sale ABSENT applying the effect, void active-to-voided + replay, void of a completed sale CONFLICTING, payment keyed and unkeyed idempotence, payment without the sale as a benign no-op, and the legacy applier consuming all three arms. DECLARED GAP: the sale-present path reverses loyalty points but NOT the customer lifetime-spend figure, because the real helper is pub(crate) and unreachable - tracked as its own follow-up | f147ef37 |
| C4 follow-up | 2026-09-23 | cargo test -p platform-sync + cargo test -p kasirmu-core loyalty | 399 passed and 53 passed - the lifetime-spend reversal now applies on a propagated refund, and the worker PROVED the new case fails against the pre-fix code (behaviourally disabling the call gave 1000 vs 600). IT ALSO CORRECTED MY BRIEF: total_spent_minor is not in loyalty.rs at all - the originator's reversal is inlined in refunds.rs:408-434 - so widening the helper alone would NOT have closed the gap; it widened the helper AND added the missing spend reversal. Declared: loyalty points now have ONE writer, lifetime spend still has TWO (a mirror in queue.rs), which the next slice consolidates | 760c82d2 |
| C4 one-writer | 2026-09-23 | cargo test -p kasirmu-core --lib refund + cargo test -p platform-sync | 64 passed and 399 passed - reverse_customer_spend_on_refund is now the single public writer in refunds.rs, both callers delegate, the queue.rs mirror is DELETED, and ZERO test assertions changed (behavioural equivalence on a money rule, which is the only acceptable proof for a refactor) | aacd0356 |
| C3 S3a | 2026-09-23 | cargo test -p platform-sync | 402 passed - the receipt is keyed on the EFFECT: a pure helper derives the key from the identity the originator already minted, so no arm signature changed, and the pre-check probes item_id OR effect_key. BOTH halves were falsified independently (reverting only the pre-check FAILED the redelivery case with stock deducted twice; reverting only the write FAILED with left None right Some("sale:sale-42:deduct")). Two pre-existing tests were UPDATED because the contract moved - a replay under a fresh item id now reports applied:false - and the worker showed the invariants they protect (status stays voided, payments_row_count 1) are still asserted. stock.adjusted and crdt_delta stock.movement return None deliberately (no single effect to key on) | 359f3b85 |
| C3 S4 | 2026-09-23 | cargo test -p platform-sync | 408 passed - the origin gate is in: before the transaction, an item whose origin equals THIS install's sync_terminal_id is receipted (effect-keyed) and NOT applied; a NULL origin is never treated as self, so legacy and in-flight rows apply exactly as today. Both new skip tests were FALSIFIED by neutering the guards (2 FAILED, then queue.rs restored byte-identically by blob hash). CRITICAL GAP DECLARED: nothing stamps a real origin yet, so the primary gate is DORMANT and only the secondary sales.terminal_id guard bites - the producer-side stamp is slice S5a, dispatched | c8486bb2 |
| C3 S5a | 2026-09-23 | cargo test -p kasirmu-core offline | 95 passed - the enqueue lane now stamps origin_terminal_id from Settings::get_sync_terminal_id, read ONCE per call, covering both funnels (enqueue_offline_inner for the conn lane and enqueue_offline_in_tx for the settlement door, which is the lane the double deduction was observed on). An unpaired install writes SQL NULL exactly as before, and a lookup ERROR is propagated rather than degraded to None - because a NULL written after a failed lookup is indistinguishable from a genuine unpaired install and would silently reopen the double deduction. With S5a landed the S4 gate is LIVE, not dormant | a15c5b68 |
| C18 P1.7 flake | 2026-09-23 | cargo test -p kasirmu-core --lib (full target, under load) | 3246 passed, 0 failed in 219.84s - the timer-based interleaving was replaced with a test-controlled one, so the race no longer depends on machine load. The worker confirmed the falsification still bites (removing the guard still FAILS the test), then died before committing; the manager ran the isolated case 6 times and the full lib target under load, and committed it at the wave gate | see log |
| C8 S4a | 2026-09-23 | cargo test -p kasirmu-app --lib recovery:: | 8 passed - the desktop shell now consumes <db>.restore-request.json BEFORE AppState::new opens anything. Three design points worth keeping: it NEVER fails the boot (returns an Outcome, no ? on that path); it IGNORES the verdict stored in the request and re-validates the candidate at boot, so a candidate swapped or truncated since the prepare is caught; and it treats the request file as untrusted input (absolute path, no '..'). The worker also caught its own bug - it first kept the lock on success, and its test showed a stale lock would refuse the NEXT request, so success now archives the request instead. Declared gap: the request JSON is matched by a hand-built fixture, not by the real writer, and the two suffix constants are duplicated (the bridge's is private) so they agree by inspection only - the round-trip test is dispatched | 4781b122 |
| C8 contract pin | 2026-09-23 | cargo test -p kasirmu-bridge data | 73 passed - the request file contract is now pinned against the CONSUMER'S OWN LITERAL, not against the writer: the exact file name, the exact JSON key the consumer deserializes (candidate_path), and the absolute-without-'..' path rule. BINDING PROOF: flipping the test's literal suffix to a typo made all three tests FAIL with NotFound, then reverted. It also reported the residual honestly - the suffix still exists as two copies, so a rename on the consumer side alone is only caught when the bridge test runs | d539eb66 |
| C8 S4b | 2026-09-23 | cargo test -p kasirmu-mobile --lib recovery:: (**SCOPED**) and cargo test -p kasirmu-mobile --lib (**FULL**, for honesty) | the TABLET now consumes the pending restore too - apps/mobile-tauri/src/recovery.rs (252 lines) + recovery_tests.rs (243) + the boot call in lib.rs + the approved one-keyword `pub(crate)` promotion of `resolve_db_path` in state.rs. Same invariants as the desktop consumer: runs BEFORE AppState::new, never fails the boot, ignores any stored verdict and re-validates, treats the request as untrusted input, archives only after success, and claims `<db>.restore.lock` with `create_new` so a double boot cannot race. Note for the record: this file was written THREE times over - two workers failed at the verify step on link contention and a third landed it. **HONEST NUMBERS, both kept:** the scoped run is `8 passed; 0 failed; 660 filtered` (all three required cases present and passing - nothing was written, deleted or weakened to get there), and the FULL mobile lib target is `667 passed; 1 failed` with TRUE exit 101 - the one failure is `commands::registration_gate_tests::drift_pin_no_computed_command_names_in_ui`, which is NOT this slice: the worker proved it by showing both gate inputs are byte-identical to its parent commit (`git diff --stat 8ce101607 HEAD -- <gate file> <offender file>` empty), that the offender arrived in `e2e4921f7` (a tracked, clean ancestor), and that the DESKTOP twin of the same gate PASSES while the mobile one has ZERO references to the test-scaffold tolerance machinery (`counts_as_test_scaffold` / `toleration_applies`, 11 references desktop-side). That gap is filed as C37 | 7acc00f6a |
| C3 S5 | 2026-09-23 | cargo test -p kasirmu-cloud sync_store + cargo test -p platform-sync | 24 passed and 411 passed - the server now filters a pull by the caller's terminal identity taken from the VERIFIED claim (never the body): `AND ($n::text IS NULL OR origin_terminal_id IS NULL OR origin_terminal_id <> $n)` appended to the pull SELECT only, in all three store shapes and all four build_pull_sql arms, written as a bound-value-IS-NULL disjunct so each shape stays ONE prepared statement. LIVE POSTGRESQL WAS REACHABLE here, so the three required cases ran against BOTH backends rather than settling for sqlite. Both halves falsified (neutering the sqlite filter -> 2 FAILED; neutering with_terminal_id -> the live-PG test FAILED) and both files restored byte-identically. Unpaired install or admin-minted token -> None -> no filter, byte-identical to today; a lookup ERROR also degrades to None deliberately, because this filter is defence in depth behind the per-effect receipt and must not stop the daemon syncing | b4c489c4 |
| C18 P1.8 | 2026-09-23 | cargo test -p kasirmu-core product | 236 passed - the two mutually exclusive arms are now ONE conditional statement whose expected_version binds to "?9 IS NULL OR version = ?9", wrapped in an IMMEDIATE transaction with the conflict-vs-not-found read-back inside. HONEST PARTIAL: the worker built the deterministic race test, ran it against the UNFIXED code and watched it PASS (SQLite's own implicit transaction already gave the old single autocommit UPDATE its CAS property), so it DELETED the test rather than ship one that passes either way - the real defect was the error diagnosis reading a different snapshot, which is not observable without progress_handler/update_hook | 4fea3894 |
| C18 P1.4 | 2026-09-23 | cargo test -p kasirmu-core gift_card | 26 passed - freeze/unfreeze are compare-and-set with the state read OUTSIDE the transaction; the worker DEMONSTRATED the negative control by reverting only the two SQL predicates and watching the two forced-interleaving race tests FAIL with the lost update visible in the panic payload (it got the winner's row clobbered). It also separated the tests that discriminate from the two state-outcome tests that pass either way by design, because those pin the pre-check | 39c6cc03 |
| C18 P1.3 | 2026-09-23 | cargo test -p kasirmu-core --lib shift | 77 passed - includes open_shift_race_cannot_create_a_second_open_shift, which FAILED against the unfixed SQL (the second open persisted a duplicate) | 4518a2b8 |
  **P2:** **audit.rs:369 log_audit DONE 2026-09-23, commit d76527e429** (`fix(core): make log_audit join an open transaction and own one otherwise`; 3 files, +160/-26) - the decision is (b) with a deliberately (c) in-transaction arm: outside a transaction `log_audit` opens and COMMITS its own, inside a caller's it JOINS and the row is LOST if the caller rolls back. That semantic existed by accident before and was documented nowhere; it is now stated in the method's docs and pinned by four tests. `cargo test -p kasirmu-core audit` -> **115 passed, 0 failed**. **The negative control found more than predicted**: forcing the autocommit arm to always open `unchecked_transaction()` fails not only the new test but a REAL production caller - `db::sales::tests::void_sale_changes_status_and_logs_audit` (sales_tests.rs:697) dies with `cannot start a transaction within a transaction`. So the branch is load-bearing, not defensive, and the naive wrap is the fix that would have shipped without this slice. Honest note carried from the author: **no test discriminates pre-fix from post-fix** (a bare `execute` was already atomic in autocommit and already joined an open transaction), so the discriminating evidence is against the naive wrap, and the commit body says so rather than claiming a HEAD failure that does not exist. Remain in P2: loyalty.rs:126/:651; promotions.rs:242 (latent, no production caller).
  **P3 - RE-SCOPED BY MEASUREMENT, 2026-09-23: this was NOT 89 correct sites of defect, and ~87% of it is a house-rule restatement rather than correctness work.** The original wording here was 'P3: 89 sites - batch one transaction per public fn at the boundary, personal-data subset first (customers.rs:196/:250, staff.rs:664/:698/:723)', and a read-only premise check has now shown what that population actually is. **THE REVIEW HAD ALREADY HEDGED, IN THE SAME SENTENCE IT COUNTED:** its heading is 'Writes that are not in transactions, and balances with more than one writer' and its body says *'There are 138 autocommit-capable conn.execute sites in db/ outside tests ... **Some ride an outer transaction, some do not.**'* - so it flagged the population BY A GREP COUNT, not by behavioural analysis, and the two concrete defects it actually named were `update_sale_status`'s unreachable Conflict branch (class b, done as P1.1) and the multi-writer loyalty balances (class b). The checklist then restated the count as a work list, which is where a house rule became a correctness mandate. **THE STRUCTURAL FACT THAT DECIDES IT: on a single shared `Store` connection, a bare statement issued while the caller's transaction is open ALREADY JOINS IT IMPLICITLY** - which is why the C18 P1.9-P1.13 worker found only one of nine rollback tests discriminating, and why the landed fixes' own doc comments justify themselves as CALLER-COMPOSABILITY (*'correct in isolation ... but it left the caller unable to make the ledger row commit or roll back with its own work'*) rather than as data-integrity repairs. **MEASURED CLASSIFICATION:** grouping every `conn.execute(` in `db/` (excluding `_tests.rs`) by enclosing function gives **131 sites across 40 files - 99 in functions with exactly ONE execute, and 12 functions holding 2 or more**; of those 12, ELEVEN hold no transaction (only `staff.rs@448` does). Class (a) single-statement = already correct, and a wrap plus a test for it manufactures coverage that cannot fail. Class (b) multi-statement = the REAL work. Class (c) hand-rolled `BEGIN` = the only class that falsifies cleanly, and it had exactly one live site (`stock_counts.rs:106`), already fixed by P1.10.
  **THE HONEST REMAINING WORK IS ~12 FUNCTIONS, NOT 89 SITES: `assignments.rs@436`, `kds_ops.rs@77`, `kds_lines.rs@315`, `image_refs.rs@222`, `downgrade.rs@64`, `loyalty.rs@696`, `loyalty.rs@892`, `profile.rs@988` (the `:995` UPDATE + `:999` INSERT pair), `staff.rs@608` (the `:632` UPDATE + `:678` upsert pair), `staff.rs@872`, `terminal_overrides.rs@63`, plus `soft_delete_user` (UPDATE then `persist_over_quota_markers`).** Three previously parked items are CLOSED BY THIS MEASUREMENT and should not be dispatched: `loyalty.rs:126` (single `INSERT OR IGNORE`, atomic by UNIQUE - its own comment says so), `loyalty.rs:651` (single UPDATE) and `promotions.rs:242` (single INSERT) are all **class (a), documentation at most** - the review's real loyalty complaint was never these but `loyalty_accounts.points` having three writers and `customers.loyalty_points` being a projection written from five sites, which IS class (b) and is in the list above. And `sales.rs:437` is **not a write at all** - it is a function parameter (`bill_type: &str`), so the 'check for dead code' note was warranted and the answer is that there is nothing there.
  **THE STYLE-RULE HALF NEEDS A DECISION, NOT A SWEEP:** if 'every public write fn owns or joins a transaction' is a house rule worth enforcing, enforce it with a MECHANICAL CHECK over the whole corpus rather than 89 hand-wrapped sites - and do NOT write tests for class (a), because they cannot fail. **Cheap discriminators, expected outputs verified this round:** (1) class (b) real work: `grep -rn --include='*.rs' 'conn\.execute(' crates/kasirmu-core/src/db | grep -v _tests.rs` grouped by enclosing `fn` -> 131 sites, 99 single-execute, 12 functions with 2+; (2) class (c): `grep -rn --include='*.rs' -E 'execute_batch\("BEGIN|BEGIN IMMEDIATE' crates/kasirmu-core/src/db` -> 1 live site (`stock_counts.rs:114`, now guarded by `is_autocommit()` at :112), every other hit a doc comment; (3) class (b) by shape: `grep -rn --include='*.rs' -B3 'conn\.execute(' crates/kasirmu-core/src/db | grep -E 'query_row|\.prepare\('` -> the guard-read-then-write pairs.
  **UNVERIFIED and stated:** the exact derivation of the '89' (that number's base could not be reproduced - the measured base is 131 sites / 138 per the review, the gap being the P1 fixes already landed), and whether the 12-function class-(b) list is exhaustive, since the grouping used an enclosing-`fn` heuristic rather than reading all 131 sites.
- [ ] **C19 slice A DONE 2026-09-23, commit 0e8ebcf2d** (`fix(core): make mark_offline_synced a transactional compare-and-set`; db/offline.rs +56, offline_tests.rs +94). Predicate `WHERE id = ?1 AND status = 'pending'`, and the DISAMBIGUATION after `affected == 0` is the load-bearing part: `NotFound` is preserved ONLY for an id that does not exist, while an existing non-pending row is an idempotent `Ok(())` no-op - and that split is **MANDATED, not chosen**, because an out-of-fence test (`platform/sync/src/pg_daemon_tests.rs::mark_offline_synced_is_idempotent`) asserts a second mark returns `is_ok()`. `Ok` is returned for the dead-lettered case too, so a stale caller cannot abort the batch loop in `apply_sync_outcomes`, which uses `?`. Transaction: join-or-own via `is_autocommit()`, the `audit.rs:411` `log_audit` idiom verbatim rather than a `_in_tx` twin. `cargo test -p kasirmu-core offline` -> **98 passed, 0 failed**. FALSIFICATION: reverting the predicate fails exactly the two new tests while the transaction test stays green - correct, because that test discriminates the transaction and not the predicate. Cross-crate: `-p platform-sync mark_offline` 3 passed, `-p platform-sync queue` 88 passed, `-p kasirmu-core --test offline_integration` 23 passed. **BRIEF CORRECTION, recorded:** this checklist cited `offline.rs:421-431`, which is STALE - the function is at 605-619 pre-fix and 646 post-fix. Line numbers in this checklist drift; re-read before fencing. **Its short-circuit answer produced C45 - a data-stranding defect on a THIRD outcome applier - and C19 slice B.** ORIGINAL: - [ ] **C19 Enforce conflicts, or stop pretending to** (5.4, 5.5) - consume Decision::LastWriterWins and Decision::AutoMerge or delete the computation; add sale to the money-entity test so a completed sale stops classifying as low-severity catalog metadata; write the 'delivering' state the CHECK constraint already allows; guard and transact mark_offline_synced (db/offline.rs:421-431).
- [x] **C20 Order the queue by priority on every push path** (5.3) - **DONE 2026-09-23, commit a8058a277** (`fix(sync): order the offline queue by priority on the daemon and bridge push paths`; platform/sync/src/daemon.rs +39, daemon_tests.rs +108, crates/kasirmu-bridge/src/sync.rs +55, sync_tests.rs +85). **ANSWER 1, and it corrects the item's own framing: priority is NOT a total order, so the fix is not a sort by priority.** `SyncPriority` has only three values and MOST items are `Critical` (every sale, void, refund and payment split passes `SyncPriority::Critical` at `db/offline.rs:391,463,489,538`), so ties are the COMMON case; and `created_at` is only millisecond precision, so a priority-only sort would leave same-millisecond items in SQLite's unspecified row order - a real run-to-run flip. The key chosen is therefore total, in three parts: **`(priority, created_at, id)` ascending**, where `id` is a UUID v7 primary key - unique AND time-ordered, which is what makes it a correct final tie-break rather than an arbitrary one. **ANSWER 2: no caller relies on a different order.** The batch is sorted ONCE before the push and every downstream consumer iterates that same vector (`transport.push_items` sends it; `apply_push_results` / `apply_sync_outcomes` `zip` it against outcomes), and the server documents that alignment as load-bearing (`sync_api.rs:283`: a reordering would mark the WRONG items as synced or failed). `queue.rs`'s effect-key/origin gate is per-item and order-independent, and was not touched. **Negative control run and reverted:** with the daemon's sort commented out, the acceptance test fails exactly as the bug describes - `left: ["bulk", "money", "catalog"]` vs `right: ["money", "catalog", "bulk"]`, i.e. the LOW item leading a CRITICAL one; the file was restored and verified by hash. Three tests per path (out-of-order critical-first, deterministic tie-break, arrival order within a tier). `cargo test -p platform-sync` -> **415 passed**; `cargo test -p kasirmu-bridge` -> **1385 passed** (run as a background job; it exceeds the 600s foreground ceiling). **TWO HONEST GAPS, both kept:** (1) the bridge tests pin the COMPARATOR, not the CALL SITE - deleting `order_pending_for_push` from `sync_run_scoped` leaves them green, so the bridge wiring is verified by inspection and compilation only, and closing it needs a `BridgeCtx` + mock HTTP harness the file does not have; (2) no real multi-item HTTP push was exercised end to end. **And it found that a shared expression ALREADY existed** (`lib.rs:150` `build_batches`, used by the third path, which was correct and left alone) - but it is private, priority-only, and unreachable from the bridge, which is why it could not be reused. That produced C49 and C51.
  ORIGINAL BRIEF:
- [ ] **C21 Stop storing gift-card numbers in plaintext** (6.3) - 20260813_init.sql:135-137, :1126; they ride every unfiltered .db and .backup.db snapshot.
- [ ] **C22 Read base_total_minor in reports, and fix the screen that adds currencies** (8.5) - kasirmu-reporting/menu_engineering.rs:90-95 is wired to a live screen (ui/src/api/reports.ts:529-534); the schema already carries sales.base_currency and base_total_minor (20260821_tender_currency.sql:1-8) and no report reads them.
- [ ] **C23 Make a panic visible and recoverable** (14.3) - catch_unwind (or the Tauri equivalent) so a command returns an error instead of hanging the promise; make spawn_watched restart or at least tear down and report (platform/startup/src/lib.rs:341-361); clear the sync daemon's running flag on task exit, not only on the normal path (platform/sync/src/daemon.rs:353, :461).
- [ ] **C24 Make accessibility able to fail a build** (9.5) - wire test:a11y into CI as blocking (check.sh:309-313 is advisory-WARN today and no workflow runs it), and fix inventory/TransactionLogScreen.tsx:238-241 (mouse-only row) plus the missing alt at ProductThumb.tsx:81.
- [ ] **C25 Make the gate registry tell the truth** (11.1) - add clippy and e2e to dev-ci.yml; re-label perf-smoke and data-testid-compliance as advisory; call check.sh from .githooks/pre-push or state plainly that a push is not the gate. Do not re-label clippy or e2e to make the registry self-consistent.
- [ ] **C26 Decide the architecture boundary before 2026-11-06** (10.2, 10.3) - re-tier the checker with a named rule for the seven type-only re-export edges and an ADR, or move the model types back down; either way add a checker test that a bumped expiry without a new reason fails. Owner decision D3: close the currency edge now, add a named rule for the seven type shims, and move the model types into foundation later - do not bump the dates.
- [x] **C27 Reconcile the container's proxy with the licence server's routes** - DONE 2026-09-23, commit 65f69112f (12.1) - apps/unified/Caddyfile:44-72 misses pairing/* and midtrans/*, so five registered routes 404 in the shipped image; the checker that would catch this already exists, is registered as required (scripts/check.sh:102, gates.json:613), and **is red on the current tree** - node scripts/check-unified-routes.mjs exits 1 naming /api/v1/pairing/*. Add the pairing and midtrans handles, then widen the checker to read the Go path constants rather than main.go string literals, because otherwise it can never see the Midtrans paths (see D8).

---

## Wave 3 - hygiene that compounds (P2)

- [ ] **C28 Size and structure** - 16 production files over the 1,000-line limit (kasirmu-api/src/pg.rs at 2,904 is worst); 568 test functions inline in 52 production files against AGENTS.md section 2; ui build debris at the frontend root; the 12,186-line single test file; the production Profiler in KdsScreen.tsx:390-394.
- [ ] **C29 Declared-but-inert surface** - decide per item (13): file/rotation log sinks, the rate-sync daemon, the scale driver that is never registered, the EDC stubs registered on a money path, the modules' test-only services, kasirmu-media (one-line workspace-exclude first, then delete) - and **kasirmu-lan**, which D6 recommends retiring the same way, since it has no client in either shell and the in-app KDS board does not need it. The empty plugin IPC surface is D7.
- [x] **C30 Make the documentation true** (11) - **DONE 2026-09-23, commit e6edf57c1** (`docs: correct stale counts and false claims across the documentation set (C30)`; **11 files**, +97/-69: README.md, ui/README.md, docs/decisions/README.md, docs/guides/product/ROADMAP.md, docs/operations/ci-pipeline.md, docs/architecture/ARCHITECTURE.md, docs/benchmarks/regression-tracking.md, docs/security/PCI-DSS_CHECKLIST.md, docs/security/license-audit-2026-07-20.md, CONTRIBUTING.md, CHANGELOG.md). **Committed by the manager**, not the writer - the writer role has no shell by design, so it reported the exact commit line instead of working around the capability, the same correct behaviour as the runbook slice. **SIXTEEN claims corrected, not eleven**, and the count is itself the point: the review measured eleven, the worker found five more by looking. Headline corrections with their commands: Rust `#[test]` fns **8,355 -> 8,695**; front-end test files **593 -> 607** with the base named, plus **616** identified as a DIFFERENT base (`ui/src/__tests__/**/*`, one `.json` fixture) so the two cannot be quoted interchangeably, and `.spec.*` measured at **0** (the old two-glob form double-counted); migration files **59 -> 67** (66 SQLite + 1 generated PG); live workflows **TWO -> THREE** (`android.yml` had appeared); the ADR index **#1-#52 -> #1-#60** with a missing row **#53** added; the `.ftl` count **50 -> 54** with the old '25 x 2' arithmetic noted as no longer closing; the `ui/` layout block corrected (`frontend/themes/` -> `theme/`, locales moved to `shared-ui/locales/`, 400 -> **607** files, 40 -> **53** api `.ts`, and `test-utils` is a FILE not a directory); and the crates.io-style count resolved arithmetically to **39 = 17 crates + 14 modules + 4 platform + foundation + 3 apps**. **THE BEST ILLUSTRATION OF THE REVIEW'S OWN THESIS: FOUR FILES INDEPENDENTLY SAID 'two live workflows' (CHANGELOG.md, CONTRIBUTING.md, docs/benchmarks/regression-tracking.md, and the CI pipeline doc), and the CHANGELOG additionally claimed `dev-ci.yml` has 'no push trigger' while the file has had `push: branches: [main]`.** Four copies of one wrong number, agreeing with each other. Two retired gates were also misdescribed as live: the Lighthouse a11y check and the coverage job are both `"status": "retired"` in `gates.json` and `.lighthouserc.json` does not exist. **FOUR claims were concluded ALREADY TRUE and left alone with evidence** (rate-sync daemon, Clippy in no live workflow - now WIDENED from a two-file grep to `.github/workflows/*.yml` so a third workflow could not hide, the seven pre-commit steps re-derived from the hook's own section headers, and 14 modules). **AND IT DELETED CLAIMS IT COULD NOT VERIFY RATHER THAN RESTATING THEM**: the `208 tracked files` and `165 tracked files` counts need `git ls-files`, and the '25 per-feature bundles' figure has no command in this checkout that groups en/id twins - so they were removed with a comment saying why, because *a number nobody can re-derive is the defect this item exists to remove*. Stated limits kept: no cargo was started (build-dependent claims marked unverified), and two numbers disagree with their shell equivalents (row 2b's base, and the docs-subdir count derived from a directory union). `manager-codebase-review*.md` was not touched.
  ORIGINAL BRIEF:

---

## Owner decisions - code is blocked on these

Full analysis - deciding facts, options with pros and cons, what would change the answer, and a recommendation - is in manager-codebase-review-decisions.md (D1-D8). The lines below are the index.

- [ ] **D1 Is OZ_MASTER_KEY set on any deployment?** Decides whether 6.2 is a live confidentiality break or a latent one.
- [ ] **D2 Has any PostgreSQL deployment run scripts/rls-cutover.sql?** Decides whether tenant isolation exists there at all.
- [ ] **D3 Architecture rule or tier order?** (10.2) Re-tier the checker with an ADR, or move ~5k lines of models back down. Forced by 2026-11-06.
- [ ] **D4 Is qris-core meant to be publishable?** (14.4) If not, it is a one-line manifest fix away from harmless.
- [ ] **D5 Is in-app restore a product feature or an ops procedure?** (14.1) Today the only restore is a CLI command.
- [ ] **D6 Is LAN KDS a product feature?** (6.7) It works and is safely defaulted, but nothing in the product can configure it.
- [ ] **D7 How much plugin trust is acceptable?** (6.4) Signed-and-verified and unsigned-hot-reload are the two ends; there is no documented middle.
- [ ] **D8 Does the unified container ship?** (12.1) If production runs the two-service compose, the five 404s are dev-only - and that should be written down.

---

## Do not do these

- [ ] Flip RLS from ENABLE to FORCE while the app connects as the table owner.
- [ ] Hand-edit the generated PG init instead of running scripts/generate-pg-migration.py.
- [ ] Backfill stock by setting qty to the sum of movement deltas.
- [ ] Raise the registration-gate debt ceilings - the ceiling is the finding.
- [ ] Delete the conflict Decision computation instead of consuming it.
- [ ] Weaken the concurrency test to success_count <= 1.
- [ ] Mark clippy or e2e advisory to make the gate registry self-consistent.
- [ ] Add busy_timeout to that test while leaving fresh_db() pragma-free.
- [ ] Add CHECK (qty >= 0) before the backfill migration has run.
- [ ] Retry the same fix a fourth time after three red gates - diagnose the root cause instead.

---

## Wave 4 - added during implementation

- [x] **C18 P1.3b [P1] Give the shift invariant a database-level guard.** - **DONE 2026-09-23, commit 152755e83** (`feat(core): guard a single open shift per user at the schema level`; 4 files, +358/-1): the partial unique index lands in `crates/kasirmu-core/migrations/20261011_open_shift_uniqueness.sql` (new, 94 lines) with the registry entry, a census pin bump 188 -> 189 plus three tests (+249), and the regenerated PG file. **The raw-SQL acceptance is the strong part:** `open_shift_uniqueness_index_bites_on_raw_sql` runs the registry MINUS this migration first and the raw `INSERT INTO shifts (id, user_id)` SUCCEEDS TWICE (asserts `open = 2`), then runs the full registry and the second insert is refused as `ConstraintViolation` naming `shifts.user_id` - so the refusal provably comes from the index and not from `Store::open_shift`. It also omits `status` on the insert deliberately, proving the column DEFAULT and the index's `WHERE status = 'open'` agree with what writers actually produce, and it checks BOTH exemptions (a different user is accepted; the same user is accepted again once the first shift is closed). **And it cannot brick a store:** the migration RECONCILES before it constrains - a deterministic `UPDATE` closes every open shift except the most recently opened (`opened_at DESC, id DESC`), data-preservingly (no row deleted, no money invented, counted-cash columns left NULL, reason in `notes`) - so the `CREATE UNIQUE INDEX` cannot fail on any database that reaches it, pinned by `open_shift_uniqueness_migration_reconciles_pre_existing_duplicates`. `cargo test -p kasirmu-core migration` -> **44 passed, 0 failed**, run both before and after the PG regeneration. open_shift is now atomic, but there is still no partial unique index on shifts(user_id) WHERE status = 'open', so any SQL writer outside that function can still create a second open shift. A migration adding the partial index closes the same class of hole the app-level fix closed - and it must land alongside its own index-surface pin bump in migrations_tests.rs, which is why it is a separate slice from any migration already in flight.

- [x] **C5b [P0] Mirror the EOD fix into the tablet's duplicate builder** - **DONE 2026-09-23, commit 1eee8e0f**. Both doors now call the shared core queries; both hazard pins INVERTED (names kept) and the boundary-row pin gained an anti-vacuity guard. The tablet builder also lost a latent bug: its payment query had no COALESCE and grouped by method without currency while the header is per-row.
- [x] **C6b [P1] Validate the timezone on the write path** - **DONE 2026-09-23, commit 1fea7d78**: validate_provision_args now rejects anything outside the three Indonesian presets plus UTC, reusing the same predicate the bridge's update path and the regional axis validator use - so the accepted set has one owner. Rejection only, storage untouched, no migration. Six unsupported values rejected with a field-named message; Asia/Pontianak is deliberately in the rejection list because the reporting path can resolve it while the write boundary's closed set does not (proof the set was reused, not widened) - crates/kasirmu-core/src/db/provisioning.rs:467-476 and :514-564 insert locations.timezone verbatim with no validation while crates/kasirmu-bridge/src/locations.rs:314-321 accepts only Asia/Jakarta|Asia/Makassar|Asia/Jayapura|UTC. Split out of C6, whose reporting half is done.
- [x] **C11b [P2] Document the pre-migration snapshot and the pre-flight** - **DONE 2026-09-23, commit 527e5e11f** (`docs(operations): add pre-upgrade snapshot and restore safe-mode runbook sections`; `docs/operations/runbook.md` +350, appended as sections 10 and 11 after section 9.5 and before the closing audit stamp, matching the existing table + callout + exact-command style - extended, not replaced). Every claim is traceable to source read beside it, and the unverifiable ones are marked rather than invented. **Two corrections the writer found while writing, both documented instead of papered over:** (1) the design note's `<db>.restore.lock` is NOT the shipped constant - it is **`<db>.restore-boot.lock`** in both shells; (2) the boot outcome lines go to **stdout only**, because `try_init_with_file` has no production caller, so there is no log file to read them in. It also recorded plainly that **no IPC command is registered** for `list_restore_candidates` / `restore_prepare` / `restore_status` (measured across both `invoke_handler!` blocks - consistent with the outstanding S5 slice), so the in-app trigger is documented as bridge functions with the operator path marked unverified, and the CLI restore is given as the currently reachable route with `scripts/restore-db.sh` warned as weaker (no `validate_candidate`, no schema gate, no rollback). Its own stated limits: the literal `%APPDATA%\mu.kasir.app\kasir.db` is DERIVED from the verified bundle identifier plus Tauri's `app_data_dir()` join, not observed, and the tablet's on-device absolute path is not resolvable from this session. **Committed by the manager**, not the writer: the writer role has no shell by design, so it reported the exact commit line instead of working around it (ORIGINAL BRIEF:) - docs/operations/ carries no pre-upgrade procedure for the desktop SQLite file, and the four pinned un-re-runnable migrations (migrations_tests.rs:361-366) are not named anywhere an operator reads. Split out of C11, whose code half is done.
- [x] **C12b [P2] Wire the variance report to an operator surface** - **DONE 2026-09-23, commit ec0ab7aef** (`feat(cli): surface the stock variance report as oz stock-variance`; 5 files, +605/-5, two of them new). **Surface decided and justified:** the CLI already owns the read-only maintenance surface - `oz export <kind>` renders a read-only report, `oz sale get --format text|json` renders a text table, `oz credential-deltas` is a report-only bounded operator command - so the new subcommand REUSES `open_db`, the path-guard shape from `open_store_for_credential_deltas`, and `run_sale_get`'s text-table rendering rather than inventing a fourth pattern. Text rather than CSV was chosen deliberately: `oz export` uses CSV because it dumps bulk data, while this is a <=1000-row triage view, and the core row type carries no `Serialize` (core was out of fence, so hand-rolling one would have created a second definition of the row - the divergent-twins failure this workstream keeps finding). **Operator command: `oz stock-variance --db <path> [--min-difference N] [--limit N]`**, defaults `--min-difference 1` (hide consistent pairs) and `--limit 100`. **All four acceptance points asserted, not assumed:** reaches the report and prints rows (the C12 scenario end to end - stored 8 / ledger 4 / diff 4 - plus the clean-ledger empty case as a real answer); **read-only ASSERTED** by reusing core's own `report_performs_no_writes` pattern (a fingerprint of `stock_movements` rows, `stock_summary` rows and `SUM(qty)` before/after across three calls including the capped and empty paths must be identical); a missing path fails clearly and does NOT create the file or either sidecar; and the bound plus validation errors surface rather than being swallowed. `cargo test -p kasirmu-cli` -> **132 passed, 0 failed**, plus a LIVE end-to-end run against a real database (migrate then stock-variance exit 0; mistyped `--db` exit 1 with the file not created; `--limit 5000` exit 1 with a field-named error; `product list` exit 0, so no regression to sibling arms). **Two judgement calls worth keeping:** the report is capped by construction, so a truncated list would read as 'this is everything' - the one failure a reconciliation report must not have - so an explicit `ROW BOUND REACHED` note appears whenever the returned count equals the limit; and the path guard is scoped to this subcommand ONLY, because `migrate`/`init-db`/`restore` legitimately provision a database, so pushing the check into shared `open_db` would have turned a one-command footgun into a first-run outage. It deliberately did NOT wire the report into any startup hook or retention path - it stays operator-invoked.

- [x] **C31 [P2] Validate the Caddyfile itself, not just its routing semantics.** - **DONE 2026-09-23, commit e48cea5e8** (`scripts/check-unified-routes.mjs` only; the Caddyfile needed no fix and no CI-only variant was required). **Availability verdict first: no `caddy` binary is reachable from the gate** - `which caddy` finds nothing on PATH, and `ops/docker/Dockerfile.unified` obtains the binary only in its RUNTIME stage (`COPY --from=caddy /usr/bin/caddy`, line 227), i.e. inside the deployed image where no gate runs. The gate is invoked as a plain node step by `scripts/check.sh:102`, and `grep -c check-unified-routes .github/workflows/dev-ci.yml` = **0**, so it is not a CI step either. A `caddy:2-alpine` image IS cached locally and `docker run ... caddy validate` works here - **but the worker refused to make a static gate depend on a Docker daemon and a cached image, which is the invented download step the brief forbade.** So it built the strongest offline check available: a real lexer plus structural parser, taking an optional path argument so the failure branch is exercisable without editing a tracked file. **NEGATIVE CONTROLS, each cross-checked against the real binary through Docker:** missing final `}` -> gate exit 1 with `line 41: ':80' opens a block that is never closed` (caddy: `unexpected token 'gzip', expecting '}'`); stray token in a block -> exit 1 with the directive named (caddy agrees); extra `}` -> exit 1 `unmatched '}'`; `import not_defined_anywhere` -> exit 1 naming neither snippet nor file (caddy: `File to import not found`). **Every fixture's gate verdict matches caddy's.** **Non-vacuity preserved:** deleting the `/api/v1/midtrans/*` handle block still fails on the ROUTING rule, not the new syntax rule, so the pre-existing behaviour is intact. **And the honest part: it found and removed THREE FALSE POSITIVES before committing** - a depth-agnostic directive whitelist rejected `output` (inside `log`), `user` (inside `basic_auth`) and `@api` (a named matcher), all of which real caddy accepts, and an arity rule rejected `reverse_proxy` with no upstream, which also validates clean. Each was confirmed against the binary, and the check was narrowed to depth 1 where the directive set is finite and documented, accepting matchers by shape. **A validator that fails a legal deploy is worse than the bug it prevents.** Stated limits, kept: it is a STRUCTURAL parse, not caddy's grammar (no argument grammar, no matcher semantics, no block-context legality); it deliberately does NOT flag an unterminated quoted string because real caddy accepts it (`encode "gzip` -> `Valid configuration`); and the real validator remains the deployed image's own `caddy validate` at start-up - this gate is a cheap pre-deploy proxy, not a replacement. Residual, out of the read-only fence: `caddy fmt` reports the file is not canonically formatted (line 13) and the `/_/*` routing-comment indent at line 13 looks misaligned; neither affects validity. scripts/check-unified-routes.mjs parses apps/unified/Caddyfile as text, so a syntax error that the parser tolerates still ships and the container fails to start. Add caddy validate (or an equivalent parse) to the same gate, or state why it cannot run in CI. Found while closing C27.

## Wave 5 - the RLS table audit (added 2026-09-23)

Source: an audit of every table in `RLS_TABLES` (scripts/generate-pg-migration.py) against every production write path in the repository. **10 of the 34 protected tables never have `tenant_id` written by any code path**: locations, product_activity, product_bundles, bundle_items, product_taxes, product_variants, edc_terminals, user_location_access, media_assets, media_thumbnails, payment_gateways, payment_settlements (the last four have no writer at all). The tenant_id column exists; nothing fills it, so every row lands on the default tenant.

**Severity, stated precisely.** Every one of these is LATENT today: no deployment has run scripts/rls-cutover.sql (that is open question D2), so no policy is forcing anything yet. But once the cutover runs, a row written without a tenant is invisible to the tenant that owns it - and in two cases that is not merely isolation, it is a wrong answer to a paying customer:

- **locations is the expensive one.** apps/cloud-server/src/quota_detector.rs:336 runs `SELECT COUNT(*) FROM locations WHERE tenant_id = $1` to enforce the per-plan location quota. A tenant whose locations all sit on 'default' scores **zero**, so the check either lets unlimited locations through or refuses a legitimate one - the direction depends on which way the detector compares. Either way the number is fiction.
- **product_activity is the quiet one.** apps/cloud-server/src/email_pg/popularity.rs:230-236 builds the cloud popularity roll-up with the same tenant predicate, so it reads zero rows for every real tenant - a feature that reports "no data" forever and looks like a product gap rather than a missing column write.

The generator states the rule these tables break, in its own comment at scripts/generate-pg-migration.py:475-476: add a table to this list only after its write path demonstrably sets `tenant_id`. A table whose writer never touches the column is not protected by RLS; it is only advertised as protected, which is worse than either state.

- [x] **C32 SITE 1 DONE 2026-09-23, commit 8ce101607** (`fix(core): stamp the caller tenant on the provisioned location row`; 2 files, +104/-10) - the item as a whole stays OPEN because D9 re-scoped it; see the re-scope note below. `provision_device_inner` now matches on `args.tenant_id.as_deref()`: the **linked** arm binds the caller's claim verbatim as `?6` onto `locations.tenant_id`, and the **local** arm OMITS the column so the schema's own `NOT NULL DEFAULT 'default'` supplies it - no code fallback, no bound NULL. **Negative control PROVED**: reverting the bind to the pre-fix unconditional INSERT fails the linked test with `a linked location must not sit on the single-tenant column DEFAULT` (`left: "default"`, `right: "default"`), and the file was restored to a verified byte-identical hash. Evidence: `cargo test -p kasirmu-core provisioning` -> **27 passed, 0 failed, 3236 filtered out**, both new tests green; the linked test asserts the stored value is not `'default'` AND equals `tenant-abc` AND matches the peer `provisioning` row, so one source cannot yield two tenants. Manager re-verified the artifact: `git show --stat` = exactly those 2 files and the diff carries the `INSERT INTO locations (..., tenant_id) VALUES (..., ?6)` arm. The column exists and no migration is needed; the work is one correct tenant source per insert site.
  Fence: crates/kasirmu-core/src/db/locations.rs, edc_terminals.rs, provisioning.rs, products.rs, product_bundles.rs, tax/assignments.rs, popularity.rs, products_crud.rs and their sibling `*_tests.rs` files; apps/desktop-tauri/src/state.rs:410. No migration, no schema change. Scripts/generate-pg-migration.py and the generated 20260813_init.pg.sql belong to C33 only.
  **Slices** (each independently shippable, all four dispatched 2026-09-23): **S1** locations + user_location_access + edc_terminals (the quota-detector path first, because it is the one with a wrong answer and not just a missing row). **S2** the catalog children: product_variants, product_bundles, bundle_items, product_taxes. **S3** product_activity (both the sale-time recorder and the product-create seed - establish which are live). **S4** the list correction, C33.
  **The trap, and it is the same one in all three code slices:** do NOT read the tenant from a parent row inside the same transaction. On SQLite `products.tenant_id` is stamped by a LATER separate UPDATE (apps/desktop-tauri/src/routes/products.rs:307), so a parent read in the same transaction still returns 'default' - a fix that does that moves the bug rather than closing it, and it passes any test that only checks "the value is set". The tenant must come from the same source the enclosing function already uses for its other rows (the caller's tenant claim). Each slice's test must assert the stamped value EQUALS the caller's tenant, not merely that it is non-empty.
  **TWO CORRECTIONS TO THIS ITEM, from the fence requests of 2026-09-23 - read them before trusting the pointers above:** (1) `apps/desktop-tauri/src/routes/products.rs` **does not exist**. The `:307` stamp this item cites is `crates/kasirmu-api/src/routes/products.rs:307` - the **cloud REST** surface, where the tenant is JWT-derived. So the "parent written at 'default' then stamped by a later UPDATE" pattern may not exist on the **desktop** path at all, in which case desktop store tables are simply all `'default'` and the composite-FK ordering hazard is a cloud-path hazard. Establish which before slicing. (2) `product_activity` is **excluded from cloud sync** - ADR #37 D4, `docs/decisions/2026-08-11-adr37-product-popularity-index.md:134-143` - and `apps/cloud-server` references the table only in the popularity query and a test-name list. So stamping the tenant is necessary but possibly **not sufficient** for the roll-up to read rows; the transport into PG is a separate open question.
  Also verified this round: `refunds` is **fixed** (commit 6f8cd1e34 - the originator reads the sale's tenant at db/refunds.rs:361 and the receiving terminal reads its own local sales row at platform/sync/src/queue.rs:500, both on the caller's transaction, with a test asserting the refund row and its outbox row share one tenant). The comment at db/offline.rs:407-411 asserting that refunds.tenant_id is never written is now stale prose and should be corrected by whoever next touches that file.
  **RE-SCOPED 2026-09-23 by three fence requests and the evidence they produced - read this before slicing any of it. The item as originally written was wrong on the desktop path.**
  The original claim was "eight tables never write tenant_id, so stamp a tenant at each insert". Three workers independently refused to do that and each came back with evidence instead. What actually holds:
  - **Only one site has a real source available.** `provisioning.rs:469` is fixable today because the same function, in the same transaction, already stamps `args.tenant_id` into the peer `provisioning` row at `:492-501` - a caller-supplied value the code already trusts for this fact. That one is being fixed, with the `local`/`None` case ruled on: omit the column and let the schema's own `NOT NULL DEFAULT 'default'` supply it, because `migrations/20260907_add_location_tenant_id.sql:10-11` declares that default to BE the single-tenant semantics.
  - **The others have no honest value at all.** `create_location_profile` (`db/locations.rs:184`) - no tenant field on `LocationProfile`, none on `Store`, none on `SessionContext`; `create_edc_terminal` (`db/edc_terminals.rs:193`) - **zero production callers**, so a parameter there is an unexercised shape and not a fix; `seed_primary_store` (`apps/desktop-tauri/src/state.rs:410`) - runs during startup before any session, licence or claim is in scope, and seeds the fixed `id = 'default'` row.
  - **And stamping them would CONTRADICT the design, not complete it.** `db/mod.rs:508-516` documents the desktop store database as single-tenant by construction and `check_tenant_integrity` **refuses to boot** on a foreign-tenant row. A desktop install that stamped a real tenant on some rows and not others would be a boot failure.
  - **A comment that asserts a mechanism which does not exist:** `crates/kasirmu-bridge/src/locations.rs:277` is documented as resolving "for the session's tenant (ADR #7)" while its actual tenant read at `:230` is the literal "default". That is the review's own thesis in one line, and it is worth keeping visible.
  - **The real defect is on the cloud side, and it is worse than a missing stamp.** `apps/cloud-server/src/quota_detector.rs:336` counts `locations WHERE tenant_id = $1` and the popularity roll-up (`email_pg/popularity.rs:230-236`) does the same, against rows that carry 'default' - so a paying tenant scores 0 locations and the roll-up reports no data forever. **CORRECTED by C36 (commit-less investigation, see D10): the impact is an alert that can never fire, NOT a denied quota** - `QuotaDimensionKind::Locations` / `CONDITION_LOCATIONS_OVER_QUOTA` is an alerting path, and the earlier wording in this file inferred a blocking one. The number is structurally zero for every tenant because `locations` has no PostgreSQL writer at all.
  - **AND those tables never reach PostgreSQL at all.** `locations`, `edc_terminals`, `product_activity`, `product_variants`, `bundle_items`, `product_taxes` and `product_bundles` appear in **neither** the sync action vocabulary (`platform/sync/src/queue.rs:450-457`) **nor** the PG copy surface (`apps/cloud-server/src/bin/migrate_sqlite_to_pg/main.rs:72-89`). So the detector is most likely counting an empty table, which makes its number structurally zero rather than merely wrong, and the tenant stamp is not even the first problem. The tenant IS already applied at ingest for the tables that do sync (`apps/cloud-server/src/sync_store/pg.rs:39-45`, `:76-87`, from the JWT claim at `sync_api.rs:269`) - so the pattern exists and seven tables were simply never added to it. C36 establishes which of the two truths is the real one before anything is changed.
  **Gate: owner decision D9** (manager-codebase-review-decisions.md) - is the desktop store DB single-tenant by construction, so the tenant belongs at the cloud ingest boundary? The recommendation is A: yes, and apply the tenant where it is actually known (`sync_terminals.tenant_id`), rather than inventing one on the terminal.
  **The FK-ordering half is the ONLY part that remains live on the desktop path, and it is confined to `product_variants`, `bundle_items`, `product_taxes`, `product_bundles`** - the four tables carrying a composite FK to `products(tenant_id, sku)`. Slices C32-S2's fence must therefore include `db/products_crud.rs` so the parent INSERT carries the tenant in the same transaction, or the child insert fails outright under `foreign_keys = ON`.
  Done when (re-scoped): `provisioning.rs` stamps the supplied tenant on the linked path and the schema default on the local path, each with a test; D9 is answered and the cloud-side read is fixed where the tenant is actually known; and every table left in `RLS_TABLES` either has a write path that demonstrably sets `tenant_id` or is moved to `RLS_EXEMPT` with a reason (the C33/C33b route).

- [x] **C33b [P1] user_location_access moved out of RLS_TABLES** - **DONE 2026-09-23, commit 31211f171** (`refactor(core): move user_location_access out of RLS_TABLES`; 2 files, +12/-2). It also has no production writer: 5 `INSERT` hits all in test files, 0 `UPDATE`, 1 `DELETE` (test-only), no dynamic builder names it, and every production mention is a read (`workspaces_instances.rs` counts it, `pg_tests.rs:1641` is a GRANT, `migrations_tests.rs:1114` is a `PRAGMA table_info` pin). Worker-reported counts: RLS_TABLES 30 -> 29, RLS_EXEMPT 19 -> 20, union 49 unchanged, overlap 0. **Manager-verified:** `git show --stat` is exactly the two files, and `python3 scripts/generate-pg-migration.py --check` exits 0 - the tree is self-consistent again, so the migration surface is free for the next slice. (Note on my own verification: I could not independently reproduce the two list lengths with a shell `awk` range and did NOT claim them; what IS verified is the generator's own fail-closed coverage gate, which validates both lists against the schema and rejects an entry in neither or both - `--check` exit 0 is that gate passing, and it is stronger evidence than my line count would have been.)

- [x] **C33 [P1] Stop advertising four stubs as RLS-protected.** - **DONE 2026-09-23, commit 103d5cd5a** (`refactor(core): move four writerless tables out of RLS_TABLES`; 2 files, one pathspec line). `RLS_TABLES` 34 -> **30**, `RLS_EXEMPT` 15 -> **19**, union 49 with overlap 0 (the fail-closed gate's both-directions invariant holds). The premise was WITNESSED per table, not assumed: zero `INSERT`/`UPDATE` into any of the four across the whole repo, and the three dynamic-statement holes the brief warned about were each closed by inspection (`db_tests.rs:984,1008` is a synthetic `rls_force_probe_<pid>` probe; `seed_demo.rs:219`'s table list excludes them; `migrate_sqlite_to_pg/main.rs:72 DEFAULT_TABLES` contains none). The three stub writers (`create_media_asset`, `upsert_gateway`, `record_settlement`) all fail fast with `PLANNED`. Evidence run by the worker: generator exit 0 (127 tables, 158 indexes, 7 seed inserts), second run byte-identical sha256 `0cc939ff…`, `--check` exit 0, `--self-test` ok, generated file carries exactly 30 `FOREACH` entries and 0 policies for the four, predicate unchanged, `FORCE` count 0; `cargo test -p kasirmu-core migration` **41 passed**. Re-verified by the manager: `git show --stat` = exactly those 2 files, and `python3 scripts/generate-pg-migration.py --check` exits 0 against the committed state. Honest gaps the worker stated itself: `--check` proves the file matches the generator, **not** that the DDL applies to a live server, and the isolation consequence is an argument from greps rather than an execution.
- [x] **C36 [P0] The locations quota axis is structurally inert** - **INVESTIGATED AND PINNED 2026-09-23, commit 5bd4a15b4** (`test(cloud): pin the inert locations quota axis`; 2 files, +220) - **decision D10 gates the fix; this item lands only the measurement.** Three corrections to the item as first written: (1) the detector counts **three** axes, not seven - only `locations`, `products`, `users`; the other six tables appear zero times in it; (2) in cloud the **SQLite** detector is dead code (the `_pg` loop runs, `count_tenant_locations_pg` is the live query); (3) the impact is an **alert that can never fire**, NOT a denied quota - the earlier wording in this file inferred a blocking path and was wrong. Nothing writes `locations` into PostgreSQL by any route: no production INSERT/UPDATE exists (every literal hit is a test fixture), it is absent from `DEFAULT_TABLES` (which carries products and users), the sync vocabulary has no location action, `sync_store` writes only `offline_queue`, and `PG_INIT` seeds none. With `max_locations()` at `Some(1)`/`Some(2)`/`Some(5)`, `0 >= 1` is false, so `CONDITION_LOCATIONS_OVER_QUOTA` can never fire - even for an operator running forty stores. The contrast is what makes it a finding: `products` and `users` DO reach PG (`pg.rs:868,1430`), so those two axes are live; `locations` is the only counted axis with no PG writer and no copy entry. **The pin is two tests, and the second was strengthened mid-flight for a good reason:** `pg_integration_locations_axis_counts_zero_on_the_cloud` first asserted `count == 0` for a fresh tenant, which the worker recognised as VACUOUS (a fresh tenant returns 0 even if a writer existed); it now seeds a PG location row and asserts the query returns **1** (proving the query is not broken), then asserts a tenant with no row returns **0** - separating "the query is broken" from "nothing writes the row", which is the actual finding. `cargo test -p kasirmu-cloud quota_detector` -> **15 passed, 0 failed**.
  **TWO CORRECTIONS THIS SLICE FORCED ON THE MANAGER, both recorded:**
  **(a) PostgreSQL IS reachable in this checkout** - port 15432 is open (5432 is not), `DATABASE_URL` is unset so a test must supply the URL explicitly. The C36 worker proved the PG test actually RAN rather than skipping: a control run against a dead port (`127.0.0.1:15999`) printed `PG integration test skipped: ...` and took 40s, while the real run printed no skip and took 2.1s, with `grep -ciE 'skipped|panicked'` over the committed run = 0. **This means the C7 circuit-breaker's stated root cause - "this environment has no PostgreSQL" - was FALSE**, which is recorded as manager error #7 and re-opens the C7 live-verification question below.
  **Honest caveats the worker stated itself, kept verbatim:** the structural pin scans **two named files**, so a writer added elsewhere (a new module, a migration) would not trip it - it catches the realistic regression (someone extends `DEFAULT_TABLES` or the sync vocabulary) but is not a whole-repo proof; and the PG test is **not hermetic** - it inserts a pid-unique `locations` row into the shared base database and deletes it in the same test, so a crash mid-test could leave one stray row (worth converting to the throwaway-DB pattern the crate's other tests use if it ever flakes).

- [x] **C34 [P0] NEW, found while deciding D9: the embedded local API can brick the next desktop boot.** - **DONE 2026-09-23, commit 669d3d109** (`fix(local-api): refuse a write whose tenant claim would brick the next boot`; `crates/kasirmu-local-api/src/lib.rs` +125, `lib_tests.rs` +244). **VERDICT: REACHABLE, but operator-credential-gated** - the honest severity, and the chain matters: the embedded `AppState` sets `allow_terminal_credentials = false`, so the client-credentials path is closed (400 `terminal_credentials_disabled`), and the UI's own mint is safe (`mint_token` passes `tenant_id = None`, resolving to `"default"`); **but the same router serves `POST /api/v1/tokens` publicly** (nested in the `public` group, no JWT), gated only by the per-install secret, and its admin-gated label mint passes `body.tenant_id` straight from the request body into `create_token_full`. That token validates against the same secret, so `POST /api/v1/products` then stamps a foreign tenant onto a row, and `check_tenant_integrity` refuses the next launch. Not unauthenticated and not internet-facing (loopback bind, off by default), but the per-install secret is one the operator is expected to hand to local scripts AND it doubles as the admin key - so a curl carrying a body field can brick the next boot. **Fix: ONE guard at the boundary, not three at the stamps** - a `reject_foreign_tenant_writes` layer applied outermost in `start_with_audit`, refusing WRITES only (GET/HEAD/OPTIONS keep exact behaviour) when a valid token's `tenant_id` is present and not `"default"`, returning 403 `foreign_tenant_write`. Empty string is refused too, because `unwrap_or("default")` maps only `None`, so `Some("")` would stamp `tenant_id = ''` - which the boot check rejects exactly like a named foreign tenant. It deliberately did NOT patch `products.rs`, because a stamp-level fix would have left `users.rs:163` - also boot-checked - open: the 'three guards vs one' trap the brief named. And because the guard is outermost and would otherwise short-circuit past the router's security headers, it re-applied the same public `security_headers_middleware` rather than copying a header list. Cloud behaviour untouched. **Evidence:** `cargo test -p kasirmu-local-api` -> 17 passed, 0 failed (4 new); `cargo test -p kasirmu-api --lib products` -> 13 passed; `cargo test -p kasirmu-core tenant_integrity` -> 6 passed (`check_tenant_integrity` untouched and still refusing); fmt clean. **Falsification proved:** short-circuiting the guard makes the two refusal tests FAIL while the read-tolerance test and the integrity test stay green, and `lib.rs` was restored byte-identically (blob `ca2e9909a49de4f5d162b83f9bb328c5263053db` before and after). **Residual, filed as C39:** the deeper fix is to stop the embedded `POST /api/v1/tokens` accepting a caller-supplied `tenant_id` at all; the boundary guard makes it harmless on this surface, but the field is still accepted there. Also noted: `tax_rates.rs:296-298` shares the stamp pattern and is NOT boot-checked.
  ORIGINAL BRIEF: `crates/kasirmu-local-api/src/lib.rs:153-169` hands the **store** database to `kasirmu_api::AppState`, and `crates/kasirmu-api/src/routes/products.rs:306-309` stamps `claims.tenant_id` (from the JWT, `unwrap_or("default")`) straight onto a `products` row. But `Store::check_tenant_integrity` (`crates/kasirmu-core/src/db/mod.rs:526-553`) **refuses to boot** when any `products`/`users` row carries a non-default `tenant_id`, and it runs on every launch (`apps/desktop-tauri/src/state.rs:247`, `apps/mobile-tauri/src/state.rs:188`). So if any caller of the embedded local API ever presents a token with a non-default tenant, the next launch of that store fails to start - a **remote way to brick a desktop install**, on the path that Pattern A was assumed to be safe for.
  Fence: crates/kasirmu-local-api/src/lib.rs, crates/kasirmu-api/src/routes/products.rs (the stamp region :295-316 only), plus their sibling test files. Do not change `check_tenant_integrity` and do not change the cloud behaviour of the route - the question is what the EMBEDDED surface should do.
  **First prove reachability, then fix, and report both.** Establish whether the embedded local API can actually mint or accept a non-default tenant claim at all (trace where its tokens come from and who signs them). If it cannot, say so with evidence and this becomes a one-line defensive ticket rather than an incident. If it can, the fix is at the boundary where the two contracts meet.
  Note the same stamp at `crates/kasirmu-api/src/routes/tax_rates.rs:296-298` and `routes/users.rs:162-164` - check whether they share the exposure before choosing where to put the guard, and prefer ONE guard over three.
  Done when: a test demonstrates the outcome for the embedded surface with a non-default claim (refused at the stamp, or the row is left alone), and `check_tenant_integrity` still refuses a genuinely foreign-tenant store DB.

  
- [x] **C37 [P1] NEW, found while landing C8 S4b: the mobile registration gate is red on a defect the desktop gate was already taught to tolerate.** - **DONE 2026-09-23, commit 7792fa26e** (`fix(mobile): port the registration gate's test-scaffold tolerance from desktop`; one file, +82/-1). The tolerance predicate is now ported, not allow-listed, and the port is non-divergent by construction (character-identical logic, origin cited in its own doc comment, pinned in both directions). `cargo test -p kasirmu-mobile --lib` -> **exit 0, 669 passed, 0 failed** (the 667 baseline + the previously-red drift pin + the new both-directions test); `cargo test -p kasirmu-app --lib drift_pin` -> **8 passed**, desktop byte-identical to HEAD (port, not edit). **The catching direction was proved non-vacuous with a negative control:** mutating the predicate to always return `false` fails the test with `a computed name in production UI must still land in the offender list: ui/src/utils/logged-invoke.ts`, then the exact original line was restored and BOTH acceptances re-run green. Two design points worth keeping: (a) the ported predicate tests for a whole path COMPONENT (`part == "__tests__"`), never a substring, so `ui/src/foo__tests__bar/Screen.tsx` remains production source - and a substring-guard assertion stops the tolerance being widened by accident; (b) the worker MEASURED the breadth rather than assuming it - 128 computed `invoke` sites under `ui/src/__tests__` (tolerated) against 19 in production `ui/src` (still adjudicated) - and grounded the rule in `scripts/verify-ipc-parity.py`, whose walks already drop `__tests__` paths (`:149`, `:274`) so the parity surface is production source only. It also chose to PORT rather than file a Fence_Request, with a reason worth accepting: the predicate is a two-line function over a `&str` inside a sibling crate's `*_tests.rs`, so the only non-duplicating alternative was creating a new shared crate for two lines - a larger change than the defect.
  (original text:)  `cargo test -p kasirmu-mobile --lib` fails `commands::registration_gate_tests::drift_pin_no_computed_command_names_in_ui` on one offender, `ui/src/__tests__/dev-mock-dto-conformance.test.ts:61`, which builds an `invoke` command name at runtime. The desktop twin of the same gate PASSES, because it carries test-scaffold tolerance (`counts_as_test_scaffold` / `toleration_applies`, 11 references) and the mobile gate has ZERO references to either, plus no `dev-mock-dto-conformance` entry in its `allowed` list. So the mobile gate is simply the older copy that never received the tolerance, and it fails on a `ui/` test file it was never taught about. The offender is pre-existing and unrelated: it arrived in `e2e4921f7`, both of the gate's inputs are byte-identical to the commit before the landing, and nothing in the landing touched `ui/`.
  Fence: apps/mobile-tauri/src/commands/registration_gate_tests.rs (and the gate's own helper module if the tolerance lives beside it).
  **Do NOT fix this by allowing the offender.** The desktop tolerance exists to distinguish a test scaffold that legitimately builds a name at runtime from production code that does - port THAT predicate to the mobile gate rather than adding a filename to an allow-list, or the next genuine offender in a test file is tolerated too. If the desktop predicate turns out to be too broad to be safe, say so and propose the tighter one, but do not widen the mobile gate's `allowed` list to make it green.
  Done when: `cargo test -p kasirmu-mobile --lib` is fully green (667 passed, 0 failed), the desktop gate's verdict is unchanged, and the ported predicate has a test that shows it tolerating the scaffold while still catching a real computed name in non-test UI code.


- [x] **C10b [P0, RE-SCOPED BY D11] Put a backstop on negative stock WITHOUT killing `allow_negative_stock`.** - **DONE 2026-09-23, commit 948094c34** (`feat(core): enforce the negative-stock backstop conditionally on the binding opt-in`; 6 files, +415/-11, exactly the widened fence with one `create mode`). The trigger WAS expressible, and the load-bearing finding is that **the literal predicate from the brief was wrong**: `NEW.qty < 0 AND NOT EXISTS (a binding with allow = 1)` refuses `deactivate_inventory_location_with_negative_stock_errors`, which seeds `-3` at a location created by `create_inventory_location` that writes **no binding at all** - so `NOT EXISTS` is true and the trigger fires on a case that must succeed. The honest predicate is `NEW.qty < 0 AND EXISTS (a binding for this location) AND NOT EXISTS (a binding for this location with allow_negative_stock = 1)`: the flag is a per-**binding** opt-out, so an unbound location has no opt-out to violate, and Rust Layer 1 already refuses negatives on that path (`adjust.rs:216-224`, where `allow_negative` can only become true *via a binding*), so no race is left open. Both the predicate and the refinement are documented in the migration header and pinned by test. **ACCEPTANCE, BOTH DIRECTIONS:** (a) the two previously-failing tests pass again, untouched - `cargo test -p kasirmu-core negative_stock` -> 4 passed; (b) a NEW raw-SQL test proves the TRIGGER bites - `stock_summary_negative_guard_is_conditional_on_the_binding` drives `INSERT` and `UPDATE` directly (the UPDATE arm is what `INSERT ... ON CONFLICT DO UPDATE` fires once the row exists, the shape both writers use), refused at a bound non-opted-in location, accepted at an opted-in binding **through its upsert**, accepted at an unbound location; plus `stock_summary_negative_guard_is_not_an_unconditional_check`, which reads `sqlite_master` and asserts the table carries **no** `qty >= 0` and no quarantine table - so a future "simplification" back to the blanket CHECK fails in CI rather than in production. **Falsification proved:** neutering the predicate (`WHEN NEW.qty < 0 AND 0`) fails the new test, restored green. **PG twin:** two `TRIGGER_MAP` entries (one shared plpgsql function + INSERT and UPDATE triggers - Postgres needs one function where SQLite needs two triggers, noted in the map comment); regenerated, `--check` exit 0, `.pg.sql` +28 lines all ours. Trigger census pin moved 6 -> 8; the table (127) and index (189) pins unchanged, since the migration adds neither. **What the guard guarantees, stated by the worker:** it constrains the **materialised rollup**, never the ledger - `stock_movements` stays append-only and unconstrained, so the two can still drift, which is what C12's report exists to surface. And per D11 **no existing row is touched**: a trigger fires on writes only, so every negative already in a store survives. **Its own honest gap, filed as C42:** no test EXECUTES the plpgsql port - the ports are hand-written, so their correctness currently rests on the generator's fail-closed parity check (dumped-trigger set vs `TRIGGER_MAP`) plus a line-for-line resemblance to the tested SQLite predicate.
  ORIGINAL BRIEF: A worker implemented the original brief exactly as written - quarantine negatives, rebuild `stock_summary` carrying `CHECK (qty >= 0)`, recreate the index, register the migration - and ran it. Two EXISTING, PASSING tests failed: `db::products::tests::negative_stock_event_fires_when_allow_negative_enabled` (products_tests.rs:2187) and `db::inventory::tests::deactivate_inventory_location_with_negative_stock_errors` (inventory_tests.rs:250, `CHECK constraint failed: qty >= 0`). They are not stale. `allow_negative_stock` is first-class and shipped: `adjust.rs:208-225` writes below zero deliberately, the CHECK reaches that same upsert at `:251`, the handler at `:258-267` turns the `ConstraintViolation` into `InsufficientStockAtLocation` - i.e. the constraint **silently re-enables the guard the flag exists to opt out of** - `sales_lifecycle.rs:279-303` and `products_stock_adjust/batch.rs:78-88` read the same flag, `LocationPicker.tsx:429` renders a "negative stock allowed" badge, `StockShortfallDialog.tsx` lets a cashier override per transaction, and **ADR #17 (`docs/decisions/2026-07-18-multi-location-inventory.md:476-477`) specifies the whole behaviour.** The review was right about the dead code and WRONG about the fix.
  **So the work is now: the CONDITIONAL guard, plus the two dead comments.** 1. A trigger that refuses a negative only when the binding has NOT opted in - `WHEN NEW.qty < 0 AND NOT EXISTS (a binding with allow_negative_stock = 1)`. 2. Fix the two comments at `adjust.rs:31` and `:144` that claim a Layer 2 guard which does not exist - that dead claim IS the documentation defect the review actually found.
  Fence (WIDENED by the D11 decision, because every literal route needs files the original fence excluded): a NEW migration under `crates/kasirmu-core/migrations/`, `crates/kasirmu-core/src/migrations.rs`, `crates/kasirmu-core/src/migrations_tests.rs`, `crates/kasirmu-core/src/db/products_stock_adjust/adjust.rs` (COMMENTS ONLY - do not change the write path; **PATH CORRECTED**: this brief first said `src/db/products/adjust.rs`, which does not exist - the real file is `products_stock_adjust/adjust.rs`, whose `:31`/`:144` carry the dead claims), `scripts/generate-pg-migration.py` (a `TRIGGER_MAP` entry - the generator's own header at `:28-30` and `:131` documents that a conditional constraint needs a hand-written plpgsql port), and the regenerated `crates/kasirmu-core/migrations/20260813_init.pg.sql`.
  **Acceptance, and it must prove BOTH directions:** the two existing tests above must still PASS (an opted-in binding may go negative), AND a new test must show a binding that has NOT opted in is REFUSED at the database level by RAW SQL (so it proves the trigger, not the Rust guard). The quarantine table and the zeroing are optional here - the worker's prepared body is recoverable if the owner ever picks the other branch, but under D11 the existing negatives are legitimate oversells and should NOT be blanket-repaired.
  Do NOT delete or weaken the two failing tests to make a constraint pass. They are the only tests pinning a shipped feature, and weakening them was the failure mode this workstream has otherwise avoided.

- [x] **C39 [P0] NEW, residual of C34: the embedded surface still ACCEPTED a caller-supplied `tenant_id`.** - **DONE 2026-09-23, commit ebddcbfcb** (`fix(api): refuse a tenant-scoped token mint on the single-tenant surface`). C34's boundary guard made a foreign-tenant write harmless, but the field was still accepted: `POST /api/v1/tokens` passes `body.tenant_id` straight into `create_token_full` (`crates/kasirmu-api/src/routes/tokens.rs:268-275`). This closes the trap rather than fencing it. The hard constraint held: the **cloud** surface must keep accepting the field, because that is how a tenant's token gets its tenant (`routes/tokens.rs:219`, minting from the registered `sync_terminals` row), so the fix is surface-discriminating rather than a deletion. **The choice was REFUSE, not drop, and the reason is worth keeping:** dropping the claim would mint a token whose scope silently differs from what the caller asked for - a script sending `tenant_id: "acme"` would get a `default`-scoped token and believe it held acme scope, so every later request would be judged against a tenant the caller never agreed to. That is a silent authority mismatch, worse than an error because nothing surfaces it. Refusal names the problem at the only moment the caller can act, and matches the shape this surface already uses for its sibling restriction (`terminal_credentials_disabled`, 400). Status 400, error `tenant_claim_not_supported`. **Mechanism: a `SingleTenantSurface` request marker** declared in `routes/tokens.rs` and inserted by the embedder's own layer, extracted as `Option<Extension<...>>` - so the CLOUD server never inserts it and is untouched **by construction** rather than by a branch that could drift. The worker chose an extension over a second `AppState` boolean deliberately: `AppState` is built as a struct literal by ~20 sites, nearly all of them test files outside the change, so a new field would have forced a sweep of files this change had no business touching; an extension is inserted by the embedder and cannot be forged by an HTTP client, unlike a sentinel header. Cloud-side change was reflexively additive: one public marker type, one optional extractor parameter, one guarded refusal placed AFTER `admin_key_authorised` and the client-credentials branch and BEFORE `resolve_read_permissions`. **Behaviour table:** embedded + foreign tenant 200 -> **400**; embedded + none/`default` 200 -> 200; cloud + foreign tenant 200 -> **200 unchanged**; embedded terminal client-credentials unchanged. **Evidence:** `cargo test -p kasirmu-local-api` -> 18 passed (the four C34 tests still pass unchanged, plus a new end-to-end mint refusal); `cargo test -p kasirmu-api --lib tokens` -> 26 passed (4 new, including cloud-still-accepts-and-the-claim-reaches-the-token); fmt clean. **Falsification:** removing the marker insert from the embedder layer fails the new test at its 400 assertion, and both files were restored byte-identically.

- [x] **C40 [P2] NEW, found when a C17 slice was mis-targeted: the tablet has no backup-status command** - **DONE 2026-09-23, commit 44c06dc55** (`ui/src/features/settings/hooks/useBackupStatus.ts` + `ui/src/__tests__/DataManagementBackup.test.tsx`, 155 insertions / 4 deletions, no Rust file touched). **VERDICT: REACHED AND SILENTLY FAILING, then swallowed** - worse than dead code, exactly as the brief suspected. Proved rather than assumed: the branch is LIVE (`useBackupStatus` is called by `DataManagementScreen.tsx:83`, registered as the `data-management` page at `features/settings/register.tsx:31`, and that page is a TABLET nav item - the `admin` workspace seeds `('admin','data-management',8)` at `migrations/20260813_init.sql:1454`, `TabletAppShell.tsx:397-424` renders it via `getPage(currentRoute)`, and `WorkspaceHome.tsx:458` switches to the `admin` workspace on tool navigation), and NOTHING GUARDS IT (`isTabletShell()` is used only inside `handleBackup` at `:97`; the mount effect has no shell check, so the ternary runs and picks `getBackupStatusScoped` or `getBackupStatus`). Both names are desktop-only, so an unregistered invoke is rejected by the IPC layer and `.catch()` at `:76-82` swallows it into a toast plus `lastBackup: undefined` - **so the operator got a spurious error toast on every mount of the Data tab and a permanently failed panel.** **The fix refused to add a tablet command, for a reason worth keeping:** the bridge's status body (`kasirmu-bridge/src/data.rs:414-431`) reads `default_backup_path` = `<db>.backup.db` beside the database, which on Android is private storage the tablet never writes - the tablet backs up to an operator-chosen destination via `create_backup_to`. Such a command would answer `lastBackup: null` and the UI would render **"Never"** for a store that HAS backed up, which is the precise compliance claim `BackupSection.tsx:73-77`'s three-way render exists to refuse. So the honest answer is that the tablet has NO readable backup status, and the fix holds `lastBackup` at `undefined` - "never answered", not "Never" - reusing the file's existing `isTabletShell()` owner rather than adding a second mechanism. **Evidence:** `npx vitest run` on that suite -> 17 passed (was 14); MUTATION PROOF - reverting the guard to the old ternary fails exactly the 3 new assertions, restored -> 17 green; a fourth case pins that tablet backup still goes through `create_backup_to`. 362 passed across the 7 neighbouring suites sharing the hook/api, and `npm run typecheck` exit 0. **Its own stated gap became C41.**

  ORIGINAL BRIEF:, yet the shared Settings screen calls for one.** `ui/src/features/settings/hooks/useBackupStatus.ts:65-67` calls `getBackupStatus()` / `getBackupStatusScoped()` on the null-session-token path. Those are **desktop-registered** names: `apps/desktop-tauri/src/commands/data.rs:33` defines `get_backup_status` and `apps/desktop-tauri/src/lib.rs:964` registers it, while `apps/mobile-tauri` has **zero** occurrences of either name outside prose comments and no backup-status read at all (`git grep -nE 'get_backup_status_scoped|create_backup_scoped' -- apps/mobile-tauri/` returns comments only). So on the tablet shell that fallback has no registered command to reach, and whether it is dead code or a silently failing call is UNVERIFIED - the finding is the gap, not a verdict on it.
  Fence: ui/src/features/settings/hooks/useBackupStatus.ts and its test; `apps/mobile-tauri/src/commands/data.rs` + the registration lines in its `lib.rs` ONLY if the honest fix is to give the tablet a scoped status read. Nothing else.
  Done when: either the fallback is shell-aware and never asks for a desktop-only command, or the tablet has a real scoped status command and the hook uses it - and the chosen path is asserted by a test that fails if the call would reach an unregistered name. Do not simply delete the fallback without checking what the Settings screen shows when the status read is unavailable.

- [x] **C38 [P1] NEW, found while landing C18 P1.3b: PostgreSQL gets the index WITHOUT the reconciliation that makes it safe.** - **DONE 2026-09-23, commit 259c4eec9** (`fix(pg): emit the open-shift reconciliation before its unique index`; `apps/cloud-server/tests/pg_init_reconciliation.rs` new 228, `scripts/generate-pg-migration.py` +199, regenerated `init.pg.sql` +32). **MECHANISM (b), an explicit `PRE_INDEX_RECONCILIATIONS` list - declared, emitted and ENFORCED - and the worker checked (a) before rejecting it:** the generator CANNOT carry the data statement because it never sees it - it rebuilds PG from the FINAL SQLite state (tables, indexes, triggers, seeds) and replays only RENAMEs as evolution ops, so the `UPDATE` is DROPPED, not mis-translated; generic SQLite-DML translation is intractable (`strftime`, `GLOB`, row-value comparisons all differ) and a half-working translation fails exactly the way this fix exists to prevent. The list mirrors the `TRIGGER_MAP`/`TRIGGER_VERIFICATION` discipline so the twin is VERIFIED rather than described: `index_name` must name an index the generator actually emits or generation fails (so a stale entry cannot emit a statement nothing depends on), `sql` is emitted inside a `to_regclass('public.shifts')` guard so a fresh database is a clean no-op, `verified_by` must name an existing test file, and `body_sha256` pins the statement. **Emission order is the whole point: the block lands at line 1380, the index at 2593.** **Acceptance, with the honest limit handled properly:** a fresh-init test asserts the index exists, and the worker explicitly refused to claim a no-op as evidence that the reconciliation ran; the STRONG form is the seeded case - two open shifts for one user, then `PG_INIT` succeeds, the index is created, the OLDER row is `closed` with the `auto-closed: duplicate open shift (COR-27 reconciliation)` note and `shift-new` survives. **Falsified by neutering the emit loop** (0 `auto-closed` occurrences): the test then FAILED with `could not create unique index "idx_shifts_open_per_user"`, `Key (user_id)=(user-dup) is duplicated`, SQLSTATE **E23505** - the exact startup failure C38 describes, reproduced rather than described. Restored to 2 occurrences, `--check` exit 0. `--self-test` now prints **three** gates ok (RLS, trigger, pre-index), and both new enforcement directions bite on the real tree (digest mismatch exit 1 with declared-vs-actual hashes; stale index name exit 1). C42 (1 PROVEN) and C44 (6 PROVEN) both still pass. Reachability control in house form: live 2 PROVEN / 0 SKIP, dead port 0 PROVEN / 2 SKIP.
  **AND IT REPORTED A BIGGER FINDING RATHER THAN SWEEPING IT, as instructed: the generator emits ZERO `UPDATE` statements while SEVEN migrations carry one** - see C46. C38 was the only one that bricks init; the other six leave legacy rows un-backfilled on a pre-existing PG database.
  ORIGINAL BRIEF: The SQLite migration reconciles before it constrains (see C18 P1.3b), but the generator has no PG port for that `UPDATE`: `grep -c auto-closed crates/kasirmu-core/migrations/20260813_init.pg.sql` returns **0** while the migration returns 2. So `init.pg.sql` receives a bare `CREATE UNIQUE INDEX IF NOT EXISTS idx_shifts_open_per_user ...` with no preceding repair. On any PostgreSQL database that already holds two open shifts for one user, that statement fails at init.
  **How reachable is it?** The landing worker verified there is NO PG-side writer of `shifts` anywhere in the repo - no `INSERT INTO shifts`/`UPDATE shifts` in `apps/cloud-server` or `crates/kasirmu-api`; every writer is `rusqlite` on the device. So the application cannot create that state. That is evidence, not proof about live data, and the PG side has no guard against a state created by any other means (a console session, a migration, a repair script - the same argument the SQLite side used to justify moving the guard into the schema).
  Fence: scripts/generate-pg-migration.py (a `TRIGGER_MAP`-style port or an explicit reconciliation statement emitted before the index) plus the regenerated `20260813_init.pg.sql`. Coordinate with any session holding a dirty generator - this file's working tree is a shared hazard.
  Done when: either the PG init carries an equivalent reconciliation before the index (preferred, and consistent with how the SQLite side handled it), or a record states with evidence why PG cannot reach the state and the index's `IF NOT EXISTS` is therefore sufficient. Do NOT add `FORCE ROW LEVEL SECURITY` or touch the RLS lists - that is D2 and C33's territory.


- [x] **C42 [P1] NEW, the worker's own stated gap from C10b: nothing EXECUTES the plpgsql trigger port.** - **DONE 2026-09-23, commit a1c132ed7a** (`test(cloud-server): execute the plpgsql negative-stock port against real PostgreSQL`; `apps/cloud-server/tests/pg_stock_guard.rs` new, 264 lines, one `create mode`, nothing else changed - the SQLite trigger, its migration and `20260813_init.pg.sql` are byte-identical to HEAD, verified by `git hash-object` after the falsification runs). **THE PORT DID NOT DIVERGE**: all three cases behave identically to the SQLite predicate - bound-non-opted-in REFUSED on both arms (the INSERT refusal asserted to be the plpgsql `RAISE` specifically, by SQLSTATE plus a message naming `allow_negative_stock`), opted-in ACCEPTED through its upsert (read back `-9`), unbound ACCEPTED (read back `-3`). **Reachability control, house standard:** the live run printed `PROVEN: executing the plpgsql port against real PostgreSQL` with 1 proven, 0 skipped; the control against a dead port printed `SKIP: PostgreSQL unreachable` with 0 proven, 1 skipped. It runs in a THROWAWAY database (`oz_stock_guard_test_%`, dropped on the happy path, stale ones reaped next run) and never touches the shared schema, roles, RLS or `rls-cutover.sql`. **Falsification:** replacing the port's guard with the divergent literal form (dropping the `EXISTS` clause, keeping the dict key) makes the test FAIL at the unbound case (`pg_stock_guard.rs:246`), then the file was restored and re-hashed. **And it found a real defect in its OWN test rather than shipping it:** the first version asserted on `tokio_postgres::Error`'s `Display`, which is the generic `"db error"` and would have passed on ANY failure including the wrong one - it now reads `as_db_error().message()` plus the SQLSTATE, the same trap `sync_store/pg.rs` documents. **Its parity-check answer is now C43 and it is the most transferable finding of the remediation.**
  ORIGINAL BRIEF: Two `TRIGGER_MAP` entries were added to `scripts/generate-pg-migration.py` (one shared plpgsql function plus the INSERT and UPDATE triggers) and the regenerated `20260813_init.pg.sql` carries them, but correctness currently rests only on the generator's fail-closed parity check (dumped-trigger set vs `TRIGGER_MAP`) plus a line-for-line resemblance to the tested SQLite predicate. A hand-written port of a tested predicate is exactly the kind of artifact that reads correct and behaves differently - and this one silently decides whether a store may go negative.
  Fence: apps/cloud-server's PG tests or a dedicated test that creates a THROWAWAY database in the dev container (the C36 worker recorded that this crate's other PG tests already use throwaway databases rather than the shared base - follow that pattern; do NOT alter the shared schema, do NOT run rls-cutover.sql, do NOT touch roles).
  **The reachability constraint is now known and must be honoured:** PostgreSQL IS reachable at 127.0.0.1:15432 with `DATABASE_URL` unset, so the test must supply its URL explicitly, and it must PROVE it ran rather than skipped - the house standard is a control run against a dead port (skip messages printed, run takes minutes) versus the live port (zero skips, run is fast). A green run that silently skipped is the failure this workstream has caught repeatedly.
  Done when: the plpgsql guard is exercised live in both directions - a negative quantity at a bound non-opted-in location is REFUSED, and the same quantity at an opted-in binding is ACCEPTED through its upsert - matching the SQLite half case for case. If the port turns out to DIVERGE from the SQLite predicate, that is the finding: report the divergence and fix the port, do not adjust the SQLite trigger to match a wrong port.
  VERIFY: the narrow PG test target with its URL, plus the control run against a dead port, both with true exit codes and skip counts.


- [x] **C43 [P0] NEW, and it is the most transferable finding of the remediation: the generator's TRIGGER_MAP check compares NAMES ONLY, never semantics.** - **DONE 2026-09-23, commit 74a9bce4e** (`scripts/generate-pg-migration.py` +193/-1, the only file committed; the generated PG file is content-identical to HEAD so it was correctly left untouched). **The worker strengthened option (a) because option (a) AS SPECIFIED COULD NOT MEET ITS OWN ACCEPTANCE CRITERION, and it checked before building:** the rule 'declare the live test that exercises it' would have left the C42 shape passing, because in that shape the body is mutated while the key is unchanged, so the `verified_by` path still exists and still resolves. Naming a test proves a test is ASSOCIATED, not that it passes or that the body is unchanged. **So the enforceable rule is (a) plus a body digest:** `verified_by` (a test path resolved from repo root, which must EXIST - a port cannot be added without naming its evidence) and `body_sha256` (sha256 of the newline-normalized port body - ANY body edit without re-recording the digest fails generation). That is an exact mechanical change detector, not a heuristic and not a plpgsql parser: it cannot verify semantics, but it makes a silent divergence **impossible to ship**, because the author must re-pin the digest, which is an explicit reviewable act the gate's own error message prompts. **Before/after, five scenarios with exit codes:** (1) the C42 shape - mutated body, unchanged key - went from `BEFORE_EXIT=0` ('ok: matches the generator') to `GEN_EXIT=1` / `AFTER_EXIT=1` with `error: trigger verification failed (the membership check proves presence only, never semantics): - stock_summary_qty_nonnegative_insert: body changed (declared de2dbc9691b7, actual 74a0e52abb38) - re-record body_sha256...`; (2) the real tree still `CHECK_EXIT=0`; (3) hand-editing the PG file with TRIGGER_MAP untouched is STILL caught (`HAND_EDIT_EXIT=1`, no regression); (4) `--self-test` now exercises SIX fail directions (undeclared port, port missing from the table, stale entry, blank `verified_by`, missing test file, digest mismatch) plus the passing direction - fail-closed both ways, matching the RLS gate's discipline; (5) a `verified_by` naming a missing file fails with the path named. Every mutation was reverted and re-verified. **Limits stated by the worker rather than implied away:** no semantic comparison exists and none was added - a re-pinned digest on a wrong port still ships green, so the gate converts SILENT divergence into EXPLICIT divergence and that is all, stated in the file header and on the table; `verified_by` is only as strong as the named test; the digest must be re-pinned by hand, which is intended friction but means a careless 'just update the hash' defeats the gate; and `--self-test` is not wired into CI (only `--check` is). **Its most valuable admission became item C44:** of the seven ports, only `stock_summary_qty_nonnegative_*` names a test that EXECUTES the plpgsql against real PostgreSQL; the other SIX name SQLite-side tests - compensating controls, not PG execution.
  ORIGINAL BRIEF: Found by the C42 worker, and PROVED rather than inferred: it mutated a `TRIGGER_MAP` body to a divergent predicate **while keeping the key**, regenerated, and `python3 scripts/generate-pg-migration.py --check` printed `ok: ... matches the generator` with **exit 0**. The code (`scripts/generate-pg-migration.py:972-983`) selects only `name` from `sqlite_master` and only compares membership in each direction:
  ```python
  dumped_triggers = [r[0] for r in db.execute("SELECT name FROM sqlite_master WHERE type='trigger' ORDER BY name")]
  missing = [t for t in dumped_triggers if t not in TRIGGER_MAP]
  stale   = [t for t in TRIGGER_MAP if t not in dumped_triggers]
  ```
  It never reads the trigger body, and never compares the SQLite `WHEN` clause against the plpgsql `IF`. **So before C42's live test existed, a WRONG plpgsql port would have shipped green through every existing gate.** The same worker also demonstrated the guard's real scope: hand-editing the generated PG file WITHOUT touching `TRIGGER_MAP` IS caught (`--check` exit 1, '535 differing lines ... port the plpgsql in TRIGGER_MAP first'), so **the guard protects the file, not the predicate.**
  Why this is P0 rather than hygiene: `TRIGGER_MAP` is the mechanism by which EVERY conditional constraint crosses into PostgreSQL, and there are now two hand-written ports in it. A name-only check makes 'the PG twin exists' feel verified while the only thing verified is that it exists.
  Fence: `scripts/generate-pg-migration.py` (the check and its comment/message only) plus the generated file if the change alters output. Do NOT change any trigger body and do NOT change the SQLite migrations.
  **Done when, and prefer an enforceable rule over a clever comparison** - comparing plpgsql to SQL semantics in general is not tractable, so the honest options are: require each `TRIGGER_MAP` entry to DECLARE the test that exercises it live (a `verified_by` field, and generation FAILS when a trigger has no such declaration or the named test does not exist), or state the limitation in the check's own message and in the file header with the C42 test named as the compensating control. Choose one, justify it, and make the choice non-silent - the failure mode being fixed here is exactly a check that reassures more than it verifies.
  Acceptance: the negative control is mandatory and must be the C42 shape - a `TRIGGER_MAP` body mutated to diverge while its key is unchanged must now FAIL the gate (or be named as unverified by the mechanism you chose), with both runs recorded.


- [x] **C44 [P1] NEW, the C43 worker's own admission: six of the seven TRIGGER_MAP ports are verified by SQLite-side tests only.** - **DONE 2026-09-23, commit 7c7bbbe1c** (`apps/cloud-server/tests/pg_trigger_ports.rs` new, 514 lines; `scripts/generate-pg-migration.py`; regenerated `20260813_init.pg.sql`). **A REAL DIVERGENCE WAS FOUND, AND IT WAS A SILENT COMPLIANCE FAILURE:** `audit_log_immutable_delete` ended with `RETURN NULL`, and on a row-level **`BEFORE DELETE`** trigger returning `NULL` **CANCELS THE DELETE**. The SQLite original (`20260920_audit_retention.sql:25-34`) is a `WHEN NOT EXISTS(marker)` trigger, where a false `WHEN` means the body never runs and the DELETE **PROCEEDS**. So the port refused correctly without the marker - the direction the old SQLite-side test happened to cover - but **deleted nothing when the marker was present**: the audit-retention sweep would report success while purging ZERO rows, forever, raising no error anywhere. Fixed to `RETURN OLD` (the correct plpgsql for a BEFORE DELETE that permits the row) with the reason in the port's own comment. **AND THE C43 DIGEST COULD NOT HAVE CAUGHT IT** - the body was wrong from the day it was written, and a digest only detects CHANGE. That is the argument for executing a never-run port rather than pinning it, made concretely and in this repository's own compliance surface. **All six ports now EXECUTE against real PostgreSQL**, each with both directions where conditional: `audit_log_immutable_update` matches (refused unconditionally, including under the sweep marker - the carve-out is DELETE-only, as in SQLite); both `loyalty_tiers_validate_*` match (valid accepted; zero multiplier, blank name, negative points, zero ppu, short colour and non-hex colour refused); both `trg_assignments_scope_id_pair*` match (org+NULL and location+id accepted; org+id and location+NULL refused; org->location accepted, org-keeping-id and location-losing-id refused). **Reachability control:** live run `cargo test -p kasirmu-cloud --test pg_trigger_ports` -> exit 0, **6 PROVEN, 0 SKIP**; control against a dead port -> exit 0, **0 PROVEN, 6 SKIP**. C42's `pg_stock_guard` re-run green (1 proven, 0 skip). `--check` exit 0 and `--self-test` ok on both gates. **Every assertion uses the structured error** - `as_db_error().message()` plus `assert_eq!(code, SqlState::RAISE_EXCEPTION)` via a shared `assert_refused` helper - so a NOT NULL violation or a type error cannot masquerade as the guard; C42's `Display` defect was not repeated. **Regression guards held:** mutating a body while keeping its key still fails generation (`body changed (declared 889d995af65e, actual 66befbef8e12)`), and hand-editing the PG file with `TRIGGER_MAP` untouched is still caught. `verified_by` for all six now names `pg_trigger_ports.rs`. **Limits stated by the worker, kept:** `verified_by` is only as strong as the named test, so a new port whose test does not actually exercise its predicate would still pass; nothing compares plpgsql semantics to SQL and the digest still only detects change; and the six tests cover the predicates each port encodes, not every path a writer could reach - the loyalty ports in particular are exercised via raw SQL, not through `Store`.
  ORIGINAL BRIEF: Of the seven entries, **only `stock_summary_qty_nonnegative_*` names a test that actually EXECUTES the plpgsql against real PostgreSQL** (`apps/cloud-server/tests/pg_stock_guard.rs`, C42). The other six name SQLite-side tests (`audit_integration.rs`, `migrations_tests.rs`, `assignments_tests.rs`) - compensating controls, not PG execution. So six ports were written by hand to mirror a SQLite trigger, are pinned by name and now by body digest, and have **never been run in PostgreSQL at all**. The C42 shape applied six more times is a real, unclaimed gap, and the worker wrote it on the table rather than letting a green gate imply otherwise.
  Fence: a new or extended PG test beside `apps/cloud-server/tests/pg_stock_guard.rs` (the C42 file is the template, including its throwaway-database pattern and its PROVEN-vs-SKIP control), plus the `verified_by` strings in `scripts/generate-pg-migration.py` for the entries whose evidence changes. Do NOT change any trigger body or the SQLite migrations - a divergence found here is a finding to report and fix in the PORT, never to reconcile by editing the tested SQLite half.
  Do them in ONE slice if the six share a shape (they are all trigger ports in the same database, so a single throwaway-database harness can exercise each), or split by trigger family if they do not. The acceptance is the same for each: the port is EXECUTED against real PostgreSQL, in both directions where the trigger is conditional, in a throwaway database that never touches the shared schema, roles, RLS or `rls-cutover.sql`, with a PROVEN line on success and a SKIP line when the server is unreachable - so a green run is shown to have run.
  **Report any divergence as the finding.** A hand-written port that has never executed is exactly the artifact this workstream has found wrong four times over (the composite FK, the briefed CHECK, the shell-blind parity gate, the name-only trigger check). Expect at least one.
  Done when: every port either has live PostgreSQL execution behind it, or carries a written reason why it cannot - and `scripts/generate-pg-migration.py --check` still exits 0 with the `verified_by` strings updated to name the new evidence.


- [x] **C45 [SEVERITY DOWNGRADED P0->LATENT, see below] NEW, found by C19 slice A answering the short-circuit question: a THIRD outcome applier strands items the server actually holds.** - **DONE 2026-09-23, commit cc21a1c6a** (`fix(sync): classify duplicate-id replays in the SyncEngine push applier`; platform/sync/src/lib.rs +78, lib_tests.rs +194). **WHAT I GOT WRONG IN FILING IT:** I wrote 'silent, terminal, one-sided - a live data-stranding bug'. The worker checked reachability and found `run_sync_cycle` has **NO production caller anywhere in the repo** (`grep -rn 'run_sync_cycle'` returns only its definition, a module-doc example and a test-helper comment; `SyncEngine` is referenced in `platform/sync` only, never from `apps/`). The live desktop path is `SyncDaemon`/`PgSyncDaemon` and the live tablet path is the immediate `apply_sync_outcomes` in mobile-tauri. **So this was a LATENT defect on a currently-unreachable path** - correct and cheap to fix, and it would have become live the moment anyone called `run_sync_cycle`, but it was NOT stranding rows today. **AND `run_sync_cycle` HAVING NO CALLER IS ITSELF A FINDING**, in the same family as the dead/aspirational code this review was written about. **Reuse, not a fourth copy:** `is_duplicate_id_rejection` was already `pub` in `kasirmu_core::sync_client` and `daemon.rs:259` already calls it fully-qualified, so visibility needed no out-of-fence edit and no Fence_Request; verified by enumeration there is now one definition and four callers, and the constant is referenced from the doc comment rather than re-typed. **Testability required a real change:** the inline `for`/`match` was extracted into a named free function `apply_push_outcomes(queue, store, batch, results)` mirroring `daemon::apply_push_results`, because `SyncTransport` is a CONCRETE STRUCT not a trait, so the applier was previously unreachable by any test that did not stand up an HTTP server. **Acceptance:** `Rejected { reason: "duplicate id: <id>" }` -> row reads `Synced` with a `synced_at`, on THIS applier, proven to fail pre-fix (removing the guard arm failed exactly that one test, 5 passed / 1 failed); a genuine rejection still marks `Failed` with `last_error` preserved; `Accepted` still synced; `Conflict` still routes through the shared ADR #21 resolver (asserts the `resolved: conflict` marker); truncated results leave trailing items `Pending` rather than inheriting a neighbour's outcome; and `..._duplicate_prefix_is_anchored` pins that a reason merely MENTIONING a duplicate id (`"validation failed: duplicate id: suffix"`) stays a rejection, since the predicate uses `starts_with`. `cargo test -p platform-sync apply_push_outcomes` -> 6 passed; `queue` -> 88 passed. **Its transfer answer is the valuable part and it went the honest way:** the id-reuse premise HOLDS (ids are `Uuid::now_v7()` assigned once at enqueue, and `enqueue_settings_update_superseding` DELETES superseded rows rather than reusing ids), the 'mutation landed' premise HOLDS (the server's duplicate-id comes from the INSERT itself, `ON CONFLICT (id) DO NOTHING RETURNING id` with zero rows returned, inside the push transaction - not from a validation stage), but the REACHABILITY premise FAILS. So the reasoning transfers semantically and the SEVERITY does not - which is why this item is downgraded rather than sold. **And it found a FOURTH applier that IS reachable: see C48.** One more boundary noted: server-side prune deletes `offline_queue` rows after a 90-day retention horizon (`apps/cloud-server/src/prune.rs:19-26`), so the replay window is time-bounded - 'the server already holds it' is true only within retention.
  ORIGINAL BRIEF: `platform/sync/src/lib.rs:556-568` routes EVERY `PushOutcome::Rejected` to `mark_failed` with **no duplicate-id predicate**, unlike the other two appliers, which both use `is_duplicate_id_rejection` (`sync_client.rs:310`, `daemon.rs:259`). Consequence: a crash between the server's insert and the local mark, then a re-push, yields `duplicate id:` -> `mark_failed` -> **terminal `failed` for an item the server already holds**. That is precisely the data-stranding failure the `DUPLICATE_ID_REJECTION_PREFIX` doc comment says the predicate exists to prevent - this third applier was simply never wired to it.
  Why P0 and not P1: it is silent, terminal and one-sided - the server has the data, the terminal believes the push failed forever, and no error surfaces. Same shape as the audit-retention divergence C44 found.
  Fence: `platform/sync/src/lib.rs` (the outcome-application region only) plus its sibling test file. Do NOT touch `sync_client.rs`, `daemon.rs`, `queue.rs` or `pg_daemon.rs` - read them as the reference implementations of the predicate, change only the third applier.
  **Reuse the existing predicate and the existing constant; do not re-implement the string match a third time.** If `is_duplicate_id_rejection` is not visible from `lib.rs`, prefer making it visible over copying it - three copies of a rejection-classification rule is the divergent-twins failure this workstream keeps finding. If visibility requires touching an out-of-fence file, STOP and file a Fence_Request naming it.
  Acceptance: a test that a `Rejected` outcome whose error carries the duplicate-id prefix marks the row SYNCED rather than failed, on THIS applier specifically (not through `sync_client`), and a test that a genuine rejection still marks failed. Prove the first fails against the pre-fix code. Confirm behaviour for the other `PushOutcome` variants is unchanged - state which variants you checked.
  VERIFY: `cargo test -p platform-sync` narrow filters around the outcome appliers, plus the queue tests. CONTENTION RULE: eleven workers have failed today, every one at a cargo step. Narrow target, retry ONCE on a lock or rust-lld permission failure, never widen, never two cargoes at once.

- [ ] **C19 slice B [P2] The two in-file twins of the function just fixed.** `mark_offline_synced_for_tenant` (`db/offline.rs:625` post-slice-A; cited as 625 by the worker) and `mark_offline_resolved` (`:648`) carry the identical unguarded shape that slice A just fixed on `mark_offline_synced`. Deliberately left: the brief scoped slice A to ONE named function, and `mark_offline_resolved` has different semantics (it writes a `last_error` marker) so changing it widens blast radius. Fence when picked up: the same two files, with the same compare-and-set shape.


- [x] **C46 [P1] NEW, found while fixing C38: the generator emits ZERO `UPDATE` statements while SEVEN migrations carry one, so six backfills never reach an existing PostgreSQL database.** - **DONE 2026-09-23, commit 3891dd219** (`apps/cloud-server/tests/pg_init_reconciliation.rs`, `scripts/generate-pg-migration.py`, regenerated `20260813_init.pg.sql`; no SQLite migration touched and no trigger body changed - verified by 0 `CREATE OR REPLACE`/`RAISE` lines in the diff). **MECHANISM: `PRE_INDEX_RECONCILIATIONS` was RENAMED to `RECONCILIATIONS`, one list with an `anchor_kind` field ("index" | "table") - NOT a second parallel list**, which is the right call for the reason stated: two lists with one discipline is how divergent twins start. `check_reconciliations` is the single gate for both kinds with identical enforcement (declared `verified_by` that must exist, pinned `body_sha256`, stale-anchor rejection, fail-closed self-test now covering both kinds). **Ordering is the semantic difference and it is stated explicitly:** `index` entries emit BEFORE the DDL (the index cannot be created until rows are repaired); `table` backfills emit AFTER the DDL, so the table and its columns exist. **THE MEASUREMENT, which is what the slice was dispatched for - six migrations ranked, ONE ported:** #1 `20260910_memo_child_tenant_id.sql` (`memo_recipients`) **PORTED because it is the only one that matters**: it has a live PG WRITER (`kasirmu-api/src/pg.rs:2621`, the memo fan-out) AND RLS membership (`memo_recipients` is in `RLS_TABLES`), so a pre-migration row carrying the column DEFAULT `'default'` while belonging to a real tenant is **invisible to its own tenant under RLS** - and the migration's own header says the denormalization exists precisely so these tables can be tenant-filtered and covered by RLS on Postgres. #2 sale_line_snapshots - **not observable**: PG reads snapshots through `COALESCE(sl.product_id, p.id, 'deleted:'||sl.sku)` (`email_pg/analytics.rs:438`), which computes exactly what the backfill would freeze, so a NULL snapshot is served identically. #3 loyalty_multiplier_fixedpoint - **inexpressible**: the pre-state never existed in PG (`grep` for the old `earn_multiplier REAL` column returns 0) and the migration's own header says 'Postgres: intentionally NOT touched'. #4 rename_store_to_location - **no reader**: zero PG readers of `workspace_screens.screen_key` anywhere, and the emitted seed already carries only the converged `'locations'` row. #5 legal_entities - **no reader**: every PG-side `legal_entity_id` read is on `tax_rates`, a different column. #6 terminals_tenant - **not an isolation key**: `terminals` is in `RLS_EXEMPT`, so its `tenant_id` is not a live isolation key on PG, where cloud tenancy is read through `sync_terminals`. **Stated limit, kept:** whether a SPECIFIC live deployment holds affected rows for #1 cannot be determined from this checkout - what could be established is that #1 is the only one where affected rows cause INCORRECT BEHAVIOUR, so it is the only one worth a port; the other five are inert regardless of whether rows exist. **Acceptance in the strong form:** fresh-DB index and the seeded-duplicate case (C38's tests, still green), PLUS a new seeded pre-migration `memo_recipients` row belonging to tenant `acme` reading back `tenant_id = "acme"` after `PG_INIT`. **Falsified:** neutering the table-backfill emit loop fails the test with `left: "default", right: "acme"` - the exact RLS isolation defect. Restored, `--check` exit 0. **Reachability control:** live 3 PROVEN / 0 SKIP; dead port 0 PROVEN / 3 SKIP; C42 (1 PROVEN) and C44 (6 PROVEN) still green; `--self-test` prints 3 gates ok including 'reconciliation gate ... both kinds'. Enforcement re-verified on the real tree (digest mismatch exit 1; stale `table:` anchor exit 1). **Transient blocker reported rather than worked around:** its first live run failed to compile with `error[E0425]: cannot find function order_for_push` at `platform/sync/src/daemon.rs:144` - ANOTHER session's uncommitted work (the C49 slice in flight). It retried ONCE per the contention rule, the other session had settled its file, and it compiled clean. It did not touch those files.
  ORIGINAL BRIEF: The worker reported it rather than sweeping it, as instructed. C38's was the only one that **bricks init** (its `UPDATE` exists to make a DDL statement applicable). The other six are convergence/backfill on EXISTING rows: on a fresh PG database the columns are created with their final defaults so nothing is missing, but on a **pre-existing** PG database they leave legacy rows un-backfilled - a data-correctness gap, not a startup failure. That distinction is why C38 was fixed alone.
  The six, each with **zero** PG twin: `20260826_sale_line_snapshots.sql` (backfill `sale_lines` snapshots); `20260831_loyalty_multiplier_fixedpoint.sql` (`earn_multiplier_millionths = CAST(ROUND(earn_multiplier*1000000))`); `20260906_rename_store_to_location.sql` (`workspace_screens.screen_key` 'stores' -> 'locations'); `20260908_legal_entities.sql` (backfill `locations.legal_entity_id`); `20260910_memo_child_tenant_id.sql` (backfill `memo_revisions` + `memo_recipients` tenant_id); `20260912_terminals_tenant.sql` (backfill `terminals.tenant_id`). Partly mitigated for loyalty: the `loyalty_tiers_validate_*` ports were executed in C44 and the fixed-point column has `DEFAULT 1000000`, so only PRE-EXISTING rows keep an un-migrated multiplier.
  **THE PREMISE CHECK COMES FIRST AND IT NEEDS SOMETHING THIS CHECKOUT CANNOT GIVE.** The worker stated plainly that it did not verify whether any of the six currently matter in practice - that needs the live cloud deployment, not this checkout (does it predate those migrations and hold un-backfilled rows?). So slice one is a MEASUREMENT, not a port: establish, per migration, whether a plausible existing deployment can hold rows that the backfill would still change, and rank them. If the honest answer is 'cannot be determined from here', say so and port the ones where the answer is unambiguous.
  Fence: `scripts/generate-pg-migration.py` (extending the mechanism the C38 worker just built - it is `PRE_INDEX_RECONCILIATIONS`, and a generalisation may be the right move, in which case say why rather than adding a second parallel list) and the regenerated `crates/kasirmu-core/migrations/20260813_init.pg.sql`, plus its own PG test beside `pg_init_reconciliation.rs`. Do NOT change the SQLite migrations and do NOT change any trigger body.
  Done when: each of the six is either ported with a live-PG proof that the backfill actually changes a seeded legacy row (the strong form the C38 worker used - seed the pre-migration state, run `PG_INIT`, assert the row converged), or carries a written reason why it cannot matter. The mechanism must be enforced like its siblings: a declared `verified_by` that must exist, a pinned body digest, and a fail-closed self-test direction, so a future port cannot be added without evidence.


- [x] **C48 [P0] NEW, found by C45's transfer question: a FOURTH outcome applier has the same asymmetry, and unlike C45's it is REACHABLE.** - **DONE 2026-09-23, commit 611685f0f** (`fix(sync): classify duplicate-id replays in the PG daemon push applier`; platform/sync/src/pg_daemon.rs +138/-39, pg_daemon_tests.rs +174). Same extraction as C45 for the same structural reason - the inline loop moved out of the `spawn_blocking` closure into a named free function at `pg_daemon.rs:675`, because `PgTransport` is a concrete struct so the applier was otherwise reachable only by standing up a live PostgreSQL. Per-item failures are still logged and swallowed, not propagated (the signature returns `()` deliberately, mirroring `daemon::apply_push_results`), so one unwritable row cannot abort the rest of the batch. **Four acceptance results:** duplicate-id replay -> `synced` (PROVEN to fail pre-fix, and it was the ONLY failure); genuine rejection (`pg insert failed: deadlock detected`) -> `failed` with `last_error` preserved verbatim; `Conflict` -> `synced` WITH the `resolved: conflict` marker, asserting it still routes through `queue.apply_push_conflict` rather than being blanket-marked; and the ANCHORING case (`pg insert failed: duplicate id: retried` -> `failed`), which is the guard against an over-broad reading since the predicate is `starts_with`. Two extra invariants pinned: `Accepted` -> synced, and truncated results leave trailing items `pending` rather than inheriting a neighbour's outcome. `cargo test -p platform-sync pg_apply_push_outcomes` -> 6 passed; `queue` -> 88 passed. **THE ENUMERATION IS THE REAL DELIVERABLE - every `PushOutcome` consumer, five rows:** (1) `sync_client.rs:310` predicate YES, live (tablet, bridge); (2) `daemon.rs:261` YES, live (desktop SyncDaemon); (3) `lib.rs:229` YES but **`run_sync_cycle` has no production caller** so it is not live; (4) `pg_daemon.rs:730` YES as of this commit, live (desktop PgSyncDaemon); (5) `apps/cloud-server/src/sync_api.rs:342-345` the metrics classifier, YES but as a HAND-WRITTEN LITERAL `reason.starts_with("duplicate id:")` rather than the shared constant - not a defect (it mutates no row) but the same divergence one layer up, and worse because the prefix's producer (`sync_store/pg.rs:107`) and its classifier live in different files, so renaming the constant would silently desynchronise them. Filed as C50. **AND IT FOUND A GENUINE SHORT-CIRCUIT while enumerating:** `sync_client.rs:463` and `:524`, the `#[cfg(not(feature = "sync-http"))]` stubs of `send_items_to_server{,_blocking}`, return `Ok(vec![PushOutcome::Accepted; items.len()])` **without performing any network call** - comment: `// Pretend all items were accepted when HTTP is compiled out.` Fed to any applier, that marks every queued mutation synced and destroys it locally, silently. **Reachability checked rather than assumed: LATENT** - `sync-http` is a DEFAULT feature and no workspace member disables default features, so it is compiled out of every shipping build; it is one `--no-default-features` build away from being a data-loss bug. Filed as C51, at P1 anyway, because a stub whose job is to pretend success is indistinguishable to every caller from a real successful push - the same shape as C44's silent retention failure and C36's inert axis. **Contention note from the worker:** another agent's workspace-wide `cargo fmt` reformatted its test region between read and edit; it re-read rather than forcing, committed only its two pathspecs, and `git show --stat` confirms exactly those two files.
  ORIGINAL BRIEF: `platform/sync/src/pg_daemon.rs:373` routes `Rejected` straight to `mark_offline_failed` with **no** `is_duplicate_id_rejection` guard, while its own `Conflict` arm DOES use the shared `apply_push_conflict` service. And `PgSyncDaemon` **is wired into the desktop** (`apps/desktop-tauri/src/state.rs:69`), unlike `run_sync_cycle`, which has no production caller at all. So this is the live version of the defect C45 fixed on a dead path.
  Fence: `platform/sync/src/pg_daemon.rs` (the outcome-application region only) plus its sibling test file. Do NOT touch `lib.rs` (just fixed, cc21a1c6a), `sync_client.rs`, `daemon.rs` or `queue.rs` - read them as the reference implementations; the predicate is `kasirmu_core::sync_client::is_duplicate_id_rejection`, already `pub`, already called fully-qualified from `daemon.rs:259`, so this needs NO visibility change and NO Fence_Request. Use it - there are currently one definition and three callers and this makes four.
  Acceptance: (1) a `Rejected` outcome whose reason carries the duplicate-id prefix marks the row SYNCED on THIS applier - reachable from a test without standing up an HTTP server (the C45 worker had to extract the inline loop into a named function to make its applier testable because `SyncTransport` is a concrete struct, not a trait; expect the same shape here and do it if needed); (2) a genuine rejection still marks failed; (3) the `Conflict` arm is unchanged - assert it still routes through the shared resolver. Prove (1) fails pre-fix. Also add the ANCHORING case: `is_duplicate_id_rejection` uses `starts_with`, so a reason that merely MENTIONS a duplicate id (`"validation failed: duplicate id: suffix"`) must stay a rejection rather than be misread as a replay - C45 added exactly that test and it is the one that catches an over-broad predicate.
  Also state in your report whether the server-side 90-day retention horizon (`apps/cloud-server/src/prune.rs:19-26` deletes `offline_queue` rows after it) creates any case where a REJECTED item's id is no longer a duplicate on the server - i.e. whether the predicate can go stale in the other direction.
  VERIFY: `cargo test -p platform-sync` narrow filters on the applier and the queue. CONTENTION RULE: eleven workers have failed today, every one at a cargo step. Narrow target, retry ONCE on a lock or rust-lld permission failure, never widen, never two cargoes at once; if one retry does not produce a clean build, report STATUS: Partial and stop.

- [x] **C47 [P1] NEW, found by the C41 rule the moment it was built: the tablet's License Activation screen calls desktop-gated commands.** - **DONE 2026-09-23, commit 464782f53** (`ui/src/features/auth/LicenseActivationScreen.tsx`, its test, `scripts/ipc-parity-allowlist.json`). **FENCE NOTE, RETROACTIVELY APPROVED:** the brief named `ui/src/features/licensing/`, which does not exist; the screen is `ui/src/features/auth/LicenseActivationScreen.tsx` - the file the brief's own evidence cited via `TabletAppShell.tsx:285`, so the worker went there. That is the FOURTH stale-pointer correction of this wave (after routes/products.rs, products/adjust.rs and offline.rs:421-431). **SHAPE: both halves, because neither alone is honest.** (1) The key tab is NOT OFFERED on the tablet (`{!isTabletShell() && <button role="tab">License Key</button>}`) - a tab an operator can open, fill in, and then watch fail is worse than one that is not there - while the pairing tab beside it stays, since that is the tablet's real activation surface and already the mode the initial state selects on that shell. (2) The three calls are GUARDED, returning `null` for 'not attempted on this shell', deliberately DISTINCT from `false` = attempted and refused - the same honest-undefined shape as C40, so a tablet reports nothing rather than inventing a failure. The calls sit inside that brace on purpose: it is what the parity leg reads, so lifting them back out is caught by the gate rather than by a licensing outage. **Option 3 (register for the tablet) was VERIFIED rather than assumed to be wrong:** the tablet's `commands::license::` surface is `get_license_status` / `check_license_status` only, its activation path is `start_device_pairing` / `poll_device_pairing`, and `activate_license` takes a machine id and hardware fingerprint the tablet has no use for - so registering it would INVENT an activation surface. **Before/after: `info[tablet-shellblind]` 9 -> 6, desktop 0 -> 0, `shell_blind` entries 9 -> 6, and the `tablet` section UNCHANGED at 129** - the three fixed names left `shell_blind` because the leg stopped reproducing them (the section is self-clearing, which is the mechanism working), while the `tablet` entries stay because a desktop-only screen legitimately calls them. The surviving six entries' reasons and the section comment were rewritten to state the new reality, including an explicit instruction not to silence an unfixed hit. **Evidence:** the screen's vitest -> exit 0, **65 passed** (was 62; +3, and test 63 pins the desktop in the OPPOSITE direction so a fix that disabled activation everywhere cannot pass); `verify-ipc-parity.py` -> exit 0; `tsc --noEmit` -> exit 0. **HONEST DISCLOSURE WORTH KEEPING:** the vitest cases alone do NOT bind the guard - with the tab hidden there is no submit button, so test 62 passes either way (measured: mutation -> 65 still passed). **The guard's discriminator is the parity gate**, which is now permanently wired to it: removing the `isTabletShell()` guard while keeping the tab hidden makes the checker exit 1 with 3 FAIL lines naming all three commands at their exact call sites. The vitest case was kept because it pins the user-visible half. **RECURRING HAZARD FLAGGED, and it is the second time:** writing the allowlist through Python re-introduced CRLF; the worker detected and normalized it, and verified the committed blob is LF and byte-identical (`git hash-object` == `git rev-parse :path`). See C52. Remaining six `shell_blind` entries recorded, not fixed, as instructed. No cargo started, per the rule.
  ORIGINAL BRIEF: `LicenseActivationScreen` is mounted by the tablet shell (`ui/src/app/tablet/TabletAppShell.tsx:285`) with its key-vs-pair tabs UNGATED, so a tablet user who reaches the License Key tab hits `activate_license` / `get_machine_id` / `get_hardware_fingerprint` - the same swallowed-rejection shape as C40, found by the same rule, on a screen an operator can actually reach. The other six hits the rule produced are calls in files that run on the tablet but whose handlers sit behind `actionsAvailable`, so they are recorded rather than fixed.
  Fence: `ui/src/features/licensing/` (the screen and its tabs) plus its test file, and `scripts/ipc-parity-allowlist.json` if the entry's reason changes. Do NOT touch either shell's command file, and do NOT simply delete the allowlist entry - the entry exists because a desktop-only screen legitimately calls these; the fix is to make the TABLET not call them.
  Done when: the tablet reaches a coherent state on that screen - either the key tab is not offered on the tablet, or it is offered and the call is not made, or the commands are registered for the tablet. State which you chose and why, and make the allowlist reason reflect it. The parity checker must still exit 0.
  VERIFY: the screen's vitest file plus `python scripts/verify-ipc-parity.py` (exit 0). No cargo needed - do NOT start one for a UI-only slice; cargo is the wave's bottleneck.

- [x] **C41 [P1] NEW, found while fixing C40: the IPC parity checker is SHELL-BLIND, so a command registered on desktop only counts as satisfied for the tablet too.** - **DONE 2026-09-23, commit 78e55701d** (`test(ipc): catch the shell-blind IPC call the parity checker could not see`; scripts/verify-ipc-parity.py +409, scripts/ipc-parity-allowlist.json +53/-14). **MY PREMISE WAS WRONG AND THE WORKER MEASURED IT RATHER THAN ACCEPTING IT:** `main()` ALREADY builds `missing[shell]` per shell and reports per shell, so the forward leg was never shell-blind - proved by deleting the four backup names from the allowlist's `tablet` section and watching the leg print all four, correctly attributed to `apps/mobile-tauri/src/lib.rs`. **What actually hid C40 was the ALLOWLIST:** `get_backup_status`, `get_backup_status_scoped`, `create_backup` and `create_backup_scoped` all sat in the `tablet` section, and an exemption that is CORRECT for a desktop-only screen and WRONG for a shared one reads identically in that file. The exemption was the blind spot, not the comparison. **The rule was chosen by MEASURING six candidate discriminators on the real tree** - entry-point import closure 46 flags (wolf-crier), unguarded call in a shell-aware file 88-116, call site outside a shell-guarded region 116, only files proving tablet execution 1 flag (misses C40) - and the survivor is: *an allowlisted gap, called from a file that branches on the shell discriminator, at a call site not inside a shell-guarded region, in a file that also calls a command this shell does register* -> **9 flags, all real**. It also built a render-gate exemption and REMOVED it after measuring that it swallowed all 9 real hits, because `authMode` (itself derived from the discriminator) appears throughout those render trees - a false positive is answered by an allowlist entry with a reason, not by weakening the rule. **Both runs:** real tree exit 0 (`IPC parity: OK`, `info[tablet-shellblind]: 9 (9 allowlisted as shell_blind)`, desktop 0); the C40 fixture reconstructed from the pre-fix file produces **11 findings** including `get_backup_status -> useBackupStatus.ts:92` and `get_backup_status_scoped -> :91`, which are ABSENT on the real tree; emptying `shell_blind` makes the checker **exit 1** with 9 FAIL lines. A permanent self-test (case 24, 5 assertions) finds the shape, names file+line, CLEARS it when the call is guarded (differential, not constant), excludes a non-branching file, and refuses to treat an `import` of a wrapper as a call. **Its stated limits:** a file that never branches on the discriminator is not examined (7 of ~470 runtime files); the guard is syntactic so an indirectly-expressed guard is a false positive; only names already in a shell's allowlist section are graded. **And it disclosed a side effect it did not intend:** its `json.dumps` round-trip also removed three `dev_mock` entries (`link_device_google`, `link_device_email_request`, `link_device_email_consume`) that were ALREADY failing at HEAD because another lane's commit `27a2bc40e` added the handlers - removing them is what took the tree from exit 1 to exit 0, but it edits a section another lane owns, and if that lane intended to keep them the correct fix is theirs. **That disclosure is cross-lane information I am carrying forward rather than burying.** Its finding produced C47 (the tablet LICENSE screen, 3 of the 9) and it left a PRE-EXISTING self-test failure alone after verifying it fails on unmodified HEAD too.
  ORIGINAL BRIEF: The C40 defect survived every existing gate because the checker compares a UI invoke against the union of registrations rather than against the registrations of the shell that will actually receive the call. Concretely: `getBackupStatus` / `getBackupStatusScoped` are called from SHARED UI (`ui/src/features/settings/hooks/useBackupStatus.ts`) and registered ONLY on desktop (`apps/desktop-tauri/src/lib.rs:964-965`); the tablet registers none of them (`git grep -nE 'fn (create_backup|get_backup_status)\b' -- apps/mobile-tauri/` returns nothing; the tablet's only backup registration is `commands::data::create_backup_to` at `lib.rs:651`). The parity surface therefore reported satisfaction, the tablet gap was invisible, and the runtime consequence was a spurious error toast on every mount of the Data tab plus a permanently failed panel.
  Fence: the IPC parity checker (find it - the review records it as `scripts/verify-ipc-parity.py`, with `UI_SCAN_DIRS` at `:83` and `__tests__` exclusion at `:149`/`:274`) plus its own test or fixture. Do NOT touch the UI or either shell's command file.
  **The hard part is deciding what the checker CAN know.** Some shared UI legitimately targets only one shell - a screen registered only in the desktop nav, or only in the tablet nav - so a naive 'every invoke must be registered by every shell' rule would produce false failures, and a gate that cries wolf gets disabled. Establish first whether the caller's screen is reachable from each shell: the C40 worker already demonstrated the trace that decides it (a page is registered in `register.tsx`, seeded into a workspace in `crates/kasirmu-core/migrations/20260813_init.sql:1454`, and rendered by that shell's app shell via `getPage(currentRoute)`). If that reachability cannot be derived reliably, say so with the reason and deliver the NARROWER rule that IS reliable, labelled with what it does not catch.
  Done when: the checker FAILS on the C40 shape reproduced as a fixture (a shared-UI invoke whose command is registered by only one shell) and still PASSES on the current tree once C40's fix is in place, with both runs recorded. Prove the failure direction by construction, not by description.
  VERIFY: run the checker on the real tree and on the fixture; paste both exit codes and tails. No cargo needed.


- [x] **C49 [P1] NEW, found by C20: the queue-ordering rule now exists in FOUR places, and the shared home for all of them is `kasirmu_core::offline`.** - **DONE 2026-09-23, commit 95f77a322** (`refactor(core): collapse the queue-ordering rule into one shared expression`; 8 files, +275/-178). **THE COUNT WAS FIVE, NOT FOUR - my brief missed one:** `crates/kasirmu-bridge/src/offline.rs:355`, the desktop `retry_offline_sync_scoped` path. Full census before -> after: `platform/sync/src/lib.rs:157` (priority-only) -> shared; `platform/sync/src/daemon.rs` `sort_pending_for_push` -> shared; `crates/kasirmu-bridge/src/sync.rs` `order_pending_for_push` -> shared; `apps/mobile-tauri/src/commands/offline.rs:342` (tablet) -> shared; and **`crates/kasirmu-bridge/src/offline.rs:355` STILL A COPY, outside the fence** - filed as C49b. One test-site copy also collapsed (`db/offline_tests.rs:510`). **The one shared expression is `kasirmu_core::offline::order_for_push`**, and `grep` for priority sorts now returns exactly ONE hit: the out-of-fence fifth copy. **THE DETERMINISM TEST IS THE ONE THIS ITEM EXISTED FOR:** it seeds **12 items sharing ONE priority tier and ONE millisecond**, so `priority` and `created_at` compare equal for every pair and only `id` can decide, then asserts the same order across **12 independent rotations** of that set - asserting *'the same permutation every time'* rather than a hard-coded one. That is precisely the case a priority-only sort gets wrong. The key is preserved exactly: **`(priority, created_at, id)` ascending**, reordering the caller's `&mut` slice IN PLACE, because a parallel sorted copy would mis-attribute every outcome - the push and the per-item apply iterate the same vector. **C20's NEGATIVE CONTROL RE-PROVEN AFTER THE REFACTOR:** commenting out the sort on the daemon still yields `left: ["bulk", "money", "catalog"]`, the Low item leading a Critical one. **AND IT CLOSED C20's OWN CALL-SITE GAP, which C20 had reported it could not afford:** the primitives already existed (`TestBridge::token_granting` for a real `sync:manage` grant, `TestBridge::ctx()`, and the tablet's raw-`TcpListener` fake-server pattern), so no mock-HTTP crate was needed - which mattered because `kasirmu-bridge` has no dev-dependency on one. The new test asserts the **order of the HTTP request body**, and deleting `order_for_push(&mut pending_items)` fails it with `left: ["bulk", "money", "catalog"]`. The three old comparator tests were DELETED rather than duplicated - the comparator is now tested once in core - and `kasirmu-bridge` went 1385 -> 1383 tests for exactly that reason. **Verify, all four targets green:** `cargo test -p kasirmu-core offline` exit 0; `cargo test -p platform-sync` **427 passed / 0 failed**; `cargo test -p kasirmu-bridge` **1383 passed / 0 failed** (~758s, background job); `cargo test -p kasirmu-mobile offline` **20 passed / 0 failed**. **Two honest gaps kept:** the TABLET call site has no call-site test (its command needs `AppState`, and the only harness that builds one for sync lives in `commands/sync_tests.rs`, outside the fence and covering a different command) - so the tablet is wired to the shared helper and compiles, and it is the same one-line shape as the bridge's now-proven call site, but that is inference rather than a test; and the fifth copy means 'one expression called by all paths' is not yet literally true, only 'one expression called by every path inside the fence'.
  ORIGINAL BRIEF: Counting what C20 DISCOVERED rather than what it created: `platform/sync/src/lib.rs:150` `build_batches` already sorted by priority and the third path (`SyncEngine`) already used it - **that path was correct and was left alone** - but it is private to the batching loop, it is **priority-only so NOT total**, and `crates/kasirmu-bridge` **cannot reach it**: `crates/kasirmu-bridge/src/sync.rs:6` states verbatim that `platform-sync` is 'a crate this one does not depend on', confirmed by the manifest (`platform-core`, `platform-kernel` only). So C20 added `sort_pending_for_push` (daemon) and `order_pending_for_push` (bridge), each the identical comparator, and the tablet has a third copy - four in total, three of them now the same one-line expression.
  **The correct shared home is `kasirmu_core::offline`**, the one crate all three paths already depend on. Moving it there collapses the copies AND upgrades `lib.rs`'s priority-only sort to the total key, so it is a consolidation and a correctness fix in one change rather than a tidy-up.
  **The total key, as C20 established it and which must survive the move:** `(priority, created_at, id)` ascending. Priority alone is not total - `SyncPriority` has only three values and MOST items are `Critical` (every sale, void, refund and payment split passes `SyncPriority::Critical` at `db/offline.rs:391,463,489,538`), so ties are the COMMON case, and `created_at` is only millisecond precision, so same-millisecond items would fall back to SQLite's unspecified row order and flip run to run. `id` is a UUID v7 primary key - unique AND time-ordered, which makes it a correct final tie-break rather than an arbitrary one.
  **ORDERING IS LOAD-BEARING BEYOND SORTING:** the batch is sorted ONCE before the push and every downstream consumer iterates that same vector - `transport.push_items` sends it and `apply_push_results` / `apply_sync_outcomes` `zip` it against per-item outcomes. The server says so explicitly (`sync_api.rs:283`: a reordering would mark the WRONG items as synced or failed) and reassembles outcomes in request order for that reason. A shared helper must therefore reorder the same vector, not build a parallel one.
  Fence: `crates/kasirmu-core/src/db/offline.rs` + its sibling test file, `platform/sync/src/daemon.rs` + `daemon_tests.rs`, `platform/sync/src/lib.rs` (the batching sort), `crates/kasirmu-bridge/src/sync.rs` + `sync_tests.rs`, and the tablet path in `apps/mobile-tauri/src/commands/offline.rs` - the ONE slice where touching the third shell is the POINT rather than scope creep. Do NOT touch `platform/sync/src/queue.rs` (per-item, order-independent, settled).
  **ALSO CLOSE C20's OWN STATED GAP, because the same test work covers it:** the bridge tests pin the COMPARATOR and not the CALL SITE - deleting `order_pending_for_push(&mut pending_items)` from `sync_run_scoped` leaves them green. The daemon test DOES cover its wiring because it goes through `read_config_and_pending`. Closing that for the bridge needs a `BridgeCtx` plus a mock HTTP harness the file does not have; use it if a shared helper makes the call-site assertion cheap, and if not, say so again rather than leaving the gap unstated.
  Acceptance: one shared expression called by all three paths; the total key proven by a test seeding same-priority same-MILLISECOND items and asserting a DETERMINISTIC order across runs; and C20's negative control preserved - commenting out the sort must still make a Low item lead a Critical one.

- [x] **C50 [P2] NEW, from C48's enumeration: the duplicate-id classification rule now has a FIFTH expression, on the server, as a hand-written literal.** - **DONE 2026-09-23, commit ab9f00fc9** (`apps/cloud-server/src/sync_api.rs` + its test file). **The conditional decision rule resolved to ONE-SOURCE-OF-TRUTH, with the condition settled by hard evidence rather than assumption:** `apps/cloud-server/Cargo.toml:20` already declares `kasirmu-core = { workspace = true }`, `sync_api.rs` already calls `kasirmu_core::` in that same file (`:224`, `:263`, `:285`, `:422`), and `is_duplicate_id_rejection` is `pub` and NOT feature-gated - so the honest minimal fix was to CALL THE EXISTING FUNCTION, with no duplication, no comment-as-contract and no new shared constant. The constant, the producer and the client predicate were all left untouched, as fenced. The classification was extracted into a private `push_outcome_label(&PushOutcome) -> &'static str` for the same structural reason as the three client-side appliers: an inline match arm is unreachable from a unit test, and the classification is the thing under test. **Both directions asserted on STRUCTURED values** (the label, not a formatted exposition string, and the classifier is called directly rather than through `/metrics` text parsing): a reason built FROM the constant labels `conflict`; ALL THREE non-duplicate reasons the handler can actually produce label `rejected` (`invalid id:`, `missing batch outcome for`, `pg insert failed:`); the ANCHORING case (`pg insert failed: duplicate id: retried` -> `rejected`) because the predicate is `starts_with`; and `Accepted`/`Conflict` labels unchanged. **ON THE 'prefer structured values' INSTRUCTION, IT MADE THE SHARPEST POINT OF THE SLICE:** the sharing itself has no RUNTIME value to assert - two independently-written literals that are equal today behave identically to one call and diverge only on the next reword, which is exactly the failure being closed, so a runtime assertion provably cannot catch it. It pinned the coupling at the SOURCE instead (`include_str!`) and then PROVED THE PIN HAS TEETH: corrupting the expected value failed the test, and restoring the hand-written `starts_with("duplicate id:")` classifier also failed it - **while all three behavioural tests kept passing, which is precisely the silently-desynchronised scenario.** **Honest limitation and the remaining half:** the producer side (`sync_store/sqlite.rs:102`, `sync_store/pg.rs:107`) still builds its reason with `format!("duplicate id: {}", item.id)`, which C50's fence forbade changing, so the two ends are PINNED rather than UNIFIED - a reword of the constant now fails this test and NAMES the files that must move with it, instead of silently splitting the metric from the thing it measures. **That remaining half is queued as C50b.** Fence integrity: `git status` on `sync_client.rs` and `sync_store/` is empty. **Contention handled per the rule:** its first `cargo test --bins` died in the linker (`rust-lld.exe`, exit `0xffffffff`); it confirmed via `cargo check --all-targets` that there were NO compile errors before retrying once, which succeeded - it did not retry blindly and did not widen the target.
  ORIGINAL BRIEF: `apps/cloud-server/src/sync_api.rs:342-345` classifies `Rejected` for Prometheus labels with `reason.starts_with("duplicate id:")` instead of using `DUPLICATE_ID_REJECTION_PREFIX`. It is NOT a defect - it mutates no row, it only labels metrics - but it is the same 'three copies of a classification rule' divergence one layer up, and it is worse than the client copies because the prefix's PRODUCER and its classifier live in different files (`apps/cloud-server/src/sync_store/pg.rs:107` constructs the `duplicate id:` string; `sync_api.rs:333/337` constructs `invalid id:` and `missing batch outcome for`), so renaming the constant would silently desynchronise them.
  Fence: `apps/cloud-server/src/sync_api.rs` (the metric label region only). Done when the classifier uses the shared constant (or a server-side equivalent documented as such), with a test that the label still appears for a duplicate-id rejection and does not for a genuine one.

- [x] **C51 [P1] NEW, from C48's enumeration, and it is a genuine short-circuit of exactly the kind this review was written about: a function that PRETENDS SUCCESS.** - **DONE 2026-09-23, commit 6347f3ee3** (`fix(core): make the sync-http-disabled push stub fail instead of faking Accepted`; sync_client.rs +82, sync_client_tests.rs +73). **THE CHOICE IS THE ERROR, AND THE STRONGEST EVIDENCE FOR IT WAS INSIDE THE SAME FILE:** four sibling disabled stubs ALREADY return `Err(SyncHttpError::Client("sync-http feature is disabled"))` - `ack_memo_on_server` (:657), `fetch_active_memos_from_server` (:812), `qris_charge_on_server` (:922), `qris_status_from_server` (:978) - plus `sync_pull::fetch_snapshot_from_server` (:247). So this was not a new shape imposed on the crate; it is the ESTABLISHED HOUSE RULE, and the push path was the LONE EXCEPTION. **Worse, and it is this review's thesis in miniature: `fetch_active_memos_from_server`'s own doc comment already NAMED the divergence in prose** - 'a read has no honest success stub, unlike the push path's pretend-accepted' - so the anomaly was documented, understood, and left standing. **NO CALLER RELIED ON PRETEND-SUCCESS, checked rather than assumed:** all seven `send_items_to_server` call sites match on the `Result` and already have an `Err` arm that does the right thing unchanged (mobile `offline.rs:351`, bridge `offline.rs:365`, bridge `sync.rs:694` and mobile `sync.rs:260` route `Err` to `mark_all_failed`, leaving the row in the queue as `failed` with the reason in `last_error` and counted in `failed_count`; mobile `lib.rs:466` logs and leaves rows `pending`). The error therefore converts a SILENT LOCAL DESTRUCTION into the existing loud, diagnosable path, and no test asserted the fake `Accepted`. **BOTH DOORS CAN NO LONGER DIVERGE:** rather than giving the two stubs the same body, the disabled-path decision became a single function `push_outcomes_without_http` that both delegate to - two identical bodies is the copy-drift this workstream keeps finding; one callee is not. **AND IT HANDLED THE UNTESTABLE HONESTLY:** the feature genuinely cannot be disabled to test this - verified, not assumed: `cargo check -p kasirmu-core --no-default-features` fails with `E0433: cannot find module or crate 'reqwest'` (ungated reqwest in `license_verification.rs`/`sync_auth.rs`, which the crate documents itself at `attestation.rs:206`). So it used a `#[cfg]`-mirrored SHARED-CALLEE test rather than a copy: the helper is `#[cfg(any(not(feature = "sync-http"), test))]`, so the test build compiles and exercises THE SAME FUNCTION the disabled build calls. A second test pins the DELEGATION ITSELF by reading the source through `include_str!` and asserting the disabled async stub contains `push_outcomes_without_http()` and does NOT contain `PushOutcome::Accepted` - so a future edit reintroducing a local `Ok(...)` fails even though the stub body can never execute in a runnable build. **Evidence:** `cargo test -p kasirmu-core sync_client` -> **44 passed, 0 failed**; falsification restored the pre-fix body and BOTH new tests failed; `cargo check -p kasirmu-core` clean with no warnings (the helper's cfg gating was added specifically to clear a `never used` warning under `-D warnings`); `grep -rn 'Pretend all items' crates/` now returns nothing. **Follow-up filed as C53:** the sibling stubs still inline the message literal (five copies, six counting sync_pull's variant).
  ORIGINAL BRIEF: `crates/kasirmu-core/src/sync_client.rs:463` and `:524` - the `#[cfg(not(feature = "sync-http"))]` stubs of `send_items_to_server{,_blocking}` - return `Ok(vec![PushOutcome::Accepted; items.len()])` **without performing any network call**, with the comment `// Pretend all items were accepted when HTTP is compiled out.` Fed to any of the four appliers, that marks every queued mutation `synced` and destroys it locally, silently.
  **Reachability, checked rather than assumed: LATENT.** `sync-http` is a DEFAULT feature (`crates/kasirmu-core/Cargo.toml:47`) and no workspace member disables default features (`kasirmu-core = { workspace = true }` in desktop, mobile and bridge), so it is compiled out of every shipping build. It is a footgun, not an active data-loss bug - and it is one `--no-default-features` build away from being the latter.
  **Why it is P1 anyway: the failure mode is silent and total.** A stub whose job is to pretend success is indistinguishable, to every caller, from a real successful push - there is no error, no log, no metric. This is the same shape as the audit-retention divergence (C44) and the inert quota axis (C36): a mechanism that reports success while doing nothing.
  Fence: `crates/kasirmu-core/src/sync_client.rs` (the two stub bodies only) plus its sibling test file. Do NOT change the feature wiring or the manifest.
  Done when the stub either returns an ERROR (so a build without HTTP fails loudly instead of destroying queued items) or is documented at the call site as unusable, with a test asserting the chosen behaviour. Prefer the error: a compile-time configuration that cannot sync should not be able to report that it synced. State which you chose and why.

- [x] **C52 [P2] NEW, a workflow hazard that has now bitten twice: rewriting `scripts/ipc-parity-allowlist.json` through Python re-introduces CRLF.** - **DONE 2026-09-23, commit 85786cce4** (`scripts/verify-ipc-parity.py` only). **MY PREMISE WAS WRONG AGAIN, AND THE WORKER CHECKED IT BEFORE BUILDING:** the by-construction fix was ALREADY IN HEAD before either incident - `write_allowlist_payload` has opened `NamedTemporaryFile("w", encoding="utf-8", newline="\n", ...)` since `baa025f77` (2026-09-13), while C41 (78e55701d) and C47 (464782f53) are 2026-09-23. **The writer never re-introduced CRLF; the two lanes hit it by rewriting the file through AD-HOC Python that BYPASSED the helper.** So 'fix it by construction' was already satisfied, and the genuinely missing half was the READ-BACK ASSERTION the brief also demanded. **Direction (1), byte-neutral rewrite observed rather than assumed:** running the real writer flag against the real file gave before = after = `69baa65691e48c761ede20bca063e2ce6341556b`, `git hash-object` == `git rev-parse :path`, `git diff --quiet` exit 0, 35545 bytes / **0 CR** / 268 LF. **Direction (2), the pre-fix path reproduced:** `Path.write_text(text, encoding="utf-8")` with no `newline=` on a temp copy produced **35813 bytes / 268 CR** against the fixed 35545 / 0 CR - `byte-identical: False` with `differing TEXT lines: 0`, i.e. a whole-file byte difference invisible to every payload assertion. Independent corroboration: the stale index stat for the real file recorded `size: 35813`, exactly the CRLF length. **The guard is proven END TO END rather than argued:** `assert_allowlist_bytes(path, written)` is called TWICE - on the sibling temp BEFORE `os.replace` (fail-safe) and on the target AFTER it (proof) - and sabotaging a copy's write path to `newline="\r\n"` makes the real CLI print `... did not come back as the bytes this run wrote: 35545 bytes written, 35813 on it now (268 CR there, 0 CR written)` and exit **2** with the target's hash UNCHANGED, no sibling temp left, and 0 CR. So a regressed write path is refused BEFORE the rename lands, which means the shared file another lane owns cannot be corrupted - and it refuses rather than normalises, which is the post-pass the brief forbade. Self-test grew 24 cases/167 assertions -> **26 cases/177 assertions**, all 10 new ones passing; `verify-ipc-parity.py` exit 0, `verify-scoped-reads.py` exit 0. **THREE THINGS IT INSISTED MUST NOT BE LOST, all filed:** (a) **TWO PRE-EXISTING SELF-TEST FAILURES are not its own and are not fixed** - `fallback the real tablet tree exposes the shape the leg was written for [n=0 names=[]]` and `case 24 real tree: the leg still sees the nine shared-UI calls it found first run`; it ran HEAD's unmmodified script from a temp probe and got the IDENTICAL 2 failures, so they are pre-existing in the C41 tablet-reachability leg - **and this self-test is NOT WIRED INTO CI** (`dev-ci.yml:694`, `scripts/check.sh:71` and `run-pre-push.py:252` all invoke it WITHOUT `--self-test`), which is why it can sit red unnoticed. Filed as C55, including the decision that wiring it would fail CI today. (b) **`--write-dev-mock-gaps` REORDERS the `dev_mock` section - a real CONTENT bug, not a line-ending one:** `merge_section_entries` returns `sorted(merged)` while the committed section is not in sorted order, so a 'no-op' reseed relocates `create_backup_to` and its 3-line reason for a 4-line content diff. It caught this mid-proof, restored the file BYTE-IDENTICALLY from a backup and re-verified the hash, and deliberately did NOT fix it because canonical ordering is a separate owner decision and another lane rewrote those entries on purpose. Filed as C56. (c) A ` M` on the allowlist with identical bytes was a `text=auto` STAT-CACHE artifact (`git diff-files --raw` reporting worktree hash `000...0`), cleared with `git update-index --refresh` - worth knowing because it is the same phantom-dirty class that cost a worker a round earlier this wave.
  ORIGINAL BRIEF: The C41 worker disclosed it as a side effect the first time; the C47 worker hit it again and had to detect and normalize it. Both were caught, both by a worker who checked `git hash-object` against `git rev-parse :path` rather than trusting the write. The file is tracked LF; `json.dumps` on Windows with default newline handling is the cause. This matters beyond tidiness: a CRLF-only diff on a shared JSON file is the same artifact that made a generated PG file look dirty across several scans this wave, and one of those checks cost a worker a round.
  Fence: `scripts/ipc-parity-allowlist.json` or the script that writes it, plus whichever parity script performs the rewrite. Done when the rewrite path preserves LF BY CONSTRUCTION (e.g. open with `newline=""` and write exact bytes) with an assertion that the produced bytes match on re-read. Do NOT reformat the file's contents - this is about the bytes of the write, not the shape of the JSON.
- [ ] **C53 [P2] NEW, from C51: the `"sync-http feature is disabled"` message is inlined in FIVE stubs, six counting sync_pull's variant.** Same class as C50 - one rule expressed in many places - and C51 showed the mechanism that fixes it: make the disabled-path decision a single callee the stubs delegate to, rather than identical bodies. The four siblings (`ack_memo_on_server`, `fetch_active_memos_from_server`, `qris_charge_on_server`, `qris_status_from_server`) plus `sync_pull::fetch_snapshot_from_server` each carry their own copy, so a change to the wording, the error type or the advice given to an operator must be made five or six times - and one of them will be missed.
  Fence: `crates/kasirmu-core/src/sync_client.rs` and `crates/kasirmu-core/src/sync_pull.rs` (the stub bodies and their messages only) plus their sibling test files. Done when a single expression produces the disabled-capability error and every stub delegates to it, with the C51-style `include_str!` delegation pin extended to the siblings so a future edit cannot re-inline one. Note `sync_pull`'s message is a VARIANT - decide whether it should unify or stay distinct, and say why.

- [x] **C49b [P2] The fifth queue-ordering copy, outside C49's fence.** - **DONE 2026-09-23, commit 09c62b09d** (`crates/kasirmu-bridge/src/offline.rs` + its test file; the shared helper untouched and none of the four already-converted paths re-opened). **The acceptance WAS the grep, and it returns ZERO copies outside the shared helper:** `grep -rnE 'sort_by_key\([^)]*priority|sort_by\([^)]*priority' --include=*.rs crates/ apps/ platform/ modules/ foundation/ | grep -v 'crates/kasirmu-core/src/offline.rs'` -> **COUNT: 0**, with `pub fn order_for_push` at `crates/kasirmu-core/src/offline.rs:212`. **AND THE INTEGRITY MOMENT IS WORTH RECORDING PRECISELY:** its first census returned **1**, and the hit was in **its own new test's doc comment**, where it had quoted the deleted line verbatim (`pending_items.sort_by_key(|i| i.priority)`). Because the grep count IS the acceptance, it REPHRASED THE COMMENT to describe the removed rule rather than reproduce it - and it said why: *'the alternative, adding an exclusion for my own file, would have been rigging the measurement.'* That is the difference between a green result and a true one, and it was one exclusion away from the wrong side. It also swept `.sort(`, `.sort_by(` and `.sort_by_key(` over the queue paths and found only a CRDT Lamport-clock comparison and test-local `Vec` sorts, none of which order the transmit queue. **(2) The existing request-body ordering test still passes** (`sync_run_scoped_pushes_critical_before_an_earlier_low_item` -> 1 passed, exit 0). **(3) The C20 negative control had to be PROVEN on this path, which had no call-site test at all, so one was added** - `retry_offline_sync_scoped_pushes_critical_before_an_earlier_low_item` asserts the ORDER OF THE HTTP REQUEST BODY captured from a raw loopback socket (same pattern as the sibling test; this crate has no mock-HTTP dev-dependency), enqueuing Low -> Critical -> Normal so the un-sorted `ORDER BY created_at ASC` read puts Low first. With the shared call commented out it fails exactly as C20 requires: `the REQUEST BODY must carry Critical before Normal before Low`, `left: ["bulk", "money", "catalog"]`. Restored and re-verified: offline filter 10 passed, sibling filter 1 passed. **A REAL BEHAVIOUR CHANGE ON TIES, stated rather than slipped in:** the bridge path now orders by `(priority, created_at, id)` instead of `priority` alone - intended, because `created_at` is millisecond-precision and most items are Critical, so the old copy left same-millisecond items to SQLite's unspecified row order. The in-place `&mut` call is preserved so the push and the per-item apply keep iterating the same index-aligned vector. **AND THE TABLET QUESTION WAS ANSWERED (b), PLAINLY, WITHOUT MANUFACTURING A TEST:** the tablet's `retry_offline_sync_scoped` (`apps/mobile-tauri/src/commands/offline.rs:298`) is **NOT** a delegate to the bridge function - it is an INDEPENDENT ~80-line body talking to `AppState` directly, while the desktop's command (`apps/desktop-tauri/src/commands/offline.rs:105`) is the one that delegates. So a test here covers the desktop wiring for free and does NOT cover the tablet, and the tablet's call site remains untested because its harness lives outside the fence. **It did not widen the fence and did not file a Fence_Request, correctly noting the conversion itself is complete.** It also flagged, as an observation rather than a defect it touched, that the tablet ALREADY calls the shared helper (`:348`) - so it is already CORRECT, merely UNVERIFIED. **That observation is a real finding and is filed as C54.** No linker failure occurred, so no `cargo check` fallback was needed; no two cargoes ran concurrently; fmt clean on both files, and the acceptances were re-run AFTER formatting rather than before.
  ORIGINAL BRIEF: `crates/kasirmu-bridge/src/offline.rs:355` - the desktop `retry_offline_sync_scoped` path - still carries its own priority sort, and it is now the ONLY remaining copy of a rule that exists once everywhere else (`kasirmu_core::offline::order_for_push`). One line, the same change as the other four. Fence: that file plus its sibling test file. Done when `grep` for a priority sort in the queue paths returns zero copies outside the shared helper.

- [x] **C54 [P1] NEW, from C49b's (a)/(b) answer: the tablet's `retry_offline_sync_scoped` is a SECOND IMPLEMENTATION of the desktop retry path.** - **DONE 2026-09-23, commit dc2b4dd8c** (`test(mobile): pin the tablet retry path's push order and its twin divergence`; `apps/mobile-tauri/src/commands/offline.rs` +7 doc-only, `offline_tests.rs` +147). MANAGER-RE-VERIFIED: git show --stat is exactly those two files. **THE DIFF WAS DONE FIRST, AS ORDERED, AND IT FOUND A LIVE BUG - see C59.** Verdict: they differ for a REAL reason and one difference is a defect, so DO NOT COLLAPSE. The step-by-step table: Phase 1 read is the same file on both paths; the permission gate is the same rule through a different seam; the config-missing error is the same message in a different error type; **Phase 3 WRITE IS A DIFFERENT FILE - the tablet locks `state.db`, the GLOBAL identity database, while Phase 1 read the STORE database**; the bridge re-resolves the store scope after its await (because its conn guard is not `Send`); ordering is the shared `order_for_push` on both; the return type is the same. So the tablet pushes successfully and then writes its outcomes to a database where those ids do not exist - `mark_offline_synced` returns NotFound, the `?` in `apply_sync_outcomes` aborts, the store's rows stay `pending`, and **every retry re-sends the same items forever**. **The worker REFUSED TO COLLAPSE, and the reasoning is the right call:** collapse is mechanically available (`AppState::bridge_ctx()` at `state.rs:450`, used by 20+ tablet commands) but changing which DATABASE a live bug writes to is a behaviour change on that bug's blast radius, not a refactor. **So it PINNED the behaviour instead: the new test asserts the command returns `AppError::Core { sub_kind: NotFound }` AFTER the request body proves the push succeeded - i.e. the outcomes went to a database where the ids do not exist - so when the collapse lands THAT assertion fails and whoever lands it deletes it deliberately.** It chose `NotFound` over a bare `is_err()` so the test names the mechanism rather than any error. **ACCEPTANCE, both proven:** the tablet CALL-SITE test (the one the bridge's test file cannot make, because the tablet command needs `AppState` and the bridge harness builds a `BridgeCtx`) binds a raw `TcpListener` loopback socket, captures the push body, and asserts the parsed array is `["money", "catalog", "bulk"]` - Critical before Normal before Low - from a queue seeded Low -> Critical -> Normal; `cargo test -p kasirmu-mobile offline` -> **21 passed, exit 0**. And the ordering key IS the shared rule, proven by the C20 control RUN rather than asserted: commenting out `order_for_push` fails it with `left: ["bulk", "money", "catalog"]`, restored -> 21 green. **NO FENCE REQUEST, AND IT EXPLAINS WHY:** the `AppState`-building harness lives in `sync_tests.rs` outside the fence, but the pattern was reproducible inside the FENCED sibling `offline_tests.rs` (mounted at `offline.rs:474`, already importing `AppState`/`migrations`), so the acceptance was achievable in-fence and no widening was warranted. **AND IT ATTRIBUTED THREE UNRELATED FAILURES TO ANOTHER SESSION BY PROOF RATHER THAN ASSERTION:** `cargo test -p kasirmu-mobile --lib` exits 101 with three `drift_pin_*` failures naming new debt `desktop_link::{login_with_email_password, request_email_login_code, verify_email_login_code}`; those three names appear **0 times at HEAD** and once each in the working tree (uncommitted registrations in `apps/mobile-tauri/src/lib.rs`, a dirty file from another lane), and it confirmed independence by reverting its OWN doc edit and re-running the gate - same three failures, exit 101. The ceiling arithmetic corroborates: 92 ungated against 89, a delta of exactly +3. It did NOT regenerate the ledger or raise the ceiling, because that would commit another session's in-flight work under its message.
  ORIGINAL BRIEF: `apps/mobile-tauri/src/commands/offline.rs:298` is an independent ~80-line body that talks to `AppState` directly (`state.resolve_scope`, `state.db.lock()`), while `apps/desktop-tauri/src/commands/offline.rs:105` delegates to `kasirmu_bridge::offline::retry_offline_sync_scoped`. So the two shells are two implementations of one operation, and the third (the shared bridge body) is used by only one of them. **This is the divergent-twins class this review opened with** - two builders of one value - and the immediate consequence is already visible: the tablet's call site has NO call-site test and CANNOT have one from the bridge's test file, which is why a sibling had to report it as unverified rather than wrong. The C49/C49b work just made the ordering rule single-source across four paths while this duplicate quietly implements the same operation again.
  Fence: `apps/mobile-tauri/src/commands/offline.rs` plus its sibling test file, and `apps/mobile-tauri/src/commands/sync_tests.rs` IF the honest fix needs the existing `AppState`-building harness (that file is the home of the pattern to follow, and reaching it may need a Fence_Request - file it rather than assuming). Do NOT change the bridge body or the desktop command.
  **FIRST, DIFF THE TWO IMPLEMENTATIONS AND REPORT THE DIVERGENCE BEFORE CHANGING ANYTHING.** The question is not 'make the tablet call the bridge function' - it is whether the two bodies can even be the same operation: the tablet may legitimately differ (different scope resolution, no session token vs a session token, a different DB handle). If they differ for a real reason, the finding is the DIVERGENCE ITSELF and the honest outcome may be a shared body with a shell-specific seam, or an explicit comment on each naming the other as its twin. If they differ for no reason, collapsing them is the fix. State which and why before writing code.
  Done when: either one body serves both shells with the divergence resolved, or each carries an explicit statement of what differs and why - plus a call-site test for the tablet's path using the loopback-socket pattern, so the tablet stops being correct-by-inspection.

- [ ] **C50b [P2] The remaining half of C50: the PRODUCER still types the literal.** `apps/cloud-server/src/sync_store/sqlite.rs:102` and `sync_store/pg.rs:107` build the reason with `format!("duplicate id: {}", item.id)` while the classifier now calls the shared predicate - so the two ends are PINNED (a reword fails a test naming both files) rather than UNIFIED. Fence: those two files plus the test that pins the coupling. Done when the producers build the reason from `DUPLICATE_ID_REJECTION_PREFIX` so one constant produces and classifies it, with the existing `include_str!` pin kept as the belt-and-braces check.

- [x] **C55 [P1] NEW, CONFIRMED BY THE MANAGER: the IPC parity self-test is RED on HEAD and is NOT wired into CI, which is why it sits unnoticed.** - **DONE (cases) 2026-09-23, commit 1ffd1904c** (`test(ipc): anchor the parity self-test to the mechanism, not a frozen population`; scripts/verify-ipc-parity.py +74/-27); **the CI wiring is a separate approved step, see below.** MANAGER-RE-VERIFIED: `python3 scripts/verify-ipc-parity.py --self-test` -> **`self-test: OK`**, 26 cases / 58 extra guards / 177 assertions / 0 FAIL, and the bare run still exits 0. **BOTH failing cases had gone stale TWICE FOR THE IDENTICAL REASON, and the worker named it exactly: 'a pinned name forbids the very repair it exists to encourage.'** `case 24` measured **6, not 9**, because C47 REPAIRED three activation call sites - a frozen `== 9` called a real repair a regression and forbade the leg from ever succeeding. The replacement asserts the MECHANISM, which is what the leg actually guarantees: (1) it still reaches the real tree (`len >= 1`, so a walk returning `{}` cannot pass the synthetic cases and hide); (2) every reported name is genuinely in that shell's gaps - the soundness property that matters, since this leg decides whether an allowlist entry hides a live call; (3) every name carries a `file:line#wrapper` site. **Strictly stronger than the count it replaced, which checked neither (2) nor (3)**, and the population now rides in the CASE NAME so a future red prints its own denominator and a reader can tell a repair (shrank) from a regression (grew). The second case was **NOT dead but MIS-ANCHORED**: the worker showed the leg is alive by measuring **4 witnesses against desktop and 0 against tablet**, so tablet-measuring-0 is a CLEAN RESULT rather than a blind leg; the frozen witness `list_products` was genuinely deleted by `b82373a80`. It is now measured on BOTH shells and re-anchored to the mechanism, and **NO CASE WAS DELETED**. **FALSIFICATION, so the new expectations are not vacuous:** neutering `no_token_fallbacks` to `{}` turns the fallback case red (`n=0 names=[] by_shell={'desktop': [], 'tablet': []}`), and making `shell_blind_findings` report a name outside `gaps` turns case 24 red (`n=7 names=['bogus_not_a_gap', ...]`). The other 24 cases and 58 guards are unchanged and genuinely passing. Allowlist untouched (`git hash-object` == `git rev-parse HEAD:` == `69baa656...`). **THE WIRING DECISION, now approved and dispatched as its own step:** the self-test is invoked by NO always-run gate (`dev-ci.yml:694`, `scripts/check.sh:71` and `run-pre-push.py:252` all call the script WITHOUT `--self-test`), which is precisely how two stale expectations sat red unnoticed. It belongs as its own CI step beside `IPC command parity`, mirroring the sibling two steps below that already does exactly this (`Scoped ambient reads self-test` running `verify-scoped-reads.py --self-test`). What it blocks: a regression in the LEG rather than in the tree - a parser that stops seeing the surface it claims to check, a mask or guard rule that goes blind - a class no bare run can see. Deliberately NOT added to `run-pre-push.py` (push-blocking is the wrong depth for this). The dispatch requires the step be proven in BOTH directions, including that it FAILS on the state just fixed, because a CI step whose failure mode has never been observed is a hypothesis. Its out-of-fence `IndexError` finding is filed as C58.
  ORIGINAL BRIEF: `python3 scripts/verify-ipc-parity.py --self-test` prints two failures - `fallback the real tablet tree exposes the shape the leg was written for [n=0 names=[]]` and `case 24 real tree: the leg still sees the nine shared-UI calls it found first run` - against `26 cases + 58 extra guards, 177 assertions`. The C52 worker proved they are PRE-EXISTING by running HEAD's unmodified script from a temp probe and getting the identical two, and **I reproduced it myself in this session**, so this is not a report about someone else's run.
  **The two halves are separate and must be decided separately.** (1) The two failures are in the C41 tablet-reachability leg, and `case 24`'s message says it best: it no longer sees the nine shared-UI calls it found on its first run - which is EXPECTED, because C47 fixed three of them and the count moved 9 -> 6. So at least one failure is a stale expectation in the self-test rather than a broken rule, and the honest fix is to make the assertion track the mechanism (the leg is self-clearing, as C47 proved) instead of a frozen count. `fallback ... [n=0 names=[]]` needs its own diagnosis and may be a genuinely dead case. (2) Wiring `--self-test` into CI is a DECISION, not a mechanical addition: `dev-ci.yml:694`, `scripts/check.sh:71` and `run-pre-push.py:252` all invoke the script WITHOUT `--self-test`, and wiring it today would fail CI immediately - which is exactly why the red has been invisible. Fix (1) first, then decide (2).
  Fence: `scripts/verify-ipc-parity.py` (the self-test cases and their expectations only) plus whatever CI or gate file the decision in (2) lands on - if that is outside the fence, file a Fence_Request rather than assuming. Do NOT weaken an assertion to make it pass: if `case 24`'s frozen count is the problem, replace it with an assertion that tracks the mechanism, and SAY which. Do NOT touch the allowlist entries.
  Done when: `--self-test` exits 0 with every case genuinely passing - not with a case deleted or an expectation loosened to whatever the tree happens to produce today - and the (2) decision is written down with its reasoning, whether or not it is implemented.

- [ ] **C56 [P2] NEW, found by C52 and confirmed by that worker's own reproduction: `--write-dev-mock-gaps` REORDERS the `dev_mock` section, so a 'no-op' reseed produces a CONTENT diff.** `merge_section_entries` returns `sorted(merged)` while the committed `dev_mock` section is NOT in sorted order (`delete_topology_template` first, `create_backup_to` later), so running that flag relocates an entry and its 3-line reason. It is not a line-ending bug - the worker verified byte-identity before and after its own probe and deliberately did NOT fix it, because choosing a canonical order is an owner-ish change and another lane had just rewritten those entries deliberately (commit 464782f53). Note `desktop` and `scoped_orphans` ARE sorted while `tablet` is not either, so the inconsistency is in the DATA, not only in the writer.
  Fence: `scripts/verify-ipc-parity.py` (the writer) and/or `scripts/ipc-parity-allowlist.json`. Done when either the writer is a true no-op on an already-canonical file and the file is canonicalised in one deliberate commit with its diff reviewed, or the ordering is explicitly stated as non-canonical with the writer tolerating it. The wrong outcome is a 'no-op' flag that silently rewrites another lane's section.

- [x] **C57 [P2] NEW, from C8 S5a: the restore runbook is now STALE in the one place that mattered.** - **DONE 2026-09-23, commit 6d75e6179** (`docs(operations): restate runbook 11.2 as the desktop-only restore IPC surface with its permissions and re-derivation commands`; one file, +120/-24, section 11 plus the provenance note; committed by the manager as the writer has no shell by design). **THE REWRITE NAMES THE COMMAND, NOT ONLY THE TRUTH** - which was the structural half of the item: 11.2 now carries the registration census grep with its expected result ('expect three hits, all in `apps/desktop-tauri/src/lib.rs`'), the repo's own gate (`python3 scripts/verify-ipc-parity.py`, the step at `scripts/check.sh:71`), and the UI-caller grep, under a heading that says *'Re-derive this, do not trust it - it changed once already'*. The provenance note goes further and records that this section has been corrected TWICE IN ONE DAY, so the next reader sees a history rather than a settled fact. **AND IT CORRECTED MY BRIEF A SIXTH TIME, ON THE POINT THAT MATTERED MOST:** I wrote that the bridge functions 'still take no token by design' for all three; true of the two reads (`crates/kasirmu-bridge/src/data.rs:1092`, `:1233`), **FALSE of `restore_prepare`** (`:1152` takes a session token and enforces `SETTINGS_EDIT` itself at `:1158-1160`). It wrote the split instead of the generalisation - and a runbook repeating my sentence would have been wrong on the one command that writes a file. **Three CONSEQUENTIAL stalenesses inside the same section were found and fixed because it followed the fact rather than the paragraph:** 11.7 step 3's 'must be re-created once the trigger path is available' (it is available on desktop now), the 11.8 heading calling the CLI 'the currently reachable route' (no longer the only one), and the 11.9 bullet claiming the tablet's in-app path 'is the one that matters on that shell' - which was INVERTED, since the tablet registers none of the three. **It also documented an honest limit instead of implying a double check:** the wrapper's permission gate is NOT independently observable at runtime (the ledger row at `tests/gate_audit.rs:111-116` is what makes it load-bearing), and the runbook now says so rather than claiming defence-in-depth. **And it caught its own draft error before reporting it** - it first wrote the live screen as `ui/src/features/settings/screens/DataManagementScreen.tsx`, checked, and found the live one is `ui/src/features/settings/DataManagementScreen.tsx` while the `screens/` file is a blank placeholder. Every citation in the new text was re-read from source after writing, and a file-wide sweep for the removed phrasing returns exactly one hit: the deliberate quotation of the old wording inside the correction. Document now 1,733 lines. No cargo was started.
  ORIGINAL BRIEF: `docs/operations/runbook.md` section 11.2 states that **no IPC command is registered** for `list_restore_candidates` / `restore_prepare` / `restore_status` - which was TRUE when it was written (commit 527e5e11f) and became FALSE with commit 91500dc0f, which registers all three on the desktop shell. The section also marked the in-app operator path unverified for that reason. Fence: `docs/operations/runbook.md` section 11 only. Done when the section states what is now reachable, on which shells, with which permission (`restore_prepare` = `SETTINGS_EDIT`; `list_restore_candidates` and `restore_status` = `SETTINGS_READ`), and re-states the tablet position accurately - **the tablet registers none of the three**, so a tablet operator still has only the CLI path. Note this is the SECOND time this runbook has gone stale inside a day; that is an argument for the section naming the COMMAND that would show the current truth rather than only the truth itself.

- [ ] **C58 [P2] NEW, reported out of fence by the C55 worker: `main()` has a latent `IndexError` on an empty site list.** `scripts/verify-ipc-parity.py`'s `main()` indexes `v[0]` on the `{shell}-fallback` info line, so if `no_token_fallbacks` ever returned a name with an empty site list the checker would crash with `IndexError: list index out of range` rather than reporting. It is UNREACHABLE on the real tree today - every witness has a site, and C55's new case now ASSERTS that - which is why it was recorded rather than fixed inside that slice. Fence: `scripts/verify-ipc-parity.py` (`main()`'s info line only). Done when an empty site list renders as an explicit 'no site' rather than crashing, with a case proving it - or with a written reason that the invariant is now enforced upstream and the crash cannot be reached.

- [x] **C59 [P0] NEW, AND IT IS A LIVE USER-VISIBLE BUG: the tablet's retry writes its sync outcomes to the WRONG DATABASE, so every retry re-sends the same items forever.** - **DONE 2026-09-23, commit 78276320f** (`fix(mobile): write tablet retry outcomes to the store database, not the global one`; `apps/mobile-tauri/src/commands/offline.rs` +69/-41, `offline_tests.rs` +185; `apps/mobile-tauri/src/lib.rs` deliberately NOT in the commit, verified by `git show --name-only`). MANAGER-RE-VERIFIED: git show --stat is exactly those two files. **THE FIX re-resolves the STORE scope for Phase 3, mirroring the bridge reference (`crates/kasirmu-bridge/src/offline.rs:371-375`) and this shell's own already-correct `sync_run_scoped` (`apps/mobile-tauri/src/commands/sync.rs:253-258`)**, replacing `state.db.lock().await` (the global identity db) with `state.resolve_scope(&session_token)` + the store connection guard. It re-resolves rather than carrying the Phase 1 guard across the HTTP await, for the reason `sync_run_scoped` documents: the store manager's `std::sync::Mutex` guard is not `Send`. The now-redundant `drop(db)` was removed. **BEFORE/AFTER, STATED AS THE OBSERVABLE DIFFERENCE:** before, the push succeeded and Phase 3 wrote outcomes to a database where those ids do not exist, `mark_offline_synced` returned NotFound, the `?` aborted the command, the store rows stayed `pending` and **every retry re-sent the same items forever**; after, Phase 3 writes the store db, the guarded CAS lands, the command returns `Ok(SyncResult { synced_count: 3, .. })`, and `list_pending_offline` no longer offers those rows - **loop closed**. **AND THE 'BEFORE' WAS PROVEN RATHER THAN QUOTED:** reverting Phase 3 to `state.db` fails the new test with exactly the documented mechanism - `a successful push must no longer abort on Phase 3: Core { sub_kind: NotFound, message: "not found: offline_queue 01a0cd1d-..." }`, exit 101 - and restoring it returns exit 0. **THE DIVERGENCE PIN WAS DELETED DELIBERATELY, WITH THE COMMIT BODY SAYING SO:** *'This is the fix the C54 divergence pin was waiting for: that pin, a NotFound assertion over a successful push, is deleted deliberately in this commit.'* That is the handoff working exactly as designed - the pin existed so that whoever closed the bug had to do it on purpose, and the replacement assertion is the positive one (rows `synced`, pending count 0). **ACCEPTANCE, three pieces, `cargo test -p kasirmu-mobile offline` -> 22 passed, exit 0:** success now marks store rows `synced` with `synced_count == 3` and `pending_offline_count() == 0`, PLUS an assertion that the GLOBAL queue stays empty so the fix cannot silently re-introduce the old write target; the ordering test still passes unchanged (body `["money", "catalog", "bulk"]`); and a NEW failure-path test drives a real HTTP 500 through the same path and asserts the command does not abort, `synced_count == 0`, `failed_count == 1`, the row is `Failed` (never `Synced`) with `retry_count == 1` and a recorded `last_error` - deliberately NOT weakened to 'an error occurred', because the hazard of moving a write target is OVER-MARKING, so the test pins that a failed push is recorded on the store row rather than reported as success. **AND IT LEFT THE OTHER SESSION'S WORK ALONE:** `apps/mobile-tauri/src/lib.rs` remains dirty with the uncommitted `desktop_link::*` registrations, the same three `drift_pin_*` failures remain (gate filter shows 8 passed / 3 failed), grepping the disagreement for `offline::` returns nothing, and it neither regenerated the ledger nor moved the ceiling. **C60 filed:** the audit record for this bug is still marked OPEN and cites pre-rebrand paths that do not exist.
  ORIGINAL BRIEF: Found by the C54 divergence diff, which is exactly what that brief was ordered to do first. The chain: `apps/mobile-tauri/src/commands/offline.rs:298` `retry_offline_sync_scoped` is three-phase - **Phase 1** reads the pending queue from the STORE database (`state.resolve_scope(&session_token)` -> `db_manager.open_store` -> `<data_dir>/store-<id>.sqlite`), **Phase 2** pushes successfully, and **Phase 3** writes the outcomes to `state.db` - which is the **GLOBAL IDENTITY database** (`AppState::new` -> `resolve_db_path` -> `<app_data_dir>/kasir.db`, `state.rs:164-171`), NOT the file Phase 1 read. Those ids do not exist there, so `mark_offline_synced` returns `CoreError::NotFound` (`crates/kasirmu-core/src/db/offline.rs:643-666`) and the `?` inside `apply_sync_outcomes` ABORTS the command. The store's rows therefore stay `pending`, and **every retry re-sends the same items forever** - a silent, permanent duplicate-push loop on the tablet. It is already recorded open at `docs/records/audit-open-findings.md:1640-1650`. The desktop path is correct: `crates/kasirmu-bridge/src/offline.rs:317` re-resolves the STORE scope for its Phase 3 write.
  **Why C54 did NOT fix it, correctly:** the collapse is mechanically available (the tablet has `AppState::bridge_ctx()` at `state.rs:450`, used by 20+ other tablet commands), but changing which database a live bug writes to is a **behaviour change on that bug's blast radius**, not a refactor - so the slice PINNED the behaviour instead: its new test asserts the command returns `AppError::Core { sub_kind: NotFound }` AFTER the request body proves the push succeeded, so **when the collapse lands that assertion fails and whoever lands it deletes it deliberately**. That is the right shape for a fix that must be decided rather than slipped in.
  Fence: `apps/mobile-tauri/src/commands/offline.rs` (the Phase 3 region only) plus its sibling `offline_tests.rs`. **Do NOT touch `apps/mobile-tauri/src/lib.rs`** - it is DIRTY from another session (uncommitted `desktop_link::*` registrations). Done when Phase 3 writes to the SAME store database Phase 1 read (via `bridge_ctx()`, the house path), the C54 divergence-pin assertion is removed deliberately with a comment saying why, and a test proves a successful retry now marks the store's rows `synced` rather than leaving them `pending` - which is the observable difference and the whole point. Report the duplicate-push loop as CLOSED with the test that shows it.

- [x] **C60 [P2] NEW, from C59: the audit record for the bug you just fixed is still marked OPEN, and its paths predate the rebrand.** - **DONE 2026-09-23, commit dbdf9efa4** (`docs(records): close the tablet retry wrong-database finding and correct its dead paths`; +26/-43 in `docs/records/audit-open-findings.md`, committed by the manager as the worker has no shell). **IT CHECKED THE FILE'S CONVENTION BEFORE CHOOSING ONE, WHICH IS THE PART WORTH COPYING:** the register carries **39** `~~...~~` strikethrough closures and its own closing instruction at :1671-1677 says *'Re-open a finding here, fix it, then flip its status in this file'*, while the companion `audit-closed-findings.md` (80 lines) holds **whole settled SECTORS**, not individual findings, and its preamble states that a block moves there only if it 'carried its own closure commit AND stated no work still owed'. So removing a single finding would have been wrong twice over - it is not a sector, and the archive's admission test is about sectors with no residual. It annotated in place and said that moving single findings out would be a different, larger decision affecting all 39. **THE ENTRY NOW CARRIES THE MECHANISM, THE CLOSURE AND THE REAL PATHS:** retitled to a struck-through heading ending `- CLOSED 2026-09-23 (78276320f)`, the one-line mechanism (Phase 1 read the store db, Phase 3 wrote the global identity db, ids did not exist there, NotFound propagated), both paths corrected **plus three more the old entry cited**, a short Path note explaining that these were pre-rebrand names so a reader is not left wondering why the text changed, the proving test named with its failure-direction sibling, and the fact that the C54 divergence pin was deleted by the fixing commit ON PURPOSE. **The old stale table, the `oz-pos.db` narrative and the six-command re-derive block are GONE rather than reworded.** Every claim re-verified: both corrected paths exist with byte sizes, both named tests exist at `offline_tests.rs:334` and `:511`, the commit SHA was checked with `git log -1` rather than taken on trust, and it confirmed the divergence pin is genuinely gone - the two surviving C54 strings are a banner and a comment EXPLAINING the deletion, and the only `NotFound` assertion left belongs to an unrelated test. **AND IT DELIVERED THE CENSUS I ASKED FOR: 57 dead pointers in the register - filed as C62.** `python scripts/verify-doc-uniqueness.py` exits 0 ('no duplicate authoritative documents'). The `generate-records-index.mjs --check` drift it found is pre-existing, unrelated, and filed as C63.
  ORIGINAL BRIEF: `docs/records/audit-open-findings.md:1640-1650` describes the tablet retry's two-database defect as an open finding, and it names the files as `apps/tablet-client/src/commands/offline.rs` and `crates/oz-bridge/src/offline.rs` - **neither path exists**: they are pre-rebrand names, so a reader following the record cannot find the code. C59 closed the defect at commit 78276320f. Fence: `docs/records/audit-open-findings.md` (that finding's entry only). Done when the entry records the defect as CLOSED with the commit that closed it, its mechanism stated in one line, both paths corrected to the real ones (`apps/mobile-tauri/src/commands/offline.rs`, `crates/kasirmu-bridge/src/offline.rs`), and the closing test named - or, if the document's convention is to REMOVE closed findings, follow that convention and say so. Do not restate the old text with better wording; the value is that a reader following the record reaches code that exists.

- [x] **C18 P1.9-P1.13 DONE 2026-09-23, commit e4b8f721e** (`fix(core): run the last bare autocommit writes inside a transaction (C18 P1.9-P1.13)`; 10 files - five sources plus five test files, two of the test files new). All five sites converted to join-or-own via `is_autocommit()`; `platform/sync/src/queue.rs` untouched, per the fence. Narrow targets: `stock_count` 47 passed, `stock_transfer` 43, `db::tables` 40, `db::products` 139; rustfmt clean over the whole `src/db/` dir. **AND THE HONEST RESULT IS NOT A CLEAN SWEEP - ONLY ONE OF NINE ROLLBACK TESTS DISCRIMINATED, AND THE WORKER PROVED THAT RATHER THAN SHIPPING THEM AS PROOFS.** Restoring the pre-fix bare `self.conn.execute(...)` at P1.9 left `insert_stock_movement_joins_a_caller_transaction` PASSING, and the reason is structural: **on a single shared `Connection`, a bare statement issued while the caller's transaction is open ALREADY JOINS THAT TRANSACTION IMPLICITLY.** So for every site that was merely a bare execute - P1.9, P1.11, P1.12, P1.13 and four of the five P1.10 functions - the pre-fix and post-fix code are BEHAVIOURALLY IDENTICAL for rollback purposes, and no runtime test can separate them. Those tests are regression guards, not proofs, and it declared that per site. **The ONE discriminating site is P1.10's `create_stock_count_with_next_number`,** which hand-rolled an UNCONDITIONAL `BEGIN IMMEDIATE` (`stock_counts.rs:106`): against pre-fix code the new test fails outright with `Db(SqliteFailure(Unknown, "cannot start a transaction within a transaction"))` and passes after. That is the only site where the defect is an ERROR rather than a lost rollback - which is exactly why it falsifies cleanly - and it is now join-or-own with the COMMIT/ROLLBACK arm skipped for a joined caller, since a rollback there would discard the caller's work. **A MODEL CORRECTION:** the brief described P1.11 as a 'delete-then-compensate PAIR'; the worker searched and there IS no separate compensation statement - the real pairing is the guard READ (status must be `draft`) plus its mutation, and the defect that matters is a CHECK-THEN-WRITE RACE in which the status reads `draft` and the line is still added after a concurrent `send_transfer` moved the transfer to `in_transit`. Both the INSERT (:328) and the DELETE (:371) are now inside the same transaction as their guard read. Its own carried note is the most useful sentence in the report: the `is_autocommit()` join is correct but **not load-bearing for most of these sites on a single-connection `Store`** - its value is (a) making the `BEGIN IMMEDIATE` site work at all and (b) keeping the contract honest for a future caller on a second connection. Recorded in each site's doc comment.
  **THE PREMISE CHECK CAME BACK AND IT CONFIRMED THE DOUBT: the sweep WAS largely a restatement of a style rule. See the re-scoped P3 note above for the full measurement (131 sites / 99 single-execute / ~12 multi-statement functions).**

- [x] **C61 [P1] NEW: the review's own section 4.4 restates a grep count as a defect list, and it should carry the correction.** - **DONE 2026-09-23, commit 55ae2abcb** (`docs(review): re-scope section 4.4 as a measured population with three classes, and record the two named defects as fixed`; manager-codebase-review.md +33/-3, section 4.4 only; committed by the manager as the writer has no shell). **EVERY NUMBER RE-DERIVED RATHER THAN COPIED, and the raw counts are now stated: 501 `conn.execute(` matches total, 370 in `_tests.rs`, 131 across 40 non-test files, 111 enclosing functions - 99 with exactly one execute (99 sites) and 12 with two or more (32 sites).** **IT CORRECTED MY BRIEF TWICE, AND BOTH CORRECTIONS WERE RIGHT:** (1) I said 11 of the 12 multi-statement functions hold no transaction, but **a grep cannot establish that** - reading the signatures showed at least three already join a caller's transaction by construction (`write_assignment_scope_on` takes `conn: &Connection` and is called both as `self.conn` and as `&tx`; `create_user_in_tx`/`update_user_in_tx` are called as `Store::new(&tx)`), so the honest figure is *'12 functions, of which some already join'* - which is also why the section now says that 'takes `&self`' does not mean 'owns its transaction' on this codebase. (2) **I told it the two named defects 'were correct and remain correct' - they have BOTH SINCE BEEN FIXED, and it wrote what the code shows:** `update_sale_status` is now a compare-and-set (`UPDATE ... WHERE id = ?3 AND status = ?4` inside `unchecked_transaction()`, `sales_crud.rs:621-634`, conflict branch reachable at :627-633, two discriminating tests, with the doc comment recording the old unconditional shape), and the `customers.loyalty_points` projection is now maintained inside the same transaction at all three sites (`loyalty.rs:512-518`, `:848-855`, `:981-987`, each marked `MSL-4`). So the section says these were real, they are fixed, and here is the verifiable fix - rather than reading as a retraction. **AND IT FOUND WHAT GENUINELY REMAINS: `customers.total_spent_minor` still has TWO production writers with OPPOSITE SIGNS in DIFFERENT transactions** - `sales_lifecycle.rs:59` adds and `refunds.rs:1027` does `MAX(total_spent_minor - ?1, 0)` - a multi-writer balance by construction, where the zero floor is what hides a double-subtraction. **Filed as C64.** It also corrected two of my line numbers (`stock_counts.rs:106` is the COMMENT, the call is :114 guarded at :112) and noted that the section's old four-line-number list was a snapshot of one function that no longer lines up - the same defect class C30 was filed about. **IT ALSO CAUGHT ITS OWN ERROR:** its first draft cited the third MSL-4 marker as `loyalty.rs:971-973`; it is `:981-987`. Reported not edited: a hand-rolled `BEGIN IMMEDIATE`/`COMMIT`/`ROLLBACK` survives OUTSIDE `db/` at `platform/core/src/settings/raw.rs:232/:235/:242`, so the section's 'only place the correct mode is used' had to be qualified. No other copy of 138 survives inside the review (checked the scorecard, verdict, revision table :642 and section 15), and the checklist's remaining copies at :70 and :368 were RESCOPED BY ME in the same pass.
  ORIGINAL BRIEF: `manager-codebase-review.md` section 4.4 is headed 'Writes that are not in transactions, and balances with more than one writer' and says verbatim *'There are 138 autocommit-capable conn.execute sites in db/ outside tests ... Some ride an outer transaction, some do not.'* - so it HEDGED correctly, but the count then travelled into the checklist as a work list and into planning as correctness work. The measurement: **131 sites, 99 single-execute (already correct), 11 multi-statement functions with no transaction, 1 hand-rolled `BEGIN`.** The review is a deliverable and its own thesis is that a claim without a measurement behind it is the defect - so it should carry the corrected characterisation, the two commands that produce it, and the note that the ONLY class that falsifies cleanly is the hand-rolled `BEGIN`. Fence: `manager-codebase-review.md` (section 4.4 and its status line only). Done when a reader of 4.4 cannot mistake the population for a defect list, and the number they see carries its command.

- [ ] **C62 [P1] NEW, from C60's census: the audit register cites 57 DEAD PATHS, 43 of them pre-rebrand.** `docs/records/audit-open-findings.md` is the register a reader consults to find out what is still wrong, and a scanner over every backticked repo-root path in it found **57 distinct paths that do not exist**, in two classes.
  **Class A - pre-rebrand names left behind by the rename (43).** `apps/desktop-client/src/...` (12 files incl. `data.rs`, `settings.rs`, `sync.rs`, `lib.rs`, `rbac_presets.rs`), `apps/tablet-client/src/...` (11 files incl. `commands/offline.rs`, `commands/pos.rs`, `state.rs`), `crates/oz-bridge/src/...` (15 files incl. `ctx.rs`, `offline.rs`, `data.rs`, `topology/model.rs`), `crates/oz-core/src/...` (5 files incl. `db/audit_security.rs`, `features.rs`, `location_profile.rs`). Two whole SECTIONS are titled on dead paths: `## Bridge extraction (crates/oz-bridge)` at :192, and the gate-integrity cluster at :680-693.
  **Class B - relocated or outright deleted (11+, with replacements found where they exist):** `docs/api-reference.md` (:135, :153) and `docs/guides/api-reference.md` (:136) both dead, both really `docs/guides/developer/api-reference.md`; `docs/multi_pos_one_location_support.md` (:159) really `docs/archived/...`; `ui/src/locales/kds.ftl` (:993) really `shared-ui/locales/kds.ftl` after the P9a move; `ui/src/platform/ui/page-registry/index.ts` (:1557) really `ui/src/registries/page-registry/`; and five `.agents/*.md` citations plus `scripts/ftl-orphan-allowlist.json` (:1041) with **no replacement found anywhere in the tree**.
  **WHY THIS IS P1 AND NOT TIDINESS:** the whole point of a register is that you can follow it to the code. This workstream's central finding is that this codebase's characteristic defect is documents that agree with each other and are all wrong together - and here the register of open defects cites 43 paths that stopped existing in a rename, so a reader cannot verify whether an entry is still open. **A mechanical rebrand pass is WRONG for Class B** - the `.agents/*` cluster and `ftl-orphan-allowlist.json` appear to be deleted outright, not renamed - so the honest fix is per-path with a decision, not a sed. Fence: `docs/records/audit-open-findings.md` (and `audit-closed-findings.md` for the sectors it points at). **The decision this item needs first, and it is the real question: should a consolidated audit register carry live pointers at all, or date-stamp its citations and name the SECTOR rather than the file?** The file already has 39 annotated closures, so the cheaper long-term shape may be 'cite the commit, not the path' - decide that before editing 57 rows.

- [ ] **C63 [P2] NEW, reported out of fence by C60: `node scripts/generate-records-index.mjs --check` exits 1 on PRE-EXISTING drift.** The drift is unrelated to any change in this wave - every differing line is a `research`-row ORDERING in the ADR section, and `grep -c audit-open-findings` over the checker's output returns 0, so no audited finding is missing from the index. It was found only because a worker ran the checker as evidence for its own doc edit, and it has presumably been red since whatever reordered those rows. Fence: `docs/records/` (the ADR research rows) or the index generator, whichever the fix belongs to. Done when `--check` exits 0, or when the drift is shown to be the CHECKER's bug rather than the data's, stated with the command. **Check first whether this is the same class as C55's red self-test - an ungated checker that can sit wrong unnoticed - and if the records index is not wired into CI either, say so, because that is the finding rather than the ordering.**

- [ ] **C64 [P1] NEW, found by C61's re-derivation: `customers.total_spent_minor` has TWO writers with OPPOSITE SIGNS in DIFFERENT transactions, and a zero floor hides a double-subtraction.** `crates/kasirmu-core/src/db/sales_lifecycle.rs:59` ADDS to it and `crates/kasirmu-core/src/db/refunds.rs:1027` subtracts via `MAX(total_spent_minor - ?1, 0)`. That is a multi-writer balance by construction - the same shape as the `loyalty_accounts.points` / `customers.loyalty_points` defect the review named and that has since been fixed into a single transaction - but this one was NOT fixed, because the review's named instance was the loyalty pair. **And the `MAX(..., 0)` floor is what makes it dangerous rather than merely untidy:** if a refund is applied before its sale is counted, the subtraction clamps at zero, so the under-count is silently swallowed and a later sale adds on top of a wrong base. A floor on a derived balance converts a visible negative into an invisible wrong number.
  Fence: `crates/kasirmu-core/src/db/{sales_lifecycle,refunds}.rs` plus their `*_tests.rs` siblings. Done when either both writers run inside ONE transaction with the other's work visible, or the value is derived rather than stored (a SUM over the sales/refunds rows it is supposed to represent) - and the discriminating test is a refund BEFORE its sale, which today should show the clamped floor swallowing the difference. If the second option is taken, say what the migration does with existing rows, because a derived value and a stored value disagree for every historical record the moment they diverge. If investigation shows a path that keeps the two writers ordered so the clamp cannot be reached, show it and close the item with that evidence instead.

- [ ] **C65 [P2] NEW, because C18 P3 class b was committed without its per-function verdicts.** Commit 5989b85e7 wraps eight multi-statement writers (`assignments.rs`, `kds_lines.rs`, `kds_ops.rs`, `loyalty.rs`, `profile.rs`, `staff.rs` + two test files), and it was committed BY THE MANAGER from an orphaned working tree after TWO workers died mid-slice - so the per-function verdict the brief demanded ('what observably breaks if step 2 fails after step 1 succeeded; if nothing, revert the wrap') was never reported. The code compiles and every filter is green (assignments 41, kds 166, loyalty 53+84, profile 90, staff 90), so nothing is broken - but some of those eight wraps may be no-ops on a single shared connection, exactly like the eight-of-nine non-discriminating rollback tests in the sibling slice. Fence: the six source files above plus their test siblings. Done when each wrapped function carries a one-line verdict in its doc comment - the observable invariant it protects, or an explicit 'contract improvement, not a defect fix' - and any wrap whose invariant turns out to be unobservable is either reverted or relabelled. **This is a documentation-quality item, not a correctness one: the risk is a future reader treating a no-op wrap as a guarantee.**

- [ ] **C66 [P2] NEW, found in the PR 113 CI logs: `kasirmu-api pg::tests::pg_integration_rest_rls_non_owner` is FLAKY and SLOW, and it failed once already.** Across two consecutive runs: TRY 1 exceeded **120s** and was reported SLOW, TRY 2 passed at **123s**, and in the earlier run it panicked at `crates/kasirmu-api/src/pg_tests.rs:820:10` while the summary counted `1 flaky`. It did not fail either run, but a test taking 120s+ against a 3-try budget is a latent red that will fail on a busier runner or a slower PostgreSQL container - and when it does, it will look like an unrelated regression. Fence: `crates/kasirmu-api/src/pg_tests.rs` (that test only) plus whatever fixture or RLS setup it depends on. Done when either its cost is brought under the retry budget with the reason recorded (e.g. per-tenant setup a shared fixture could do once), or its slow-and-flaky character is written down where a future reader will find it - because the wrong outcome is a test quietly retried green forever, which teaches everyone to ignore a red.

- [ ] **C67 [P2] NEW, found while fixing the tablet pins: the tablet's floor pin carries a slack allowance that SWALLOWED a three-command registration entirely.** `apps/mobile-tauri/src/commands/registration_gate_tests.rs`'s `drift_pin_registration_floor_is_met` asserts `REGISTERED_FLOOR == REGISTERED_TOTAL` (339 == 339) **plus** `parsed <= floor + REGISTERED_SLACK` where the slack is **24**. Commit `fc2ea1938` registered three commands, the tree parsed 342, and 342 sits inside 339 + 24 - so **a stale floor and a stale ledger total agreed with each other while both lagged the tree by three, and the pin stayed GREEN.** It only surfaced via the ceiling pin, because the ceiling counts ungated doors and had no slack. **The slack is documented as intentional and it does have a purpose (it lets registrations land before the floor is re-pinned), but its size is the bug: at 24 it can absorb twenty-four registrations - a whole slice - and the pin that exists to notice registrations is the one pin that cannot notice them.** Contrast the DESKTOP floor pin, which is an outright equality against the tree and therefore went red immediately: the two shells have different guarantees for the same-named pin.
  Fence: `apps/mobile-tauri/src/commands/registration_gate_tests.rs` (the slack constant and its comment). **The decision, not the number: is `REGISTERED_SLACK` meant to absorb a working session or a single commit?** If a session, 24 is defensible and the honest fix is to document that the ceiling pin is the one that catches registrations; if a commit, shrink it to something that cannot hide three. **Do not just shrink it to make CI green - that is not the failure mode here; the pin is green.** Done when the slack's purpose and its size are reconciled in writing, and the comment states which pin is the backstop for a registration that lands without the floor moving.

- [ ] **C35 [P1, DEFERRED - do not start until D9 is answered] Parameterize the four catalog children so the FK wall cannot fire.** Gated because YAGNI now applies: the parameterization only earns its keep once a sync action for these tables exists to carry a real tenant into them, and the finding recorded above says none does. The analysis is kept so the slice is ready the moment that changes. This is the surviving half of C32-S2, and it is a PREPARATION slice, not a live-defect fix: today `product_variants` omits `tenant_id` (`db/products.rs:248-258`) so it takes the column default, and the parent's tenant is also `'default'`, so the composite FK at `migrations/20260831_per_tenant_unique_rebuild.sql:173,199,216,239` is satisfied. The defect is that the wall is *reachable*, not that it is being hit.
  Fence: crates/kasirmu-core/src/db/products_crud.rs (`create_product_with_attributes` :251 and the `create_product` wrapper :224), db/products.rs:248, db/product_bundles.rs:112/:129/:182, db/tax/assignments.rs:44, plus the sibling `*_tests.rs` of each. A second slice carries the value from crates/kasirmu-bridge/src/products.rs:662-735, bundles.rs:135-176, product_variants.rs:266-313.
  Shape, decided by the D9 analysis and NOT to be re-litigated: an explicit `tenant_id: &str` PARAMETER on the writer, never a field on `Store`. There are already **54** `tenant_id: &str` parameter sites in `crates/kasirmu-core/src/db`, and `db/offline.rs:305-310` states why: the parameter exists because `enqueue_offline_priority` hardcoded `"default"` and "enqueues another store's sale under `default` - a real pre-existing bug". `Store::new(` occurs 1,560 times, so a field is either a 1,560-site edit or a silent default, and a silent default is exactly the bug the parameter was invented to prevent.
  The `create_product` wrapper keeps passing `"default"` explicitly so roughly 160 legacy callers and their `'default'` assertions stay green - parameterization must NOT change desktop behaviour, because a real tenant value on desktop bricks the next boot per C34.
  Done when: a test calls `create_product_with_attributes(.., "tenant-a")` on `fresh_db()`, then the variant/bundle/bundle-item/tax writers, and asserts all four child rows carry `tenant-a` **and that no `ConstraintViolation` occurred** (that is the ordering-by-construction proof); a second test pins that `create_product` still writes `'default'`. media_assets, media_thumbnails, payment_gateways and payment_settlements have no production writer at all - they are declared tables. Keeping them in `RLS_TABLES` makes the list's length a claim rather than a fact and hides the four tables that genuinely do need work. Move them to `RLS_EXEMPT` with a reason naming the absence of a writer, then regenerate `crates/kasirmu-core/migrations/20260813_init.pg.sql` (generated, never hand-edited).
  Fence: scripts/generate-pg-migration.py, crates/kasirmu-core/migrations/20260813_init.pg.sql.
  The premise is now WITNESSED, not assumed: the worker on this slice proves the absence of a writer itself, per table, and reports the greps. A wrong exemption is worse than the current state - a table that IS written must stay in `RLS_TABLES`.
  Done when: the generator's two lists are consistent with its own rule, the regenerated SQL is idempotent (running the generator twice is byte-identical), and the PG schema-drift gate passes.


## C17 work list (from the IPC gate-debt inventory; ceilings measured 75 desktop / 95 tablet during remediation)

**Slice 2 finding (recorded):** three of the four tablet settings reads are dead, but `get_hardware_settings` is NOT - `ui/src/hooks/useTerminalHardware.ts:240` calls it from a production fallback arm whenever the session token is null, and the null initial state makes that reachable. So it is queued as **C17b**: retire that fallback (wait for the token, or fail the card closed visibly) and only then delete the command and its ledger row, with an acceptance that proves the UI still resolves hardware settings with a session. The IPC parity gate independently confirms the split - it lists the three dead doors among the 15 commands named by no shipped UI file, and omits hardware.

The ordered, lowest-risk-first list is in the inventory recorded in the journal: (1) delete the tablet's five ungated history reads and their registrations, because the scoped twins are already live and the UI already prefers them; (2) the tablet's four ungated settings reads; (3) the three ungated branding setters; (4) the ungated create_backup and get_backup_status; (5) six vendor/value wrappers; (6) five more; (7) set_setting's renderer-supplied user_id (needs a decision first: the ungated door writes the GLOBAL identity database while the scoped twin writes the store database, so they are not interchangeable); (8) five tablet wrappers; (9) hoist the tablet pos/refunds permission checks out of private run_* helpers into the wrapper bodies so the sweep can see them; (10) route the tablet promotions wrappers through the bridge shims that already enforce PROMOTIONS_*. Per deletion: remove the command and its registration, lower REGISTERED_FLOOR, DEBT_CEILING and the matching class count together, regenerate with KASIRMU_REGENERATE_GATE_LEDGER=1, then run both registration_gate_tests files. Ceilings may only fall.


---


---

## Handover - where this stands and what is next

**State at 2026-09-23 (mid-implementation).** 21 items verified and ticked with a commit SHA each; 6 slices landed but partial with a named remainder; the rest queued or parked. Every tick's evidence is in the verification log below, and the gate command block above re-derives all of it.

**Landed and verified (each with its SHA):** C5 + C5b (completed sales only, one day definition, both shells), C6 + C6b (one timezone contract on the read path, validation on the write path), C7 detection half (RLS posture reported at boot and on /health), C9 (qris-core proprietary), C10a (the seven money-path doors are BEGIN IMMEDIATE, and the regression test can now detect the mode), C11 code half (snapshot before migrating), C12 (read-only stock variance report), C13 (plugin discounts gated, Lua tax bounds enforced), C17 slices 1-2 (ceilings 95 to 89 on the tablet, 75 to 72 desktop-pending), C18 P1.1/P1.2/P1.3/P1.5 (sale-status CAS, cash payouts, open_shift, fiscal sequence), C27 (unified routing, checker widened), C1 stages S1 and S1.5 (branch-tolerant credential reads, fail closed on undecryptable), C8 slices S1 and S2 (atomic verified backups; validated atomic restore with the CLI re-pointed onto it), C2 first step (no silent plugin hot-swap, capability flags fail closed), C3 slice S1 (origin column + per-effect receipt schema).

**The next three dispatches, in order, with fences (do these before starting anything else):**
1. **C4 slice S2** - the enqueue sites, once C4-S1's arms land: `crates/kasirmu-core/src/db/refunds.rs` (after the refund/line inserts, before the stock credit), `sales_lifecycle.rs` (void_sale, after the CAS and before commit), `sales_checkout.rs` and `sales_lifecycle.rs:544` (one payment.recorded per split, after each INSERT), plus new `enqueue_*_outbox_in_tx` helpers in `db/offline.rs`. Use the in-transaction enqueue form only - the non-transactional insert is the wrong one. Acceptance: a rolled-back refund or settlement leaves zero queue rows; a committed one leaves exactly one.
2. **C18 slice P1.3b** - the shift invariant needs a database-level guard: a migration adding a PARTIAL UNIQUE INDEX on shifts(user_id) WHERE status = 'open'. It must land with its own index-surface pin bump in `crates/kasirmu-core/src/migrations_tests.rs` (the pin is a census over sqlite_master; the comment block above it explains each increment).
3. ~~**C8 slice S4** - the boot-time safe-mode consumer in both shells~~ **DONE: S4a desktop 4781b122, S4b tablet 7acc00f6a.** Both shells now consume a pending restore before opening anything. ~~**S7** (the operator runbook)~~ **DONE 2026-09-23, commit 527e5e11f** - section 11 of docs/operations/runbook.md covers request -> boot -> refusal, the four `Outcome` values with their exact log lines, `<db>.pre-restore.db`, and the four-value verdict table, and states the one behavioural consequence an operator will notice: a restore is **REFUSED, not queued**, with `AlreadyClaimed` (lock held by another boot - the next boot retries, because the request file remains) separated from the database being held by another process (`restore_from` has no retry and no wait, so it rolls back and refuses). **S5a (IPC registration) DONE 2026-09-23, commits 91500dc0f + b68568005** - the restore chain is reachable from the desktop app for the first time; three commands registered and all gated (`restore_prepare` -> resolve_session + SETTINGS_EDIT; `list_restore_candidates` and `restore_status` -> resolve_session + SETTINGS_READ, deliberately the read half of the same family so that seeing a pending restore does not confer the right to request one); ceilings REGISTERED_FLOOR and ledger TOTAL 469 -> 472 with DEBT_CEILING UNCHANGED at 75 and the regenerated ledger at exactly 75 rows, the measurement itself saying none of the three is debt - and the STOP condition fired first: the two reads written session-free were classified as DEBT and the remedy applied was to add the permission to the wrapper rather than raise a ceiling; a THIRD PIN my brief missed (PINNED_DESKTOP in apps/desktop-tauri/tests/gate_audit.rs:102, 7 -> 10, keys gain SETTINGS_READ) became fence request #6 and was approved only after I reproduced the red myself; and the slice's best result is a FALSIFICATION THAT DID NOT WORK, reported as such - removing the wrapper permission check leaves all six new tests passing because the bridge enforces the same permission internally, so the wrapper gate is unobservable at runtime and the CENSUS PIN is the binding proof, not the unit test. **What remains in C8 is S5b (the Safe Mode SCREEN with locale strings) and S6 (the updater writes its own candidate file after its pre-update backup).** Note for S5: the runbook already records that no IPC command is registered for `list_restore_candidates` / `restore_prepare` / `restore_status`, so the in-app trigger is unreachable until S5 lands - the runbook says so rather than describing a UI flow that does not exist.

**Owner-blocked, each waiting on one question (do not guess these):**
- **C1 stage 2** (per-install keychain key + `oz rekey`): is `OZ_MASTER_KEY` set on any deployment, and is whole-file .db credential portability a requirement? The reader is already branch-tolerant, which was the precondition.
- **C2 signature verification + operator grant store**: D7 - how much plugin trust is acceptable. The silent hot-swap is already removed, so the remaining risk is a first load of a dropped file.
- **C7 role cutover**: has any PostgreSQL deployment run scripts/rls-cutover.sql? Detection now reports the posture at boot, so the answer is one HTTP call away.
- **C26 architecture**: rule versus tier order, deadline **2026-11-07** (the grandfathered entries expire then and the checker treats expired as blocking).
- **C17 slice 3** (desktop create_backup): does the pre-login updater keep a session-less door as a documented exemption? And does set_setting's global-identity-vs-store-database split allow a straight repoint to its scoped twin?

**Two operational lessons from implementation, worth keeping:**
- **Verify worker artifacts, not the roster.** Four workers in this wave stopped silently - two produced nothing, one produced complete green work it never committed, one wrote nothing at all - while still reporting running or ready. A file mtime, a grep for the expected marker, or running the acceptance yourself is the reliable signal, and a commit SHA is the only tick worth recording.
- **A worker that cannot demonstrate a failing test should say so.** Three slices here landed with a proven RED: the concurrency test failed against the unfixed SQL first (sale-status CAS, cash payout, open_shift). Two others correctly reported that no discriminating test exists for their change (the fiscal sequence wrap, the C13 tax bounds) rather than shipping a test that would pass either way.
## Gate commands - re-verify everything ticked so far

Run from the repository root. These are the exact commands whose output is recorded in the verification log; none of them mutates the tree. (Per AGENTS.md, a full `cargo test --workspace` is not an iteration command - the workspace CHECK is fine, and the per-crate test targets below are what the ticks actually assert.)

```bash
# integration: does the whole workspace still compile
cargo check --workspace

# C2 step 1 - plugin host: no silent hot-swap, capability flags fail closed
cargo test -p kasirmu-plugin                       # 181 passed
cargo test -p kasirmu-app --lib state::tests        # 11 passed

# C1 S1 + S1.5 - branch-tolerant reads, and fail closed on undecryptable
cargo test -p kasirmu-core --test credential_storage_form   # 28 passed, 1 ignored
cargo test -p kasirmu-crypto                                # 21 passed
cargo test -p platform-core                                 # 410 passed

# C8 S1 - atomic, verified, 3-generation backups
cargo test -p kasirmu-core recovery                         # 5 passed
cargo test -p kasirmu-core --test backup_restore_integration # 21 passed

# C6 + C6b - one timezone contract on the read path, validation on the write path
cargo test -p kasirmu-core datetime                         # 8 passed
cargo test -p kasirmu-core --lib reports::tests             # 80 passed
cargo test -p kasirmu-core provisioning                     # 25 passed

# C12 - read-only stock variance report
cargo test -p kasirmu-core stock_variance                   # 6 passed

# C5 + C5b - completed sales only, one day definition, on BOTH shells
cargo test -p kasirmu-core sales                            # 177 passed
cargo test -p kasirmu-bridge history                        # 17 passed
cargo test -p kasirmu-mobile history                        # 19 passed
cargo test -p kasirmu-mobile known_hazard                   # 2 passed (both pins inverted)

# C17 slice 1 (+2 in flight) - gate-debt ceilings, which may only fall
cargo test -p kasirmu-mobile registration_gate              # 10 passed, all four drift pins

# C27 - the shipped container routes every licence-server namespace
node scripts/check-unified-routes.mjs                       # EXIT 0, 7 prefixes

# C9 - no package may be publishable in a proprietary repository
cargo metadata --no-deps --format-version 1                 # expect 0 packages with publish enabled
grep -n qris-core deny.toml                                 # expect a clarify entry
cargo deny check licenses                                   # expect 'licenses ok'
```

A tick without a command in the log is not a tick. If a command here fails, the item it belongs to is NOT done, whatever its checkbox says.

## Verification log

Fill one row per ticked item. An item is not done until the command and its result are here.

| Item | Date | Command | Result | Commit |
|---|---|---|---|---|
| C1 | | | | |
| C2 | | | | |
| C3 | | | | |
| C4 | | | | |
| C5 | | | | |
| C6 | | | | |
| C7 | | | | |
| C8 | | | | |
| C9 | 2026-09-23 | cargo metadata --no-deps --format-version 1 | 39 packages, **0 with publish enabled**; qris-core now license = SEE LICENSE IN LICENSE, publish = [], repository = the real org | 57e0f8c84 |
| C9 | 2026-09-23 | grep -n qris-core deny.toml | deny.toml:273 name = "qris-core" (clarify entry present) | 57e0f8c84 |
| C9 | 2026-09-23 | cargo deny check licenses | exit 0 - licenses ok (cargo-deny 0.19.8 installed, so a real pass not a skip) | 57e0f8c84 |
| C27 | 2026-09-23 | node scripts/check-unified-routes.mjs (before) | EXIT 1 - /api/v1/pairing/* not carved out | - |
| C27 | 2026-09-23 | node scripts/check-unified-routes.mjs (after) | EXIT 0 - 7 prefixes (admin, desktop, license, midtrans, paddle, pairing, web) all -> pocketbase | 65f69112f |
| C27 | 2026-09-23 | non-vacuity: delete the new midtrans handle, re-run | EXIT 1 naming /api/v1/midtrans/* - proves the widened parser sees constant-declared routes | 65f69112f |
| C1 S1 | 2026-09-23 | cargo test -p kasirmu-core --test credential_storage_form | 28 passed, 0 failed, 1 ignored (EXIT 0) | 012dd2663 |
| C1 S1 | 2026-09-23 | cargo test -p kasirmu-crypto | 21 passed, 0 failed (EXIT 0) | 012dd2663 |
| C1 S1 | 2026-09-23 | fails-before proof (lib.rs reverted at HEAD, new test in place) | 27 passed / 1 FAILED on decision_pin_reads_are_branch_tolerant, then lib.rs restored byte-for-byte | 012dd2663 |
| C18 P1.1 | 2026-09-23 | (dispatched) acceptance is a CONFLICT-path test, not a happy path | pending | - |
| C6 | 2026-09-23 | cargo test -p kasirmu-core datetime | 8 passed, 0 failed (EXIT 0) | 7559a3f6b |
| C6 | 2026-09-23 | cargo test -p kasirmu-core --lib reports::tests | 80 passed, 0 failed - includes the INVERTED test iana_timezone_names_resolve_to_the_store_local_day | 7559a3f6b |
| C11 | 2026-09-23 | cargo test -p platform-core migrations | 39 passed, 0 failed, 370 filtered (EXIT 0) | 9c6d76e1 |
| C12 | 2026-09-23 | cargo test -p kasirmu-core stock_variance | 6 passed, 0 failed (EXIT 0) | bdaffa47 |
| C17 slice 1 | 2026-09-23 | cargo test -p kasirmu-mobile registration_gate | 4 drift pins ok (ceilings_only_shrink, three_way_partition, registration_floor, generated_ledger_is_the_sweeps_own_output) - ceilings 92 | 3aa5af0e |
| C5 | 2026-09-23 | cargo test -p kasirmu-core sales + cargo test -p kasirmu-bridge history | core 177 passed; bridge 17 passed incl. the new eod_report_reconciles_on_a_refund_and_void_fixture | 743f222f |
| C2 step 1 | 2026-09-23 | cargo test -p kasirmu-plugin | 181 passed, 0 failed (+2 doctests) | 28a2bfeb |
| C2 step 1 | 2026-09-23 | cargo test -p kasirmu-app --lib state::tests | 11 passed incl. plugin_change_is_refused_and_live_set_survives | 28a2bfeb |
| C8 S1 | 2026-09-23 | cargo test -p kasirmu-core recovery | 5 passed, 0 failed | 6814222 |
| C8 S1 | 2026-09-23 | cargo test -p kasirmu-core --test backup_restore_integration | 21 passed, 0 failed - the deletion did not break the RUST-03 directory-target pins | 6814222 |
| C5b | 2026-09-23 | cargo test -p kasirmu-mobile history | 19 passed, 0 failed (EXIT 0) | 1eee8e0f |
| C5b | 2026-09-23 | cargo test -p kasirmu-mobile known_hazard | 2 passed - both pins inverted in place, names unchanged, both now asserting the CORRECTED behaviour | 1eee8e0f |
| C6b | 2026-09-23 | cargo test -p kasirmu-core provisioning | 25 passed, 0 failed (was 22) - includes an_unsupported_timezone_is_rejected_and_names_the_accepted_values | 1fea7d78 |
| C13 | 2026-09-23 | cargo test -p kasirmu-core db::sales + concurrent_complete_sale | 158 passed and the concurrency test passed, after the plugin-discount gate, the bounded rate_bps and the shortfall-door override symmetry landed | 1b7bd246 |
| C1 S1.5 | 2026-09-23 | cargo test -p platform-core | 410 passed, 0 failed (+4 doctests) - five new fail_closed_* tests, five legacy_plaintext_* still passing | 37a3c00b |
| C18 P1.2 | 2026-09-23 | cargo test -p kasirmu-core cash_payout | 34 passed, 0 failed - includes payout_refused_when_shift_closes_between_read_and_write, which was PROVED to fail against the unfixed SQL before shipping | b0e78e69 |
| C10 | | | | |
| C11 | | | | |
| C12 | | | | |

Wave 2 and wave 3 items append below as they are ticked.
